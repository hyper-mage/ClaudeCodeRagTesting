---
phase: "08"
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_demo_bootstrap.py
  - backend/tests/test_anon_cleanup.py
  - backend/tests/test_auth_anon.py
  - backend/tests/test_chat_retry.py
  - data/sample-private-docs/dnd5e-quickref.md
  - docs/CREDITS.md
autonomous: false
requirements:
  - PORT-01
  - PORT-02
user_setup:
  - service: supabase-prod
    why: "Enable anonymous sign-ins toggle and empirically capture an anon JWT to verify the `aud` claim (RESEARCH Pitfall 1)."
    dashboard_config:
      - task: "Enable Anonymous Sign-Ins in prod Supabase Auth > Providers panel."
        location: "Supabase Dashboard → Project (boardgame-rag-prod) → Authentication → Providers → Anonymous Sign-Ins (toggle ON)."
      - task: "Mint one anon JWT in prod-supabase-anon-key browser console to decode the `aud` claim (Pitfall 1 verification recipe)."
        location: "Open https://boardgame-rag-prod.pages.dev → DevTools console → paste `(await supabase.auth.signInAnonymously()).data.session.access_token` → decode payload at jwt.io → record `aud` and `role` claim values."

must_haves:
  truths:
    - "Anon sign-ins are enabled on prod Supabase (toggle ON) [D-01]"
    - "Anon JWT `aud` claim value is empirically known; auth.py decode policy is decided (keep `authenticated` OR widen to list) [RESEARCH Pitfall 1, T-08-01]"
    - "All 4 Wave-0 test files exist as collectable pytest modules (empty test bodies marked xfail/skip allowed)"
    - "Conftest shared fixtures `anon_jwt`, `permanent_jwt`, `seed_sample_doc_path` exist for downstream waves to consume"
    - "`data/sample-private-docs/dnd5e-quickref.md` exists, is ≤2 MB, contains CC-BY 4.0 attribution block (D-02)"
    - "`docs/CREDITS.md` exists with SRD 5.1 + lucide-react MIT attributions (D-02)"
  artifacts:
    - path: "backend/tests/test_demo_bootstrap.py"
      provides: "Stub: collectable pytest module for bootstrap endpoint tests"
      contains: "def test_sample_doc_file_exists"
    - path: "backend/tests/test_anon_cleanup.py"
      provides: "Stub: collectable pytest module for purge_stale_anon_users tests"
      contains: "import pytest"
    - path: "backend/tests/test_auth_anon.py"
      provides: "Stub: collectable pytest module for anon JWT acceptance tests"
      contains: "import pytest"
    - path: "backend/tests/test_chat_retry.py"
      provides: "Stub: collectable pytest module for retry deduplication tests"
      contains: "import pytest"
    - path: "data/sample-private-docs/dnd5e-quickref.md"
      provides: "Anon-user sample private PDF (markdown form — Docling ingests .md natively)"
      contains: "Creative Commons Attribution 4.0"
    - path: "docs/CREDITS.md"
      provides: "License attribution for D&D 5e SRD + lucide-react + third-party assets"
      contains: "SRD 5.1"
    - path: "backend/tests/conftest.py"
      provides: "Shared fixtures: anon_jwt, permanent_jwt, seed_sample_doc_path"
      exports: ["anon_jwt", "permanent_jwt", "seed_sample_doc_path"]
  key_links:
    - from: "downstream plans 08-01..08-04"
      to: "backend/tests/conftest.py"
      via: "pytest fixture autodiscovery"
      pattern: "@pytest.fixture\\s+def (anon_jwt|permanent_jwt|seed_sample_doc_path)"
    - from: "backend/services/demo_service.py (Plan 08-02)"
      to: "data/sample-private-docs/dnd5e-quickref.md"
      via: "SAMPLE_DOC_PATH constant"
      pattern: "dnd5e-quickref\\.md"
---

<objective>
Wave 0 scaffolding for Phase 8. Lands three load-bearing prerequisites that downstream waves all depend on:

1. **Empirical anon-JWT `aud` claim verification** (RESEARCH Pitfall 1, T-08-01) — the single most likely deploy-breaker for PORT-01. Must be resolved BEFORE Plan 08-01 touches `backend/auth.py`.
2. **Pytest scaffolding** — 4 new test files + conftest fixtures so downstream waves write tests, not test infrastructure.
3. **Sample doc + credits** — the D&D 5e quick-reference markdown that the bootstrap seeder will ingest (Plan 08-02), plus the CC-BY 4.0 attribution per D-02.

Purpose: De-risk Pitfall 1 + create deterministic verification surface for Waves 1–3.
Output: 4 pytest stubs + 1 conftest extension + 1 sample doc + 1 credits file + 1 documented decision on `auth.py` widening.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-portfolio-polish/08-CONTEXT.md
@.planning/phases/08-portfolio-polish/08-RESEARCH.md
@.planning/phases/08-portfolio-polish/08-PATTERNS.md
@.planning/phases/08-portfolio-polish/08-VALIDATION.md
@backend/tests/conftest.py
@backend/tests/test_health.py
@backend/tests/test_rate_limit.py
@backend/tests/test_chat_cap.py
@backend/tests/test_seed_default_kb.py
@backend/auth.py

<interfaces>
<!-- Existing pytest conftest fixtures (from backend/tests/conftest.py — verified) -->
<!-- These already exist and MUST NOT be redefined: -->

```python
# Already in conftest.py:
@pytest.fixture
def mock_user_id() -> str: ...                  # uuid-shaped string
@pytest.fixture
def mock_stream_chat_completion(monkeypatch): ... # patches services.llm_service
```

<!-- NEW fixtures this plan ADDS (do not remove or rename existing ones): -->
- `anon_jwt`             : str — fake anon JWT (payload {"sub": "<uuid>", "aud": "<empirically-verified value>", "role": "authenticated"})
- `permanent_jwt`        : str — fake permanent JWT (payload {"sub": "<uuid>", "aud": "authenticated", "role": "authenticated"})
- `seed_sample_doc_path` : pathlib.Path — absolute path to data/sample-private-docs/dnd5e-quickref.md

<!-- Test mock-db chain shape (from test_health.py, lines 12-24) — reuse, do not invent new helpers -->
```python
def _build_mock_db_chain(...): ...  # mirror, do not duplicate; tests import via local helper
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify anon JWT `aud` claim against prod Supabase (RESEARCH Pitfall 1 — T-08-01)</name>
  <read_first>
    - .planning/phases/08-portfolio-polish/08-RESEARCH.md (§Common Pitfalls — Pitfall 1, lines 697-723)
    - .planning/phases/08-portfolio-polish/08-PATTERNS.md (§backend/auth.py section, lines 555-577)
    - backend/auth.py:38-44 (hard-coded `audience="authenticated"`)
  </read_first>
  <what-built>
    No code yet. This checkpoint asks the developer to:
    1. Toggle ON "Allow anonymous sign-ins" in prod Supabase Dashboard → Authentication → Providers → Anonymous Sign-Ins.
    2. Mint one anon JWT in browser DevTools against the deployed CF Pages URL (https://boardgame-rag-prod.pages.dev) and record the decoded `aud` + `role` claim values.

    The verification recipe is the 3-line snippet in RESEARCH Pitfall 1:
    ```
    const { data } = await supabase.auth.signInAnonymously()
    console.log(JSON.parse(atob(data.session.access_token.split('.')[1])))
    ```
  </what-built>
  <how-to-verify>
    1. Confirm Supabase Dashboard → Authentication → Providers shows "Anonymous Sign-Ins = Enabled".
    2. In a fresh incognito tab, open https://boardgame-rag-prod.pages.dev → open DevTools console → paste the snippet above.
    3. Copy the decoded JSON payload — specifically the `aud` field and the `role` field — and report them in the resume-signal.
    4. Record decision in this plan's SUMMARY:
       - If `aud === "authenticated"` → no `backend/auth.py` change needed for Plan 08-01. Mark Pitfall 1 as resolved.
       - If `aud === "anon"` or any other value → Plan 08-01 MUST widen `audience` to `["authenticated", "anon"]` per RESEARCH Pitfall 7 (PyJWT list-audience support).

    No code changes in this task. The decision is the deliverable; it gates Plan 08-01 Task 1.
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint task; resume-signal records the empirical `aud` value the planner used to gate Plan 08-01. Plan 08-01 Task 1 has the actual code verification.</automated>
  </verify>
  <done>Developer reports `aud` and `role` claim values from a freshly-minted anon JWT against prod Supabase. Anon sign-ins toggle is ON. Decision recorded: "Plan 08-01 will widen audience" OR "Plan 08-01 keeps audience='authenticated' as-is".</done>
  <resume-signal>Paste the decoded JWT payload (or just the `aud`/`role` claim values) and one of: "keep audience=authenticated" / "widen audience to list".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author D&amp;D 5e SRD quick-reference markdown + credits attribution (D-02)</name>
  <files>data/sample-private-docs/dnd5e-quickref.md, docs/CREDITS.md</files>
  <read_first>
    - .planning/phases/08-portfolio-polish/08-CONTEXT.md (D-02 — Starter content; lines 35-37)
    - .planning/phases/08-portfolio-polish/08-RESEARCH.md (§Code Examples Example 7 — CC-BY 4.0 boilerplate, lines 875-885)
    - .planning/phases/08-portfolio-polish/08-PATTERNS.md (§dnd5e-quickref.md section, lines 714-722)
    - data/default-kb/catan.md (analog: existing seeded markdown — confirm front-matter shape and section structure)
  </read_first>
  <action>
    Create `data/sample-private-docs/` directory if absent. Author `dnd5e-quickref.md` — a hand-written quick-reference cheat sheet (target ≤500 lines, ≤2 MB) derived from D&D 5e SRD 5.1 content. Required sections: Ability Checks · Saving Throws · Advantage &amp; Disadvantage · Combat Actions (Attack, Cast, Dash, Disengage, Dodge, Help, Hide, Ready, Search, Use Object) · Conditions (Blinded, Charmed, Deafened, Frightened, Grappled, Incapacitated, Invisible, Paralyzed, Petrified, Poisoned, Prone, Restrained, Stunned, Unconscious, Exhaustion). Prepend the CC-BY 4.0 attribution block verbatim from RESEARCH Example 7 (top of file, before the first H2). Append the same attribution as a footer block (one line, italicized) so the SRD provenance is captured at both ends. Markdown format only — do NOT generate a PDF (RESEARCH §Don't Hand-Roll: Docling ingests .md natively).

    Create `docs/` directory if absent. Author `docs/CREDITS.md` with three sections: (a) "D&D 5e SRD 5.1" — same CC-BY 4.0 attribution block, (b) "Icons — lucide-react" — MIT attribution (just `(c) 2022 Lucide Contributors, MIT License`), (c) "Other assets" — placeholder ("None at this time"). File must mention the path `data/sample-private-docs/dnd5e-quickref.md` so reviewers can trace attribution to artifact.
  </action>
  <verify>
    <automated>Bash: `wc -l data/sample-private-docs/dnd5e-quickref.md && stat -c%s data/sample-private-docs/dnd5e-quickref.md && grep -F "Creative Commons Attribution 4.0" data/sample-private-docs/dnd5e-quickref.md && grep -F "Creative Commons Attribution 4.0" docs/CREDITS.md && grep -F "lucide-react" docs/CREDITS.md` — assert file size ≤ 2_097_152 bytes (2 MB), line count ≤ 500, all three greps return non-empty.</automated>
  </verify>
  <done>Both files exist; quick-ref ≤2 MB and ≤500 lines; CC-BY 4.0 attribution appears in both files; CREDITS.md mentions lucide-react MIT; CREDITS.md references the quick-ref path.</done>
  <acceptance_criteria>
    - File `data/sample-private-docs/dnd5e-quickref.md` exists, mime-type `text/markdown` when read.
    - File size of quick-ref ≤ 2_097_152 bytes (2 MB hard cap per D-02).
    - File contains the literal string "Creative Commons Attribution 4.0" exactly once (top or bottom — either acceptable).
    - File contains H2 sections for: `## Ability Checks`, `## Saving Throws`, `## Advantage`, `## Combat Actions`, `## Conditions` (case-insensitive substring match on `## ` prefix).
    - File `docs/CREDITS.md` exists; contains "SRD 5.1" + "lucide-react" + "data/sample-private-docs/dnd5e-quickref.md" substrings.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Pytest scaffolding — 4 test stubs + conftest fixtures</name>
  <files>backend/tests/conftest.py, backend/tests/test_demo_bootstrap.py, backend/tests/test_anon_cleanup.py, backend/tests/test_auth_anon.py, backend/tests/test_chat_retry.py</files>
  <read_first>
    - backend/tests/conftest.py (existing fixtures — do NOT redefine `mock_user_id` or `mock_stream_chat_completion`)
    - backend/tests/test_health.py:12-24 (the `_build_mock_db_chain` analog the new tests will mirror in Waves 1-2)
    - backend/tests/test_rate_limit.py:44-108 (FastAPI stand-in app pattern for `test_auth_anon.py`)
    - backend/tests/test_chat_cap.py:32-90 (TestClient + monkeypatched `get_supabase` pattern for `test_chat_retry.py`)
    - .planning/phases/08-portfolio-polish/08-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - .planning/phases/08-portfolio-polish/08-PATTERNS.md (§ Test mock-db chain pattern + § Test stand-in FastAPI app pattern, lines 836-844)
  </read_first>
  <action>
    Extend `backend/tests/conftest.py` (do not overwrite — append after existing fixtures) with three new module-scoped pytest fixtures:
    - `anon_jwt` returns a string-shaped fake JWT (header.payload.signature) with payload {"sub": "<uuid4>", "aud": "<empirically-verified-from-task-1>", "role": "authenticated", "is_anonymous": True}. Use base64url-encode of stringified JSON for header + payload; signature is a fixed dummy string. Comment cites Task 1 resume signal as the source of truth for `aud`.
    - `permanent_jwt` returns the same shape but {"sub": "<uuid4>", "aud": "authenticated", "role": "authenticated", "is_anonymous": False}.
    - `seed_sample_doc_path` returns `pathlib.Path` resolved to `<repo_root>/data/sample-private-docs/dnd5e-quickref.md`. Use `__file__`-relative resolution (mirror existing relative-path pattern in `backend/scripts/seed_default_kb.py`).

    Create 4 stub test files. Each MUST:
    - Import `pytest`.
    - Include exactly ONE marker test that pytest can collect: `def test_module_collectable(): assert True`.
    - Include a top-of-file docstring listing the test plan (which test functions Wave 1-2 will add: e.g. `test_seed_idempotent`, `test_seed_skips_permanent_user`, `test_purge_filters_correctly`, etc.).
    - Mark the file's Wave-1/2 tests as `pytest.mark.skip(reason="Wave N stub — implemented in Plan 08-XX")` placeholders so the suite stays green.

    Concrete stub function names per RESEARCH §Phase Requirements → Test Map (lines 969-989):
    - `test_demo_bootstrap.py`: `test_sample_doc_file_exists` (REAL test using `seed_sample_doc_path` fixture — verify file exists, byte count), `test_seed_idempotent`, `test_seed_skips_permanent_user`, `test_bootstrap_endpoint_calls_seed_and_schedules_purge`. Last three are `@pytest.mark.skip` stubs.
    - `test_anon_cleanup.py`: `test_purge_filters_correctly`, `test_cascade_order`, `test_purge_swallows_per_user_errors`. All `@pytest.mark.skip` stubs.
    - `test_auth_anon.py`: `test_anon_jwt_accepted_by_get_user_id`, `test_permanent_jwt_still_accepted`. Both `@pytest.mark.skip` stubs (Plan 08-01 implements).
    - `test_chat_retry.py`: `test_retry_deletes_prior_failed_assistant_row`, `test_non_retry_path_unchanged`. Both `@pytest.mark.skip` stubs (Plan 08-03 implements).

    Do NOT import internal modules (`from services.demo_service import ...`) in stubs — those modules don't exist yet. Import only inside `@pytest.mark.skip` decorated functions so collection succeeds.
  </action>
  <verify>
    <automated>`cd backend && venv/Scripts/python -m pytest tests/test_demo_bootstrap.py tests/test_anon_cleanup.py tests/test_auth_anon.py tests/test_chat_retry.py --collect-only -q` — must collect ≥10 tests across the 4 files with zero collection errors. Then `cd backend && venv/Scripts/python -m pytest tests/test_demo_bootstrap.py::test_sample_doc_file_exists -x` — must PASS (proves Task 2's sample doc is reachable via the fixture).</automated>
  </verify>
  <done>All 4 stub files exist and collect without ImportError; sample-doc existence test PASSES; remaining tests are skipped placeholders; conftest exposes `anon_jwt`/`permanent_jwt`/`seed_sample_doc_path`.</done>
  <acceptance_criteria>
    - `pytest --collect-only tests/test_demo_bootstrap.py tests/test_anon_cleanup.py tests/test_auth_anon.py tests/test_chat_retry.py` exits 0.
    - `pytest tests/test_demo_bootstrap.py::test_sample_doc_file_exists -x` PASSES.
    - `grep -c "def anon_jwt" tests/conftest.py` ≥ 1.
    - `grep -c "def permanent_jwt" tests/conftest.py` ≥ 1.
    - `grep -c "def seed_sample_doc_path" tests/conftest.py` ≥ 1.
    - No new pytest collection errors introduced in the suite (`pytest --collect-only -q` exits 0 across the whole tests/ tree).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures | Untrusted JWT shapes constructed for unit tests must never leak into runtime — fixtures are test-scope only. |
| Sample doc content | CC-BY 4.0 source material; ingested via standard pipeline at runtime — no untrusted-input boundary inside Wave 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | `backend/auth.py` JWT verifier (Pitfall 1 — anon JWT `aud` claim) | mitigate | Task 1 empirically captures the real `aud` claim against prod Supabase; Plan 08-01 either keeps `audience="authenticated"` (if claim matches) or widens to `audience=["authenticated","anon"]` (per PyJWT list support). Decision is documented in this plan's SUMMARY before Plan 08-01 lands. |
| T-08-W0-LIC | Tampering / Compliance | `data/sample-private-docs/dnd5e-quickref.md` (CC-BY 4.0 attribution) | mitigate | Task 2 prepends + appends the verbatim CC-BY 4.0 attribution block from RESEARCH Example 7 and adds the same block to `docs/CREDITS.md` so the SRD provenance is captured at both the artifact and repo level. |
</threat_model>

<verification>
- Anon sign-ins toggle ON in prod Supabase (manual check via Task 1).
- `aud` claim value recorded in Task 1 SUMMARY; downstream Plan 08-01 cites it.
- `pytest --collect-only tests/test_demo_bootstrap.py tests/test_anon_cleanup.py tests/test_auth_anon.py tests/test_chat_retry.py` exits 0.
- `pytest tests/test_demo_bootstrap.py::test_sample_doc_file_exists -x` PASSES.
- `wc -c data/sample-private-docs/dnd5e-quickref.md` ≤ 2_097_152.
- `grep -q "Creative Commons Attribution 4.0" data/sample-private-docs/dnd5e-quickref.md docs/CREDITS.md`.
</verification>

<success_criteria>
- Pitfall 1 resolved: empirical `aud` claim value known, decision recorded.
- 4 pytest stubs exist + collect; one real assertion (sample-doc presence) PASSES.
- Sample doc + credits exist with CC-BY 4.0 attribution; sample doc ≤ 2 MB and ≤ 500 lines.
- Downstream plans (08-01..08-04) can import the new conftest fixtures without redefining them.
</success_criteria>

<output>
After completion, create `.planning/phases/08-portfolio-polish/08-00-SUMMARY.md` capturing:
- The decoded JWT payload from Task 1 (specifically `aud` + `role` values).
- The decision for Plan 08-01 (`auth.py` widening yes/no).
- Sample-doc byte count + line count.
- Pytest collection output (proof of 10+ collected tests, 0 errors).
</output>
