---
phase: "08"
plan: "05"
type: execute
wave: 3
depends_on: ["08-04", "08-06", "08-07"]
files_modified:
  - README.md
  - docs/MASTERCLASS.md
autonomous: false
requirements:
  - PORT-03

must_haves:
  truths:
    - "Existing course README is preserved at docs/MASTERCLASS.md before README.md is overwritten (D-10)"
    - "New repo-root README.md is a full rewrite focused on portfolio pitch (D-10)"
    - "README sections appear in the locked order from D-11 (title+pitch → live demo → badges → hero GIF → what it does → tech tables → architecture → screenshots → deploy sequence → masterclass link)"
    - "Two tech tables present per D-13: Code Stack (Tech | Role) + Services / Infrastructure (Service | Link | What it does | How this project uses it)"
    - "Services table answers the developer's verbatim ask: linked rows for Cloudflare Pages, Fly.io, Supabase, OpenRouter, Sentry, LangSmith, UptimeRobot, GitHub, Docling"
    - "Live demo section includes the deployed CF Pages URL + a 'Try the demo (no signup)' callout citing D-01 anon-auth UX"
    - "Deploy command sequence documented: `docker build` → `flyctl deploy` → CF Pages push (matches D-11 #9)"
    - "Architecture diagram asset (docs/architecture.png) is referenced via relative markdown image link (depends on Plan 08-06)"
    - "Hero GIF (docs/hero.gif) and ≥4 screenshots (docs/screenshots/*.png) are referenced via relative markdown image links (depends on Plan 08-06)"
    - "Both badges from Plan 08-07 appear in the Badges row (D-15)"
    - "README links to docs/MASTERCLASS.md for course backstory"
  artifacts:
    - path: "README.md"
      provides: "Portfolio-grade landing page for the repo"
      contains: "## Live demo"
      contains_also: "Try the demo"
    - path: "docs/MASTERCLASS.md"
      provides: "Archived copy of the original AI Automators course README"
      contains: "Cloud Code Agentic RAG Masterclass"
  key_links:
    - from: "README.md hero section"
      to: "docs/hero.gif"
      via: "relative markdown image link"
      pattern: "!\\[.*\\]\\(docs/hero\\.gif\\)"
    - from: "README.md architecture section"
      to: "docs/architecture.png"
      via: "relative markdown image link"
      pattern: "!\\[.*\\]\\(docs/architecture\\.png\\)"
    - from: "README.md badges row"
      to: "shields.io / uptimerobot.com"
      via: "external badge URL"
      pattern: "!\\[.*\\]\\(https://"
    - from: "README.md masterclass link"
      to: "docs/MASTERCLASS.md"
      via: "relative markdown link"
      pattern: "\\(docs/MASTERCLASS\\.md\\)"
---

<objective>
Deliver PORT-03 README portion: full portfolio rewrite of `README.md` at repo root, with the existing course README archived to `docs/MASTERCLASS.md` (D-10). The new README pitches the project to reviewers, links the live demo, displays badges + hero GIF, lists the two tech tables (D-13 — including the developer's verbatim ask for linked services with what/how-used columns), embeds the architecture diagram from Plan 08-06, shows screenshots from Plan 08-06, documents the deploy command sequence, and links back to the course README.

Out of scope (other plans): architecture diagram asset (08-06), screenshots/GIF (08-06), badge URLs (08-07).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-portfolio-polish/08-CONTEXT.md
@.planning/phases/08-portfolio-polish/08-RESEARCH.md
@.planning/phases/08-portfolio-polish/08-PATTERNS.md
@.planning/phases/08-portfolio-polish/08-UI-SPEC.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Archive existing course README to docs/MASTERCLASS.md (D-10)</name>
  <files>docs/MASTERCLASS.md, README.md</files>
  <read_first>
    - README.md (current course/masterclass README — full content)
    - .planning/phases/08-portfolio-polish/08-CONTEXT.md (D-10 — "Full portfolio rewrite, archive old README to docs/MASTERCLASS.md")
  </read_first>
  <action>
    Copy the full current content of `README.md` to `docs/MASTERCLASS.md` verbatim. Prepend a single H1 line to MASTERCLASS.md: `# AI Automators Masterclass — Original README`. Below the H1, add an italic line: `*This is the original course README from the AI Automators Claude Code RAG Masterclass. The repo-root [README.md](../README.md) is the portfolio pitch for the live deployment.*` Leave a blank line, then dump the prior README content unchanged. Do NOT delete `README.md` yet — Task 2 overwrites it.
  </action>
  <verify>
    <automated>`test -f docs/MASTERCLASS.md && head -1 docs/MASTERCLASS.md | grep -F "AI Automators Masterclass" && grep -F "Cloud Code Agentic RAG Masterclass" docs/MASTERCLASS.md && grep -F "(../README.md)" docs/MASTERCLASS.md`</automated>
  </verify>
  <done>`docs/MASTERCLASS.md` exists; H1 + back-link present; full prior course content preserved verbatim below.</done>
  <acceptance_criteria>
    - `docs/MASTERCLASS.md` exists.
    - First line is exactly `# AI Automators Masterclass — Original README`.
    - File contains the literal string `Cloud Code Agentic RAG Masterclass` (proves the original H1 / pitch line was preserved).
    - File contains relative link `(../README.md)`.
    - Byte count is within 100 bytes of the prior README.md size + ~250 bytes of the prepended header (sanity check that nothing was truncated).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite README.md as portfolio landing page (D-10, D-11, D-13)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/08-portfolio-polish/08-CONTEXT.md (D-10 disposition, D-11 section order, D-13 two tech tables, specifics block — developer's verbatim service-table ask)
    - .planning/phases/08-portfolio-polish/08-RESEARCH.md (§Tech stack + Services breakdown, badge URL patterns)
    - .planning/phases/08-portfolio-polish/08-07-PLAN.md (badge URL outputs from Plan 08-07 — needed for the Badges row)
    - .planning/phases/08-portfolio-polish/08-06-PLAN.md (architecture diagram path + screenshot inventory from Plan 08-06)
    - .planning/codebase/ARCHITECTURE.md (data flow + layer summary — source material for the "what it does" pitch)
    - .planning/codebase/STACK.md (canonical tech versions for Code Stack table)
    - .planning/STATE.md (deployed URLs: prod CF Pages + Fly)
  </read_first>
  <action>
    Overwrite `README.md` at repo root with a full portfolio rewrite. Follow D-11 section order exactly:

    1. **Title + one-line pitch.** H1: `# Board Game Knowledge Base RAG`. Sub-line: `Agentic RAG over a curated board-game corpus — search rules, compare mechanics, recommend games. Built in public as a portfolio piece.`
    2. **Live demo block.** H2: `## Live demo`. Show the prod CF Pages URL as a clickable link + bold "Try the demo — no signup required". One-line callout: `Click "Try the demo" on the login page. Anonymous Supabase session, 7-day cleanup, isolated by RLS.` Cite the cross-genre data point: `Bundled with the public board-game KB and a D&D 5e quick-reference cheat sheet so you can ask about Catan rules and an advantage roll in the same conversation.`
    3. **Badges row.** Two markdown image links side by side: the UptimeRobot uptime-ratio badge and the last-deploy badge. Use the exact URLs from `08-07-SUMMARY.md` (after Plan 08-07 runs) — leave a single comment line `<!-- badges sourced from 08-07-SUMMARY.md -->` directly above the row.
    4. **Hero GIF.** `![Demo of the chat agent answering across the public KB and a private upload](docs/hero.gif)`.
    5. **What it does.** H2: `## What it does`. Three to six tight bullets pulled from CONTEXT.md domain block + ARCHITECTURE.md data flow — chat over public KB, anon demo with seeded thread + sample doc, agentic tool loop (retrieval / SQL / web search / subagent), hybrid search + RRF + reranking, mobile-first UX.
    6. **Tech tables.** H2: `## Tech stack`. Sub-H3 `### Code stack` with a 2-column table `| Tech | Role |` covering React 19 + TypeScript 5.9 + Tailwind 4 + Vite 6 + FastAPI 0.115 + Python 3.11 + pgvector + Docling + OpenAI SDK 1.74. Sub-H3 `### Services & infrastructure` with a 4-column table `| Service | Link | What it does | How this project uses it |` covering Cloudflare Pages, Fly.io, Supabase, OpenRouter, Sentry, LangSmith, UptimeRobot, GitHub, Docling. Link cells are markdown links to the official product page for each (e.g. `[Cloudflare Pages](https://pages.cloudflare.com)`). Every "How this project uses it" cell is one short sentence pinned to the actual usage in this repo. This table answers the developer's verbatim CONTEXT.md ask — do not abbreviate.
    7. **Architecture.** H2: `## Architecture`. Embed: `![Architecture diagram showing React SPA → CF Pages CDN → Fly backend → Supabase + OpenRouter + observability stack](docs/architecture.png)`. Below the image, three to five sentences explaining the data flow (stateless completions, RLS isolation, agentic tool loop, observability fan-out).
    8. **Screenshots gallery.** H2: `## Screenshots`. Inline 4 screenshots from `docs/screenshots/` produced by Plan 08-06: login with Try-demo CTA, chat with tool-call cards expanded, documents page mid-upload, mobile drawer open over chat.
    9. **Deploy command sequence.** H2: `## Deploy`. Three numbered subsections: `### 1. Backend container` (the `docker build .` + smoke-test recipe from Phase 2), `### 2. Backend to Fly` (`flyctl deploy` + reference to Phase 4 secrets contract), `### 3. Frontend to Cloudflare Pages` (push-to-deploy via repo connection + reference to Phase 5 `_redirects`). Each subsection is short — link out to the relevant `.planning/phases/0N-*/0N-SUMMARY.md` for full detail.
    10. **Built as.** H2: `## Built as`. One paragraph: `Capstone for the AI Automators Claude Code Masterclass — the full course README is preserved at [docs/MASTERCLASS.md](docs/MASTERCLASS.md).` Add a one-line license/attribution pointer to `docs/CREDITS.md`.

    Hard rules:
    - No emojis (project convention — see CLAUDE.md / global rules).
    - No fenced code blocks except the deploy command lines (`docker build`, `flyctl deploy`).
    - Tech-table cells stay terse (one short sentence per `How this project uses it` cell).
    - Live URLs and badge URLs MUST be the real URLs from `.planning/STATE.md` and `08-07-SUMMARY.md` — placeholders like `<your-url-here>` are not acceptable.
    - File length target ≤ 250 lines (a portfolio README is a pitch, not a manual).
  </action>
  <verify>
    <automated>`bash -c 'grep -F "# Board Game Knowledge Base RAG" README.md && grep -F "## Live demo" README.md && grep -F "## Tech stack" README.md && grep -F "### Code stack" README.md && grep -F "### Services" README.md && grep -F "## Architecture" README.md && grep -F "## Screenshots" README.md && grep -F "## Deploy" README.md && grep -F "docs/MASTERCLASS.md" README.md && grep -F "docs/architecture.png" README.md && grep -F "docs/hero.gif" README.md && grep -F "Try the demo" README.md && grep -F "Cloudflare Pages" README.md && grep -F "Fly.io" README.md && grep -F "Supabase" README.md && grep -F "OpenRouter" README.md && grep -F "Sentry" README.md && grep -F "LangSmith" README.md && grep -F "UptimeRobot" README.md && grep -F "Docling" README.md && [ $(wc -l < README.md) -le 250 ]'`</automated>
  </verify>
  <done>All 20 substring greps return a match; README is ≤250 lines; the file renders without raw HTML/code-fence artifacts when previewed in GitHub markdown.</done>
  <acceptance_criteria>
    - File `README.md` is overwritten (prior course content is no longer at root — Task 1 archived it).
    - All sections from D-11 are present in the locked order (verified by grep order check: `Live demo` line number < `Tech stack` line number < `Architecture` line number < `Deploy` line number).
    - Services table contains all 9 mandatory rows (Cloudflare Pages, Fly.io, Supabase, OpenRouter, Sentry, LangSmith, UptimeRobot, GitHub, Docling) with markdown links and 4 columns.
    - Hero GIF reference resolves to `docs/hero.gif` and the file exists (depends on Plan 08-06).
    - Architecture image reference resolves to `docs/architecture.png` and the file exists (depends on Plan 08-06).
    - Both badges from Plan 08-07 are present (verified by `grep -F` against the badge URL strings recorded in `08-07-SUMMARY.md`).
    - No emojis in file (`grep -P "[\x{1F300}-\x{1FAFF}]" README.md` returns nothing).
    - Total length ≤ 250 lines.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: GitHub render check + portfolio-reviewer cold-read</name>
  <read_first>
    - README.md (the rewritten file from Task 2)
    - docs/MASTERCLASS.md (the archived course README from Task 1)
  </read_first>
  <what-built>
    No code change. This checkpoint asks the developer to push the new README + docs/ assets to the GitHub default branch and visually confirm the rendered output on github.com (markdown rendering differs from local preview, especially for tables, badges, and image scaling).
  </what-built>
  <how-to-verify>
    1. Commit + push README.md + docs/MASTERCLASS.md to the default branch.
    2. Open the repo URL on github.com in a fresh incognito window — confirm: hero GIF plays inline, architecture diagram renders, all 4 screenshots load, both badges render with live values (not "invalid"), tech tables render with 4 columns intact, `docs/MASTERCLASS.md` link works.
    3. Read the README cold (or have someone else read it) — confirm a reviewer who has never seen the project can: (a) understand what it does in <30 s, (b) click through to the live demo, (c) understand the deploy story.
    4. If anything fails — broken image, malformed table, missing badge — report it in the resume-signal and Task 4 fixes.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual visual check on github.com; resume-signal records pass/fail per the 4 acceptance items above.</automated>
  </verify>
  <done>Developer reports "PASS — README renders correctly on github.com and reads well cold" OR "FAIL — issues X, Y, Z" with specifics for Task 4.</done>
  <resume-signal>Report: render OK / broken-images / badge-issue / table-issue / cold-read-confusing — and the URL to the GitHub repo page so the resume context can audit it.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix any issues reported in Task 3 (CONDITIONAL — skip if PASS)</name>
  <files>README.md (conditional)</files>
  <read_first>
    - Task 3 resume signal (issue list, if any)
    - README.md
  </read_first>
  <action>
    Only execute if Task 3 reported failures. For each issue in the resume signal, apply the minimum edit needed to fix render/cold-read issues. Common fixes: escape pipe characters in tech-table cells, normalize image alt text, swap `<br>` for `&nbsp;` in tables, adjust badge URL casing, rewrite a confusing pitch line. Re-run the Task 2 acceptance greps after each edit. If Task 3 reported PASS, mark this task done with no edits.
  </action>
  <verify>
    <automated>Re-run Task 2's full grep chain; all 20 substring checks must still pass. If Task 3 reported PASS, this task is auto-complete (no edits needed).</automated>
  </verify>
  <done>All Task 3 issues resolved; Task 2 grep chain still green.</done>
  <acceptance_criteria>
    - Either: Task 3 reported PASS and this task is a no-op, OR every issue listed in the Task 3 resume signal has a corresponding edit in `git diff README.md`.
    - Task 2's full acceptance grep chain still passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → external badge SVGs | Browsers fetch shields.io and UptimeRobot SVGs at render time; users can be served stale or attacker-tampered content if the badge host is compromised. Standard accepted risk for the entire badge ecosystem. |
| README.md → docs/ assets | Local repo content; trust boundary = whoever can push to default branch. No new risk over baseline. |
| Live URL exposure in README | Public-facing demo URL was already published as part of Phase 5/6/7; documenting it does not increase attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-SSRF | Information Disclosure | shields.io custom-badge endpoints fetched at render time | accept | shields.io is the standard portfolio badge host; SSRF mitigation is the host's responsibility. README uses only documented public badge endpoints, no `?query=` payloads sourced from untrusted input. |
| T-08-05-STALE | Information Disclosure | README documents prod URL + deploy steps; risk that secrets or URLs become stale | accept | URLs are sourced from `.planning/STATE.md` at write time; portfolio README is expected to be re-edited on each major change. No secret material is ever written to README. |
| T-08-05-PII | Information Disclosure | Screenshots may inadvertently include PII | mitigate | Plan 08-06 captures screenshots from a fresh anon-demo session (no real user data); Task 3 cold-read includes a check for visible PII in any embedded image. |
</threat_model>

<verification>
- README.md renders correctly on github.com (Task 3 manual verify).
- All D-11 section order is preserved (grep line-number ordering check).
- D-13 two tech tables present with required columns.
- D-10 archive: `docs/MASTERCLASS.md` exists with original course content + back-link.
- Hero GIF + architecture diagram + ≥4 screenshots are linked via relative paths and all files exist (depends on Plan 08-06 landing first).
- Both badge URLs from Plan 08-07 are inline in the Badges row.
- README length ≤ 250 lines; no emojis; no broken markdown.
</verification>

<success_criteria>
- Portfolio README is live on the default branch and renders cleanly on github.com.
- A cold reviewer can understand the project + reach the live demo in <30 s.
- Course README backstory preserved at `docs/MASTERCLASS.md`.
- Developer's verbatim service-table ask (CONTEXT.md specifics block) is satisfied with all 9 services + links + what + how-used columns.
- D-10 / D-11 / D-13 acceptance criteria all met.
- Phase 8 success criterion #3 verified.
</success_criteria>

<output>
After completion, create `.planning/phases/08-portfolio-polish/08-05-SUMMARY.md` capturing:
- Final README.md line count.
- Confirmation of all 20 substring greps passing.
- GitHub repo URL where the rendered README can be reviewed.
- Cold-read result from Task 3.
- Any deviations from D-11 section order with justification.
</output>
