---
phase: "08"
plan: "01"
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - backend/auth.py
  - backend/tests/test_auth_anon.py
autonomous: true
requirements:
  - PORT-01

must_haves:
  truths:
    - "Backend `get_user_id` accepts anon JWTs without 401 (T-08-01 mitigated) [D-01]"
    - "Backend `get_user_id` still accepts permanent JWTs unchanged (no regression)"
    - "`test_auth_anon.py::test_anon_jwt_accepted_by_get_user_id` PASSES"
    - "`test_auth_anon.py::test_permanent_jwt_still_accepted` PASSES"
  artifacts:
    - path: "backend/auth.py"
      provides: "JWT verifier that accepts both authenticated and anon JWTs (if widening required by Plan 08-00 Task 1)"
      contains: "audience="
    - path: "backend/tests/test_auth_anon.py"
      provides: "Unit tests proving anon + permanent JWT acceptance"
      exports: ["test_anon_jwt_accepted_by_get_user_id", "test_permanent_jwt_still_accepted"]
  key_links:
    - from: "backend/routers/demo.py (Plan 08-02)"
      to: "backend/auth.py::get_user_id"
      via: "Depends(get_user_id)"
      pattern: "Depends\\(get_user_id\\)"
    - from: "frontend Try-demo CTA (Plan 08-04)"
      to: "backend/auth.py JWT acceptance"
      via: "Authorization: Bearer <anon_jwt> on /api/demo/bootstrap"
      pattern: "Authorization.*Bearer"
---

<objective>
Resolve T-08-01 (anon JWT misverification). Based on the empirical `aud` claim captured in Plan 08-00 Task 1, either:
- **No-op path**: confirm `backend/auth.py:42` `audience="authenticated"` already accepts anon JWTs (resume signal said `aud=="authenticated"`); only land tests that prove it.
- **Widen path**: change `audience="authenticated"` to `audience=["authenticated", "anon"]` per RESEARCH Pitfall 7 (PyJWT list-audience support).

Purpose: Unblock Plan 08-02 (bootstrap endpoint) and the entire anon-user flow. Without this, every anon API call returns 401 and the Try-demo flow is broken end-to-end.
Output: `auth.py` decode policy + 2 passing unit tests in `test_auth_anon.py`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-portfolio-polish/08-00-SUMMARY.md
@.planning/phases/08-portfolio-polish/08-CONTEXT.md
@.planning/phases/08-portfolio-polish/08-RESEARCH.md
@.planning/phases/08-portfolio-polish/08-PATTERNS.md
@backend/auth.py
@backend/tests/conftest.py
@backend/tests/test_rate_limit.py
@backend/tests/test_health.py

<interfaces>
<!-- backend/auth.py — current signature (verified) -->
```python
def get_user_id(
    request: Request, settings: Settings = Depends(get_settings)
) -> str:
    """Verifies Supabase JWT (HS256 legacy / ES256 modern); returns sub claim.
    Raises HTTPException(401) on missing/invalid/expired token.
    Side effect: sets request.state.user_id (slowapi key-func bridge, SEC-04)."""
```

<!-- jwt.decode contract (PyJWT 2.10.1 — verified) -->
```python
jwt.decode(token, key, algorithms=[alg], audience=..., leeway=30)
# audience accepts EITHER a str OR a list[str] — token's `aud` must match at least one entry.
```

<!-- Conftest fixtures from Plan 08-00 (verified) -->
- `anon_jwt`             : str — fake anon JWT with empirically-verified `aud` value
- `permanent_jwt`        : str — fake permanent JWT with `aud="authenticated"`
- `mock_user_id`         : str — existing fixture (do not redefine)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update auth.py JWT decode policy (conditional on Plan 08-00 Task 1 outcome)</name>
  <files>backend/auth.py</files>
  <read_first>
    - .planning/phases/08-portfolio-polish/08-00-SUMMARY.md (the `aud` claim decision from Plan 08-00 Task 1 — MUST be read first; gates this task)
    - backend/auth.py (entire file — only 54 lines; line 42 is the change site)
    - .planning/phases/08-portfolio-polish/08-RESEARCH.md (§Common Pitfalls — Pitfall 1 + Pitfall 7, lines 697-723 + 800-802)
    - .planning/phases/08-portfolio-polish/08-PATTERNS.md (§backend/auth.py section, lines 555-577)
  </read_first>
  <behavior>
    - Test 1: `test_anon_jwt_accepted_by_get_user_id` — patching `jwt.decode` to return the conftest `anon_jwt` payload, `get_user_id` returns the `sub` claim without raising.
    - Test 2: `test_permanent_jwt_still_accepted` — same but with `permanent_jwt`; `get_user_id` returns the `sub` claim without raising.
    - Test 3: `test_invalid_aud_rejected` — patching `jwt.decode` to raise `jwt.InvalidAudienceError`, `get_user_id` raises HTTPException(401).
    - Existing behavior preserved: HS256/ES256 alg branching unchanged; ExpiredSignatureError → 401; InvalidTokenError → 401; missing `sub` claim → 401.
  </behavior>
  <action>
    Read `.planning/phases/08-portfolio-polish/08-00-SUMMARY.md` first to determine which path applies:
    - **If SUMMARY says `aud == "authenticated"` → NO CODE CHANGE to `auth.py`.** Skip directly to Task 2 (which lands the tests proving no regression).
    - **If SUMMARY says `aud == "anon"` or any other non-`"authenticated"` value → WIDEN the `audience` parameter on `backend/auth.py:42` from the string literal `"authenticated"` to the list literal `["authenticated", "anon"]`.** Per RESEARCH Pitfall 7, PyJWT's `jwt.decode(audience=...)` accepts a list — semantics: token's `aud` must match at least one entry. No other changes to `auth.py`. Do NOT add a try/except chain; do NOT introduce new env vars; do NOT touch HS256/ES256 alg branching.

    Add a one-line comment above the `audience=` line citing "Phase 8 D-01 + RESEARCH Pitfall 1 — anon JWT support" so future readers know why the list literal exists.
  </action>
  <verify>
    <automated>`cd backend && venv/Scripts/python -m pytest tests/test_auth_anon.py -x -q` — Task 2's tests must PASS against whichever auth.py policy was chosen.</automated>
  </verify>
  <done>
    `backend/auth.py` `audience=` value matches the decision from Plan 08-00 (either unchanged `"authenticated"` OR widened to `["authenticated", "anon"]`). One-line citation comment present if widened. No other lines changed in `auth.py`.
  </done>
  <acceptance_criteria>
    - `grep -n "audience" backend/auth.py | grep -v "^#"` returns exactly one match.
    - If widened: the match contains both substrings `"authenticated"` AND `"anon"` on the same line.
    - If unchanged: the match contains `"authenticated"` and NOT `"anon"`.
    - File line count unchanged ±2 (only the audience line + optional comment changed).
    - `cd backend && venv/Scripts/python -m pytest tests/ -x -q --ignore=tests/test_e2e_subagent.py` — full backend suite remains green (no regression in `test_health.py`, `test_rate_limit.py`, `test_chat_cap.py`, `test_folders_api.py`, etc.).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement test_auth_anon.py — anon + permanent JWT acceptance tests</name>
  <files>backend/tests/test_auth_anon.py</files>
  <read_first>
    - backend/tests/test_auth_anon.py (existing stub from Plan 08-00 — un-skip the test functions, keep file structure)
    - backend/tests/conftest.py (consume `anon_jwt`, `permanent_jwt`, `mock_user_id` fixtures — do not redefine)
    - backend/tests/test_rate_limit.py:44-108 (FastAPI stand-in app pattern — `_build_minimal_limited_app`)
    - backend/auth.py (the function under test — `get_user_id` signature)
    - .planning/phases/08-portfolio-polish/08-PATTERNS.md (§backend/tests/test_auth_anon.py section, lines 579-606)
  </read_first>
  <behavior>
    - `test_anon_jwt_accepted_by_get_user_id`: patches `auth.jwt.decode` to return an anon payload {"sub": "<uuid>", "aud": "<empirically-verified>", "role": "authenticated", "is_anonymous": True}; calls `get_user_id(request, settings)`; asserts return value equals the `sub`; asserts `request.state.user_id` was set (SEC-04 bridge preserved).
    - `test_permanent_jwt_still_accepted`: same shape with `aud="authenticated"`, `is_anonymous=False`; asserts return value equals `sub`.
    - `test_invalid_aud_rejected`: patches `jwt.decode` to raise `jwt.InvalidTokenError` (parent class of `InvalidAudienceError`); asserts `HTTPException(401)` raised with detail "Invalid token".
    - `test_missing_sub_claim_rejected`: patches `jwt.decode` to return `{"aud": "authenticated"}` (no `sub`); asserts `HTTPException(401)` with detail "Invalid token: no sub claim".
    - All four tests un-skip the placeholder stubs in `test_auth_anon.py` from Plan 08-00.
  </behavior>
  <action>
    Replace the Plan 08-00 `@pytest.mark.skip` stubs with the four real tests above. Use `unittest.mock.patch` to mock `auth.jwt.decode` and `auth.jwt.get_unverified_header` (return `{"alg": "HS256"}` so HS256 branch is exercised — no JWKS roundtrip). Use a minimal `Request` stub: build via `fastapi.Request` with a fake scope dict (mirror `test_rate_limit.py:65-78` if pattern available) OR use `unittest.mock.MagicMock(spec=Request)` with `headers={"Authorization": "Bearer fake.jwt.token"}` and a `state` attribute that supports `.user_id = ...` assignment. Use `get_settings()` directly (or `MagicMock(spec=Settings)` with `supabase_jwt_secret="dummy"` if env not loaded).

    Test file MUST import only stdlib + `pytest` + `fastapi` + `unittest.mock`; the function under test (`auth.get_user_id`) is imported inside each test (mirror `test_health.py` pattern). The `anon_jwt` / `permanent_jwt` fixtures from conftest are used as the string-token argument the request header carries — their decoded payload is what `patch("auth.jwt.decode", return_value=...)` simulates.

    Do NOT make live JWKS network calls. Do NOT depend on prod Supabase. Tests run offline.
  </action>
  <verify>
    <automated>`cd backend && venv/Scripts/python -m pytest tests/test_auth_anon.py -x -v` — all 4 tests PASS; collection time ≤ 1 s; total runtime ≤ 5 s.</automated>
  </verify>
  <done>All 4 tests PASS. No `@pytest.mark.skip` remaining in `test_auth_anon.py`. Tests are fully offline (no JWKS HTTP calls, no live JWT decode).</done>
  <acceptance_criteria>
    - `pytest tests/test_auth_anon.py::test_anon_jwt_accepted_by_get_user_id -x` exits 0.
    - `pytest tests/test_auth_anon.py::test_permanent_jwt_still_accepted -x` exits 0.
    - `pytest tests/test_auth_anon.py::test_invalid_aud_rejected -x` exits 0.
    - `pytest tests/test_auth_anon.py::test_missing_sub_claim_rejected -x` exits 0.
    - `grep -c "pytest.mark.skip" backend/tests/test_auth_anon.py` == 0.
    - `grep -c "import requests\\|httpx\\|urllib" backend/tests/test_auth_anon.py` == 0 (no network deps).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → backend JWT verification | Untrusted JWT bearer tokens arrive at `get_user_id`; verifier must reject forged / wrong-audience / expired tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | `backend/auth.py` JWT verifier (anon JWT `aud` mismatch) | mitigate | Task 1 either confirms current `audience="authenticated"` matches the empirically-captured anon `aud` claim OR widens to `["authenticated", "anon"]` per PyJWT list-audience support. Task 2 lands 4 unit tests covering anon-accepted, permanent-accepted, invalid-aud-rejected, missing-sub-rejected. |
| T-08-01-LEAK | Information Disclosure | JWT verifier error responses | accept | Existing 401 "Invalid token" detail does not leak claim values; unchanged. |
</threat_model>

<verification>
- `pytest tests/test_auth_anon.py -x -q` exits 0 with 4 passing tests.
- `pytest tests/ -x -q --ignore=tests/test_e2e_subagent.py` — full backend suite green (no regression).
- `grep "audience" backend/auth.py` matches the policy decided in Plan 08-00.
</verification>

<success_criteria>
- Anon JWTs flow through `get_user_id` without 401.
- Permanent JWTs continue flowing through `get_user_id` (no regression).
- Decision recorded inline in `auth.py` (one-line citation comment if widened).
- Plan 08-02 can now build `/api/demo/bootstrap` knowing JWT acceptance is solved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-portfolio-polish/08-01-SUMMARY.md` capturing:
- Which path was taken (no-op vs widen) and why (cite Plan 08-00 SUMMARY).
- Test runtime + pass count.
- Confirmation that `pytest tests/ -x -q` exits 0 (no regression).
</output>
