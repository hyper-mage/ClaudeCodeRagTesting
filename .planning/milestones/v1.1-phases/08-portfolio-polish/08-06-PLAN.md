---
phase: "08"
plan: "06"
type: execute
wave: 3
depends_on: ["08-04"]
files_modified:
  - docs/architecture.excalidraw
  - docs/architecture.png
  - docs/screenshots/01-login-try-demo.png
  - docs/screenshots/02-chat-tool-calls.png
  - docs/screenshots/03-documents-upload.png
  - docs/screenshots/04-mobile-drawer.png
  - docs/hero.gif
autonomous: false
requirements:
  - PORT-03

must_haves:
  truths:
    - "Architecture diagram is a PNG/SVG asset authored in Excalidraw with the source `.excalidraw` file committed alongside the export (D-12)"
    - "Architecture diagram nodes include: React SPA, Cloudflare Pages CDN, Fly backend (FastAPI), Supabase (Postgres+pgvector / Auth / Storage / Realtime), OpenRouter LLM, Docling, Sentry, LangSmith, UptimeRobot (per RESEARCH §Architecture diagram nodes)"
    - "At least 4 screenshots committed to docs/screenshots/: login with Try-demo CTA, chat with tool-call cards expanded, documents page with upload in progress, mobile drawer open over chat (D-14)"
    - "Mobile drawer screenshot showcases Phase 06.1 work (D-14 specifics line: mobile drawer must appear in at least one screenshot)"
    - "One hero GIF at docs/hero.gif, ≤5 MB, ~15–20 s, shows full Try-demo → chat → tool-call → streaming answer flow (D-14)"
    - "All screenshots captured from a fresh anon-demo session — no real user PII visible (T-08-05-PII)"
  artifacts:
    - path: "docs/architecture.excalidraw"
      provides: "Source file for the architecture diagram — editable in Excalidraw, checked into version control for future edits"
      contains: "elements"
    - path: "docs/architecture.png"
      provides: "Rendered PNG export of the architecture diagram for README embed"
      mime: "image/png"
    - path: "docs/hero.gif"
      provides: "Animated hero loop for README"
      mime: "image/gif"
      max_size_bytes: 5242880
    - path: "docs/screenshots/"
      provides: "≥4 screenshot PNGs of key surfaces (login, chat, documents, mobile)"
  key_links:
    - from: "README.md (Plan 08-05)"
      to: "docs/architecture.png"
      via: "relative markdown image link"
      pattern: "!\\[.*\\]\\(docs/architecture\\.png\\)"
    - from: "README.md (Plan 08-05)"
      to: "docs/hero.gif"
      via: "relative markdown image link"
      pattern: "!\\[.*\\]\\(docs/hero\\.gif\\)"
    - from: "README.md (Plan 08-05)"
      to: "docs/screenshots/*.png"
      via: "relative markdown image links"
      pattern: "docs/screenshots/0[1-4]-"
---

<objective>
Deliver PORT-03 asset portion: produce the architecture diagram + 4 screenshots + hero GIF that Plan 08-05's README rewrite embeds. Excalidraw chosen over draw.io per RESEARCH §Stack decisions (faster, simpler JSON source, lighter export). All assets land under `docs/`.

Out of scope (other plans): the README that embeds these assets (08-05), the badges in the README (08-07).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-portfolio-polish/08-CONTEXT.md
@.planning/phases/08-portfolio-polish/08-RESEARCH.md
@.planning/phases/08-portfolio-polish/08-PATTERNS.md
@.planning/phases/08-portfolio-polish/08-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/06.1-mobile-responsive-chat-layout/06.1-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Author + export architecture diagram via Excalidraw (D-12)</name>
  <read_first>
    - .planning/phases/08-portfolio-polish/08-RESEARCH.md (§Architecture diagram nodes — full node list + edge list + sample text labels)
    - .planning/codebase/ARCHITECTURE.md (Layers + Data Flow + Key Abstractions — canonical source for diagram nodes/edges)
    - .planning/phases/08-portfolio-polish/08-CONTEXT.md (D-12 — Excalidraw or draw.io; RESEARCH recommends Excalidraw)
  </read_first>
  <what-built>
    Author the architecture diagram in Excalidraw at https://excalidraw.com (free, no signup) or the Excalidraw desktop/VS Code extension. Required nodes (from RESEARCH §Architecture diagram nodes):
    - Browser (React 19 SPA: Vite bundle on Cloudflare Pages CDN)
    - Cloudflare Pages (static frontend host)
    - Fly.io (backend container — FastAPI + Docling, shared-cpu-1x@1gb)
    - Supabase grouped node containing 4 sub-nodes: Postgres + pgvector, Auth (JWT + anon sign-ins), Storage (documents bucket), Realtime
    - OpenRouter (LLM provider — chat completions)
    - Docling (PDF/DOCX/MD parsing — on backend)
    - Sentry (frontend errors)
    - LangSmith (backend trace capture)
    - UptimeRobot (external uptime monitor)

    Required edges (with short labels):
    - Browser → Cloudflare Pages: `HTTPS (static)`
    - Browser → Fly.io: `HTTPS + SSE (/api/*)`
    - Browser → Supabase Auth: `JWT auth`
    - Browser → Supabase Realtime: `WS (doc status)`
    - Browser → Sentry: `error events`
    - Fly.io → Supabase Postgres/Storage: `service-role`
    - Fly.io → OpenRouter: `chat completions`
    - Fly.io → Docling: `inline parse`
    - Fly.io → LangSmith: `traces`
    - UptimeRobot → Fly.io /api/health: `5-min HTTP`
    - UptimeRobot → Cloudflare Pages: `5-min HTTP`

    Export rules:
    - Save the editable source as `docs/architecture.excalidraw` (Excalidraw → Save as → `.excalidraw`).
    - Export PNG with 2x scale (Excalidraw → Export image → PNG → scale 2x → background white) to `docs/architecture.png`.
    - Target PNG width 1600–2400 px, file size ≤ 500 KB after PNG optimization (run through TinyPNG or `pngquant` if oversized).
  </what-built>
  <how-to-verify>
    1. Confirm both files exist at `docs/architecture.excalidraw` and `docs/architecture.png`.
    2. Open `docs/architecture.png` in an image viewer — assert all 9 service nodes from the required list are visible and labeled.
    3. Confirm PNG file size ≤ 500 KB.
    4. Re-open `docs/architecture.excalidraw` in Excalidraw to confirm it round-trips and is editable.
  </how-to-verify>
  <verify>
    <automated>`test -f docs/architecture.excalidraw && test -f docs/architecture.png && [ $(stat -c%s docs/architecture.png) -le 512000 ] && file docs/architecture.png | grep -F "PNG image"`</automated>
  </verify>
  <done>Excalidraw source + PNG export both committed; PNG ≤ 500 KB; all required nodes + edges present.</done>
  <resume-signal>Report: "diagram done — N nodes, N edges, PNG size KB" OR list missing nodes/edges if the diagram is incomplete.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture 4 screenshots from a fresh anon-demo session (D-14)</name>
  <read_first>
    - .planning/phases/08-portfolio-polish/08-CONTEXT.md (D-14 — required screenshots list)
    - .planning/phases/08-portfolio-polish/08-UI-SPEC.md (Surface 1 — Try-demo CTA visual; Surface 3 — Demo pill on mobile)
    - .planning/phases/06.1-mobile-responsive-chat-layout/06.1-CONTEXT.md (mobile drawer pattern)
  </read_first>
  <what-built>
    Using a fresh incognito session against the deployed prod URL (no real PII), capture 4 PNG screenshots:

    1. `docs/screenshots/01-login-try-demo.png` — Login page showing the "Try the demo" primary CTA above the email/password form. Desktop viewport ~1440×900. Demo pill not yet visible (user not signed in).
    2. `docs/screenshots/02-chat-tool-calls.png` — Chat thread mid-conversation with at least one expanded ToolCallCard (search_documents OR analyze_document OR web_search) visible inline. Captured AFTER clicking "Try the demo" so the amber Demo pill is visible in the IconSidebar.
    3. `docs/screenshots/03-documents-upload.png` — Documents page showing the folder tree + an in-progress upload (status badge visible: parsing/embedding/indexed). Use a small fresh PDF/markdown so the upload happens in real time.
    4. `docs/screenshots/04-mobile-drawer.png` — Mobile viewport (375×667 or 390×844 via DevTools device emulation) with the hamburger drawer OPEN over the chat surface, showing the IconNavRow + ThreadListContent. This screenshot is mandatory per CONTEXT.md specifics ("Mobile drawer must appear in at least one screenshot").

    Capture tooling: any of (a) OS native screenshot (Win+Shift+S → save as PNG), (b) ShareX, (c) browser DevTools full-page screenshot. Optimize each PNG ≤ 200 KB via TinyPNG or `pngquant`. No watermarks, no annotations — clean app screenshots only.

    PII rules: do NOT capture: a real user's email anywhere in the UI, a real upload filename containing personal data, any Sentry/LangSmith dashboard with real user UUIDs. The whole point of using anon-demo for screenshots is that there is no PII to leak.
  </what-built>
  <how-to-verify>
    1. Confirm all 4 files exist at the exact paths above.
    2. Open each PNG and confirm it matches the description (login + Try-demo visible, chat + tool card visible, documents + upload visible, mobile drawer visible).
    3. Confirm no email/UUID/PII is visible in any image.
    4. Confirm total file size ≤ 800 KB combined (≤ 200 KB each).
  </how-to-verify>
  <verify>
    <automated>`for f in docs/screenshots/01-login-try-demo.png docs/screenshots/02-chat-tool-calls.png docs/screenshots/03-documents-upload.png docs/screenshots/04-mobile-drawer.png; do test -f "$f" && [ $(stat -c%s "$f") -le 204800 ] && file "$f" | grep -F "PNG image" || { echo "FAIL: $f"; exit 1; }; done`</automated>
  </verify>
  <done>4 screenshot PNGs exist, each ≤ 200 KB, each clearly shows the described surface, no visible PII.</done>
  <resume-signal>Report: "4/4 captured" OR list missing/oversized screenshots.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Record hero GIF showcasing the full demo flow (D-14)</name>
  <read_first>
    - .planning/phases/08-portfolio-polish/08-CONTEXT.md (D-14 — hero GIF specifics: ~15–20 s, full flow, ≤5 MB)
    - .planning/phases/08-portfolio-polish/08-RESEARCH.md (§Screenshot tooling + hero GIF — recommends ScreenToGif on Windows)
    - .planning/phases/08-portfolio-polish/08-UI-SPEC.md (locked copy strings so the GIF reflects the actual UX)
  </read_first>
  <what-built>
    Using ScreenToGif (https://www.screentogif.com — free, Windows-native) OR equivalent (Kap on macOS, Peek on Linux), record a 15–20 second loop on the deployed prod URL. Required flow in order:

    1. Land on login page (Try-demo CTA visible).
    2. Click "Try the demo" → loading spinner appears briefly.
    3. Land in chat surface with seeded welcome thread (Demo pill visible in sidebar).
    4. Type a query that exercises the agentic tool loop (e.g. "Recommend a 2-player strategy game" or "Compare Catan and Carcassonne").
    5. Watch tool-call cards fire (search_documents at minimum; subagent / web_search optional bonus).
    6. Final streamed answer renders.

    Capture rules:
    - Resolution 1280×720 or 960×540 (browser viewport at that size BEFORE recording).
    - Frame rate 12–15 fps (smooth enough, keeps file size down).
    - Total length 15–20 seconds.
    - Optimize via ScreenToGif's built-in compressor: reduce color palette to 128 colors, drop every-other-duplicate-frame, target ≤ 5 MB final file size.
    - Save to `docs/hero.gif`.

    If the final GIF is > 5 MB, recompress further OR export as animated WebP and adjust Plan 08-05 README link accordingly (animated WebP is supported by GitHub markdown). Hard cap is 5 MB regardless of format.

    PII rules: same as Task 2 — anon-demo session, no real user content.
  </what-built>
  <how-to-verify>
    1. Confirm `docs/hero.gif` exists.
    2. Open in an image viewer or `ffprobe docs/hero.gif` to confirm: animated, total duration 12–22 s (some slack on the spec), reasonable frame count.
    3. Confirm file size ≤ 5 MB (`stat -c%s docs/hero.gif` ≤ 5242880).
    4. Open the GIF and watch through one full loop — confirm all 6 flow steps are visible.
  </how-to-verify>
  <verify>
    <automated>`test -f docs/hero.gif && [ $(stat -c%s docs/hero.gif) -le 5242880 ] && file docs/hero.gif | grep -F "GIF image"`</automated>
  </verify>
  <done>Hero GIF exists at `docs/hero.gif`, ≤ 5 MB, shows the full Try-demo → chat → tool-call → answer flow.</done>
  <resume-signal>Report: "hero GIF done — N MB, N seconds" OR explain why the file is missing/oversized.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recorded media → public repo | Screenshots and GIFs committed to a public repo are world-readable forever; PII in any frame is irretrievable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-PII | Information Disclosure | Screenshots + hero GIF could include real user email / UUID / private docs | mitigate | All capture done from a fresh anonymous-demo session per Task 2 + Task 3 capture rules; no real user JWT / email / UUID visible in any frame. Task verification includes a manual "no PII" review. |
| T-08-06-LIC | Tampering / Compliance | Screenshots or GIF could show third-party copyrighted board-game art | mitigate | Public seeded KB content is text-only (markdown rulebooks summarized in our own words from public sources per Phase 3); sample private doc is the CC-BY 4.0 D&D 5e quick-reference. No third-party images or trademarks captured. |
| T-08-06-OVERSIZE | Availability | Oversized assets bloat repo clone time + GitHub raw bandwidth | mitigate | Hard caps: PNG ≤ 500 KB (diagram), each screenshot ≤ 200 KB, hero GIF ≤ 5 MB. Verified by automated `stat -c%s` checks per task. |
</threat_model>

<verification>
- `docs/architecture.excalidraw` + `docs/architecture.png` exist; PNG ≤ 500 KB; all 9 nodes + 11 edges visible.
- 4 screenshots at `docs/screenshots/01..04-*.png`, each ≤ 200 KB.
- `docs/hero.gif` exists, ≤ 5 MB, ~15–20 s.
- No PII (email, UUID, real upload content) in any captured image.
- Plan 08-05 can resolve all asset paths via relative links from `README.md`.
</verification>

<success_criteria>
- All 6 asset files exist with the exact paths and size caps above.
- Architecture diagram source is committed so future edits don't require re-authoring from scratch.
- Mobile drawer is featured in at least one screenshot (D-14 specifics).
- Hero GIF runs end-to-end demo flow within 20 s.
- Plan 08-05's README can embed every asset without broken-link issues.
</success_criteria>

<output>
After completion, create `.planning/phases/08-portfolio-polish/08-06-SUMMARY.md` capturing:
- File sizes of every asset (architecture PNG, 4 screenshots, hero GIF).
- Tooling used for each asset (Excalidraw vs draw.io, ScreenToGif vs alternative).
- Confirmation that all screenshots were captured from an anon-demo session.
- Any deferred polish (e.g. "diagram could be re-themed dark mode in a future pass").
</output>
