---
phase: "07"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/services/tracing.py
  - backend/scripts/verify_langsmith_routing.py
  - backend/tests/test_tracing.py
autonomous: true
requirements:
  - OBS-02
must_haves:
  truths:
    - "Settings.langsmith_project replaces Settings.langchain_project; pydantic-settings reads it from the LANGSMITH_PROJECT env var (canonical SDK name)"
    - "setup_tracing() resolves project name in precedence order: LANGSMITH_PROJECT env > LANGCHAIN_PROJECT env > settings.langsmith_project default"
    - "setup_tracing() writes BOTH os.environ['LANGSMITH_PROJECT'] AND os.environ['LANGCHAIN_PROJECT'] to the resolved value so all langsmith SDK code paths see the right routing key (defense in depth — RESEARCH §5)"
    - "backend/scripts/verify_langsmith_routing.py mints a Supabase JWT, POSTs a chat to the prod Fly URL, then asserts ≥1 completed run lands in boardgame-rag-prod AND 0 runs in boardgame-rag-dev within a 90s polling window"
    - "Verification script exits non-zero with a clear stderr message on any assertion failure"
  artifacts:
    - path: "backend/config.py"
      provides: "Settings.langsmith_project field (env LANGSMITH_PROJECT, default 'rag-masterclass'); langchain_project removed"
      contains: "langsmith_project"
    - path: "backend/services/tracing.py"
      provides: "setup_tracing() with three-tier project name precedence + writes both env var aliases"
      contains: "LANGSMITH_PROJECT"
    - path: "backend/scripts/verify_langsmith_routing.py"
      provides: "Automated OBS-02 verification: prod traces land in prod project, NOT dev project"
      contains: "boardgame-rag-prod"
      min_lines: 100
    - path: "backend/tests/test_tracing.py"
      provides: "Tests for setup_tracing precedence + no-op-when-key-missing path"
      contains: "test_setup_tracing"
      min_lines: 40
  key_links:
    - from: "backend/services/tracing.py"
      to: "os.environ['LANGSMITH_PROJECT']"
      via: "set after resolution from precedence chain"
      pattern: "os.environ\\[.LANGSMITH_PROJECT.\\]"
    - from: "backend/services/tracing.py"
      to: "os.environ['LANGCHAIN_PROJECT']"
      via: "set to same resolved value as LANGSMITH_PROJECT (legacy alias)"
      pattern: "os.environ\\[.LANGCHAIN_PROJECT.\\]"
    - from: "backend/scripts/verify_langsmith_routing.py"
      to: "langsmith.Client.list_runs"
      via: "called twice — once with project_name='boardgame-rag-prod', once with project_name='boardgame-rag-dev'"
      pattern: "list_runs\\(\\s*project_name="
    - from: "backend/scripts/verify_langsmith_routing.py"
      to: "https://boardgame-rag-prod.fly.dev/api/threads"
      via: "httpx POST + SSE stream against deployed prod"
      pattern: "boardgame-rag-prod\\.fly\\.dev"
---

<objective>
Close OBS-02 by (1) renaming `Settings.langchain_project` → `Settings.langsmith_project` so the canonical `LANGSMITH_PROJECT` env var routes traces, (2) patching `backend/services/tracing.py` to apply the three-tier precedence resolution from RESEARCH §Pitfall 5 / Code Example §5 and write BOTH env-var aliases, and (3) shipping `backend/scripts/verify_langsmith_routing.py` (per RESEARCH §Code Example §4) which fires a real chat against the deployed Fly URL and asserts the run lands in `boardgame-rag-prod` and NOT in `boardgame-rag-dev`. Pytest coverage for the precedence logic.

Purpose: This is the bug-fix half of OBS-02. `tracing.py:15` currently unconditionally sets `os.environ["LANGCHAIN_PROJECT"] = settings.langchain_project` (defaulting to `"rag-masterclass"`) — so even when Fly has `LANGSMITH_PROJECT=boardgame-rag-prod` staged, traces would route to `rag-masterclass`. RESEARCH §Pitfall 5 documents this bug; user decision 2026-05-15 (CONTEXT) commits to the rename + tracing fix.
Output: 2 modified backend files, 1 new verification script, 1 new test file. Green pytest + (post-deploy) green verify_langsmith_routing.py.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-baseline/07-CONTEXT.md
@.planning/phases/07-observability-baseline/07-RESEARCH.md
@.planning/phases/07-observability-baseline/07-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

# Source files this plan modifies / extends
@backend/config.py
@backend/services/tracing.py
@backend/scripts/seed_default_kb.py
@backend/scripts/fly_smoke.sh
@backend/scripts/_lib/get_test_jwt.sh

<interfaces>
<!-- Current shapes; locked decisions from CONTEXT + RESEARCH. -->

From `backend/config.py:12-19` (current Settings prelude):
```
class Settings(BaseSettings):
    supabase_url: str = ""
    supabase_service_role_key: str = ""
    supabase_jwt_secret: str = ""
    openai_api_key: str = ""
    langsmith_api_key: str = ""
    langchain_tracing_v2: str = "true"
    langchain_project: str = "rag-masterclass"
```
- `pydantic-settings` maps field names to UPPERCASE env vars by default. So `langchain_project` reads `LANGCHAIN_PROJECT` env. After rename: `langsmith_project` reads `LANGSMITH_PROJECT` env.

From `backend/services/tracing.py` (current 16-line module, all of it):
```
import os
from config import get_settings

def setup_tracing():
    """Configure LangSmith tracing. No-op if LANGSMITH_API_KEY is not set."""
    settings = get_settings()
    if not settings.langsmith_api_key:
        return
    os.environ["LANGCHAIN_TRACING_V2"] = settings.langchain_tracing_v2
    os.environ["LANGCHAIN_API_KEY"] = settings.langsmith_api_key
    os.environ["LANGCHAIN_PROJECT"] = settings.langchain_project
```

User decision 2026-05-15 (CONTEXT.md lines 55-58):
1. Rename `Settings.langchain_project` → `Settings.langsmith_project` (env field name `LANGSMITH_PROJECT`)
2. Update `tracing.py` to set `os.environ["LANGSMITH_PROJECT"]` (and keep `LANGCHAIN_PROJECT` alias for SDK compat)
3. Local `.env` updated; Fly secret renamed via `flyctl secrets unset LANGCHAIN_PROJECT && flyctl secrets set LANGSMITH_PROJECT=boardgame-rag-prod`

From RESEARCH §Pitfall 5 + Code Example §5 — RECOMMENDED patch (option B):
- Three-tier precedence: `os.environ.get("LANGSMITH_PROJECT") or os.environ.get("LANGCHAIN_PROJECT") or settings.langsmith_project`
- Write BOTH env vars to the resolved value (defense in depth — langsmith SDK 0.3.42 `get_env_var()` walker checks LANGSMITH_PROJECT first then LANGCHAIN_PROJECT; some downstream `@traceable` paths may read either)

From RESEARCH §Code Example §4 — verification script template (covers the exact `list_runs` signature verified against langsmith 0.3.42):
- `Client.list_runs(project_name: Optional[Union[str, Sequence[str]]], start_time: Optional[datetime.datetime], error: Optional[bool], limit, ...)` — `start_time` accepts native datetime, NOT ISO string or epoch int
- Completion detection: `error=False` filter + post-filter `run.end_time is not None` for defensive client-side check
- Run schema confirmed via `Run.__fields__` — `end_time`, `error`, `status` all present

JWT acquisition pattern from `backend/scripts/_lib/get_test_jwt.sh`:
- POSTs to `${SUPABASE_URL}/auth/v1/token?grant_type=password` with `apikey` header set to anon key + JSON body `{"email": ..., "password": ...}`
- Returns `access_token` from response body
- RESEARCH recommends Python re-implementation in the verify script for cross-platform durability (PATTERNS §5 analog C)

Test credentials from CLAUDE.md:
- Email: `ragtest1@gmail.com`
- Password: `testpass123`

Fly URL from CONTEXT.md + ROADMAP:
- `https://boardgame-rag-prod.fly.dev`

Project names from CONTEXT.md (CONTEXT D-3):
- Local dev `.env`: `LANGSMITH_PROJECT=boardgame-rag-dev`
- Fly prod secret: `LANGSMITH_PROJECT=boardgame-rag-prod`

Existing config.py default `"rag-masterclass"` is preserved as the FIELD DEFAULT (zero-config local fallback), but the local `.env` should set `LANGSMITH_PROJECT=boardgame-rag-dev` explicitly going forward.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Settings.langchain_project → langsmith_project in backend/config.py</name>
  <files>backend/config.py</files>
  <read_first>backend/config.py, .planning/phases/07-observability-baseline/07-CONTEXT.md</read_first>
  <action>
    Edit `backend/config.py`:

    1. Replace line 19 `langchain_project: str = "rag-masterclass"` with `langsmith_project: str = "rag-masterclass"`. Field name only changes; default value preserved as zero-config local fallback. pydantic-settings will now map this field to the `LANGSMITH_PROJECT` env var (UPPERCASE of the new field name).
    2. Search the rest of `backend/config.py` for any other reference to `langchain_project` — there should be none. If any exist, remove or rename.
    3. Do NOT remove the `langchain_tracing_v2` field (line 18) — that is a separate SDK env var that langsmith still reads.

    After edit, run `grep -n 'langchain_project\\|langsmith_project' backend/config.py` and confirm only `langsmith_project: str = "rag-masterclass"` remains.

    Codebase-wide check: `grep -rn 'langchain_project' backend/` — should return matches ONLY in `backend/services/tracing.py` (which Task 2 fixes). Any other matches must be renamed in this task or surfaced in the task summary.

    Style per CONVENTIONS.md: 4-space indent, double quotes, snake_case field name.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -c "from config import get_settings, Settings; s = get_settings(); assert hasattr(s, 'langsmith_project'), 'missing langsmith_project'; assert not hasattr(s, 'langchain_project'), 'langchain_project not removed'; print('OK', s.langsmith_project)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'langsmith_project: str' backend/config.py` returns exactly 1 match
    - `grep -n 'langchain_project' backend/config.py` returns 0 matches
    - `grep -rn 'langchain_project' backend/` (excluding services/tracing.py — which Task 2 fixes) returns 0 matches in any other file
    - `from config import Settings; Settings()` instantiates without error; `Settings().langsmith_project` equals `"rag-masterclass"` (default) when `LANGSMITH_PROJECT` env unset
    - With `LANGSMITH_PROJECT=foobar` in env, `Settings().langsmith_project == "foobar"`
  </acceptance_criteria>
  <done>`Settings.langsmith_project` field replaces `langchain_project`; no other references remain in config.py; pydantic-settings reads from `LANGSMITH_PROJECT` env.</done>
</task>

<task type="auto">
  <name>Task 2: Patch backend/services/tracing.py for three-tier precedence + dual env-var write</name>
  <files>backend/services/tracing.py, backend/tests/test_tracing.py</files>
  <read_first>backend/services/tracing.py, backend/config.py, .planning/phases/07-observability-baseline/07-RESEARCH.md</read_first>
  <action>
    Replace `backend/services/tracing.py` entirely with the implementation from RESEARCH §Code Example §5 (verbatim, adjusted for the field rename from Task 1):

    ```
    import os
    from config import get_settings


    def setup_tracing():
        """Configure LangSmith tracing. No-op if LANGSMITH_API_KEY is not set.

        Project name resolution (highest to lowest precedence):
          1. LANGSMITH_PROJECT env var (canonical name per langsmith SDK 0.3.42+)
          2. LANGCHAIN_PROJECT env var (legacy name, still respected by SDK)
          3. settings.langsmith_project (pydantic-settings default)
        """
        settings = get_settings()

        if not settings.langsmith_api_key:
            return

        # Determine project name from highest-precedence source.
        project = (
            os.environ.get("LANGSMITH_PROJECT")
            or os.environ.get("LANGCHAIN_PROJECT")
            or settings.langsmith_project
        )

        os.environ["LANGCHAIN_TRACING_V2"] = settings.langchain_tracing_v2
        os.environ["LANGCHAIN_API_KEY"] = settings.langsmith_api_key
        os.environ["LANGCHAIN_PROJECT"] = project
        # Also set canonical name for SDK's own env-var walker.
        os.environ["LANGSMITH_PROJECT"] = project
    ```

    Important nuance — pydantic-settings env-var resolution order: if `Settings.langsmith_project` field is populated from `LANGSMITH_PROJECT` env var, then `os.environ.get("LANGSMITH_PROJECT")` reads the same value the field already has. The three-tier chain still works because (a) it tolerates the env-set case (returns env value, identical to settings value) and (b) it also tolerates the legacy `LANGCHAIN_PROJECT`-only case (env LANGSMITH_PROJECT unset, LANGCHAIN_PROJECT set → second tier picks it up). The chain is the documented Pitfall 5 fix; do not simplify.

    Now create `backend/tests/test_tracing.py` with three tests covering the contract:

    Module header:
    ```
    """Tests for setup_tracing() — OBS-02 project-routing precedence (Pitfall 5)."""
    import os
    from unittest.mock import patch
    import pytest

    from services.tracing import setup_tracing
    ```

    Test 1 — `test_setup_tracing_noop_when_api_key_missing`:
    - Patch `services.tracing.get_settings` to return a `MagicMock` with `langsmith_api_key=""`, `langchain_tracing_v2="true"`, `langsmith_project="boardgame-rag-dev"`
    - Clear env: use `monkeypatch.delenv("LANGCHAIN_PROJECT", raising=False)` and same for `LANGSMITH_PROJECT`, `LANGCHAIN_API_KEY`, `LANGCHAIN_TRACING_V2`
    - Call `setup_tracing()`
    - Assert `"LANGCHAIN_API_KEY" not in os.environ` (early return — no env writes)

    Test 2 — `test_setup_tracing_precedence_env_langsmith_wins(monkeypatch)`:
    - Patch settings to return `langsmith_api_key="fake-key"`, `langchain_tracing_v2="true"`, `langsmith_project="settings-default"`
    - `monkeypatch.setenv("LANGSMITH_PROJECT", "env-langsmith-wins")`
    - `monkeypatch.setenv("LANGCHAIN_PROJECT", "env-langchain-loses")`
    - Call `setup_tracing()`
    - Assert `os.environ["LANGSMITH_PROJECT"] == "env-langsmith-wins"`
    - Assert `os.environ["LANGCHAIN_PROJECT"] == "env-langsmith-wins"` (dual-write to the SAME resolved value — defense in depth)
    - Assert `os.environ["LANGCHAIN_API_KEY"] == "fake-key"`
    - Assert `os.environ["LANGCHAIN_TRACING_V2"] == "true"`

    Test 3 — `test_setup_tracing_falls_back_to_settings_when_no_env(monkeypatch)`:
    - Patch settings to return `langsmith_api_key="fake-key"`, `langchain_tracing_v2="true"`, `langsmith_project="settings-default"`
    - `monkeypatch.delenv("LANGSMITH_PROJECT", raising=False)`
    - `monkeypatch.delenv("LANGCHAIN_PROJECT", raising=False)`
    - Call `setup_tracing()`
    - Assert both `os.environ["LANGSMITH_PROJECT"]` and `os.environ["LANGCHAIN_PROJECT"]` equal `"settings-default"`

    Additionally — bonus 4th test if straightforward, otherwise skip — `test_setup_tracing_legacy_langchain_env_still_works(monkeypatch)`:
    - Patch settings; `monkeypatch.delenv("LANGSMITH_PROJECT", raising=False)`; `monkeypatch.setenv("LANGCHAIN_PROJECT", "legacy-fallback")`
    - Call `setup_tracing()`
    - Assert both env vars now equal `"legacy-fallback"` (legacy env var promoted to canonical via the precedence chain + dual-write)

    Style per CONVENTIONS.md.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -m pytest tests/test_tracing.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'os.environ\\["LANGSMITH_PROJECT"\\]' backend/services/tracing.py` returns 1 match
    - `grep -n 'os.environ\\["LANGCHAIN_PROJECT"\\]' backend/services/tracing.py` returns 1 match
    - `grep -n 'os.environ.get("LANGSMITH_PROJECT")' backend/services/tracing.py` returns 1 match
    - `grep -n 'os.environ.get("LANGCHAIN_PROJECT")' backend/services/tracing.py` returns 1 match
    - `grep -n 'settings.langsmith_project' backend/services/tracing.py` returns 1 match
    - `grep -n 'settings.langchain_project' backend/services/tracing.py` returns 0 matches
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_tracing.py -x -v` reports 3 or 4 passed (depending on whether bonus test was implemented), 0 failed
    - Full backend suite still green: `cd backend && venv/Scripts/python.exe -m pytest tests/ -x`
  </acceptance_criteria>
  <done>`tracing.py` implements three-tier precedence + dual env-var write; 3+ new pytest tests cover the precedence and no-op paths; full backend pytest suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Create backend/scripts/verify_langsmith_routing.py per RESEARCH §Code Example §4</name>
  <files>backend/scripts/verify_langsmith_routing.py</files>
  <read_first>backend/scripts/seed_default_kb.py, backend/scripts/fly_smoke.sh, backend/scripts/_lib/get_test_jwt.sh, .planning/phases/07-observability-baseline/07-RESEARCH.md</read_first>
  <action>
    Create new file `backend/scripts/verify_langsmith_routing.py` implementing the script from RESEARCH §Code Example §4 verbatim shape (with field-rename adjustments — `settings.langsmith_project` is now valid; `settings.vite_supabase_anon_key` is already present in config.py at line 124).

    The script MUST:

    1. Module docstring at top per CONVENTIONS.md — explain purpose, usage, exit codes, and the three assertions (per RESEARCH §Code Example §4 lines 564-581).
    2. `sys.path.insert(0, ...)` shim so `from config import get_settings` resolves when run as `python backend/scripts/verify_langsmith_routing.py` (mirror `backend/scripts/seed_default_kb.py:1-21`).
    3. Imports: `os, sys, time, datetime, pathlib`, then third-party `httpx`, `from langsmith import Client`, local `from config import get_settings`.
    4. Module-level config constants:
       - `FLY_URL = "https://boardgame-rag-prod.fly.dev"`
       - `PROD_PROJECT = "boardgame-rag-prod"`
       - `DEV_PROJECT = "boardgame-rag-dev"`
       - `POLL_TIMEOUT_S = 90`
       - `POLL_INTERVAL_S = 5`
    5. Early-exit guard: if `settings.langsmith_api_key` is unset → `sys.exit("FAIL: LANGSMITH_API_KEY not set in env")`.
    6. `get_test_jwt() -> str`: Python re-implementation of `_lib/get_test_jwt.sh` per PATTERNS §5 analog C "Recommendation: Python re-implementation":
       - Read `supabase_url = settings.supabase_url_resolved` (uses existing property at config.py:127-128)
       - Read `anon_key = settings.vite_supabase_anon_key` (exists at config.py:124); fail with `sys.exit` if missing
       - `httpx.post(f"{supabase_url}/auth/v1/token?grant_type=password", headers={"apikey": anon_key, "Content-Type": "application/json"}, json={"email": "ragtest1@gmail.com", "password": "testpass123"}, timeout=10.0)`
       - `resp.raise_for_status()`; return `resp.json()["access_token"]`
    7. `send_chat() -> tuple[datetime.datetime, str]`:
       - Mint JWT via `get_test_jwt()`
       - Headers: `{"Authorization": f"Bearer {jwt}", "Content-Type": "application/json"}`
       - POST `{FLY_URL}/api/threads` with body `{"title": "OBS-02 verify"}`, timeout 15s; extract `thread_id` from response
       - Record `t0 = datetime.datetime.now(datetime.timezone.utc)` BEFORE sending the message (so `start_time` filter lower bound is correct — RESEARCH §Code Example §4 line 638 comment)
       - `httpx.stream("POST", f"{FLY_URL}/api/threads/{thread_id}/messages", headers=..., json={"content": "What is the simplest board game in the KB?"}, timeout=60.0)`; drain the SSE stream by iterating `iter_lines()` without parsing
       - Return `(t0, thread_id)`
    8. `assert_routing(t0: datetime.datetime) -> None`:
       - Loop with `deadline = time.monotonic() + POLL_TIMEOUT_S`; sleep `POLL_INTERVAL_S` between polls
       - Call `client.list_runs(project_name=PROD_PROJECT, start_time=t0, error=False, limit=10)` — pass `start_time` as a native `datetime.datetime` (NOT ISO string), per RESEARCH §OQ4 resolution
       - Post-filter to `completed = [r for r in prod_runs if r.end_time is not None]` (defensive — Run.end_time is None while in-progress)
       - If `completed`: print PASS line with count; break out of poll loop
       - On timeout (loop exhausted without break): `sys.exit(f"FAIL: no completed runs in {PROD_PROJECT} within {POLL_TIMEOUT_S}s")` — use `while/else` pattern OR explicit flag
       - After prod-pass: call `client.list_runs(project_name=DEV_PROJECT, start_time=t0, limit=10)`; if any runs exist: `sys.exit(f"FAIL: {len(dev_runs)} run(s) leaked into {DEV_PROJECT} — routing broken")`
       - Else: print PASS line for dev
    9. `main()`: prints two progress lines (`[1/2] Sending test chat...`, `[2/2] Polling LangSmith...`), calls `send_chat()` then `assert_routing()`, prints `"OBS-02 VERIFY: PASS"` on success, returns 0
    10. `if __name__ == "__main__": main()`

    Script MUST exit with non-zero status on any failure (`sys.exit("FAIL: ...")` already exits non-zero by convention when string is passed). Print to stderr where appropriate; PASS messages go to stdout.

    Do NOT hardcode credentials anywhere — JWT minting uses CLAUDE.md test creds (which are also documented test creds in CLAUDE.md, acceptable for a verify script).

    Style per CONVENTIONS.md: snake_case, 4-space indent, double quotes, module docstring + function docstrings, `logger` optional (script uses `print` + `sys.exit` per `fly_smoke.sh` idiom — verification scripts are CLI-style).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; venv/Scripts/python.exe -c "import ast; tree = ast.parse(open('scripts/verify_langsmith_routing.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'get_test_jwt' in funcs and 'send_chat' in funcs and 'assert_routing' in funcs and 'main' in funcs, f'missing funcs: {funcs}'; print('OK', funcs)"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/scripts/verify_langsmith_routing.py` exists with ≥100 lines (4 functions + imports + docstring)
    - `grep -n 'boardgame-rag-prod' backend/scripts/verify_langsmith_routing.py` returns ≥1 match
    - `grep -n 'boardgame-rag-dev' backend/scripts/verify_langsmith_routing.py` returns ≥1 match
    - `grep -n 'list_runs' backend/scripts/verify_langsmith_routing.py` returns ≥2 matches (prod query + dev query)
    - `grep -n 'start_time=' backend/scripts/verify_langsmith_routing.py` returns ≥2 matches
    - `grep -n 'from langsmith import Client' backend/scripts/verify_langsmith_routing.py` returns 1 match
    - `grep -n 'datetime.datetime.now(datetime.timezone.utc)' backend/scripts/verify_langsmith_routing.py` returns ≥1 match
    - Syntactic + import sanity: `cd backend && venv/Scripts/python.exe -c "import ast; ast.parse(open('scripts/verify_langsmith_routing.py').read())"` exits 0
    - Module-level import sanity (no immediate sys.exit while parsing): `cd backend && venv/Scripts/python.exe -c "import scripts.verify_langsmith_routing as v; print(v.PROD_PROJECT, v.DEV_PROJECT, v.FLY_URL)"` prints the constants if LANGSMITH_API_KEY is set in .env (script's early-exit guard is at module level — acceptable; tester should have key)
    - Note: full end-to-end execution (`python scripts/verify_langsmith_routing.py`) is performed AFTER Wave 3 Plan 07-04/07-05 confirm prod is reachable + Fly secret rename + redeploy. It is not run as part of this plan's per-task verify because it depends on the renamed Fly secret being live.
  </acceptance_criteria>
  <done>Verification script exists at `backend/scripts/verify_langsmith_routing.py`, parses cleanly, imports the locked SDK constants, and is ready to run post-redeploy in Wave 3. Module-level guard returns clear error when `LANGSMITH_API_KEY` is unset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI process → LangSmith SaaS | Trace payloads (chat messages, tool calls, embeddings) shipped over HTTPS. Project name routing determines which Org project receives them. Misrouting leaks prod traffic into dev project (and vice-versa). |
| `backend/scripts/verify_langsmith_routing.py` → Supabase Auth | JWT mint via password grant against `${SUPABASE_URL}/auth/v1/token`. Uses test-account credentials documented in CLAUDE.md — acceptable for verification scripts. |
| `backend/scripts/verify_langsmith_routing.py` → prod Fly | Single chat round-trip to deployed prod URL with a minted JWT. Same surface as a real authenticated user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information Disclosure | Prod traces silently routed to `rag-masterclass` (legacy default) due to `tracing.py:15` unconditional `LANGCHAIN_PROJECT` write — cross-env trace leak within own LangSmith org | mitigate | Three-tier precedence chain in `setup_tracing()` honors `LANGSMITH_PROJECT` env first; dual env-var write ensures all SDK code paths agree; `test_setup_tracing_*` tests pin the contract; `verify_langsmith_routing.py` validates against deployed prod end-to-end. (Closes RESEARCH Pitfall 5.) |
| T-07-14 | Information Disclosure | Verification script logs JWT or thread contents to stdout/stderr | mitigate | Script never prints the JWT or message content; only prints PROD_PROJECT/DEV_PROJECT counts and PASS/FAIL strings. JWT is held in a local variable + passed via Authorization header. |
| T-07-15 | Repudiation | Routing bug introduced post-fix goes undetected; prod traces silently route to dev project for weeks | mitigate | `verify_langsmith_routing.py` is a regression check that can be run on demand (and as part of Wave 3 sign-off); SUMMARY records last-passed timestamp. |
| T-07-16 | Spoofing | Test credentials `ragtest1@gmail.com` / `testpass123` shipped publicly in verify script | accept | Same credentials are already documented in CLAUDE.md as project-wide test creds; not a real user account; isolated to dev/test data. |
| T-07-17 | Denial of Service | Verification script's chat round-trip + 90s polling loop hits LangSmith rate limits | accept | Free-tier LangSmith allows generous list_runs polling; 18 polls/run × manual invocation cadence << any realistic limit. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python.exe -m pytest tests/test_tracing.py -x -v` reports 3 (or 4) passed
- `cd backend && venv/Scripts/python.exe -m pytest tests/ -x` (full suite) passes — no regressions
- `grep -rn 'langchain_project' backend/` returns 0 matches (rename complete across the codebase)
- `grep -n 'os.environ\\["LANGSMITH_PROJECT"\\]' backend/services/tracing.py` returns 1 match (the dual-write defense)
- `cd backend && venv/Scripts/python.exe -c "import ast; ast.parse(open('scripts/verify_langsmith_routing.py').read())"` exits 0
- `git diff --stat backend/` shows changes in: `config.py`, `services/tracing.py`, `tests/test_tracing.py` (new), `scripts/verify_langsmith_routing.py` (new). No other backend file modified.
- (Post-Wave-3) Run `cd backend && venv/Scripts/python.exe scripts/verify_langsmith_routing.py` once the Fly secret rename is live (per 07-CONTEXT.md user decision 2026-05-15: `flyctl secrets unset LANGCHAIN_PROJECT && flyctl secrets set LANGSMITH_PROJECT=boardgame-rag-prod`, then redeploy). Expected output: two PASS lines + `OBS-02 VERIFY: PASS`.
</verification>

<success_criteria>
- `Settings.langchain_project` removed; `Settings.langsmith_project` added; default `"rag-masterclass"` preserved
- `setup_tracing()` resolves project name via three-tier precedence (LANGSMITH_PROJECT env > LANGCHAIN_PROJECT env > settings default) and writes BOTH env-var aliases to the resolved value
- 3+ pytest tests cover no-op, env-LANGSMITH-wins, and settings-fallback paths; all pass
- `backend/scripts/verify_langsmith_routing.py` exists with all 4 functions; parses cleanly; constants match CONTEXT.md project names
- Full backend pytest suite green; no regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-baseline/07-03-SUMMARY.md`. SUMMARY must document the Fly-side secret rename runbook (per CONTEXT.md user decision 2026-05-15): `flyctl secrets unset LANGCHAIN_PROJECT && flyctl secrets set LANGSMITH_PROJECT=boardgame-rag-prod`, then redeploy via `fly deploy` so the new precedence chain reads the renamed secret. Note: the Fly secret rename itself executes during Wave 3 alongside the dashboard work; this plan ships the code that consumes the renamed secret.
</output>
