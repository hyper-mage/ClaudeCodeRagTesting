---
phase: 05-deploy-frontend-to-cloudflare-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/_redirects
  - frontend/.nvmrc
autonomous: false
requirements:
  - DEPLOY-05
user_setup:
  - service: cloudflare-pages
    why: "Static SPA host; project must be created via dashboard (D-01 explicitly forbids wrangler CLI auth on dev laptop)"
    dashboard_config:
      - task: "Create Pages project boardgame-rag-prod, connect GitHub repo, set production branch=main, root dir=frontend, build cmd=npm run build, output dir=dist, env vars (Production scope only): NODE_VERSION=20, VITE_API_BASE_URL=https://boardgame-rag-prod.fly.dev, VITE_SUPABASE_URL=<from .env.prod>, VITE_SUPABASE_ANON_KEY=<from .env.prod>; disable Preview deploys (Branch control = None)"
        location: "https://dash.cloudflare.com/ -> Workers & Pages -> Create application -> Pages -> Connect to Git"
  - service: fly-io
    why: "Phase 4 placeholder CORS_ALLOWED_ORIGINS=http://localhost:5173 must be overwritten with the CF Pages prod origin (D-11)"
    cli_command: "flyctl secrets set CORS_ALLOWED_ORIGINS=https://boardgame-rag-prod.pages.dev -a boardgame-rag-prod"

must_haves:
  truths:
    - "Public URL https://boardgame-rag-prod.pages.dev returns the SPA login page with no console errors"
    - "Browser DevTools Network tab shows /api/* requests targeting absolute https://boardgame-rag-prod.fly.dev/... (not same-origin)"
    - "Hard-refresh of deep route /documents returns the SPA (200 text/html), not a CF 404 (proves _redirects works)"
    - "After flyctl secrets set, cross-origin SSE chat from CF origin to Fly succeeds without CORS rejection in console"
    - "Production bundle on the deployed CF URL contains zero matches for service_role / sk-proj / sk-or- / sb_secret_ (SEC-07 re-run)"
  artifacts:
    - path: "frontend/public/_redirects"
      provides: "SPA catch-all rewrite rule for CF Pages"
      contains: "/* /index.html 200"
    - path: "frontend/.nvmrc"
      provides: "Local-dev Node version pin matching CF NODE_VERSION env var"
      contains: "20"
    - path: "frontend/dist/_redirects"
      provides: "Built artifact proving Vite copies public/_redirects verbatim"
      contains: "/* /index.html 200"
  key_links:
    - from: "frontend (CF Pages bundle)"
      to: "https://boardgame-rag-prod.fly.dev"
      via: "VITE_API_BASE_URL baked at build time, consumed by apiFetch/apiStream from Phase 1"
      pattern: "boardgame-rag-prod\.fly\.dev"
    - from: "https://boardgame-rag-prod.pages.dev/<deep-route>"
      to: "/index.html (200)"
      via: "frontend/public/_redirects -> dist/_redirects -> CF Pages serves index.html"
      pattern: "/\* /index.html 200"
    - from: "Fly secret CORS_ALLOWED_ORIGINS"
      to: "Browser cross-origin policy"
      via: "flyctl secrets set ... -a boardgame-rag-prod (overwrites Phase 4 placeholder)"
      pattern: "https://boardgame-rag-prod\.pages\.dev"
---

<objective>
Ship the Vite SPA to a public Cloudflare Pages URL (`https://boardgame-rag-prod.pages.dev`) with API calls pointing at the Fly backend, deep-link refresh working, and Fly's CORS allowlist overwritten from the Phase 4 placeholder to the new CF origin. Closes DEPLOY-05.

Purpose: Front-half of the public deploy story — the Fly backend (Phase 4) is reachable but not yet from a browser-hosted SPA. After this plan, a reviewer with the public URL can load the login page and (once Phase 6 lands Auth redirect URLs) chat end-to-end.

Output: One committed file (`frontend/public/_redirects`), one optional file (`frontend/.nvmrc` per Claude's discretion / D-06 Note), a live CF Pages project, an updated Fly secret, and a passing manual browser checklist.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deploy-frontend-to-cloudflare-pages/05-CONTEXT.md
@.planning/phases/05-deploy-frontend-to-cloudflare-pages/05-RESEARCH.md
@.planning/phases/05-deploy-frontend-to-cloudflare-pages/05-VALIDATION.md
@.planning/phases/01-secrets-repo-hygiene/01-CONTEXT.md
@.planning/phases/04-deploy-backend-to-fly-io/04-CONTEXT.md
@frontend/package.json
@frontend/package-lock.json
@frontend/vite.config.ts

<interfaces>
<!-- Phase 1 already wired the URL prefix; no frontend code changes in this plan. -->
<!-- Reproduced here so executor does NOT have to scavenger-hunt for the contract. -->

From frontend/src/lib/api.ts (Phase 1 D-05/D-08, current behavior):
```typescript
// apiFetch / apiStream both prefix every request:
const API_BASE = import.meta.env.VITE_API_BASE_URL ?? '';
// Then: fetch(`${API_BASE}${path}`, ...)
// Dev (VITE_API_BASE_URL unset) -> "" + "/api/threads" -> Vite proxy hits localhost:8000
// Prod (VITE_API_BASE_URL = "https://boardgame-rag-prod.fly.dev") -> absolute Fly URL
```

From frontend/vite.config.ts (current — DO NOT MODIFY):
```typescript
envDir: '..'   // Loads .env from repo root in local dev only.
                // CF Pages does NOT use .env — env vars come from CF dashboard at build time.
                // Vite still inlines any VITE_* var present in process.env at build time.
```

CF Pages `_redirects` rule format (from CF docs):
```
[source] [destination] [status]
/* /index.html 200       # splat catches all unmatched paths; status 200 = rewrite, not redirect
```

Vite public/ behavior (from Vite docs):
"Assets in [public/] are copied to the root of the dist directory as-is."
=> frontend/public/_redirects -> frontend/dist/_redirects after `npm run build`.

Fly CORS env var (Phase 1 D-01/D-02 + Phase 4 D-07):
```python
# backend/config.py: comma-separated string -> list[str] via Settings.cors_origins_list
# Dev fallback (when env unset) = ["http://localhost:5173"]
# Phase 4 placeholder Fly secret value: "http://localhost:5173"
# Phase 5 final value (this plan): "https://boardgame-rag-prod.pages.dev"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _redirects + .nvmrc and verify local build emits dist/_redirects</name>
  <read_first>
    - frontend/vite.config.ts (confirms envDir + no public-dir override -> default `public/`)
    - frontend/package.json (confirms `build` script = `tsc -b && vite build`)
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-CONTEXT.md §D-10 + §D-06 Claude's Discretion
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-RESEARCH.md §"Pattern 2: `_redirects` for SPA Fallback" + §"Pitfall 2"
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-VALIDATION.md tasks 5-01-01 / 5-01-02
  </read_first>
  <files>frontend/public/_redirects, frontend/.nvmrc</files>
  <action>
    Create `frontend/public/_redirects` containing exactly one line, no extension on filename, no trailing whitespace:

        /* /index.html 200

    Then create `frontend/.nvmrc` containing exactly:

        20

    Rationale (D-10 + D-06 + Pitfall 2): Vite copies `public/*` verbatim to `dist/`, so the file MUST be named `_redirects` (no `.txt`) and live inside `public/` — not `frontend/_redirects`. CF Pages picks it up automatically; no dashboard wiring for the file itself. The `.nvmrc` is Claude's discretion under D-06; we add it because (a) it costs nothing, (b) it mirrors the CF `NODE_VERSION=20` env var to keep local dev consistent, and (c) it documents the lockfile-generating Node major (per RESEARCH §Standard Stack: lockfile produced under Node 20.14.0).

    DO NOT modify `frontend/vite.config.ts` — `envDir: '..'` is dev-only and harmless on CF Pages where env vars come from the dashboard. DO NOT touch `frontend/src/**` — Phase 1 already wired `VITE_API_BASE_URL` through `apiFetch` / `apiStream`.

    After both files exist, run a clean local build to prove the `_redirects` file lands in `dist/`:

        cd frontend && npm run build

    Then commit (Phase 5 task 1):

        git add frontend/public/_redirects frontend/.nvmrc
        git commit -m "feat(05-01): add CF Pages _redirects + .nvmrc Node 20 pin"

    DO NOT push yet — push happens in Task 2 only after the CF Pages project is configured to receive it (otherwise CF auto-build kicks off without env vars and produces a broken first deploy).
  </action>
  <verify>
    <automated>cd frontend && test -f public/_redirects && grep -qx '/\* /index.html 200' public/_redirects && test -f .nvmrc && grep -qx '20' .nvmrc && npm run build && test -f dist/_redirects && grep -qx '/\* /index.html 200' dist/_redirects</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/public/_redirects` exists with EXACT contents `/* /index.html 200` (one line, no extension, no trailing whitespace) — verified by `grep -qx '/\* /index.html 200' frontend/public/_redirects` returning 0.
    - `frontend/.nvmrc` exists with EXACT contents `20` — verified by `grep -qx '20' frontend/.nvmrc` returning 0.
    - `cd frontend && npm run build` exits 0 (TypeScript strict + Vite production build pass).
    - `frontend/dist/_redirects` exists after build with the same contents (proves Vite copied `public/` verbatim per D-10) — verified by `grep -qx '/\* /index.html 200' frontend/dist/_redirects` returning 0.
    - `git log -1 --name-only` shows the two files staged in a single commit, with no other unintended files.
    - Maps to VALIDATION.md tasks 5-01-01 and 5-01-02.
  </acceptance_criteria>
  <done>Both files exist on disk, local prod build succeeds, dist/_redirects is verbatim correct, single commit landed on the working branch.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create CF Pages project + push commit + verify first green deploy</name>
  <read_first>
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-CONTEXT.md §D-01 / §D-02 / §D-03 / §D-05 / §D-06 / §D-07 / §D-08 / §D-09
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-RESEARCH.md §"Pattern 1: GitHub Git Integration" + §"Pitfall 1" + §"Pitfall 3" + §"Pitfall 5" + §"Pitfall 6"
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-VALIDATION.md tasks 5-01-03 / 5-01-04 / 5-01-05 / 5-01-06
    - .env.prod at repo root (canonical source for VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY values per Phase 3 D-15/D-16; gitignored)
  </read_first>
  <files>(out-of-repo: Cloudflare Pages dashboard configuration only)</files>
  <action>
    No CLI/API automation in this phase per D-01 (explicitly forbids wrangler CLI auth on dev laptop). This task is **manual dashboard configuration** plus a single `git push`. Reason this is `checkpoint:human-action` and NOT `auto`: project creation requires interactive Cloudflare dashboard navigation, GitHub OAuth authorization (browser-only flow), and the developer must paste secret values from `.env.prod` into the dashboard form (no CLI equivalent given D-01).

    Step-by-step (executor surfaces this list verbatim to user, then waits):

    1. **Open** https://dash.cloudflare.com/ → **Workers & Pages** → **Create application** → **Pages** tab → **Connect to Git**.
    2. **Authorize GitHub** (if not already) → select repository `hyper-mage/claude-code-agentic-rag-masterclass` (or current remote name).
    3. **Project name:** `boardgame-rag-prod` (D-03 — must match exactly; this becomes the `*.pages.dev` subdomain).
    4. **Production branch:** `main` (D-02).
    5. **Build configuration:**
       - Framework preset: **Vite** (or **None** — both work; Vite preset just pre-fills the next two fields).
       - **Build command:** `npm run build`
       - **Build output directory:** `dist`
       - **Root directory (advanced section, MUST set):** `frontend`  ← critical per D-05 + Pitfall 1; without this CF runs npm at repo root and the build fails with `ENOENT package.json`.
    6. **Environment variables (Production scope ONLY — DO NOT add to Preview scope per D-07/D-08):**
       - `NODE_VERSION` = `20`  (D-06 — matches local lockfile-generating Node 20.14.0)
       - `VITE_API_BASE_URL` = `https://boardgame-rag-prod.fly.dev`  (D-08 — exact value, NO trailing slash; Pitfall 5)
       - `VITE_SUPABASE_URL` = `<paste value from .env.prod line `VITE_SUPABASE_URL=` OR `SUPABASE_URL=`>`  (D-08 — Phase 3 prod project URL)
       - `VITE_SUPABASE_ANON_KEY` = `<paste value from .env.prod line `VITE_SUPABASE_ANON_KEY=` OR `SUPABASE_ANON_KEY=`>`  (D-08/D-09 — anon key only; service-role key MUST NOT be set anywhere on CF per D-09 + Pitfall 6)
       - **DO NOT** set `SUPABASE_SERVICE_ROLE_KEY`, `OPENROUTER_API_KEY`, `LANGSMITH_API_KEY`, `TAVILY_API_KEY`, or any non-`VITE_*` backend secret. They live exclusively in Fly secrets (Phase 4).
    7. **Save and Deploy.** First deploy auto-triggers from latest `main` commit — but our Task 1 commit is not yet pushed, so the very first deploy will be from the previous commit (without `_redirects`). That's expected and harmless: the auto-SPA fallback (CF Pages v3 default when no `404.html` exists, per RESEARCH §"Pattern 2 Note") covers deep-link refresh in the interim.
    8. **Disable Preview deploys** (D-07 + Pitfall): Project → **Settings** → **Builds & deployments** → **Configure Preview deployments** → set to **None**. This eliminates the per-PR `<hash>.boardgame-rag-prod.pages.dev` URLs that would otherwise need a CORS regex.
    9. **Push the Task 1 commit** to trigger the canonical green deploy with `_redirects` baked in:

           git push origin main

    10. **Watch the build log** at CF dashboard → boardgame-rag-prod → Deployments. The build for the just-pushed commit must reach **Success**. Estimated wall-clock: 1–3 minutes.
    11. **Confirm deploy details** in the Deployments tab:
        - Status = **Success**
        - Source = `main` at the SHA from `git rev-parse HEAD`
        - Build log shows `Detected the following tools from environment: nodejs@20.x.x` (proves NODE_VERSION env var was honored).
        - Build log shows `npm run build` invoked from `/opt/buildhome/repo/frontend` (proves Root directory was applied).

    Why this is NOT a `checkpoint:human-verify` (which only verifies after Claude automated something): there is no preceding automation in this task. CF Pages project creation is the action itself, performed by the human in the dashboard. D-01 explicitly chose this path over wrangler. Acceptance is automated *after* the manual step lands (curl checks below).
  </action>
  <verify>
    <automated>curl -fsS https://boardgame-rag-prod.pages.dev/_redirects | grep -qx '/\* /index.html 200' && curl -fsSI https://boardgame-rag-prod.pages.dev/documents | grep -iE '^content-type:[[:space:]]*text/html' && curl -fsS https://boardgame-rag-prod.pages.dev/ | grep -qE '<div id="root"' </automated>
  </verify>
  <acceptance_criteria>
    - CF Pages dashboard shows project `boardgame-rag-prod` exists with Production branch = `main`, Root directory = `frontend`, Build command = `npm run build`, Output dir = `dist`. Verified by visual inspection of Settings → Builds & deployments page.
    - CF Pages dashboard shows the four env vars listed above, all scoped to **Production**, none scoped to Preview. Verified by Settings → Environment variables page.
    - CF Pages dashboard shows latest deployment from `main` at the post-push SHA with status **Success**.
    - Preview deployments are disabled (Settings → Builds & deployments → Configure Preview deployments = None).
    - `curl -fsS https://boardgame-rag-prod.pages.dev/_redirects` returns exit 0 with body exactly `/* /index.html 200` (proves Pitfall 2 avoided — file landed in dist).
    - `curl -fsSI https://boardgame-rag-prod.pages.dev/documents` returns HTTP 200 with `content-type: text/html...` (proves SPA fallback works for a deep route — VALIDATION 5-01-06).
    - `curl -fsS https://boardgame-rag-prod.pages.dev/` returns the index HTML containing `<div id="root"` (React app root mount).
    - Maps to VALIDATION.md tasks 5-01-03, 5-01-04, 5-01-05, 5-01-06.
  </acceptance_criteria>
  <resume-signal>Reply "deploy green" with the deployed SHA, OR paste the failing build-log error. If CORS errors are observed in browser at this point that is EXPECTED and resolved by Task 3.</resume-signal>
  <done>CF Pages project live at https://boardgame-rag-prod.pages.dev, latest `main` SHA shows Success, the three curl probes above all pass, Preview deploys disabled.</done>
</task>

<task type="auto">
  <name>Task 3: Overwrite Fly CORS_ALLOWED_ORIGINS with the CF Pages prod origin</name>
  <read_first>
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-CONTEXT.md §D-11 / §D-12
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-RESEARCH.md §"Pattern 3: CORS Hand-off" + §"Pitfall 4"
    - .planning/phases/04-deploy-backend-to-fly-io/04-CONTEXT.md §D-07 / §D-08 (placeholder hand-off)
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-VALIDATION.md task 5-01-07
  </read_first>
  <files>(out-of-repo: Fly app `boardgame-rag-prod` secret store; no repo files modified)</files>
  <action>
    Capture the current CORS digest (Phase 4 baseline) before overwrite, then run the exact `flyctl secrets set` command, then capture the new digest. Single-key overwrite triggers a single Fly machine restart with the new env (verified per Fly docs in RESEARCH §Pattern 3). Order matters: this runs AFTER Task 2 confirms CF deploy is green; running it first would leave the Fly backend rejecting all valid prod origins (D-12: no localhost fallback in the prod allowlist).

    Step-by-step:

    1. Capture the pre-overwrite digest:

           flyctl secrets list -a boardgame-rag-prod | tee .planning/phases/05-deploy-frontend-to-cloudflare-pages/_cors_before.txt | grep CORS_ALLOWED_ORIGINS

       (The temp file under `.planning/.../` is for reference only; do NOT commit it — `.planning/` is not gitignored but operational scratch like this should be deleted at the end of the task.)

    2. Run the canonical overwrite (D-11 verbatim):

           flyctl secrets set CORS_ALLOWED_ORIGINS=https://boardgame-rag-prod.pages.dev -a boardgame-rag-prod

       Expected stdout includes `Secrets are staged for the next deployment` followed by `Updating existing machines in 'boardgame-rag-prod' with rolling strategy` — the machine restart is automatic for `secrets set` (unlike `secrets stage` which would defer it).

    3. Wait ~30s for the machine to restart, then capture the post-overwrite digest:

           flyctl secrets list -a boardgame-rag-prod | grep CORS_ALLOWED_ORIGINS

       The DIGEST column value MUST differ from step 1's value. The actual secret value is never shown by `flyctl secrets list` (Fly only stores digests for non-Fly-managed secrets) — digest delta proves the value changed, even though we can't read it back.

    4. Verify the backend honors the new origin via a non-CORS-preflight request (server-to-server, bypasses browser CORS but proves the backend is up post-restart):

           curl -fsS https://boardgame-rag-prod.fly.dev/api/health

       Expected 200 with body `{"status":"ok"}` (or whatever the existing handler returns — Phase 4 verified this).

    5. Delete the scratch file:

           rm .planning/phases/05-deploy-frontend-to-cloudflare-pages/_cors_before.txt

    DO NOT add `localhost:5173` or any comma-separated fallback to the prod value (D-12 forbids it; backend's dev fallback handles local dev when CORS_ALLOWED_ORIGINS is *unset*, which only happens on the developer laptop — not on Fly).
  </action>
  <verify>
    <automated>flyctl secrets list -a boardgame-rag-prod | grep -E '^CORS_ALLOWED_ORIGINS[[:space:]]+[a-f0-9]{8,}' && curl -fsS https://boardgame-rag-prod.fly.dev/api/health</automated>
  </verify>
  <acceptance_criteria>
    - `flyctl secrets list -a boardgame-rag-prod` shows `CORS_ALLOWED_ORIGINS` with a digest column populated (column 2).
    - The digest in step 3 of action differs from the digest captured in step 1 (the Phase 4 placeholder digest).
    - `curl -fsS https://boardgame-rag-prod.fly.dev/api/health` returns HTTP 200 within 30s of the secrets set command (proves machine restarted cleanly with new env).
    - No `_cors_before.txt` file remains under `.planning/phases/05-deploy-frontend-to-cloudflare-pages/`.
    - Maps to VALIDATION.md task 5-01-07.
  </acceptance_criteria>
  <done>Fly secret CORS_ALLOWED_ORIGINS overwritten to the CF Pages origin, digest changed, backend healthy after restart.</done>
</task>

<task type="auto">
  <name>Task 4: Re-run SEC-07 prod-bundle leak grep against the deployed CF URL</name>
  <read_first>
    - .planning/phases/01-secrets-repo-hygiene/01-CONTEXT.md §SEC-07 grep guard (D-12)
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-CONTEXT.md §D-09
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-RESEARCH.md §"Pitfall 6: Anon Key Confusion"
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-VALIDATION.md task 5-01-08
  </read_first>
  <files>(verification only; no repo files modified)</files>
  <action>
    Phase 1 SEC-07 grep guard runs against locally-built bundles. This task re-runs the same guard against the *actually-deployed* CF Pages bundle to prove no secret slipped in via the dashboard env-var paste in Task 2 (e.g. accidentally pasting service-role key into VITE_SUPABASE_ANON_KEY, or copy-paste error pulling in OPENROUTER_API_KEY).

    Step-by-step:

    1. Discover the hashed asset filename (Vite emits `dist/assets/index-<hash>.js`):

           INDEX_HTML=$(curl -fsS https://boardgame-rag-prod.pages.dev/)
           ASSET_PATH=$(echo "$INDEX_HTML" | grep -oE '/assets/index-[A-Za-z0-9_-]+\.js' | head -1)
           echo "Asset path: $ASSET_PATH"

    2. Fetch the bundle and grep for the canonical leak patterns (matches Phase 1 SEC-07 guard regex):

           curl -fsS "https://boardgame-rag-prod.pages.dev${ASSET_PATH}" \
             | grep -Ei 'service_role|sk-proj-|sk-or-|sb_secret_' \
             && echo "LEAK DETECTED" && exit 1 \
             || echo "SEC-07 grep guard: clean (no matches)"

       The exit-1 on match inverts grep's normal exit semantics so a leak fails this task loudly. Zero matches = `grep` exits 1 = `||` branch runs = task succeeds.

    3. (Belt-and-suspenders) Confirm the anon key in the bundle decodes as `role: anon` (NOT `role: service_role`). The anon key is a JWT — extract any `eyJ`-prefixed token from the bundle and decode the payload:

           TOKEN=$(curl -fsS "https://boardgame-rag-prod.pages.dev${ASSET_PATH}" | grep -oE 'eyJ[A-Za-z0-9_-]+\.eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+' | head -1)
           if [ -n "$TOKEN" ]; then
             PAYLOAD=$(echo "$TOKEN" | cut -d. -f2)
             # base64url decode (pad if needed)
             case $((${#PAYLOAD} % 4)) in 2) PAYLOAD="${PAYLOAD}==";; 3) PAYLOAD="${PAYLOAD}=";; esac
             echo "$PAYLOAD" | tr '_-' '/+' | base64 -d 2>/dev/null | grep -E '"role"[[:space:]]*:[[:space:]]*"anon"' && echo "ANON OK" || (echo "FAIL: token role is NOT anon" && exit 1)
           else
             echo "No JWT in bundle (acceptable — Supabase client may build it from anon key string at runtime)"
           fi

       This is defensive against Pitfall 6.

    DO NOT log the full bundle output anywhere persistent — it can contain inline anon JWTs. The grep above streams and discards.
  </action>
  <verify>
    <automated>set -e; INDEX_HTML=$(curl -fsS https://boardgame-rag-prod.pages.dev/); ASSET_PATH=$(echo "$INDEX_HTML" | grep -oE '/assets/index-[A-Za-z0-9_-]+\.js' | head -1); test -n "$ASSET_PATH"; ! curl -fsS "https://boardgame-rag-prod.pages.dev${ASSET_PATH}" | grep -Ei 'service_role|sk-proj-|sk-or-|sb_secret_'</automated>
  </verify>
  <acceptance_criteria>
    - Asset discovery succeeds: `curl https://boardgame-rag-prod.pages.dev/` returns HTML containing a `/assets/index-<hash>.js` reference.
    - Bundle leak grep returns ZERO matches for the regex `service_role|sk-proj-|sk-or-|sb_secret_` (Phase 1 SEC-07 canonical pattern set).
    - If a JWT is found inline in the bundle, its decoded payload contains `"role":"anon"` (not `service_role`).
    - Maps to VALIDATION.md task 5-01-08 and re-validates Phase 1 SEC-07 against the live deploy (not the local build).
  </acceptance_criteria>
  <done>Live CF Pages bundle contains no backend secrets; SEC-07 guard re-passes against the deployed surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual D-13 5-step browser verification</name>
  <read_first>
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-CONTEXT.md §D-13
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-RESEARCH.md §"Browser Verification Sequence" + §"Pitfall 7: Browser Cache Hides New Deploy"
    - .planning/phases/05-deploy-frontend-to-cloudflare-pages/05-VALIDATION.md task 5-01-09
    - CLAUDE.md §Test Credentials (ragtest1@gmail.com / testpass123)
  </read_first>
  <files>(no repo files modified — manual browser verification only)</files>
  <action>This is a checkpoint:human-verify task. Claude does NOT automate browser interaction this phase (D-14: no scripted CF smoke). Surface the <how-to-verify> 5-step checklist verbatim to the user, wait for the resume-signal, then record the captured results in the per-plan SUMMARY. The "what was built" by Tasks 1–4 is described in <what-built>; this task's "action" is presenting the checklist and recording the verdict.</action>
  <verify>
    <automated>echo "Manual checkpoint — see how-to-verify block; automated phase-gate checks already passed in Tasks 1-4 (build, _redirects curl, /documents curl, CORS digest, bundle leak grep)."</automated>
  </verify>
  <done>User has reported PASS/FAIL/deferred for each of the 5 D-13 steps; results recorded in 05-01-SUMMARY.md.</done>
  <what-built>
    Tasks 1–4 produced: a CF Pages project at https://boardgame-rag-prod.pages.dev serving the Vite SPA with `_redirects` for SPA fallback and `VITE_API_BASE_URL` baked to the Fly URL; an updated Fly CORS allowlist permitting the CF origin; and a verified-clean bundle. Manual browser verification is the final gate (D-14: no scripted CF smoke this phase; D-13 is the canonical end-to-end check).
  </what-built>
  <how-to-verify>
    Open a fresh **incognito / private** browser window (avoids Pitfall 7: cached `index.html`). Then perform exactly these five steps in order:

    1. **Hard-load** `https://boardgame-rag-prod.pages.dev`. The login page renders. Open DevTools → **Console** tab — there must be **zero red error rows**. (Yellow warnings are tolerable.)

    2. **DevTools → Network tab**, then click any UI element that issues an API call (e.g. tab to `/documents` route via the URL bar). Confirm at least one request whose **Request URL** column begins with `https://boardgame-rag-prod.fly.dev/api/...` — NOT `https://boardgame-rag-prod.pages.dev/api/...` (same-origin would mean Pitfall 3 — VITE_API_BASE_URL didn't bake in).

    3. **Log in** with the seeded test user from CLAUDE.md: `ragtest1@gmail.com` / `testpass123`. (NOTE: if Phase 6 Auth redirect URLs aren't yet configured, login may fail with a Supabase redirect error — that is EXPECTED at this phase boundary and does NOT fail this verification. Document the exact error if seen and proceed to step 4 by manually navigating to `/documents`.)

    4. Navigate to `https://boardgame-rag-prod.pages.dev/documents` (URL bar, not in-app nav). Then **Ctrl+Shift+R** (Cmd+Shift+R on macOS) to hard-refresh. The SPA must re-render `/documents` (not a CF 404 page). This proves `_redirects` works at the edge.

    5. (Only attempt if step 3 login succeeded.) Send a chat message. The Network tab shows a request to `https://boardgame-rag-prod.fly.dev/api/chat` of type `eventsource` (or `fetch` with `Content-Type: text/event-stream` in the response headers), and chunks arrive incrementally in the `Response` panel. NO red CORS errors in console.

    Capture results in the resume signal. If step 3 fails on Auth-redirect grounds and steps 1/2/4 pass, the phase is acceptable to ship — Phase 6 owns SEC-01 Auth redirect config.
  </how-to-verify>
  <acceptance_criteria>
    - Step 1: Login page renders, console has zero red errors. (PASS / FAIL)
    - Step 2: At least one Network-tab request URL starts with `https://boardgame-rag-prod.fly.dev/api/`. (PASS / FAIL)
    - Step 4: Hard-refresh of `/documents` renders SPA, not CF 404. (PASS / FAIL)
    - Step 5 (conditional on Step 3): SSE chunks stream from Fly without CORS console error. (PASS / FAIL / N/A-deferred-to-Phase-6)
    - Maps to VALIDATION.md task 5-01-09 and ROADMAP Phase 5 success criterion #4.
  </acceptance_criteria>
  <resume-signal>
    Reply with one of:
    - "all 5 PASS" — phase verified, ready to mark Phase 5 complete.
    - "1/2/4 PASS, 3/5 deferred to Phase 6 (auth redirect)" — acceptable, document in SUMMARY.
    - "step N FAIL: <error / screenshot>" — surface the failure for diagnosis (likely CORS still propagating, env var typo, or _redirects missing).
  </resume-signal>
</task>

</tasks>

<verification>
Phase-wide verification (after all 5 tasks):

1. Files committed: exactly two — `frontend/public/_redirects` and `frontend/.nvmrc` — plus zero source code changes (Phase 1 already wired the URL prefix).
2. CF Pages dashboard shows project `boardgame-rag-prod`, latest `main` deployment = Success, Preview deploys = None, four Production-scope env vars set.
3. `curl -fsS https://boardgame-rag-prod.pages.dev/_redirects` returns the rewrite rule verbatim.
4. `curl -fsSI https://boardgame-rag-prod.pages.dev/documents` returns 200 + `text/html`.
5. `flyctl secrets list -a boardgame-rag-prod | grep CORS_ALLOWED_ORIGINS` shows a digest different from the Phase 4 baseline.
6. Bundle leak grep against the deployed `/assets/index-*.js` returns zero matches for `service_role|sk-proj-|sk-or-|sb_secret_`.
7. Manual D-13 5-step browser checklist results captured in SUMMARY (steps 3/5 may legitimately defer to Phase 6 SEC-01).

Aligned with VALIDATION.md tasks 5-01-01 through 5-01-09.
</verification>

<success_criteria>
Phase 5 (DEPLOY-05) is complete when:

- [x] `frontend/public/_redirects` exists with `/* /index.html 200`.
- [x] `frontend/.nvmrc` exists with `20` (Claude's discretion artifact per D-06).
- [x] CF Pages project `boardgame-rag-prod` is connected to the GitHub repo with build cmd `npm run build`, output `dist`, root `frontend`, latest `main` deploy = Success.
- [x] CF Pages env vars (Production scope only): `NODE_VERSION=20`, `VITE_API_BASE_URL=https://boardgame-rag-prod.fly.dev` (no trailing slash), `VITE_SUPABASE_URL=<prod>`, `VITE_SUPABASE_ANON_KEY=<prod>`. Preview scope empty.
- [x] Preview deploys disabled.
- [x] Public URL https://boardgame-rag-prod.pages.dev serves SPA; deep-link refresh on `/documents` returns SPA (curl 200 text/html).
- [x] Fly secret `CORS_ALLOWED_ORIGINS` overwritten to `https://boardgame-rag-prod.pages.dev` (digest changed from Phase 4 placeholder).
- [x] Live bundle grep returns zero matches for backend-secret patterns (SEC-07 re-passes against deployed surface).
- [x] Manual D-13 browser checklist: steps 1, 2, 4 PASS at minimum (step 3 login + step 5 SSE may defer to Phase 6 SEC-01).
</success_criteria>

<output>
After completion, create `.planning/phases/05-deploy-frontend-to-cloudflare-pages/05-01-SUMMARY.md` capturing:
- The deployed CF Pages URL + deploy SHA + green status timestamp
- Pre/post Fly CORS digest values
- Bundle leak grep result (clean / leak found)
- D-13 browser checklist results (PASS / FAIL / deferred per step)
- Any deviations from CONTEXT.md decisions (expected: none)
</output>
