---
phase: 999.1-chat-empty-state-ux
verified: 2026-06-24T12:35:00Z
status: passed
score: 15/15 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 999.1: Chat Empty-State UX Verification Report

**Phase Goal:** Make the first chat message "just work": auto-create a thread on send for any null-thread state (cold start AND after deleting the active thread), and replace the bland text-only empty-state with a welcoming headline + 2–3 tappable board-game example prompts. Frontend-only UX fix; the "block input until + New Chat" alternative was rejected (D-01).
**Verified:** 2026-06-24T12:35:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

ROADMAP `success_criteria` is empty for this backlog phase (Requirements: TBD). Per Step 2c, must-haves are taken from the merged `must_haves.truths` of the three PLAN frontmatters (the phase contract derived from CONTEXT decisions D-01..D-04 + the approved UI-SPEC). All 15 deduplicated truths verified below.

### Observable Truths

| #   | Truth (source plan)                                                                                                      | Status      | Evidence |
| --- | ----------------------------------------------------------------------------------------------------------------------- | ----------- | -------- |
| 1   | A frontend component-test runner exists and runs via an npm script (P01)                                                | ✓ VERIFIED  | `npm run test` → vitest 4.1.9, **23/23 passed across 4 files**; `frontend/vitest.config.ts` (jsdom, globals, setupFiles); `package.json` script `"test": "vitest run"`. |
| 2   | sendMessage accepts an explicit threadId and sends with it even when the closured threadId is null (P01)                 | ✓ VERIFIED  | `useChat.ts:94` `const effectiveThreadId = opts?.threadId ?? threadId`; both SSE URLs use `effectiveThreadId` (`:127`, `:128`). `useChat.test.tsx` proves explicit-id send on `useChat(null)`. |
| 3   | loadMessages does not wipe optimistic/streaming messages while a send is in flight (P01)                                 | ✓ VERIFIED  | In-stream guard now lives in `isStreamingRef` + `skipNextLoadRef` one-shot (`useChat.ts:40,46,68-82`); `ChatPage.test.tsx` Test 1 asserts the optimistic bubble survives the `setActiveThreadId` switch; stream-end regression (Test 7) green. |
| 4   | D-02: welcoming empty-state with 2–3 tappable example-prompt chips replaces the text-only block (P02)                    | ✓ VERIFIED  | `ChatContainer.tsx:43-69`: `<h2 text-2xl font-bold>` headline + `<p text-sm text-gray-400>` sub-line + 3-chip cluster, replacing the old `<p>`. |
| 5   | When messages.length === 0, the pane shows headline + sub-line + 2–3 example-prompt chips (P02)                          | ✓ VERIFIED  | Entire block wrapped in `{messages.length === 0 && (…)}` (`:43`); `EXAMPLE_PROMPTS` (`:32-36`) maps to 3 `<button>` chips. `ChatContainer.test.tsx` Test 1 asserts all three by role+accessible-name. |
| 6   | Tapping a chip calls onSend with that chip's exact text, routing through the same send path (P02)                        | ✓ VERIFIED  | `ChatContainer.tsx:55` `onClick={() => onSend(q)}`; `ChatContainer.test.tsx` Test 2 asserts `onSend` called once with exact chip text. `ChatPage.test.tsx` Test 2 drives chip-tap → auto-create end-to-end. |
| 7   | Anonymous users additionally see a D&D-sample hint; authed users do not; both share one chip layout (P02)               | ✓ VERIFIED  | `ChatContainer.tsx:62-66` hint gated on `{isAnon && (…)}`; headline/sub-line/chips render unconditionally. `ChatContainer.test.tsx` Test 3 asserts anon-hint reconciliation (present for anon, absent for authed, chips shared). |
| 8   | The empty-state lives strictly inside the messages.length === 0 guard so it vanishes on first bubble (P02)              | ✓ VERIFIED  | Single conditional wrapper at `:43`. `ChatContainer.test.tsx` Test 4 asserts a non-empty `messages` array hides the headline/chips. |
| 9   | D-01: sending with no active thread auto-creates (POST /api/threads {}) then sends — zero clicks, composer always enabled (P03) | ✓ VERIFIED  | `ChatPage.tsx:66-95` null-branch POSTs untitled, sets target, sends with explicit id. `ChatInput` mount stays `disabled={isStreaming}` only (`ChatContainer.tsx:83`) — never gated on thread existence. `ChatPage.test.tsx` Test 1 asserts the full chain. |
| 10  | D-04: auto-create fires for every activeThreadId === null state (cold start AND post-delete) via one code path (P03)     | ✓ VERIFIED  | Single `if (targetId === null)` branch (`ChatPage.tsx:67`); `handleDeleteThread` sets `activeThreadId=null` (`:57-59`) re-entering it. `ChatPage.test.tsx` Test 3 asserts post-delete parity (fresh POST). |
| 11  | The optimistic user bubble renders on the very first send (no silent no-op) (P03)                                        | ✓ VERIFIED  | `ChatPage.test.tsx` Test 1: `await screen.findByText('How do you win Azul?')` after first send from cold null state. The root-cause `if(!threadId) return` no-op is defeated by the explicit-id path. |
| 12  | New thread is created untitled; loadThreads() after send surfaces the backend-generated title (D-03) (P03)              | ✓ VERIFIED (jsdom); live title round-trip = documented external dependency | POST body asserted `JSON.stringify({})` (`ChatPage.test.tsx` Test 4); `loadThreads()` fires post-send (`ChatPage.tsx:97`, asserted Test 1). **Live backend titling not observed — blocked by a SEPARATE out-of-scope `:free` OpenRouter provider error (D-999.1-LLM-A), which affects ANY chat on ANY thread; user explicitly accepted the phase on this basis.** Frontend wiring (untitled create + loadThreads refresh) fully verified. |
| 13  | A failed create surfaces feedback (toast + Sentry), never a silent dead-end (P03)                                       | ✓ VERIFIED  | `ChatPage.tsx:82-89` catch → `Sentry.captureException` + `showToast(…, 'error')` + `return` before `sendMessage`. `ChatPage.test.tsx` create-failure test asserts no `apiStream` call + toast/Sentry. |
| 14  | Closure-proof explicit-id send is used (no flushSync, no create-in-hook) (P03 success criteria)                        | ✓ VERIFIED  | `ChatPage.tsx:95` `sendMessage(content, { threadId: targetId as string })`; `grep -c flushSync` = 0; create lives in `handleSend`, not `useChat`. |
| 15  | CR-01 (mid-stream thread switch) closed: switching threads mid-stream aborts the in-flight stream and loads the target thread | ✓ VERIFIED  | `useChat.ts:65-82` abort-on-switch load effect (`abortRef.current?.abort()` + `void loadMessages()` on `[threadId]` change); WR-01 ref guard (`:98`), WR-05 retry threadId (`:307`). Dedicated regression `useChat.test.tsx` Test 7 asserts the abort + new-thread load; green. Fixed in commit `b132556` during verification. |

**Score:** 15/15 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `frontend/vitest.config.ts` | jsdom test config wired to setup file | ✓ VERIFIED | `environment: 'jsdom'`, `globals: true`, `setupFiles`. Runner boots; 23/23 green. |
| `frontend/src/test/setup.ts` | jest-dom matchers + crypto.randomUUID polyfill | ✓ VERIFIED | Present; smoke test green. (IN-04 stale line-number comment is cosmetic info, not a defect.) |
| `frontend/src/test/utils.tsx` | renderWithProviders + apiFetch/apiStream/SSE mocks | ✓ VERIFIED | Exports `renderWithProviders`, `ProvidersWrapper`, `mockSSEResponse`, mock factories; consumed by all 3 test files. |
| `frontend/src/hooks/useChat.ts` | closure-proof send + load guard + CR-01 abort-on-switch | ✓ VERIFIED (WIRED, data flows) | `opts?.threadId ?? threadId`; `isStreamingRef` guard; `[threadId]`-only abort+load effect. Imported/used by `ChatPage.tsx`. |
| `frontend/src/hooks/useChat.test.tsx` | behavioral tests for explicit-id + no-clobber + CR-01 | ✓ VERIFIED | 7 `it()` blocks incl. Test 7 (mid-stream switch). All green. |
| `frontend/src/components/ChatContainer.tsx` | empty-state headline + sub-line + chips | ✓ VERIFIED (WIRED, data flows) | Static `EXAMPLE_PROMPTS` → live `onSend` prop; `messages` rendered from `useChat`. No `bg-blue-600` (0), no `font-medium` (0). |
| `frontend/src/components/ChatContainer.test.tsx` | chip render / tap→onSend / anon-hint / gating | ✓ VERIFIED | 5 `it()` blocks. All green. |
| `frontend/src/pages/ChatPage.tsx` | handleSend auto-create branch + create-failure | ✓ VERIFIED (WIRED, data flows) | Null-branch POST → setThreads/setActiveThreadId → `sendMessage(content,{threadId})` → `loadThreads()`. Wired into `<ChatContainer onSend={handleSend}>`. |
| `frontend/src/pages/ChatPage.test.tsx` | cold-start / chip / post-delete / untitled / failure | ✓ VERIFIED | 8 `it()` blocks driving the real onSend→handleSend→sendMessage chain. All green. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `ChatPage.handleSend` null-branch | `POST /api/threads {}` | `apiFetch` reusing handleNewThread pattern | ✓ WIRED | `ChatPage.tsx:71-74` |
| `ChatPage.handleSend` | `sendMessage(content,{threadId:targetId})` | explicit id defeats stale closure | ✓ WIRED | `ChatPage.tsx:95` |
| `ChatPage.handleSend` | `loadThreads()` | post-send title refresh (D-03) | ✓ WIRED | `ChatPage.tsx:97` |
| `useChat.sendMessage` | `effectiveThreadId = opts?.threadId ?? threadId` | guard at former line 55 | ✓ WIRED | `useChat.ts:94,98` |
| chip `<button>` | `onSend(chipText)` | onClick | ✓ WIRED | `ChatContainer.tsx:55` |
| empty-state render | `messages.length === 0` | conditional wrapper | ✓ WIRED | `ChatContainer.tsx:43` |
| `ChatContainer` `onSend` prop | `ChatPage.handleSend` | JSX prop wiring | ✓ WIRED | `ChatPage.tsx:120` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| ChatContainer | `messages` | `useChat` state, populated by real `apiStream` SSE + `apiFetch('/api/threads/{id}')` | Yes (live API; mocked in tests) | ✓ FLOWING |
| ChatContainer | example-prompt chips | static `EXAMPLE_PROMPTS` const → live `onSend` prop | N/A (intentional static content routed to live handler) | ✓ FLOWING |
| ChatPage | `threads` | `loadThreads()` → `apiFetch('/api/threads')` | Yes | ✓ FLOWING |
| ChatPage | new `targetId` | server-issued `POST /api/threads` response `.id` (never client-minted) | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full frontend test suite | `npm run test` | 4 files, **23 tests passed** | ✓ PASS |
| Type-check clean | `npx tsc -b --noEmit` | exit 0 | ✓ PASS |
| Lint (phase files) | `npx eslint useChat.ts ChatPage.tsx ChatContainer.tsx` | 1 error (pre-existing `set-state-in-effect` on `loadThreads()` effect, ChatPage.tsx:42) | ✓ PASS (pre-existing, not phase-introduced — see Anti-Patterns) |
| Live cold-start + post-delete first-send (no silent no-op) | Human-verify (Plan 03 Task 3) | APPROVED — auto-create + optimistic bubble + persisting error/Retry surface confirmed live | ✓ PASS |
| Live streamed assistant answer + backend title | Human-verify (Plan 03 Task 3) | Blocked by separate `:free` OpenRouter provider error (affects any chat/any thread) | ? SKIP — documented external dependency (D-999.1-LLM-A), user accepted |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` probes; this is a frontend-only UX phase whose verification is the vitest component suite (run above, 23/23 green).

### Requirements Coverage

Backlog phase; REQUIREMENTS.md (v1.2 BYOK/SEC set) does not cover this phase. Coverage is against the derived CONTEXT decisions D-01..D-04 + the approved UI-SPEC, as instructed.

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| D-01 | 01, 03 | Auto-create thread on first send; composer always enabled | ✓ SATISFIED | Truths 2, 9, 11, 14 |
| D-02 | 02 | Welcoming empty-state with tappable example-prompt chips | ✓ SATISFIED | Truths 4–8 |
| D-03 | 01, 03 | Untitled create; backend titles from first message; no placeholder | ✓ SATISFIED (frontend wiring); live titling = D-999.1-LLM-A external dep | Truth 12 |
| D-04 | 03 | One null-thread code path (cold start AND post-delete) | ✓ SATISFIED | Truth 10 |

No orphaned requirements: REQUIREMENTS.md maps no IDs to this phase (it tracks a different v1.2 set).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| ChatPage.tsx | 42 | `react-hooks/set-state-in-effect` on `loadThreads()` initial-load effect | ℹ️ Info | PRE-EXISTING on HEAD (confirmed: present at commit `ae23700`, before any 999.1 code edit). Idiomatic initial-load-on-mount; does not break behavior. Tracked as D-999.1-LINT-A. Not phase-introduced, not a gap. |
| useChat.ts / ChatPage.tsx | 113, 262 / 70 | "placeholder" in comments | ℹ️ Info | False positive — refers to the real optimistic assistant bubble (streamed into) and a comment explaining D-03 ("no placeholder New Chat persisted"). Not stubs; no hardcoded empty data flows to UI. |

**Debt markers (TBD/FIXME/XXX):** none found in any modified file. No BLOCKER-class markers.

### Human Verification Required

None outstanding. The blocking human-verify checkpoint (Plan 03 Task 3) was **already executed live by the user and APPROVED**. The deliverable was confirmed working live: a first message on a null-thread state no longer silently no-ops — it auto-creates a thread, renders the optimistic user bubble immediately, and surfaces a persistent error/Retry. The single item not confirmable live (real streamed assistant answer + backend-generated title) is blocked by a SEPARATE, documented, out-of-scope provider error (`:free` OpenRouter model returns an in-band SSE error; affects any chat on any thread, proven by orchestrator probes to be a runtime provider/rate-limit issue, not a phase defect). The user explicitly accepted the phase on this basis.

### Gaps Summary

No gaps. All 15 must-haves are verified against the actual codebase, not merely the SUMMARY narrative:

- The closure-proof send (`opts?.threadId ?? threadId`), the auto-create branch, the untitled-create + loadThreads refresh, the chip empty-state, the anon-hint reconciliation, and the create-failure feedback are all present in source and covered by passing behavioral tests that drive the real onSend→handleSend→sendMessage chain in jsdom.
- The critical code-review finding (CR-01 mid-stream thread switch) and warnings WR-01/WR-05 were genuinely closed in commit `b132556` — verified directly in `useChat.ts` source AND backed by a dedicated regression test (`useChat.test.tsx` Test 7), which passes.
- Full suite 23/23 green; tsc exit 0; the only lint error is a pre-existing, phase-external `set-state-in-effect` on the initial threads-load effect (D-999.1-LINT-A), independently confirmed present before any phase edit.
- Two out-of-scope runtime follow-ups (`:free` LLM model failure D-999.1-LLM-A; `/api/demo/bootstrap` anon-demo failure D-999.1-DEMO-A) are recorded and are NOT phase gaps — both affect surfaces outside this frontend-only empty-state phase.

The two truths that depend on the live streamed answer/title (Truth 12 happy-path titling) are honestly marked "verified via jsdom; live LLM round-trip blocked by a separate documented provider error" — not falsely green. The frontend wiring they depend on (untitled create + loadThreads refresh) is fully verified.

---

_Verified: 2026-06-24T12:35:00Z_
_Verifier: Claude (gsd-verifier)_
