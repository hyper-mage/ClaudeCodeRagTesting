---
phase: 10-oauth-pkce-backend-exchange-frontend-connect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240301000028_add_connected_at_to_user_api_keys.sql
autonomous: true
requirements: [KEY-03, KEY-04]
must_haves:
  truths:
    - "user_api_keys has a connected_at TIMESTAMPTZ column on the live dev Supabase project"
    - "The Phase 9 SQL-tool lockdown (REVOKE + FROM-allowlist) is unchanged — user_api_keys is still unreadable by the Text-to-SQL tool"
  artifacts:
    - path: "supabase/migrations/20240301000028_add_connected_at_to_user_api_keys.sql"
      provides: "Additive ALTER TABLE adding connected_at TIMESTAMPTZ (nullable)"
      contains: "ADD COLUMN IF NOT EXISTS connected_at"
  key_links:
    - from: "migration 028"
      to: "user_api_keys table (live dev project)"
      via: "supabase db push"
      pattern: "ALTER TABLE user_api_keys"
---

<objective>
Add the `connected_at TIMESTAMPTZ` column to `user_api_keys` via a new additive forward migration (028) and apply it to the DEV Supabase project. This column backs the "Connected since {date}" display on `/settings` (KEY-03) and re-stamps on reconnect (KEY-04). The masked-label display column (`key_label`) already exists from Phase 9 migration 025 — only `connected_at` is new.

Purpose: The backend `GET /api/keys/status` and exchange upsert (Plan 02) read/write `connected_at`. Without the column on the live dev DB, build + type checks would pass but the status read would fail at runtime — this is the false-positive guard the schema-push requirement calls out.
Output: One new migration file applied live to dev; Phase 9 lockdown provably intact.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-RESEARCH.md
@.planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-PATTERNS.md
@supabase/migrations/20240301000025_create_user_api_keys.sql
@supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql
@supabase/migrations/20240301000027_harden_sql_allowlist_and_rls.sql

<interfaces>
Live user_api_keys columns after Phase 9 (migration 025, read directly):
  user_id UUID PRIMARY KEY, provider TEXT, encrypted_key TEXT, key_version INTEGER,
  key_label TEXT (nullable — the masked-tail display column, REUSE this, do NOT re-add),
  created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
  -- NO connected_at column. That is the only thing this migration adds.
SEC-02 lockdown (do NOT regress): migration 025 line 53 `REVOKE SELECT ON user_api_keys FROM authenticated`;
  sql_service.py ALLOWED_SQL_TABLES = frozenset({"threads","messages","documents","document_chunks"}).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the additive connected_at migration (028)</name>
  <read_first>
    - supabase/migrations/20240301000025_create_user_api_keys.sql (the table being altered; column list + the REVOKE on line 53)
    - supabase/migrations/20240301000027_harden_sql_allowlist_and_rls.sql (latest applied migration — confirms 028 is the next free number)
    - .planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-PATTERNS.md (§"supabase/migrations/...028" — the exact additive ALTER pattern + hard constraints)
  </read_first>
  <action>
    Create `supabase/migrations/20240301000028_add_connected_at_to_user_api_keys.sql`. It contains a single additive statement: `ALTER TABLE user_api_keys ADD COLUMN IF NOT EXISTS connected_at TIMESTAMPTZ;` (nullable, no default). Add a header comment block (matching the 025 comment style) stating: Phase 10, backs "Connected since {date}" (KEY-03), set EXPLICITLY in the exchange upsert because Postgres ON CONFLICT DO UPDATE skips column defaults, and that `key_label` already exists from 025 (do NOT re-add). Do NOT add any GRANT, do NOT touch RLS policies, do NOT touch the FROM-allowlist, do NOT recreate the table, do NOT edit migrations 025/026/027. This migration is additive-only per the schema-push requirement.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS connected_at" "supabase/migrations/20240301000028_add_connected_at_to_user_api_keys.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20240301000028_add_connected_at_to_user_api_keys.sql` exists.
    - File contains the literal string `ADD COLUMN IF NOT EXISTS connected_at` and the type `TIMESTAMPTZ`.
    - File contains NO `GRANT`, NO `CREATE POLICY`, NO `DROP`, NO `CREATE TABLE`, NO reference to `ALLOWED_SQL_TABLES` (grep for each returns 0 — filter comments with `grep -v '^--'` first).
    - Migrations 025, 026, 027 are byte-for-byte unchanged (git diff shows only the new 028 file added).
  </acceptance_criteria>
  <done>Migration 028 written, additive-only, next free number, Phase 9 migrations untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 028 to the DEV Supabase project</name>
  <read_first>
    - .planning/STATE.md (Decisions — Phase 9 D-03 dual-env discipline; the migration-history-repair lesson for 025/026)
    - .planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-RESEARCH.md (§Runtime State Inventory — dev project is `ntkkmljbariflblldmha`, 025/026/027 already LIVE)
  </read_first>
  <action>
    Apply migration 028 to the DEV Supabase project ONLY (the `.env` project per the dual-env discipline — prod `.env.prod` is applied at the deploy step in a later phase, mirroring Phase 9 D-03). First attempt the non-interactive push: ensure `SUPABASE_ACCESS_TOKEN` is set in the environment, then run `supabase db push` (linked to the dev project). If `db push` replays old migrations or errors "already exists" / migration-history mismatch, repair the prior applied range first with `supabase migration repair --status applied <version-range>` (per project memory `reference_supabase_migration_history_repair`), then re-run `supabase db push`. After applying, capture the dev project ref and the applied-migration list as evidence in the SUMMARY. This task is mandatory — the build + type checks pass without it (types are not DB-derived here), so verification would be a false positive without the live column.
  </action>
  <what-built>
    Migration 028 (additive `connected_at` column) written and ready to apply to the live dev Supabase project.
  </what-built>
  <how-to-verify>
    1. Confirm the CLI is linked to the DEV project (the `.env` project, ref `ntkkmljbariflblldmha`), NOT prod.
    2. Run the push and confirm 028 applies cleanly (repair the history range first if it replays/errors).
    3. Confirm the live `user_api_keys` table now has a `connected_at` column — run a probe SELECT of `information_schema.columns` via the Supabase SQL editor or `supabase db` and check `connected_at` is present with type `timestamp with time zone`.
    4. Confirm the SEC-02 lockdown is intact: a `select * from user_api_keys` issued through the `authenticated` role (the SQL-tool path) is still rejected (REVOKE unchanged). The Phase 9 test `test_sql_keys_lockdown.py` must stay green.
  </how-to-verify>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_sql_keys_lockdown.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `connected_at` column is present on `user_api_keys` in the live DEV project (type `timestamp with time zone`, nullable).
    - `supabase db push` reports 028 applied with no errors (history repaired first if needed).
    - `pytest tests/test_sql_keys_lockdown.py -x` exits 0 (Phase 9 lockdown not regressed).
    - SUMMARY records the dev project ref and confirms prod was NOT touched (deferred to deploy step per D-03).
  </acceptance_criteria>
  <resume-signal>Type "applied" once 028 is live on the dev project and the lockdown test is green, or describe the push error.</resume-signal>
  <done>Migration 028 is live on the dev Supabase project; `connected_at` column exists; Phase 9 SQL-tool lockdown verified intact; prod deferred to deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Text-to-SQL tool (`authenticated` role) → DB | Prompt-injected SQL runs as `authenticated`; must NOT reach `user_api_keys` |
| Migration author → live DB schema | A schema change must not relax the Phase 9 lockdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure / Elevation | `user_api_keys` exposed to SQL tool via a relaxed grant | mitigate | Migration 028 is additive-only — NO `GRANT SELECT`, NO RLS edit, NO allowlist edit. Acceptance criteria grep for `GRANT`/`CREATE POLICY` (must be 0). `test_sql_keys_lockdown.py` must stay green after push. (Pitfall 6) |
| T-10-02 | Tampering | Editing already-applied migrations 025/026/027 instead of a forward migration | mitigate | Forward migration 028 only; acceptance criterion asserts 025/026/027 are byte-for-byte unchanged (git diff). |
</threat_model>

<verification>
- `supabase/migrations/20240301000028_add_connected_at_to_user_api_keys.sql` exists and is additive-only.
- Migration 028 applied to the live DEV Supabase project; `connected_at` column present.
- `cd backend && venv/Scripts/python -m pytest tests/test_sql_keys_lockdown.py -x` exits 0.
- `git diff` shows migrations 025/026/027 unchanged.
</verification>

<success_criteria>
- A new additive migration adds `connected_at TIMESTAMPTZ` to `user_api_keys`.
- The column is live on the dev Supabase project (backs the "Connected since" status read in Plan 02).
- The Phase 9 SQL-tool lockdown is provably intact (lockdown test green, no GRANT/RLS/allowlist change).
</success_criteria>

<output>
After completion, create `.planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-01-SUMMARY.md` (record dev project ref, confirm prod deferred, confirm lockdown intact).
</output>
