---
phase: 15-options-ui-capstone-demo-gating
plan: 09
type: execute
wave: 1
depends_on: []
files_modified: [backend/routers/chat.py, backend/tests/test_key_model_resolution.py]
autonomous: false
gap_closure: true
requirements: [SEC-03, DEMO-01]

must_haves:
  truths:
    - "In demo mode, a turn whose thread is pinned to a deprecated model resolves through the D-03 free-guard: a paid or unknown user default falls back to settings.demo_fallback_model — it NEVER runs a paid completion on the owner key (closes 15-02 #1 / gap truth 1)"
    - "A regression test pins demo + deprecated-pin + paid user default → settings.demo_fallback_model, and a non-demo control proves the guard is demo-only"
    - "Prod backend is redeployed carrying the fix — the flag is already ON in prod (15-08), so this replaces the live vulnerable code"
  artifacts:
    - path: "backend/routers/chat.py"
      provides: "Free-guarded deprecated-pin override via a testable _deprecated_pin_default_model helper"
      contains: "_deprecated_pin_default_model"
    - path: "backend/tests/test_key_model_resolution.py"
      provides: "Regression coverage for the demo + deprecated-pin interaction (previously zero)"
      contains: "deprecated_pin"
  key_links:
    - from: "chat.py deprecated-pin block (_deprecated_pin_default_model, mode=='demo')"
      to: "_demo_model_for(db, default_model, settings)"
      via: "free-guard re-application before the notice + model override"
      pattern: "_demo_model_for\\(db, default_model, settings\\)"
    - from: "fixed backend code"
      to: "Fly prod runtime (flag already ON)"
      via: "flyctl deploy (repo-root Dockerfile) — no migration, no secret flip"
---

<objective>
Close CR-01: the deprecated-pin fallback in `send_message` overrides the already-free-guarded demo model with an UNguarded, user-influenceable default, minting a PAID completion on the owner key for any authenticated keyless user when `DEMO_FALLBACK_ENABLED=true`. This defect is LIVE IN PROD.

Fix: re-apply the D-03 free-guard on the deprecated-pin branch when `mode == "demo"`, extracted into a testable module-level helper mirroring `_demo_model_for` / `_safe_user_default_model`. Add the missing regression test, clean up the stale Wave-0 test scaffolding (IN-03), then redeploy the backend to Fly prod.

Purpose: restore the D-03 structural $0 owner-cost bound on the keyless demo path (SEC-03) so DEMO-01 holds end-to-end, not just in the isolated resolver.
Output: guarded chat.py resolution, regression test green, prod backend redeployed.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-VERIFICATION.md   (CR-01 gap: gaps[0], root cause + missing[])
@.planning/phases/15-options-ui-capstone-demo-gating/15-02-PLAN.md        (the demo-resolution contract this truth belongs to: 15-02 #1)
@backend/routers/chat.py
@backend/routers/threads.py
@backend/tests/test_key_model_resolution.py

Notes:
- The demo-fallback flag is ALREADY ON in prod (set by 15-08). This plan ships CODE ONLY — no migration, no secret change. The redeploy replaces the vulnerable binary.
- Prod deploy mechanics (project memory / 15-08-SUMMARY): backend = flyctl deploy with the repo-root Dockerfile; frontend unaffected here (backend-only fix). `.env.prod` = PROD Supabase project; never print secret values.
- The resolver `_resolve_key_and_model` (chat.py ~179-250) and `_demo_model_for` (~152-176) are already CORRECT — do NOT touch them. The defect is strictly in the caller's deprecated-pin block (~850-895).

<interfaces>
<!-- Contracts the executor needs — extracted from backend/routers/chat.py. Use directly. -->

Module-level testable helpers (mirror these when adding the new one):
  _safe_user_default_model(db, user_id) -> str | None        # reads user_preferences.default_model, tolerant of absent table
  _demo_model_for(db, model, settings) -> str                # D-03 free-guard: returns `model` iff model_cache.is_free is True, else settings.demo_fallback_model
  _resolve_key_and_model(db, user_id, thread_row, body) -> (api_key, model, mode, is_user_key)   # returns mode in {"user","demo","no_key"}

Caller seam (send_message.event_generator, ~838):
  api_key, model, mode, is_user_key = _resolve_key_and_model(db, user_id, thread.data, body)   # `mode` IS in scope in the deprecated-pin block

Deprecated-pin block (~856-895) — the defect:
  thread_model = thread.data.get("model")                    # a user-settable pin (threads.py PATCH writes it with NO cache validation)
  if cached_ids and thread_model not in cached_ids:
      default_model = _safe_user_default_model(db, user_id) or settings.llm_model   # <-- UNGUARDED paid default
      db.table("messages").insert({... "using {default_model} instead" ...})        # notice copy interpolates it
      model = default_model                                                          # <-- clobbers the free-guarded demo model
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-apply the demo free-guard on the deprecated-pin branch + regression test</name>
  <files>backend/routers/chat.py, backend/tests/test_key_model_resolution.py</files>
  <read_first>
    - backend/routers/chat.py lines ~130-250 (the `_safe_user_default_model`, `_demo_model_for`, `_resolve_key_and_model` helpers — the source of truth for the guard) AND lines ~838-896 (the caller seam + the deprecated-pin block being fixed)
    - backend/routers/threads.py lines ~58-89 (PATCH `update_thread_model` — writes `body.model` with NO model_cache validation; this is the attack surface that makes the pin arbitrary. READ ONLY — do NOT modify: the fix belongs at the resolution seam, not the write, per the project pattern "backend never trusts the FE model choice")
    - backend/tests/test_key_model_resolution.py lines ~30-104 (the `_fake_settings` + `_db_with_key_row` harness: note `pref_model` seeds user_preferences.default_model and `cache_is_free` seeds model_cache.is_free) AND lines ~1-22 (the stale `_WAVE0` / "Wave 0 stub" docstring to clean up, IN-03)
  </read_first>
  <behavior>
    - Test A (the fix, RED first): demo mode + a paid user default + a cache read that is NOT free → the resolved deprecated-pin model is `settings.demo_fallback_model`, never the paid default. Cover both `cache_is_free=False` (known-paid) and `cache_is_free=None` (unknown ≠ free).
    - Test B (non-regression control): a non-demo mode (e.g. "user") → the deprecated-pin default is the paid user default UNCHANGED (the free-guard is demo-only; the connected user pays with their own key as before).
    - Existing killswitch + resolver tests stay green (guard change is additive to the caller, resolver untouched).
  </behavior>
  <action>
    Extract the deprecated-pin default-model computation into a new module-level helper `_deprecated_pin_default_model(db, user_id, mode, settings) -> str`, placed beside `_demo_model_for` / `_safe_user_default_model` and mirroring their tolerant-read style. It computes the base default as `_safe_user_default_model(db, user_id) or settings.llm_model`, then — when `mode == "demo"` — returns `_demo_model_for(db, base, settings)` (the D-03 free-guard re-application); otherwise returns the base unchanged. In the deprecated-pin block (~871-890) replace the inline `default_model = _safe_user_default_model(db, user_id) or settings.llm_model` with `default_model = _deprecated_pin_default_model(db, user_id, mode, settings)` so BOTH the notice copy ("using {default_model} instead") AND the `model = default_model` override use the guarded value — in demo mode the notice now correctly names the free fallback. `mode` is already in scope from the `_resolve_key_and_model` unpack at ~838; add no new plumbing. Do NOT touch `_resolve_key_and_model`, `_demo_model_for`, or threads.py. Then add the two regression tests to test_key_model_resolution.py using the existing `_db_with_key_row(None, pref_model=<paid slug>, cache_is_free=...)` + `_fake_settings(demo_fallback_enabled=True, demo_fallback_model=...)` harness, calling `chat._deprecated_pin_default_model` directly and asserting the resolved slug. While in the file, delete the dead `_WAVE0` constant and rewrite the stale "Wave 0 stub" module docstring to describe the now-live suite (IN-03 cleanup).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_key_model_resolution.py -q</automated>
  </verify>
  <done>The deprecated-pin block resolves the model via `_deprecated_pin_default_model`; in demo mode a paid/unknown default is free-guarded to `settings.demo_fallback_model`; two new regression tests plus the full resolution suite are green; `_WAVE0` and the stale docstring are gone.</done>
  <acceptance_criteria>
    - `grep -n "_deprecated_pin_default_model" backend/routers/chat.py` shows the helper definition AND its call inside the deprecated-pin block; the old inline `_safe_user_default_model(db, user_id) or settings.llm_model` no longer appears in that block
    - The helper applies `_demo_model_for(db, base, settings)` only when `mode == "demo"`
    - New test (name contains `deprecated_pin` + `demo`): demo mode + `pref_model` a paid slug + `cache_is_free=False` → returns `settings.demo_fallback_model`; a variant with `cache_is_free=None` (unknown) → also `settings.demo_fallback_model`
    - New control test: `mode != "demo"` → returns the paid `pref_model` unchanged
    - `pytest tests/test_key_model_resolution.py -q` reports all tests passed (the 15 prior + the new ones); no test asserts the paid default is used on a demo turn
    - `grep -c "_WAVE0" backend/tests/test_key_model_resolution.py` returns 0; the module docstring no longer contains "Wave 0 stub"
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Redeploy the fixed backend to Fly prod + live security verification</name>
  <files>none (ops — no repo file changes)</files>
  <read_first>
    - .planning/phases/15-options-ui-capstone-demo-gating/15-08-SUMMARY.md (prod deploy mechanics: flyctl deploy repo-root Dockerfile, release confirmation, secrets listed by name only)
    - fly.toml (app name)
    - Task 1 result (resolution suite green)
  </read_first>
  <action>PAUSE execution and present the runbook and verification checklist below to the operator. Do not run any prod deploy command past a failed local suite; record the deploy confirmations and the live log evidence verbatim in the SUMMARY. This is a code-only backend redeploy — no migration, no secret flip (the flag is already ON from 15-08).</action>
  <what-built>chat.py now free-guards the deprecated-pin override in demo mode via `_deprecated_pin_default_model`, closing the owner-key paid-spend path. Regression tests pin it. Local resolution suite is green. The prod flag is ALREADY ON, so the running code is still the vulnerable version until this redeploy lands.</what-built>
  <how-to-verify>
    PAUSE and hand the operator this runbook (auth prompts from flyctl surface as sub-checkpoints; never echo secret values):
    1. Confirm the local backend suite is green (Task 1) before deploying.
    2. Redeploy backend ONLY — no migration, no secret change (the flag stays ON): `flyctl deploy` from the repo root (repo-root Dockerfile per 15-08). Confirm a new release is visible and `/api/health` returns 200.
    3. Confirm no secret flip occurred: `flyctl secrets list` still shows `DEMO_FALLBACK_ENABLED` present and `LLM_API_KEY` present (names/digests only — no values).
    4. Live security check (the reason this is human-gated — needs a real authenticated session against the flag-ON backend): sign in as a KEYLESS prod account, `PATCH /api/threads/{id}` to a bogus/deprecated model id, `PUT /api/preferences` a PAID `default_model`, then send a message. Inspect the backend logs: the turn MUST resolve `model == settings.demo_fallback_model` with `is_byok: false` — it must NEVER run the paid default. The demo banner/notice reflects the free fallback.
    If the free provider rate-limits (environmental 429, precedent D-999.1-LLM-A), the resolution log line is still the pass signal — do not retry-until-green against the live provider.
  </how-to-verify>
  <verify>
    <automated>MISSING — prod redeploy + live authenticated session require the operator; this checkpoint is the test (log line `model == demo_fallback_model` on the keyless deprecated-pin turn is the pass signal)</automated>
  </verify>
  <done>Operator confirms the backend redeployed (new release, health 200, flag still ON) and the live keyless deprecated-pin turn resolved to `demo_fallback_model`, never the paid default.</done>
  <acceptance_criteria>
    - New Fly release visible; `/api/health` = 200
    - `flyctl secrets list` still shows `DEMO_FALLBACK_ENABLED` + `LLM_API_KEY` (names only; no value printed anywhere)
    - Live log evidence: keyless account + bogus thread pin + paid default → `model == demo_fallback_model`, `is_byok: false` (or a documented environmental 429 with the correct resolution line still present)
    - Operator replies "approved" or "approved-with-caveat: <environmental notes>"; any paid-default resolution is a FAIL captured verbatim
  </acceptance_criteria>
  <resume-signal>Type "approved", "approved-with-caveat: <notes>", or describe the failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → prod chat with demo ON | any authenticated keyless user can influence the resolved model via the thread pin (PATCH, unvalidated) + their default_model (PUT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-32 | Denial of service (cost) / Elevation | `send_message` deprecated-pin override on the owner key | mitigate | re-apply the D-03 free-guard (`_demo_model_for`) inside `_deprecated_pin_default_model` when `mode == "demo"`, so a paid/unknown pin resolves to `settings.demo_fallback_model` — owner cost returns to structural $0 |
| T-15-33 | Tampering | thread `model` pin set to an arbitrary id (threads.py PATCH, no cache validation) | accept | the write stays unvalidated by design; the enforcing boundary is the resolution-seam free-guard (defense where the model is USED, not written) — a bogus pin now resolves to the free fallback regardless |
| T-15-34 | Information disclosure | secrets in deploy logs | mitigate | flyctl secrets listed by name/digest only; no secret value echoed |

Residual: worst-case owner spend on the keyless path returns to $0 structurally; the provider ~$0.10 budget guardrail (999.2 PASS) remains as the backstop.
</threat_model>

<verification>
- `pytest tests/test_key_model_resolution.py -q` green with the two new deprecated-pin+demo regression tests
- The deprecated-pin block resolves through `_deprecated_pin_default_model`; no unguarded paid default reachable in demo mode
- Prod backend redeployed (new release, health 200, flag still ON); live keyless deprecated-pin turn logs `demo_fallback_model`
</verification>

<success_criteria>
- CR-01 closed: 15-02 #1 holds end-to-end — demo turns never mint a paid completion on the owner key
- SEC-03 structural $0 bound (D-03) restored on the keyless path; DEMO-01 holds in prod
- IN-03 test-scaffold debt cleaned up
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-09-SUMMARY.md`
</output>
