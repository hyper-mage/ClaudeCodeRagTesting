---
phase: 15-options-ui-capstone-demo-gating
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routers/chat.py
  - backend/tests/test_key_model_resolution.py
autonomous: true
requirements: [DEMO-01, SEC-03]

must_haves:
  truths:
    - "A keyless turn with the flag ON runs the picked model ONLY when model_cache marks it is_free=True; paid, unknown, or cache-error outcomes fall back to the pinned demo_fallback_model"
    - "use_demo=true short-circuits to the demo branch BEFORE the user-key branch only when demo_fallback_enabled is True; with the flag OFF the override is completely inert"
    - "The three SEC-03 kill-switch tests (test_sec03_killswitch_no_owner_spend_when_flag_off, test_no_key_flag_off_refuses, test_fail_closed_no_or_fallback) still pass unmodified"
  artifacts:
    - path: "backend/routers/chat.py"
      provides: "_demo_model_for free-guard helper + use_demo override in _resolve_key_and_model"
      contains: "_demo_model_for"
    - path: "backend/tests/test_key_model_resolution.py"
      provides: "free-guard + override coverage extending the existing scaffolding"
  key_links:
    - from: "backend/routers/chat.py _resolve_key_and_model"
      to: "model_cache.is_free"
      via: "_demo_model_for one-row read, unknown != free"
      pattern: "is_free.*is True"
    - from: "use_demo override"
      to: "settings.demo_fallback_enabled"
      via: "flag checked in the SAME condition, before the user-key branch"
      pattern: "use_demo.*demo_fallback_enabled"
---

<objective>
Upgrade the demo resolution branch in `chat.py` to run the user's PICKED free model (D-03) with a non-negotiable server-side free-guard, and add the D-11 `use_demo` retry override — while preserving the SEC-03 fail-closed posture byte-for-byte when the flag is OFF.

Purpose: D-03 server-side guard is the structural $0 owner-spend bound (never trust the frontend). The security requirement is explicit: the use_demo override MUST be gated on demo_fallback_enabled BEFORE the user-key branch so the SEC-03 kill-switch tests stay green.
Output: new resolution order `use_demo+flagON → demo` › `user key → user` › `flagON keyless → demo` › `no_key`, fully tested.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-CONTEXT.md  (D-03, D-11)
@.planning/phases/15-options-ui-capstone-demo-gating/15-PATTERNS.md  (§chat.py, §BE resolution tests)
@.planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md  (Pattern 4, Pitfalls 4-5, Code Examples "Demo branch free-guard")

Notes:
- MessageCreate.use_demo (the request-body field) lands in plan 15-01 (schemas.py). This plan's resolver reads getattr(body, "use_demo", False), which is correct with or without that field — no file overlap, safe to run in parallel.
- model_cache table + is_free column exist since migration 030 (Phase 12); is_free is precomputed by model_catalog_service._to_cache_row. Never recompute freeness from pricing or the :free suffix in chat.py.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extend the resolution test scaffolding with model_cache and write failing free-guard + override tests</name>
  <files>backend/tests/test_key_model_resolution.py</files>
  <read_first>
    - backend/tests/test_key_model_resolution.py (FULL file: _fake_settings ~30-42, _db_with_key_row + _table dispatcher ~45-87, killswitch trio ~110+ — the exact assertion shapes to copy; header lines 13-15 for config-test guidance)
    - backend/routers/chat.py lines 140-215 (_resolve_key_and_model signature + current branches: model-tier resolution ~171-177, user-key read ~181-194, demo branch ~196-204, no_key tail)
  </read_first>
  <behavior>
    - test_demo_runs_picked_free_model: keyless, flag ON, model_cache row {"is_free": True} → returns (settings.resolved_llm_api_key, picked model, "demo", False)
    - test_demo_paid_model_falls_back_to_pinned: keyless, flag ON, {"is_free": False} → model == settings.demo_fallback_model
    - test_demo_unknown_model_falls_back: keyless, flag ON, maybe_single .data is None → pinned fallback (unknown != free)
    - test_demo_cache_error_falls_back: keyless, flag ON, model_cache execute raises → pinned fallback, no exception escapes
    - test_use_demo_override_beats_user_key_when_flag_on: user key row EXISTS, body.use_demo=True, flag ON → mode "demo", api_key == resolved_llm_api_key (owner), model free-guarded
    - test_use_demo_inert_when_flag_off_with_key: user key row exists, use_demo=True, flag OFF → mode "user" (normal resolution, override ignored)
    - test_use_demo_inert_when_flag_off_keyless: no key row, use_demo=True, flag OFF → mode "no_key", api_key None (kill switch intact)
    - Existing killswitch trio stays green and UNMODIFIED: test_sec03_killswitch_no_owner_spend_when_flag_off, test_no_key_flag_off_refuses, test_fail_closed_no_or_fallback
  </behavior>
  <action>
    Extend _db_with_key_row's _table dispatcher with a "model_cache" branch whose select().eq().maybe_single().execute() outcome is configurable per test (data dict with is_free True/False, data None, or raising). Keep the existing user_api_keys branch untouched so the killswitch trio's mocks still resolve. For bodies: existing tests use MagicMock(spec=[]) which makes getattr(body, "use_demo", False) default safely; override tests pass MagicMock(spec=["use_demo"], use_demo=True) or types.SimpleNamespace(use_demo=True). Patch chat.get_settings per the file's established with-patch shape. Write all seven new tests per the behavior list; run the file and confirm the new tests FAIL (resolver not yet changed) while the killswitch trio still PASSES.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_key_model_resolution.py -q -k "killswitch or refuses or fail_closed" && ! venv/Scripts/python.exe -m pytest tests/test_key_model_resolution.py -q -k "picked_free or falls_back or use_demo"</automated>
  </verify>
  <done>Seven new tests exist and fail for the right reason (current demo branch pins the fallback / no override exists); killswitch trio green; no existing test modified.</done>
  <acceptance_criteria>
    - pytest -k "killswitch or refuses or fail_closed" exits 0 (trio untouched and green)
    - pytest -k "picked_free or falls_back or use_demo" exits non-zero (RED state)
    - git diff shows NO modification to the three killswitch test function bodies
    - _table dispatcher handles table name "model_cache" with per-test configurable outcomes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement _demo_model_for free-guard and the flag-gated use_demo override</name>
  <files>backend/routers/chat.py</files>
  <read_first>
    - backend/routers/chat.py lines 140-215 (resolver), lines 185-194 (the defensive maybe_single dict-guard shape to mirror), lines 845-855 (T-13-CRASH except shape with scrub_secrets to mirror), lines 790-810 (fail-closed caller contract that must survive: mode "no_key" → SSE no_api_key, NO LLM call)
    - backend/tests/test_key_model_resolution.py (the now-RED tests define the exact contract)
  </read_first>
  <action>
    Add a module-level helper def _demo_model_for(db, model: str, settings) -> str above _resolve_key_and_model: read model_cache via db.table("model_cache").select("is_free").eq("model_id", model).maybe_single().execute() inside try/except; return model ONLY when row and isinstance(row.data, dict) and row.data.get("is_free") is True; ANY other outcome — is_free False, data None, non-dict data, or exception — returns settings.demo_fallback_model (unknown != free, Pitfall 5). The except branch logs logger.warning with scrub_secrets(str(e)) mirroring the T-13-CRASH shape; a cache-read error must never crash the turn. Docstring: D-03 server-side guard, never trusts the frontend.

    In _resolve_key_and_model, insert the D-11 override AFTER the model-tier resolution (~line 177) and BEFORE the user-key row read (~line 181): if getattr(body, "use_demo", False) and settings.demo_fallback_enabled → return (settings.resolved_llm_api_key, _demo_model_for(db, model, settings), "demo", False). The flag check MUST be in the same condition — when demo_fallback_enabled is False the override falls through to normal resolution (security requirement; SEC-03 kill switch intact).

    Replace the body of the existing keyless demo branch (~lines 196-204) so it routes through the same _demo_model_for helper instead of pinning: return (settings.resolved_llm_api_key, _demo_model_for(db, model, settings), "demo", False). Keep the branch's comment intent (cost bound comes from the MODEL) and update it to reference D-03. Leave the user-key branch and the no_key tail byte-identical. Resulting order: use_demo+flagON → demo › user key → user › flagON keyless → demo › no_key.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_key_model_resolution.py -x -q</automated>
  </verify>
  <done>All resolution tests green including the seven new ones and the untouched killswitch trio. Full backend suite runs at the wave-1 merge (shared working tree — RESEARCH §Validation Architecture sampling), not in-task.</done>
  <acceptance_criteria>
    - pytest tests/test_key_model_resolution.py exits 0
    - chat.py contains "def _demo_model_for(" and the guard expression `.get("is_free") is True`
    - chat.py contains a single condition combining getattr(body, "use_demo", False) AND settings.demo_fallback_enabled, positioned before the user_api_keys read in _resolve_key_and_model
    - No resolution path returns settings.resolved_llm_api_key without demo_fallback_enabled in its governing condition (RESEARCH Pitfall 4 warning sign)
    - Wave-1 merge gate (not in-task): full backend suite 0 new failures vs baseline (2 pre-existing record_manager errors permitted)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST messages (use_demo, picked model) | untrusted request influences which key/model mint a completion |
| chat.py → model_cache | server-side truth source for freeness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Elevation of privilege | use_demo override | mitigate | override honored ONLY when settings.demo_fallback_enabled is True, checked in the same condition BEFORE the user-key branch; flag-OFF inert tests + killswitch trio pinned green |
| T-15-06 | Tampering | FE-forged "free" model on owner key | mitigate | _demo_model_for validates model_cache.is_free is True server-side; non-free/unknown/error → pinned demo_fallback_model (owner spend structurally $0) |
| T-15-07 | Denial of service | model_cache read during a turn | mitigate | try/except around the cache read; any failure falls back to the pinned free model, never a 500 |
| T-15-08 | Information disclosure | new except-branch logging | mitigate | log via scrub_secrets(str(e)) only; no exc_info near key material (SEC-01 house style) |
| T-15-09 | Denial of service (cost) | owner key spend if guard bypassed | transfer | provider budget guardrail 403-blocks at ~$0.10 (999.2 SEC-03 PASS finding) as the backstop |
</threat_model>

<verification>
- pytest tests/test_key_model_resolution.py -q → all green, killswitch trio unmodified (git diff clean on those three functions)
- Wave-1 merge gate: full backend suite green modulo the 2 documented pre-existing record_manager errors (run after all wave-1 plans complete — never inside parallel same-wave plans)
</verification>

<success_criteria>
- Picked-free-model demo turns work; paid/unknown/error inputs cannot spend beyond the pinned free model
- use_demo override live behind the flag; flag OFF is provably inert both keyed and keyless
- SEC-03 fail-closed posture intact (no_key path byte-identical)
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-02-SUMMARY.md`
</output>
