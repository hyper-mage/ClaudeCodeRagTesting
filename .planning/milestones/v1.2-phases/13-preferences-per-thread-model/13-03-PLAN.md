---
phase: 13-preferences-per-thread-model
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - backend/routers/preferences.py
  - backend/routers/threads.py
  - backend/main.py
autonomous: true
requirements: [MODEL-05, MODEL-06, PREF-02]
user_setup: []

must_haves:
  truths:
    - "GET /api/preferences returns the calling user's default_model + theme, defaulting a brand-new user to {default_model: null, theme: 'dark'}"
    - "PUT /api/preferences upserts a partial body (theme-only PUT does not clobber default_model and vice-versa), binding user_id from the JWT"
    - "PATCH /api/threads/{id} sets threads.model, and {model:null} explicitly clears the pin to default"
    - "PATCH on a non-owned thread returns 404"
  artifacts:
    - path: "backend/routers/preferences.py"
      provides: "GET + PUT /api/preferences (upsert PK user_id)"
      exports: ["router"]
    - path: "backend/routers/threads.py"
      provides: "PATCH /{thread_id} for per-thread model set/clear"
      contains: "update_thread_model"
    - path: "backend/main.py"
      provides: "preferences router wired into the app"
      contains: "preferences"
  key_links:
    - from: "backend/routers/preferences.py"
      to: "user_preferences table"
      via: "upsert(on_conflict=user_id) + maybe_single read"
      pattern: "upsert\\(.*on_conflict"
    - from: "backend/routers/threads.py PATCH"
      to: "threads.model"
      via: "explicit update({'model': body.model}) scoped by id + user_id"
      pattern: "\\.update\\(\\{.*model"
---

<objective>
Ship the preference WRITE/READ endpoints: GET + PUT /api/preferences (default_model + theme, upsert keyed on user_id) and PATCH /api/threads/{id} (set or clear threads.model). Wire the new preferences router into main.py. These feed the already-live three-tier resolution (chat.py:172-177) — resolution logic is UNCHANGED; this plan only supplies the write side and the GET the frontend needs to hydrate the selector + theme.

Purpose: MODEL-05 (set default model), MODEL-06 (per-thread model persisted), PREF-02 (theme persisted server-side). Turns the Plan 01 Wave 0 tests for these behaviors GREEN.
Output: one new router, an extended threads router, main.py wiring.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-preferences-per-thread-model/13-RESEARCH.md
@.planning/phases/13-preferences-per-thread-model/13-CONTEXT.md

<interfaces>
From backend/routers/keys.py (the upsert + maybe_single-guard pattern to MIRROR):
  get_supabase().table("user_api_keys").upsert({...}, ...).execute()
  row = ....select(...).eq("user_id", user_id).maybe_single().execute()
  if not row or not row.data: return {"connected": False}   # the new-user default guard to copy

From backend/routers/threads.py (ownership re-check pattern from get_thread/delete_thread to MIRROR for PATCH):
  owned = db.table("threads").select("id").eq("id", thread_id).eq("user_id", user_id).maybe_single().execute()
  if not owned.data: raise HTTPException(status_code=404, detail="Thread not found")

From backend/models/schemas.py (authored in Plan 01 — consume directly):
  PreferencesResponse(default_model: str | None, theme: str = "dark")
  PreferencesUpdate(default_model: str | None = None, theme: Literal["light","dark"] | None = None)
  ThreadModelUpdate(model: str | None = None)
  ThreadResponse(... model: str | None)

From backend/auth.py + backend/database.py (existing deps):
  get_user_id  (Depends → JWT sub; NEVER read user_id from the body)
  get_supabase  (service-role client)

From backend/main.py (the include_router block to extend):
  from routers import threads, chat, documents, folders, demo, keys, models
  app.include_router(models.router)   # add: app.include_router(preferences.router) after this
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: preferences router — GET + PUT /api/preferences</name>
  <read_first>
    - backend/routers/keys.py (upsert pattern lines 60-71; status new-user guard lines 76-93)
    - backend/models/schemas.py (PreferencesResponse, PreferencesUpdate — from Plan 01)
    - backend/tests/test_preferences_api.py (the RED Wave 0 cases this task turns green)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Pattern 2; Pitfall 4 maybe_single new-user; Code Examples — GET with safe default)
  </read_first>
  <behavior>
    - GET /api/preferences for a user with no row → 200 {"default_model": null, "theme": "dark"} (Pitfall 4 maybe_single guard; never a 406).
    - GET for an existing row → 200 with that row's default_model + theme (theme falls back to "dark" if somehow null).
    - PUT {"default_model": "x/y"} → upserts; subsequent GET returns default_model "x/y". (test_put_then_get_default_model)
    - PUT {"theme": "light"} when a default_model already exists → theme updates but default_model is NOT clobbered (partial upsert via exclude_unset). (test_theme_persist_and_validate)
    - PUT binds user_id from Depends(get_user_id) into the upsert payload; a user_id in the body is ignored/never used (cross-user write mitigation T-13-02).
    - PUT {"theme": "purple"} → 422 (Pydantic Literal) before any DB call.
  </behavior>
  <action>
    Create backend/routers/preferences.py with APIRouter(prefix="/api/preferences", tags=["preferences"]). Two handlers, both Depends(get_user_id), both via the service-role get_supabase() client:
    GET "" (response_model=PreferencesResponse): select("default_model, theme").eq("user_id", user_id).maybe_single().execute(); if not row or not row.data → return {"default_model": None, "theme": "dark"}; else return default_model + (theme or "dark"). Mirror keys.py status guard exactly (Pitfall 4).
    PUT "" (response_model=PreferencesResponse): build patch = body.model_dump(exclude_unset=True) so only client-sent fields are written (a theme-only PUT must not null default_model — RESEARCH Pattern 2); set patch["user_id"] = user_id (from the JWT, NEVER the body — T-13-02) and patch["updated_at"] = datetime.now(timezone.utc).isoformat() explicitly (ON CONFLICT skips column defaults — keys.py Pitfall 4). upsert(patch, on_conflict="user_id").execute(); re-select default_model + theme and return them (theme defaulting to "dark" if null). Import datetime/timezone like keys.py.
    Do not REVOKE anything; do not add a DELETE. Keep it minimal — this is the temporary write surface P14 builds on.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_preferences_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_preferences_api.py passes (test_put_then_get_default_model, test_get_defaults_for_new_user, test_theme_persist_and_validate all GREEN).
    - preferences.py exports `router` with GET "" and PUT "".
    - The PUT upsert payload contains user_id sourced from get_user_id, and a theme-only PUT's exclude_unset payload does NOT contain default_model.
    - PUT with theme="purple" returns 422 (Literal enforced upstream of the DB).
  </acceptance_criteria>
  <done>GET/PUT /api/preferences work with partial upsert + new-user defaults + JWT-bound user_id; the Wave 0 preferences tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH /api/threads/{thread_id} — set/clear per-thread model</name>
  <read_first>
    - backend/routers/threads.py (existing get_thread/delete_thread ownership pattern to mirror; this file is edited, not replaced)
    - backend/models/schemas.py (ThreadModelUpdate, ThreadResponse.model — from Plan 01)
    - backend/tests/test_thread_model_patch.py (the RED Wave 0 cases this task turns green)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Pattern 3; Anti-Patterns — exclude_unset is WRONG here, write null explicitly)
  </read_first>
  <behavior>
    - PATCH /api/threads/{id} {"model": "x/y"} by the owner → threads.model becomes "x/y"; response is the updated ThreadResponse including model. (test_patch_sets_model)
    - PATCH {"model": null} → threads.model is explicitly set to NULL (the clear-to-default path, D-05) — written through, NOT skipped. (test_patch_null_clears)
    - PATCH on a thread the caller does not own → 404 (ownership re-check returns no row). (the 404 case)
    - The update is scoped by .eq("id", thread_id).eq("user_id", user_id) — IDOR defense-in-depth even though service-role bypasses RLS (T-13-IDOR).
  </behavior>
  <action>
    Edit backend/routers/threads.py: add @router.patch("/{thread_id}", response_model=ThreadResponse) async def update_thread_model(thread_id, body: ThreadModelUpdate, user_id=Depends(get_user_id)). Mirror delete_thread's ownership check: select("id").eq("id", thread_id).eq("user_id", user_id).maybe_single().execute(); if not owned.data → raise HTTPException(404, "Thread not found"). Then db.table("threads").update({"model": body.model}).eq("id", thread_id).eq("user_id", user_id).execute() — write body.model EXPLICITLY so a null clears the column (do NOT use exclude_unset here — RESEARCH Pattern 3 caution; this is the inverse of Task 1). Re-select("*") the thread and return it (model rides along). Import ThreadModelUpdate into the existing schemas import line. Do not change list_threads/create_thread/get_thread/delete_thread.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_thread_model_patch.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_thread_model_patch.py passes (test_patch_sets_model, test_patch_null_clears, and the 404 case all GREEN).
    - The PATCH update call writes {"model": None} when body.model is null (verified by the test asserting the update arg), NOT an empty/omitted payload.
    - The handler raises 404 when the ownership re-check finds no owned row.
    - Existing threads endpoints are unchanged (no other test regresses).
  </acceptance_criteria>
  <done>PATCH /api/threads/{id} sets and clears threads.model with an ownership re-check; the Wave 0 patch tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire preferences router into main.py + wave-merge full backend suite</name>
  <read_first>
    - backend/main.py (the include_router block + the routers import line)
    - backend/routers/preferences.py (the router exported in Task 1)
  </read_first>
  <action>
    Edit backend/main.py: add `preferences` to the `from routers import ...` line and add `app.include_router(preferences.router)` after the existing `app.include_router(models.router)` line. No other changes (CORS, limiter, health route untouched). Then run the full backend suite as the Wave-merge gate (13-VALIDATION Sampling Rate: per-wave merge runs the full backend suite) to confirm no regression from the new router/endpoint and the now-real schema-tolerant resolution.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - main.py imports preferences and calls app.include_router(preferences.router).
    - The full backend suite passes (the three preferences/patch cases green; no regression in test_key_model_resolution.py or any existing suite). Pre-existing known debt test_record_manager.py::test_check_duplicate_integration may remain skipped/failing as documented in STATE.md Pending Todos — note it in the SUMMARY but do not fix it here.
  </acceptance_criteria>
  <done>The preferences router is reachable at /api/preferences; the full backend suite is green (modulo the documented pre-existing record_manager debt).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PUT /api/preferences | Untrusted default_model/theme; user_id MUST come from JWT, never body |
| client → PATCH /api/threads/{id} | Path thread_id is attacker-controlled; ownership must be re-checked server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-IDOR | Tampering / Elevation of Privilege | PATCH /api/threads/{id} | mitigate | Ownership re-check `.eq("id",tid).eq("user_id",uid)` before update (Task 2) + threads RLS; a forged thread_id → 404 |
| T-13-02 | Information Disclosure / EoP | PUT/GET /api/preferences | mitigate | user_id bound from Depends(get_user_id) (JWT sub), never from the body; own-row RLS on user_preferences (Plan 01) |
| T-13-08 | Tampering | PUT theme value | mitigate | Pydantic Literal['light','dark'] on PreferencesUpdate → 422 before the DB; DB CHECK is the backstop |
| T-13-09 | Tampering | default_model / model free strings | accept | Must stay free strings (D-06 stores deprecated-but-valid ids); never interpolated into SQL (parameterized via supabase-py) |
</threat_model>

<verification>
- tests/test_preferences_api.py + tests/test_thread_model_patch.py green (Tasks 1-2).
- Full backend suite green via `cd backend && ./venv/Scripts/python -m pytest -q` (Task 3 wave-merge gate).
- Resolution unchanged: `pytest tests/test_key_model_resolution.py -q` still green, now including test_thread_model_wins_when_set once the column is real.
</verification>

<success_criteria>
- GET/PUT /api/preferences and PATCH /api/threads/{id} exist with the locked behavior (partial upsert, explicit-null clear, 404 on non-owned, JWT-bound user_id).
- Preferences router wired into main.py; full backend suite green.
- The three-tier resolution now reads REAL user_preferences.default_model + threads.model (no logic change).
</success_criteria>

<output>
After completion, create `.planning/phases/13-preferences-per-thread-model/13-03-SUMMARY.md`.
</output>
