---
phase: 13-preferences-per-thread-model
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: []
autonomous: false
requirements: [MODEL-05, MODEL-06, PREF-02]
user_setup: []

must_haves:
  truths:
    - "Migration 20240301000032 is applied LIVE to the dev Supabase project"
    - "user_preferences table exists in the live DB with own-row RLS"
    - "threads.model column exists in the live DB (nullable)"
    - "A messages row with role 'notice' inserts successfully against the live DB"
  artifacts:
    - path: "supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql"
      provides: "the migration applied in this plan (authored in Plan 01)"
      contains: "CREATE TABLE user_preferences"
  key_links:
    - from: "supabase db push"
      to: "dev Supabase (ntkkmljbariflblldmha)"
      via: "migration apply (or dashboard SQL editor fallback)"
      pattern: "user_preferences"
---

<objective>
[BLOCKING] Apply the Phase 13 additive migration (authored in Plan 01) to the dev Supabase project, then prove the new schema is live: user_preferences exists with own-row RLS, threads.model exists, and a role 'notice' message inserts. Build + type checks pass WITHOUT this push (resolution types come from config, not the live DB), so the push is mandatory to avoid a false-positive verification — the downstream endpoint/notice plans are meaningless until the schema is real.

Purpose: make the schema real so Plans 03/04 read/write real data instead of the pre-P13 tolerant-fallback path.
Output: applied migration + a live probe confirming the three additive changes.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-preferences-per-thread-model/13-RESEARCH.md

<interfaces>
<!-- Migration-apply caveats from RESEARCH Pitfall 1 + project MEMORY.md -->
- Push command: `supabase db push`
- Non-TTY: set SUPABASE_ACCESS_TOKEN env var so the CLI does not prompt.
- CAVEAT: `db push` may try to replay the full migration series and error with 42P07 "already exists" on old tables. Per MEMORY.md ("Supabase migration history repair") + Phase 9/10/12 precedent: run `supabase migration repair --status applied <prior-range>` to mark the already-applied migrations, THEN `db push` only the new ...000032.
- FALLBACK: if the CLI is unavailable/unauthed or prompts cannot be suppressed, apply the single new migration SQL directly in the Supabase dashboard SQL editor against the DEV project (ntkkmljbariflblldmha) — this sidesteps the replay issue entirely (MEMORY.md confirms this is the established workaround).
- DEV ONLY this phase: prod is deferred to deploy (project convention D-03 across Phases 9/10/12; verify against .env, not .env.prod).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration 20240301000032 to dev Supabase</name>
  <read_first>
    - supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql (the file to apply — authored in Plan 01)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Pitfall 1 — replay/already-exists; Environment Availability — dashboard fallback)
    - .planning/STATE.md (Decisions: migrations 025/026/028/030/031 applied LIVE to dev after a history repair; prod deferred to deploy)
  </read_first>
  <what-built>
    Claude first ATTEMPTS the apply autonomously (automation-first): with SUPABASE_ACCESS_TOKEN set, run `supabase db push`. If push reports a replay / 42P07 already-exists, run `supabase migration repair --status applied` for the prior migration range (the series up to ...000031), then re-run `supabase db push` for only ...000032. If the CLI is unavailable, unauthed, or requires an interactive prompt that cannot be suppressed, STOP and present the migration SQL for manual application in the dev Supabase dashboard SQL editor. This is the human-action checkpoint: applying a schema change to a live hosted DB whose migration history is known-finicky (MEMORY.md) and may need a dashboard apply that the CLI cannot do non-interactively.
  </what-built>
  <how-to-verify>
    1. Confirm the apply targeted the DEV project (ntkkmljbariflblldmha / the project in .env, NOT .env.prod).
    2. In the Supabase dashboard SQL editor (or via the CLI), run: SELECT to_regclass('public.user_preferences'); — expect a non-null result.
    3. Run: SELECT column_name FROM information_schema.columns WHERE table_name='threads' AND column_name='model'; — expect one row.
    4. Run a role-CHECK probe: INSERT a temporary messages row with role='notice' against a throwaway/owned thread+user, confirm it succeeds (then delete it), OR inspect the constraint: SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='messages_role_check'; — expect the def to include 'notice'.
    5. Confirm user_preferences RLS is enabled: SELECT relrowsecurity FROM pg_class WHERE relname='user_preferences'; — expect true.
  </how-to-verify>
  <action>
    Attempt `supabase db push` (SUPABASE_ACCESS_TOKEN set for non-TTY). On replay/42P07: `supabase migration repair --status applied <range-through-000031>` then push only ...000032. On any blocker the CLI cannot resolve non-interactively, present the ...000032 SQL for dashboard application against the dev project. After apply, run the five live probes above. Apply to DEV ONLY (prod deferred to deploy per project convention). Do NOT touch .env.prod / the prod project.
  </action>
  <verify>
    <automated>MISSING — live-DB apply is human-action (no local automated gate); verification is the five live SQL probes in how-to-verify. Downstream Plan 03/04 backend tests run against mocks and do NOT require the live DB, but the live probes here are the schema-applied gate.</automated>
  </verify>
  <acceptance_criteria>
    - to_regclass('public.user_preferences') is non-null (table exists live).
    - information_schema shows threads.model exists.
    - messages_role_check def includes 'notice' (probe insert of role='notice' succeeds, then cleaned up).
    - user_preferences relrowsecurity is true.
    - Apply targeted the DEV project only; prod untouched.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the five live probes confirm the schema, or describe the blocker (e.g. CLI replay error, auth failure) so it can be resolved.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration → live hosted DB | A schema change crosses into the dev Supabase project; must target dev, never prod, this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | wrong-environment apply | mitigate | Explicit DEV-only scope (.env project ntkkmljbariflblldmha); prod deferred to deploy per Phase 9/10/12 convention; checkpoint confirms target before resume |
| T-13-06 | Denial of Service | migration replay corrupting history | mitigate | `migration repair --status applied` for the prior range before pushing only ...000032 (MEMORY.md); dashboard single-statement fallback sidesteps replay entirely |
| T-13-07 | Elevation of Privilege | user_preferences RLS not enforced post-apply | mitigate | Live probe asserts relrowsecurity=true; own-row policies authored in Plan 01 |
</threat_model>

<verification>
- The five live SQL probes in Task 1 how-to-verify all pass.
- Apply confined to the dev project; .env.prod untouched.
- No regression to the Phase 9 SEC-02 lockdown (this migration does not touch user_api_keys grants or the SQL-tool allowlist — additive only).
</verification>

<success_criteria>
- Migration 20240301000032 is live on dev Supabase.
- user_preferences (own-row RLS), threads.model (nullable), and messages.role='notice' are all confirmed against the live DB.
- Prod deferred to deploy; no false-positive verification (the schema is genuinely real, not just type-checkable).
</success_criteria>

<output>
After completion, create `.planning/phases/13-preferences-per-thread-model/13-02-SUMMARY.md` (record which apply path was used — CLI push, repair+push, or dashboard — and the probe results, so the deploy-time prod apply has a runbook).
</output>
