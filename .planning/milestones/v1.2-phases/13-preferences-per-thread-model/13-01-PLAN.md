---
phase: 13-preferences-per-thread-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql
  - backend/models/schemas.py
  - backend/tests/test_preferences_api.py
  - backend/tests/test_thread_model_patch.py
  - backend/tests/test_deprecated_model_fallback.py
  - backend/tests/test_key_model_resolution.py
autonomous: true
requirements: [MODEL-05, MODEL-06, PREF-02]
user_setup: []

must_haves:
  truths:
    - "A user_preferences table exists with own-row RLS (default_model nullable, theme NOT NULL DEFAULT 'dark' CHECK in ('light','dark'))"
    - "threads has a nullable model TEXT column (no backfill)"
    - "messages.role CHECK accepts 'notice' in addition to 'user' and 'assistant'"
    - "Pydantic models PreferencesResponse, PreferencesUpdate, ThreadModelUpdate exist and ThreadResponse exposes model: str | None"
    - "Wave 0 backend test files exist and fail RED (endpoints/behavior not yet implemented)"
  artifacts:
    - path: "supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql"
      provides: "user_preferences table + RLS, threads.model column, messages_role_check ALTER"
      contains: "CREATE TABLE user_preferences"
    - path: "backend/models/schemas.py"
      provides: "PreferencesResponse / PreferencesUpdate / ThreadModelUpdate + ThreadResponse.model"
      contains: "class PreferencesUpdate"
    - path: "backend/tests/test_preferences_api.py"
      provides: "MODEL-05 + PREF-02 endpoint test scaffolds"
      contains: "test_put_then_get_default_model"
    - path: "backend/tests/test_thread_model_patch.py"
      provides: "MODEL-06 PATCH set/clear/404 test scaffolds"
      contains: "test_patch_sets_model"
    - path: "backend/tests/test_deprecated_model_fallback.py"
      provides: "SC#4 notice insert + fallback + history-exclusion test scaffolds"
      contains: "test_inserts_notice_and_falls_back"
  key_links:
    - from: "supabase/migrations/20240301000032_*.sql"
      to: "user_preferences.theme"
      via: "CHECK (theme IN ('light','dark'))"
      pattern: "theme IN \\('light','dark'\\)"
    - from: "supabase/migrations/20240301000032_*.sql"
      to: "messages.role"
      via: "DROP + re-ADD messages_role_check including 'notice'"
      pattern: "role in \\('user', 'assistant', 'notice'\\)"
---

<objective>
Lay the Phase 13 contracts: the combined additive migration (user_preferences table + per-user RLS + threads.model column + messages.role CHECK widened to allow 'notice'), the Pydantic request/response models, and the Wave 0 RED test scaffolds that downstream plans (03/04) turn green.

Purpose: interface-first ordering — every downstream backend plan implements against the schemas and the failing tests defined here, with no codebase scavenger hunt. This plan creates files only (SQL + schemas + tests); it does NOT apply the migration (Plan 02) and does NOT implement endpoints (Plans 03/04).
Output: one migration file, schema additions, four backend test files (three new + one extended), all RED.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-preferences-per-thread-model/13-CONTEXT.md
@.planning/phases/13-preferences-per-thread-model/13-RESEARCH.md
@.planning/phases/13-preferences-per-thread-model/13-VALIDATION.md

<interfaces>
<!-- Source-of-truth analogs. Use these directly; do not re-derive. -->

From supabase/migrations/20240301000025_create_user_api_keys.sql (own-row RLS pattern to MIRROR — but do NOT REVOKE SELECT here; prefs are non-secret):
  CREATE TABLE user_api_keys ( user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE, ... );
  ALTER TABLE user_api_keys ENABLE ROW LEVEL SECURITY;
  CREATE POLICY "Users can view own key row"   ON user_api_keys FOR SELECT USING (auth.uid() = user_id);
  CREATE POLICY "Users can insert own key row" ON user_api_keys FOR INSERT WITH CHECK (auth.uid() = user_id);
  CREATE POLICY "Users can update own key row" ON user_api_keys FOR UPDATE USING (auth.uid() = user_id);
  CREATE POLICY "Users can delete own key row" ON user_api_keys FOR DELETE USING (auth.uid() = user_id);

From supabase/migrations/20240301000002_create_messages.sql (the role CHECK that BLOCKS 'notice' — VERIFIED, inline-unnamed → Postgres auto-names it messages_role_check):
  role text not null check (role in ('user', 'assistant')),

From backend/models/schemas.py (ThreadResponse to EXTEND with model; existing shape):
  class ThreadResponse(BaseModel): id: str; title: str | None; created_at: datetime; updated_at: datetime
  class ThreadWithMessages(ThreadResponse): messages: list["MessageResponse"]

From backend/tests/test_models_api.py (the EXACT patching style new endpoint tests MUST mirror):
  app.dependency_overrides[get_user_id] = lambda: "user-uuid"
  with patch("routers.<name>.get_supabase", return_value=mock_db): ...
  finally: app.dependency_overrides.clear()

From backend/tests/test_key_model_resolution.py (helpers to REUSE in new test for the extended case):
  _fake_settings(**overrides)  # MagicMock Settings: llm_model, resolved_llm_api_key, demo_fallback_enabled, demo_fallback_model
  _db_with_key_row(encrypted_key, pref_model=None, pref_raises=False)  # fake supabase for _resolve_key_and_model

From backend/tests/conftest.py:
  mock_stream_chat_completion (line ~74) — programmable stream_chat_completion patch; reuse for deprecation-fallback test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Combined additive migration (user_preferences + threads.model + messages role CHECK)</name>
  <read_first>
    - supabase/migrations/20240301000025_create_user_api_keys.sql (own-row RLS pattern; do NOT copy the REVOKE)
    - supabase/migrations/20240301000002_create_messages.sql (the role CHECK that must be widened — constraint auto-named messages_role_check)
    - supabase/migrations/20240301000001_create_threads.sql (threads RLS that threads.model inherits)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Pattern 1; Assumption A1 — messages.role constraint confirmed present)
  </read_first>
  <action>
    Create supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql as ONE additive migration (continues the timestamped series; latest existing is ...000031). Three statements:
    (1) CREATE TABLE user_preferences with columns: user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE; default_model TEXT (nullable — null means owner default per D-05); theme TEXT NOT NULL DEFAULT 'dark' CHECK (theme IN ('light','dark')) per the invalid-theme-poisoning mitigation; created_at TIMESTAMPTZ NOT NULL DEFAULT now(); updated_at TIMESTAMPTZ NOT NULL DEFAULT now(). Then ALTER TABLE user_preferences ENABLE ROW LEVEL SECURITY and four own-row policies (SELECT/INSERT/UPDATE/DELETE, each auth.uid() = user_id) mirroring user_api_keys. DO NOT REVOKE SELECT — preferences are non-secret (RESEARCH Security Domain; contrast with user_api_keys). Add a comment noting default_model is intentionally NOT a FK to model_cache (D-06: a deprecated-but-pinned string must persist).
    (2) ALTER TABLE threads ADD COLUMN model TEXT (nullable, no DEFAULT, no backfill — existing threads resolve via the default tier per D-05). It inherits the existing threads RLS automatically.
    (3) Widen the messages role CHECK so the deprecation notice row (role 'notice') can insert: ALTER TABLE messages DROP CONSTRAINT messages_role_check; ALTER TABLE messages ADD CONSTRAINT messages_role_check CHECK (role in ('user', 'assistant', 'notice')). This closes RESEARCH Assumption A1 (the inline unnamed CHECK in migration 000002 is auto-named messages_role_check by Postgres). Add a comment that 'notice' is the persisted deprecation-fallback line (D-06) excluded from LLM history.
    Keep the whole migration additive and idempotent-safe in intent (single transaction, Supabase default). Do NOT apply it here — Plan 02 owns the push.
  </action>
  <verify>
    <automated>grep -E "CREATE TABLE user_preferences|ADD COLUMN model|role in \('user', 'assistant', 'notice'\)|theme IN \('light','dark'\)" supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql exists.
    - Contains literal "CREATE TABLE user_preferences" with user_id UUID PRIMARY KEY and theme CHECK (theme IN ('light','dark')).
    - Contains four user_preferences policies all using auth.uid() = user_id; contains NO "REVOKE SELECT".
    - Contains "ALTER TABLE threads ADD COLUMN model TEXT" (nullable, no DEFAULT).
    - Contains "DROP CONSTRAINT messages_role_check" followed by an ADD CONSTRAINT whose CHECK lists 'user', 'assistant', 'notice'.
    - The grep verify command returns 4 (all four required tokens present).
  </acceptance_criteria>
  <done>The single additive migration creates user_preferences (own-row RLS, no REVOKE), adds nullable threads.model, and widens messages.role to allow 'notice'. Not yet applied to any DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic schemas (PreferencesResponse / PreferencesUpdate / ThreadModelUpdate) + ThreadResponse.model</name>
  <read_first>
    - backend/models/schemas.py (existing ThreadResponse, ThreadWithMessages, MessageResponse, KeyStatusResponse for style)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Patterns 2 + 3; V5 Input Validation — theme is a Literal)
  </read_first>
  <behavior>
    - PreferencesResponse serializes {default_model: str | None, theme: str} (theme always present, defaults filled by the endpoint, never None on the wire).
    - PreferencesUpdate accepts a partial body: default_model: str | None = unset-aware, theme: Literal['light','dark'] | None = unset-aware. A body with only theme set MUST NOT carry default_model (so the endpoint's exclude_unset upsert won't clobber the other field).
    - PreferencesUpdate rejects theme="purple" at validation time (422) because theme is Literal['light','dark'].
    - ThreadModelUpdate accepts {model: str | None} where null is a VALID, explicit value (clears the pin — D-05).
    - ThreadResponse exposes model: str | None (so the now-SELECT-* thread row serializes the column).
  </behavior>
  <action>
    Edit backend/models/schemas.py. Add three models near the thread models / a new "Phase 13 preferences" section:
    PreferencesResponse(BaseModel): default_model: str | None = None; theme: str = "dark".
    PreferencesUpdate(BaseModel): default_model: str | None = None; theme: Literal["light", "dark"] | None = None — both fields default to None so model_dump(exclude_unset=True) in the endpoint sends only what the client provided (per RESEARCH Pattern 2; mirrors the keys.py partial-upsert intent). Use the existing `Literal` import already at top of file.
    ThreadModelUpdate(BaseModel): model: str | None = None — null is a deliberate clear-to-default value; the PATCH endpoint writes it explicitly (NOT exclude_unset — RESEARCH Pattern 3 caution).
    Add `model: str | None = None` to ThreadResponse so the per-thread model rides along on every thread read (ThreadWithMessages inherits it). Do not change existing fields.
    Do NOT implement any endpoint here — schemas only.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -c "from models.schemas import PreferencesResponse, PreferencesUpdate, ThreadModelUpdate, ThreadResponse; PreferencesUpdate(theme='dark'); import pydantic; \
print('model' in ThreadResponse.model_fields); \
exec('try:\n PreferencesUpdate(theme=\"purple\")\n raise SystemExit(\"LITERAL_NOT_ENFORCED\")\nexcept pydantic.ValidationError:\n print(\"literal_ok\")')"</automated>
  </verify>
  <acceptance_criteria>
    - `from models.schemas import PreferencesResponse, PreferencesUpdate, ThreadModelUpdate` imports without error.
    - ThreadResponse.model_fields contains "model".
    - PreferencesUpdate(theme="purple") raises pydantic.ValidationError (Literal enforced) → the verify prints "literal_ok".
    - PreferencesUpdate(theme="dark").model_dump(exclude_unset=True) == {"theme": "dark"} (default_model not present).
    - ThreadModelUpdate(model=None) is valid (null accepted).
  </acceptance_criteria>
  <done>Three new Pydantic models exist with the validation contract above; ThreadResponse exposes model: str | None. No endpoint logic added.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 backend test scaffolds (RED) for MODEL-05, MODEL-06, PREF-02, SC#4</name>
  <read_first>
    - backend/tests/test_models_api.py (the canonical patching style: app.dependency_overrides[get_user_id], patch("routers.<name>.get_supabase"), clear in finally)
    - backend/tests/test_key_model_resolution.py (helpers _fake_settings, _db_with_key_row; existing fallthrough cases to NOT break)
    - backend/tests/conftest.py (mock_stream_chat_completion fixture ~line 74)
    - .planning/phases/13-preferences-per-thread-model/13-VALIDATION.md (Per-Task Verification Map — test function names MUST match verbatim so downstream <verify> commands resolve)
    - backend/routers/chat.py:122-207 (the resolution helpers the new resolution case asserts against)
  </read_first>
  <action>
    Create three new test files and extend one, with the EXACT function names from 13-VALIDATION.md (downstream plans' verify commands reference them by name). Mirror test_models_api.py patching. Tests are RED now (routers/preferences does not exist; PATCH not added; deprecation check not added) — that is correct for Wave 0.
    (1) backend/tests/test_preferences_api.py — functions: test_put_then_get_default_model (PUT {default_model} upserts, GET returns it), test_get_defaults_for_new_user (no row → GET returns {"default_model": None, "theme": "dark"}), test_theme_persist_and_validate (PUT {theme:"light"} persists; assert the upsert payload binds user_id from the dependency-override JWT, NEVER from the body — IDOR/cross-user mitigation). Patch target: patch("routers.preferences.get_supabase", ...). Use app.dependency_overrides[get_user_id].
    (2) backend/tests/test_thread_model_patch.py — functions: test_patch_sets_model (PATCH /api/threads/{id} {model:"x/y"} → threads.update called with {"model":"x/y"} scoped by .eq("id",tid).eq("user_id",uid)), test_patch_null_clears (PATCH {model:null} → update called with {"model": None} explicitly, NOT skipped — the clear-to-default path), and a 404 case for a non-owned thread (ownership re-check returns no row → 404). Patch target: patch("routers.threads.get_supabase", ...).
    (3) backend/tests/test_deprecated_model_fallback.py — functions: test_inserts_notice_and_falls_back (thread.model set to a string ABSENT from model_cache at send time → a messages row with role "notice" is inserted AND the model passed to stream_chat_completion is the user default, not the deprecated string; no exception), test_notice_excluded_from_history (the history list sent to the LLM filters role to ('user','assistant') — a 'notice' row never appears in the messages passed to stream_chat_completion). Reuse mock_stream_chat_completion + the _db_with_key_row mock shape; patch routers.chat.get_supabase / stream_chat_completion as the existing chat tests do.
    (4) backend/tests/test_key_model_resolution.py — ADD test_thread_model_wins_when_set: with the real column present (thread_row carries model="thread/pinned"), _resolve_key_and_model returns model == "thread/pinned" over the user default (proves no regression once the column exists). Do not modify existing cases.
    Where an endpoint/behavior does not yet exist, write the test so it FAILS on the missing import/attribute/behavior (RED) — do not xfail/skip; downstream plans flip them green.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_preferences_api.py tests/test_thread_model_patch.py tests/test_deprecated_model_fallback.py tests/test_key_model_resolution.py --collect-only -q 2>&1 | grep -E "test_put_then_get_default_model|test_get_defaults_for_new_user|test_theme_persist_and_validate|test_patch_sets_model|test_patch_null_clears|test_inserts_notice_and_falls_back|test_notice_excluded_from_history|test_thread_model_wins_when_set" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist; collection succeeds (no syntax/collection errors).
    - The verify command finds all 8 required test function names (returns 8).
    - Running `pytest tests/test_preferences_api.py -x` FAILS (RED) — routers.preferences does not exist yet (this is the expected Wave 0 state; record the RED output in the SUMMARY).
    - test_key_model_resolution.py existing cases still collect (no edits to existing functions).
  </acceptance_criteria>
  <done>Four backend test files carry the 8 named cases from 13-VALIDATION.md, RED as expected, ready for Plans 03/04 to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (PUT/PATCH bodies) | Untrusted body fields (default_model, theme, model) cross here; user_id must come from JWT, never body |
| DB schema constraints | theme + role values are constrained at the DB layer as a backstop to app validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | user_preferences.theme | mitigate | DB CHECK (theme IN ('light','dark')) created in Task 1; Pydantic Literal in Task 2 — invalid theme rejected before paint |
| T-13-02 | Elevation of Privilege | user_preferences RLS | mitigate | Own-row RLS (auth.uid() = user_id) on all four ops in Task 1; cross-user read/write blocked at DB |
| T-13-03 | Information Disclosure | user_preferences SELECT grant | accept | Preferences are non-secret → intentionally do NOT REVOKE SELECT (unlike user_api_keys); rationale documented in migration |
| T-13-04 | Tampering | messages.role 'notice' | mitigate | CHECK widened to an explicit allowlist ('user','assistant','notice') in Task 1 — no arbitrary role strings |
</threat_model>

<verification>
- Migration file present with all four token checks (Task 1 verify == 4).
- Schemas import + Literal enforcement + ThreadResponse.model field (Task 2 verify).
- 8 named Wave 0 tests collectable and RED (Task 3 verify == 8).
- `cd backend && ./venv/Scripts/python -m pytest tests/test_key_model_resolution.py -q` — existing resolution cases still pass (no regression from the added case file edit).
</verification>

<success_criteria>
- One additive migration file creates user_preferences (own-row RLS, no REVOKE), nullable threads.model, and a widened messages.role CHECK allowing 'notice'.
- PreferencesResponse / PreferencesUpdate / ThreadModelUpdate exist with the locked validation contract; ThreadResponse exposes model: str | None.
- Four backend test files carry the 8 named cases, RED, aligned to 13-VALIDATION.md.
- Nothing applied to any database (Plan 02 owns the push).
</success_criteria>

<output>
After completion, create `.planning/phases/13-preferences-per-thread-model/13-01-SUMMARY.md` (record the RED pytest output as the expected Wave 0 baseline).
</output>
