---
phase: 09-crypto-encrypted-key-storage-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240301000025_create_user_api_keys.sql
  - supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql
  - backend/services/sql_service.py
  - backend/tests/test_sql_keys_lockdown.py
autonomous: true
requirements: [KEY-02, SEC-02]
must_haves:
  truths:
    - "A user_api_keys migration creates a ciphertext-only table (encrypted_key + key_version, NO plaintext column) with per-user own-row RLS"
    - "The migration REVOKEs SELECT on user_api_keys from the authenticated role (privilege precedes RLS — denies the SQL-tool read path)"
    - "execute_readonly_query enforces a positive FROM-table allowlist of exactly {threads, messages, documents, document_chunks}; user_api_keys is excluded by default"
    - "A model-authored 'select * from user_api_keys' is rejected by the allowlist (unit-tested at a deterministic seam)"
    - "Legitimate threads/messages/documents/document_chunks queries still pass the allowlist (no regression)"
    - "Implements D-01 (defense-in-depth: REVOKE SELECT on user_api_keys FROM authenticated + positive FROM-table allowlist {threads, messages, documents, document_chunks}) and the SEC-02 probe half of D-05; writes the migrations that D-03 applies to dev in Plan 03"
  artifacts:
    - path: "supabase/migrations/20240301000025_create_user_api_keys.sql"
      provides: "user_api_keys table + per-user RLS + REVOKE SELECT FROM authenticated"
      contains: "revoke select on user_api_keys from authenticated"
    - path: "supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql"
      provides: "CREATE OR REPLACE execute_readonly_query with FROM-table allowlist"
      contains: "execute_readonly_query"
    - path: "backend/tests/test_sql_keys_lockdown.py"
      provides: "SEC-02 exfiltration probe (allowlist seam)"
      contains: "def test_keys_table_query_blocked"
  key_links:
    - from: "supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql"
      to: "execute_readonly_query"
      via: "FROM/JOIN identifier extraction + allowlist membership check before SET LOCAL role"
      pattern: "from.*join"
---

<objective>
Build the SQL-tool exfiltration defense (SEC-02) plus the keys-table schema (KEY-02 storage half): the `user_api_keys` migration (table + per-user RLS + `key_version` + `REVOKE SELECT ... FROM authenticated`) and the `execute_readonly_query` hardening migration that adds a positive FROM-table allowlist of exactly `{threads, messages, documents, document_chunks}`. A deterministic unit test proves the keys-table query is rejected while legitimate queries pass.

Purpose: Two independent DB-layer gates (defense-in-depth per D-01) make `user_api_keys` provably unreachable by the chat's Text-to-SQL tool. The REVOKE denies the read path entirely (privilege checked before RLS); the allowlist is the second gate that excludes any non-allowlisted table by default. This is the storage + lockdown half of Phase 9; the live apply to dev happens in Plan 03 (blocking).

Output: two new migration files (025, 026), an additive allowlist helper in sql_service.py (single Python source of truth for the allowlist set), and a new SEC-02 probe test.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-CONTEXT.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-PATTERNS.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-VALIDATION.md

<interfaces>
supabase/migrations/20240301000015_execute_readonly_query.sql — the RPC being hardened (CREATE OR REPLACE it, preserving every gate):
- Signature: execute_readonly_query(query_text TEXT, max_rows INT DEFAULT 50, calling_user_id UUID DEFAULT NULL) RETURNS JSONB, LANGUAGE plpgsql, SECURITY DEFINER.
- Gate 1 (line 21): IF NOT (lower(sanitized) ~ '^(select|with)\s') THEN RAISE — SELECT/WITH only.
- Gate 2 (line 26): keyword block ~ '\b(insert|update|delete|drop|alter|create|truncate|grant|revoke|execute|copy)\b' — already blocks grant/revoke in user SQL.
- RLS context (lines 30-32): PERFORM set_config('request.jwt.claim.sub', calling_user_id::text, true); SET LOCAL role = 'authenticated';  <- the line the REVOKE applies against.
- Execution (lines 35-39): EXECUTE format('... FROM (%s) sub LIMIT %s ...', sanitized, max_rows). The allowlist MUST inspect the inner `sanitized` SQL, NOT the wrapper.
- Cleanup (lines 42-44): RESET role; RETURN result;

supabase/migrations/20240301000018_create_folders_table.sql + 20240301000020_update_rls_policies.sql — the table+RLS analog:
- CREATE TABLE with `user_id UUID NOT NULL REFERENCES auth.users(id)`, TIMESTAMPTZ NOT NULL DEFAULT now() timestamps, ALTER TABLE ... ENABLE ROW LEVEL SECURITY.
- Own-row policy wording: `auth.uid() = user_id` (SELECT/UPDATE/DELETE use USING; INSERT uses WITH CHECK). For keys, DROP the `OR visibility = 'public'` clause — the keys table is NEVER public, own-row only.

backend/services/sql_service.py — the SQL-tool entry point (you ADD an allowlist helper here; do NOT change execute_sql's runtime path):
- QUERYABLE_SCHEMA (lines 8-41) advertises exactly: threads, messages, documents, document_chunks. The allowlist MUST match this set.
- execute_sql(user_id: str, query: str) -> dict (line 48) calls db.rpc("execute_readonly_query", {"query_text": query, "max_rows": ..., "calling_user_id": user_id}).execute(); returns {"success": bool, "rows": list, "row_count": int, ...}. On exception returns {"success": False, "error": str, "rows": [], "row_count": 0}.

backend/tests/test_folders_api.py — the test analog:
- top-of-file sys.path insert (lines 11-13); _result(data) helper; TEST_USER_ID = "11111111-1111-1111-1111-111111111111".
- conftest.py also inserts backend/ on sys.path.

RECONCILED ALLOWLIST (D-01, RESEARCH Open Question 1 CLOSED): the FROM-table allowlist is exactly {threads, messages, documents, document_chunks} — matching QUERYABLE_SCHEMA. Do NOT use the RESEARCH-era {documents, document_chunks, folders}. folders is intentionally excluded (KB-tree/grep/glob tools, not Text-to-SQL); user_api_keys is excluded by default. Security intent unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write the failing SEC-02 lockdown probe + the allowlist seam it asserts against</name>
  <files>backend/services/sql_service.py, backend/tests/test_sql_keys_lockdown.py</files>
  <read_first>
    - backend/tests/test_folders_api.py (lines 1-75 — the patch(get_supabase)+MagicMock chain pattern and _result helper)
    - backend/services/sql_service.py (execute_sql signature line 48; QUERYABLE_SCHEMA lines 8-41 — the canonical allowlist set)
    - backend/tests/conftest.py (sys.path insert; TEST_USER_ID via explorer_fixtures)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md (Pitfall 5 lines 310-314 — false-PASS-against-mock warning; Open Question 2 lines 407-410 — probe-layer choice; Code Examples lines 364-379)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-PATTERNS.md (test_sql_keys_lockdown.py NEW section lines 204-224 — patch target services.sql_service.get_supabase; real param name is query= not sql=)
  </read_first>
  <behavior>
    - A pure-Python allowlist helper extracts FROM/JOIN table identifiers from a SQL string and returns whether ALL are in {threads, messages, documents, document_chunks}.
    - test_keys_table_query_blocked — the helper rejects "select * from user_api_keys" (returns False), proving the keys table is non-allowlisted at a deterministic, mockable seam (NOT just a mocked DB return).
    - test_allowlisted_queries_pass — the helper accepts "select count(*) from threads", "select * from messages", "select id from documents", and a documents JOIN document_chunks query (regression guard so legitimate Text-to-SQL still works).
    - test_user_api_keys_not_in_allowlist — asserts user_api_keys (and folders) are NOT members of the allowlist set.
  </behavior>
  <action>
    Per RESEARCH Open Question 2 / Pitfall 5: the durable CI gate must assert the ALLOWLIST LOGIC at a deterministic seam, not a mocked DB return (a purely-mocked probe is a false PASS). Add a small pure-Python allowlist helper that mirrors the SQL allowlist so the unit test is deterministic and the same set is asserted in two places. Create the helper as a module-level constant + function in backend/services/sql_service.py: ALLOWED_SQL_TABLES = a frozenset of exactly {"threads", "messages", "documents", "document_chunks"} and a function is_query_allowlisted(query: str) -> bool that lower-cases the query, extracts every identifier following the from or join keyword via a case-insensitive regex (the same shape used in migration 026: word after from/join, optional double-quote, identifier chars), and returns True only if every extracted table is in ALLOWED_SQL_TABLES (empty match also returns False — defensive). Do NOT wire this helper into execute_sql's runtime path in this plan (the DB RPC remains the enforcing gate); it exists so the allowlist set has a single Python source of truth and is unit-testable. This satisfies the Nyquist gate that the SQL allowlist set is regression-covered.

    Create backend/tests/test_sql_keys_lockdown.py (NEW) with the three tests above, importing is_query_allowlisted and ALLOWED_SQL_TABLES from services.sql_service. Follow the test_folders_api.py top-of-file sys.path insert convention. The helper is trivial pure Python, so the file ends GREEN at task close.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_sql_keys_lockdown.py -x</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_sql_keys_lockdown.py contains def test_keys_table_query_blocked, def test_allowlisted_queries_pass, and def test_user_api_keys_not_in_allowlist.
    - backend/services/sql_service.py contains ALLOWED_SQL_TABLES and def is_query_allowlisted(.
    - ALLOWED_SQL_TABLES equals exactly {"threads", "messages", "documents", "document_chunks"} (no folders, no user_api_keys).
    - cd backend && venv/Scripts/python.exe -m pytest tests/test_sql_keys_lockdown.py -x exits 0 (allowlist rejects user_api_keys, accepts the four legitimate tables).
    - is_query_allowlisted("select * from user_api_keys") returns False; is_query_allowlisted("select count(*) from threads") returns True.
  </acceptance_criteria>
  <done>SEC-02 allowlist set has a single Python source of truth, unit-tested to reject user_api_keys and accept the four legitimate tables — the deterministic CI gate per RESEARCH Open Question 2.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 025 — user_api_keys table (ciphertext-only) + per-user RLS + REVOKE</name>
  <files>supabase/migrations/20240301000025_create_user_api_keys.sql</files>
  <read_first>
    - supabase/migrations/20240301000018_create_folders_table.sql (CREATE TABLE + auth.users FK + ENABLE ROW LEVEL SECURITY + header-comment + 4-policy block structure)
    - supabase/migrations/20240301000020_update_rls_policies.sql (the auth.uid() = user_id own-row policy wording — take this, DROP the visibility='public' clause)
    - supabase/migrations/20240301000015_execute_readonly_query.sql (line 26 — confirms the RPC keyword-blocks revoke, so the REVOKE MUST live in this migration file, never via the RPC)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md (Pattern 2 lines 194-224 — proposed schema + REVOKE + privilege-before-RLS rationale)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-PATTERNS.md (migration 025 section lines 94-129)
  </read_first>
  <action>
    Create supabase/migrations/20240301000025_create_user_api_keys.sql (next free number — last migration is ...024). Start with a header comment block (purpose + RLS intent + Depends-on, mirroring migration 018 lines 1-4) noting this is the BYOK key store: ciphertext-only, per-user RLS, SQL-tool REVOKE'd (Phase 9 D-01/D-02). Each migration runs as one transaction.

    CREATE TABLE user_api_keys with columns: user_id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE (one key per user — PK is user_id itself per D-01 carried-forward); provider text NOT NULL DEFAULT 'openrouter'; encrypted_key text NOT NULL (Fernet ciphertext — NEVER a plaintext column); key_version integer NOT NULL DEFAULT 1 (D-02 — which master key encrypted this row); key_label text (nullable; OpenRouter display label set in Phase 10); created_at timestamptz NOT NULL DEFAULT now(); updated_at timestamptz NOT NULL DEFAULT now(). There is NO plaintext key column and NO visibility column.

    ALTER TABLE user_api_keys ENABLE ROW LEVEL SECURITY. Then create FOUR own-row policies, all gated on plain auth.uid() = user_id (NO public clause): SELECT and UPDATE and DELETE use USING (auth.uid() = user_id); INSERT uses WITH CHECK (auth.uid() = user_id). Name them clearly (e.g. "Users can view own key row", etc.).

    Add the SEC-02 GATE 1 REVOKE as the final statement, with a comment explaining privilege-is-checked-before-RLS so this denies the SQL-tool ('authenticated') read path entirely while the service-role backend is unaffected: revoke select on user_api_keys from authenticated;
  </action>
  <verify>
    <automated>grep -iq "create table user_api_keys" "supabase/migrations/20240301000025_create_user_api_keys.sql" && grep -iq "revoke select on user_api_keys from authenticated" "supabase/migrations/20240301000025_create_user_api_keys.sql" && grep -iq "key_version" "supabase/migrations/20240301000025_create_user_api_keys.sql" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20240301000025_create_user_api_keys.sql exists.
    - Contains CREATE TABLE user_api_keys with user_id as PRIMARY KEY referencing auth.users(id) ON DELETE CASCADE.
    - Contains columns encrypted_key text NOT NULL and key_version integer NOT NULL DEFAULT 1; contains NO plaintext column (only encrypted_key) and NO visibility column.
    - Contains ALTER TABLE user_api_keys ENABLE ROW LEVEL SECURITY and four auth.uid() = user_id policies (no OR visibility = 'public').
    - Contains the literal revoke select on user_api_keys from authenticated.
  </acceptance_criteria>
  <done>Ciphertext-only user_api_keys table with per-user RLS and the SQL-tool REVOKE is written — Gate 1 of SEC-02 plus the KEY-02 storage shape.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 026 — harden execute_readonly_query with the FROM-table allowlist (SEC-02 Gate 2)</name>
  <files>supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql</files>
  <read_first>
    - supabase/migrations/20240301000015_execute_readonly_query.sql (the FULL current function body — you CREATE OR REPLACE it preserving every existing gate verbatim)
    - backend/services/sql_service.py (QUERYABLE_SCHEMA lines 8-41 + the ALLOWED_SQL_TABLES set from Task 1 — the allowlist must match exactly: threads, messages, documents, document_chunks)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md (Pattern 3 lines 226-249 — allowlist placement + regex shape; Pitfall 4 lines 304-308 — regex pitfalls / CTE handling)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-PATTERNS.md (migration 026 section lines 133-160 — RECONCILED allowlist {threads, messages, documents, document_chunks}, gates to preserve)
  </read_first>
  <action>
    Create supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql. Use CREATE OR REPLACE FUNCTION execute_readonly_query(query_text TEXT, max_rows INT DEFAULT 50, calling_user_id UUID DEFAULT NULL) RETURNS JSONB LANGUAGE plpgsql SECURITY DEFINER — redefining the WHOLE body. PRESERVE every existing gate verbatim from migration 015: the trim; the SELECT/WITH-only check (line 21); the dangerous-keyword block including grant|revoke (line 26); the RLS context (set_config request.jwt.claim.sub + SET LOCAL role = 'authenticated', lines 30-32); the subquery-wrap + LIMIT EXECUTE format (lines 35-39); RESET role; RETURN result (lines 42-44).

    INSERT the NEW FROM-table allowlist (SEC-02 Gate 2, D-01) AFTER the keyword block and BEFORE the SET LOCAL role line. It must inspect the inner sanitized SQL (the user's query), not the wrapper. Use a positive default-deny allowlist of EXACTLY ('threads', 'messages', 'documents', 'document_chunks') — matching QUERYABLE_SCHEMA and the ALLOWED_SQL_TABLES set from Task 1. Loop over every identifier following from or join via regexp_matches(sanitized, '(?:from|join)\s+["]?([a-z_][a-z0-9_]*)', 'gi'), lower-cased, and RAISE EXCEPTION 'Query references a non-allowlisted table: %' for any table NOT in the allowlist. Do NOT include folders or user_api_keys in the allowlist. Add a comment block citing D-01, the reconciled set, and that this is the second independent gate after the REVOKE in migration 025. Keep CTE-alias handling in mind (Pitfall 4) — a CTE name will appear as a FROM target; decide and comment whether self-referencing CTE aliases are tolerated, but DO NOT widen the allowlist to admit real non-allowlisted tables.
  </action>
  <verify>
    <automated>grep -iq "create or replace function execute_readonly_query" "supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql" && grep -iq "documents" "supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql" && grep -iq "non-allowlisted table" "supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql exists and uses CREATE OR REPLACE FUNCTION execute_readonly_query.
    - The function body preserves the SELECT/WITH-only check, the keyword block, the SET LOCAL role = 'authenticated' RLS context, the subquery-wrap LIMIT EXECUTE, and RESET role.
    - The allowlist set is exactly ('threads', 'messages', 'documents', 'document_chunks') — file does NOT contain 'folders' or 'user_api_keys' in the allowlist IN (...) clause.
    - The allowlist check is placed after the keyword block and before SET LOCAL role, and RAISEs on a non-allowlisted table.
    - grep "non-allowlisted table" matches (the deny path exists).
  </acceptance_criteria>
  <done>execute_readonly_query now enforces a positive FROM-table allowlist matching QUERYABLE_SCHEMA — Gate 2 of SEC-02, written and ready for the live apply in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-authored SQL -> execute_readonly_query (Text-to-SQL tool) | Untrusted, prompt-injectable SQL crosses here under the user's JWT (role = authenticated). This is the SEC-02 exfiltration surface. |
| authenticated role -> user_api_keys | The SQL tool runs as authenticated; this boundary must deny all access to the keys table. |
| service-role backend -> user_api_keys | Trusted; bypasses RLS and REVOKE by design (write/read the key server-side). Not a threat surface this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Information Disclosure | prompt-injected `select * from user_api_keys` via execute_readonly_query (the core SEC-02 threat, T-09) | mitigate | TWO independent gates (D-01 defense-in-depth): (1) REVOKE SELECT ON user_api_keys FROM authenticated in migration 025 — privilege checked before RLS, denies the read path outright; (2) positive FROM-table allowlist in migration 026 excluding user_api_keys by default. Unit-tested at the allowlist seam (Task 1). |
| T-09-07 | Information Disclosure | own-row ciphertext returnable under user JWT via RLS-only scoping | mitigate | REVOKE removes the authenticated read path entirely; RLS own-row policy is defense-in-depth only. The model cannot retrieve even the user's own ciphertext row. |
| T-09-08 | Tampering | allowlist regex too loose (admits a non-allowlisted table) or too strict (breaks legitimate queries) | mitigate | Positive default-deny allowlist of exactly the QUERYABLE_SCHEMA set; regression test (Task 1) asserts legitimate threads/messages/documents/document_chunks queries pass AND user_api_keys is rejected (Pitfall 4). |
| T-09-09 | Elevation of Privilege | issuing REVOKE/GRANT through the RPC | accept | Already mitigated by the existing keyword block (migration 015 line 26 blocks grant|revoke); the REVOKE lives in the migration file, never via the RPC. No new code needed. |
| T-09-10 | Information Disclosure | plaintext-at-rest in the keys table | mitigate | Schema has no plaintext column — only encrypted_key (Fernet ciphertext) + key_version (Task 2 acceptance check); a raw DB dump leaks ciphertext only. |
</threat_model>

<verification>
- cd backend && venv/Scripts/python.exe -m pytest tests/test_sql_keys_lockdown.py -x exits 0 (allowlist rejects user_api_keys, accepts the four legitimate tables).
- cd backend && venv/Scripts/python.exe -m pytest tests/ -q — full backend suite green (no regression; the new helper is additive and not wired into the runtime path).
- Migration 025 contains the CREATE TABLE, ENABLE RLS, four own-row policies, and the REVOKE.
- Migration 026 CREATE OR REPLACEs the RPC with the exact reconciled allowlist and preserves all prior gates.
- NOTE: the LIVE effect of the REVOKE + allowlist (success criteria #2 and #3) is proven against the dev Supabase project in Plan 03 (the blocking apply) — this plan writes the SQL; Plan 03 applies and probes it live.
</verification>

<success_criteria>
- ROADMAP criterion #2 (migration creates a ciphertext-only table with per-user RLS + key_version) — SQL written (Task 2); live-applied in Plan 03.
- ROADMAP criterion #3 (prompt-injected select * from user_api_keys returns nothing via REVOKE + RPC allowlist) — both gates written (Tasks 2+3) and the allowlist seam unit-tested (Task 1); live-applied + probed in Plan 03.
- Allowlist reconciled to {threads, messages, documents, document_chunks} per D-01 (RESEARCH Open Question 1 closed) with a single Python source of truth.
</success_criteria>

<output>
After completion, create `.planning/phases/09-crypto-encrypted-key-storage-foundation/09-02-SUMMARY.md`
</output>
