# Phase 999.2: Cost-Guardrail Burn Script - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-30
**Phase:** 999.2-cost-guardrail-burn-script
**Areas discussed:** Burn target + env, What closes SEC-03, Kill-switch definition, Spend ceiling + abort, Guardrail precondition

---

## Burn target + environment

| Option | Description | Selected |
|--------|-------------|----------|
| Prod owner/demo account | The real account that funds the demo fallback. Faithful; safe now (demo flag OFF → no user impact). | ✓ |
| Throwaway account (~$1) | Isolated separate account; identical guardrail behavior but doesn't prove the real account's guardrail. | |
| Dev account / .env key | Lowest stakes, least representative. | |

**User's choice:** Prod owner/demo account.
**Notes:** User added the operational model — demo fallback is meant to be **owner-toggled ON for demoing**; everyone else uses **BYOK** (connect their own OpenRouter account). The guardrail + kill switch bound owner-key exposure during owner-initiated demo windows. → Captured as D-01 + D-02.

---

## What closes SEC-03 (done-bar)

| Option | Description | Selected |
|--------|-------------|----------|
| Block + kill-switch, email noted | Block-after-$0.10 + kill-switch demo + email outcome recorded (no-email accepted). | ✓ |
| Block-only | Just confirm the block; skip email + kill-switch demo. | |
| Require email confirmation | Only close if email-on-trip positively confirmed (risky — may never close). | |

**User's choice:** Block + kill-switch, email noted (Recommended).
**Notes:** → D-04. "No email" is an accepted finding; closure is NOT gated on a positive email.

---

## Kill-switch definition

| Option | Description | Selected |
|--------|-------------|----------|
| Both: app flag + provider guardrail | Prove demo_fallback_enabled=False stops the code path AND the $0.10 guardrail caps spend as backstop. | ✓ |
| App flag only | Rely solely on demo_fallback_enabled=False. | |
| Provider guardrail only | Rely solely on OpenRouter's $0.10 block. | |

**User's choice:** Both, proven independently (Recommended).
**Notes:** → D-05. Defense in depth.

---

## Spend ceiling + abort

| Option | Description | Selected |
|--------|-------------|----------|
| ~$0.10 trip, hard abort at $0.25 | Drive just past $0.10 with paid gpt-4o-mini in small batches; HARD STOP at $0.25 → blocker if no block. | ✓ |
| Headroom to ~$0.50 | Allow up to ~$0.50 for extra certainty. | |
| You decide bounds | Planner picks conservative bounds. | |

**User's choice:** ~$0.10 trip, hard abort at $0.25 (Recommended).
**Notes:** → D-06. Paid model required (free model can't trip a $ guardrail); bound overshoot via serial-ish batches with spend re-check.

---

## Guardrail precondition (closer)

| Option | Description | Selected |
|--------|-------------|----------|
| Already configured | $0.10 guardrail already live on the prod owner account. | ✓ |
| Phase sets it up | Document configuring the guardrail as a precondition. | |
| Not sure — verify first | Plan checks config, sets if missing. | |

**User's choice:** Already configured.
**Notes:** → D-03. Defensive verify-first is acceptable but treat as present.

## Claude's Discretion

- How the script mints paid calls (standalone OpenRouter call vs forcing the demo path with a paid override) — standalone is sufficient since the guardrail is account-level.
- Spend-reading method between batches (`GET /api/v1/key` usage delta vs credits page).
- Batch size / minimal concurrency / inter-batch sleep within D-06 bounds.

## Deferred Ideas

- Recurring/automated owner-key cost monitoring beyond this one-time trip-test — out of scope.
- Faithful demo-path exercise (demo_fallback_enabled=True + paid override) — optional, not required.
