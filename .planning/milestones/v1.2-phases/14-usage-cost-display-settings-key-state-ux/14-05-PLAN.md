---
phase: 14-usage-cost-display-settings-key-state-ux
plan: 05
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/ChatPage.tsx
autonomous: true
requirements: [COST-02, COST-03, PREF-01]

must_haves:
  truths:
    - "The settings page shows a tri-state key status with locked copy: 'Your key: connected' (+ masked label, connected-since, balance) vs 'No key — connect to chat'"
    - "The balance line renders all four states: Balance: $X / Pay-as-you-go — no limit set / Checking balance… / Balance unavailable right now."
    - "A low capped balance shows an amber warning line near the balance (settings surface #2)"
    - "Default model and Theme are full settings sections; their temporary ChatPage inline mounts are removed with no dangling empty footer wrapper"
    - "Implements decisions D-05 surface #2 (settings balance warning line), D-06 (relocate default-model + theme controls into Settings, remove ChatPage temp mounts), and D-07 (per-thread model selector left untouched in the thread header)"
    - "The settings page reads coherently in BOTH light and dark themes (no orphan dark panel)"
    - "SCOPE NOTE (D-08 / SC#4): the settings key-state ships TWO LIVE states this phase — 'Your key: connected' and 'No key — connect to chat'. The third D-08 state, 'Demo mode', is STRUCTURALLY RESERVED (not built as dead UI): demo_fallback_enabled is OFF this phase and CONTEXT.md Deferred Ideas LOCKS demo enablement to Phase 15, so mode=='demo' is unreachable on any settings surface here (chat.py:196). The Demo branch goes live in Phase 15 when demo_fallback is enabled — the post-execution verifier must NOT false-flag the missing Demo state against D-08's literal three-state wording, and the executor must NOT re-ask the user (the CONTEXT decision is locked)."
  artifacts:
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "3-section theme-aware settings page (OpenRouter + balance/warning, Default model, Theme)"
      contains: "Pay-as-you-go"
    - path: "frontend/src/pages/ChatPage.tsx"
      provides: "prefsControls removed; controls relocated to Settings"
      min_lines: 150
  key_links:
    - from: "SettingsPage balance line"
      to: "useKeyStatus balance/isLow/loading/error"
      via: "tri-state render of the four locked copy states + amber warning when isLow"
      pattern: "Pay-as-you-go|Checking balance"
    - from: "ChatPage"
      to: "ThreadSidebar/ThreadListContent"
      via: "footer prop removed entirely (no dangling empty wrapper)"
      pattern: "footer"
---

**Complexity:** ⚠️ Medium — grow the dark-only `/settings` stub into a 3-section theme-aware page (tri-state copy + 4-state balance line + amber warning) AND a clean ChatPage relocation that must leave no dangling footer and pass strict `noUnusedLocals`. Two files, locked copy, theme-token discipline.

<objective>
Grow the Phase-10 `/settings` stub into the full PREF-01 settings/account page and finish the D-06 relocation. This consolidates the key state (tri-state + masked label + balance + low-balance warning + disconnect), the default-model control, and the theme toggle into one coherent, theme-aware page — and removes the temporary inline mounts from ChatPage.

Purpose: PREF-01 requires a settings/account page that always makes key state unambiguous; D-06 requires the default-model + theme controls to live here (fulfilling Phase 13 D-04's "temp spot until P14 absorbs it"); D-05 requires the settings-side low-balance warning. The page is currently dark-only and must become theme-aware now that the theme toggle lives inside it.

Output: a 3-section SettingsPage (OpenRouter / Default model / Theme) with the balance + warning lines and tri-state copy, theme-aware surfaces; a cleaned-up ChatPage with the prefsControls cluster and footer props removed.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-CONTEXT.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-02-SUMMARY.md

<interfaces>
<!-- Components/hooks this plan composes (no internal edits to the relocated components). -->

DefaultModelSelector (frontend/src/components/DefaultModelSelector.tsx): self-PUTs /api/preferences
  {default_model} on select AND renders its OWN "Default model" heading + helper. Props:
  { value: string | null; onChange?: (id: string) => void; models?: ModelResponse[] }.
  → In Settings, mount it directly; do NOT add a duplicate "Default model" heading.

ThemeToggle (frontend/src/components/ThemeToggle.tsx): self-contained neutral h-11 w-11 button that
  self-PUTs theme. Mount under a "Theme" section heading (UI-SPEC: heading row + toggle).

useKeyStatus (Plan 02): exposes { status:{connected, masked_label, connected_at}, loading,
  isLow, balance:{limit_remaining, is_low}|null, balanceLoading, balanceError }.
  → If 14-02-SUMMARY records DIFFERENT final field names, bind to the names in that SUMMARY.

formatConnectedSince (already in SettingsPage.tsx:9-17) — keep unchanged.
ConfirmDialog — reuse the existing disconnect confirm (copy unchanged).

ChatPage footer: ThreadSidebar + ThreadListContent both accept an OPTIONAL `footer?: ReactNode`
  (ThreadSidebar.tsx:20, rendered only when truthy :75-76) — removing the prop leaves no empty wrapper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grow SettingsPage to 3 theme-aware sections + balance/warning lines + tri-state copy</name>
  <files>frontend/src/pages/SettingsPage.tsx</files>
  <read_first>
    - frontend/src/pages/SettingsPage.tsx (whole file — dark-only wrapper :37-38, OpenRouter section + connected/no-key states :41-95, formatConnectedSince :9-17, ConfirmDialog disconnect :86-95)
    - frontend/src/components/ChatContainer.tsx:67-73 (the theme-aware surface tokens to copy: page bg-white dark:bg-gray-950, card bg-gray-50 dark:bg-gray-900, border gray-200 dark:gray-800)
    - frontend/src/components/DefaultModelSelector.tsx (it self-PUTs + carries its own heading) and frontend/src/components/ThemeToggle.tsx
    - frontend/src/pages/ChatPage.tsx:59-90 (the /api/models + /api/preferences mount-fetch pattern to mirror for seeding defaultModel + models here)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md § SettingsPage.tsx (Analogs A/B + balance/warning copy)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Copywriting Contract (tri-state, balance, warning), § Color (light tokens, amber), § Typography, § Components (SettingsPage grow)
  </read_first>
  <action>
    THEME-AWARE SURFACES: rewrite the page wrapper (`:37`) from `bg-gray-950 text-white` to `bg-white text-gray-900 dark:bg-gray-950 dark:text-white`, and the card (`:38`) from `bg-gray-900` to `bg-gray-50 dark:bg-gray-900` (add `border border-gray-200 dark:border-gray-800` to match ChatContainer surfaces). Apply muted `text-gray-600 dark:text-gray-400` to caption text. No orphan dark panel may remain in light mode.
    TRI-STATE COPY (D-08): in the connected branch, change the status line from `Connected` to the LOCKED `Your key: connected`; keep the green dot, masked label (`font-mono text-sm`), and `Connected since ${date}`. In the no-key branch, add the LOCKED status line `No key — connect to chat` above the existing helper body `Connect your OpenRouter account to chat with your own key. No key to copy or paste.` + the `Connect OpenRouter` CTA. The `Demo mode` tri-state copy is structurally reserved: there is no settings-side demo-eligibility signal in Phase 14 and `demo_fallback_enabled` is OFF (chat.py:196), so it will not render this phase — wire the connected/no-key states from `status.connected` and do NOT invent a demo signal or build dead Demo UI (Phase 15 wires it; RESEARCH Open Q2; CONTEXT Deferred Ideas locks demo enablement to Phase 15).
    BALANCE LINE (COST-02, only inside the connected branch, directly under `Connected since`): render the FOUR locked states from the Plan-02 balance surface — `balanceLoading` → `Checking balance…`; `balanceError` (no data) → `Balance unavailable right now.`; `balance.limit_remaining == null` → `Pay-as-you-go — no limit set`; else → `Balance: $${remaining}` (4-decimal as reported, never recomputed). Caption typography `text-xs text-gray-600 dark:text-gray-400`. (Bind to the EXACT field names recorded in 14-02-SUMMARY if they differ from balanceLoading/balanceError/balance.limit_remaining.)
    LOW-BALANCE WARNING LINE (COST-03 / D-05 surface #2, immediately after the balance line, only when `balance.is_low`): a lucide `AlertTriangle` (14px, `amber-500`) + `Balance low: $${remaining} — add credits` in `amber-700` (light) / `amber-300` (dark). Inline caption — NOT a pill, toast, or banner. The lucide icon IS the warning mark (do not also ship a literal `⚠` glyph).
    NEW SECTIONS: own `defaultModel` + `models` state here — fetch `/api/preferences` (seed defaultModel) and `/api/models` (catalog) once on mount, mirroring ChatPage's silent-on-failure, array-guarded pattern. After the OpenRouter section add a **Default model** section: a `mt-6` wrapper that mounts `<DefaultModelSelector value={defaultModel} onChange={setDefaultModel} models={models} />` — do NOT add a duplicate `Default model` heading (the component supplies its own). Then a **Theme** section: an `<h2 className="text-base font-semibold mt-6">Theme</h2>` heading row + `<ThemeToggle />`. Keep the existing ConfirmDialog disconnect flow + `notifyKeyStatusChanged()` unchanged.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint clean.
    - Source assertion (exact locked copy present): `Your key: connected`, `No key — connect to chat`, `Pay-as-you-go — no limit set`, `Checking balance…`, `Balance unavailable right now.`, `Balance low:` and `— add credits`.
    - Source assertion: the page wrapper carries `bg-white` AND `dark:bg-gray-950`; the card carries `bg-gray-50` AND `dark:bg-gray-900` (no remaining dark-only `bg-gray-950`/`bg-gray-900` without a light counterpart); the warning line uses `amber-700` (light) / `amber-300` (dark) + an `AlertTriangle` import.
    - Source assertion: the page wires only the connected/no-key states from `status.connected`; NO `Demo mode` literal and no `mode === 'demo'` branch are added this phase (structurally reserved for Phase 15).
    - Source assertion: `<DefaultModelSelector` and `<ThemeToggle` are mounted; there is no duplicate `>Default model<` heading added by the page (the component owns it).
    - Browser-observable: connected key with a capped low balance → `Your key: connected`, masked label, `Connected since …`, `Balance: $0.40`, amber warning line, Disconnect; null-limit key → `Pay-as-you-go — no limit set`, no warning; no key → `No key — connect to chat` + helper + Connect CTA; toggle light/dark → page + card + all sections coherent in both. Default-model select persists (PUT /api/preferences) and survives reload.
  </acceptance_criteria>
  <done>SettingsPage is a coherent 3-section theme-aware page with the two live tri-state states (Demo reserved for P15), the four-state balance line, the amber low-balance warning, and the relocated Default model + Theme sections; build + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the temporary prefsControls mounts from ChatPage (clean relocation)</name>
  <files>frontend/src/pages/ChatPage.tsx</files>
  <read_first>
    - frontend/src/pages/ChatPage.tsx (whole file — imports :6-7, defaultModel state :31, the /api/preferences hydrate effect :73-90, prefsControls cluster :174-182, footer consumers :199 + :219, per-thread wiring :201-210)
    - frontend/src/components/ThreadSidebar.tsx (footer is optional `footer?: ReactNode`, rendered only when truthy — safe to drop the prop)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md § ChatPage.tsx (the deletion checklist + Pitfall 6: no dangling empty footer)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Components (relocation must not regress)
  </read_first>
  <action>
    DELETE the `prefsControls` block (`:174-182`). Remove the `DefaultModelSelector` and `ThemeToggle` imports (`:6-7`). Remove the `footer={prefsControls}` prop ENTIRELY from BOTH `<ThreadSidebar>` (`:199`) and `<ThreadListContent>` (`:219`) — do NOT leave a `footer={...}` with an empty value (Pitfall 6 / ThreadSidebar renders nothing when footer is absent, so no empty wrapper remains). Remove the now-unused `defaultModel`/`setDefaultModel` state (`:31`) AND the `setDefaultModel(prefs.default_model ?? null)` line inside the `/api/preferences` effect (`:78`) — but KEEP the rest of that effect (the post-login theme reconcile is a global concern and stays). KEEP the `models` state + the `/api/models` mount fetch (`:59-67`) and the `models={models}` prop on `<ChatContainer>` (`:209`) — the per-thread `ModelSelector` still needs the catalog (D-07: the per-thread selector stays in the header). KEEP `handleThreadModelChange` and all per-thread wiring untouched. After editing, confirm no unused-import / unused-variable lint errors remain (the repo's tsconfig has `noUnusedLocals`/`noUnusedParameters`).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint clean (noUnusedLocals will fail the build if defaultModel/imports are left dangling — a clean build proves the removal is complete).
    - Source assertion: `grep -c "prefsControls" frontend/src/pages/ChatPage.tsx` returns 0; `grep -c "DefaultModelSelector" frontend/src/pages/ChatPage.tsx` returns 0; `grep -c "ThemeToggle" frontend/src/pages/ChatPage.tsx` returns 0; `grep -c "footer=" frontend/src/pages/ChatPage.tsx` returns 0.
    - Source assertion: `models` state and the `models={models}` ChatContainer prop are RETAINED; `handleThreadModelChange` is retained.
    - Browser-observable: the desktop sidebar footer and the mobile drawer no longer show the default-model/theme cluster and have NO empty box; the per-thread model selector still works in the chat header; theme still reconciles on login.
  </acceptance_criteria>
  <done>ChatPage no longer mounts the temporary default-model/theme controls, drops the footer prop cleanly with no empty wrapper, retains the per-thread selector + catalog, and builds lint-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useKeyStatus → SettingsPage render | Only non-secret display values (masked label, connected_at, limit_remaining, is_low) cross into the page |
| DefaultModelSelector/ThemeToggle → backend | Each self-PUTs /api/preferences with its own value; no new secret crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-16 | Information Disclosure | settings balance/key display | mitigate | Render only the masked label (existing) + the secret-free balance fields (Plan 01); never the full key; balance failure shows the fixed `Balance unavailable right now.` (no provider error, no HTTP code) |
| T-14-17 | Information Disclosure | balance line copy | mitigate | House-style locked copy only — never interpolate a caught error / provider message / sk-or fragment into the balance or warning line |
| T-14-18 | Tampering | low-balance warning trigger | mitigate | Warning gated on the server-computed `balance.is_low` (Plan 01); the FE never re-derives low-ness from a client threshold |
| T-14-19 | Spoofing | disconnect action | accept | Existing ConfirmDialog + bearer'd DELETE /api/keys bound to the JWT (unchanged Phase-10 control) |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` clean after both tasks.
- Source: `grep -n "Pay-as-you-go" frontend/src/pages/SettingsPage.tsx`; `grep -c "footer=" frontend/src/pages/ChatPage.tsx` == 0.
- Manual browser smoke (VALIDATION.md Manual-Only): open `/settings` — verify all 3 sections + the two live tri-state states + balance states; toggle theme and confirm light/dark coherence; configure LOW_BALANCE_THRESHOLD_USD above a test balance to confirm the amber warning line; confirm ChatPage sidebar/drawer have no orphan footer and the per-thread selector still works.
</verification>

<success_criteria>
- PREF-01: a settings/account page with unambiguous key state (the two live states — connected / no-key) + masked label + balance + the relocated default-model and theme controls.
- SCOPE (D-08 / SC#4): the settings key-state ships 2 LIVE states this phase (connected, no-key); the 'Demo mode' branch is structurally RESERVED — it is unreachable while demo_fallback_enabled is OFF and is LOCKED to Phase 15 by CONTEXT Deferred Ideas. No dead Demo UI is built this phase, and verification must not false-flag the absent Demo state against D-08's literal three-state wording.
- COST-02: balance line with all four locked states (capped / pay-as-you-go / loading / failed).
- COST-03 (surface #2): amber low-balance warning line near the balance.
- D-06 relocation complete with no dangling ChatPage footer; light + dark coherent; build + lint green.
</success_criteria>

<output>
After completion, create `.planning/phases/14-usage-cost-display-settings-key-state-ux/14-05-SUMMARY.md`.
</output>
</content>
