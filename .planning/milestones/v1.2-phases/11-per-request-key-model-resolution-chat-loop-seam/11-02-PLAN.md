---
phase: 11-per-request-key-model-resolution-chat-loop-seam
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240301000029_add_usage_to_messages.sql
autonomous: false
requirements: [DEMO-03]
must_haves:
  truths:
    - "The messages table has a nullable usage column ready to persist captured OpenRouter usage/cost"
    - "The new column applies cleanly to the dev Supabase project without disturbing existing RLS"
  artifacts:
    - path: "supabase/migrations/20240301000029_add_usage_to_messages.sql"
      provides: "Additive nullable usage column on messages (D-04)"
      contains: "ALTER TABLE messages ADD COLUMN"
  key_links:
    - from: "supabase/migrations/20240301000029_add_usage_to_messages.sql"
      to: "messages table (dev Supabase project ntkkmljbariflblldmha)"
      via: "supabase db push (dev only)"
      pattern: "ADD COLUMN IF NOT EXISTS usage"
---

<objective>
Add migration 029 — an additive nullable `usage` JSONB column on `messages` (D-04) —
and apply it LIVE to the DEV Supabase project so plan 11-04's usage-persistence write
targets a real column (without the push, build/type checks pass against a phantom column
and produce a false-positive verification state).

Purpose: Give plan 11-04 a durable place to persist the summed OpenRouter `usage` object
(incl. `cost`) from day one — Phase 14 only reads + renders, no re-plumbing. The column is
additive nullable so old rows keep `usage = NULL` and the existing messages RLS (migration 002)
is inherited unchanged.
Output: 1 migration file applied to dev. Prod apply is deferred to deploy (D-03 dual-env discipline).
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-CONTEXT.md
@.planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-PATTERNS.md

<interfaces>
<!-- Migration analogs the executor copies — confirmed via ls of supabase/migrations/ -->

Latest applied migration: 20240301000028_add_connected_at_to_user_api_keys.sql
→ 029 is the NEXT FREE NUMBER (confirmed by directory listing; RESEARCH + MEMORY agree).

Closest content analog: supabase/migrations/20240301000024_add_tools_used_to_messages.sql
  -- same table (messages), same shape (additive nullable JSONB), no backfill:
  ALTER TABLE messages ADD COLUMN tools_used JSONB DEFAULT NULL;

Idempotency analog: supabase/migrations/20240301000028_*.sql uses `IF NOT EXISTS` for
  safe replay (MEMORY: db push replays old migrations / "already exists"; use the guard).

Dual-env discipline (MEMORY [[project_dual_supabase_envs]] + STATE Phase 9/10 D-03):
  - dev project = ntkkmljbariflblldmha (.env)
  - apply to DEV ONLY this phase; prod (.env.prod) is deferred to deploy.
  - Non-TTY db push workaround: set SUPABASE_ACCESS_TOKEN env var.
  - MEMORY [[reference_supabase_migration_history_repair]]: if `db push` replays old
    migrations / errors "already exists", `supabase migration repair --status applied`
    the prior range first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 029 — additive nullable usage column on messages</name>
  <files>supabase/migrations/20240301000029_add_usage_to_messages.sql</files>
  <read_first>
    - supabase/migrations/20240301000024_add_tools_used_to_messages.sql (exact content analog — copy the additive-nullable-JSONB shape)
    - supabase/migrations/20240301000028_add_connected_at_to_user_api_keys.sql (IF NOT EXISTS idempotency guard + comment/rationale style)
  </read_first>
  <action>
    Create `supabase/migrations/20240301000029_add_usage_to_messages.sql` mirroring the
    `024_add_tools_used` shape: a single additive `ALTER TABLE messages ADD COLUMN IF NOT EXISTS
    usage JSONB DEFAULT NULL;` (use the `IF NOT EXISTS` guard like 028 for idempotent replay).
    Store the summed OpenRouter `usage` dict (prompt/completion/total tokens + `cost`); a single
    JSONB column is preferred over split columns (Claude's Discretion per CONTEXT, but JSONB matches
    the `tools_used` precedent and keeps Phase 14 read flexible). Add a leading comment explaining:
    additive nullable, no backfill (old rows keep NULL), inherits the existing messages RLS
    (migration 002 unchanged), persisted by Phase 11 / read by Phase 14 (D-04). Do NOT modify any
    RLS policy or other table.
  </action>
  <verify>
    <automated>test -f "supabase/migrations/20240301000029_add_usage_to_messages.sql" && grep -qE "ADD COLUMN IF NOT EXISTS usage" supabase/migrations/20240301000029_add_usage_to_messages.sql && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20240301000029_add_usage_to_messages.sql` exists
    - `grep -nE "ADD COLUMN IF NOT EXISTS usage" supabase/migrations/20240301000029_add_usage_to_messages.sql` matches
    - No `DROP`, no `POLICY`, no other table referenced (grep for `DROP|POLICY|user_api_keys` returns nothing)
    - 029 is the next free number (no 029 pre-existed before this task)
  </acceptance_criteria>
  <done>Migration 029 file exists, additive nullable usage JSONB on messages, idempotent, RLS untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 029 to DEV Supabase</name>
  <files>supabase/migrations/20240301000029_add_usage_to_messages.sql</files>
  <what-built>
    Migration 20240301000029 adds a nullable `usage` JSONB column to the `messages` table.
    It must be applied LIVE to the DEV Supabase project (ntkkmljbariflblldmha) so plan 11-04's
    usage-persistence write hits a real column — `git`/type checks pass without the live column
    and would otherwise create a false-positive verification state. A HALF-applied push (e.g. the
    025-028 repair was skipped and `db push` errored "already exists" before ever reaching 029)
    would leave `messages.usage` ABSENT while looking like it ran — which is exactly the
    false-positive state plan 11-04's mocked unit test cannot catch. The resume signal below
    therefore requires the literal probe output, not the word "applied".
  </what-built>
  <action>
    Attempt the push autonomously first: `supabase db push` against the DEV project, setting
    `SUPABASE_ACCESS_TOKEN` for the non-TTY path (MEMORY workaround). If `db push` replays old
    migrations or errors "already exists", run `supabase migration repair --status applied` for the
    prior applied range (025-028) first, then re-push (MEMORY [[reference_supabase_migration_history_repair]]).
    Apply to DEV ONLY — do NOT target prod (.env.prod); prod is deferred to deploy (D-03). If the
    CLI hits an interactive auth prompt or a credential it cannot satisfy, surface this checkpoint.
    Do NOT report success until the information_schema probe below returns the exact row — a bare
    "it ran" is insufficient because a partial/short-circuited push can look successful.
  </action>
  <how-to-verify>
    1. Confirm the column exists on dev: run a read-only probe against the dev project, e.g.
       `select column_name, data_type, is_nullable from information_schema.columns
        where table_name='messages' and column_name='usage';`
       Expect EXACTLY one row: `usage | jsonb | YES`. If zero rows → the push did NOT reach 029
       (likely the 025-028 repair was skipped); repair the prior range and re-push, then re-probe.
    2. Confirm the existing messages RLS is intact (a normal authenticated read of the user's own
       messages still works) — the additive column inherits migration 002 policies.
    3. Do NOT apply to prod this phase.
  </how-to-verify>
  <verify>
    <automated>MISSING — live DB apply is a human-gated action; verified via the information_schema probe in how-to-verify (no in-repo automated command for a live dev push). The probe output IS the resume condition.</automated>
  </verify>
  <resume-signal>Paste the actual `information_schema.columns` probe output for `messages.usage` showing the literal row `usage | jsonb | YES` (the word "applied" alone is NOT accepted — a half-applied push would pass it while the column is absent). If the probe returns zero rows or any other shape, describe the blocker (e.g. needed `migration repair --status applied` for 025-028) instead.</resume-signal>
  <done>messages.usage column proven present on the dev Supabase project — the pasted `information_schema` probe output literally reads `usage | jsonb | YES` (not a bare "applied"); messages RLS intact; prod NOT touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live dev Postgres | An over-broad migration could weaken RLS or expose the keys table. This migration is additive-nullable-column-only and touches no policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering / Information Disclosure | migration 029 scope | mitigate | Migration is a single additive nullable column on `messages`; it does NOT touch any RLS policy, the `user_api_keys` table, or the Phase 9 SEC-02 lockdown (REVOKE + FROM-allowlist). Acceptance criteria grep-asserts no `DROP`/`POLICY`/`user_api_keys`. |
| T-11-05 | Information Disclosure | persisted `usage` content | accept | Stored `usage` is token counts + `cost` (no key, no prompt body). The new column inherits the existing per-user messages RLS so a user only ever reads their own usage. No PII/secret in the column. |
| T-11-06 | Tampering (env discipline) | dev vs prod apply | mitigate | Apply to DEV only (D-03); prod deferred to deploy. Checkpoint instruction explicitly forbids prod targeting this phase, preventing an accidental prod schema drift. The resume signal requires the literal `information_schema` probe row (not a bare "applied") so a half-applied push that never reached 029 cannot pass the checkpoint and create a false-positive state for plan 11-04. |
</threat_model>

<verification>
- Migration file exists with the additive nullable column and idempotency guard
- `messages.usage` column present on dev — the pasted `information_schema` probe output literally reads `usage | jsonb | YES`
- Existing messages RLS intact; prod untouched
</verification>

<success_criteria>
- 20240301000029_add_usage_to_messages.sql created (additive nullable JSONB, idempotent)
- Column applied LIVE to dev Supabase and probe-confirmed via the literal `usage | jsonb | YES` row (not a bare "applied")
- No RLS policy or other table modified; prod deferred
</success_criteria>

<output>
After completion, create `.planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-02-SUMMARY.md`
</output>
</output>
