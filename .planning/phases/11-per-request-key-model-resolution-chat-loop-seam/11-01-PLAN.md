---
phase: 11-per-request-key-model-resolution-chat-loop-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/services/log_scrub.py
  - backend/tests/conftest.py
  - backend/tests/test_config.py
  - backend/tests/test_key_model_resolution.py
  - backend/tests/test_langsmith_gate.py
  - backend/tests/test_error_surfacing.py
  - backend/tests/test_usage_capture.py
autonomous: true
requirements: [SEC-01, DEMO-03]
must_haves:
  truths:
    - "A keyless user with the demo flag OFF gets a structured no_api_key refusal (fail-closed), never a silent owner-key completion (D-09 demo_fallback_enabled default OFF)"
    - "No sk-or-… value reaches any backend log line or SSE-error payload"
  artifacts:
    - path: "backend/config.py"
      provides: "demo_fallback_enabled (default False) + demo_fallback_model (free :free slug) settings fields (D-06 free-model pin config)"
      contains: "demo_fallback_enabled"
    - path: "backend/services/log_scrub.py"
      provides: "scrub_secrets() — sk-or- regex redaction reused by chat.py + aux services"
      contains: "sk-or-"
    - path: "backend/tests/test_key_model_resolution.py"
      provides: "Wave 0 test scaffold for DEMO-03 + SEC-04 fail-closed/no-bleed/model-fallthrough"
      contains: "def test_"
    - path: "backend/tests/test_error_surfacing.py"
      provides: "Wave 0 test scaffold for SEC-01 scrub + 402/429 distinct codes + exc_info logging.Filter"
      contains: "def test_"
    - path: "backend/tests/test_usage_capture.py"
      provides: "Wave 0 test scaffold for D-04 usage drain + sum + persist"
      contains: "def test_"
    - path: "backend/tests/test_langsmith_gate.py"
      provides: "Wave 0 test scaffold for SEC-01 wrap_openai gate"
      contains: "def test_"
  key_links:
    - from: "backend/config.py"
      to: "DEMO_FALLBACK_ENABLED / DEMO_FALLBACK_MODEL env vars"
      via: "pydantic-settings BaseSettings field"
      pattern: "demo_fallback_(enabled|model)"
    - from: "backend/services/log_scrub.py"
      to: "frontend/src/lib/sentry.ts regex"
      via: "mirrored sk-or- regex (broadened per D-11)"
      pattern: "sk-or-\\[A-Za-z0-9_-\\]\\+"
---

<objective>
Lay the Wave 0 + config foundation for the BYOK chat-loop seam: the two new
`config.py` fields (`demo_fallback_enabled` default OFF per D-09, `demo_fallback_model`
pinned to a free `:free` slug per D-06), the shared `scrub_secrets()` helper (D-11),
and the four NEW pytest stub files + conftest/test_config extensions that every
downstream plan's `<verify>` command targets.

Purpose: Establish the secret-scrub primitive and the config switches BEFORE the
seam wiring lands, and create the failing-first test scaffolds (Wave 0) so plans
03 and 04 turn them green. SEC-01 (backend scrub primitive) and DEMO-03 (config
flag that gates the fail-closed branch) originate here.
Output: 2 config fields, 1 new helper module, 4 new test files, 2 extended test files.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-CONTEXT.md
@.planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-RESEARCH.md
@.planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-PATTERNS.md

<interfaces>
<!-- Config analogs the executor mirrors — from backend/config.py (read directly) -->

backend/config.py existing fields to mirror:
- `rerank_enabled: bool = False`  (line 73 — bool-default style for demo_fallback_enabled)
- `llm_model: str = ""`           (line 46 — str-default style for demo_fallback_model)
- `key_encryption_secret: str = ""` (line 23 — env-doc comment style)
- `@property resolved_llm_api_key` (lines 134-136 — owner-only fallback the demo branch reads; do NOT add a resolution method here)
- `@lru_cache get_settings()`      (lines 147-149 — cached; tests MUST call get_settings.cache_clear() after monkeypatch.setenv on a cached field)

frontend/src/lib/sentry.ts:31 (source-of-truth regex to mirror, BROADENED on backend per D-11):
  const OR_KEY = /sk-or-v1-[A-Za-z0-9_-]+/g     // FE form (v1-only)
  // Backend uses the broader: r"sk-or-[A-Za-z0-9_-]+"  (catches sk-or-v1-… AND any future sk-or- prefix)

backend/tests/conftest.py:74-126 — `mock_stream_chat_completion` fixture:
  controller.set_events(events_per_call: list[list[dict]])  — drives per-call event lists
  Today emits only system_content / tool_call / text_delta. Extend to ALSO permit a
  trailing {"type":"usage","usage":{…}} event so D-04 usage-capture tests can drive it.

backend/tests/test_config.py:35-47 — default + env-override pair pattern to copy verbatim
  (uses bare `Settings()` for default test — avoids the lru_cache).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add demo-fallback config fields + scrub_secrets helper</name>
  <files>backend/config.py, backend/services/log_scrub.py, backend/tests/test_config.py</files>
  <read_first>
    - backend/config.py (the file being modified — fields at lines 23/46/73, property at 134-136, lru_cache at 147-149)
    - frontend/src/lib/sentry.ts (source-of-truth regex at line 31 — mirror BROADENED per D-11)
    - backend/tests/test_config.py (lines 35-47 — default + env-override pair pattern to copy)
  </read_first>
  <behavior>
    - test_demo_fallback_enabled_default: bare `Settings().demo_fallback_enabled is False`
    - test_demo_fallback_enabled_env_override: `DEMO_FALLBACK_ENABLED=true` → `Settings().demo_fallback_enabled is True`
    - test_demo_fallback_model_default: bare `Settings().demo_fallback_model` is a non-empty `:free` slug
    - test_demo_fallback_model_env_override: `DEMO_FALLBACK_MODEL=x/y:free` → overrides
    - test_scrub_secrets_redacts: `scrub_secrets("err sk-or-v1-ABC123def_-")` returns a string with no `sk-or-` substring, replaced by `[redacted-key]`
    - test_scrub_secrets_passthrough: `scrub_secrets(123)` returns `123` unchanged (non-str passthrough)
  </behavior>
  <action>
    In `backend/config.py`, add two `Settings` fields next to the BYOK/chat fields:
    `demo_fallback_enabled: bool = False` (env-driven, OFF in dev AND prod this phase — D-09;
    mirror the `rerank_enabled: bool = False` style at line 73) and
    `demo_fallback_model: str = "meta-llama/llama-3.3-70b-instruct:free"` (D-06; mirror the
    `llm_model: str` style at line 46). Add a comment noting the executor SHOULD re-confirm a
    currently-live `:free` slug at build time via openrouter.ai/models `:free` filter (Assumption A1) —
    `demo_fallback_enabled` defaulting OFF makes a stale slug harmless. Do NOT add any resolution
    method to `Settings` and do NOT add `@lru_cache` to a key-bearing function (Pitfall 8) — keep
    `resolved_llm_api_key` (lines 134-136) as the owner-only property the demo branch reads.
    Create NEW `backend/services/log_scrub.py` with `scrub_secrets(s: str) -> str` compiling
    `re.compile(r"sk-or-[A-Za-z0-9_-]+")` once at module scope and substituting `[redacted-key]`,
    passing non-str values through unchanged (mirror sentry.ts:31, broadened per D-11).
    Extend `backend/tests/test_config.py` with the four default/env-override config tests (copy the
    pair pattern at lines 35-47, using bare `Settings()` for default tests). Add the two scrub tests
    in the same file or a `test_log_scrub.py` (Claude's Discretion).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_config.py -x -q -k "demo_fallback or scrub"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "demo_fallback_enabled: bool = False" backend/config.py` matches
    - `grep -n "demo_fallback_model" backend/config.py` matches a `:free` slug default
    - `backend/services/log_scrub.py` exists and exports `scrub_secrets`
    - `grep -nE "sk-or-\[A-Za-z0-9_-\]\+" backend/services/log_scrub.py` matches (broadened regex, not the v1-only FE form)
    - The six config/scrub tests pass
    - No `@lru_cache` added to any new key-bearing function; no resolution method added to `Settings`
  </acceptance_criteria>
  <done>Both config fields present with correct defaults; scrub_secrets redacts sk-or- prefixes; 6 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — create failing test scaffolds + extend conftest usage event</name>
  <files>backend/tests/test_key_model_resolution.py, backend/tests/test_langsmith_gate.py, backend/tests/test_error_surfacing.py, backend/tests/test_usage_capture.py, backend/tests/conftest.py</files>
  <read_first>
    - backend/tests/conftest.py (lines 74-126 — mock_stream_chat_completion controller to extend)
    - backend/tests/test_crypto_service.py (monkeypatch + get_settings.cache_clear() pattern — MANDATORY for config-touching tests)
    - .planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-RESEARCH.md (§Validation Architecture → Phase Requirements → Test Map: the exact test function names per requirement; §Open Questions (RESOLVED) Q2 → the logging.Filter test)
    - .planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-PATTERNS.md (test analog section, lines 293-326)
  </read_first>
  <action>
    Create the four NEW test files as Wave 0 scaffolds. Each test function from the RESEARCH Test Map
    (PLUS the `test_logging_filter_scrubs_exc_info` added by revision per RESEARCH Open Q2) is created
    and marked `@pytest.mark.skip(reason="Wave 0 stub — turned green by plan 11-03/11-04")`
    (or `pytest.skip()` inside the body) so the suite stays green now and downstream plans un-skip +
    implement the assertion. Function names MUST match the RESEARCH Test Map verbatim so the
    `<verify>` commands in plans 03/04 resolve:
    `test_key_model_resolution.py`: test_no_key_flag_off_refuses, test_demo_fallback_uses_free_model,
    test_user_key_threaded_to_all_call_sites, test_no_cross_user_bleed, test_fail_closed_no_or_fallback,
    test_model_fallthrough_absent_p13_schema.
    `test_langsmith_gate.py`: test_user_key_client_not_wrapped.
    `test_error_surfacing.py`: test_sk_or_scrubbed_in_sse_error, test_429_402_distinct_codes,
    test_logging_filter_scrubs_exc_info (the exc_info/logging.Filter coverage — un-skipped + implemented
    by plan 11-04 Task 2; SEC-01 "never in logs" closure).
    `test_usage_capture.py`: test_usage_summed_across_tool_loop, test_usage_persisted_to_messages.
    Add a docstring per file naming the requirement(s) it covers. Where a config field is touched,
    each `monkeypatch.setenv(...)` is followed by `get_settings.cache_clear()` (mirror
    test_crypto_service.py:11-20). Extend `mock_stream_chat_completion` in conftest.py so the
    controller can emit a trailing `{"type":"usage","usage":{…}}` event after the text/tool events
    (additive to the existing `set_events` path — do not break current callers).
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_key_model_resolution.py tests/test_langsmith_gate.py tests/test_error_surfacing.py tests/test_usage_capture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist and are collected by pytest (no import/collection errors)
    - `pytest --collect-only -q tests/test_key_model_resolution.py` lists all 6 named functions
    - `pytest --collect-only -q tests/test_error_surfacing.py` lists test_sk_or_scrubbed_in_sse_error, test_429_402_distinct_codes, and test_logging_filter_scrubs_exc_info
    - `pytest --collect-only -q tests/test_usage_capture.py` lists both usage functions
    - `pytest --collect-only -q tests/test_langsmith_gate.py` lists test_user_key_client_not_wrapped
    - Full suite `cd backend && venv/Scripts/python -m pytest tests/ -q` stays GREEN (stubs skipped, nothing red)
    - `mock_stream_chat_completion` accepts a usage-event configuration without breaking existing callers
  </acceptance_criteria>
  <done>12 named test stubs collected across 4 files; conftest emits optional trailing usage event; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend error/log path → observability (Sentry/LangSmith/logs/SSE) | A decrypted `sk-or-…` key could be stringified into a log/SSE payload and cross out of the trust boundary. `scrub_secrets()` is the chokepoint primitive built here. |
| env vars → `Settings` | `DEMO_FALLBACK_ENABLED` gates the owner-key spend branch; default OFF is the structural cost bound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `scrub_secrets()` (log_scrub.py) | mitigate | Regex `sk-or-[A-Za-z0-9_-]+` → `[redacted-key]`; broader than the FE v1-only form so any future `sk-or-` prefix is caught (D-11). Unit-tested (test_scrub_secrets_redacts). Consumed by plan 11-04's SSE/log paths AND the `_ScrubFilter` logging.Filter (the exc_info-traceback closure). |
| T-11-02 | Denial of Service (cost) / Elevation | `demo_fallback_enabled` default | mitigate | Field defaults `False` in dev AND prod this phase (D-09); enabling in prod is Phase 15, hard-gated on SEC-03. The fail-closed branch that reads it is built in plan 11-04 and tested (test_no_key_flag_off_refuses). |
| T-11-03 | Information Disclosure / Tampering | new config fields on `Settings` | accept | Adding plain bool/str fields to the already-`@lru_cache`'d owner/global `get_settings()` is safe — these are NOT key-bearing per-request values. The per-request key+model is resolved in plan 11-04 and is explicitly NOT cached (Pitfall 8). |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_config.py -q` — config + scrub tests green
- `cd backend && venv/Scripts/python -m pytest tests/ -q` — full suite green with Wave 0 stubs collected + skipped
- `grep -rn "sk-or-\[A-Za-z0-9_-\]\+" backend/services/log_scrub.py` — broadened backend regex present
</verification>

<success_criteria>
- `demo_fallback_enabled: bool = False` and `demo_fallback_model` (`:free` slug) exist in config.py
- `scrub_secrets()` redacts `sk-or-` prefixes and passes non-strings through
- 12 named Wave 0 test functions collected across 4 new files (incl. test_logging_filter_scrubs_exc_info)
- conftest `mock_stream_chat_completion` can emit a trailing usage event
- Full backend suite green
</success_criteria>

<output>
After completion, create `.planning/phases/11-per-request-key-model-resolution-chat-loop-seam/11-01-SUMMARY.md`
</output>
</output>
