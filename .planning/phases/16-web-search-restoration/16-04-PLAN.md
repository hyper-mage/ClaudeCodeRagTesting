---
phase: 16-web-search-restoration
plan: 04
type: execute
wave: 4
depends_on: [16-02, 16-03]
files_modified: []
autonomous: false
requirements: [WSRCH-01, WSRCH-02, WSRCH-03, WSRCH-04]
must_haves:
  truths:
    - "The phase-16 backend (Bearer-auth transport + is_error SSE flag) and frontend (red failed-state card) are DEPLOYED to live prod"
    - "The WEB_SEARCH_API_KEY Fly secret is set on the prod app and the machine restarted, so @lru_cache get_settings() re-reads it and web_search_enabled is true in the running process (SC-5)"
    - "On live prod, a current-info board-game query invokes web_search and returns a web-grounded answer with inline markdown-link citations + a trailing 'Sources:' list, with the Web Search tool card visible (SC-1 / SC-3 / SC-5)"
    - "On live prod (or dev) with an invalid key, the tool card shows the red failed state and the agent briefly notes it couldn't reach the web, then answers best-effort — the turn does not crash (SC-4)"
    - "Fail-closed is confirmed: with no key configured the web_search tool is cleanly absent and chat turns still complete (SC-2 — unit-verified in 16-01, spot-checked in an unkeyed environment)"
  artifacts:
    - path: ".planning/phases/16-web-search-restoration/16-04-SUMMARY.md"
      provides: "Recorded evidence of the SC-1..SC-5 live verification (query used, answer excerpt with citations, tool-card notes, failure-path result)"
      contains: "SC-5"
  key_links:
    - from: "fly secrets set WEB_SEARCH_API_KEY"
      to: "backend config.get_settings() @lru_cache"
      via: "machine restart re-reads settings → web_search_enabled true → WEB_SEARCH_TOOL appended live"
      pattern: "web_search_enabled"
    - from: "live prod chat turn"
      to: "api.tavily.com Bearer auth"
      via: "restored _search_tavily header transport (plan 16-02)"
      pattern: "Bearer"
---

<objective>
Prod-verify the restored web_search tool end-to-end against the real Tavily key on the live deployment — the phase's SC-5 gate, which no unit test can cover. Deploy the phase-16 backend + frontend to prod, set the owner's `WEB_SEARCH_API_KEY` Fly secret, confirm the machine restarted (so `@lru_cache` settings re-read and `web_search_enabled` flips true), then run the success smoke (a current-info board-game query returns a web-grounded, cited answer with a visible tool card) and the failure smoke (an invalid key → red failed-state card + the agent acknowledges + best-effort answer).

Purpose: SC-5 requires live prod verification with the production search-provider key — this is the only way to prove WSRCH-01/03 work against real Tavily auth and that the D-03 failed-state renders in prod. This plan is autonomous:false because it depends on the owner-held Tavily prod key and owner-approved prod deploy, plus human visual confirmation of the tool card + citations. Everything the executor CAN automate (discover the app, draft exact commands, confirm state) it does; the human gate covers the secret and the visual smoke.

Output: SC-1..SC-5 confirmed on prod; 16-04-SUMMARY.md records the evidence and closes the phase.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-web-search-restoration/16-RESEARCH.md
@.planning/phases/16-web-search-restoration/16-VALIDATION.md

<interfaces>
<!-- Live-verification context (deploy + secret mechanics). -->

Prod deploy mechanics (from project memory — confirm before acting, do not assume):
  - The prod Supabase/Fly environment is defined by `.env.prod` (prod project); `.env` is dev.
  - Prod app builds from `main`; the working branch is `master` (push master→main triggers the Cloudflare build; Fly deploys via the repo-root Dockerfile). Confirm the exact app name via `fly apps list` / `fly status` — do NOT hardcode.
  - Prod Fly already holds KEY_ENCRYPTION_SECRET (BYOK); WEB_SEARCH_API_KEY is the NEW secret for this phase.

Settings caching (RESEARCH Pitfall 4, VERIFIED config.py:191/@lru_cache):
  - get_settings() is @lru_cache'd and read once per process; setting the Fly secret does NOT enable the tool until the machine restarts. Fly restarts on `secrets set` by default — confirm the restart before smoking.

Live-verify targets:
  - SC-1/SC-3/SC-5: a current-info board-game query the KB can't answer (e.g. "What's the current price / latest expansion of Catan?" or "What's Wingspan's current BGG ranking?") → the agent fires web_search (tool card visible), and the answer carries inline [text](url) links + a "Sources:" list (D-02).
  - SC-4: an invalid tvly- key (or blocked network) → the Web Search tool card shows the red failed state (plan 16-03) and the agent briefly notes it couldn't reach the web, then answers best-effort — no turn crash.
  - SC-2: with no key configured, web_search is absent and chat still completes (unit-verified by test_gating_fail_closed in 16-01; spot-check in an unkeyed env — do NOT unset the prod key mid-verification if it disrupts other users; the dev env with an empty key is an acceptable place to confirm).

Test credentials for the prod smoke (CLAUDE.md): ragtest1@gmail.com / testpass123.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy phase-16 to prod + set the WEB_SEARCH_API_KEY Fly secret + confirm web_search_enabled live</name>
  <files>none</files>
  <read_first>
    - .planning/phases/16-web-search-restoration/16-RESEARCH.md (§Pitfall 4 @lru_cache restart; §Environment Availability — prod WEB_SEARCH_API_KEY required, no fallback; §Runtime State Inventory — new WEB_SEARCH_DEPTH read is optional/defaults basic)
    - .planning/phases/16-web-search-restoration/16-02-PLAN.md + 16-03-PLAN.md (what shipped: Bearer transport, is_error SSE flag, red failed-state card — must be on prod for the smoke)
    - project memory: prod deploy mechanics (`.env.prod`, push master→main, Fly repo-root Dockerfile, prod Fly secrets)
  </read_first>
  <action>
    Executor (automatable prep before the human gate): confirm plans 16-02 + 16-03 are merged on master and the full backend suite + frontend build are green; confirm the exact prod Fly app name via `fly apps list` / `fly status` (do NOT hardcode); then DRAFT the exact commands for the owner to run — the prod deploy (push master→main so Cloudflare/Fly rebuild from main) and `fly secrets set WEB_SEARCH_API_KEY=tvly-<owner-key> --app <prod-app>` (optionally `WEB_SEARCH_DEPTH=advanced` if the owner wants it). After the owner runs them and returns, confirm via `fly status` that the machine restarted post-secret (required because get_settings() is @lru_cache'd). Do NOT push to prod or set the secret without the owner's explicit go-ahead, and never echo the raw key.
  </action>
  <what-built>
    Plans 16-02 (backend: header-only Bearer Tavily auth, settings-driven search_depth, D-01/D-02 prompt edits, is_error SSE flag, tvly- scrub) and 16-03 (frontend: red failed-state tool card) are merged on master and green. The executor discovers the prod app name, drafts the deploy + secret commands, and after the owner runs them confirms the restart.
  </what-built>
  <how-to-verify>
    Owner action (Claude cannot supply the owner-held Tavily prod key or approve a prod deploy):
    1. Approve/perform the prod deploy of the phase-16 changes (push master→main per the established mechanics; confirm Cloudflare build + Fly deploy succeed).
    2. Run the drafted `fly secrets set WEB_SEARCH_API_KEY=tvly-<your-key> --app <prod-app>` (paste your real Tavily prod key). Confirm with `fly secrets list` that WEB_SEARCH_API_KEY is listed (value hidden).
    3. Confirm the machine restarted (`fly status` shows a fresh start after the secret set) — required because get_settings() is @lru_cache'd (RESEARCH Pitfall 4).
    Do NOT paste the raw key into chat or any logged command output beyond the `fly secrets set` invocation.
  </how-to-verify>
  <verify>
    <automated>fly secrets list --app "$PROD_APP" 2>/dev/null | grep -q WEB_SEARCH_API_KEY && echo SECRET-PRESENT</automated>
  </verify>
  <done>The phase-16 changes are live on prod, WEB_SEARCH_API_KEY is set as a Fly secret, and the machine has restarted so web_search_enabled reads true in the running process.</done>
  <resume-signal>Type "deployed" once the prod deploy is live, the WEB_SEARCH_API_KEY secret is set, and the machine has restarted — or describe any deploy/secret error.</resume-signal>
  <acceptance_criteria>
    - The prod app name is identified (not hardcoded) and the phase-16 backend+frontend are confirmed deployed to prod
    - `fly secrets list --app <prod-app>` shows WEB_SEARCH_API_KEY present (value not printed)
    - `fly status` confirms a machine restart AFTER the secret was set (so @lru_cache settings re-read)
    - The raw tvly- key was never echoed into chat, a logged command, or the SUMMARY
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live prod smoke — success (cited web-grounded answer + tool card) and failure (red failed-state + agent ack)</name>
  <files>none</files>
  <read_first>
    - .planning/phases/16-web-search-restoration/16-VALIDATION.md (§Manual-Only Verifications — the exact live smokes for SC-5 + fail-closed)
    - .planning/phases/16-web-search-restoration/16-RESEARCH.md (§Validation Architecture Test Map — the two manual prod smokes; §Pitfall 3 vague 401/429 error text is acceptable)
    - CLAUDE.md (Development Flow §Validate — browser testing via an appropriate MCP; test credentials)
  </read_first>
  <action>
    Executor: drive the live prod smoke via a browser MCP (or guide the owner step-by-step) using the ragtest1@gmail.com credentials. Run the success smoke (current-info board-game query → confirm Web Search tool card + web-grounded answer with inline links + a "Sources:" list) and the failure smoke (invalid key → confirm the red failed-state card + agent acknowledgement + best-effort answer, no crash), then confirm the valid key is restored. Record the query, an answer excerpt showing the citations, and the failure-path outcome in 16-04-SUMMARY.md with no raw key.
  </action>
  <what-built>
    With the prod key live (Task 1), the restored web_search tool should now answer current-info board-game queries end-to-end with cited sources, and fail gracefully on an invalid key. This checkpoint is the human/browser confirmation of SC-1, SC-3, SC-4, SC-5 (and a spot-check of SC-2) that no automated test can cover.
  </what-built>
  <how-to-verify>
    Success smoke (SC-1 / SC-3 / SC-5) — in the live prod chat, signed in as ragtest1@gmail.com:
    1. Ask a current/external board-game question the KB can't answer (e.g. "What is the current price of Catan, and what's its latest expansion?" or "What's Wingspan's current BGG ranking?").
    2. Confirm a "Web Search" tool card appears (the agent invoked web_search) and completes (gray check, not red).
    3. Confirm the answer is grounded in live web results and cites sources as INLINE markdown links at the point facts are used AND ends with a short "Sources:" list (D-02).
    Failure smoke (SC-4) — on prod (or dev to avoid disrupting prod users): temporarily set an INVALID WEB_SEARCH_API_KEY (or block egress), restart, ask a web-needing question, and confirm:
    4. The Web Search tool card shows the RED failed state (plan 16-03), and
    5. The agent briefly notes it couldn't reach the web, then answers best-effort from the KB / its own knowledge — the turn does NOT crash. Restore the valid key + restart afterward.
    Fail-closed spot-check (SC-2): in an environment with NO key configured (e.g. dev with empty WEB_SEARCH_API_KEY), confirm the web_search tool is absent and a chat turn still completes normally (already unit-verified by test_gating_fail_closed).
    Record the query used, an answer excerpt showing the inline links + Sources list, and the failure-path outcome in 16-04-SUMMARY.md (no raw key in the notes).
  </how-to-verify>
  <verify>
    <automated>MANUAL — live prod browser smoke; no automated command (frontend has no test runner). Verified via the how-to-verify steps + browser MCP; evidence recorded in 16-04-SUMMARY.md.</automated>
  </verify>
  <done>The live success smoke shows a cited, web-grounded answer with a visible Web Search tool card; the failure smoke shows the red failed-state card + agent acknowledgement + best-effort answer with no crash; the valid key is restored; evidence recorded in the SUMMARY.</done>
  <resume-signal>Type "verified" if the success smoke shows a cited web-grounded answer + tool card AND the failure smoke shows the red card + agent acknowledgement; otherwise describe what failed (no tool card / no citations / crash / card not red).</resume-signal>
  <acceptance_criteria>
    - SC-1/SC-5: a current-info prod query produced a visible completed Web Search tool card and a web-grounded answer
    - SC-3: the answer used inline markdown-link citations AND a trailing "Sources:" list
    - SC-4: an invalid-key run showed the RED failed-state tool card and an agent acknowledgement + best-effort answer, with no turn crash
    - SC-2: an unkeyed environment showed web_search absent and chat still completing (spot-check; unit-backed)
    - The valid prod key was restored after the failure smoke; 16-04-SUMMARY.md records the evidence with no raw key
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner Tavily prod key → Fly secret store | The real `tvly-` key is set as a Fly secret; it must not be echoed into chat, command transcripts, logs, or the SUMMARY. |
| prod deploy → live users | Deploying + toggling the key affects the live app; the failure smoke intentionally sets an invalid key and must be reverted. |
| live tool result → browser | The verified answer + tool card render server-mapped results only; the key never reaches the client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Information Disclosure | raw tvly- key leaked during secret-set / verification | mitigate | Key is set only via `fly secrets set` (value hidden in `fly secrets list`); the acceptance criteria forbid echoing it into chat/logs/SUMMARY; scrub_secrets (plan 16-02) also redacts tvly- from any log line as a backstop. |
| T-16-13 | Denial of Service | failure smoke leaves prod with an invalid key | mitigate | The failure smoke is preferably run in dev; if run on prod, the acceptance criteria require restoring the valid key + restart afterward and confirming the success smoke again. |
| T-16-14 | Tampering | wrong app / wrong environment targeted | mitigate | App name is discovered via `fly apps list`/`fly status` (not hardcoded); `.env.prod` distinguishes prod from dev; owner approves the exact command before running. |
</threat_model>

<verification>
- Task 1: `fly secrets list --app <prod-app>` shows WEB_SEARCH_API_KEY; `fly status` shows a post-secret restart (so web_search_enabled re-reads true)
- Task 2: human/browser confirmation of the success smoke (tool card + inline-link + Sources citations) and the failure smoke (red card + agent ack, no crash)
- Full backend suite green (from 16-02) and frontend build green (from 16-03) BEFORE deploy — no live verification against unbuilt code
- 16-04-SUMMARY.md records the SC-1..SC-5 evidence with no raw key
</verification>

<success_criteria>
- Phase-16 backend + frontend deployed to prod; WEB_SEARCH_API_KEY set + machine restarted (SC-5 precondition)
- Live prod success smoke: cited, web-grounded answer with a visible Web Search tool card (SC-1, SC-3, SC-5)
- Live failure smoke: red failed-state card + agent acknowledgement + best-effort answer, no crash (SC-4)
- Fail-closed spot-check with no key: tool absent, chat completes (SC-2)
- Evidence recorded in 16-04-SUMMARY.md; no secret leaked
</success_criteria>

<output>
After completion, create `.planning/phases/16-web-search-restoration/16-04-SUMMARY.md`
</output>
