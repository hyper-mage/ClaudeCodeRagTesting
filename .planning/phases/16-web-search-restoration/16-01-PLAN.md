---
phase: 16-web-search-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_web_search.py
  - backend/tests/test_config.py
autonomous: true
requirements: [WSRCH-01, WSRCH-02, WSRCH-03, WSRCH-04]
must_haves:
  truths:
    - "backend/tests/test_web_search.py exists and encodes the WSRCH-01..04 + D-04 contract; its assertions FAIL or ERROR before the Wave 2 backend fix lands (RED baseline)"
    - "test_config.py asserts web_search_depth defaults to 'basic' and is env-overridable via WEB_SEARCH_DEPTH, and that scrub_secrets redacts a tvly- prefixed key to [redacted-key]"
    - "Tests mock httpx.post (no respx / pytest-httpx) via monkeypatch + MagicMock, matching the project's existing test convention"
  artifacts:
    - path: "backend/tests/test_web_search.py"
      provides: "RED unit tests for Tavily Bearer auth, result mapping, fail-closed gating, citation-prompt string, graceful failure + logging, and configurable search_depth"
      contains: "def test_tavily_bearer_auth"
      min_lines: 60
    - path: "backend/tests/test_config.py"
      provides: "web_search_depth default + env-override tests and a tvly- scrub-redaction test appended to the existing file"
      contains: "test_web_search_depth_default"
  key_links:
    - from: "backend/tests/test_web_search.py"
      to: "services.web_search_service._search_tavily + search_web"
      via: "monkeypatch services.web_search_service.httpx.post + services.web_search_service.get_settings"
      pattern: "web_search_service"
    - from: "backend/tests/test_config.py"
      to: "config.Settings.web_search_depth + services.log_scrub.scrub_secrets"
      via: "Settings() construction + scrub_secrets() call"
      pattern: "web_search_depth"
---

<objective>
Create the Wave 0 test scaffold for Phase 16 (`backend/tests/test_web_search.py` is MISSING today — Validation Strategy §Wave 0 Gaps) and extend `backend/tests/test_config.py`. These tests encode the full phase contract (WSRCH-01..04 + D-04 + the Tavily-key scrub) and FAIL/ERROR now — they are the RED baseline the Wave 2 backend fix (plan 16-02) turns GREEN.

Purpose: Establish executable, sub-30s feedback for the restoration before touching production code. The one genuine bug (Tavily now requires header-only `Authorization: Bearer`, not a body `api_key`) is pinned by a test that asserts the request transport, so the fix cannot regress. `web_search_depth`, the citation-prompt guidance, the graceful-failure log, and the `tvly-` scrub are each locked by an assertion.

Output: two test files; running the backend suite shows the new web-search tests RED (impl absent) while the rest of the suite stays green.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-web-search-restoration/16-CONTEXT.md
@.planning/phases/16-web-search-restoration/16-RESEARCH.md
@.planning/phases/16-web-search-restoration/16-VALIDATION.md

<interfaces>
<!-- Current source-of-truth seams the tests target (read directly, line numbers verified). -->

backend/services/web_search_service.py:
  - search_web(query: str) -> dict  — calls get_settings(); guards `if not settings.web_search_enabled: return {"error":"Web search not configured","results":[]}`; dispatches to _search_tavily when provider=="tavily".
  - _search_tavily(query, settings) -> dict  — TODAY posts `json={"api_key": ..., "query", "max_results", "include_answer": True, "search_depth": "basic"}` with NO headers (the bug). Returns {answer, results:[{title,url,snippet}]} on 200; on Exception returns {"error": str(e), "results": []} and logs `logger.error(..., exc_info=True)`.
  - Module symbols to monkeypatch: `services.web_search_service.httpx` (patch `.post`), `services.web_search_service.get_settings`, `services.web_search_service.logger`.

backend/config.py:
  - Settings.web_search_provider="tavily", web_search_api_key="", web_search_max_results=5 (lines 129-131). web_search_depth does NOT exist yet (added by 16-02 Task 1 — a default test asserting "basic" ERRORs until then = RED).
  - Settings.system_prompt (lines 95-102) — today only says "always cite your sources with URLs"; the D-02 inline-links + "Sources:" guidance is NOT present yet (RED).
  - Settings.web_search_enabled property (line 191) = bool(web_search_api_key).

backend/services/log_scrub.py:
  - scrub_secrets(s) redacts only `sk-or-[A-Za-z0-9_-]+` today; a `tvly-` key passes through UNredacted (RED until 16-02 Task 3).

Test conventions (VERIFIED in-repo):
  - Mocking: monkeypatch.setattr on the module symbol + unittest.mock.MagicMock (NO respx/pytest-httpx — not installed). Mirror tests/test_error_surfacing.py (builds MagicMock responses) and tests/test_config.py (constructs Settings() directly).
  - A fake response: MagicMock with `.raise_for_status` a no-op (or set to raise httpx.HTTPStatusError for the failure case) and `.json()` returning a canned Tavily body {"answer": "...", "results": [{"title","url","content"}...]}.
  - Fake settings for _search_tavily: construct `Settings(web_search_api_key="tvly-test-key", web_search_depth="advanced")` (once the field exists) OR a types.SimpleNamespace exposing web_search_api_key / web_search_max_results / web_search_depth — the depth-default test in test_config.py is what fails on the missing field, so test_web_search.py may pass depth explicitly via SimpleNamespace to isolate the transport assertion.
  - pytest.ini: asyncio_mode=auto, testpaths=tests. Run command uses the backend venv.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend/tests/test_web_search.py (RED contract for WSRCH-01..04 + D-04)</name>
  <files>backend/tests/test_web_search.py</files>
  <read_first>
    - backend/services/web_search_service.py (the module under test — search_web + _search_tavily, the get_settings + httpx.post + logger symbols to monkeypatch)
    - backend/tests/test_error_surfacing.py (lines 1-60 — MagicMock response + monkeypatch convention to mirror; NO respx)
    - backend/tests/conftest.py (fixture + sys.path convention; MagicMock import)
    - .planning/phases/16-web-search-restoration/16-RESEARCH.md (§Validation Architecture "Phase Requirements → Test Map" for the exact test names; §Code Examples "The WSRCH-01 fix" for the target request shape; §Tavily API contract for the 200 response shape)
  </read_first>
  <behavior>
    - test_tavily_bearer_auth (WSRCH-01): _search_tavily(query, settings) issues httpx.post with headers Authorization == "Bearer <settings.web_search_api_key>"; the posted JSON body contains NO "api_key" key; body["search_depth"] equals settings.web_search_depth; body["query"] equals the passed query; body["include_answer"] is True; body["max_results"] equals settings.web_search_max_results.
    - test_tavily_maps_results (WSRCH-01): a 200 body {"answer":"A","results":[{"title":"T","url":"U","content":"C"}]} maps to {"answer":"A","results":[{"title":"T","url":"U","snippet":"C"}]} (content→snippet rename preserved).
    - test_gating_fail_closed (WSRCH-02): with get_settings() returning a settings whose web_search_api_key is "" (web_search_enabled False), search_web("x") returns {"error":"Web search not configured","results":[]} and httpx.post is NEVER called; with a key set, web_search_enabled is True and the tavily path is taken.
    - test_system_prompt_citation_guidance (WSRCH-03 / D-02): Settings().system_prompt contains a "Sources:" marker AND references inline markdown-link citation (assert on the literal substrings the 16-02 edit will introduce, e.g. "Sources:" and "inline" / markdown-link phrasing) — RED until 16-02 Task 2.
    - test_graceful_failure_logs (WSRCH-04): when httpx.post raises (generic Exception or httpx.HTTPStatusError built from a real httpx.Request/Response), _search_tavily returns a dict containing an "error" key and "results": [] and does NOT propagate the exception; the module logger.error is invoked with exc_info=True (assert via a MagicMock swapped onto services.web_search_service.logger.error, or caplog with record.exc_info set).
    - test_search_depth_passed (D-04): with settings.web_search_depth == "advanced", the posted body["search_depth"] == "advanced" (proves depth is read from settings, not hardcoded "basic").
  </behavior>
  <action>
    Create backend/tests/test_web_search.py with a module docstring noting it is the Phase 16 Wave 0 scaffold (RED until plan 16-02). Import unittest.mock.MagicMock, httpx, logging, and the module-under-test symbols. Implement the six test functions named EXACTLY as in the RESEARCH Test Map (listed under <behavior>). Use a shared helper that monkeypatches `services.web_search_service.httpx.post` with a capturing fake that records the `headers`/`json`/`timeout` kwargs and returns a MagicMock whose `.raise_for_status()` is a no-op and whose `.json()` returns a canned Tavily 200 body. For the failure test, set the fake `.post` (or `.raise_for_status`) to raise. For _search_tavily tests, pass a fake settings object (types.SimpleNamespace with web_search_api_key="tvly-test-key", web_search_max_results=5, web_search_depth="advanced") so the transport assertions are isolated from the not-yet-added Settings field. For search_web gating tests, monkeypatch `services.web_search_service.get_settings` to return a settings with web_search_enabled derived from an empty vs set api_key (SimpleNamespace exposing web_search_enabled + web_search_provider="tavily"). For the citation-prompt test, construct `Settings()` and assert on the D-02 substrings. Do NOT weaken assertions to make them pass now — they MUST be RED against current code (missing Bearer header, missing depth field/behavior, missing "Sources:" guidance). Do NOT add respx or any new dependency.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_web_search.py -q 2>&1 | grep -Eq "failed|error" && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/tests/test_web_search.py` is true
    - `grep -c "def test_" backend/tests/test_web_search.py` returns 6 (the six named functions)
    - `grep -Eq "def (test_tavily_bearer_auth|test_gating_fail_closed|test_graceful_failure_logs|test_search_depth_passed|test_system_prompt_citation_guidance|test_tavily_maps_results)" backend/tests/test_web_search.py` — all six names present
    - `grep -q "respx\|pytest_httpx\|pytest-httpx" backend/tests/test_web_search.py` returns NOTHING (no new mocking dependency)
    - `grep -q "Authorization" backend/tests/test_web_search.py` and `grep -q "api_key" backend/tests/test_web_search.py` (asserts Bearer header present + no-api_key-in-body check)
    - Running the file exits NON-zero now (RED) — at least test_tavily_bearer_auth, test_search_depth_passed, and test_system_prompt_citation_guidance fail/error against current code
  </acceptance_criteria>
  <done>test_web_search.py exists with the six named RED tests; it fails against current code and pins the WSRCH-01..04 + D-04 contract; no new dependency added.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test_config.py with web_search_depth + tvly- scrub tests (RED)</name>
  <files>backend/tests/test_config.py</files>
  <read_first>
    - backend/tests/test_config.py (the file being modified — mirror test_chat_max_iterations_default/_env_override at lines 5-17 and test_scrub_secrets_redacts at lines 87-92)
    - backend/config.py (lines 128-131 web_search_* fields; the pattern web_search_depth will follow)
    - backend/services/log_scrub.py (the scrub_secrets regex the tvly- test targets)
    - .planning/phases/16-web-search-restoration/16-RESEARCH.md (§Pattern 2 settings mirroring; §Security Domain "API key leakage" — the tvly- scrub gap)
  </read_first>
  <behavior>
    - test_web_search_depth_default: Settings().web_search_depth == "basic" (RED — attribute does not exist yet → AttributeError until 16-02 Task 1).
    - test_web_search_depth_env_override: with monkeypatch.setenv("WEB_SEARCH_DEPTH","advanced"), Settings().web_search_depth == "advanced".
    - test_scrub_secrets_redacts_tavily: scrub_secrets("err tvly-ABC123def_-") contains "[redacted-key]" and does NOT contain "tvly-" (RED — log_scrub only matches sk-or- today).
  </behavior>
  <action>
    Append three test functions to backend/tests/test_config.py, mirroring the existing default/env-override pair pattern (test_chat_max_iterations_*) and the existing test_scrub_secrets_redacts style. test_web_search_depth_default and test_web_search_depth_env_override construct `Settings()` (and use monkeypatch.setenv for the override, matching the existing convention). test_scrub_secrets_redacts_tavily imports scrub_secrets from services.log_scrub and asserts a tvly- token is redacted. Keep the existing sk-or- tests untouched. Do not modify config.py or log_scrub.py in this plan — these are the RED assertions 16-02 satisfies.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest tests/test_config.py -q 2>&1 | grep -Eq "failed|error" && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq "def (test_web_search_depth_default|test_web_search_depth_env_override|test_scrub_secrets_redacts_tavily)" backend/tests/test_config.py` — all three present
    - `grep -q "WEB_SEARCH_DEPTH" backend/tests/test_config.py` (env-override test uses the env var name)
    - `grep -q "tvly-" backend/tests/test_config.py` (scrub test uses a tvly- token)
    - The existing sk-or- tests (test_scrub_secrets_redacts / _passthrough) remain in the file unchanged
    - Running test_config.py exits NON-zero now (RED) — the three new tests fail/error against current code
  </acceptance_criteria>
  <done>test_config.py carries the depth default/override + tvly- scrub RED tests, mirroring existing patterns; existing tests untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture value → source repo | A test may embed a fake API key literal (`tvly-test-key`, `sk-or-...`); if a REAL key were pasted it would enter version control. |
| scrub assertion → security regression detector | The tvly- scrub test is itself the tripwire proving the Wave 2 mitigation lands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | real key committed in a test fixture | mitigate | Tests use only synthetic non-secret tokens (`tvly-test-key`, `tvly-ABC123def_-`); no real `tvly-`/`sk-or-` value is written. Acceptance greps assert the literal test tokens, not env-sourced secrets. |
| T-16-02 | Tampering | scrub coverage silently regresses | mitigate | test_scrub_secrets_redacts_tavily is the RED tripwire; it stays in the suite as a permanent regression guard for the `tvly-` redaction added in 16-02. |
</threat_model>

<verification>
- `cd backend && venv/Scripts/python -m pytest tests/test_web_search.py tests/test_config.py -q` — the NEW web-search/depth/scrub tests are RED (impl absent), proving they exercise real behavior
- `cd backend && venv/Scripts/python -m pytest -q` — the REST of the suite stays green (the RED tests are the only new failures; no collateral breakage from adding the files)
- No new dependency introduced: `grep -rq "respx\|pytest-httpx" backend/tests/` returns nothing
</verification>

<success_criteria>
- backend/tests/test_web_search.py exists with the six RESEARCH-named tests, RED against current code
- backend/tests/test_config.py gains the two depth tests + the tvly- scrub test, RED against current code
- All mocking via monkeypatch + MagicMock; no respx / pytest-httpx / other new dep
- The rest of the backend suite is unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/16-web-search-restoration/16-01-SUMMARY.md`
</output>
