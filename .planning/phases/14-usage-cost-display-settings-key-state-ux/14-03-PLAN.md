---
phase: 14-usage-cost-display-settings-key-state-ux
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - frontend/src/components/MessageBubble.tsx
  - frontend/src/components/ErrorMessageBubble.tsx
  - frontend/src/components/ChatContainer.tsx
autonomous: true
requirements: [COST-01, COST-04, PREF-01]

must_haves:
  truths:
    - "Each assistant message shows an always-visible muted cost line ($0.0021 · 1.2k tok), omitting the cost segment for free/null-cost turns"
    - "The active-thread header shows a running total Σ $0.0142 summed from persisted usage.cost, rendered only when > 0, surviving reload"
    - "A mid-chat 401/402/403 renders a typed in-thread recovery bubble with the locked sentence + mapped action buttons (no toast)"
    - "Implements decisions D-01 (per-message muted cost line), D-02 (per-thread Σ total in header), and D-09 frontend half (typed recovery bubble keyed on errorType)"
  artifacts:
    - path: "frontend/src/components/MessageBubble.tsx"
      provides: "per-message cost caption (assistant only)"
      contains: "tok"
    - path: "frontend/src/components/ErrorMessageBubble.tsx"
      provides: "typed recovery variant (no_api_key/payment_required/forbidden)"
      contains: "Reconnect"
    - path: "frontend/src/components/ChatContainer.tsx"
      provides: "Σ thread total in header + usage passthrough + typed error bubble wiring"
      contains: "reduce"
  key_links:
    - from: "ChatContainer message map"
      to: "MessageBubble usage prop + ErrorMessageBubble type prop"
      via: "usage={msg.usage} and type={msg.errorType}"
      pattern: "msg.usage|msg.errorType"
    - from: "ErrorMessageBubble [Reconnect]"
      to: "startOpenRouterConnect()"
      via: "lib/pkce import"
      pattern: "startOpenRouterConnect"
---

**Complexity:** ⚠️ Medium — three coordinated frontend render surfaces (cost caption, Σ header total, typed recovery bubble) plus ChatContainer interface widening that must satisfy strict tsc against the Plan-02 hook contract. Pure render/wiring, no new fetches.

<objective>
Render the cost surfaces and the typed mid-chat recovery bubble. This is the user-visible payoff of COST-01 (per-message cost), COST-04 (per-thread total), and the PREF-01/D-09 recoverable key-failure path. ChatContainer is the integration point that wires both into the existing message list.

Purpose: All data is already on `useChat.Message` (Plan 02). This plan is pure render + wiring against locked UI-SPEC copy/color/spacing — no recomputation of cost (shown exactly as OpenRouter reports it), no new fetches.

Output: a muted cost caption inside the assistant bubble; the `Σ` total in the existing thread-header row; a typed `ErrorMessageBubble` variant with mapped recovery buttons; ChatContainer widened to pass `usage` and `errorType` through.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-CONTEXT.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-02-SUMMARY.md

<interfaces>
<!-- Exported by Plan 02 (useChat.ts). Import these — do not redefine cost logic. -->
  export interface Usage { prompt_tokens?: number; completion_tokens?: number; total_tokens?: number; cost?: number }
  Message.usage?: Usage; Message.errorType?: 'no_api_key' | 'payment_required' | 'forbidden'

ChatContainer currently declares LOCAL Message/ToolEvent interfaces (ChatContainer.tsx:8-22) that
  must be widened to include usage?:Usage + errorType (or import Message/Usage/ToolEvent from ../hooks/useChat).

startOpenRouterConnect (frontend/src/lib/pkce.ts) — zero-arg async; triggers the OAuth redirect. Use for [Reconnect].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-message cost caption in MessageBubble</name>
  <files>frontend/src/components/MessageBubble.tsx</files>
  <read_first>
    - frontend/src/components/MessageBubble.tsx (whole file — Props :6-10, bubble flex column + muted tool-toggle caption :12-31, assistant branch :51-78)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md § MessageBubble.tsx + § Shared Patterns "Muted caption token"
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Copywriting Contract (Per-message cost line) + § Color (muted captions) + § Typography (Caption 12px/400)
  </read_first>
  <action>
    Add an optional `usage?: Usage` prop to `Props` (import `Usage` from `../hooks/useChat` next to the existing `ToolEvent` import). Render a caption row at the BOTTOM of the assistant bubble's flex column (assistant role ONLY — never on user bubbles). Format per locked copy: `${cost} · ${tokens} tok` where `cost` = `usage.cost` to 4 decimals (`$0.0021`), `tokens` = `usage.total_tokens` formatted `1.2k` for ≥1000 (one decimal, drop trailing `.0`) else the integer. When `usage.cost` is null/absent (free model), OMIT the cost segment AND the `·` separator → show `${tokens} tok` only. Render NOTHING when there is no `usage` (or no displayable text). Use the locked muted token `text-xs text-gray-600 dark:text-gray-400 mt-1` — NOT `gray-500` on white (contrast guardrail). The caption must not change the `max-w-[70%]` bubble width (it is a row inside the existing container). A small inline `CostLine` sub-component is acceptable (RESEARCH § Code Examples shows the shape).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint clean.
    - Source assertion: the caption uses `text-gray-600 dark:text-gray-400` (not `gray-500`) and `text-xs`; it renders only when `role === 'assistant'` and `usage` yields displayable text.
    - Browser-observable: a paid-model assistant turn shows e.g. `$0.0021 · 1.2k tok` under the bubble; a free-model turn shows `840 tok` (no `$`, no `·`); a turn with no usage shows no caption; user bubbles never show a cost line.
  </acceptance_criteria>
  <done>Assistant bubbles carry an always-visible muted cost caption with correct null-cost and token-formatting behavior, in both themes; build + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Typed recovery variant of ErrorMessageBubble</name>
  <files>frontend/src/components/ErrorMessageBubble.tsx</files>
  <read_first>
    - frontend/src/components/ErrorMessageBubble.tsx (whole file — Props :3-6, red container + single Retry button :15-42)
    - frontend/src/components/ConfirmDialog.tsx (the neutral secondary-button treatment to mirror — gray-700/gray-600 dark, gray-200/gray-300 light)
    - frontend/src/lib/pkce.ts (startOpenRouterConnect signature)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md § ErrorMessageBubble.tsx
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Copywriting Contract (Recovery 401/402/403) + § Color (Error tokens light+dark) + § Components (typed recovery)
  </read_first>
  <action>
    Extend Props with an optional `type?: 'no_api_key' | 'payment_required' | 'forbidden'` and an optional `demoEligible?: boolean` (default false). Keep the existing untyped generic path (`onRetry` + `isStreaming` Retry button) intact for non-key stream errors. When `type` is set, render the typed variant: keep the `role="alert"` red-wash container but ADD light-mode tokens (`bg-red-50 border-red-300 text-gray-900`, icon `text-red-600`) beside the existing dark tokens (`bg-red-950/40 border-red-700 text-gray-100`, icon `text-red-400`). Render the LOCKED sentence + buttons per code:
      • `no_api_key` → sentence `Connect your OpenRouter account to keep chatting.` + `[Reconnect]` (primary blue-600, calls `startOpenRouterConnect()` from ../lib/pkce).
      • `payment_required` → sentence `Your key is out of credit (402).` + `[Add credits ⇗]` (primary blue-600, an `<a href="https://openrouter.ai/settings/credits" target="_blank" rel="noopener noreferrer">` styled as the primary button) + `[Reconnect]` (secondary neutral, calls startOpenRouterConnect).
      • `forbidden` → sentence `Your key was rejected (403).` + `[Reconnect]` (primary blue-600) + `[Use demo]` (secondary neutral) ONLY when `demoEligible` is true; with `demoEligible` false (the Phase-14 default) render `[Reconnect]` alone.
    Buttons reuse the existing Retry sizing `inline-flex items-center gap-1 px-3 py-2.5 md:py-1.5 rounded text-sm font-semibold` (≥44px touch); primary = `bg-blue-600 hover:bg-blue-700 text-white`; secondary = neutral (`bg-gray-700 hover:bg-gray-600 text-white dark` / `bg-gray-200 hover:bg-gray-300 text-gray-900 light`, matching ConfirmDialog cancel). NEVER interpolate a caught error, HTTP body, or sk-or fragment — only the locked sentences (the parenthetical 401/402/403 is the structured code, allowed). Use lucide `RotateCw` for Reconnect and `ExternalLink` for Add credits.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint clean.
    - Source assertion (exact copy): the file contains the strings `Connect your OpenRouter account to keep chatting.`, `Your key is out of credit (402).`, `Your key was rejected (403).`, `Reconnect`, `Add credits`.
    - Source assertion: `[Add credits ⇗]` is an anchor with `target="_blank"` and `rel="noopener noreferrer"`; `[Use demo]` is gated behind `demoEligible`.
    - Source assertion: the container carries BOTH light (`red-50`/`red-300`) and dark (`red-950/40`/`red-700`) tokens; the generic untyped Retry path still exists.
    - Browser-observable: a 401 bubble shows the reconnect sentence + a single blue Reconnect button; a 402 shows two buttons (Add credits opens OpenRouter credits in a new tab + Reconnect); a 403 shows Reconnect alone (demo OFF).
  </acceptance_criteria>
  <done>ErrorMessageBubble renders code-keyed recovery copy + mapped buttons in both themes, gates [Use demo] off this phase, and preserves the generic Retry path; build + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Σ total + usage passthrough + typed bubble in ChatContainer</name>
  <files>frontend/src/components/ChatContainer.tsx</files>
  <read_first>
    - frontend/src/components/ChatContainer.tsx (whole file — local Message/ToolEvent :8-22, header row :72-87, message map + ErrorMessageBubble/MessageBubble render :116-129)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md § ChatContainer.tsx (Analogs A/B) + § Shared Patterns "Muted caption token"
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Copywriting Contract (Per-thread running total) + § Components (Per-thread cost total consistency req: SAME h-12 row, ml-auto)
  </read_first>
  <action>
    Widen ChatContainer's LOCAL `Message`/`ToolEvent` interfaces (:8-22) to include `usage?: Usage` and `errorType?: 'no_api_key' | 'payment_required' | 'forbidden'` — simplest is to import `Message`, `Usage`, `ToolEvent` from `../hooks/useChat` and delete the local duplicates (confirm the rest of the file type-checks). Compute `const threadCost = messages.reduce((s, m) => s + (m.usage?.cost ?? 0), 0)` (D-02: persisted sum is the source of truth, correct on reload). In the EXISTING `activeThreadId !== null` header row (:72-87) — do NOT add a second row — append a muted caption `Σ ${threadCost.toFixed(4)}` (rendered with a `$`, e.g. `Σ $0.0142`) right-aligned via `ml-auto`, using the locked muted token `text-xs text-gray-600 dark:text-gray-400`; render it ONLY when `threadCost > 0` (omit `Σ $0.0000` for free/empty threads). In the message map (:116-129): pass `usage={msg.usage}` into `MessageBubble`; for the `msg.role === 'error'` case, pass `type={msg.errorType}` and `demoEligible={false}` into `ErrorMessageBubble` (keep `onRetry`/`isStreaming` so the untyped generic path still works when `errorType` is undefined). Preserve the `flex-1 flex flex-col h-full` layout and the per-thread `ModelSelector` wiring untouched (D-07).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint clean (interface widening must satisfy tsc strict — this catches usage/errorType type mismatches against useChat).
    - Source assertion: a `messages.reduce(` cost sum exists; the `Σ` caption is gated on `threadCost > 0` and uses `ml-auto` inside the existing `h-12` header row (no new header row added).
    - Source assertion: `MessageBubble` receives `usage={msg.usage}`; `ErrorMessageBubble` receives `type={msg.errorType}`.
    - Browser-observable: after a paid turn the header shows `Σ $…`; reload the thread and the total persists (read from history usage); a free-only thread shows no `Σ`; a 402 stream error renders the typed bubble (not the generic Retry bubble).
  </acceptance_criteria>
  <done>The thread header shows a reload-stable Σ total when > 0; per-message usage flows to MessageBubble; the typed recovery bubble renders for keyed errors; build + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useChat.Message → rendered bubbles | Only structured fields (usage numbers, errorType code) cross into render; raw provider strings never do |
| ErrorMessageBubble → OpenRouter | `[Reconnect]` starts the existing PKCE OAuth flow; `[Add credits ⇗]` is a static external link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Information Disclosure | recovery bubble copy | mitigate | Render ONLY the locked UI-SPEC sentences keyed on `errorType`; never interpolate `parsed.detail`, the caught error, or any sk-or fragment (acceptance asserts exact strings) |
| T-14-12 | Tampering / Spoofing | [Add credits ⇗] link | mitigate | `rel="noopener noreferrer"` + `target="_blank"` on the external anchor (no `window.opener` access) |
| T-14-13 | Information Disclosure | cost caption / Σ total | accept | Cost + token counts are non-secret display values, shown exactly as reported (no recomputation, SC#1) |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` clean after all three tasks.
- Source: `grep -n "ml-auto" frontend/src/components/ChatContainer.tsx` (Σ total right-aligned); `grep -n "openrouter.ai/settings/credits" frontend/src/components/ErrorMessageBubble.tsx`.
- Manual browser smoke per VALIDATION.md Manual-Only table: cost line + Σ persist across reload; typed 401/402/403 bubbles render correctly in light AND dark.
</verification>

<success_criteria>
- COST-01: always-visible per-message cost caption (null-cost safe).
- COST-04: reload-stable `Σ` per-thread total in the existing header row.
- PREF-01/D-09: typed in-thread recovery bubble with code-mapped actions, [Use demo] gated off this phase.
- Build + lint green; both themes coherent.
</success_criteria>

<output>
After completion, create `.planning/phases/14-usage-cost-display-settings-key-state-ux/14-03-SUMMARY.md`.
</output>
</content>
