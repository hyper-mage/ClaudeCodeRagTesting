---
phase: 14-usage-cost-display-settings-key-state-ux
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/src/hooks/useChat.ts
  - frontend/src/hooks/useKeyStatus.ts
autonomous: true
requirements: [COST-01, COST-02, COST-03, COST-04, PREF-01]

must_haves:
  truths:
    - "useChat captures usage from the live done SSE event and from history load, so cost data is on every assistant Message"
    - "A structured 401/402/403 chat-stream error reaches the rendered Message as a typed errorType, with NO toast on the typed key-failure path"
    - "useKeyStatus exposes balance (limit_remaining) + a derived isLow flag + loading/error state, fetched on demand (no polling, no Realtime)"
  artifacts:
    - path: "frontend/src/hooks/useChat.ts"
      provides: "Usage interface + Message.usage + Message.errorType + done.usage capture + loadMessages usage map + typed error stamping"
      contains: "errorType"
    - path: "frontend/src/hooks/useKeyStatus.ts"
      provides: "balance fetch + derived isLow + balanceLoading/balanceError on the key-status hook"
      contains: "balance"
  key_links:
    - from: "useChat done branch"
      to: "Message.usage"
      via: "usage: parsed.usage ?? m.usage on the message_id swap"
      pattern: "parsed.usage"
    - from: "useKeyStatus"
      to: "GET /api/keys/balance"
      via: "apiFetch gated on session+connected, captures loading/error, preserves notifyKeyStatusChanged broadcast"
      pattern: "api/keys/balance"
---

**Complexity:** ⚠️ Medium — the contract layer two Wave-3 waves consume; type-sensitive (strict tsc) hook edits for usage capture, typed errors, and an on-demand balance fetch. Low surface area, high blast radius (field names bind Plans 04/05).

<objective>
Extend the two read hooks so the new backend data (per-message usage, balance, typed errors) is available to every render surface. These hooks are the contract layer — Wave 3's plans (cost caption, amber dot, settings balance, typed recovery bubble) all consume what this plan exports.

Purpose: Per RESEARCH, `usage` must survive three hops (DB → MessageResponse → useChat.Message → bubble prop) and the structured error CODE must reach the rendered bubble as a typed field, not a thrown string. This plan owns the hook half of both.

Output: `Usage` interface + `usage`/`errorType` on `useChat`'s `Message`, live `done.usage` capture, `loadMessages` usage mapping, typed error stamping (no toast on the key-failure path); a balance fetch + derived `isLow` + loading/error state on `useKeyStatus`.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-CONTEXT.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-01-SUMMARY.md

<interfaces>
<!-- The shapes this plan exports for Wave 3 to consume. -->

Add to useChat.ts and export alongside ToolEvent:
  export interface Usage { prompt_tokens?: number; completion_tokens?: number; total_tokens?: number; cost?: number }
Message gains: usage?: Usage; errorType?: 'no_api_key' | 'payment_required' | 'forbidden'

Backend done event (chat.py:1184-1195): { message_id, content, usage?: {...}, mode?: "demo" }
Backend SSE error event (chat.py _sse_error): { error: <code>, detail: <fixed copy> }
  codes: 'no_api_key' | 'payment_required' | 'forbidden' | 'rate_limit' | 'upstream_error'
  (NOTE per Plan 01: a mid-stream 401 now also emits 'no_api_key' and a 403 emits 'forbidden' — both typed key-failure codes already in the union above.)
Backend BalanceResponse (GET /api/keys/balance, Plan 01): { connected: boolean; limit_remaining: number | null; is_low: boolean }

KeyStatus interface today (useKeyStatus.ts:5-9): { connected: boolean; masked_label?: string; connected_at?: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useChat — usage capture + typed errorType</name>
  <files>frontend/src/hooks/useChat.ts</files>
  <read_first>
    - frontend/src/hooks/useChat.ts (whole file — Message interface :25-32, loadMessages map :56-61, done branch :231-237, error branch :238-243, outer catch :254-283)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md § useChat.ts (Analogs A/B/C)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Copywriting Contract (the typed 401/402/403 path is the in-thread bubble alone — NO toast; the generic non-key path keeps its toast)
  </read_first>
  <action>
    Add an exported `Usage` interface (`prompt_tokens?`, `completion_tokens?`, `total_tokens?`, `cost?` — all optional numbers) next to `ToolEvent`. Add `usage?: Usage` and `errorType?: 'no_api_key' | 'payment_required' | 'forbidden'` to the `Message` interface.
    In `loadMessages` (the `data.messages.map` at :58-61) add `usage: m.usage as Usage | undefined` alongside the existing `toolsUsed` mapping so persisted usage survives reload (D-02 source-of-truth).
    In the `done` branch (`else if (parsed.message_id)` at :231-237) extend the message update to also set `usage: parsed.usage ?? m.usage` when swapping in the final id (captures the live turn's summed usage; falls back to any existing usage).
    Make the structured error CODE reach the rendered Message as `errorType`: the error SSE branch (:238-243) already throws the code string. In the outer `catch` (:254-283) detect when `err.message` is one of the typed key-failure codes (`no_api_key`, `payment_required`, `forbidden`); for those, set the placeholder to `{ role: 'error', errorType: <code>, content: '' }` (the typed bubble supplies copy from UI-SPEC — never `parsed.detail`) and DO NOT call `showToast` (the in-thread bubble is the single surface, Pitfall 3 + D-09). For every other error (rate_limit, upstream_error, AbortError already returns early, network) keep the EXISTING behavior unchanged: generic `content` sentence + the 4s error toast. Preserve the AbortError early-return and the `Sentry.captureException(err)` call for all non-abort errors.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc strict + vite) and `npm run lint` both pass with no new errors.
    - Source assertion: `Usage` is exported; `Message` declares both `usage?` and `errorType?`.
    - Source assertion: the `done` branch references `parsed.usage`; `loadMessages` map includes `m.usage`.
    - Browser-observable (verified in Wave 3 integration): a successful turn's Message carries `usage.cost`; a simulated 401/402/403 produces a Message with `role:'error'` + `errorType` and NO error toast; a generic stream failure still shows the toast.
  </acceptance_criteria>
  <done>useChat exports Usage; Message carries usage + errorType; done.usage and loadMessages usage are captured; typed key-failures stamp errorType and suppress the toast while non-key errors keep it; build + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend useKeyStatus — balance fetch + derived isLow + loading/error state</name>
  <files>frontend/src/hooks/useKeyStatus.ts</files>
  <read_first>
    - frontend/src/hooks/useKeyStatus.ts (whole file — KeyStatus interface :5-9, refresh :34-44, mount + broadcast effects :46-56, notifyKeyStatusChanged :20-22)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md § useKeyStatus.ts + § Shared Patterns "Hook fetch-into-state"
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Copywriting Contract (Balance line — the four states: capped / pay-as-you-go / loading / failed)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-RESEARCH.md Open Question 3 (on-demand only — settings open / after a turn; no poll, no Realtime)
  </read_first>
  <action>
    Add a balance fetch following the SAME fetch-into-state shape as `refresh`: gate on `session`, `apiFetch('/api/keys/balance')`, set a loading flag, catch errors WITHOUT throwing (keep last-known balance) but record that the fetch failed. Surface enough state for the TWO distinct consumers:
      • The dots (Plan 04) need a derived `isLow` boolean — read the server `is_low` directly, do NOT recompute against any FE threshold (D-03 keeps the threshold backend-only). `isLow` defaults to false (not low) when balance is unknown — never clobber the green dot on a transient balance failure.
      • SettingsPage (Plan 05) needs to render FOUR distinct balance-line states, so expose: the `balance` data (the {connected, limit_remaining, is_low} object or null), a `balanceLoading` flag (true while a fetch is in flight with no resolved data), and a `balanceError`/`balanceUnavailable` flag (true when a fetch threw and there is no usable data). `limit_remaining` may be null = pay-as-you-go (uncapped).
    Exact field names are your discretion, but all of {connected, isLow, limit_remaining/limitRemaining, balanceLoading, balanceError} must be reachable by consumers. CRITICAL CROSS-PLAN CONTRACT: Plans 04 and 05 hard-code the names `isLow`, `balance.limit_remaining`, `balanceLoading`, and `balanceError` against this hook. If you choose those exact names, Wave 3 binds with no change; if you deviate, you MUST record the FINAL field names VERBATIM in 14-02-SUMMARY.md (see <output>) so the Plan 04/05 executors rebind correctly — an undocumented rename silently breaks the amber dot and the settings balance line. Trigger the balance fetch on mount and on the existing `KEY_STATUS_EVENT` broadcast, and only when connected (a no-key fetch returns `{connected:false}` harmlessly — gating on connected avoids a pointless OpenRouter round-trip). Preserve `notifyKeyStatusChanged()` and the no-poll / no-Realtime contract exactly. Extend the `KeyStatus` interface (or add a new balance type) with the optional balance fields; keep `apiFetch` (it attaches the Supabase bearer and throws on non-2xx).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` and `npm run lint` pass with no new errors.
    - Source assertion: the hook calls `apiFetch('/api/keys/balance')` and exposes an `isLow`-equivalent boolean derived from the server `is_low` (no client-side threshold comparison appears) plus a loading flag and an error/unavailable flag for the balance.
    - Source assertion: no `setInterval`, no polling loop, and no Supabase `.channel(`/Realtime subscription added (on-demand only).
    - Browser-observable (Wave 3): with a low-balance key the hook reports isLow=true; with null limit_remaining it reports isLow=false; a thrown fetch sets the error flag; the status dot consumers and settings line read these.
  </acceptance_criteria>
  <done>useKeyStatus fetches balance on demand, exposes isLow + limit_remaining + loading + error derived from the server response, tolerates null limit_remaining, keeps the no-poll broadcast contract, and the FINAL balance field names are recorded in 14-02-SUMMARY.md; build + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → useChat | The `error` event carries a structured code + fixed detail; only the code drives UI, detail is never rendered |
| useKeyStatus → client state | Only `{connected, limit_remaining, is_low}` from the backend (already secret-free per Plan 01) lands in hook state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Information Disclosure | useChat error rendering | mitigate | The Message stores only the structured `errorType` code; `parsed.detail` / raw error is never written to `content` for the typed path (Wave 3 bubble renders locked copy) |
| T-14-09 | Information Disclosure | balance in client state | accept | Plan 01 guarantees the balance response is already secret-free (no key, no raw body); the hook only stores non-secret display values |
| T-14-10 | Denial of Service (quota) | balance fetch frequency | mitigate | On-demand only (mount + broadcast), gated on connected; no polling/Realtime — bounded OpenRouter calls (REQUIREMENTS out-of-scope: no balance polling) |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` clean after both tasks.
- Source: `grep -n "api/keys/balance" frontend/src/hooks/useKeyStatus.ts` matches; `grep -n "errorType" frontend/src/hooks/useChat.ts` matches.
- No new dependencies added (no package.json change).
</verification>

<success_criteria>
- `useChat.Message` carries `usage` (live + reloaded) and `errorType` (typed key-failures, no toast).
- `useKeyStatus` exposes balance + derived `isLow` + loading/error on demand, null-tolerant, no polling.
- Build + lint green; the contract is ready for the Wave 3 render surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/14-usage-cost-display-settings-key-state-ux/14-02-SUMMARY.md`.

In that SUMMARY, include a short "Hook contract / field names" section that records, VERBATIM, the FINAL exported balance field names from `useKeyStatus` (the chosen names for is-low, limit-remaining, balance-loading, balance-error, and the balance object). Plans 04 and 05 (Wave 3) bind to these — they currently assume `isLow`, `balance.limit_remaining`, `balanceLoading`, `balanceError`; flag any deviation explicitly so the Wave 3 executors rebind.
</output>
</content>
