---
phase: 14-usage-cost-display-settings-key-state-ux
plan: 04
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - frontend/src/components/IconSidebar.tsx
  - frontend/src/components/MobileTopBar.tsx
autonomous: true
requirements: [COST-03]

must_haves:
  truths:
    - "The persistent key-status dot turns amber when the connected key's balance is below threshold"
    - "The dot stays green when connected with OK/unknown (null) balance, gray when not connected — no animation, only fill + aria-label change"
    - "Implements decision D-05 surface #1 (the always-visible header key indicator turning amber when balance is low)"
  artifacts:
    - path: "frontend/src/components/IconSidebar.tsx"
      provides: "tri-state amber/green/gray status dot"
      contains: "amber-500"
    - path: "frontend/src/components/MobileTopBar.tsx"
      provides: "tri-state amber/green/gray status dot (identical logic)"
      contains: "amber-500"
  key_links:
    - from: "IconSidebar/MobileTopBar dot"
      to: "useKeyStatus isLow"
      via: "connected + isLow → amber fill + aria-label"
      pattern: "isLow|is_low"
---

**Complexity:** ✅ Simple — two near-identical dot recolors (gray/green/amber) driven by the existing `useKeyStatus.isLow` boolean; fill class + aria-label only, no new state, no animation, no fetch.

<objective>
Recolor the always-visible OpenRouter status dot (desktop rail + mobile top bar) to a tri-state so a low balance is visible without opening settings (COST-03 / D-05 surface #1). This is the non-intrusive header half of the low-balance warning.

Purpose: D-05 says the warning surfaces two non-intrusive ways — the header dot (here) and the settings line (Plan 05). No toast, no banner. The dot already exists as green/gray; this adds the amber low state driven by the extended `useKeyStatus`.

Output: an identical tri-state dot in both `IconSidebar` and `MobileTopBar`.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-CONTEXT.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md
@.planning/phases/14-usage-cost-display-settings-key-state-ux/14-02-SUMMARY.md

<interfaces>
<!-- Provided by Plan 02 (useKeyStatus). -->
  useKeyStatus() now exposes connection state + a derived isLow boolean (true only when connected AND
  limit_remaining is non-null AND below the server threshold; null limit_remaining → isLow false).

Current dot (IconSidebar.tsx:41-45, MobileTopBar.tsx:35-39):
  <span role="status" aria-label={connected ? 'OpenRouter connected' : 'OpenRouter not connected'}
        className={`h-2 w-2 rounded-full ${connected ? 'bg-green-500' : 'bg-gray-500'}`} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tri-state dot in IconSidebar</name>
  <files>frontend/src/components/IconSidebar.tsx</files>
  <read_first>
    - frontend/src/components/IconSidebar.tsx (whole file — useKeyStatus usage :11, the dot :41-45; note `mb-2` on the rail dot)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-PATTERNS.md § IconSidebar.tsx + MobileTopBar.tsx
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Color (Status-dot tri-state)
  </read_first>
  <action>
    Read `isLow` from `useKeyStatus()` alongside `status`. Replace the two-state dot with the locked tri-state: not connected → `bg-gray-500` (dark) / `bg-gray-400` (light) + `aria-label="OpenRouter not connected"`; connected AND `isLow` → `bg-amber-500` + `aria-label="OpenRouter balance low"`; connected AND not low (OK/unknown) → `bg-green-500` + `aria-label="OpenRouter connected"`. Keep `h-2 w-2 rounded-full role="status"` and the existing `mb-2`. No animation, no shape change — only the fill class and the `aria-label` change. A small `dot = !connected ? … : isLow ? … : …` lookup (RESEARCH § Code Examples status-dot tri-state) keeps it readable.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint clean.
    - Source assertion: the file contains `bg-amber-500` and `aria-label="OpenRouter balance low"`; the dot is still `h-2 w-2 rounded-full` with `role="status"`; no `animate-` class added to the dot.
    - Browser-observable: connect a key with a low capped balance → rail dot amber; connect with null/OK balance → green; disconnect → gray.
  </acceptance_criteria>
  <done>IconSidebar dot is tri-state, driven by useKeyStatus.isLow, with correct aria-labels and no animation; build + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tri-state dot in MobileTopBar (identical logic)</name>
  <files>frontend/src/components/MobileTopBar.tsx</files>
  <read_first>
    - frontend/src/components/MobileTopBar.tsx (whole file — useKeyStatus :19, the dot :35-39; note it has NO `mb-2`, keeps its inline placement)
    - frontend/src/components/IconSidebar.tsx (the just-edited dot logic to mirror exactly)
    - .planning/phases/14-usage-cost-display-settings-key-state-ux/14-UI-SPEC.md § Color (Status-dot tri-state)
  </read_first>
  <action>
    Apply the SAME tri-state dot logic as IconSidebar (gray-500 dark/gray-400 light not-connected, amber-500 connected+low, green-500 connected+OK), driven by `isLow` from `useKeyStatus()`, with matching `aria-label` values. Preserve MobileTopBar's inline placement and the absence of `mb-2` (do not copy the rail spacing). Keep `h-2 w-2 rounded-full role="status"`, no animation.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint clean.
    - Source assertion: the file contains `bg-amber-500` and `aria-label="OpenRouter balance low"`; dot remains `h-2 w-2 rounded-full role="status"` with no `mb-2` and no animation.
    - Browser-observable (mobile width / responsive): the top-bar dot matches the rail dot state (amber on low, green on OK, gray on disconnected).
  </acceptance_criteria>
  <done>MobileTopBar dot mirrors the IconSidebar tri-state with correct placement and labels; build + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useKeyStatus.isLow → dot fill | Only a boolean crosses into the indicator; the raw balance/threshold never reaches these components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Information Disclosure | low-balance dot | mitigate | The dot consumes only the boolean `isLow` (computed server-side, exposed by Plan 02); the exact balance and threshold never reach the indicator |
| T-14-15 | Tampering | dot state | accept | Display-only indicator; no user input, no action — incorrect state is a cosmetic risk only, bounded by the on-demand fetch |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` clean.
- Source: `grep -rn "amber-500" frontend/src/components/IconSidebar.tsx frontend/src/components/MobileTopBar.tsx` matches both.
- Manual: VALIDATION.md Manual-Only "Amber low-balance indicator" — configure LOW_BALANCE_THRESHOLD_USD above the test balance, confirm both dots amber.
</verification>

<success_criteria>
- COST-03 (surface #1): both persistent dots show amber on low balance, green on OK/unknown, gray on disconnected, with accurate aria-labels and no animation.
- Build + lint green; light + dark coherent.
</success_criteria>

<output>
After completion, create `.planning/phases/14-usage-cost-display-settings-key-state-ux/14-04-SUMMARY.md`.
</output>
</content>
