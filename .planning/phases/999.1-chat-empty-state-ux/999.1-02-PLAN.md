---
phase: "999.1"
plan: "02"
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - frontend/src/components/ChatContainer.tsx
  - frontend/src/components/ChatContainer.test.tsx
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "When messages.length === 0, the message pane shows a welcoming headline + sub-line + 2–3 tappable example-prompt chips"
    - "Tapping a chip calls onSend with that chip's exact text, routing through the same send path"
    - "Anonymous users additionally see a D&D-sample hint; authed users do not; both share one chip layout"
    - "The empty-state lives strictly inside {messages.length === 0 && (…)} so it vanishes the instant the first bubble lands"
  artifacts:
    - path: "frontend/src/components/ChatContainer.tsx"
      provides: "Empty-state with headline (text-2xl font-bold), sub-line (text-sm text-gray-400), and example-prompt chips (bg-gray-800, min-h-11)"
      contains: "text-2xl font-bold"
    - path: "frontend/src/components/ChatContainer.test.tsx"
      provides: "Component tests for chip render, chip tap → onSend, and isAnon hint reconciliation"
      contains: "onSend"
  key_links:
    - from: "example-prompt chip <button>"
      to: "onSend(chipText)"
      via: "onClick"
      pattern: "onSend\\("
    - from: "empty-state render"
      to: "messages.length === 0"
      via: "conditional wrapper"
      pattern: "messages\\.length === 0"
---

<objective>
Replace the bland text-only empty-state at `ChatContainer.tsx:34-42` with the approved welcoming empty-state: a headline, a one-sentence sub-line, and 2–3 board-game example-prompt chips that send through the existing `onSend` chain (D-02).

Purpose: The empty pane today is a single muted `<p>`. The UI-SPEC (approved) specifies a warm, on-domain welcome whose chips give first-time users an instant way to exercise the agent. Because chips call the same `onSend` the composer uses, they automatically benefit from the auto-create-on-send fix (Plan 03) — no parallel send path.

Output:
- `ChatContainer.tsx` empty-state block rebuilt with headline + sub-line + chip cluster, the `isAnon` branch reconciled to one layout + an anon-only hint line.
- `ChatContainer.test.tsx` covering chip render, chip tap → `onSend`, and the anon-hint reconciliation.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-chat-empty-state-ux/999.1-CONTEXT.md
@.planning/phases/999.1-chat-empty-state-ux/999.1-UI-SPEC.md
@.planning/phases/999.1-chat-empty-state-ux/999.1-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly. -->

ChatContainer current props (frontend/src/components/ChatContainer.tsx) — UNCHANGED by this plan:
  interface Props {
    messages: Message[]
    onSend: (content: string) => void
    isStreaming: boolean
    onRetry: () => void
  }
  - useAuth() supplies `isAnon` (already imported at line 4, used at line 30).
  - The empty-state block to replace is lines 34-42 (the `{messages.length === 0 && (...)}` wrapper).
  - ChatInput mount at line 56 stays `disabled={isStreaming}` only — do NOT gate it on thread existence.

Auth contract (frontend/src/contexts/AuthContext.tsx):
  useAuth() -> { user, session, loading, signOut, isAnon }   (isAnon = user?.is_anonymous ?? false)

Test helper (created in Plan 01, frontend/src/test/utils.tsx):
  renderWithProviders(ui, { isAnon? })  — wraps AuthProvider-equivalent + ToastProvider, lets a test set isAnon.
  Use @testing-library/user-event for the chip clicks. onSend is a vi.fn() the test passes as a prop.
</interfaces>

@frontend/src/components/ChatContainer.tsx
@frontend/src/components/ChatInput.tsx
@frontend/src/components/MessageBubble.tsx
@frontend/src/components/ErrorMessageBubble.tsx
@frontend/src/contexts/AuthContext.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild the empty-state with headline + sub-line + example-prompt chips</name>
  <files>frontend/src/components/ChatContainer.tsx</files>
  <read_first>
    - frontend/src/components/ChatContainer.tsx (the file being modified — empty-state at 34-42, Props at 22-27, useAuth at 30, ChatInput mount at 56)
    - .planning/phases/999.1-chat-empty-state-ux/999.1-UI-SPEC.md § Typography, § Color, § Spacing, § Copywriting Contract, § Accessibility (the APPROVED visual contract — every token below comes from here)
    - .planning/phases/999.1-chat-empty-state-ux/999.1-PATTERNS.md § ChatContainer.tsx (the target empty-state shape, the chip surface reference from MessageBubble, the button conventions from ErrorMessageBubble)
    - frontend/src/components/MessageBubble.tsx (chip surface: assistant bubble is bg-gray-800; user bubble is bg-blue-600 — chips MUST use the gray surface, never the blue fill)
    - frontend/src/components/ErrorMessageBubble.tsx (button conventions: type="button", text-sm font-semibold NOT font-medium, py-2.5 touch target)
  </read_first>
  <action>
    In frontend/src/components/ChatContainer.tsx, replace the empty-state block (lines 34-42) while keeping it inside `{messages.length === 0 && (…)}`. Render: a centered container (`h-full flex items-center justify-center`, inner `max-w-md w-full flex flex-col items-center gap-4 text-center`); a real heading element `<h2 className="text-2xl font-bold text-gray-200">` with warm on-domain copy (UI-SPEC default "Ask about any board game" — wording is Claude's Discretion within the board-game-KB tone, do NOT drift to generic "Start chatting"); a `<p className="text-sm text-gray-400">` sub-line naming the three things the agent does (UI-SPEC default "Search rules, compare mechanics, or get a recommendation."); and a vertical chip cluster (`flex flex-col gap-2 w-full`).
    Define the chip prompts as a module-level `UPPER_SNAKE_CASE` constant array `EXAMPLE_PROMPTS` (matching the project's TOOL_LABELS/ACCEPTED_TYPES convention) with the three UI-SPEC prompts: "Compare Catan vs Wingspan", "How do you win Azul?", "Recommend a 2-player game". Render each as a native `<button type="button" onClick={() => onSend(q)}>` with classes `min-h-11 px-4 py-2.5 bg-gray-800 hover:bg-gray-700 rounded text-left text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500`. Pick the FILLED `bg-gray-800` chip treatment (apply uniformly — do NOT mix with the bordered alternative). NEVER use `bg-blue-600` for the chip resting fill (UI-SPEC explicit prohibition — it reads as a sent user bubble and blows the 10% accent budget). Do NOT use `font-medium` on any new element (2-weight contract: 400 or 600/700 only); chip labels may be body weight or font-semibold, never font-medium.
    Reconcile the `isAnon` branch: unify the layout for both anon and authed users (same headline + sub-line + chips). Preserve the anon onboarding cue as an OPTIONAL hint line shown ONLY when `isAnon` — `<p className="text-xs text-gray-500">` with copy like "A D&D 5e quick-reference is already attached — try asking about it." Do NOT branch the entire empty-state on auth state (that re-introduces the maintenance split the fix removes).
    Chip component extraction (inline vs a new EmptyState.tsx/ExamplePrompts.tsx) is Claude's Discretion — inline in ChatContainer is acceptable and keeps file ownership simple; if extracted, default-export a PascalCase component taking `{ isAnon, onSend }` and keep EXAMPLE_PROMPTS module-level. Either way, files_modified for this plan must stay limited to ChatContainer.tsx (+ its test); if you extract a component, add it to the SUMMARY's files list but prefer inline to avoid scope creep.
    Leave the ChatInput mount (line 56) and the message-map (43-54) untouched.
  </action>
  <acceptance_criteria>
    - Source: ChatContainer.tsx empty-state is wrapped in `{messages.length === 0 && (…)}` and contains `text-2xl font-bold` (headline) and `text-sm text-gray-400` (sub-line).
    - Source: a module-level `EXAMPLE_PROMPTS` array holds 2–3 board-game prompts; each chip is `<button type="button">` with `bg-gray-800` and `min-h-11`; NO chip uses `bg-blue-600` as a resting fill; NO new element uses `font-medium`.
    - Source: the D&D hint renders only under `isAnon`; the headline/sub-line/chips render for both anon and authed.
    - CLI: `cd frontend && npx tsc -b --noEmit` exits 0 and `npm run lint` is clean for ChatContainer.tsx.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx eslint src/components/ChatContainer.tsx</automated>
  </verify>
  <done>The empty-state renders a headline, sub-line, and 2–3 filled gray chips inside the messages.length===0 guard; the anon hint is gated on isAnon; no blue chip fill and no font-medium; type-check and lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Component tests — chip render, chip tap → onSend, anon-hint reconciliation</name>
  <files>frontend/src/components/ChatContainer.test.tsx</files>
  <read_first>
    - frontend/src/components/ChatContainer.tsx (the component just modified in Task 1 — the chip texts and the isAnon hint are the assertions)
    - frontend/src/test/utils.tsx (renderWithProviders with settable isAnon + the mock helpers, created in Plan 01)
    - .planning/phases/999.1-chat-empty-state-ux/999.1-RESEARCH.md § Phase Requirements → Test Map (the exact D-02 rows: renders chips, chip tap sends prompt, anon hint reconciliation)
    - .planning/phases/999.1-chat-empty-state-ux/999.1-PATTERNS.md § Tappable-button conventions (accessible name = visible text; query chips by role 'button' + name)
  </read_first>
  <behavior>
    - Test 1 (renders empty-state chips): rendering ChatContainer with `messages={[]}` shows the headline text and exactly the EXAMPLE_PROMPTS chips (query `getByRole('button', { name: /Compare Catan vs Wingspan/ })` etc. — accessible name is the visible text).
    - Test 2 (chip tap sends prompt): clicking a chip calls the `onSend` vi.fn() exactly once with that chip's exact text.
    - Test 3 (anon hint reconciliation): with `isAnon: true` the D&D hint text is present AND the three chips are present; with `isAnon: false` the D&D hint is absent but the same three chips ARE present (shared layout).
    - Test 4 (empty-state gating): rendering with a non-empty `messages` array does NOT render the headline/chips (the empty-state is strictly behind messages.length === 0).
  </behavior>
  <action>
    Write frontend/src/components/ChatContainer.test.tsx using `renderWithProviders` (settable isAnon) from src/test/utils.tsx and `userEvent` from @testing-library/user-event. Pass `onSend={vi.fn()}`, `isStreaming={false}`, `onRetry={vi.fn()}`. Cover the four behaviors above. Query chips by role+accessible-name (the visible question text) per the accessibility contract — do NOT query by class. For Test 4, pass a one-element messages array (a user message) and assert the headline is NOT in the document. Keep the tests resilient to discretionary copy: assert the three EXACT chip strings (those are locked by the UI-SPEC table) but for the headline assert on the `<h2>` via role 'heading' (or a stable substring you also used in the component). ChatContainer makes no network calls; only onSend (a vi.fn) matters — mock nothing network-related.
    RED→GREEN: tests should pass against the Task-1 implementation; if any fail, fix the component (not the test) to match the UI-SPEC contract.
  </action>
  <acceptance_criteria>
    - Behavior: the three EXAMPLE_PROMPTS chips render and each is reachable via `getByRole('button', { name })`.
    - Behavior: clicking a chip calls `onSend` once with that chip's exact text.
    - Behavior: `isAnon:true` shows the D&D hint + chips; `isAnon:false` hides the hint but keeps the chips.
    - Behavior: a non-empty messages array hides the headline/chips.
    - CLI: `cd frontend && npx vitest run src/components/ChatContainer.test.tsx` exits 0; `npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ChatContainer.test.tsx && npx tsc -b --noEmit</automated>
  </verify>
  <done>All four ChatContainer behaviors are green; chips are asserted by accessible name; the anon hint is correctly gated; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| empty-state UI → React render | Chip labels are static, hard-coded constants rendered as React text nodes (auto-escaped). No user-controlled string enters the empty-state markup. |
| chip tap → onSend → existing send path | A chip routes a constant string into the same `onSend` the composer uses; no new endpoint, no new sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-04 | Tampering (XSS) | empty-state headline / sub-line / chip labels | mitigate | All copy is static constants rendered as JSX text nodes (auto-escaped by React). No `dangerouslySetInnerHTML`, no interpolated user/assistant content in the empty-state. No new XSS sink. |
| T-999.1-05 | Spoofing (UI affordance) | example-prompt chips | accept | Chips use the gray suggestion surface (bg-gray-800), visually distinct from the blue sent-user bubble, so they cannot masquerade as already-sent messages (UI-SPEC color rule). Cosmetic; low risk. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/ChatContainer.test.tsx` is green.
- `cd frontend && npx tsc -b --noEmit` is green.
- `cd frontend && npx eslint src/components/ChatContainer.tsx src/components/ChatContainer.test.tsx` is clean.
- Source grep (color guard): `grep -v '^\s*//' frontend/src/components/ChatContainer.tsx | grep -c 'bg-blue-600'` returns 0 (no blue chip fill introduced).
- Source grep (weight guard): `grep -v '^\s*//' frontend/src/components/ChatContainer.tsx | grep -c 'font-medium'` returns 0.
</verification>

<success_criteria>
- The empty-state renders a welcoming headline + sub-line + 2–3 tappable board-game chips when `messages.length === 0`.
- Tapping a chip calls `onSend` with the chip text (single send path; benefits from Plan 03's auto-create).
- Anon users get the D&D hint; authed users do not; both share one chip layout.
- No `bg-blue-600` chip fill, no `font-medium`, no new color/font/size/dependency.
- All D-02 behaviors covered by automated component tests; type-check + lint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-chat-empty-state-ux/999.1-02-SUMMARY.md`.
</output>
