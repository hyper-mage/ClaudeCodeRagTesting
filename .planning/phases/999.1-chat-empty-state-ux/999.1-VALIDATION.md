---
phase: 999.1
slug: chat-empty-state-ux
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-23
updated: 2026-06-23
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **Wave 0 decision (recorded):** Option (a) — INSTALL a frontend component-test runner.
> Plan 01 Task 1 installs `vitest` + `@testing-library/react` + `@testing-library/jest-dom`
> + `@testing-library/user-event` + `jsdom` and wires an `npm run test` script + a shared
> `renderWithProviders`/SSE-mock helper. The behaviors D-01..D-04 are component-testable in
> jsdom; the one thing jsdom cannot prove (a live SSE round-trip + real Supabase create + backend
> titling) is covered by a single blocking human-verify checkpoint in Plan 03 Task 3.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (installed in Plan 01 Task 1; standard Vite/React 19 component-test stack) + @testing-library/react + jsdom |
| **Config file** | `frontend/vitest.config.ts` (`environment: 'jsdom'`, `globals: true`, `setupFiles: ['./src/test/setup.ts']`) — created in Plan 01 Task 1 (`none` until then) |
| **Quick run command** | `cd frontend && npx tsc -b --noEmit && npm run lint` (always available) — add `&& npx vitest run --changed` once Plan 01 Task 1 lands the runner |
| **Full suite command** | `cd frontend && npm run test && npx tsc -b --noEmit && npm run lint` |
| **Estimated runtime** | ~10–25 seconds (small jsdom component suite + tsc + eslint) |

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && npx tsc -b --noEmit && npm run lint` (pre-runner) → `cd frontend && npx vitest run <changed test> && npx tsc -b --noEmit` (post-runner, from Plan 01 Task 1 onward).
- **After every plan wave:** Run the full suite `cd frontend && npm run test && npx tsc -b --noEmit && npm run lint`.
- **Before `/gsd-verify-work`:** Full suite green (all D-01..D-04 component tests) AND the Plan 03 human-verify checkpoint approved.
- **Max feedback latency:** ~25 seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.1-01-01 | 01 | 1 | Wave 0 infra | T-999.1-02 | Test fixtures use synthetic ids, no real tokens | infra/smoke | `cd frontend && npm run test && npx tsc -b --noEmit` | ❌ → ✅ this task | ⬜ pending |
| 999.1-01-02 | 01 | 1 | D-01, D-03 | T-999.1-01 / T-999.1-03 | Explicit threadId only from server-issued ids; loadMessages guard non-destructive | unit (hook) | `cd frontend && npx vitest run src/hooks/useChat.test.tsx && npx tsc -b --noEmit` | ❌ → ✅ this task | ⬜ pending |
| 999.1-02-01 | 02 | 2 | D-02 | T-999.1-04 | Static auto-escaped chip text; no dangerouslySetInnerHTML | source/UI | `cd frontend && npx tsc -b --noEmit && npx eslint src/components/ChatContainer.tsx` | n/a (source) | ⬜ pending |
| 999.1-02-02 | 02 | 2 | D-02 | T-999.1-05 | Chips use gray suggestion surface, not blue sent-bubble fill | component (RTL) | `cd frontend && npx vitest run src/components/ChatContainer.test.tsx && npx tsc -b --noEmit` | ❌ → ✅ this task | ⬜ pending |
| 999.1-03-01 | 03 | 2 | D-01, D-03, D-04 | T-999.1-06 / T-999.1-07 / T-999.1-08 | Server-issued thread id only; create-failure surfaces toast+Sentry; generic error copy | source | `cd frontend && npx tsc -b --noEmit && npx eslint src/pages/ChatPage.tsx` | n/a (source) | ⬜ pending |
| 999.1-03-02 | 03 | 2 | D-01, D-03, D-04 | T-999.1-06 / T-999.1-07 | End-to-end onSend→handleSend→sendMessage in jsdom; optimistic bubble survives; failure feedback | component (RTL) | `cd frontend && npx vitest run src/pages/ChatPage.test.tsx && npx tsc -b --noEmit` | ❌ → ✅ this task | ⬜ pending |
| 999.1-03-03 | 03 | 2 | D-01, D-03, D-04 | — | Live SSE + RLS create + backend titling (cold start + post-delete + chip + failure) | manual / human-verify | see Manual-Only table | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Sampling continuity check:** No run of 3 consecutive tasks lacks an automated verify — every task except the final human-verify checkpoint (999.1-03-03) has an `<automated>` command, and that checkpoint is the documented manual exception (live SSE cannot run in jsdom).

---

## Wave 0 Requirements

Wave 0 is **Plan 01 Task 1** (decision: install the runner). It must create:

- [ ] `frontend/package.json` — add `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom` to devDependencies + a `"test": "vitest run"` script (run mode, no watch).
- [ ] `frontend/vitest.config.ts` — `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./src/test/setup.ts']`, reusing the existing `@vitejs/plugin-react`.
- [ ] `frontend/src/test/setup.ts` — `@testing-library/jest-dom` matchers + `crypto.randomUUID` polyfill if jsdom lacks it (useChat.ts calls it for message ids).
- [ ] `frontend/src/test/utils.tsx` — `renderWithProviders(ui, { isAnon? })` wrapping AuthContext (settable `isAnon`) + `ToastProvider`, plus `vi.mock`-based `apiFetch`/`apiStream` mocks and a `mockSSEResponse(chunks)` helper. Imported by Plans 02 and 03.
- [ ] `frontend/src/hooks/useChat.test.tsx` — D-01 explicit-threadId send, no-opts guard preserved, fallback to closured id, Pitfall-2 loadMessages no-clobber (Plan 01 Task 2).
- [ ] `frontend/src/components/ChatContainer.test.tsx` — D-02 chip render, chip tap → onSend, anon-hint reconciliation, empty-state gating (Plan 02 Task 2).
- [ ] `frontend/src/pages/ChatPage.test.tsx` — D-01/D-03/D-04 cold-start auto-create, explicit-id send, post-delete parity, untitled create, create-failure feedback (Plan 03 Task 2).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live cold-start: first message auto-creates a real thread, streams a real SSE reply, sidebar shows backend-derived title (not a stuck "New Chat") | D-01, D-03 | Requires the running FastAPI backend + Supabase (RLS create + real SSE + backend titling) — not reproducible in jsdom | Plan 03 Task 3 checkpoint step 1 (login `ragtest1@gmail.com`/`testpass123`, send from a null state, observe bubble + stream + title) |
| Chip path end-to-end | D-01, D-02 | Same live-SSE reason | Plan 03 Task 3 step 2 (tap "Compare Catan vs Wingspan", observe auto-create + stream) |
| Post-delete parity live | D-04 | Same live-SSE reason | Plan 03 Task 3 step 3 (delete active thread → send → new thread auto-created) |
| Empty-state does not flicker back after first bubble (live) | D-02 / Pitfall 2 | Visual timing under a real stream | Plan 03 Task 3 step 1 (watch the empty-state vanish on bubble append, not reappear) |
| Anon D&D hint shows only for anonymous sessions (live auth) | D-02 | Depends on a real anonymous Supabase session | Plan 03 Task 3 step 5 |

*(All other phase behaviors have automated component-test verification.)*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or are the documented live-SSE human-verify exception (999.1-03-03)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (the runner + helper + three test files)
- [x] No watch-mode flags (`vitest run`, never `vitest`/`--watch`)
- [x] Feedback latency < 25s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready
