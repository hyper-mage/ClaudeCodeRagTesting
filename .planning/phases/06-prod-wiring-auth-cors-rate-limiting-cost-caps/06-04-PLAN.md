---
phase: 06-prod-wiring-auth-cors-rate-limiting-cost-caps
plan: 04
type: execute
wave: 2
depends_on: [06-03]
files_modified:
  - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md
autonomous: false
requirements: [SEC-01, SEC-06]
requirements_addressed: [SEC-01, SEC-06]

must_haves:
  truths:
    - "Supabase prod Site URL = https://boardgame-rag-prod.pages.dev (D-12)"
    - "Supabase prod Redirect URLs allowlist includes both https://boardgame-rag-prod.pages.dev/** and http://localhost:5173/** (D-13)"
    - "A FRESH throwaway-email signup from prod CF URL completes end-to-end: email arrives, click lands on prod (NOT localhost), user is logged in, and a chat message succeeds via SSE"
    - "OpenRouter dashboard has a $0.01 alert configured with developer email <your-email> (D-19)"
    - "OpenRouter alert delivery confirmed: load $1 → swap LLM_MODEL to openai/gpt-4o-mini → send 1 chat → receive alert email → revert LLM_MODEL to :free → drain remaining balance (D-20)"
    - "Verification artifacts (screenshots, timestamps, fresh email used) recorded in 06-04-VERIFICATION.md"
  artifacts:
    - path: ".planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md"
      provides: "Manual verification log with timestamps + screenshot references for D-15 (signup E2E) and D-19/D-20 (alert config + delivery)"
      contains: "D-15"
  key_links:
    - from: "Supabase prod project dashboard"
      to: "Authentication → URL Configuration"
      via: "Site URL field + Redirect URLs allowlist"
      pattern: "boardgame-rag-prod\\.pages\\.dev"
    - from: "OpenRouter dashboard"
      to: "Account → Alerts ($0.01 threshold)"
      via: "Email recipient configuration"
      pattern: "0\\.01"
    - from: "Test paid swap (one-time)"
      to: "Alert email at developer inbox"
      via: "flyctl secrets set + chat request + email confirmation"
      pattern: "openai/gpt-4o-mini"
---

<objective>
Complete the two manual / out-of-repo verifications for SEC-01 (Supabase Auth redirect URLs working end-to-end from prod CF URL) and SEC-06 (OpenRouter alert configured + delivery confirmed). Records all evidence in `06-04-VERIFICATION.md` so the verifier and future-you can audit.

Purpose: Close the SEC-01 and SEC-06 requirements which CANNOT be automated:
- SEC-01 requires real email delivery and a real human clicking a link.
- SEC-06 alert delivery requires loading real money + a real email arriving + a screenshot.

This plan is `autonomous: false` because every task is a checkpoint. It runs LAST in the phase (after 06-03 has confirmed the runtime backend is healthy with `:free` model active).

Output: `06-04-VERIFICATION.md` populated with timestamps, screenshots references, and pass/fail status for D-15 (signup E2E), D-19 (alert configured), D-20 (alert delivered).
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-CONTEXT.md
@.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-RESEARCH.md
@.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-03-PLAN.md

<interfaces>
<!-- Out-of-repo systems touched -->
- Supabase Dashboard (prod project): Authentication → URL Configuration
  - Site URL field
  - Redirect URLs (allowlist)
- OpenRouter Dashboard: Account Settings → Alerts
  - $0.01 threshold
  - Email recipient
- Developer email inbox (<your-email> per user MEMORY.md)
- Fly secrets: LLM_MODEL temporarily swapped to openai/gpt-4o-mini, then reverted

<!-- D-12 / D-13 locked values -->
SITE_URL = https://boardgame-rag-prod.pages.dev
REDIRECT_URLS = [
  https://boardgame-rag-prod.pages.dev/**,
  http://localhost:5173/**,
]

<!-- D-16 / D-20 locked model ids -->
LLM_MODEL_FREE_DEFAULT = openai/gpt-oss-120b:free   # active on prod (set in 06-03)
LLM_MODEL_PAID_OPTION = openai/gpt-4o-mini          # for one-time alert delivery test
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4-1: Configure Supabase Auth Site URL + Redirect URLs in prod dashboard (D-12, D-13)</name>
  <files>.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-supabase-url-config.png</files>
  <read_first>
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-CONTEXT.md §D-12, D-13, D-14 (lines 56-63)
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-RESEARCH.md §Pattern 4 (lines 244-260) — wildcard syntax, Site URL semantics
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-RESEARCH.md §Pitfall 7 (lines 378-381) — Site URL change vs old tokens (mitigated by D-15 fresh-email rule)
    - .planning/phases/03-prod-supabase-project/03-02-SUMMARY.md (if exists — to find prod project ref / dashboard URL)
  </read_first>
  <action>
    CHECKPOINT — Human action required. Supabase Auth dashboard has no usable CLI/API for these specific fields, so Claude cannot automate. Operator performs the steps below.

    Background: Backend (Phase 4) and frontend (Phase 5) are deployed; runtime hardening (rate limit, max-iter cap, :free model) is live (06-01..06-03). The ONLY remaining out-of-repo blocker for SEC-01 is the Supabase Auth URL Configuration in the prod project dashboard. This MUST be set BEFORE the signup E2E test in Task 4-2 — confirmation links generated by Supabase use `{{ .SiteURL }}` at email-send time, so old (dev) URL = old links.

    Steps for the human to perform:

    1. Open the Supabase prod project dashboard (created in Phase 3, NOT dev). The exact URL is in your password manager (1Password) per Phase 3 D-22; form: `https://supabase.com/dashboard/project/<prod-project-ref>`.

    2. Navigate: Authentication → URL Configuration.

    3. Set Site URL field to: `https://boardgame-rag-prod.pages.dev`
       - No trailing slash. No `/**` suffix on Site URL — wildcards belong only in Redirect URLs.
       - Click Save. Confirm the save success toast.

    4. Add Redirect URLs (allowlist). Add these TWO entries — both with the `/**` wildcard suffix:
       - `https://boardgame-rag-prod.pages.dev/**`
       - `http://localhost:5173/**`
       The first allows production redirects (any path); the second preserves local dev workflow per D-13 rationale ("future dev-against-prod-auth sessions don't break").
       Click Save for each. Confirm both appear in the allowlist after saving.

    5. Verify Email Templates remain DEFAULT (D-14: no custom HTML this phase). Navigate Authentication → Email Templates and confirm:
       - "Confirm signup" template is the default (no custom HTML edits).
       - The body contains `{{ .ConfirmationURL }}` token.
       If anything has been customized, revert to default.

    6. Take a screenshot of the URL Configuration page showing both Site URL and Redirect URLs. Save with filename:
       `.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-supabase-url-config.png`
       Create the `screenshots/` subdirectory if it doesn't exist. Commit it (image files are small and provide audit evidence).

    7. Initialize the verification log file. Create `.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md` with this structure:

       ```markdown
       # Phase 6 — Manual Verification Log (D-15, D-19, D-20)

       ## D-12, D-13: Supabase Auth URL Configuration (SEC-01 setup)

       - Date/time configured: <YYYY-MM-DD HH:MM TZ>
       - Site URL: `https://boardgame-rag-prod.pages.dev`
       - Redirect URLs:
         - `https://boardgame-rag-prod.pages.dev/**`
         - `http://localhost:5173/**`
       - Email templates: default (D-14 — no custom HTML)
       - Screenshot: `screenshots/06-04-supabase-url-config.png`
       - Status: [ ] PASS / [ ] FAIL

       ---

       ## D-15: Signup E2E from prod CF URL (SEC-01 verification)

       (To be filled by Task 4-2.)

       ---

       ## D-19: OpenRouter $0.01 alert configured (SEC-06 setup)

       (To be filled by Task 4-3.)

       ---

       ## D-20: OpenRouter alert delivery test (SEC-06 verification)

       (To be filled by Task 4-4.)
       ```

       Tick the D-12/D-13 PASS checkbox after completing steps 1-6.

    Resume signal: Type "configured" once Site URL + Redirect URLs are saved, screenshot captured, and 06-04-VERIFICATION.md initialized. If unable to access the dashboard or any field is grayed out, describe the blocker.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md && test -f .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-supabase-url-config.png && grep -q "Site URL.*boardgame-rag-prod.pages.dev" .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Supabase prod dashboard → URL Configuration shows `Site URL = https://boardgame-rag-prod.pages.dev` (no trailing slash, no wildcard).
    - Redirect URLs allowlist contains both `https://boardgame-rag-prod.pages.dev/**` AND `http://localhost:5173/**`.
    - Email templates are default (Authentication → Email Templates → "Confirm signup" body unchanged).
    - Screenshot saved at `.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-supabase-url-config.png` (verify: `test -f` exits 0).
    - `06-04-VERIFICATION.md` exists with the D-12/D-13 section filled in and PASS ticked (verify: `grep -q "Site URL.*boardgame-rag-prod.pages.dev" 06-04-VERIFICATION.md`).
    - User responds with `configured` to resume.
  </acceptance_criteria>
  <done>Supabase Auth Site URL + Redirect URLs configured in prod dashboard per D-12/D-13; email templates left default per D-14; verification log initialized with first section PASS and screenshot saved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4-2: Manual signup E2E from prod CF URL with FRESH throwaway email (D-15, SEC-01)</name>
  <files>.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-signup-step1.png, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-signup-step2-email.png, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-signup-step3-confirmed.png, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-signup-step4-chat.png</files>
  <read_first>
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-CONTEXT.md §D-15 (lines 64-69) — verbatim verification flow
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-RESEARCH.md §Pitfall 7 (lines 378-381) — fresh email rule
    - 06-04-VERIFICATION.md (after Task 4-1; D-12/D-13 section filled in PASS)
  </read_first>
  <action>
    CHECKPOINT — Human action required. SEC-01 verification: real human signing up from production frontend with a fresh email and confirming end-to-end flow works.

    CRITICAL: D-15 specifies a FRESH throwaway email — do NOT reuse `ragtest1@gmail.com` (that user already exists; verification of "first-time signup" needs a brand-new account). Use a `+suffix` Gmail alias OR a throwaway service.

    Pre-flight check:
    1. Confirm `https://boardgame-rag-prod.fly.dev/api/health` returns 200 (backend health, NOT through CF Pages).
    2. Confirm `https://boardgame-rag-prod.pages.dev` loads the login page in a fresh incognito window.

    Pick a fresh email. Recommended format: `<your-email>+phase6-{YYYYMMDDHHMM}@gmail.com` (e.g. `<your-email>+phase6-202605091420@gmail.com`). Gmail's `+suffix` aliases land in the same inbox; each new suffix is a unique address from Supabase's perspective. Note the EXACT email used.

    Step-by-step verification (from CONTEXT.md D-15):

    1. Sign up on the prod CF URL (`https://boardgame-rag-prod.pages.dev`):
       - Click sign-up tab/button.
       - Enter the fresh throwaway email + strong password.
       - Submit.
       - EXPECTED: UI message "check your email to confirm" or similar.
       - Save screenshot: `screenshots/06-04-signup-step1.png`.

    2. Email arrival check:
       - Open the inbox associated with the fresh email.
       - EXPECTED: Confirmation email arrives within ~60 seconds. Subject "Confirm your signup" (default Supabase template).
       - HOST CHECK: hover over the confirmation link (do NOT click yet). URL should be either:
         - `https://boardgame-rag-prod.pages.dev/...` (correct), OR
         - `https://<supabase-project-ref>.supabase.co/auth/v1/verify?token=...&redirect_to=https://boardgame-rag-prod.pages.dev/...` (also correct — Supabase routes through their domain first then redirects to Site URL).
       - FAIL CHECK: if the link goes to `http://localhost:5173` or anything other than prod CF URL → STOP. Site URL config didn't save. Re-check Task 4-1.
       - Save screenshot of email with hover-URL visible: `screenshots/06-04-signup-step2-email.png`.

    3. Click the confirmation link:
       - EXPECTED: browser navigates to a CF Pages URL (`boardgame-rag-prod.pages.dev`), and you see the app logged in.
       - NOT EXPECTED: errors like "redirect_uri not allowed", 422, or landing on a localhost URL.
       - Save screenshot: `screenshots/06-04-signup-step3-confirmed.png`.

    4. Send a chat message (verify auth → SSE → chat works end-to-end):
       - In the now-authenticated session, navigate to chat.
       - Create new thread.
       - Send: "What is Catan?".
       - EXPECTED: streaming response with tokens appearing in real-time.
       - NOT EXPECTED: silent failure (Phase 5 chat empty-state UX backlog item — not a blocker but note if encountered), 401, 403, or no streaming.
       - Save screenshot: `screenshots/06-04-signup-step4-chat.png`.

    5. (Optional) Trigger password reset:
       - Log out.
       - On login page, click "forgot password".
       - Enter the same fresh email.
       - EXPECTED: reset email arrives; reset link also lands on prod (`https://boardgame-rag-prod.pages.dev/...`), NOT localhost.
       - Save screenshot: `screenshots/06-04-signup-step5-reset.png` (optional).

    6. Update 06-04-VERIFICATION.md D-15 section. Replace the placeholder with:

       ```markdown
       ## D-15: Signup E2E from prod CF URL (SEC-01 verification)

       - Date/time: <YYYY-MM-DD HH:MM TZ>
       - Fresh email used: `<your-email>+phase6-XXXXXXXXXX@gmail.com`
       - Step 1 — Signup: [ ] PASS / [ ] FAIL — `screenshots/06-04-signup-step1.png`
       - Step 2 — Email arrival + correct host: [ ] PASS / [ ] FAIL — `screenshots/06-04-signup-step2-email.png`
         - Confirmation link host: `<paste hovered URL>`
       - Step 3 — Click confirms + lands on prod: [ ] PASS / [ ] FAIL — `screenshots/06-04-signup-step3-confirmed.png`
         - Final URL after redirect: `<paste URL>`
       - Step 4 — Chat message succeeds: [ ] PASS / [ ] FAIL — `screenshots/06-04-signup-step4-chat.png`
       - Step 5 — Password reset (optional): [ ] PASS / [ ] FAIL / [ ] SKIPPED
       - Overall: [ ] PASS / [ ] FAIL
       - Notes: <any deviations or issues observed>
       ```

    Resume signal: Type "signup verified" once D-15 steps 1-4 PASS with screenshots in 06-04-VERIFICATION.md. If a step failed, describe which one + symptom + screenshot.
  </action>
  <verify>
    <automated>grep -q "Step 1 — Signup" .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md && grep -q "Step 4 — Chat message succeeds" .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md && test -f .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-signup-step4-chat.png && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Steps 1-4 all show PASS in 06-04-VERIFICATION.md.
    - 4 screenshots saved (step 1, step 2, step 3, step 4) under `screenshots/`.
    - Confirmation link host (recorded in step 2) matches `boardgame-rag-prod.pages.dev` (or routes through `*.supabase.co/auth/v1/verify` with `redirect_to=...pages.dev...`).
    - Chat message in step 4 produces streamed response (proof JWT issued by Supabase is accepted by Fly backend, slowapi limiter doesn't immediately fire on first request, and `:free` model responds).
    - 06-04-VERIFICATION.md D-15 section updated with timestamps + screenshot references + overall PASS.
    - User reports `signup verified` to resume.

    On FAIL of any step:
    - User describes which step failed + paste any error text observed.
    - Common failure modes per RESEARCH Pitfall 7: Site URL change MAY invalidate already-issued tokens — but this is the FIRST signup with the new Site URL so should not apply. If it does, re-verify Task 4-1.
  </acceptance_criteria>
  <done>Fresh-email signup E2E verified end-to-end on prod CF URL: signup, email confirm landing on prod (not localhost), login, and chat all PASS with screenshots. SEC-01 fully verified.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4-3: Configure $0.01 OpenRouter alert in dashboard (D-19, SEC-06 setup)</name>
  <files>.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-openrouter-alert-config.png</files>
  <read_first>
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-CONTEXT.md §D-18, D-19 (lines 74-75) — $0 balance + $0.01 alert + developer email
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-RESEARCH.md §Pattern 6 (lines 278-294) — three-layer cost cap rationale
  </read_first>
  <action>
    CHECKPOINT — Human action required. OpenRouter alert configuration is dashboard-only (no public API for alert thresholds as of phase research date).

    Background: The `:free` model is active on prod (06-03 P02). The $0 balance is the structural cost cap (D-18). The alert is the BACKSTOP that fires the moment any non-zero spend appears — defends against accidental paid-model commits or bugs.

    Steps for the human to perform:

    1. Open the OpenRouter dashboard: `https://openrouter.ai/settings/credits` (or whatever the current path is for billing/alerts — UI may have shifted; navigate from Account Settings).

    2. Locate the Alerts section. Possible labels: "Spend alerts", "Usage alerts", "Notifications".

    3. Add a new alert with:
       - Threshold: `$0.01`
       - Email recipient: `<your-email>` (developer email per user MEMORY.md)
       - Frequency / type: "Email when threshold reached" (one-time OR recurring — either is fine for portfolio)

    4. Save the alert.

    5. Verify current account balance: should be `$0.00` (D-18 — no credits loaded for `:free` operation). If balance is non-zero from prior testing, note the value.

    6. Take a screenshot of the alerts page showing the configured alert with both threshold and email visible. Save:
       `.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-openrouter-alert-config.png`

    7. Update 06-04-VERIFICATION.md D-19 section:

       ```markdown
       ## D-19: OpenRouter $0.01 alert configured (SEC-06 setup)

       - Date/time configured: <YYYY-MM-DD HH:MM TZ>
       - Threshold: `$0.01`
       - Email recipient: `<your-email>`
       - Account balance at config time: `$<value>` (expected $0.00 unless recovering)
       - Screenshot: `screenshots/06-04-openrouter-alert-config.png`
       - Status: [ ] PASS / [ ] FAIL
       - Notes: <any UI quirks observed>
       ```

       Tick PASS once the alert is saved and visible in the dashboard.

    Resume signal: Type "alert configured" once $0.01 alert with developer email is live in OpenRouter dashboard and screenshot is captured. If UI doesn't support alert thresholds, describe what was possible.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-openrouter-alert-config.png && grep -q "D-19:" .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md && grep -q "0\.01" .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - OpenRouter dashboard shows an active alert with threshold = $0.01 and email = <your-email>.
    - Screenshot saved at `screenshots/06-04-openrouter-alert-config.png`.
    - 06-04-VERIFICATION.md D-19 section filled in with PASS.
    - User reports `alert configured` to resume.

    Common failure modes:
    - OpenRouter free-account features may differ from paid. If you cannot create an alert, document what's available + any workaround.
    - If the dashboard UI has changed substantially since this plan was written, navigate by intuition + capture screenshots of what was actually possible.
  </acceptance_criteria>
  <done>OpenRouter $0.01 spend alert configured with developer email; screenshot captured; D-19 section of verification log marked PASS.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4-4: OpenRouter alert delivery test — load $1, swap to paid, send chat, confirm email, revert, drain (D-20, SEC-06 verification)</name>
  <files>.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-d20-step1-balance.png, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-d20-step3-chat.png, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-d20-step4-alert-email.png, .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-d20-step6-balance-final.png</files>
  <read_first>
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-CONTEXT.md §D-20 (lines 76-83) — verbatim 6-step procedure
    - .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-RESEARCH.md §Pattern 6 (line 286-294) — alert delivery test rationale + recommended LLM_MODEL_PAID_OPTION (openai/gpt-4o-mini)
    - 06-04-VERIFICATION.md (after Task 4-3; D-19 section PASS)
    - .env.prod (active line should read `LLM_MODEL=openai/gpt-oss-120b:free` from 06-03 Task 3-2)
  </read_first>
  <action>
    CHECKPOINT — Human action required. PROVE the alert (configured in Task 4-3) actually delivers an email when spend crosses $0.01.

    Requires loading real money temporarily, briefly swapping the deployed Fly secret to a paid model, sending one real chat request, confirming email arrival, then reverting to `:free` and draining the remaining balance. One-time verification per D-20.

    Step 1: Load $1 USD into OpenRouter balance.
    - Open OpenRouter dashboard → Credits / Billing.
    - Add $1 USD via credit card / available payment method.
    - Confirm balance now shows `$1.00`.
    - Save screenshot: `screenshots/06-04-d20-step1-balance.png`. Record timestamp.

    Step 2: Swap LLM_MODEL on Fly to a cheap paid model.
    From your local shell (with `flyctl` installed and authenticated):

    ```bash
    flyctl secrets set LLM_MODEL="openai/gpt-4o-mini" -a boardgame-rag-prod
    until curl -sSf https://boardgame-rag-prod.fly.dev/api/health >/dev/null 2>&1; do sleep 2; done
    echo "health OK"
    ```

    Wait ~30-60s for the auto-restart cycle. Record timestamp of secret change.

    Step 3: Send a single chat message via prod UI.
    - Open `https://boardgame-rag-prod.pages.dev` in browser, log in (use `ragtest1@gmail.com`/`testpass123` or fresh user from Task 4-2).
    - Create a new thread.
    - Send the message: `hi` (single word — minimal token cost). Note: to force the alert quickly, you may need to send 10-20 short messages OR send one slightly longer message asking for a paragraph. Pragmatic: ask for "Write a 500-word essay about the history of Catan." This generates enough output tokens to cross $0.01 in one shot.
    - Wait for streaming response.
    - Save screenshot: `screenshots/06-04-d20-step3-chat.png`. Record timestamp.

    Step 4: Wait for alert email.
    - Check the developer inbox (`<your-email>`) every minute.
    - EXPECTED: Alert email from OpenRouter arrives within ~5 minutes. Subject typically "Spend alert" or similar.
    - If after 15 minutes no email, check OpenRouter dashboard usage page to confirm spend actually exceeded $0.01. If spend < $0.01, send more / longer messages.
    - EXPECTED: Email body references the threshold and the current spend.
    - Save screenshot of the email: `screenshots/06-04-d20-step4-alert-email.png`. Record arrival timestamp.

    Step 5: Revert LLM_MODEL to :free.

    ```bash
    flyctl secrets set LLM_MODEL="openai/gpt-oss-120b:free" -a boardgame-rag-prod
    until curl -sSf https://boardgame-rag-prod.fly.dev/api/health >/dev/null 2>&1; do sleep 2; done
    echo "reverted to :free"
    ```

    Verify the local `.env.prod` file's active line is also `LLM_MODEL=openai/gpt-oss-120b:free` (set in 06-03 Task 3-2; should already match).

    Step 6: Drain remaining balance.
    Two acceptable approaches:
    - Withdraw via OpenRouter dashboard if available (refund to original payment method).
    - Burn by sending more chat requests (requires re-swapping to paid; not recommended — withdraw is cleaner).
    Document final balance. Goal: `$0.00` so paid requests cannot fire.
    Save screenshot: `screenshots/06-04-d20-step6-balance-final.png`.

    Step 7: Update 06-04-VERIFICATION.md D-20 section:

    ```markdown
    ## D-20: OpenRouter alert delivery test (SEC-06 verification)

    - Test date: <YYYY-MM-DD HH:MM TZ>
    - Step 1 — Load $1: [ ] PASS — balance $1.00 confirmed at <time> — `screenshots/06-04-d20-step1-balance.png`
    - Step 2 — Swap LLM_MODEL to openai/gpt-4o-mini: [ ] PASS — secret set at <time>
    - Step 3 — Send chat: [ ] PASS — chat at <time>, response streamed correctly — `screenshots/06-04-d20-step3-chat.png`
    - Step 4 — Alert email received: [ ] PASS — email at <time>, ~<delta>min after threshold crossed — `screenshots/06-04-d20-step4-alert-email.png`
    - Step 5 — Revert to :free: [ ] PASS — secret set at <time>, /api/health 200 confirmed
    - Step 6 — Drain balance: [ ] PASS — final balance $<value> at <time> — `screenshots/06-04-d20-step6-balance-final.png`
    - Overall: [ ] PASS / [ ] FAIL
    - Notes: <any anomalies, e.g. email took >15min, refund delayed, etc.>
    ```

    Resume signal: Type "alert delivered" once D-20 steps 1-6 PASS with all screenshots and email confirmed. If alert email did NOT arrive after spend crossed $0.01, type "alert NOT delivered" + describe what was attempted.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/screenshots/06-04-d20-step4-alert-email.png && grep -q "Step 4 — Alert email received" .planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-VERIFICATION.md && flyctl secrets list -a boardgame-rag-prod 2>/dev/null | grep -qi "LLM_MODEL" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 6 steps show PASS in 06-04-VERIFICATION.md.
    - 4 screenshots saved (step 1 balance, step 3 chat, step 4 alert email, step 6 final balance).
    - Alert email from OpenRouter arrived at `<your-email>` within reasonable time after spend crossed $0.01.
    - `flyctl secrets list -a boardgame-rag-prod` (run after step 5) shows LLM_MODEL was updated TWICE since the start of this task (paid swap + revert).
    - Final OpenRouter balance is $0.00 OR documented as draining-pending (refunds may take 3-5 days).
    - `06-04-VERIFICATION.md` D-20 section is fully filled in with timestamps and screenshot references.

    Failure modes (with mitigation):
    - Alert email never arrives → check OpenRouter alert config still exists; check spam folder; verify spend exceeded threshold; if all checks pass and email still missing, document as FAIL — escalate to summary as "SEC-06 alert delivery NOT confirmed" so Phase 7 observability can pick it up.
    - Cannot withdraw remaining balance (refund disabled / delayed) → drain by burn (not recommended) OR document balance and accept lingering small credit (no functional issue since LLM_MODEL is reverted to :free; even with $0.99 sitting around, paid requests can't fire because env var is :free).
  </acceptance_criteria>
  <done>OpenRouter alert delivery proven end-to-end: $1 loaded → paid model active → spend crossed $0.01 → email arrived at developer inbox → :free reverted → balance drained. All 4 screenshots captured. SEC-06 fully verified.</done>
</task>

</tasks>

<verification>
- [ ] `06-04-VERIFICATION.md` exists and contains 4 sections (D-12/13, D-15, D-19, D-20) all marked PASS.
- [ ] At least 9 screenshots in `screenshots/` subdirectory: supabase URL config, signup steps 1-4, alert config, D-20 steps 1, 3, 4, 6.
- [ ] Final Fly secret `LLM_MODEL` digest matches a `:free` value (NOT the paid one — revert confirmed): `flyctl secrets list -a boardgame-rag-prod | grep LLM_MODEL` shows the most-recent-update timestamp matches the revert (Task 4-4 step 5).
- [ ] OpenRouter dashboard balance is $0.00 OR documented as drain-pending.
- [ ] All 4 manual checkpoints have user-confirmed resume signals.
</verification>

<success_criteria>
- SEC-01 fully verified: prod signup → email confirm → land on prod → log in → chat works (D-15 PASS in verification log).
- SEC-06 fully verified: $0.01 alert configured + delivery tested via paid swap + reverted to :free + balance drained (D-19 + D-20 PASS in verification log).
- All evidence (screenshots, timestamps, fresh email used) recorded in 06-04-VERIFICATION.md.
- Production state at end of plan: LLM_MODEL=:free on Fly, $0 OpenRouter balance, Supabase Auth URLs configured for prod.
- No regression in 06-03 deploy state (smoke still green if re-run).
</success_criteria>

<output>
After completion, create `.planning/phases/06-prod-wiring-auth-cors-rate-limiting-cost-caps/06-04-SUMMARY.md`. Document:
- Path to 06-04-VERIFICATION.md.
- Outcome of each of the 4 checkpoint tasks (PASS / partial / FAIL).
- Total verification time elapsed (Task 4-1 start → Task 4-4 end — useful retrospective signal for future manual phases).
- Any anomalies (alert email delay, dashboard UI quirks, refund pending) for Phase 7 follow-up.
- Confirmation that LLM_MODEL has been reverted to `:free` and current OpenRouter balance.
</output>
