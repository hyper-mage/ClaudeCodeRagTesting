---
phase: 15-options-ui-capstone-demo-gating
plan: 05
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/hooks/useKeyGate.tsx
  - frontend/src/hooks/useKeyGate.test.tsx
  - frontend/src/components/ConfirmDialog.tsx
  - frontend/src/hooks/useKeyStatus.ts
  - frontend/src/pages/ChatPage.tsx
  - frontend/src/components/DefaultModelSelector.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/components/ErrorMessageBubble.tsx
autonomous: true
requirements: [KEY-05]

must_haves:
  truths:
    - "Connected user picks any model: applies immediately, no modal, on both surfaces"
    - "Keyless + demo ON: a free-model pick applies immediately; a paid pick opens the gate modal with the paid body; keyless + demo OFF: ANY pick opens the modal with the demo-OFF body"
    - "[Connect] writes the or_pending_selection stash then launches startOpenRouterConnect(); [Cancel] closes with the selection unchanged (trigger keeps the prior model)"
    - "A keyless settings pick does NOT optimistically call onChange or fire the PUT before the gate decides (Pitfall 7)"
    - "The three non-gate connect launchers (Settings CTA, error-bubble Reconnect) clear any stale stash before launching"
  artifacts:
    - path: "frontend/src/hooks/useKeyGate.tsx"
      provides: "Shared gate hook + modal state (D-04, locked name)"
      exports: ["useKeyGate"]
    - path: "frontend/src/hooks/useKeyGate.test.tsx"
      provides: "Decision-table + stash coverage (RESEARCH Wave-0 gap)"
    - path: "frontend/src/components/ConfirmDialog.tsx"
      provides: "variant 'danger'|'primary' + light shell tokens; dark danger output byte-identical"
  key_links:
    - from: "frontend/src/hooks/useKeyGate.tsx"
      to: "frontend/src/hooks/useKeyStatus.ts"
      via: "reads status.connected + status.demo_enabled from the shared no-poll store"
      pattern: "demo_enabled"
    - from: "useKeyGate [Connect]"
      to: "sessionStorage 'or_pending_selection' + startOpenRouterConnect()"
      via: "stash written immediately before the PKCE launch"
      pattern: "or_pending_selection"
    - from: "DefaultModelSelector.handleSelect"
      to: "useKeyGate.guardedSelect"
      via: "gate decision BEFORE onChange/PUT"
---

<objective>
Build the shared key gate (KEY-05): one `useKeyGate` hook wrapping the apply path of BOTH selection surfaces (thread-header ModelSelector via ChatPage, settings DefaultModelSelector), opening a `primary`-variant ConfirmDialog for keyless picks and stashing the pending selection before launching the existing PKCE flow.

Purpose: D-01 (confirm modal, no instant redirect, no locked rows), D-03 gate branch (free rows fast-path under demo ON — full catalog always rendered per the UI-SPEC resolution), D-04 (identical UX on both surfaces). The gate is UX only — enforcement stays the server's fail-closed resolution (a bypassed gate still cannot spend).
Output: new gate hook + tests, ConfirmDialog variant, demo_enabled on the FE status type, both surfaces wired, stale-stash hygiene at non-gate launchers.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-CONTEXT.md  (D-01..D-04)
@.planning/phases/15-options-ui-capstone-demo-gating/15-PATTERNS.md  (§useKeyGate composite, §ConfirmDialog, §DefaultModelSelector, §ChatPage, §SettingsPage, §sessionStorage or_* family)
@.planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md  (Pattern 1 decision table, Pattern 2 stash lifecycle, Pitfalls 6/7, Open Q1, Assumption A3)
@.planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md  (§Components useKeyGate + ConfirmDialog primary variant + Pending-selection stash; §Copywriting — locked modal strings)

<interfaces>
Stash contract this plan WRITES (consumed by plan 15-03 — do not deviate):
sessionStorage key 'or_pending_selection', JSON { kind: 'thread' | 'default', modelId: string, threadId?: string, returnTo: string } — returnTo '/' for thread picks, '/settings' for default picks.

From plan 15-01 (live): GET /api/keys/status now returns demo_enabled: boolean; the shared useKeyStatus store carries it after this plan's interface extension.

Locked modal copy (${model} = display name name ?? id, the ONLY interpolation):
- heading: `Connect OpenRouter?`  · confirm: `Connect` · cancel: `Cancel`
- paid body (demo ON): `Paid models need your OpenRouter key. Connect to continue — ${model} will be applied when you're back.`
- demo-OFF body: `Chatting needs your OpenRouter key. Connect to continue — ${model} will be applied when you're back.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfirmDialog primary variant + light shell; KeyStatus.demo_enabled</name>
  <files>frontend/src/components/ConfirmDialog.tsx, frontend/src/hooks/useKeyStatus.ts</files>
  <read_first>
    - frontend/src/components/ConfirmDialog.tsx (FULL 62 lines — shell :36, icon :39, confirm button :50-56, Esc :22-28, overlay :31-34)
    - frontend/src/hooks/useKeyStatus.ts (KeyStatus interface :5-9; loadStatus silent-on-error contract :105-120 — must stay untouched)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md §Components "ConfirmDialog — primary variant" (exact token lists)
  </read_first>
  <action>
    ConfirmDialog: add optional prop variant?: 'danger' | 'primary' defaulting to 'danger' (the existing SettingsPage disconnect call site needs ZERO changes). primary: confirm button bg-blue-600 hover:bg-blue-700 text-white; icon KeyRound size 24 text-blue-600. danger keeps AlertTriangle text-red-400 and bg-red-600 hover:bg-red-700. Add light shell tokens with dark: prefixes so today's DARK output stays byte-identical: shell bg-white border-gray-300 dark:bg-gray-900 dark:border-gray-700; heading text-gray-900 dark:text-white; body text-gray-600 dark:text-gray-400; cancel bg-gray-200 hover:bg-gray-300 text-gray-900 dark:bg-gray-700 dark:hover:bg-gray-600 dark:text-white. Esc handler and overlay-click cancel untouched. The gate modal is NON-destructive — never red-600/AlertTriangle on the primary variant.

    useKeyStatus.ts: KeyStatus interface gains demo_enabled?: boolean (optional — while status is null/loading it is undefined, which downstream render conditions treat as false → no flash). NO other change: silent-on-error, in-flight dedup, broadcast-refresh machinery byte-identical.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test</automated>
  </verify>
  <done>Variant prop compiles with default 'danger'; existing dialogs render unchanged in dark mode; KeyStatus carries demo_enabled.</done>
  <acceptance_criteria>
    - ConfirmDialog.tsx contains "variant" prop with 'danger' | 'primary' union and default 'danger'
    - grep bg-red-600 frontend/src/components/ConfirmDialog.tsx still returns a match (danger path intact); grep bg-blue-600 returns a match (primary path)
    - Shell class string contains both bg-white and dark:bg-gray-900
    - useKeyStatus.ts KeyStatus interface contains demo_enabled?: boolean; loadStatus function body unchanged (git diff limited to the interface)
    - npm run build and full FE suite exit 0 (no call-site regressions)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useKeyGate hook + decision-table tests</name>
  <files>frontend/src/hooks/useKeyGate.tsx, frontend/src/hooks/useKeyGate.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useKeyStatus.ts (public hook surface the gate reads)
    - frontend/src/pages/SettingsPage.tsx lines 27, 177-186 (ConfirmDialog state-management analog)
    - frontend/src/lib/pkce.ts lines 34-45 (startOpenRouterConnect — called as-is; the stash sits beside its sessionStorage writes)
    - frontend/src/hooks/useChat.test.tsx lines 1-19 (renderHook + module-mock analog for hook tests)
  </read_first>
  <behavior>
    - connected → onApply called immediately, no modal
    - status null (unresolved) → onApply immediately (A3: no flash-gate; server stays fail-closed)
    - keyless + demo_enabled + m.is_free === true → onApply immediately (D-03 free fast-path)
    - keyless + demo_enabled + paid model → modal opens with the paid body; onApply NOT called
    - keyless + demo_enabled + modelId not found in catalog → treated as paid (modal)
    - keyless + !demo_enabled + ANY model (free or paid) → modal with the demo-OFF body
    - modelId null (extraOption clear row) → onApply immediately, NEVER gated (Open Q1 resolution: clearing a pin needs no key; send stays fail-closed server-side)
    - [Connect] → sessionStorage or_pending_selection written with the exact locked JSON (kind/modelId/threadId?/returnTo) THEN startOpenRouterConnect() called
    - [Cancel] → modal closes, onApply never called, no stash written
  </behavior>
  <action>
    Create frontend/src/hooks/useKeyGate.tsx (tsx — it returns modal JSX). Signature: useKeyGate(opts: { kind: 'thread' | 'default', threadId?: string | null, models: ModelResponse[], onApply: (modelId: string | null) => void }) returning { guardedSelect: (modelId: string | null) => void, gateModal: ReactNode }. guardedSelect implements the decision table above; the model row is looked up in opts.models by id (RESEARCH Pattern 1 option (b) — the onSelect signature stays string | null; both callers already hold the catalog). is_free is read from the catalog row verbatim, never recomputed. Internal state: pending modelId + open flag. gateModal renders ConfirmDialog variant="primary" with heading `Connect OpenRouter?`, confirmLabel `Connect`, cancelLabel `Cancel`, and the locked paid / demo-OFF body chosen by the branch, interpolating ONLY the display name (name ?? id). onConfirm: write sessionStorage.setItem('or_pending_selection', JSON.stringify({ kind, modelId, ...(kind === 'thread' && threadId ? { threadId } : {}), returnTo: kind === 'thread' ? '/' : '/settings' })) then call startOpenRouterConnect(). onCancel: close, clear pending, no side effects. Import ModelResponse type from ModelSelector (or its shared type location — check the actual export in read_first). Named export per hooks convention (export function useKeyGate).

    Create useKeyGate.test.tsx: renderHook with a host component rendering gateModal; mock '../lib/pkce' (spy startOpenRouterConnect) and mock '../hooks/useKeyStatus' returning a configurable {status, loading} per test. Cover every behavior row; assert the stash JSON deep-equals the locked shape; sessionStorage cleared between tests.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useKeyGate.test.tsx && npm run lint</automated>
  </verify>
  <done>Hook implements the full locked decision table; all tests green; stash write byte-matches the contract plan 15-03 consumes.</done>
  <acceptance_criteria>
    - useKeyGate.test.tsx covers all 9 behavior rows and exits 0
    - useKeyGate.tsx contains the literal strings 'Connect OpenRouter?' and 'or_pending_selection'
    - The stash test asserts JSON.parse(sessionStorage.getItem('or_pending_selection')) deep-equals {kind:'thread', modelId:<id>, threadId:<tid>, returnTo:'/'} for the thread case and {kind:'default', modelId:<id>, returnTo:'/settings'} for the default case
    - startOpenRouterConnect asserted called AFTER the stash write ([Connect] test) and never called on [Cancel]
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire both surfaces through the gate + stale-stash clears at non-gate launchers</name>
  <files>frontend/src/pages/ChatPage.tsx, frontend/src/components/DefaultModelSelector.tsx, frontend/src/pages/SettingsPage.tsx, frontend/src/components/ErrorMessageBubble.tsx</files>
  <read_first>
    - frontend/src/pages/ChatPage.tsx (handleThreadModelChange :92-109 — becomes the gate's onApply; models catalog held at :56-64; where the handler is passed down to ChatContainer)
    - frontend/src/components/DefaultModelSelector.tsx (handleSelect :30-40 — the optimistic onChange + fire-and-forget PUT the gate must intercept BEFORE; confirm the models prop it receives)
    - frontend/src/pages/SettingsPage.tsx (handleConnect :59-61)
    - frontend/src/components/ErrorMessageBubble.tsx (the [Reconnect] onClick around :117-127 — third non-gate launcher; do NOT touch demoEligible/onUseDemo props or the [Use demo] button — plan 15-07 owns that wiring)
  </read_first>
  <action>
    ChatPage: instantiate the gate — const { guardedSelect, gateModal } = useKeyGate({ kind: 'thread', threadId: activeThreadId, models, onApply: <the existing handleThreadModelChange body> }). Pass guardedSelect wherever handleThreadModelChange was previously passed down (prop signature unchanged: (modelId: string | null) => void — ChatContainer is a pass-through and needs NO edit in this plan). Render {gateModal} in the ChatPage JSX. The clear row (null) flows straight through per the gate's built-in bypass.

    DefaultModelSelector: route handleSelect through its own useKeyGate({ kind: 'default', models, onApply }) where onApply contains the CURRENT handleSelect body (null-guard, onChange?.(modelId), fire-and-forget PUT {default_model}). The gate decision runs BEFORE onChange/PUT — a keyless gated pick must leave the trigger showing the prior model and fire no PUT (Pitfall 7). Render the gateModal beside the selector. If DefaultModelSelector does not already receive the catalog as a prop, thread it from SettingsPage (which fetches models at :36-44).

    Stale-stash hygiene (Pitfall 6 per-call-site table — useKeyGate is the ONLY writer): prepend sessionStorage.removeItem('or_pending_selection') to (a) SettingsPage.handleConnect before startOpenRouterConnect(), and (b) the ErrorMessageBubble [Reconnect] onClick before its launch. The OAuthCallbackPage Retry PRESERVES the stash — that file is owned by plan 15-03; make no change to it here.
  </action>
  <verify>
    <automated>cd frontend && npm run test && npm run build && npm run lint</automated>
  </verify>
  <done>Both surfaces gate identically through one hook; keyless settings picks cannot half-apply; stale stashes cannot leak onto plain connects.</done>
  <acceptance_criteria>
    - grep guardedSelect frontend/src/pages/ChatPage.tsx and frontend/src/components/DefaultModelSelector.tsx each return >= 1 match
    - grep -c "removeItem('or_pending_selection')" frontend/src/pages/SettingsPage.tsx returns 1 and frontend/src/components/ErrorMessageBubble.tsx returns 1
    - DefaultModelSelector: the PUT call and onChange are reachable ONLY via the gate's onApply (no direct call in handleSelect outside it)
    - Full FE suite + build + lint exit 0; existing SettingsPage disconnect-dialog tests unaffected
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser gate → server resolution | gate is UX; the server re-validates key/model at send time |
| sessionStorage stash → OAuth round-trip | selection intent persists across a full-page redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-17 | Elevation of privilege | bypassed/spoofed gate | accept | gate is deliberately UX-only; server fail-closed resolution + plan 15-02 free-guard are the enforcement (a bypassed gate cannot spend) |
| T-15-18 | Tampering | stale stash auto-applying on unrelated connects | mitigate | single-writer rule (useKeyGate only) + removeItem at Settings CTA and bubble Reconnect; callback Retry preserves (locked table) |
| T-15-19 | Information disclosure | modal copy | mitigate | locked strings; ${model} display name is the ONLY interpolation; never a caught error or sk-or- fragment (SEC-01) |
| T-15-20 | Session | stash storage choice | mitigate | sessionStorage only (tab-scoped, Phase-10 semantics); contents carry zero secret material; localStorage forbidden |
</threat_model>

<verification>
- cd frontend && npx vitest run src/hooks/useKeyGate.test.tsx → green (full decision table)
- npm run test && npm run build && npm run lint → green
- Manual spot: dark-mode disconnect dialog visually unchanged (danger variant default)
</verification>

<success_criteria>
- KEY-05 trigger path complete: keyless paid pick → modal → Connect → PKCE launch with the pending selection stashed
- No keyless UX divergence between chat and settings surfaces (one hook, one modal)
- No disabled/locked rows anywhere; Cancel always leaves the selection unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-05-SUMMARY.md`
</output>
