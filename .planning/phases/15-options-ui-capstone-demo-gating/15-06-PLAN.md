---
phase: 15-options-ui-capstone-demo-gating
plan: 06
type: execute
wave: 3
depends_on: ["15-01", "15-04"]
files_modified:
  - frontend/src/components/ModelSelector.tsx
  - frontend/src/components/ModelSelector.test.tsx
autonomous: true
requirements: [MODEL-08]

must_haves:
  truths:
    - "Every catalog row (never the extraOption row) shows an always-visible star; clicking it toggles the favorite WITHOUT selecting the row or closing the dropdown"
    - "Shift+Enter toggles favorite on the active row while plain Enter still selects"
    - "A toggle optimistically updates local state and fires a fire-and-forget PUT /api/preferences {favorite_models} whole-array replace — no toast, no revert on failure"
    - "Starring a model makes it appear in the Favorites section during the same open session; favorites persist across remount via the mount GET"
  artifacts:
    - path: "frontend/src/components/ModelSelector.tsx"
      provides: "Favorite star sub-element + optimistic persistence (MODEL-08, D-05)"
      contains: "favorite_models"
    - path: "frontend/src/components/ModelSelector.test.tsx"
      provides: "Star toggle / no-close / Shift+Enter / PUT-payload coverage"
  key_links:
    - from: "star onClick"
      to: "favorites state + PUT /api/preferences"
      via: "stopPropagation → optimistic set → void apiFetch PUT .catch(() => {})"
      pattern: "stopPropagation"
    - from: "favorites state"
      to: "Favorites section"
      via: "intersection with catalog re-derived per render"
---

<objective>
Complete MODEL-08: the favorite-star affordance on every picker row, persisting to `user_preferences.favorite_models` through the existing preferences PUT with the house-style optimistic fire-and-forget pattern.

Purpose: plan 15-04 shipped the Favorites section reading favorites at mount; this plan makes favorites writable. Both picker surfaces get the star automatically (one shared component). Kept as its own wave-3 plan because it shares ModelSelector.tsx with plan 15-04.
Output: star sub-element, toggle + PUT, Shift+Enter keyboard path, tests.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-CONTEXT.md  (D-05)
@.planning/phases/15-options-ui-capstone-demo-gating/15-PATTERNS.md  (§ModelSelector favorite star, §Optimistic fire-and-forget PUT)
@.planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md  (§Components "ModelSelector — favorite star", §Copywriting star aria-labels, §Color favorited star)

<interfaces>
From plan 15-01 (live): PUT /api/preferences accepts {favorite_models: string[]} — whole-array replace, partial upsert (a favorites-only PUT never clobbers theme/default_model).
From plan 15-04 (in place): favorites useState<string[]> seeded by mount GET; Favorites section renders the intersection, hidden when empty; navigable-array keyboard machinery on the search input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Star sub-element + optimistic toggle + Shift+Enter</name>
  <files>frontend/src/components/ModelSelector.tsx</files>
  <read_first>
    - frontend/src/components/ModelSelector.tsx (post-15-04 state: row render, navigable array, input keydown handler, favorites state + mount GET)
    - frontend/src/components/DefaultModelSelector.tsx lines 30-40 (the fire-and-forget PUT house-style excerpt to mirror: void apiFetch(...).catch(() => {}), never block, never revert)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md §Components "ModelSelector — favorite star" (exact classes and behavior)
  </read_first>
  <behavior>
    - star click toggles favorite; dropdown stays OPEN and onSelect is NOT called
    - Shift+Enter on the input toggles favorite on the ACTIVE row; plain Enter still selects
    - toggle fires PUT /api/preferences with body {favorite_models: <complete new array>} — no other keys in the body
    - PUT rejection does not revert local state and shows no toast
    - aria-label flips between `Add ${label} to favorites` and `Remove ${label} from favorites`
    - extraOption row has no star
  </behavior>
  <action>
    Add the star button to every catalog option row in every section AND in flat search results, never on the extraOption row: button absolute right-0 inset-y-0 w-11 flex items-center justify-center, tabIndex={-1} (mouse/touch + Shift+Enter reach it; never a tab stop), lucide Star size 16. Favorited: text-blue-600 fill-blue-600. Unfavorited: outline text-gray-400 dark:text-gray-500. Always visible (no hover-gating — touch parity). Add focus-visible ring per the existing focus:ring-2 focus:ring-blue-500 convention. Row content gains pr-12 clearance. onClick: e.stopPropagation() then toggleFavorite(m.id) — must not select, must not close. Star state is derived from the favorites array per model id, so every duplicate section instance renders identically.

    toggleFavorite(id): compute next = favorites includes id ? without id : [...favorites, id]; setFavorites(next) optimistically; then void apiFetch('/api/preferences', { method: 'PUT', body: JSON.stringify({ favorite_models: next }) }).catch(() => {}) — whole-array replace, silent, no revert, NO toast (house style per DefaultModelSelector).

    Keyboard: in the input's onKeyDown, add the Shift+Enter branch BEFORE the plain-Enter select branch — when e.shiftKey && e.key === 'Enter', preventDefault and toggleFavorite on the active navigable row (skip when the active row is the extraOption). aria-labels per the locked copy with ${label} = name ?? id.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ModelSelector.test.tsx && npm run lint</automated>
  </verify>
  <done>Star renders on all catalog rows in both themes; toggle is optimistic, silent, whole-array; Shift+Enter path live.</done>
  <acceptance_criteria>
    - ModelSelector.tsx contains fill-blue-600 (favorited) and tabIndex={-1} on the star button, and stopPropagation in its click handler
    - grep "pr-12" frontend/src/components/ModelSelector.tsx returns >= 1
    - The PUT body is exactly {favorite_models: <array>} (no default_model/theme keys)
    - Shift+Enter branch precedes and does not break plain-Enter selection
    - Existing 15-04 tests still green (npx vitest run on the file exits 0)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Star behavior tests + full regression sweep</name>
  <files>frontend/src/components/ModelSelector.test.tsx</files>
  <read_first>
    - frontend/src/components/ModelSelector.test.tsx (post-15-04 state: per-URL apiFetch dispatch, section fixtures)
    - frontend/src/test/utils.tsx (mock helpers)
  </read_first>
  <action>
    Extend ModelSelector.test.tsx: (1) star click on a row toggles — assert the PUT was called with the exact whole-array payload, the listbox is still in the document (not closed), and the onSelect spy was NOT called; (2) starring adds the model to the Favorites section in the same open session (section header appears; row present under it); (3) un-starring the only favorite hides the Favorites section; (4) PUT rejection: mock the PUT to reject — star stays visually favorited (no revert) and no toast text appears; (5) Shift+Enter on the active row toggles (assert PUT payload) while plain Enter selects; (6) aria-label flips between the two locked strings; (7) extraOption row renders no star button. Then run the FULL frontend suite + build to confirm zero regressions across 15-04's search/sections and the untouched surfaces.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ModelSelector.test.tsx && npm run test && npm run build && npm run lint</automated>
  </verify>
  <done>All star tests green; full FE suite, build, lint green.</done>
  <acceptance_criteria>
    - ModelSelector.test.tsx contains >= 7 new star-behavior tests matching the list above
    - A test asserts the PUT call body deep-equals {favorite_models: [...]} with the complete new array
    - A test asserts the dropdown remains open and onSelect uncalled after a star click
    - npm run test, npm run build, npm run lint all exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PUT /api/preferences | client writes the favorites array for the JWT-bound user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-21 | Tampering | cross-user favorites write | mitigate | server binds user_id from JWT sub (plan 15-01 / existing preferences.py pattern); own-row RLS backstop — FE sends only the array |
| T-15-22 | Denial of service | unbounded array from a hostile client | mitigate | server-side Pydantic max_length=200 (plan 15-01); UI can only add catalog slugs |
| T-15-23 | Information disclosure | favorites content | accept | model slugs are non-secret; no PII; silent failure path logs nothing sensitive |
</threat_model>

<verification>
- cd frontend && npx vitest run src/components/ModelSelector.test.tsx → green
- npm run test && npm run build && npm run lint → green
- Live spot-check (dev): star a model on the settings surface, reload, star persists and the model sits in Favorites on the chat surface too (favorites are global)
</verification>

<success_criteria>
- MODEL-08 closed: favoriting pins models to the top section, persisted per-user, identical on both surfaces
- House style held: optimistic, silent, fire-and-forget, whole-array replace
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-06-SUMMARY.md`
</output>
