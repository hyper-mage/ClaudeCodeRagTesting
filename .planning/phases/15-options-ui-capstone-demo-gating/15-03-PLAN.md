---
phase: 15-options-ui-capstone-demo-gating
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/OAuthCallbackPage.tsx
  - frontend/src/pages/OAuthCallbackPage.test.tsx
autonomous: true
requirements: [KEY-05]

must_haves:
  truths:
    - "After a successful exchange with a pending selection, the stash is removed FIRST, the selection applies (thread PATCH / prefs PUT), the combined toast fires (NOT 'OpenRouter connected.'), and navigation goes to the allowlisted returnTo"
    - "Apply failure clears the stash, fires the locked warning toast, and STILL navigates — the failure screen never renders for an apply error"
    - "No stash (or unparseable stash) → legacy behavior unchanged: 'OpenRouter connected.' toast → /settings"
    - "'Back to settings' on the failure screen clears the stash; Retry preserves it"
  artifacts:
    - path: "frontend/src/pages/OAuthCallbackPage.tsx"
      provides: "One-shot pending-selection resume (D-02)"
      contains: "or_pending_selection"
    - path: "frontend/src/pages/OAuthCallbackPage.test.tsx"
      provides: "Resume lifecycle coverage (RESEARCH Wave-0 gap)"
      min_lines: 60
  key_links:
    - from: "sessionStorage or_pending_selection"
      to: "PATCH /api/threads/{id} | PUT /api/preferences"
      via: "removeItem FIRST, then apply (one-shot even if apply throws)"
      pattern: "removeItem\\('or_pending_selection'\\)"
    - from: "pending.returnTo"
      to: "navigate()"
      via: "allowlist {'/', '/settings'} at read time"
---

<objective>
Implement the D-02 auto-apply resume: after a successful OpenRouter exchange, `OAuthCallbackPage` consumes the `or_pending_selection` stash one-shot, applies the user's original pick (thread PATCH or default PUT), fires the locked combined toast, and navigates to the originating surface.

Purpose: the user's original intent (the model they picked before being sent to OAuth) completes without re-picking. The stash CONTRACT is locked in the UI-SPEC (key name `or_pending_selection`, JSON shape `{kind, modelId, threadId?, returnTo}`) — plan 15-05 writes it; this plan consumes it. The two plans share only that string contract, no files, so they run independently.
Output: extended callback page + the new page test file.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-CONTEXT.md  (D-02)
@.planning/phases/15-options-ui-capstone-demo-gating/15-PATTERNS.md  (§OAuthCallbackPage, §FE page test)
@.planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md  (Pattern 2 lifecycle, Pitfall 6, Code Examples "OAuthCallbackPage resume")
@.planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md  (§Copywriting — locked toast strings; §Components "Pending-selection stash" + "OAuthCallbackPage — resume")

<interfaces>
Locked stash contract (written by useKeyGate in plan 15-05 — consume exactly this):

sessionStorage key: 'or_pending_selection'
JSON: { kind: 'thread' | 'default', modelId: string, threadId?: string, returnTo: string }
returnTo values: '/' (thread picks) or '/settings' (default picks)

Locked toast strings (the ONLY interpolation is ${label} = model display name, name ?? id):
- thread apply success: `Connected — ${label} is set for this chat.` (success variant)
- default apply success: `Connected — ${label} is now your default model.` (success variant)
- apply failure: `Connected, but your model pick didn't apply — pick it again.` (warning variant)
- no stash (legacy, unchanged): `OpenRouter connected.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — create OAuthCallbackPage.test.tsx covering the full resume lifecycle</name>
  <files>frontend/src/pages/OAuthCallbackPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/OAuthCallbackPage.tsx (FULL 94 lines — ranRef StrictMode guard :23-27, exchange success path :40-47, Retry :74-78, Back to settings :81-87, how code/state are read from the URL)
    - frontend/src/pages/ChatPage.test.tsx (analog: MemoryRouter wrap, vi.mock('../lib/api'), makeAuthMock, renderWithProviders usage)
    - frontend/src/test/utils.tsx (renderWithProviders, makeApiMock, makeAuthMock helpers)
  </read_first>
  <behavior>
    - success + thread stash: seed sessionStorage or_pkce_verifier/state + or_pending_selection {kind:'thread', modelId, threadId:'t1', returnTo:'/'} and matching URL params → exchange POST fires, PATCH /api/threads/t1 with {model}, toast text 'Connected — … is set for this chat.', stash removed, navigate to '/'
    - success + default stash: {kind:'default', modelId, returnTo:'/settings'} → PUT /api/preferences {default_model}, toast 'Connected — … is now your default model.', navigate '/settings'
    - stash removed BEFORE apply: make the PATCH reject and assert sessionStorage no longer has the key AND warning toast 'Connected, but your model pick didn't apply — pick it again.' AND navigation still happened AND the failure screen did not render
    - no stash: legacy behavior — toast 'OpenRouter connected.', navigate '/settings'
    - malformed stash JSON: behaves as no-stash legacy path (no crash)
    - returnTo not in allowlist (e.g. 'https://evil.example' or '/other'): navigates to '/settings'
    - failure screen 'Back to settings' click removes or_pending_selection; Retry does NOT remove it
  </behavior>
  <action>
    Create frontend/src/pages/OAuthCallbackPage.test.tsx following the ChatPage.test.tsx analog: vi.mock('../lib/api') via makeApiMock or inline {apiFetch: vi.fn()}, vi.mock('../contexts/AuthContext') via makeAuthMock, vi.mock('../lib/pkce') exposing a spyable startOpenRouterConnect. Render inside MemoryRouter with initialEntries carrying the callback path + query (?code=...&state=...); seed sessionStorage keys in beforeEach and clear in afterEach. Assert navigation via the rendered destination or by mocking react-router-dom's useNavigate (follow whichever pattern ChatPage.test.tsx uses; if none, mock useNavigate). Toast text asserted via screen.findByText on the rendered toast (renderWithProviders includes the toast provider). For the label: mock GET /api/models to return a catalog row so display-name resolution is covered in one test, and have another test where /api/models rejects → toast falls back to the raw modelId. Run the file — all resume tests must FAIL (resume not implemented), while any test of current legacy behavior passes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/OAuthCallbackPage.test.tsx 2>&1 | grep -E "failed|passed"; cd frontend && npx vitest run src/pages/OAuthCallbackPage.test.tsx > /dev/null 2>&1; test $? -ne 0</automated>
  </verify>
  <done>Test file exists with the full lifecycle matrix; resume tests RED, legacy-behavior test green.</done>
  <acceptance_criteria>
    - frontend/src/pages/OAuthCallbackPage.test.tsx exists with >= 7 test cases matching the behavior list
    - vitest run on the file exits non-zero (RED) with failures ONLY in resume-behavior tests
    - Tests assert the exact locked toast strings (copy-paste from the interfaces block, including the em-dash)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement the one-shot resume in OAuthCallbackPage</name>
  <files>frontend/src/pages/OAuthCallbackPage.tsx</files>
  <read_first>
    - frontend/src/pages/OAuthCallbackPage.tsx (current success path :40-47 = insertion point; Back-to-settings button :81-87)
    - frontend/src/pages/OAuthCallbackPage.test.tsx (the RED tests define the contract)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md Code Examples "OAuthCallbackPage resume (D-02, one-shot)" (target shape)
  </read_first>
  <action>
    Inside the existing ranRef-guarded effect, after the exchange succeeds and or_pkce_verifier/or_pkce_state are removed (:40-45), replace the fixed toast+navigate (:46-47) with the resume: read raw = sessionStorage.getItem('or_pending_selection'). If present: removeItem FIRST (one-shot even if apply throws — Pitfall 6), JSON.parse inside try/catch (parse failure → fall through to legacy path). Define a local PendingSelection interface {kind: 'thread' | 'default', modelId: string, threadId?: string, returnTo: string}. Resolve the toast label best-effort: try GET /api/models via apiFetch, find the row by id, label = name ?? id; ANY failure → label = pending.modelId (silent). Apply: kind 'thread' with threadId → await apiFetch PATCH /api/threads/${threadId} body {model: modelId}, success toast `Connected — ${label} is set for this chat.`; otherwise → await apiFetch PUT /api/preferences body {default_model: modelId}, success toast `Connected — ${label} is now your default model.`. Catch around the apply only: warning toast `Connected, but your model pick didn't apply — pick it again.` — NEVER the failure screen (the connection succeeded). Then navigate with replace:true to pending.returnTo constrained to the allowlist ['/', '/settings'] — anything else navigates '/settings' (stash-injected navigation guard). No stash / unparseable → existing legacy behavior byte-identical ('OpenRouter connected.' → '/settings').

    Add sessionStorage.removeItem('or_pending_selection') to the 'Back to settings' onClick (:81-87). Leave Retry (:74-78) untouched — startOpenRouterConnect rewrites only verifier/state so the stash survives naturally (locked). SEC-01: no caught error, HTTP body, or sk-or- fragment ever reaches copy — the locked strings above are the only new UI text.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/OAuthCallbackPage.test.tsx && npm run build && npm run lint</automated>
  </verify>
  <done>All lifecycle tests green; build + lint clean; legacy no-stash path unchanged.</done>
  <acceptance_criteria>
    - vitest run src/pages/OAuthCallbackPage.test.tsx exits 0
    - OAuthCallbackPage.tsx: removeItem('or_pending_selection') appears BEFORE the PATCH/PUT apply call in source order within the resume block
    - The Back-to-settings handler contains removeItem('or_pending_selection'); the Retry handler does not
    - navigate target is derived through an allowlist check against '/' and '/settings'
    - npm run build and npm run lint exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage → navigation/apply | stash contents (written client-side) drive navigation and API writes |
| OAuth redirect → callback page | URL params cross from the provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | pending.returnTo | mitigate | allowlist {'/', '/settings'} at read time; consumed via SPA navigate(), never window.location |
| T-15-11 | Tampering | double/stale apply | mitigate | one-shot removeItem-first; ranRef StrictMode guard already prevents double-run; Back-to-settings clears |
| T-15-12 | Information disclosure | new toasts | mitigate | locked strings; ${label} display name is the ONLY interpolation; apply errors never interpolated (SEC-01) |
| T-15-13 | Spoofing | stash carries no secrets | accept | contents are {kind, modelId, threadId, returnTo} — zero secret material; CSRF state validation on the exchange is existing Phase-10 code, untouched |
</threat_model>

<verification>
- cd frontend && npx vitest run src/pages/OAuthCallbackPage.test.tsx → green
- npm run test (full FE suite) → no regressions; npm run build && npm run lint → clean
</verification>

<success_criteria>
- Pending selection applies exactly once after connect, with the correct combined toast per surface
- Apply failure degrades to a warning toast + navigation, never a failure screen
- Legacy connect flow (no stash) byte-identical
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-03-SUMMARY.md`
</output>
