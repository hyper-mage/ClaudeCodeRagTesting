---
phase: 15-options-ui-capstone-demo-gating
plan: 07
type: execute
wave: 3
depends_on: ["15-02", "15-05"]
files_modified:
  - frontend/src/hooks/useChat.ts
  - frontend/src/hooks/useChat.test.tsx
  - frontend/src/pages/ChatPage.tsx
  - frontend/src/components/ChatContainer.tsx
  - frontend/src/components/ChatContainer.test.tsx
autonomous: true
requirements: [DEMO-02, DEMO-01]

must_haves:
  truths:
    - "The demo banner renders with the LOCKED Phase-11 copy whenever (!connected && demo_enabled) or after a mode:'demo' done event — non-dismissible, no interactive children, first shrink-0 child of the chat pane, both themes"
    - "The 403 (forbidden) error bubble shows [Use demo] when demo_enabled is true; clicking retries the last user turn with use_demo:true in the body"
    - "Normal sends carry NO use_demo key; only the demo retry adds use_demo:true"
    - "While key status is still loading (null), the banner does not flash"
  artifacts:
    - path: "frontend/src/hooks/useChat.ts"
      provides: "lastTurnWasDemo signal read + useDemo send option + demo retry"
      contains: "lastTurnWasDemo"
    - path: "frontend/src/components/ChatContainer.tsx"
      provides: "Demo banner (D-10) + live demoEligible/onUseDemo wiring (D-11)"
      contains: "Demo mode — a free model is in use"
  key_links:
    - from: "useChat done-event branch"
      to: "parsed.mode === 'demo'"
      via: "setLastTurnWasDemo(true) on the done event"
      pattern: "mode.*demo"
    - from: "ChatPage"
      to: "ChatContainer"
      via: "lastTurnWasDemo + onUseDemo props"
    - from: "ChatContainer ErrorMessageBubble call site"
      to: "useKeyStatus.demo_enabled"
      via: "demoEligible={Boolean(status?.demo_enabled)}"
      pattern: "demoEligible"
---

<objective>
Surface demo mode to the user: read the Phase-11 `mode:"demo"` SSE signal (emitted since Phase 11, unread until now), render the non-dismissible amber banner (DEMO-02, D-10), and bring the dead `demoEligible`/`onUseDemo` props to life for the [Use demo] 403 recovery (D-11).

Purpose: DEMO-02 is the user-facing half of the demo rollout — the banner is the honesty contract ("a free model is in use"). The FE only renders server signals; the retry override is honored server-side only when the flag is ON (plan 15-02).
Output: useChat demo state + retry, ChatContainer banner + wiring, tests.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-CONTEXT.md  (D-10, D-11)
@.planning/phases/15-options-ui-capstone-demo-gating/15-PATTERNS.md  (§useChat, §ChatContainer, §FE hook tests)
@.planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md  (Pattern 5 banner condition, Pitfall 11, Open Q2, Code Examples "Demo banner" + "useChat demo signal")
@.planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md  (§Components "Demo banner" — locked classes; §Copywriting — LOCKED banner sentence; §Color semantic amber)

<interfaces>
From plan 15-01 (live): useKeyStatus status carries demo_enabled?: boolean.
From plan 15-02 (live): POST message body {use_demo: true} short-circuits to the demo branch when the flag is ON; inert when OFF. Done event carries mode:"demo" on demo turns (Phase 11, already emitted).
From plan 15-05 (in place): ChatPage renders the gate; this plan adds sibling props — no gate code is touched.
LOCKED banner sentence (verbatim, do not paraphrase): Demo mode — a free model is in use; it may be slower, less accurate, or temporarily rate-limited (no usage left).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useChat — lastTurnWasDemo signal, useDemo send option, demo retry</name>
  <files>frontend/src/hooks/useChat.ts, frontend/src/hooks/useChat.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useChat.ts (done-event branch :251-261, send body :148-155, retryLastUserMessage :354-362, thread-switch state semantics, hook return object)
    - frontend/src/hooks/useChat.test.tsx (renderHook + mockSSEResponse harness :1-19)
    - frontend/src/test/utils.tsx (mockSSEResponse signature)
  </read_first>
  <behavior>
    - a done event {"message_id":"m1","mode":"demo"} sets lastTurnWasDemo to true
    - a done event without mode leaves lastTurnWasDemo false
    - lastTurnWasDemo resets to false on thread switch (per-hook state follows the existing thread-scoped reset semantics — Open Q2 resolution)
    - sendMessage(content) body is {"content": ...} with NO use_demo key
    - sendMessage(content, {useDemo: true}) body contains "use_demo": true
    - retryWithDemo strips error bubbles and re-sends the last user message with {retry: true, useDemo: true, threadId} (mirrors retryLastUserMessage)
    - retryWithDemo is a no-op while streaming or when no prior user message exists
  </behavior>
  <action>
    useChat.ts: add lastTurnWasDemo boolean state (default false). In the done-event branch (:251-261) add: if (parsed.mode === 'demo') setLastTurnWasDemo(true). Reset it wherever per-thread message state resets on thread switch (alongside the existing loadMessages/thread-change flow — keep the isStreamingRef no-clobber discipline from 999.1 untouched). Extend sendMessage opts with useDemo?: boolean; the POST body becomes JSON.stringify({ content, ...(opts?.useDemo ? { use_demo: true } : {}) }) — the key is ABSENT on normal sends. Add retryWithDemo: useCallback mirroring retryLastUserMessage (:354-362) exactly — guard isStreaming, find last user message, strip role==='error' bubbles, void sendMessage(lastUser.content, { retry: true, useDemo: true, threadId: threadId ?? undefined }). Export lastTurnWasDemo and retryWithDemo from the hook's return object. Do not change any existing SSE parsing or the closure-proof effectiveThreadId pattern.

    Tests: extend useChat.test.tsx per the behavior list using mockSSEResponse(['data: {"text":"hi"}', 'data: {"message_id":"m1","mode":"demo"}']) for the demo-done case; assert body payloads via the apiStream mock's call args.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useChat.test.tsx && npm run lint</automated>
  </verify>
  <done>Hook exposes the demo signal + retry; body shape proven both with and without the override; existing streaming tests untouched and green.</done>
  <acceptance_criteria>
    - useChat.test.tsx exits 0 with >= 5 new tests matching the behavior list
    - useChat.ts return object contains lastTurnWasDemo and retryWithDemo
    - A test asserts the normal-send body has NO use_demo key (JSON.parse of the mock call body lacks the property)
    - A test asserts the retryWithDemo body contains "use_demo": true and strips error bubbles
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ChatContainer demo banner + demoEligible/onUseDemo wiring through ChatPage</name>
  <files>frontend/src/components/ChatContainer.tsx, frontend/src/components/ChatContainer.test.tsx, frontend/src/pages/ChatPage.tsx</files>
  <read_first>
    - frontend/src/components/ChatContainer.tsx (root flex :56-63, thread-header shrink-0 sibling pattern, ErrorMessageBubble call site :113-123 with demoEligible={false})
    - frontend/src/pages/ChatPage.tsx (post-15-05 state: useChat destructuring, ChatContainer props)
    - frontend/src/components/ChatContainer.test.tsx (existing harness + mocks)
    - frontend/src/components/DemoPill.tsx (DIFFERENT "demo" concept — anon sessions; leave untouched, Pitfall 11)
  </read_first>
  <action>
    ChatContainer: call useKeyStatus() locally (shared no-poll store — zero extra fetches) for connected + demo_enabled; accept two new props from ChatPage: lastTurnWasDemo: boolean and onUseDemo: () => void. Render the banner as the FIRST shrink-0 child of the root flex column (:56-63) — ABOVE the thread-header row, full-bleed, present with or without an active thread. Exact element (UI-SPEC locked): div role="status" className "shrink-0 flex items-center gap-2 px-4 py-2 text-xs border-b bg-amber-500/10 border-amber-500/30 text-amber-700 dark:text-amber-300", containing lucide Info size 14 className "text-amber-500 shrink-0" aria-hidden, and a span with the LOCKED sentence verbatim: Demo mode — a free model is in use; it may be slower, less accurate, or temporarily rate-limited (no usage left). NO close button, NO interactive children, NOT a pill (DEMO-02 non-dismissible). Render condition: (!status?.connected && Boolean(status?.demo_enabled)) || lastTurnWasDemo — while status is null both terms are false → no flash. Do NOT touch DemoPill (anon-session badge; both may legitimately show together).

    Wire the dead props (:113-123): demoEligible={Boolean(status?.demo_enabled)} and onUseDemo={onUseDemo}. ErrorMessageBubble itself is untouched — it already renders [Use demo] on type==='forbidden' && demoEligible.

    ChatPage: destructure lastTurnWasDemo and retryWithDemo from useChat; pass lastTurnWasDemo={lastTurnWasDemo} and onUseDemo={retryWithDemo} to ChatContainer. No other ChatPage change (the 15-05 gate code is a sibling — leave it byte-identical).

    Tests (ChatContainer.test.tsx): banner render-condition matrix — (a) keyless + demo_enabled → banner present; (b) connected + no demo turn → absent; (c) connected + lastTurnWasDemo → present; (d) status null → absent; locked copy asserted verbatim; role="status" present; banner contains no button element. [Use demo]: forbidden-type error message + demo_enabled → button visible, click calls the onUseDemo spy; demo_enabled false → button absent. Mock ../hooks/useKeyStatus for status control (module mock, configurable per test).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ChatContainer.test.tsx && npm run test && npm run build && npm run lint</automated>
  </verify>
  <done>Banner live per the locked condition in both themes; [Use demo] wired end-to-end (bubble → retryWithDemo → use_demo:true body → server demo branch); full suite green.</done>
  <acceptance_criteria>
    - ChatContainer.tsx contains role="status" and the exact locked sentence (grep for 'Demo mode — a free model is in use')
    - The banner div is the first shrink-0 child of the root flex column (before the activeThreadId header block in JSX order)
    - grep 'demoEligible={false}' frontend/src/components/ChatContainer.tsx returns 0 matches (dead prop replaced)
    - ChatContainer.test.tsx covers the 4-row render matrix + [Use demo] visibility/click
    - npm run test, npm run build, npm run lint all exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE done event → banner state | server-emitted mode signal drives UI honesty |
| [Use demo] click → POST use_demo | client requests the owner-key demo branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-24 | Elevation of privilege | use_demo from the FE | accept (mitigated upstream) | server honors the override only when demo_fallback_enabled AND free-guards the model (plan 15-02) — the FE flag grants nothing by itself |
| T-15-25 | Information disclosure | banner/bubble copy | mitigate | LOCKED strings only; no error/model-cost interpolation; SSE parse errors never rendered raw (existing taxonomy) |
| T-15-26 | Repudiation | user unaware turn ran on demo | mitigate | that is DEMO-02's purpose — banner guaranteed after any observed mode:"demo" (lastTurnWasDemo latch) |
| T-15-27 | Denial of service | banner layout breaking chat | mitigate | shrink-0 first-child placement preserves flex-1 scroll area (Pitfall 11); asserted in tests |
</threat_model>

<verification>
- cd frontend && npx vitest run src/hooks/useChat.test.tsx src/components/ChatContainer.test.tsx → green
- npm run test && npm run build && npm run lint → green
- Live dev spot (flag ON locally): keyless account sees the banner proactively; a demo turn latches it; chat input still visible (layout intact)
</verification>

<success_criteria>
- DEMO-02 closed: non-dismissible banner with locked copy on the locked condition, both themes
- D-11 closed: [Use demo] live on the 403 bubble, retrying with use_demo:true
- Zero regressions in streaming/error-bubble behavior
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-07-SUMMARY.md`
</output>
