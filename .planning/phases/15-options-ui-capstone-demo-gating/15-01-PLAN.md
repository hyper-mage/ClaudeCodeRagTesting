---
phase: 15-options-ui-capstone-demo-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240301000033_add_favorite_models.sql
  - backend/models/schemas.py
  - backend/routers/preferences.py
  - backend/routers/keys.py
  - backend/tests/test_preferences_api.py
  - backend/tests/test_keys_status.py
autonomous: true
requirements: [MODEL-08, DEMO-01]

must_haves:
  truths:
    - "GET /api/keys/status returns demo_enabled reflecting DEMO_FALLBACK_ENABLED in BOTH the keyless and connected branches"
    - "GET /api/preferences returns favorite_models (default []) and PUT /api/preferences {favorite_models} persists a whole-array replace without clobbering theme or default_model"
    - "POST message bodies accept an optional use_demo boolean (MessageCreate) without 422"
    - "user_preferences.favorite_models column exists in the LIVE dev database (schema pushed, not just authored)"
  artifacts:
    - path: "supabase/migrations/20240301000033_add_favorite_models.sql"
      provides: "Additive TEXT[] favorites column on user_preferences"
      contains: "ADD COLUMN favorite_models TEXT[] NOT NULL DEFAULT '{}'"
    - path: "backend/models/schemas.py"
      provides: "MessageCreate.use_demo, KeyStatusResponse.demo_enabled, PreferencesResponse.favorite_models, PreferencesUpdate.favorite_models"
    - path: "backend/routers/keys.py"
      provides: "demo_enabled in both status() return branches"
    - path: "backend/routers/preferences.py"
      provides: "favorite_models in both selects and both response dicts"
  key_links:
    - from: "backend/routers/keys.py"
      to: "backend/config.py demo_fallback_enabled"
      via: "get_settings().demo_fallback_enabled computed once, included in BOTH return dicts"
      pattern: "demo_enabled"
    - from: "backend/routers/preferences.py"
      to: "user_preferences.favorite_models"
      via: "select string + upsert exclude_unset"
      pattern: "favorite_models"
---

<objective>
Land every backend seam the Phase-15 frontend consumes: migration 033 (favorites column), four additive Pydantic fields, `favorite_models` on the preferences GET/PUT roundtrip, and `demo_enabled` on the key-status endpoint — then push the schema to the LIVE dev database.

Purpose: MODEL-08 persistence (D-05) and DEMO-01 flag exposure (UI-SPEC-resolved smallest seam: ride `GET /api/keys/status`). The keys.py early return at lines 93-94 is a known security-shaped trap — keyless users are exactly the demo audience, and relying on the Pydantic default would hide the flag from them (RESEARCH Pitfall 3).
Output: migration 033 applied to dev, extended schemas/routers, green extended test files.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-CONTEXT.md
@.planning/phases/15-options-ui-capstone-demo-gating/15-PATTERNS.md  (sections: migration, schemas.py, keys.py, preferences.py, BE router tests)
@.planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md  (Pattern 5, Pattern 6, Pitfall 3, Pitfall 12, Code Examples)

<interfaces>
Contracts this plan creates (consumed by FE plans 15-04/05/06/07 — do not deviate):

GET /api/keys/status → { connected: bool, masked_label?: str, connected_at?: str, demo_enabled: bool }
GET /api/preferences → { default_model: str|null, theme: str, favorite_models: string[] }
PUT /api/preferences {favorite_models: string[]} → partial upsert, whole-array replace, echo includes favorite_models
POST /api/threads/{id}/messages body: { content: str, use_demo?: bool }  (use_demo default False)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 033 and the four additive schema fields</name>
  <files>supabase/migrations/20240301000033_add_favorite_models.sql, backend/models/schemas.py</files>
  <read_first>
    - supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql (header-comment style, plain-TEXT non-FK rationale, own-row RLS that covers new columns automatically)
    - backend/models/schemas.py (PreferencesUpdate at ~53-63, PreferencesResponse at ~42-50, KeyStatusResponse at ~122-131, MessageCreate at ~77-78, Field(max_length) precedent at ExplorerFinding ~172-177)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-PATTERNS.md (§migration 033, §schemas.py)
  </read_first>
  <action>
    Create supabase/migrations/20240301000033_add_favorite_models.sql containing exactly one DDL statement: ALTER TABLE user_preferences ADD COLUMN favorite_models TEXT[] NOT NULL DEFAULT '{}'. Header comment mirrors migration 032's style: Phase 15 / MODEL-08 / D-05, additive, no backfill, no destructive change; deliberately NOT a FK to model_cache (same deprecation-tolerance rationale as default_model); existing own-row RLS from migration 032 covers the new column — add NO policies (per D-05 discretion resolved by UI-SPEC: TEXT[] shape).

    Extend backend/models/schemas.py with four additive fields, each with a phase + requirement-ID comment (house convention, see MessageResponse.usage):
    - MessageCreate gains use_demo: bool = False — comment: Phase 15 D-11 [Use demo] retry override; server honors it ONLY when demo_fallback_enabled (fail-closed preserved when OFF).
    - KeyStatusResponse gains demo_enabled: bool = False — comment: Phase 15 DEMO-01; env-driven flag surfaced read-only, never settable via API.
    - PreferencesResponse gains favorite_models: list[str] = [] (use Field(default_factory=list) to avoid a shared mutable default).
    - PreferencesUpdate gains favorite_models: list[str] | None = Field(default=None, max_length=200) — whole-array replace; None default keeps exclude_unset partial-upsert semantics (a theme-only PUT must NOT carry favorite_models); max_length=200 is the RESEARCH Open Q3 abuse bound.
    Do not touch any other model.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -c "from models.schemas import MessageCreate, KeyStatusResponse, PreferencesResponse, PreferencesUpdate; assert MessageCreate(content='x').use_demo is False; assert KeyStatusResponse(connected=False).demo_enabled is False; assert PreferencesResponse().favorite_models == []; assert PreferencesUpdate().model_dump(exclude_unset=True) == {}; print('schemas ok')"</automated>
  </verify>
  <done>Migration file exists with the single ALTER statement; all four schema fields importable with correct defaults; PreferencesUpdate() with no args dumps to {} under exclude_unset.</done>
  <acceptance_criteria>
    - supabase/migrations/20240301000033_add_favorite_models.sql exists and contains "ADD COLUMN favorite_models TEXT[] NOT NULL DEFAULT '{}'"
    - Migration file contains no CREATE POLICY / ALTER POLICY statements
    - The python assert command above exits 0
    - schemas.py contains "use_demo: bool = False" and "demo_enabled: bool = False"
    - PreferencesUpdate.favorite_models declared with max_length=200
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: favorite_models roundtrip in preferences.py + demo_enabled in both keys.py status branches, with tests</name>
  <files>backend/routers/preferences.py, backend/routers/keys.py, backend/tests/test_preferences_api.py, backend/tests/test_keys_status.py</files>
  <read_first>
    - backend/routers/preferences.py (GET select ~42-55, partial upsert ~70-75, PUT echo ~77-89)
    - backend/routers/keys.py (status() at ~82-99 — the early return at 93-94 is the trap branch)
    - backend/tests/test_keys_status.py (TestClient + dependency-override + patch-get_supabase + finally-clear shape at ~25-39)
    - backend/tests/test_preferences_api.py (_mock_db_with_pref_row at ~25-38)
    - backend/tests/test_key_model_resolution.py lines 1-20 (header guidance: monkeypatch.setenv + get_settings.cache_clear for config-touching tests)
  </read_first>
  <behavior>
    - test_status_keyless_carries_demo_enabled_true: no key row + flag ON → {"connected": false, "demo_enabled": true} (pins the keys.py:93-94 trap)
    - test_status_connected_carries_demo_enabled_true: key row + flag ON → connected true, demo_enabled true
    - test_status_demo_enabled_false_by_default: flag unset → demo_enabled false in both branches
    - test_preferences_get_returns_favorites_default_empty: no row → favorite_models == []
    - test_preferences_get_returns_stored_favorites: row with favorite_models → echoed verbatim
    - test_preferences_put_favorites_only_no_clobber: PUT {favorite_models:[...]} → upsert payload contains favorite_models + user_id + updated_at and does NOT contain theme or default_model
    - test_preferences_put_theme_only_preserves_favorites: PUT {theme:"light"} → upsert payload does NOT contain favorite_models (Pitfall 12 regression, both directions)
    - test_preferences_put_echo_includes_favorites: PUT echo response carries favorite_models
  </behavior>
  <action>
    preferences.py (4 touch points): change the GET select string to "default_model, theme, favorite_models"; add "favorite_models": [] to the no-row default dict; add "favorite_models": row.data.get("favorite_models") or [] to the GET return dict; make the identical two changes to the PUT echo select and echo dict. The upsert itself needs no change — model_dump(exclude_unset=True) already carries favorite_models only when the client sent it. user_id stays bound from Depends(get_user_id), never the body.

    keys.py status(): compute demo_enabled = get_settings().demo_fallback_enabled once at the top of the function (get_settings already imported at line 32), then add "demo_enabled": demo_enabled to BOTH return dicts — the keyless early return at :93-94 AND the connected return. Do NOT rely on the Pydantic default for the keyless branch (RESEARCH Pitfall 3 / security requirement: keyless users are the demo audience).

    Tests: extend the two test files per the behavior list, following each file's existing TestClient + dependency_overrides + patch shape. For the flag, either patch routers.keys.get_settings with a MagicMock(demo_fallback_enabled=True) or monkeypatch.setenv("DEMO_FALLBACK_ENABLED", "true") + get_settings.cache_clear() (clear again in teardown). For upsert-payload assertions, capture mock_db.table.return_value.upsert.call_args.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_keys_status.py tests/test_preferences_api.py -x -q</automated>
  </verify>
  <done>Both test files green including all new tests; keys.py carries demo_enabled in both branches; preferences.py roundtrips favorite_models at all 4 touch points; no-clobber proven in both directions.</done>
  <acceptance_criteria>
    - pytest tests/test_keys_status.py tests/test_preferences_api.py exits 0
    - backend/routers/keys.py: grep -v '^\s*#' backend/routers/keys.py | grep -c 'demo_enabled' returns >= 3 (one compute + two dict entries)
    - backend/routers/preferences.py: grep -c 'favorite_models' backend/routers/preferences.py returns >= 4
    - test_keys_status.py contains a test asserting the KEYLESS branch response has demo_enabled true when the flag is on
    - test_preferences_api.py asserts the theme-only upsert payload has no favorite_models key AND the favorites-only payload has no theme/default_model keys
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push migration 033 to the LIVE dev database and probe the column</name>
  <files>supabase/migrations/20240301000033_add_favorite_models.sql</files>
  <read_first>
    - supabase/migrations/ directory listing (confirm 033 is the only unapplied local migration; dev previously applied through 032)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md (§Pattern 6 dev-first note, §Pattern 7 repair contingency)
  </read_first>
  <action>
    Run `supabase db push` from the repo root against the LINKED DEV project (the `.env` project — per project memory .env = dev, .env.prod = prod; prod is EXCLUSIVELY plan 15-08's concern, never push prod here). Non-TTY: set SUPABASE_ACCESS_TOKEN env var if the CLI demands login. Contingency (established Phase-9 pattern): if push tries to replay older migrations and errors with "already exists", run `supabase migration repair --status applied` for the prior range, then re-push. If an interactive confirm prompt cannot be suppressed, stop and surface a checkpoint rather than guessing.

    After push, prove the column is LIVE (build/type checks pass without the push — false-positive trap): run a probe from backend/ with venv python: from database import get_supabase; get_supabase().table("user_preferences").select("favorite_models").limit(1).execute() — succeeds only if the column exists.

    Do NOT run the full backend suite in-task — wave 1 runs plans 15-01/15-02/15-03 in parallel on one working tree; the full-suite regression gate runs at the wave-1 merge (RESEARCH §Validation Architecture sampling).
  </action>
  <verify>
    <automated>supabase migration list 2>&1 | grep 20240301000033 && cd backend && venv/Scripts/python.exe -c "from database import get_supabase; get_supabase().table('user_preferences').select('favorite_models').limit(1).execute(); print('column live')"</automated>
  </verify>
  <done>Migration 033 shows as applied on the dev remote; live column probe succeeds. Full backend suite (green modulo the 2 documented pre-existing test_record_manager.py fixture errors — out of scope, do not fix) runs at the wave-1 merge.</done>
  <acceptance_criteria>
    - `supabase migration list` output shows 20240301000033 with a remote-applied timestamp/marker
    - The live probe prints "column live" and exits 0
    - Wave-1 merge gate (not in-task): full suite 0 new failures vs baseline (only the 2 pre-existing record_manager errors permitted)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/preferences | untrusted body writes favorites for the JWT-bound user |
| client → /api/keys/status | reads server flag state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information disclosure | keys.py status() keyless branch | mitigate | demo_enabled dropped for keyless users (keys.py:93-94 early return) hides the demo UX from its audience — set the flag explicitly in BOTH branches + pinned test |
| T-15-02 | Tampering | preferences.py upsert | mitigate | user_id bound from JWT sub in the payload (existing pattern preferences.py:70-72); own-row RLS backstop from migration 032 |
| T-15-03 | Denial of service | favorite_models array | mitigate | Pydantic Field(max_length=200) bounds the array (Open Q3) |
| T-15-04 | Elevation of privilege | demo_enabled field | accept | field is read-only output computed from env settings; never parsed from any request |
</threat_model>

<verification>
- Wave-1 merge gate: cd backend && venv/Scripts/python.exe -m pytest tests/ -q → green modulo 2 documented pre-existing record_manager errors (run after all wave-1 plans complete)
- supabase migration list shows 033 applied on dev remote
- Live column probe succeeds via supabase-py (validates RESEARCH Assumption A1: list[str] → TEXT[] through PostgREST)
</verification>

<success_criteria>
- Migration 033 authored AND applied to dev (schema push is blocking — phase cannot verify without it)
- Four additive schema fields with correct defaults; exclude_unset semantics intact
- demo_enabled in both status branches with a keyless-branch pinned test
- favorite_models roundtrip + bidirectional no-clobber regression tests green
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-01-SUMMARY.md`
</output>
