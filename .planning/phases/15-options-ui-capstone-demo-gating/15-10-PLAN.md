---
phase: 15-options-ui-capstone-demo-gating
plan: 10
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/ModelSelector.tsx, frontend/src/components/ModelSelector.test.tsx]
autonomous: true
gap_closure: true
requirements: [KEY-05, MODEL-08, MODEL-01, MODEL-03]

must_haves:
  truths:
    - "The Settings-surface picker renders Favorites → Popular → All sections and selectable option rows when the models prop arrives AFTER mount (the normal SettingsPage fetch timing) — closes 15-04 #2 / gap truth 2"
    - "A connected user can pick any model on BOTH the chat and settings surfaces; the settings panel is operable under the normal /api/models fetch timing — closes 15-05 #1 / gap truth 3"
    - "A late-arrival test proves options render from a post-mount catalog prop without a lazy fetch and honors the UI-SPEC section order; the prop-at-mount chat path and the empty-[]-prop lazy-fetch regression are NOT broken"
  artifacts:
    - path: "frontend/src/components/ModelSelector.tsx"
      provides: "effectiveState render gating so a late suppliedModels flips the panel to 'loaded'"
      contains: "effectiveState"
    - path: "frontend/src/components/ModelSelector.test.tsx"
      provides: "Late-arrival (models undefined → rerender with catalog) render test"
      contains: "late"
  key_links:
    - from: "SettingsPage models prop (setModels after the post-mount /api/models fetch)"
      to: "ModelSelector open panel render"
      via: "effectiveState = suppliedModels ? 'loaded' : state"
      pattern: "effectiveState"
    - from: "effectiveState"
      to: "the four panel render gates (loading / error / loaded-empty / loaded-list)"
      via: "replacing the stale `state ===` gates"
      pattern: "effectiveState ==="
---

<objective>
Close CR-02: `ModelSelector` seeds its `state` once at mount and never transitions to 'loaded' when the `models` prop arrives after mount. On the Settings surface — where SettingsPage seeds `models=undefined` and the `/api/models` fetch resolves post-mount (the guaranteed timing) — the open panel renders ONLY the search input: no sections, no options. This breaks the 15-04 sections truth and the 15-05 "both surfaces" truth, and collaterally blocks settings-surface search (MODEL-01), starring (MODEL-08), and gating (KEY-05).

Fix: derive an effective render state (`suppliedModels ? 'loaded' : state`) so a late-arriving catalog prop immediately unlocks the panel, without regressing the lazy-fetch path (idle → loading → loaded/error) or the chat surface (prop present at mount).
Purpose: make the settings picker operable under real fetch timing so all four settings-surface requirements hold.
Output: effectiveState-gated ModelSelector, late-arrival regression test.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-VERIFICATION.md   (CR-02 gap: gaps[1] + gaps[2], same root cause)
@.planning/phases/15-options-ui-capstone-demo-gating/15-04-PLAN.md        (the section-render contract this truth belongs to: 15-04 #2)
@.planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md        (LOCKED section contract: Favorites → Popular → All models)
@frontend/src/components/ModelSelector.tsx
@frontend/src/components/ModelSelector.test.tsx
@frontend/src/pages/SettingsPage.tsx                                      (the late-prop wiring: models seeded undefined at :32, setModels after the post-mount fetch)

<interfaces>
<!-- Contracts the executor needs — extracted from ModelSelector.tsx. Use directly, no exploration. -->

At mount / render (~118-143):
  const suppliedModels = models && models.length > 0 ? models : undefined   // [] means "no catalog yet" → stays undefined (LOCKED regression)
  const [state, setState] = useState<LoadState>(suppliedModels ? 'loaded' : 'idle')   // seeded ONCE — the bug
  const rows: ModelResponse[] = suppliedModels ?? fetched ?? []             // already picks up the LATE prop at render (data is fine)

The four render gates that must key off the effective state (~414-436):
  {state === 'loading' && ( ...Loading models… )}
  {state === 'error'   && ( ...retry button )}
  {state === 'loaded' && navigable.length === 0 && ( ...empty/no-match )}
  {state === 'loaded' && navigable.length > 0  && ( <ul role="listbox"> ... )}

loadModels early-returns when suppliedModels is defined (~202) and openMenu only calls it `if (state === 'idle')` (~220) — so on the late-prop path `state` is stuck at 'idle' forever and none of the four gates fire.

Section labels (LOCKED, from COPY): 'Favorites' → 'Popular' → 'All models'. Test harness constants: SECTIONED_COUNT = 5 (Popular 2 + All 3, no favorites), MODELS = 3 rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive effectiveState so a late catalog prop unlocks the panel + late-arrival test</name>
  <files>frontend/src/components/ModelSelector.tsx, frontend/src/components/ModelSelector.test.tsx</files>
  <read_first>
    - frontend/src/components/ModelSelector.tsx lines ~118-221 (suppliedModels/state/rows derivation, loadModels early-return, openMenu idle-gate) AND lines ~400-460 (the four `state ===` render gates being retargeted)
    - frontend/src/pages/SettingsPage.tsx lines ~23-57 (models seeded undefined, the post-mount /api/models useEffect that calls setModels — the exact timing this reproduces)
    - frontend/src/components/ModelSelector.test.tsx lines ~1-70 (the makeApiMock + mockCatalog + SECTIONED_COUNT harness) AND lines ~229-249 (the empty-[]-prop lazy-fetch regression + genuine-empty-catalog tests that must stay green)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md (the Favorites → Popular → All section order the test asserts)
  </read_first>
  <behavior>
    - Late-arrival (the fix): render with `models` UNDEFINED (SettingsPage seeds undefined), drain mount microtasks, then rerender with a non-empty catalog prop (the post-mount fetch resolving), then open → the listbox renders with option rows and section headers in the LOCKED order. Seed one favorite so all three headers assert (Favorites → Popular → All models); assert the option count is SECTIONED_COUNT-shaped and that `/api/models` was NOT lazily fetched (the late prop supplied the catalog, no fetch needed).
    - Non-regression, chat surface: render with the catalog prop present AT mount → sections still render exactly as today.
    - Non-regression, lazy path: an empty `models={[]}` prop still lazy-fetches on open (suppliedModels stays undefined → effectiveState follows `state` through idle→loading→loaded); a genuinely-empty loaded catalog still shows "No models available."; a failed fetch still shows the error/retry.
  </behavior>
  <action>
    In ModelSelector.tsx, add a derived `const effectiveState: LoadState = suppliedModels ? 'loaded' : state` next to the existing `rows` derivation (~143). Retarget the four panel render gates (~414/418/428/436) from `state` to `effectiveState`: the 'loading' and 'error' gates only ever matter on the lazy-fetch path where `suppliedModels` is undefined and `effectiveState === state`, so their behavior is unchanged; the two `'loaded'` gates now fire immediately whenever `suppliedModels` is present (mount OR late arrival). Do NOT modify `openMenu`, `loadModels`, or any `setState` call — the lazy path keeps its idle→loading→loaded/error transitions and the empty-`[]`-prop regression (suppliedModels stays undefined for `[]`) is untouched. Do NOT change the section-build logic, favorites read, search, or a11y wiring. Then add the late-arrival test to ModelSelector.test.tsx using `renderWithProviders`'s returned `rerender` (or an equivalent rerender helper) and the existing `mockCatalog({ favorites: [...] })` harness, following the file's `await act(async () => {})` mount-drain convention.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ModelSelector.test.tsx</automated>
  </verify>
  <done>A late-arriving `models` prop renders the sectioned panel; the new late-arrival test passes; every existing ModelSelector test (chat-surface sections, empty-[] lazy fetch, genuine-empty, error/retry, search, star) stays green.</done>
  <acceptance_criteria>
    - `grep -n "effectiveState" frontend/src/components/ModelSelector.tsx` shows the derivation `suppliedModels ? 'loaded' : state` AND all four panel render gates keying off `effectiveState` (no bare `state ===` remains on those four gates)
    - New test (name contains "late"): render with `models` undefined → rerender with a non-empty catalog → open → `getAllByRole('option')` renders the catalog rows; section headers assert `['Favorites', 'Popular', 'All models']` (with one seeded favorite); `mockApiFetch` was NOT called with `'/api/models'`
    - `npx vitest run src/components/ModelSelector.test.tsx` reports all tests passed (prior + new); the empty-`[]`-prop lazy-fetch test and the genuine-empty-catalog test still pass unchanged
    - `npm run build` (tsc + vite) passes — no type error from the `effectiveState: LoadState` derivation
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server catalog (/api/models) → ModelSelector render | display-only; the change is render-gating of already-fetched, already-trusted data — no new untrusted-input path crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-35 | Information disclosure | effectiveState render gate | accept | the fix only flips a display gate on a late-arriving prop; it renders no new data, exposes no cross-user state, and adds no fetch — server-side RLS + the keyless gate (useKeyGate, unchanged) remain the security boundaries |
</threat_model>

<verification>
- `npx vitest run src/components/ModelSelector.test.tsx` green including the new late-arrival test
- The four panel render gates key off `effectiveState`; a post-mount `models` prop unlocks the sectioned panel
- Chat-surface, empty-[]-prop lazy-fetch, and genuine-empty-catalog paths unregressed
</verification>

<success_criteria>
- CR-02 closed: 15-04 #2 and 15-05 #1 hold on BOTH surfaces
- Settings-surface search (MODEL-01), starring (MODEL-08), and gating (KEY-05) become operable again
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-10-SUMMARY.md`
</output>
