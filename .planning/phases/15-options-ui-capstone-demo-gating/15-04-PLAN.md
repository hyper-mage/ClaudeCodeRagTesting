---
phase: 15-options-ui-capstone-demo-gating
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/lib/fuzzy.ts
  - frontend/src/lib/fuzzy.test.ts
  - frontend/src/components/ModelSelector.tsx
  - frontend/src/components/ModelSelector.test.tsx
autonomous: true
requirements: [MODEL-01, MODEL-03]

must_haves:
  truths:
    - "Typing in the picker's pinned search input filters models fuzzily (typo-tolerant subsequence, e.g. 'lama33' matches 'llama-3.3') over id AND name with the locked ranking; non-matches are removed; 'No models match your search.' renders when nothing matches"
    - "The open dropdown shows Favorites → Popular → All models sections with non-interactive headers; Favorites is hidden entirely when empty; Popular preserves popularity_rank order; All is the complete catalog alphabetical"
    - "Every row whose popularity_rank is non-null shows a 'Popular' chip mirroring the Free tag classes, in EVERY section instance including search results (closes audit B-1 / MODEL-03)"
    - "On open, focus moves to the search input; ArrowUp/Down skip headers; Enter selects; Esc closes back to the trigger; existing locked picker behaviors (lazy fetch, loading/error/empty states, outside-click, max-h-72, min-h-11 rows, selected check) survive"
  artifacts:
    - path: "frontend/src/lib/fuzzy.ts"
      provides: "Hand-rolled scorer (D-08, zero new deps)"
      exports: ["fuzzyScore", "matchModel"]
    - path: "frontend/src/lib/fuzzy.test.ts"
      provides: "Locked ranking spec coverage (RESEARCH Wave-0 gap)"
    - path: "frontend/src/components/ModelSelector.tsx"
      provides: "Sectioned, searchable combobox with Popular chip"
      contains: "Search models"
  key_links:
    - from: "frontend/src/components/ModelSelector.tsx"
      to: "frontend/src/lib/fuzzy.ts"
      via: "matchModel(query, m.id, m.name) filtering + score-desc sort"
      pattern: "matchModel"
    - from: "ModelSelector mount"
      to: "GET /api/preferences"
      via: "favorites read once at mount (favorite_models ?? [])"
      pattern: "api/preferences"
    - from: "search input"
      to: "listbox rows"
      via: "aria-activedescendant on section-scoped option ids"
---

<objective>
Rebuild `ModelSelector`'s flat listbox into a sectioned, searchable combobox: hand-rolled fuzzy matcher (D-08), Favorites → Popular → All sections (D-06), the Popular badge chip (D-07, closes audit B-1 / MODEL-03), and the search input with the locked combobox a11y contract (closes audit W-1 / MODEL-01).

Purpose: this is the phase's biggest surgery — the risk is breaking the picker's locked Phase-12 behaviors while restructuring. Both picker surfaces (thread header + settings) share this one component, so sections/search land on both at once.
Constraint: zero new npm packages (D-08 locked bias); do NOT run `npx shadcn init` — the STATE.md todo saying otherwise is SUPERSEDED by the approved UI-SPEC (`Tool: none`).
Output: fuzzy util + tests, restructured ModelSelector, migrated component tests.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-CONTEXT.md  (D-06, D-07, D-08)
@.planning/phases/15-options-ui-capstone-demo-gating/15-PATTERNS.md  (§ModelSelector — all load-bearing excerpts, §fuzzy.ts, §FE component tests)
@.planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md  (Pattern 3, Pitfalls 1/2/8, Code Examples "Fuzzy matcher")
@.planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md  (§Components ModelSelector rows, §Interaction Contract — picker a11y LOCKED, §Copywriting, §Color neutral chips)

<interfaces>
From plan 15-01 (live): GET /api/preferences → { default_model, theme, favorite_models: string[] }
Existing ModelResponse (ModelSelector.tsx:10-20): { id, name, is_free, popularity_rank?: number | null, price_per_mtok_prompt, price_per_mtok_completion, context_length } — is_free and popularity_rank are server-computed, rendered verbatim, NEVER recomputed client-side.
This plan leaves the favorites list read-only (state + section render). The star toggle + PUT land in plan 15-06 to keep this plan within budget.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fuzzy.ts scorer + locked-ranking unit tests</name>
  <files>frontend/src/lib/fuzzy.ts, frontend/src/lib/fuzzy.test.ts</files>
  <read_first>
    - frontend/src/lib/pkce.ts lines 1-10 (lib/ module conventions: named exports only, no default export, no React import, camelCase, 2-space, single quotes, no semicolons)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md Code Examples "Fuzzy matcher" (the implementation spec with the locked scoring tiers)
    - frontend/src/test/themeBootstrap.test.ts (pure-function test analog — no providers, no api mock)
  </read_first>
  <behavior>
    - exact substring outranks any subsequence match ('llama' vs a scattered l-l-a-m-a)
    - substring starting at a word boundary (/, -, ., space, :) outranks a mid-word substring; earlier index wins within a tier
    - subsequence tier: query chars in order with gaps allowed — 'lama33' matches 'llama-3.3'; tighter span scores higher
    - non-subsequence → null (row removed, never disabled)
    - case-insensitive; empty query → 0 (match-all)
    - matchModel returns the max of id/name scores; null name falls back to id-only; both null → null
  </behavior>
  <action>
    Create frontend/src/lib/fuzzy.ts exporting fuzzyScore(query: string, target: string): number | null and matchModel(query: string, id: string, name: string | null): number | null, implementing exactly the RESEARCH Code Example spec: substring tier 10000 + 1000 boundary bonus - index; subsequence tier 1000 - (span - query.length); BOUNDARY = /[\s/\-.:]/. Ties are resolved by the CALLER sorting alphabetically by label (do not bake tie-breaking into the scorer). Create fuzzy.test.ts per the behavior list. No new dependencies.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/fuzzy.test.ts</automated>
  </verify>
  <done>Both files exist; all ranking-spec tests green; conventions match sibling lib/ files.</done>
  <acceptance_criteria>
    - fuzzy.ts exports named fuzzyScore and matchModel, no default export, no React import
    - fuzzy.test.ts includes the 'lama33' → 'llama-3.3' subsequence case and a null (non-subsequence) case
    - vitest run src/lib/fuzzy.test.ts exits 0
    - package.json diff is empty (zero new deps)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sections (Favorites/Popular/All) + Popular chip + favorites mount read + section-scoped keys</name>
  <files>frontend/src/components/ModelSelector.tsx, frontend/src/components/ModelSelector.test.tsx</files>
  <read_first>
    - frontend/src/components/ModelSelector.tsx (FULL 330 lines — options array :96-100, seed effect :146-152, ModelHint/Free tag :315-330, COPY const :41-45, lazy fetch :102-118, outside-click :155-164, states :246-262, row/check classes :285-297)
    - frontend/src/components/ModelSelector.test.tsx (existing fixture with popularity_rank rows :14-35, apiFetch boundary mock :1-11)
    - frontend/src/pages/ChatPage.tsx lines 56-64 (silent array-guarded fetch pattern for the favorites read)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md §Components rows "ModelSelector — sections" and "— Popular chip"
  </read_first>
  <action>
    Restructure the flat options array (:96-100) into TWO derived structures: (a) a flat NAVIGABLE array of selectable options only (activeIndex indexes this — headers are never navigable), and (b) a RENDER row list interleaving header rows and option rows. Section order when no search query: the extraOption row first (outside sections, unchanged semantics) → Favorites → Popular → All models. Favorites = intersection of the favorites state with the catalog, alphabetical by label, section ENTIRELY ABSENT when the intersection is empty (locked — no empty-state hint). Popular = rows with popularity_rank != null sorted ascending by popularity_rank (curated POPULAR_MODELS order). All models = the COMPLETE catalog alphabetical by label. Duplication across sections is deliberate: React keys and option DOM ids MUST be section-scoped — key `${section}:${m.id}`, id `${listboxId}-opt-${section}-${index}` (Pitfall 1) — and the selected check (left-1.5 text-blue-600) renders identically in every duplicate instance. Headers: role="presentation", classes px-3 pt-2 pb-1 text-xs font-semibold text-gray-600 dark:text-gray-400, labels exactly `Favorites` / `Popular` / `All models` (sentence case, no uppercase/tracking), added to the LOCKED COPY const.

    Popular chip (D-07, closes B-1): in ModelHint (:315-330), render a sibling span whenever model.popularity_rank != null with text `Popular` and classes EXACTLY mirroring the Free tag: rounded bg-gray-200 px-1 text-gray-700 dark:bg-gray-700 dark:text-gray-200. Order on the hint line: [Free|price] [Popular] [context]. It renders in every section instance.

    Favorites data (read-only this plan): useState<string[]>([]) seeded by a mount effect calling apiFetch('/api/preferences') with the silent array-guard pattern (set favorite_models only when Array.isArray; catch silently; cancelled-flag cleanup). The star toggle + PUT are plan 15-06 — do NOT add a star button here.

    Fix the seed effect (:146-152) to re-seed activeIndex (to the selected option's NAVIGABLE index, else 0) on OPEN only — remove the options.length dependency (Pitfall 2 groundwork; search lands next task). Keep list focus (listRef) for now — the focus migration is Task 3. Preserve verbatim: COPY strings, lazy fetch on open, Loading/error-retry/'No models available.' states, empty-models-prop-means-unfetched semantics, outside-click close, max-h-72 scroll, min-h-11 rows.

    Tests (same task — Pitfall 8 discipline): update ModelSelector.test.tsx — the apiFetch mock must now dispatch per-URL (GET /api/models → fixture catalog; GET /api/preferences → {favorite_models: [...]}; default existing behavior otherwise). Add: section headers render in order with a favorited fixture; Favorites absent when favorite_models is []; Popular section sorted by popularity_rank; All contains every model alphabetically; Popular chip present on popularity_rank rows in BOTH their Popular and All instances; no duplicate-key React warnings (assert console.error not called with key warning, or rely on strict render); selected check present in every duplicate instance of the selected model.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ModelSelector.test.tsx && npm run lint</automated>
  </verify>
  <done>Sections + chip render per spec; existing locked-behavior tests still green; favorites read at mount; keys section-scoped.</done>
  <acceptance_criteria>
    - ModelSelector.test.tsx passes with new section/chip assertions
    - ModelSelector.tsx contains header labels 'Favorites', 'Popular', 'All models' and a chip span with text 'Popular' whose class list includes bg-gray-200 and dark:bg-gray-700
    - grep "role=\"presentation\"" frontend/src/components/ModelSelector.tsx returns >= 1
    - Option ids/keys are section-scoped (grep for a template containing ':' between section token and model id, e.g. `${section}` or literal 'fav:'/'pop:'/'all:')
    - GET /api/preferences fetched once at mount (test asserts the mock URL was called)
    - npm run lint exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Search input + fuzzy wiring + combobox focus-model migration (locked a11y contract)</name>
  <files>frontend/src/components/ModelSelector.tsx, frontend/src/components/ModelSelector.test.tsx</files>
  <read_first>
    - frontend/src/components/ModelSelector.tsx (post-Task-2 state: focus effect :167-172 region, onListKeyDown :188-222 region, PANEL_MAX :135-143 region)
    - .planning/phases/15-options-ui-capstone-demo-gating/15-UI-SPEC.md §Interaction Contract — picker a11y (LOCKED) and §Components "ModelSelector — search input"
    - frontend/src/components/ModelSelector.test.tsx (UL-focus assertions to migrate — contract-sanctioned, Pitfall 8)
  </read_first>
  <action>
    Add the search input as the FIRST element inside the open panel, above everything: row h-11 full-width, Search icon (lucide) size 14 text-gray-500 dark:text-gray-400 left-inset, input px-3 text-sm on the panel surface bg-gray-50 dark:bg-gray-900, border-b border-gray-300 dark:border-gray-700 (no side borders), placeholder `Search models…` (same ellipsis char as Loading models…), placeholder-gray-500 dark:placeholder-gray-400, focus:outline-none with NO focus ring inside the panel (cmdk convention). Debounce the applied query 150ms behind the input value (locked).

    While the debounced query is non-empty: the render list becomes ONE flat score-ranked list — filter the catalog by matchModel(query, m.id, m.name) != null, sort score desc with ties alphabetical by label, NO section headers, extraOption row HIDDEN, non-matches removed. Empty result renders `No models match your search.` (text-xs caption, added to COPY). Clearing the query restores sections.

    Focus-model migration (LOCKED contract, supersedes listbox-focus): on open, focus the search input instead of the UL (replace the :167-172 effect). Input carries aria-autocomplete="list", aria-controls={listboxId}, aria-activedescendant={activeOptionId} (the section-scoped id of the active navigable row). Move the keyboard machinery from onListKeyDown (:188-222) to the input's onKeyDown: ArrowDown/ArrowUp move activeIndex over the NAVIGABLE array (headers skipped structurally; wrapping not required), Enter selects the active row, Esc closes and returns focus to the trigger, Tab stays trapped (preventDefault), printable keys fall through to the input natively, Home/End keep native caret behavior (no list jump). Do NOT implement Shift+Enter here (needs the favorite toggle — plan 15-06 adds it). On query change: reset activeIndex to the first navigable option and clamp when the filtered list shrinks (Pitfall 2). Trigger button semantics unchanged (aria-haspopup/aria-expanded/aria-controls; Enter/Space/ArrowDown opens). Bump PANEL_MAX 320 → 370 (:139) so drop-up math accounts for the h-11 search row (the settings sidebar-footer selector relies on drop-up).

    Tests (same task): migrate existing UL-focus assertions to the input-focus model (contract-sanctioned — do NOT weaken min-h-11, locked-copy, or role assertions). Add: open focuses the input; typing 'lama33' (with vi.useFakeTimers + advanceTimersByTime(150) or userEvent configured with advanceTimers) filters to the llama fixture; flat search mode has no role=presentation headers and hides the extraOption; empty query restores sections; no-match renders 'No models match your search.'; Esc returns focus to the trigger; ArrowDown from the input moves active over options only.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ModelSelector.test.tsx && npm run test && npm run build && npm run lint</automated>
  </verify>
  <done>Search + a11y contract live; full FE suite, build, and lint green; drop-up math honest.</done>
  <acceptance_criteria>
    - ModelSelector.tsx contains placeholder 'Search models…', aria-autocomplete="list", and aria-activedescendant wiring on the input
    - PANEL_MAX constant equals 370
    - Tests assert: input focused on open; 150ms debounce respected; extraOption hidden while searching; 'No models match your search.' rendered on no-match; headers absent in search mode
    - Full FE suite (npm run test), npm run build, npm run lint all exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server catalog → picker render | is_free / popularity_rank are server-computed fields rendered verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Tampering | client-side freeness/popularity | mitigate | render server-computed m.is_free / popularity_rank verbatim; never recompute from pricing or the :free suffix (Phase 12 contract); enforcement stays server-side (plan 15-02 guard) |
| T-15-15 | Denial of service | fuzzy filter on keystrokes | mitigate | 150ms debounce; catalog ~400 rows, O(n·q) scorer is trivial |
| T-15-16 | Information disclosure | search input contents | accept | query is local component state, never sent to any API or log |
</threat_model>

<verification>
- cd frontend && npx vitest run src/lib/fuzzy.test.ts src/components/ModelSelector.test.tsx → green
- npm run test && npm run build && npm run lint → green (full-suite regression)
- package.json unchanged (zero new deps; shadcn NOT initialized — components.json still absent)
</verification>

<success_criteria>
- MODEL-01 closed: catalog is genuinely searchable with typo-tolerant fuzzy matching per the locked ranking
- MODEL-03 / audit B-1 closed: popularity_rank renders as a Popular chip on every instance
- D-06 sections live on both picker surfaces; all Phase-12 locked behaviors survive
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-04-SUMMARY.md`
</output>
