---
phase: 15-options-ui-capstone-demo-gating
plan: 08
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05", "15-06", "15-07"]
files_modified: []
autonomous: false
requirements: [DEMO-01, SEC-03]

must_haves:
  truths:
    - "Prod database has migrations 029-033 applied (it was 4 behind + this phase's 033)"
    - "Fly prod carries LLM_API_KEY (the OpenRouter OWNER sk-or… key — the 999.2 guardrail lives on that key) AND DEMO_FALLBACK_ENABLED=true (D-09)"
    - "Deployed prod backend and frontend carry the Phase-15 code (new resolution branch, status field, picker, gate, banner)"
    - "Live prod smoke passes: keyless free pick streams with the banner; paid pick gates → connect → auto-apply toast"
  artifacts:
    - path: ".planning/phases/15-options-ui-capstone-demo-gating/15-08-SUMMARY.md"
      provides: "Deploy record: migration list output, secrets confirmation (names only), smoke results"
  key_links:
    - from: "Fly secret DEMO_FALLBACK_ENABLED=true"
      to: "config.py demo_fallback_enabled"
      via: "pydantic-settings env read (case-insensitive)"
    - from: "prod OpenRouter owner key"
      to: "provider budget guardrail (403 at ~$0.10)"
      via: "guardrail is on the key itself — applies wherever the key is used (999.2 finding)"
---

<objective>
The human-gated capstone deploy: apply prod migrations 029-033, flip `DEMO_FALLBACK_ENABLED=true` in prod (D-09), ship the Phase-15 backend + frontend, and smoke the demo UX live.

Purpose: this discharges SEC-03 (gate released by the 999.2 PASS finding: guardrail 403-blocked at $0.1026, both kill switches proven, no-email outcome accepted) and closes DEMO-01. The flag stays env-driven — no admin UI (project rule). Owner cost is bounded three ways: app-level flag, D-03 free-guard (structural $0), provider budget ceiling (~$0.10).
Output: live prod running demo fallback, deploy record in the SUMMARY.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md  (Pattern 7 ordered checklist, Pitfall 9, Pitfall 10, Open Q4/Q5, State of the Art demo-slug liveness)
@.planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md  (PASS verdict + the deployment note this plan executes)

Notes (project memory):
- .env = DEV Supabase project; .env.prod = PROD project. Prod applied through migration 028; 029-032 pending + this phase's 033.
- Deploy mechanics: backend = flyctl deploy with the repo-root Dockerfile; frontend = Cloudflare Pages builds `main`; push `master:main`.
- Migration-history repair pattern: `db push` replaying old migrations / "already exists" → `supabase migration repair --status applied` for the prior range first.
- Anon demo bootstrap is pre-broken (D-999.1-DEMO-A) — smoke with a SIGNED-IN keyless account, not the anon path.
- Free-model flakiness (rate limits, single Venice endpoint ~88% uptime) is environmental, NOT a phase defect (precedent D-999.1-LLM-A) — do not retry-until-green against the live provider.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight (read-only) — suites, prod migration drift, demo-slug liveness, Fly secrets inventory</name>
  <files>none (read-only ops)</files>
  <read_first>
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md (deployment note: LLM_API_KEY must be the owner sk-or… key on Fly)
    - .env.prod (prod SUPABASE_URL → project ref; NEVER print secret values)
    - fly.toml (app name for flyctl commands)
  </read_first>
  <action>
    Produce a go/no-go table from four read-only checks. (1) Local gates: full backend suite (venv pytest tests/ -q; only the 2 documented record_manager errors tolerated) + frontend npm run test + npm run build + npm run lint. (2) Prod migration drift: target the PROD project (link or --db-url per the .env.prod project ref — the established dual-env discipline) and run `supabase migration list` to enumerate remote-applied vs local; expect 029-033 pending; record whether the 025-028 range needs `migration repair --status applied` first (Open Q4). Do NOT push anything in this task. (3) Demo-slug liveness (volatile fact, re-verify at deploy): curl https://openrouter.ai/api/v1/models/meta-llama/llama-3.3-70b-instruct:free/endpoints → expect 200 with tools in supported_parameters; if the slug is dead, identify a live :free slug WITH tools support as the replacement candidate and mark no-go pending the Task-2 decision (a config default change would ride the deploy). (4) Fly secrets inventory: flyctl secrets list (NAMES + digests only — never values): LLM_API_KEY must be present (owner key per the SEC-03 finding note); DEMO_FALLBACK_ENABLED should be absent or false pre-flip. Assemble the table and stop.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/ -q && cd ../frontend && npm run test && npm run build</automated>
  </verify>
  <done>Go/no-go table produced covering suites, migration drift (with/without repair need), slug liveness, and secrets presence — zero writes performed.</done>
  <acceptance_criteria>
    - Both suites + build green (modulo the 2 documented record_manager errors)
    - `supabase migration list` output captured showing the exact pending range for prod
    - Slug endpoint probe result recorded (HTTP status + tools-support present/absent)
    - `flyctl secrets list` output captured showing LLM_API_KEY present; no secret VALUE appears anywhere in output or logs
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve the prod flip</name>
  <files>none</files>
  <read_first>
    - Task 1 go/no-go table (in-session output)
  </read_first>
  <decision>Enable demo fallback in prod: apply migrations 029-033, set DEMO_FALLBACK_ENABLED=true, deploy backend + frontend</decision>
  <action>PAUSE execution and present the Task-1 go/no-go table with this decision block to the user. Do not run any deploy command until the user replies with an option id.</action>
  <context>SEC-03 gate is discharged (999.2 PASS: provider guardrail 403-blocked at $0.1026; app kill switch proven; demo is free-models-only by the D-03 server guard, so structural owner cost is $0 and worst-case bounded ~$0.10 by the provider ceiling). This is still a prod schema push + a cost-bearing flag flip — user approval required (D-09 is the user's decision to execute at THIS deploy).</context>
  <options>
    <option id="proceed">
      <name>Proceed with the full sequence (Task 3)</name>
      <pros>Closes DEMO-01, DEMO-02 live, SEC-03; v1.2 capstone ships</pros>
      <cons>Prod mutation; demo turns spend on the owner key up to the ~$0.10 provider ceiling</cons>
    </option>
    <option id="abort">
      <name>Abort — defer the flip, optionally still ship code + migrations with the flag OFF</name>
      <pros>Zero cost exposure; code is safe to ship dark (flag defaults False)</pros>
      <cons>DEMO-01/SEC-03 stay open; phase cannot verify complete</cons>
    </option>
  </options>
  <verify>
    <automated>MISSING — human decision gate; no automated check applies (resume signal recorded in SUMMARY)</automated>
  </verify>
  <done>User has answered with an option id.</done>
  <acceptance_criteria>
    - User response "proceed" or "abort" recorded; Task 3 runs only on "proceed"
  </acceptance_criteria>
  <resume-signal>Reply "proceed" or "abort"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the ordered deploy sequence</name>
  <files>none (ops — no repo file changes)</files>
  <read_first>
    - .planning/phases/15-options-ui-capstone-demo-gating/15-RESEARCH.md §Pattern 7 (the ordered checklist this task executes)
    - Task 1 outputs (repair-needed flag, app name, pending range)
  </read_first>
  <action>
    Run in EXACTLY this order (Pitfall 10 — the flag must never be ON while prod lacks 029-032): (1) prod `supabase db push` applying 029-033, with the repair contingency first if Task 1 flagged it (`supabase migration repair --status applied` for the prior range, then push); re-run `supabase migration list` to confirm 029-033 applied. (2) Confirm LLM_API_KEY still present on Fly (names only). (3) Deploy the NEW backend code with the flag still OFF: flyctl deploy (repo-root Dockerfile). (4) Flip the flag: flyctl secrets set DEMO_FALLBACK_ENABLED=true (machine restarts with new code + flag). (5) Frontend: git push origin master:main → Cloudflare Pages builds main. (6) Post-deploy probes: flyctl secrets list shows DEMO_FALLBACK_ENABLED; backend health endpoint returns 200; CF build completes. If ANY step fails, stop and report — do not proceed past a failed migration push or a failed deploy (the flag flip in step 4 must not happen if steps 1-3 failed). Auth gates (supabase/flyctl login prompts) surface as checkpoints if they occur.
  </action>
  <verify>
    <automated>supabase migration list 2>&1 | grep -c "2024030100003" && flyctl secrets list 2>&1 | grep -c DEMO_FALLBACK_ENABLED</automated>
  </verify>
  <done>Migrations 029-033 applied to prod; backend deployed with the flag ON; frontend rebuilt from main; all probes green.</done>
  <acceptance_criteria>
    - `supabase migration list` (prod) shows 20240301000029 through 20240301000033 applied remote
    - `flyctl secrets list` shows DEMO_FALLBACK_ENABLED (digest present)
    - flyctl deploy completed successfully (release visible); git push master:main accepted
    - No secret value printed at any step
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live prod smoke — demo UX end-to-end</name>
  <files>none</files>
  <read_first>
    - Task 3 outputs (deploy confirmations)
  </read_first>
  <action>PAUSE execution and present the verification checklist below to the user. Wait for the resume signal; record the outcome (and any caveats) verbatim in the SUMMARY.</action>
  <what-built>Prod now runs the full Phase-15 stack: sectioned/searchable picker with favorites, key-gated selection with OAuth auto-apply resume, demo fallback ON with the server-side free-guard, demo banner, and [Use demo] recovery.</what-built>
  <how-to-verify>
    Use a SIGNED-IN prod account with NO OpenRouter key connected (not the anon path — anon bootstrap is pre-broken, D-999.1-DEMO-A):
    1. Open the app → chat pane shows the amber demo banner with the exact sentence "Demo mode — a free model is in use; it may be slower, less accurate, or temporarily rate-limited (no usage left)." and no close button.
    2. Open the model picker → full catalog renders with Favorites/Popular/All sections; type a partial name → fuzzy results; Popular chips visible.
    3. Pick a FREE model → it applies with no modal; send a message → the turn streams on the picked free model (if the free provider rate-limits, the in-band error with recovery UI is ACCEPTABLE — environmental, not a defect; note it and move on).
    4. Pick a PAID model → the "Connect OpenRouter?" modal appears; click Connect → complete the OpenRouter OAuth → you land back with a toast "Connected — <model> is set for this chat." and the model applied.
    5. Star a model → reopen the picker → it sits under Favorites; reload the page → still favorited.
    6. (Optional, keyed-account path) If you have an account whose key 403s: the error bubble shows [Use demo]; clicking retries the turn in demo mode with the banner latched.
  </how-to-verify>
  <verify>
    <automated>MISSING — live OAuth + provider round-trip requires the real user account (this checkpoint is the test)</automated>
  </verify>
  <done>User confirms the six checks (or approves with documented environmental caveats per Pitfall 9).</done>
  <acceptance_criteria>
    - User replies "approved" (or "approved-with-caveat" listing only environmental free-provider issues)
    - Any functional failure is captured verbatim for gap-closure planning
  </acceptance_criteria>
  <resume-signal>Type "approved", "approved-with-caveat: <notes>", or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine → prod DB / Fly / CF | privileged deploy operations |
| public internet → prod app with demo ON | keyless users can now mint owner-key (free-model) completions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-28 | Denial of service (cost) | owner key with flag ON | mitigate | three bounds: D-03 free-guard (structural $0, plan 15-02), app flag (instant off switch), provider budget guardrail 403 at ~$0.10 (999.2 PASS) |
| T-15-29 | Elevation of privilege | flag ON against a stale schema | mitigate | strict ordering: migrations → code deploy → flag flip; abort on any step failure (Pitfall 10) |
| T-15-30 | Information disclosure | secrets in deploy logs | mitigate | secrets listed by NAME/digest only; values never echoed; SEC-01 scrub discipline in all captured output |
| T-15-31 | Spoofing | wrong key on Fly (guardrail elsewhere) | mitigate | SEC-03 finding deployment note executed verbatim: verify LLM_API_KEY is the owner sk-or… key BEFORE the flip — the guardrail lives on that key |
</threat_model>

<verification>
- Prod migration list shows 029-033 applied; Fly shows DEMO_FALLBACK_ENABLED; releases visible on Fly + CF
- Human smoke checklist passed (banner, free-pick stream, paid-pick gate → OAuth → auto-apply, favorites persistence)
- SUMMARY records the deploy evidence and closes the stale STATE.md shadcn-init todo (superseded by the UI-SPEC — note for the phase-completion state update)
</verification>

<success_criteria>
- DEMO-01 closed: flag ON in prod, env-driven only
- SEC-03 closed: enabled only atop the 999.2 PASS finding with the deployment note satisfied
- v1.2 capstone live end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/15-options-ui-capstone-demo-gating/15-08-SUMMARY.md`
</output>
