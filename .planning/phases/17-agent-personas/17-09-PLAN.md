---
phase: 17-agent-personas
plan: 09
type: execute
wave: 4
depends_on: [17-03, 17-06, 17-07]
files_modified:
  - frontend/src/components/PersonaSelector.tsx
  - frontend/src/components/DefaultPersonaSelector.tsx
autonomous: true
requirements: [PERS-01, PERS-04]
must_haves:
  truths:
    - "PersonaSelector renders the persona options and calls onSelect(id) on click, reflecting the current value — no key gate (PERS-01)"
    - "DefaultPersonaSelector self-PUTs /api/preferences {default_persona} and calls onChange(id) on select — no key gate (PERS-04)"
    - "Both pickers render the list passed as a prop (fetched by the parent from GET /api/personas) — the list is never hardcoded (D-07)"
    - "Picker-only attribution: neither component renders a per-message persona badge on assistant bubbles, and no messages schema/column is added — persona is shown only via the current-thread picker (D-12)"
  artifacts:
    - path: "frontend/src/components/PersonaSelector.tsx"
      provides: "lightweight chat persona dropdown (NOT ModelSelector)"
      contains: "onSelect"
      min_lines: 25
    - path: "frontend/src/components/DefaultPersonaSelector.tsx"
      provides: "settings default persona dropdown with self-PUT"
      contains: "default_persona"
      min_lines: 25
  key_links:
    - from: "frontend/src/components/DefaultPersonaSelector.tsx"
      to: "/api/preferences"
      via: "void apiFetch PUT default_persona (fire-and-forget)"
      pattern: "default_persona"
    - from: "frontend/src/components/PersonaSelector.tsx"
      to: "onSelect callback"
      via: "option click"
      pattern: "onSelect"
---

<objective>
Build the two lightweight persona picker components — a chat-header PersonaSelector (calls onSelect; parent owns the PATCH) and a settings DefaultPersonaSelector (self-PUTs /api/preferences {default_persona}). Both render a personas list supplied by the parent (fetched from GET /api/personas — never hardcoded, D-07) and carry NO key gate (persona has no cost/key surface). Turns the 17-03 RED FE scaffolds GREEN.

Purpose: PERS-01 (chat picker), PERS-04 (settings default picker), D-07 (fetched list), D-12 (picker-only). Deliberately does NOT reuse the 560-line ModelSelector (its ModelResponse shape + useKeyGate do not fit).
Output: PersonaSelector.tsx, DefaultPersonaSelector.tsx.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md
@frontend/src/components/DefaultModelSelector.tsx

<interfaces>
<!-- Test contracts from 17-03 (must go GREEN):
PersonaSelector props { value: string | null; onSelect: (id: string) => void; personas?: {id,label,is_default}[] } — dropdown; option click → onSelect(id); trigger shows the label of `value`; NO gate.
DefaultPersonaSelector props { value: string | null; onChange?: (id: string) => void; personas?: {id,label,is_default}[] } — on select → onChange(id) AND void apiFetch('/api/preferences',{method:'PUT',body:JSON.stringify({default_persona:id})}).catch(()=>{}). NO gate.
Persona list item shape: { id: string; label: string; is_default: boolean }. apiFetch from ../lib/api. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersonaSelector.tsx — chat-header persona dropdown</name>
  <files>frontend/src/components/PersonaSelector.tsx</files>
  <read_first>
    - frontend/src/components/DefaultModelSelector.tsx (structure/styling reference — but STRIP useKeyGate; persona has no gate)
    - frontend/src/components/PersonaSelector.test.tsx (the 4 it() blocks this component must satisfy — from 17-03)
    - frontend/src/components/ChatContainer.tsx lines 91-116 (the header row + Tailwind token classes the picker sits in, for visual consistency)
  </read_first>
  <behavior>
    - Renders a trigger button showing the label for `value` (or a placeholder when value is null).
    - Opening the dropdown lists every persona label from the `personas` prop; clicking one calls onSelect(persona.id) and closes.
    - No key gate, no modal — a click always calls onSelect.
  </behavior>
  <action>
    Create frontend/src/components/PersonaSelector.tsx as a small self-contained dropdown (button trigger + a listbox of options; use lucide-react Check/ChevronDown for parity with the model picker, and the same Tailwind surface tokens as ChatContainer's header row). Props: `{ value: string | null; onSelect: (personaId: string) => void; personas?: Array<{ id: string; label: string; is_default: boolean }> }`. Render options from `personas ?? []` (never a hardcoded list — D-07). The trigger shows the label of the option whose id === value, else a "Persona" placeholder. On option click, call onSelect(option.id) and close. Use role="button"/role="listbox"/role="option" so the 17-03 tests' getByRole queries resolve. Import NOTHING from useKeyGate/useKeyStatus. Default export.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/PersonaSelector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PersonaSelector.test.tsx turns fully GREEN (renders options, onSelect(id) on click, value reflected on trigger, no-gate).
    - The component imports NO useKeyGate/useKeyStatus (`grep -L "useKeyGate\|useKeyStatus" ...` confirms absence).
    - Options are rendered from the `personas` prop — no persona ids/labels are hardcoded in the component.
  </acceptance_criteria>
  <done>PersonaSelector renders the fetched list and calls onSelect(id) with no gate; 17-03 chat-picker test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DefaultPersonaSelector.tsx — settings default persona dropdown with self-PUT</name>
  <files>frontend/src/components/DefaultPersonaSelector.tsx</files>
  <read_first>
    - frontend/src/components/DefaultModelSelector.tsx lines 30-60 (the self-PUT + optimistic onChange body — clone MINUS the useKeyGate wrapper)
    - frontend/src/components/DefaultPersonaSelector.test.tsx (the 3 it() blocks this must satisfy — from 17-03)
    - frontend/src/lib/api.ts (apiFetch signature — the fire-and-forget PUT boundary)
  </read_first>
  <behavior>
    - Selecting a persona calls onChange(id) AND fires a fire-and-forget PUT /api/preferences {default_persona:id}.
    - No key gate — a keyless user can set a default persona.
    - Renders a LOCKED heading + helper line above the dropdown (persona equivalent of DefaultModelSelector's copy).
  </behavior>
  <action>
    Create frontend/src/components/DefaultPersonaSelector.tsx mirroring DefaultModelSelector.tsx's layout (a heading + helper line above a dropdown) but WITHOUT useKeyGate. Props: `{ value: string | null; onChange?: (personaId: string) => void; personas?: Array<{ id: string; label: string; is_default: boolean }> }`. Reuse the PersonaSelector dropdown (import and render it) for the control, or inline an identical dropdown. On select, define `onSelect(personaId)` that calls `onChange?.(personaId)` then `void apiFetch('/api/preferences', { method: 'PUT', body: JSON.stringify({ default_persona: personaId }) }).catch(() => {})` (fire-and-forget, house style). Add a LOCKED heading "Default persona" + helper "New chats use this persona unless you pick a different one for a chat." Default export.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/DefaultPersonaSelector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DefaultPersonaSelector.test.tsx turns fully GREEN (PUT {default_persona:id} on select, onChange(id), keyless works).
    - The component fires exactly one PUT /api/preferences with body { default_persona: <id> } per selection.
    - No useKeyGate/useKeyStatus import.
  </acceptance_criteria>
  <done>DefaultPersonaSelector self-PUTs default_persona with no gate; 17-03 settings-picker test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PUT /api/preferences | The settings picker persists the user's default persona; authorization is server-side (JWT-bound, 17-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-26 | Tampering | client-sent persona id | accept | The picker only offers server-fetched ids; even a crafted id is registry-validated server-side (D-10, 17-06/17-07). The client is not the enforcement point. |
| T-17-27 | (n/a) | picker key gate | accept | Deliberately no gate — persona has no cost/key surface; a keyless user picking a persona is intended (tests assert it). |
| T-17-28 | Info Disclosure | persona list contents | accept | The list is the non-secret GET /api/personas payload (id/label/is_default) — no prompt text (voice_block) is present client-side. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/PersonaSelector.test.tsx src/components/DefaultPersonaSelector.test.tsx` all GREEN.
- `cd frontend && npm run build` succeeds (TypeScript strict — the new components type-check).
- Neither component imports useKeyGate/useKeyStatus; neither hardcodes the persona list.
</verification>

<success_criteria>
- Both persona pickers exist, render the fetched list, and carry no key gate; the 17-03 RED scaffolds are GREEN.
- The chat picker delegates persistence to the parent (onSelect); the settings picker self-PUTs default_persona.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-09-SUMMARY.md`.
</output>
