---
phase: 17-agent-personas
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/PersonaSelector.test.tsx
  - frontend/src/components/DefaultPersonaSelector.test.tsx
autonomous: true
requirements: [PERS-01, PERS-04]
must_haves:
  truths:
    - "A failing test pins that PersonaSelector renders the persona options and calls onSelect(id) on click (PERS-01)"
    - "A failing test pins that DefaultPersonaSelector PUTs /api/preferences {default_persona} on select and calls onChange(id) (PERS-04)"
    - "A failing test pins that a keyless user is NOT gated when picking a persona (no useKeyGate — persona has no key/cost surface)"
  artifacts:
    - path: "frontend/src/components/PersonaSelector.test.tsx"
      provides: "RED scaffold for the chat persona picker (mirror DefaultModelSelector.test.tsx minus gate mock)"
      contains: "onSelect"
    - path: "frontend/src/components/DefaultPersonaSelector.test.tsx"
      provides: "RED scaffold for the settings default persona picker"
      contains: "default_persona"
  key_links:
    - from: "frontend/src/components/DefaultPersonaSelector.test.tsx"
      to: "frontend/src/components/DefaultPersonaSelector.tsx"
      via: "import of the component under test (unresolved until 17-09)"
      pattern: "DefaultPersonaSelector"
---

<objective>
Author the Wave 0 RED frontend test scaffolds for the two persona pickers, mirroring DefaultModelSelector.test.tsx but WITHOUT the key-gate machinery (persona has no key/cost/demo surface — a keyless user must be able to pick a persona). These FAIL RED (module not found) until 17-09 creates the components.

Purpose: Nyquist Dim 8 — pins PERS-01 (chat picker → onSelect) and PERS-04 (settings picker → PUT default_persona) at the component boundary, and the "no gate" invariant.
Output: 2 new vitest files, both RED.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md

<interfaces>
<!-- Component contracts these RED tests assert (shipped by 17-09). -->

PersonaSelector (17-09) — props: { value: string | null; onSelect: (personaId: string) => void; personas?: Array<{id,label,is_default}> }. A lightweight dropdown (NOT ModelSelector). On option click → onSelect(personaId). NO useKeyGate. The parent (ChatPage) owns the PATCH.

DefaultPersonaSelector (17-09) — props: { value: string | null; onChange?: (personaId: string) => void; personas?: Array<{id,label,is_default}> }. On select → onChange(id) AND void apiFetch('/api/preferences', {method:'PUT', body: JSON.stringify({ default_persona: id })}).catch(()=>{}). Fire-and-forget, NO useKeyGate.

Test harness (frontend/src/test/utils.tsx): renderWithProviders, makeApiMock() → { apiFetch: vi.fn(), apiStream: vi.fn() }. Mock '../lib/api' via `vi.mock('../lib/api', () => makeApiMock())`. Do NOT mock useKeyStatus (persona has no gate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffold — PersonaSelector.test.tsx</name>
  <files>frontend/src/components/PersonaSelector.test.tsx</files>
  <read_first>
    - frontend/src/components/DefaultModelSelector.test.tsx (FULL — the render + userEvent.click(option) + assertion pattern; note lines 16-21 useKeyStatus mock which this test DELIBERATELY OMITS)
    - frontend/src/test/utils.tsx (renderWithProviders, makeApiMock)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"Tests (unit — Wave 0 RED scaffolds)" (DROP the useKeyStatus gate mock line)
  </read_first>
  <action>
    Create frontend/src/components/PersonaSelector.test.tsx. Import the not-yet-existent `PersonaSelector` from './PersonaSelector' (RED = module resolution failure). Provide a PERSONAS fixture = [{id:'board_game_expert',label:'Board-Game Expert',is_default:true},{id:'general_assistant',label:'General Assistant',is_default:false}]. Do NOT mock useKeyStatus (no gate). Write:
    - it('renders the persona options'): renderWithProviders(<PersonaSelector value={null} personas={PERSONAS} onSelect={vi.fn()} />); open the dropdown (click the trigger button) and assert both labels ("Board-Game Expert", "General Assistant") appear as options.
    - it('calls onSelect with the persona id on click'): click the "General Assistant" option → onSelect toHaveBeenCalledWith('general_assistant') (PERS-01).
    - it('reflects the current value on the trigger'): value="general_assistant" → the trigger shows "General Assistant".
    - it('does NOT gate a keyless pick'): with no key-status mock present, clicking an option still calls onSelect (no modal, no gate) — assert onSelect fired and no "Connect OpenRouter?" text appears.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/PersonaSelector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `npx vitest run` on it FAILS RED with a module-not-found / cannot-resolve './PersonaSelector' error (the component is authored in 17-09).
    - 4 `it(...)` blocks present.
    - The test file contains NO `vi.mock('../hooks/useKeyStatus'...)` and NO useKeyGate import (persona has no gate).
    - No component/source file created by this task.
  </acceptance_criteria>
  <done>PersonaSelector.test.tsx fails RED (missing component); pins onSelect(id) + value reflection + no-gate (PERS-01).</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffold — DefaultPersonaSelector.test.tsx</name>
  <files>frontend/src/components/DefaultPersonaSelector.test.tsx</files>
  <read_first>
    - frontend/src/components/DefaultModelSelector.test.tsx lines 48-93 (the "selecting → PUT /api/preferences {default_model}" + onChange assertions to clone for default_persona)
    - frontend/src/components/DefaultModelSelector.tsx lines 30-60 (the self-PUT + optimistic onChange body — the persona version drops useKeyGate)
    - frontend/src/test/utils.tsx (makeApiMock — apiFetch is the mocked boundary)
  </read_first>
  <action>
    Create frontend/src/components/DefaultPersonaSelector.test.tsx cloning DefaultModelSelector.test.tsx but WITHOUT the useKeyStatus mock and WITHOUT the keyless-gate test. `vi.mock('../lib/api', () => makeApiMock())`; import { apiFetch } and cast to the vi.fn. Same PERSONAS fixture as Task 1. Write:
    - it('selecting a persona PUTs /api/preferences with {default_persona: id}'): render <DefaultPersonaSelector value={null} personas={PERSONAS} />, open + click "General Assistant"; find the mockApiFetch call where path==='/api/preferences' && opts.method==='PUT' and assert JSON.parse(body) deepEquals { default_persona: 'general_assistant' } (PERS-04).
    - it('invokes onChange with the selected id'): pass onChange=vi.fn(); click "Board-Game Expert" → onChange toHaveBeenCalledWith('board_game_expert').
    - it('a keyless user can set a default persona (no gate)'): with no key-status mock, selecting still fires the PUT — assert the PUT call exists and NO "Connect OpenRouter?" text is rendered.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/DefaultPersonaSelector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `npx vitest run` FAILS RED with cannot-resolve './DefaultPersonaSelector'.
    - 3 `it(...)` blocks present; the PUT-shape test asserts the exact body { default_persona: <id> }.
    - No useKeyStatus mock / useKeyGate import in the file.
    - No component/source file created by this task.
  </acceptance_criteria>
  <done>DefaultPersonaSelector.test.tsx fails RED (missing component); pins PUT {default_persona} + onChange + no-gate (PERS-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new — test-only) | Client-side picker tests. The only server write they assert (PUT default_persona) is authorized server-side (JWT-bound) by 17-07; these tests pin the request SHAPE, not the auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | Client sends persona id | accept | The client may send any string; the backend registry-validates it (D-10, pinned by 17-02/17-06). The FE picker only ever offers server-fetched ids, reducing (not enforcing) the surface — enforcement is server-side. |
| T-17-08 | (n/a) | Persona picker gating | accept | Tests assert NO key gate — persona has no cost/key surface, so a keyless pick is intended and safe. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/PersonaSelector.test.tsx src/components/DefaultPersonaSelector.test.tsx` runs and reports RED (module-not-found).
- Neither test file imports useKeyStatus/useKeyGate.
- No component source files created.
</verification>

<success_criteria>
- 2 new vitest files exist, both RED for the correct reason (missing component).
- RED baseline covers PERS-01 (onSelect) and PERS-04 (PUT default_persona) plus the no-gate invariant.
- Zero component/source code touched.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-03-SUMMARY.md`.
</output>
