---
phase: 17-agent-personas
plan: 08
type: execute
wave: 3
depends_on: [17-05]
files_modified: []
autonomous: false
requirements: [PERS-01, PERS-04, PERS-05]
user_setup:
  - service: supabase
    why: "Apply migration 035 (persona pin columns) to the live dev Postgres — types come from config, not the DB, so build/unit checks pass WITHOUT the push, creating a false-positive verification state. The phase cannot pass UAT until the columns exist."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (needed for non-TTY `supabase db push`)"
    dashboard_config:
      - task: "Confirm threads.persona and user_preferences.default_persona columns exist after push"
        location: "Supabase Dashboard → Table Editor (dev project = the one in .env)"
must_haves:
  truths:
    - "Migration 035 is applied to the dev Supabase project so threads.persona and user_preferences.default_persona exist (D-08)"
    - "Any SYSTEM_PROMPT override is removed from local .env so the refactored operational base reaches the running dev app (Pitfall 6)"
    - "Prod (.env.prod) column creation is explicitly deferred to deploy per the dual-env discipline"
  artifacts:
    - path: "supabase/migrations/20240301000035_add_persona_columns.sql"
      provides: "the migration applied by this plan (authored in 17-05)"
      contains: "ADD COLUMN persona"
  key_links:
    - from: "supabase db push (dev)"
      to: "threads.persona + user_preferences.default_persona"
      via: "migration repair guard then db push"
      pattern: "db push"
---

<objective>
[BLOCKING] Apply migration 035 to the DEV Supabase project so the two persona pin columns physically exist, and remove any stale SYSTEM_PROMPT override from the local .env. Build/type/unit checks pass WITHOUT this push (persona types come from config + schemas, not the live DB) — so the phase would falsely appear complete. This plan closes that gap. Prod is deferred to deploy per the project's dual-env discipline.

Purpose: PERS-01/PERS-04/PERS-05 persistence require the live columns; D-08 (additive columns); Pitfall 6 (SYSTEM_PROMPT env-shadow); MEMORY (dual Supabase envs + migration-history repair).
Output: dev DB has the columns; local .env has no SYSTEM_PROMPT override; prod deferral recorded.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md
@supabase/migrations/20240301000035_add_persona_columns.sql

<interfaces>
<!-- MEMORY: .env = dev Supabase project; .env.prod = prod. `db push` may replay old migrations / "already exists" — run `supabase migration repair --status applied <range>` on the prior range FIRST, then push. Non-TTY needs SUPABASE_ACCESS_TOKEN. Migration head before this phase = 034; 035 is the only new file. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove any SYSTEM_PROMPT override from local .env (Pitfall 6)</name>
  <files>.env (local, gitignored — no repo change)</files>
  <read_first>
    - .planning/phases/17-agent-personas/17-RESEARCH.md §"Runtime State Inventory" + §"Pitfall 6" (SYSTEM_PROMPT env value shadows the repurposed base via pydantic-settings)
    - .planning/STATE.md (Phase 16 note: local .env SYSTEM_PROMPT override shadows the new default — must be removed)
  </read_first>
  <action>
    Check the repo-root .env (and .env.prod if present locally) for a `SYSTEM_PROMPT=` line. If present, remove it so the refactored config.py operational base (17-04) is the value pydantic-settings loads at runtime — otherwise all personas share a stale bundled prompt. Do NOT commit .env (gitignored); this is a local runtime fix. If SYSTEM_PROMPT is absent, record "no override present" and proceed.
  </action>
  <verify>
    <automated>! grep -qE "^SYSTEM_PROMPT=" .env 2>/dev/null && echo NO_SYSTEM_PROMPT_OVERRIDE</automated>
  </verify>
  <acceptance_criteria>
    - The command prints NO_SYSTEM_PROMPT_OVERRIDE (no active SYSTEM_PROMPT= line in .env).
    - If .env.prod exists locally, note whether it carries SYSTEM_PROMPT (its removal is a deploy-time action, flagged for 17-10/prod deploy — do not push prod here).
  </acceptance_criteria>
  <done>Local .env carries no SYSTEM_PROMPT override; the refactored operational base will reach the running dev app.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 035 to dev Supabase (repair guard → db push)</name>
  <files>supabase/migrations/20240301000035_add_persona_columns.sql (applied to dev DB)</files>
  <read_first>
    - supabase/migrations/20240301000035_add_persona_columns.sql (the migration to apply — authored in 17-05)
    - .planning/phases/17-agent-personas/17-RESEARCH.md §"Pitfall 7" (db push replays old migrations / "already exists" → migration repair first)
    - MEMORY reference_supabase_migration_history_repair (repair the prior applied range before push)
  </read_first>
  <action>
    Apply migration 035 to the DEV project (the credentials in .env). First, guard against Supabase migration-history drift (MEMORY / Pitfall 7): if a plain `supabase db push` reports "already exists" or tries to replay migrations < 035, run `supabase migration repair --status applied` for the prior applied range (the 000001..000034 series) first, then retry the push. Use SUPABASE_ACCESS_TOKEN for non-TTY auth. Run `supabase db push` (dev). Do NOT touch the prod project (.env.prod) — prod migration is deferred to deploy per the dual-env discipline. If the CLI cannot complete non-interactively (unsuppressable prompt), fall back to applying the two ADD COLUMN statements via the Supabase dashboard SQL editor against the dev project (Environment Availability fallback).
  </action>
  <verify>
    <automated>cd backend && python -c "from database import get_supabase; db=get_supabase(); db.table('threads').select('persona').limit(1).execute(); db.table('user_preferences').select('default_persona').limit(1).execute(); print('COLUMNS_EXIST')"</automated>
  </verify>
  <acceptance_criteria>
    - The python check prints COLUMNS_EXIST (a select of `persona` off threads and `default_persona` off user_preferences succeeds — a missing column would raise PostgREST 42703).
    - `supabase migration list` (or equivalent) shows 035 applied to the dev project.
    - The prod project was NOT modified (no push against .env.prod).
  </acceptance_criteria>
  <done>Dev Supabase has threads.persona + user_preferences.default_persona; migration history is clean; prod untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify columns in dev + acknowledge prod deferral</name>
  <files>(none — human verification only)</files>
  <action>Confirm with the developer that migration 035 landed on the dev project (both nullable columns present, existing rows NULL) and that the prod migration is deferred to deploy. Do not proceed to phase verification until this is confirmed.</action>
  <read_first>
    - .planning/phases/17-agent-personas/17-RESEARCH.md §"Runtime State Inventory" (the two runtime states: SYSTEM_PROMPT shadow + columns must exist in BOTH dev and prod projects; prod deferred)
  </read_first>
  <what-built>
    Migration 035 applied to the DEV Supabase project (threads.persona + user_preferences.default_persona), and the SYSTEM_PROMPT override removed from local .env.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → Table Editor for the DEV project (the one in .env).
    2. Confirm the `threads` table shows a nullable `persona` (text) column and `user_preferences` shows a nullable `default_persona` (text) column.
    3. Confirm no data was backfilled (existing rows show NULL) — the resolver defaults them to Board-Game Expert.
    4. Acknowledge that the PROD project (.env.prod) has NOT been migrated yet — that push happens at deploy time (dual-env discipline; verify prod against .env.prod during the milestone deploy).
  </how-to-verify>
  <acceptance_criteria>
    - User confirms both nullable columns are present in the dev project and existing rows are NULL (no backfill).
    - User acknowledges the prod migration is deferred to deploy.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the dev columns are confirmed, or describe any issue (e.g. push failed, column missing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → Supabase dev project | Schema DDL applied via the Supabase CLI with an owner access token. No user input crosses here — this is an operator action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-23 | Tampering (schema drift) | db push replaying old migrations | mitigate | Run `supabase migration repair --status applied` on the prior range before push (Pitfall 7 / MEMORY) so 035 applies cleanly without re-running < 035. |
| T-17-24 | Denial of Service (wrong project) | dual-env confusion | mitigate | Push targets ONLY the dev project (.env); prod (.env.prod) is explicitly deferred to deploy — no accidental prod DDL. |
| T-17-25 | Info Disclosure | new columns cross-user | accept | Own-row RLS (migration 032) already covers the new columns — no new policy needed (D-08). |
</threat_model>

<verification>
- Task 2 python check prints COLUMNS_EXIST against the dev project.
- `supabase migration list` shows 035 applied (dev).
- Prod project unmodified (no .env.prod push).
- Local .env has no SYSTEM_PROMPT override.
</verification>

<success_criteria>
- Dev Supabase physically has both nullable persona pin columns; migration history is clean.
- The SYSTEM_PROMPT env-shadow is removed locally so the new operational base is live in dev.
- Prod deferral is recorded for the milestone deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-08-SUMMARY.md`.
</output>
