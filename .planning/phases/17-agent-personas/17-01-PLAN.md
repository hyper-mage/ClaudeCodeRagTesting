---
phase: 17-agent-personas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_persona_resolution.py
  - backend/tests/test_persona_prompt.py
  - backend/tests/test_config.py
autonomous: true
requirements: [PERS-02, PERS-03, PERS-06]
must_haves:
  truths:
    - "A failing test pins that a null pin + null default resolves to board_game_expert (PERS-03)"
    - "A failing test pins that two back-to-back resolutions with different pins never cross (PERS-06)"
    - "A failing test pins that an unknown/removed persona id falls back to the default (D-10)"
    - "A failing test pins that the composed system prompt has no board-game framing for General Assistant and identical tools across personas (PERS-02/D-04)"
    - "A failing test pins that the operational base keeps citation guidance but drops the KB-first bias (D-02/D-03)"
  artifacts:
    - path: "backend/tests/test_persona_resolution.py"
      provides: "RED scaffold for the persona resolver + registry (PERS-03/06, D-10)"
      contains: "def test_"
    - path: "backend/tests/test_persona_prompt.py"
      provides: "RED scaffold for base+voice composition + tools-independence (PERS-02, D-01/D-04)"
      contains: "def test_"
    - path: "backend/tests/test_config.py"
      provides: "Extended assertions: base keeps citation, drops KB-first bias (D-02/D-03)"
      contains: "def test_system_prompt_operational_base"
  key_links:
    - from: "backend/tests/test_persona_resolution.py"
      to: "backend/services/persona_service.py + backend/routers/chat.py"
      via: "in-function imports of resolve_persona_id/get_persona_voice/_resolve_persona"
      pattern: "resolve_persona_id|_resolve_persona"
---

<objective>
Author the Wave 0 RED test scaffolds that pin the persona RESOLVER + REGISTRY + PROMPT-COMPOSITION contracts before any production code exists. These tests FAIL RED on first run (the imported symbols do not exist yet) and are turned GREEN by plans 17-04 (prompt core) and 17-06 (chat resolver).

Purpose: Nyquist Dimension 8 — every downstream implementation task has an automated test to satisfy. This plan pins PERS-03 (Expert default), PERS-06 (no-bleed), D-10 (unknown-id fallback), PERS-02 (General Assistant vanilla + full tools), and the D-02/D-03 base/voice split.
Output: 2 new backend test files + extended test_config.py assertions, all RED.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md

<interfaces>
<!-- Contracts these RED tests will import (authored by 17-04/17-06). Executor writes the tests AGAINST these signatures. -->

To be created in backend/services/persona_service.py (plan 17-04):
- DEFAULT_PERSONA_ID = "board_game_expert"
- PERSONAS: list[dict] with keys id, label, is_default, voice_block (2 entries: board_game_expert is_default=True, general_assistant is_default=False)
- list_personas() -> list[dict]   # [{id, label, is_default}] — no voice_block
- resolve_persona_id(pinned: str | None) -> str   # valid registry id wins, else DEFAULT_PERSONA_ID (D-10)
- get_persona_voice(persona_id: str) -> str   # voice_block for id; unknown → default voice

To be created in backend/routers/chat.py (plan 17-06), siblings to _safe_thread_model/_safe_user_default_model:
- _safe_thread_persona(thread_row: dict | None) -> str | None
- _safe_user_default_persona(db, user_id: str) -> str | None   # 42P01-tolerant
- _resolve_persona(db, user_id: str, thread_row: dict | None, body) -> str   # returns the resolved VOICE string, NOT cached

Composition contract (plan 17-04, backend/services/llm_service.py):
- stream_chat_completion(..., persona_voice: str | None = None) composes system_content = persona_voice + "\n\n" + settings.system_prompt + ("\n\n" + tool_guide if tool_guide) — the first "system_content" SSE event carries the final text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffold — persona resolver + registry (test_persona_resolution.py)</name>
  <files>backend/tests/test_persona_resolution.py</files>
  <read_first>
    - backend/tests/test_key_model_resolution.py (analog: _db_with_key_row MagicMock chain, _fake_settings, in-function imports so collection never breaks)
    - backend/routers/chat.py lines 144-296 (_safe_thread_model / _safe_user_default_model / _resolve_key_and_model — the tier-chain + 42P01 tolerance shape to mirror)
    - .planning/phases/17-agent-personas/17-RESEARCH.md §"Validation Architecture" + §"Code Examples" (resolver + registry helper signatures)
  </read_first>
  <action>
    Create backend/tests/test_persona_resolution.py mirroring test_key_model_resolution.py's structure: a `_db_with_persona_row(default_persona=None, pref_raises=False)` MagicMock helper whose `user_preferences.select("default_persona").eq().maybe_single().execute().data` returns `{"default_persona": ...}` (or None), or raises RuntimeError('relation "user_preferences" does not exist (42P01)') when `pref_raises`. Put ALL imports of `resolve_persona_id`, `get_persona_voice`, `DEFAULT_PERSONA_ID`, `PERSONAS` (from services.persona_service) and `_resolve_persona`, `_safe_thread_persona`, `_safe_user_default_persona` (from routers.chat) INSIDE the test functions so the module still collects (RED = per-test import/assertion failure, not a collection error). Write these tests:
    - test_registry_has_exactly_two_with_one_default: len(PERSONAS)==2 and exactly one is_default==True and DEFAULT_PERSONA_ID=="board_game_expert" (D-05/PERS-03).
    - test_null_pin_and_null_default_resolves_to_expert: `_resolve_persona(db, user, thread_row=None, body=object())` with a db whose default_persona is None returns get_persona_voice("board_game_expert") (PERS-03).
    - test_thread_pin_wins_over_user_default: thread_row={"persona":"general_assistant"} + db default_persona="board_game_expert" → resolves the general_assistant voice (tier order D-09).
    - test_user_default_used_when_no_thread_pin: thread_row=None + db default_persona="general_assistant" → general_assistant voice.
    - test_unknown_pinned_id_falls_back_to_default: thread_row={"persona":"__nonsense__"} → Expert voice; assert resolve_persona_id("__nonsense__")==DEFAULT_PERSONA_ID and no exception (D-10, Pitfall 5).
    - test_no_cross_thread_bleed: two back-to-back `_resolve_persona` calls with different thread pins return different voices (PERS-06, Pitfall 4).
    - test_user_preferences_absent_is_tolerated: pref_raises=True + thread_row=None → Expert voice (no crash; 42P01 tolerance).
    Use a plain object() (or a SimpleNamespace without a `persona` attr) as `body` so the resolver's `getattr(body, "persona", None)` tier is exercised as absent.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_persona_resolution.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_persona_resolution.py exists and pytest COLLECTS it with zero collection/syntax errors (imports are in-function).
    - Running the file FAILS RED: every test errors at the in-function import of a not-yet-existing symbol (services.persona_service / routers.chat._resolve_persona), NOT at a malformed assertion.
    - The 7 test functions above are present (grep `grep -c "def test_" backend/tests/test_persona_resolution.py` returns 7).
    - No production file is modified by this task.
  </acceptance_criteria>
  <done>test_persona_resolution.py collects and fails RED for the right reason (missing impl); 7 tests present covering PERS-03/06 + D-10 + tier order + 42P01 tolerance.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffold — base+voice composition + tools-independence (test_persona_prompt.py)</name>
  <files>backend/tests/test_persona_prompt.py</files>
  <read_first>
    - backend/services/llm_service.py lines 62-132 (current system_content assembly at ~90-92 — the composition seam that will gain persona_voice)
    - backend/config.py lines 95-109 (current system_prompt bundling opener + KB-first bias + operational rules)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"Pattern A3" (composition ordering: voice FIRST, then base, then tool_guide) + §"Pitfall 3" (tools never gated by persona)
  </read_first>
  <action>
    Create backend/tests/test_persona_prompt.py. In-function imports of `get_persona_voice`, `PERSONAS` (services.persona_service) and `stream_chat_completion` (services.llm_service). Because stream_chat_completion is a generator that opens an OpenAI client, drive it only far enough to read the FIRST yielded event (type "system_content") — mirror how other tests consume the generator's first event, and patch services.llm_service.get_llm_client so no network call is made; capture the composed system_content from that first event. Write:
    - test_general_assistant_prompt_has_no_board_game_framing: compose with persona_voice=get_persona_voice("general_assistant"); assert the general voice text is present and the phrase "board-game expert" / "Prefer the knowledge base" is ABSENT from the composed prompt (PERS-02/D-06).
    - test_expert_prompt_carries_kb_first_bias: compose with the board_game_expert voice; assert "Prefer the knowledge base" (KB-first bias) IS present (D-03).
    - test_base_operational_rules_present_for_both_personas: for BOTH personas, the composed prompt contains the base citation guidance ("Sources:") and the analyze_document guidance (D-02 base applies to every persona).
    - test_voice_leads_then_base: the persona voice substring appears BEFORE the base "Sources:" substring in the composed string (Pitfall 2 — exactly one "You are…" opener, voice first).
    - test_tools_are_persona_independent: call stream_chat_completion twice with the same `tools=[...]` list but different persona_voice values and assert the `tools` argument passed through is identical across personas — persona changes ONLY the system message, never the tools list (PERS-02/D-04, Pitfall 3). (If tools-passthrough is not observable from the generator, assert instead that tool_guide is appended for BOTH personas by checking the composed prompt contains the tool_guide text when tool_guide is supplied.)
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_persona_prompt.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_persona_prompt.py exists and COLLECTS with zero errors.
    - Running it FAILS RED at the in-function import of get_persona_voice / the persona_voice kwarg (not yet on stream_chat_completion).
    - 5 test functions present (`grep -c "def test_"` == 5) covering PERS-02 (no board-game framing + tools identical), D-02 (base for both), D-03 (Expert KB-first), Pitfall 2 (voice leads).
    - No production file modified.
  </acceptance_criteria>
  <done>test_persona_prompt.py collects and fails RED; pins the D-01/D-02/D-03/D-04 composition contract + PERS-02 tools-independence.</done>
</task>

<task type="auto">
  <name>Task 3: Extend test_config.py — operational base keeps citation, drops KB-first bias</name>
  <files>backend/tests/test_config.py</files>
  <read_first>
    - backend/tests/test_config.py lines 121-150 (existing web_search/depth tests + the SYSTEM_PROMPT env-isolation pattern via monkeypatch.delenv)
    - backend/tests/test_web_search.py lines 139-152 (test_system_prompt_citation_guidance — MUST stay GREEN; asserts "Sources:" + "inline" in prompt)
    - backend/config.py lines 95-109 (system_prompt to be stripped to base by 17-04)
  </read_first>
  <action>
    Append to backend/tests/test_config.py (do NOT edit existing tests) a new section with in-function `import config` after `monkeypatch.delenv("SYSTEM_PROMPT", raising=False)` (mirror test_web_search's env-isolation so a local .env override cannot mask the shipped default):
    - test_system_prompt_operational_base_keeps_citation: `config.Settings().system_prompt` still contains "Sources:" and "inline" (D-02 base retains citation guidance — keeps test_web_search::test_system_prompt_citation_guidance GREEN).
    - test_system_prompt_operational_base_drops_kb_first_bias: `config.Settings().system_prompt` does NOT contain "Prefer the knowledge base" (the KB-first bias moved to the Expert voice_block per D-03).
    - test_system_prompt_operational_base_drops_opener: `config.Settings().system_prompt` does NOT start with "You are a helpful assistant" (the opener moved into each persona voice_block per Pattern A1).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_config.py -k "operational_base"</automated>
  </verify>
  <acceptance_criteria>
    - The two/three new tests are appended to test_config.py; the file still collects and all PRE-EXISTING test_config tests are unmodified.
    - `pytest tests/test_config.py -k operational_base` FAILS RED (the current bundled system_prompt still contains "Prefer the knowledge base" and the opener).
    - Test names contain the substring "operational_base" so the -k filter selects exactly the new tests.
    - No production file modified.
  </acceptance_criteria>
  <done>test_config.py carries 3 RED assertions pinning the D-02/D-03 base/voice split; existing config tests untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan — no runtime code path, no new input crosses a boundary. Tests encode the security expectations that 17-04/17-06 must satisfy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Persona resolver (pinned id) | mitigate (pinned by test) | test_unknown_pinned_id_falls_back_to_default encodes D-10: a crafted/stale id must resolve to the default voice, never raise or inject — 17-06 must satisfy it. |
| T-17-02 | Information Disclosure | Cross-thread persona bleed | mitigate (pinned by test) | test_no_cross_thread_bleed encodes PERS-06: back-to-back resolutions with different pins must not cross — 17-06's non-cached resolver must satisfy it. |
| T-17-03 | Elevation of Privilege | Persona gating tools | accept (pinned by test) | test_tools_are_persona_independent encodes PERS-02/D-04 — no tool is gated by persona; General Assistant keeps full tool access. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_persona_resolution.py tests/test_persona_prompt.py -x` runs and reports RED (expected — impl not present).
- `cd backend && python -m pytest tests/test_config.py -k operational_base` runs and reports RED.
- `cd backend && python -m pytest tests/test_web_search.py::test_system_prompt_citation_guidance` still PASSES (regression guard — this plan does not touch config.py).
- No production file under backend/ (non-test) was modified.
</verification>

<success_criteria>
- 2 new backend test files + 3 appended test_config assertions exist, all failing RED for the correct reason (missing implementation).
- The RED baseline covers PERS-02, PERS-03, PERS-06, D-01/D-02/D-03/D-04/D-05/D-10.
- Zero production code touched.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-01-SUMMARY.md`.
</output>
