---
phase: 17-agent-personas
plan: 07
type: execute
wave: 3
depends_on: [17-05]
files_modified:
  - backend/routers/threads.py
  - backend/routers/preferences.py
autonomous: true
requirements: [PERS-01, PERS-04, PERS-05]
must_haves:
  truths:
    - "PATCH /api/threads/{id} accepts persona and writes it scoped by id+user_id without clobbering model (PERS-01/PERS-05)"
    - "A persona-only PATCH does not carry model; a model-only PATCH does not carry persona (exclude_unset)"
    - "PUT /api/preferences persists default_persona and GET returns it (null for new users) without clobbering theme/model (PERS-04)"
    - "Ownership is re-checked (.eq id .eq user_id → 404 on non-owned) before any persona write (IDOR)"
  artifacts:
    - path: "backend/routers/threads.py"
      provides: "PATCH switched to exclude_unset partial write accepting persona"
      contains: "exclude_unset"
    - path: "backend/routers/preferences.py"
      provides: "default_persona in GET/PUT select + response dicts"
      contains: "default_persona"
  key_links:
    - from: "backend/routers/threads.py PATCH"
      to: "threads.persona column"
      via: "body.model_dump(exclude_unset=True) update scoped by id+user_id"
      pattern: "model_dump\\(exclude_unset=True\\)"
    - from: "backend/routers/preferences.py"
      to: "user_preferences.default_persona"
      via: "select + return dict + exclude_unset upsert"
      pattern: "default_persona"
---

<objective>
Ship the persona WRITE + PERSISTENCE paths: extend PATCH /api/threads/{id} to accept `persona` via an exclude_unset partial update (so persona and model never clobber each other), and extend PUT/GET /api/preferences with `default_persona`. Turns the 17-02 thread-persona-patch + preferences-persona RED scaffolds GREEN.

Purpose: PERS-01 (chat picker PATCH target), PERS-04 (settings default), PERS-05 (persistence), D-08 write path. Mirrors the shipped favorite_models partial-upsert + the model-pin PATCH ownership re-check.
Output: threads.py PATCH (exclude_unset), preferences.py GET/PUT (default_persona).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md
@backend/routers/threads.py
@backend/routers/preferences.py

<interfaces>
<!-- From 17-05: models.schemas.ThreadUpdate {model?, persona?}, PreferencesResponse/PreferencesUpdate now carry default_persona. threads.py already imports ThreadUpdate (renamed in 17-05). -->
Current PATCH (threads.py:58-89) hardcodes update({"model": body.model}) with an ownership re-check (.eq id .eq user_id). preferences.py GET (37-60) + PUT (63-96) already use exclude_unset for the upsert and null-tolerant return dicts for default_model/theme/favorite_models.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH /api/threads/{id} — exclude_unset partial write accepting persona</name>
  <files>backend/routers/threads.py</files>
  <read_first>
    - backend/routers/threads.py lines 58-89 (update_thread_model — keep the ownership re-check L70-79 verbatim; replace only the hardcoded update payload)
    - backend/routers/preferences.py line 75 (the body.model_dump(exclude_unset=True) mechanism to adopt)
    - backend/tests/test_thread_persona_patch.py + backend/tests/test_thread_model_patch.py (the contracts both must satisfy after the switch)
  </read_first>
  <behavior>
    - PATCH {"persona":"general_assistant"} → update patch == {"persona":"general_assistant"} (no "model" key), scoped by id+user_id.
    - PATCH {"model":"x/y"} → update patch == {"model":"x/y"} (no "persona" key) — the model-pin path stays intact.
    - PATCH on a non-owned thread → 404 and no update call (IDOR).
  </behavior>
  <action>
    In backend/routers/threads.py, edit the PATCH handler (currently update_thread_model, body already typed ThreadUpdate after 17-05): keep the ownership re-check block (select id .eq id .eq user_id .maybe_single → 404 if no row) verbatim. Replace the hardcoded `update({"model": body.model})` with `patch = body.model_dump(exclude_unset=True)` then `db.table("threads").update(patch).eq("id", thread_id).eq("user_id", user_id).execute()`. Return updated.data[0]. Update the handler docstring to describe the partial write (persona OR model, exclude_unset avoids clobbering the sibling field; a `{model: null}` explicit body still clears because null is set → included). Optionally rename the function to `update_thread` for accuracy (update any local references only — it is not imported elsewhere).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_thread_persona_patch.py tests/test_thread_model_patch.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model_dump(exclude_unset=True)" backend/routers/threads.py` == 1 in the PATCH handler.
    - test_thread_persona_patch.py turns fully GREEN (persona set + no-clobber-model + model-only-still-works + 404 non-owned).
    - test_thread_model_patch.py stays GREEN (model set + null clear + 404) — the exclude_unset switch preserves the explicit-null-clear behavior because null is a set value.
    - The ownership re-check (.eq id .eq user_id → 404) is unchanged.
  </acceptance_criteria>
  <done>PATCH writes persona/model via exclude_unset without clobbering the sibling; both thread-patch test suites GREEN; IDOR re-check intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PUT/GET /api/preferences — default_persona</name>
  <files>backend/routers/preferences.py</files>
  <read_first>
    - backend/routers/preferences.py lines 37-96 (GET select + return dict at 45-60; PUT exclude_unset upsert + read-back at 75-96 — clone the favorite_models threading)
    - backend/tests/test_preferences_api.py (the default_persona roundtrip + no-clobber tests from 17-02, plus the existing favorite_models block as the exact analog)
  </read_first>
  <behavior>
    - GET for a user with no row → default_persona is None (alongside default_model None, theme "dark", favorite_models []).
    - PUT {"default_persona":"general_assistant"} → upsert payload carries default_persona (+ user_id + updated_at); GET/echo returns it.
    - PUT {"theme":"light"} → upsert payload does NOT contain default_persona (exclude_unset — no clobber). PUT {"default_persona":...} → payload does NOT contain default_model/theme.
  </behavior>
  <action>
    In backend/routers/preferences.py: add `default_persona` to BOTH `.select("default_model, theme, favorite_models")` strings (the GET select ~L48 and the PUT read-back select ~L84) so it becomes `.select("default_model, theme, favorite_models, default_persona")`. Add `"default_persona": row.data.get("default_persona")` to ALL THREE return dicts (GET row-present ~L55-60, GET no-row default ~L54, PUT read-back both branches ~L89-96) — for the no-row branches use `"default_persona": None`. The PUT upsert path itself is already generic via `body.model_dump(exclude_unset=True)` (L75) and user_id stays JWT-bound (L76) — no logic change there, only the select strings + return dicts.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_preferences_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Both `.select(...)` strings in preferences.py include default_persona; all return dicts (4 total: GET no-row, GET row, PUT no-row, PUT row) include a default_persona key.
    - test_preferences_api.py -k persona turns fully GREEN (roundtrip + theme-only-no-clobber + persona-only-no-clobber + new-user-null).
    - The existing favorite_models/theme/default_model preference tests stay GREEN (`pytest tests/test_preferences_api.py` full file passes).
  </acceptance_criteria>
  <done>default_persona persists via PUT and returns via GET (null for new users) without clobbering siblings; preferences suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /api/threads/{id} | Untrusted persona id written to threads.persona; must be scoped to the caller's own thread. |
| client → PUT /api/preferences | Untrusted default_persona written to the caller's own preferences row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-19 | Elevation of Privilege (IDOR) | PATCH threads persona | mitigate | Ownership re-check `.eq("id",tid).eq("user_id",uid)` returns 404 for non-owned threads before any write (test_thread_persona_patch 404 test). |
| T-17-20 | Tampering (field clobber) | exclude_unset partial writes | mitigate | model_dump(exclude_unset=True) on both PATCH and PUT ensures a persona-only write cannot null model/theme/default_model and vice-versa (no-clobber tests). |
| T-17-21 | Tampering (cross-user write) | PUT preferences user_id | mitigate | user_id is bound from Depends(get_user_id) (JWT), never from the body (unchanged, T-13-02). |
| T-17-22 | Tampering | raw persona string stored | accept | The stored id is validated only at read time by the resolver (D-10, 17-06) — no CHECK/FK by design; own-row RLS scopes the row. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_thread_persona_patch.py tests/test_thread_model_patch.py tests/test_preferences_api.py -x` all GREEN.
- `cd backend && python -m pytest` full backend suite GREEN.
- The ownership re-check and JWT-bound user_id are unchanged (grep-confirm .eq("user_id", user_id) present in both writes).
</verification>

<success_criteria>
- PATCH accepts persona via exclude_unset (no sibling clobber); PUT/GET preferences roundtrip default_persona; 17-02 write-path RED scaffolds GREEN.
- IDOR re-check + JWT-bound user_id preserved; the model-pin PATCH and existing preference tests stay GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-07-SUMMARY.md`.
</output>
