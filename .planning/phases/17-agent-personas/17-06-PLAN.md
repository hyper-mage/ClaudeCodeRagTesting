---
phase: 17-agent-personas
plan: 06
type: execute
wave: 3
depends_on: [17-04, 17-05]
files_modified:
  - backend/routers/personas.py
  - backend/main.py
  - backend/routers/chat.py
autonomous: true
requirements: [PERS-01, PERS-03, PERS-06]
must_haves:
  truths:
    - "GET /api/personas returns the auth-gated catalog [{id,label,is_default}] (PERS-01/D-07)"
    - "The persona resolves per chat turn via a non-cached sibling resolver: thread pin → user default → system default (Board-Game Expert) (D-09/PERS-03)"
    - "An unknown/removed persona pin falls back to the default voice with no crash (D-10)"
    - "The resolved persona_voice is passed into stream_chat_completion once per turn with no cross-user/thread bleed (PERS-06)"
    - "tool_guide / tools remain unchanged for every persona (D-04/PERS-02)"
  artifacts:
    - path: "backend/routers/personas.py"
      provides: "GET /api/personas endpoint (clone of models.py)"
      contains: "/api/personas"
    - path: "backend/routers/chat.py"
      provides: "_safe_thread_persona/_safe_user_default_persona/_resolve_persona + call site + stream kwarg"
      contains: "_resolve_persona"
  key_links:
    - from: "backend/routers/chat.py event_generator"
      to: "stream_chat_completion(persona_voice=...)"
      via: "per-turn _resolve_persona then kwarg"
      pattern: "persona_voice=persona_voice"
    - from: "backend/main.py"
      to: "routers.personas.router"
      via: "app.include_router"
      pattern: "personas.router"
---

<objective>
Ship the persona READ + RESOLUTION seam: a new GET /api/personas endpoint (clone of models.py), its registration in main.py, and the per-turn `_resolve_persona` sibling resolver in chat.py that resolves the persona voice (thread pin → user default → system default) and threads it into stream_chat_completion. Turns the 17-01 resolver tests and 17-02 personas_api tests GREEN.

Purpose: PERS-01 (catalog for the picker), PERS-03 (Expert system-default fallback), PERS-06 (per-request no-bleed), D-04/D-07/D-09/D-10/D-11.
Output: personas.py router, main.py registration, chat.py resolver + call site + stream kwarg.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md
@backend/routers/models.py
@backend/main.py

<interfaces>
<!-- From 17-04: services.persona_service.list_personas/resolve_persona_id/get_persona_voice, DEFAULT_PERSONA_ID. From 17-05: models.schemas.PersonaResponse. -->
chat.py existing resolver anchors: _safe_thread_model (L144-150), _safe_user_default_model (L153-171), _resolve_key_and_model (L224-295), event_generator call site (L898-901), stream_chat_completion call (L1077-1086). stream_chat_completion now accepts persona_voice (17-04). MessageCreate has NO persona field — getattr(body,"persona",None) is the future-override seam (A4), harmless today.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GET /api/personas router + register in main.py</name>
  <files>backend/routers/personas.py, backend/main.py</files>
  <read_first>
    - backend/routers/models.py lines 24-53 (auth-gated GET catalog pattern — Depends(get_user_id), response_model=list[...])
    - backend/main.py lines 9, 64-71 (import line + include_router block)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"backend/routers/personas.py (NEW router)"
  </read_first>
  <action>
    Create backend/routers/personas.py: APIRouter(prefix="/api/personas", tags=["personas"]); import get_user_id from auth, PersonaResponse from models.schemas, list_personas from services.persona_service. Add `@router.get("", response_model=list[PersonaResponse])` handler `get_personas(user_id: str = Depends(get_user_id)) -> list[dict]` that returns `list_personas()` (no DB, no refresh — the catalog is a code constant). Include a short module docstring noting voice_block is never shipped (A5) and the route is auth-gated (codebase norm). Then edit backend/main.py: add `personas` to the `from routers import ...` line (L9) and add `app.include_router(personas.router)` alongside the other include_router calls (after preferences.router).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_personas_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - backend/routers/personas.py exists with the auth-gated GET returning list_personas().
    - main.py imports personas and calls app.include_router(personas.router) exactly once (`grep -c "personas.router" backend/main.py` == 1).
    - test_personas_api.py turns fully GREEN (200 + [{id,label,is_default}], exactly one default == board_game_expert, voice_block absent, auth-gated).
  </acceptance_criteria>
  <done>GET /api/personas is live and registered; test_personas_api.py GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the per-turn persona resolver siblings to chat.py</name>
  <files>backend/routers/chat.py</files>
  <read_first>
    - backend/routers/chat.py lines 144-171 (_safe_thread_model / _safe_user_default_model — clone their exact shape incl. the 42P01 try/except + scrub_secrets debug log)
    - backend/routers/chat.py lines 224-231 (the "NOT @lru_cache'd — called once per turn" no-bleed rationale to mirror)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"backend/routers/chat.py — per-turn persona resolver" (do NOT widen _resolve_key_and_model's tuple — Pitfall 8)
  </read_first>
  <behavior>
    - _safe_thread_persona({"persona":"general_assistant"}) == "general_assistant"; _safe_thread_persona(None) is None.
    - _safe_user_default_persona tolerates a raised query (pre-migration 42P01) and returns None without crashing.
    - _resolve_persona(db, user, thread_row=None, body) with no pins returns get_persona_voice(DEFAULT_PERSONA_ID); a thread pin wins over the user default; an unknown pin resolves to the default voice.
  </behavior>
  <action>
    In backend/routers/chat.py, add three module-level siblings NEXT TO the model helpers (do NOT modify _resolve_key_and_model or widen its 4-tuple — Pitfall 8): `_safe_thread_persona(thread_row: dict | None) -> str | None` (returns thread_row.get("persona") if thread_row else None — absent-key read off the SELECT * row); `_safe_user_default_persona(db, user_id: str) -> str | None` (clone _safe_user_default_model exactly but select "default_persona"; wrap in try/except swallowing the 42P01 pre-migration error with a logger.debug(scrub_secrets(...)) and returning None); and `_resolve_persona(db, user_id: str, thread_row: dict | None, body) -> str` that computes `pinned = getattr(body, "persona", None) or _safe_thread_persona(thread_row) or _safe_user_default_persona(db, user_id)` then `return get_persona_voice(resolve_persona_id(pinned))` (D-09 tier chain + D-10 validate). Import resolve_persona_id and get_persona_voice from services.persona_service at the top of chat.py. Add a docstring noting it is NOT @lru_cache'd (per-turn no-bleed, PERS-06/Pitfall 4).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_persona_resolution.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The three functions exist in chat.py; `grep -c "def _resolve_persona" backend/routers/chat.py` == 1.
    - _resolve_key_and_model is byte-for-byte unchanged (still returns a 4-tuple) — the existing test_key_model_resolution.py suite still passes.
    - test_persona_resolution.py turns fully GREEN (tier order, unknown-id fallback, no-bleed, 42P01 tolerance, Expert default).
  </acceptance_criteria>
  <done>chat.py has the non-cached persona resolver siblings; test_persona_resolution.py GREEN; the model resolver + its tests untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the resolver into event_generator + stream_chat_completion call</name>
  <files>backend/routers/chat.py</files>
  <read_first>
    - backend/routers/chat.py lines 890-901 (the per-request key/model resolve inside event_generator — add the persona resolve immediately after)
    - backend/routers/chat.py lines 1077-1086 (the stream_chat_completion kwargs — add persona_voice)
    - backend/routers/chat.py around 963-980 (the tools list — MUST stay persona-independent, Pitfall 3/D-04)
  </read_first>
  <action>
    In backend/routers/chat.py event_generator: immediately after the existing `api_key, model, mode, is_user_key = _resolve_key_and_model(db, user_id, thread.data, body)` (inside the same try block, same per-request scope), add `persona_voice = _resolve_persona(db, user_id, thread.data, body)`. Then in the stream_chat_completion(...) call (~L1077-1086), add the kwarg `persona_voice=persona_voice` alongside api_key/model/trace. Do NOT touch the `tools=tools` assembly or the TOOL_SELECTION_GUIDE append (they remain persona-independent — D-04/PERS-02).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_persona_resolution.py tests/test_persona_prompt.py tests/test_key_model_resolution.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "persona_voice = _resolve_persona" backend/routers/chat.py` == 1 and `grep -c "persona_voice=persona_voice" backend/routers/chat.py` == 1.
    - The persona resolve sits inside the same per-request event_generator scope as the key/model resolve (not module/global — no-bleed, PERS-06).
    - The `tools=tools` argument and TOOL_SELECTION_GUIDE append are unchanged (grep-confirm they still key on doc/web availability, not persona).
    - `cd backend && python -m pytest` — full backend suite passes (persona wiring green; no model/key regression).
  </acceptance_criteria>
  <done>event_generator resolves persona once per turn and passes persona_voice into the stream call; tools stay persona-independent; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JWT → GET /api/personas | Auth-gated read of a non-secret catalog. |
| stored/pinned persona id → LLM system message | The per-turn resolver converts an untrusted pinned id into a server-defined voice string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | Spoofing/Auth | GET /api/personas | mitigate | Depends(get_user_id) (JWT) gates the route, matching every other router (test_personas_api asserts the gate). |
| T-17-16 | Tampering/Injection | pinned persona id → voice | mitigate | _resolve_persona calls resolve_persona_id first; an unknown/crafted id resolves to the default voice (D-10) — only registry voice_block strings reach the system message (test_persona_resolution unknown-id test). |
| T-17-17 | Information Disclosure | cross-user/thread bleed | mitigate | _resolve_persona is a plain per-turn function (NOT @lru_cache'd), called in event_generator's per-request scope — mirrors the proven no-bleed key/model resolver (PERS-06). |
| T-17-18 | Elevation of Privilege | persona gating tools | accept | tools + TOOL_SELECTION_GUIDE are untouched and persona-independent (D-04) — no persona can restrict or widen tool access. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_personas_api.py tests/test_persona_resolution.py tests/test_persona_prompt.py -x` all GREEN.
- `cd backend && python -m pytest tests/test_key_model_resolution.py -x` still GREEN (resolver tuple unchanged).
- `cd backend && python -m pytest` full suite GREEN.
- `grep -rn "TOOL_SELECTION_GUIDE" backend/routers/chat.py` shows the append still unconditional for tools (D-04).
</verification>

<success_criteria>
- GET /api/personas is live + registered; the per-turn persona resolver + stream wiring are in place; 17-01 resolver + 17-02 personas_api RED scaffolds are GREEN.
- No cross-user/thread bleed (non-cached per-request resolution); unknown pin → default (D-10); tools persona-independent (D-04).
- The model/key resolver and its ~15 tests are untouched (Pitfall 8 avoided).
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-06-SUMMARY.md`.
</output>
