---
phase: 17-agent-personas
plan: 12
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/pages/ChatPage.tsx
  - frontend/src/pages/ChatPage.test.tsx
autonomous: true
requirements: [PERS-01, PERS-03, PERS-04, PERS-05]
must_haves:
  truths:
    - "For a new/unpinned thread the chat-header picker DISPLAYS the effective active persona (Board-Game Expert) instead of the 'Select a persona' placeholder (SC-1)"
    - "When a user default persona is set in settings, the new-chat picker DISPLAYS that chosen default (SC-3)"
    - "A pinned thread persona is displayed on the picker and remains visible after reload (SC-4)"
    - "The display fallback NEVER fires a PATCH — only an explicit user selection pins thread.persona (locked display-only constraint)"
  artifacts:
    - path: "frontend/src/pages/ChatPage.tsx"
      provides: "userDefaultPersona state captured from GET /api/preferences.default_persona + a computed displayed picker value passed to ChatContainer"
      contains: "userDefaultPersona"
    - path: "frontend/src/pages/ChatPage.test.tsx"
      provides: "picker-display fallback spec (SC-1/SC-3/SC-4) + a no-PATCH-on-display assertion"
      contains: "is_default"
  key_links:
    - from: "frontend/src/pages/ChatPage.tsx (/api/preferences fetch)"
      to: "userDefaultPersona state"
      via: "setUserDefaultPersona(prefs.default_persona ?? null)"
      pattern: "default_persona"
    - from: "frontend/src/pages/ChatPage.tsx (ChatContainer render)"
      to: "ChatContainer threadPersona prop"
      via: "activeThread?.persona ?? userDefaultPersona ?? personas?.find(p => p.is_default)?.id ?? null"
      pattern: "userDefaultPersona"
---

<objective>
Close Gap 1 from 17-VERIFICATION.md: the chat-header persona picker shows the placeholder "Select a persona" for any unpinned thread even though the backend resolves NULL → user default → Board-Game Expert. The feature works; the UI does not communicate which persona is live. Fix is DISPLAY-ONLY: mirror the backend resolver's tier chain in the value passed to PersonaSelector so the picker reflects the effective persona, WITHOUT firing any write.

Purpose: unblock SC-1 (picker shows Expert on a new chat), SC-3 (picker shows the chosen settings default on a new chat), SC-4 (a pinned persona is visible and survives reload). Requirements PERS-01/PERS-03/PERS-04/PERS-05.
Output: ChatPage.tsx (capture `default_persona` from the existing /api/preferences fetch + compute the displayed picker value), ChatPage.test.tsx (fallback + no-PATCH spec).

LOCKED CONSTRAINT (17-CONTEXT D-12 + VERIFICATION Gap 1): the display fallback MUST NOT trigger a PATCH. Only an explicit user click through PersonaSelector.onSelect → handleThreadPersonaChange pins `thread.persona`. Do NOT add any auto-persist / auto-PATCH behavior anywhere in this plan.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-VERIFICATION.md
@frontend/src/pages/ChatPage.tsx
@frontend/src/components/PersonaSelector.tsx

<interfaces>
<!-- Contracts the executor needs — no codebase exploration required. -->

From frontend/src/components/PersonaSelector.tsx (shipped 17-09):
- `PersonaOption` = { id: string; label: string; is_default: boolean }  (voice_block withheld server-side, A5)
- Props: { value: string | null; onSelect: (personaId: string) => void; personas?: PersonaOption[]; placeholder?: string }
- Trigger label logic: `options.find(p => p.id === value)?.label` else `placeholder ?? 'Select a persona'`.
  So passing the RESOLVED persona id as `value` makes the trigger render that persona's label — this is the entire display fix.

From frontend/src/pages/ChatPage.tsx (current shipped state, 17-10):
- `Thread` interface already carries `persona: string | null` (L23-24).
- `personas` state is already fetched once from GET /api/personas (L36, L86-94) and passed to ChatContainer (L260).
- The /api/preferences fetch effect (L101-117) currently types its payload `{ theme?: string | null } | null` and reads ONLY theme.
- The ChatContainer render passes `threadPersona={activeThread?.persona ?? null}` (L258) — THIS is the buggy expression.
- `handleThreadPersonaChange` (L146-163) is the ONLY persona PATCH path; it must stay untouched.

From backend (already GREEN — do NOT touch): GET /api/preferences returns `default_persona` (preferences.py); catalog ids are `board_game_expert` (label "Board-Game Expert", is_default true, the system default) and `general_assistant` (label "General Assistant", is_default false).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatPage.tsx — capture default_persona + compute the displayed picker value (display-only)</name>
  <files>frontend/src/pages/ChatPage.tsx</files>
  <read_first>
    - frontend/src/pages/ChatPage.tsx (FULL — the `personas` state L36, the /api/preferences fetch effect L101-117, the `handleThreadPersonaChange` PATCH handler L146-163 which MUST stay unchanged, and the ChatContainer render L249-263 where `threadPersona={activeThread?.persona ?? null}` lives at L258)
    - frontend/src/components/PersonaSelector.tsx (trigger-label logic: `value` matching a `PersonaOption.id` renders that option's `label`, else the placeholder — this is why passing a resolved id fixes the display)
    - .planning/phases/17-agent-personas/17-VERIFICATION.md §"Gap 1" (root cause + the exact display-only fix expression; NO auto-PATCH)
  </read_first>
  <action>
    Edit frontend/src/pages/ChatPage.tsx (display-only — add NO write/PATCH):
    - Add a `userDefaultPersona` state next to `personas` (L36): `const [userDefaultPersona, setUserDefaultPersona] = useState<string | null>(null)`. This mirrors the backend's user-default tier for DISPLAY only.
    - Widen the /api/preferences fetch effect (L101-117): change its `.then` param type from `{ theme?: string | null } | null` to `{ theme?: string | null; default_persona?: string | null } | null`, and inside the existing handler (after the `if (cancelled || !prefs) return` guard and the theme reconcile) call `setUserDefaultPersona(prefs.default_persona ?? null)`. Best-effort, silent on failure like the theme reconcile — capture the value, do NOT write it back and do NOT call handleThreadPersonaChange.
    - Change the ChatContainer `threadPersona` prop (L258) from `activeThread?.persona ?? null` to `activeThread?.persona ?? userDefaultPersona ?? personas?.find(p => p.is_default)?.id ?? null`. This mirrors the resolver tier chain (thread pin → user default → system default via the `is_default` catalog row) purely for display; the terminal `?? null` keeps the prop type `string | null`.
    - Leave `handleThreadPersonaChange` and its PATCH call byte-for-byte unchanged — it stays the ONLY code path that pins `thread.persona`, and it fires only on an explicit PersonaSelector.onSelect. The computed display value must never reach it.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (TypeScript strict — the widened preferences payload type and the new state type-check).
    - `grep -c "userDefaultPersona" frontend/src/pages/ChatPage.tsx` >= 3 (state declaration + setter call + the threadPersona expression).
    - The threadPersona prop expression contains the literal chain `activeThread?.persona ?? userDefaultPersona ?? personas?.find(p => p.is_default)?.id` (grep-confirm).
    - The /api/preferences handler reads `prefs.default_persona` and calls `setUserDefaultPersona` (grep-confirm `default_persona` appears in the effect).
    - DISPLAY-ONLY invariant: no new `method: 'PATCH'` call to `/api/threads` was added — `grep -c "method: 'PATCH'" frontend/src/pages/ChatPage.tsx` is unchanged (still exactly the two pre-existing model + persona handlers), and `handleThreadPersonaChange` is unmodified (still the sole persona PATCH path).
  </acceptance_criteria>
  <done>ChatPage captures the user default persona from /api/preferences and passes a resolver-mirroring displayed value to the picker; no auto-PATCH was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: ChatPage.test.tsx — picker-display fallback spec (SC-1/SC-3/SC-4) + no-PATCH-on-display + validate</name>
  <files>frontend/src/pages/ChatPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/ChatPage.test.tsx (the `routeApiFetch` helper L45-69, `renderChatPage` L31-36, and the Thread-selection pattern in Test 8 L265-344 — clicking a sidebar thread title sets activeThreadId so the header row renders)
    - frontend/src/components/PersonaSelector.tsx (the trigger is a `<button>` whose accessible name is the selected persona's `label`, else the placeholder "Select a persona")
    - frontend/src/pages/ChatPage.tsx (the new fallback expression + userDefaultPersona from Task 1)
  </read_first>
  <action>
    Extend frontend/src/pages/ChatPage.test.tsx. First, extend the `routeApiFetch` helper (or add a local variant) so `apiFetch` also resolves GET `/api/personas` → a two-row catalog `[{id:'board_game_expert',label:'Board-Game Expert',is_default:true},{id:'general_assistant',label:'General Assistant',is_default:false}]` and GET `/api/preferences` → a configurable object `{ theme: null, default_persona: <value> }`. Keep the existing /api/threads routing intact so current tests are unaffected.
    Add a `describe('ChatPage persona picker display fallback (Gap 1 / SC-1,3,4)')` block with three `it`s. In each, seed `threadsList` with one thread `{ id:'t1', title:'Thread 1', created_at:'', updated_at:'', model:null, persona:<case> }`, render, click the sidebar 'Thread 1' title (mirror Test 8's `findAllByText('Thread 1')[0]` click) to activate it, then assert the PersonaSelector trigger button's label via `screen.findByRole('button', { name: <expected label> })`:
    - SC-1: thread persona `null`, preferences `default_persona: null` → trigger shows 'Board-Game Expert' (the `is_default` catalog row), and `screen.queryByRole('button', { name: 'Select a persona' })` is null.
    - SC-3: thread persona `null`, preferences `default_persona: 'general_assistant'` → trigger shows 'General Assistant'.
    - SC-4: thread persona `'general_assistant'` (a pin), preferences `default_persona: null` → trigger shows 'General Assistant' (pin visible/survives the thread read).
    Add a fourth `it` asserting DISPLAY-ONLY: for the SC-1 case (unpinned thread rendered), no PATCH fired — `mockedApiFetch.mock.calls.find(([p,o]) => /^\/api\/threads\/[^/]+$/.test(p) && (o as RequestInit|undefined)?.method === 'PATCH')` is undefined (the resolved default is shown WITHOUT persisting it).
    Note: the header row (and thus the picker) only renders when `activeThreadId !== null`, so the sidebar-click activation step is required in every case. Use `findBy*`/`waitFor` so the /api/personas + /api/preferences fetches resolve before asserting.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx vitest run src/pages/ChatPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` passes and `npx vitest run src/pages/ChatPage.test.tsx` is GREEN including the new fallback block.
    - SC-1 asserts the trigger shows 'Board-Game Expert' AND that 'Select a persona' is NOT present (the placeholder-blank symptom is gone).
    - SC-3 asserts the trigger shows 'General Assistant' when preferences.default_persona is 'general_assistant'.
    - SC-4 asserts a pinned persona ('general_assistant') is displayed after the thread read.
    - The no-PATCH `it` proves rendering an unpinned thread fires no PATCH /api/threads (display-only invariant).
    - The full suite still passes: `cd frontend && npx vitest run` reports no regressions (>= the prior 133 plus the new cases).
  </acceptance_criteria>
  <done>A vitest spec pins the resolver-mirroring picker display for SC-1/SC-3/SC-4 and proves the display fallback never PATCHes; build + suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/preferences | Auth-gated read; the client now also reads the non-secret `default_persona` for display. |
| computed display value → (no write) | The resolver-mirroring picker value is render-only; it never crosses back to a PATCH. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-32 | Tampering | display fallback silently persisting a default | mitigate | The fallback is computed inline for the `threadPersona` prop only; `handleThreadPersonaChange` (the sole PATCH path) is untouched and fires only on explicit onSelect — Task 1 acceptance greps the unchanged PATCH count, and Task 2 asserts no PATCH on display. |
| T-17-33 | Info Disclosure | `default_persona` in the browser | accept | Only the non-secret preferences field (a persona id) is read client-side; no voice_block/prompt text is exposed. |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds (ChatPage.tsx + ChatPage.test.tsx type-check under strict mode).
- `cd frontend && npx vitest run` — the new picker-display fallback block is GREEN and no existing spec regresses.
- Manual re-test alignment (17-11 UAT): a new/unpinned chat shows 'Board-Game Expert' in the header picker (SC-1); setting the settings default to General Assistant makes a new chat's picker read 'General Assistant' (SC-3); a pinned persona is visible after reload (SC-4) — no PATCH fires from merely opening a chat.
</verification>

<success_criteria>
- The chat-header persona picker reflects the effective active persona (thread pin → user default → system default) for every thread, replacing the blank "Select a persona" placeholder — SC-1/SC-3/SC-4 unblocked.
- The fix is strictly display-only: no auto-PATCH is introduced; explicit selection remains the only way to pin a thread persona.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-12-SUMMARY.md`.
</output>
