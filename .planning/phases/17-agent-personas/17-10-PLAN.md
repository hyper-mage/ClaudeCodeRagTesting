---
phase: 17-agent-personas
plan: 10
type: execute
wave: 5
depends_on: [17-08, 17-09]
files_modified:
  - frontend/src/pages/ChatPage.tsx
  - frontend/src/components/ChatContainer.tsx
  - frontend/src/pages/SettingsPage.tsx
autonomous: true
requirements: [PERS-01, PERS-04, PERS-05]
must_haves:
  truths:
    - "The chat header shows a PersonaSelector beside the model selector; selecting a persona PATCHes the thread and updates optimistically (PERS-01)"
    - "The persona list is fetched once from GET /api/personas and passed to both pickers — never hardcoded (D-07)"
    - "A thread's persona seeds the chat picker from the thread read on reopen (PERS-05)"
    - "The settings page renders a DefaultPersonaSelector seeded from GET /api/preferences.default_persona (PERS-04)"
  artifacts:
    - path: "frontend/src/pages/ChatPage.tsx"
      provides: "persona on Thread + /api/personas fetch + handleThreadPersonaChange (PATCH, no gate) + prop pass-through"
      contains: "handleThreadPersonaChange"
    - path: "frontend/src/components/ChatContainer.tsx"
      provides: "renders PersonaSelector in the thread header row"
      contains: "PersonaSelector"
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "renders DefaultPersonaSelector seeded from preferences"
      contains: "DefaultPersonaSelector"
  key_links:
    - from: "frontend/src/pages/ChatPage.tsx"
      to: "/api/threads/{id} PATCH {persona}"
      via: "handleThreadPersonaChange optimistic update (no useKeyGate)"
      pattern: "handleThreadPersonaChange"
    - from: "frontend/src/pages/ChatPage.tsx"
      to: "ChatContainer PersonaSelector"
      via: "threadPersona + personas + onThreadPersonaChange props"
      pattern: "threadPersona"
---

<objective>
Wire the two persona pickers into the running app: fetch the catalog once (GET /api/personas), render PersonaSelector in the chat header (with an optimistic PATCH handler that carries NO key gate), seed it from the thread's persona on reopen (PERS-05), and render DefaultPersonaSelector on the settings page seeded from GET /api/preferences.default_persona. This is the last implementation wave; end-to-end verification is 17-11.

Purpose: PERS-01 (chat picker live), PERS-04 (settings default live), PERS-05 (restore from thread), D-07 (fetched list), D-11/D-12 (per-thread pin, picker-only). Mirrors the model-pin wiring MINUS useKeyGate.
Output: ChatPage.tsx (persona state + handler + fetch + pass-through), ChatContainer.tsx (render), SettingsPage.tsx (render).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-PATTERNS.md
@frontend/src/pages/ChatPage.tsx
@frontend/src/components/ChatContainer.tsx
@frontend/src/pages/SettingsPage.tsx

<interfaces>
<!-- From 17-09: PersonaSelector { value, onSelect, personas } and DefaultPersonaSelector { value, onChange, personas }. From 17-06: GET /api/personas → [{id,label,is_default}]. From 17-05/17-07: threads carry `persona`; GET /api/preferences returns default_persona. -->
Analogs to mirror MINUS the gate:
- ChatPage Thread interface (15-22) + handleThreadModelChange (103-120) + the /api/models mount fetch (67-75) + /api/preferences theme reconcile (82-98) + the ChatContainer props (206-217).
- ChatContainer header row (91-116) rendering ModelSelector.
- SettingsPage /api/models fetch (36-44) + /api/preferences seed (48-57) + DefaultModelSelector render (169-171).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatPage.tsx — persona state, catalog fetch, PATCH handler, prop pass-through</name>
  <files>frontend/src/pages/ChatPage.tsx</files>
  <read_first>
    - frontend/src/pages/ChatPage.tsx (FULL — Thread interface L15-22, handleThreadModelChange L103-120, the /api/models mount fetch L67-75, the ChatContainer render L206-217; note useKeyGate L125-130 which persona OMITS)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"frontend/src/pages/ChatPage.tsx" (clone the handler, DROP the useKeyGate wrap — pass the handler DIRECTLY)
  </read_first>
  <action>
    Edit frontend/src/pages/ChatPage.tsx:
    - Add `persona: string | null` to the `Thread` interface (beside `model`).
    - Add a `personas` state (typed `Array<{id:string;label:string;is_default:boolean}> | undefined`) and a one-time mount `useEffect` that fetches `apiFetch('/api/personas')`, guarding `Array.isArray` before setState (mirror the /api/models fetch at L67-75, silent on failure).
    - Add `handleThreadPersonaChange = useCallback(async (personaId: string) => {...})` cloning handleThreadModelChange (L103-120): optimistic `setThreads` updating the active thread's `persona`, then `apiFetch(\`/api/threads/${activeThreadId}\`, { method:'PATCH', body: JSON.stringify({ persona: personaId }) })`, with the existing Sentry.captureException + showToast("Couldn't update the persona for this chat. Try again.", 'error') on failure. Do NOT wrap it in useKeyGate — pass it DIRECTLY (persona has no gate).
    - Pass `threadPersona={activeThread?.persona ?? null}`, `onThreadPersonaChange={handleThreadPersonaChange}`, and `personas={personas}` into the `<ChatContainer .../>` render (alongside threadModel/onThreadModelChange/models).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (TypeScript strict — Thread.persona + new props type-check).
    - `grep -c "handleThreadPersonaChange" frontend/src/pages/ChatPage.tsx` >= 2 (definition + prop pass).
    - handleThreadPersonaChange is passed DIRECTLY to ChatContainer (NOT via guardedSelect/useKeyGate) — grep confirms no useKeyGate wrapping of the persona handler.
    - The /api/personas fetch guards Array.isArray before setState (defensive, mirrors /api/models).
  </acceptance_criteria>
  <done>ChatPage fetches the persona catalog, owns the optimistic no-gate PATCH handler, and passes persona state to ChatContainer.</done>
</task>

<task type="auto">
  <name>Task 2: ChatContainer.tsx — render PersonaSelector in the header row</name>
  <files>frontend/src/components/ChatContainer.tsx</files>
  <read_first>
    - frontend/src/components/ChatContainer.tsx lines 12-116 (Props interface + the per-thread header row at 91-116 where ModelSelector renders)
    - frontend/src/components/PersonaSelector.tsx (the component from 17-09 — its props value/onSelect/personas)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"ChatContainer analog" (render PersonaSelector beside ModelSelector in the same shrink-0 header row)
  </read_first>
  <action>
    Edit frontend/src/components/ChatContainer.tsx: extend the Props interface with `threadPersona: string | null`, `onThreadPersonaChange: (personaId: string) => void`, and `personas?: Array<{id:string;label:string;is_default:boolean}>`. Destructure them in the component signature. Inside the existing `activeThreadId !== null` header row (L94-116), render `<PersonaSelector value={threadPersona} onSelect={onThreadPersonaChange} personas={personas} />` beside the ModelSelector (add a small label consistent with the "Model for this chat" caption, e.g. a "Persona" caption, using the same Tailwind tokens). Keep the per-thread cost Σ (ml-auto) placement intact. Import PersonaSelector at the top.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx vitest run src/components/PersonaSelector.test.tsx src/components/DefaultPersonaSelector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; ChatContainer imports and renders PersonaSelector in the thread header row.
    - The ModelSelector render and the Σ cost caption are unchanged (grep-confirm both still present).
    - The picker tests stay GREEN (no regression to the components).
  </acceptance_criteria>
  <done>ChatContainer renders the PersonaSelector beside the model selector in the active-thread header row.</done>
</task>

<task type="auto">
  <name>Task 3: SettingsPage.tsx — render DefaultPersonaSelector seeded from preferences</name>
  <files>frontend/src/pages/SettingsPage.tsx</files>
  <read_first>
    - frontend/src/pages/SettingsPage.tsx lines 23-57, 167-177 (the /api/models fetch + /api/preferences seed + the DefaultModelSelector render placement)
    - frontend/src/components/DefaultPersonaSelector.tsx (the component from 17-09 — props value/onChange/personas)
  </read_first>
  <action>
    Edit frontend/src/pages/SettingsPage.tsx: add `personas` state + a one-time `apiFetch('/api/personas')` mount fetch (Array.isArray-guarded, mirror the /api/models fetch at L36-44). Add `defaultPersona` state seeded from the existing /api/preferences GET (extend the prefs seed effect at L48-57 to also read `prefs.default_persona` when defined). Render `<DefaultPersonaSelector value={defaultPersona} onChange={setDefaultPersona} personas={personas} />` in a new section beside the DefaultModelSelector block (L167-171) — the component supplies its own heading/helper, so do not add a duplicate heading.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; SettingsPage renders DefaultPersonaSelector seeded from GET /api/preferences.default_persona.
    - The persona list comes from the /api/personas fetch (Array.isArray-guarded) — not hardcoded.
    - The existing DefaultModelSelector + Theme + OpenRouter sections are unchanged.
  </acceptance_criteria>
  <done>Settings page renders the default-persona picker seeded from preferences, using the fetched catalog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PATCH /api/threads/{id} {persona} | The chat picker persists the per-thread persona pin; server re-checks ownership (17-07). |
| browser → GET /api/personas, GET/PUT /api/preferences | Auth-gated reads/writes; the client renders the fetched list and seeds the default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-29 | Tampering | client PATCH persona | mitigate | Server ownership re-check (.eq id .eq user_id → 404) + registry validation (17-06/17-07) — the client is not the enforcement point. |
| T-17-30 | Info Disclosure | persona list in the browser | accept | Only the non-secret GET /api/personas payload (id/label/is_default) is fetched — no voice_block/prompt text client-side. |
| T-17-31 | (n/a) | persona picker gating | accept | No key gate on either picker (persona has no cost) — a keyless/anon user can still pick a persona. |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds (all three edited files type-check under strict mode).
- `cd frontend && npm test` — the persona picker tests + the existing FE suite stay GREEN.
- `cd frontend && npm run lint` passes (no unused vars/imports from the wiring).
- The chat header renders PersonaSelector beside ModelSelector; the settings page renders DefaultPersonaSelector; both lists come from GET /api/personas.
</verification>

<success_criteria>
- The persona pickers are wired end-to-end in the running app: chat header (per-thread PATCH, no gate), settings (default PUT), both fed by GET /api/personas and seeded from the thread/preferences reads.
- A thread's persona is restored from the thread read on reopen (PERS-05); no key gate anywhere (persona is cost-free).
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-10-SUMMARY.md`.
</output>
