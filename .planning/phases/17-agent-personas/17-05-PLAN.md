---
phase: 17-agent-personas
plan: 05
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - backend/models/schemas.py
  - backend/routers/threads.py
  - supabase/migrations/20240301000035_add_persona_columns.sql
autonomous: true
requirements: [PERS-01, PERS-04, PERS-05]
must_haves:
  truths:
    - "PersonaResponse (id/label/is_default) exists for GET /api/personas (PERS-01/D-07)"
    - "ThreadResponse declares persona so it survives response_model on every thread read (PERS-05/Pitfall 1)"
    - "ThreadUpdate accepts model AND persona for the partial PATCH (PERS-01/PERS-05)"
    - "PreferencesResponse/PreferencesUpdate declare default_persona for the settings default (PERS-04)"
    - "Migration 035 adds nullable threads.persona + user_preferences.default_persona, additive, no backfill, no new RLS (D-08)"
  artifacts:
    - path: "backend/models/schemas.py"
      provides: "PersonaResponse + persona field additions (Pitfall 1 declared before wiring)"
      contains: "class PersonaResponse"
    - path: "supabase/migrations/20240301000035_add_persona_columns.sql"
      provides: "additive nullable persona pin columns (D-08)"
      contains: "ADD COLUMN persona"
  key_links:
    - from: "backend/models/schemas.py ThreadResponse.persona"
      to: "GET /api/threads read paths"
      via: "response_model field declaration (Pitfall 1)"
      pattern: "persona: str"
    - from: "supabase/migrations/20240301000035_add_persona_columns.sql"
      to: "threads / user_preferences tables"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "ADD COLUMN (persona|default_persona)"
---

<objective>
Author the DATA CONTRACT for personas: the Pydantic schema additions (new PersonaResponse + persona/default_persona fields on the thread/preferences models) and the additive migration 035 file adding the two nullable pin columns. Schema fields land BEFORE the read/write wiring (Pitfall 1 — FastAPI response_model strips undeclared fields even when select('*') returns them). This plan writes the migration FILE only; applying it is the [BLOCKING] plan 17-08.

Purpose: PERS-01 (PersonaResponse for the picker catalog), PERS-04 (default_persona), PERS-05 (persona rides every thread read), D-07/D-08.
Output: schemas.py field additions + threads.py import rename + migration 035 SQL.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md
@backend/models/schemas.py
@supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql

<interfaces>
<!-- Consumers of these schemas: 17-06 (personas router uses PersonaResponse), 17-07 (threads PATCH uses ThreadUpdate; preferences uses PreferencesResponse/Update). Migration head confirmed 034 → next 035. -->
Current: ThreadResponse (schemas.py:25-33) has model: str | None. ThreadModelUpdate (73-81) has model only. PreferencesResponse (42-53) has default_model/theme/favorite_models. PreferencesUpdate (56-70) has default_model/theme/favorite_models. Pitfall 1 documented on this file at the `usage` field (97-102). threads.py:4 imports ThreadModelUpdate (the only in-repo importer).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas.py — PersonaResponse + persona/default_persona field additions</name>
  <files>backend/models/schemas.py, backend/routers/threads.py</files>
  <read_first>
    - backend/models/schemas.py lines 25-102 (ThreadResponse, PreferencesResponse, PreferencesUpdate, ThreadModelUpdate, ModelResponse, and the `usage`-field Pitfall-1 comment at 97-102)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"backend/models/schemas.py — response/request field additions" (exact field set + the ThreadModelUpdate→ThreadUpdate rename)
    - backend/routers/threads.py line 4 (the ThreadModelUpdate import that must be renamed when the class renames)
  </read_first>
  <action>
    Edit backend/models/schemas.py:
    - Add a new `class PersonaResponse(BaseModel)` with exactly three fields: `id: str`, `label: str`, `is_default: bool`. Include a docstring noting voice_block is DELIBERATELY absent (prompt text stays server-side, A5).
    - Add `persona: str | None = None` to `ThreadResponse` (beside `model`), with a comment referencing Pitfall 1 (without this, response_model strips persona even though select('*') returns it — PERS-05).
    - Rename `class ThreadModelUpdate` → `class ThreadUpdate` and add `persona: str | None = None` alongside the existing `model: str | None = None`. Keep both defaulting to None so exclude_unset partial-PATCH semantics work (a persona-only body must not carry model). Update the docstring to describe the partial-update behavior (this class moves from explicit-write to exclude_unset in 17-07).
    - Add `default_persona: str | None = None` to BOTH `PreferencesResponse` and `PreferencesUpdate` (mirroring how favorite_models rides both — None default preserves exclude_unset partial-upsert).
    - Then update backend/routers/threads.py: change the import on line 4 from `ThreadModelUpdate` to `ThreadUpdate` and update the `body: ThreadModelUpdate` type hint in update_thread_model's signature to `body: ThreadUpdate`. (17-07 rewrites the body of that endpoint; this task only fixes the rename so the module still imports.)
  </action>
  <verify>
    <automated>cd backend && python -c "from models.schemas import PersonaResponse, ThreadResponse, ThreadUpdate, PreferencesResponse, PreferencesUpdate; assert set(PersonaResponse.model_fields)=={'id','label','is_default'}; assert 'persona' in ThreadResponse.model_fields; assert {'persona','model'} <= set(ThreadUpdate.model_fields); assert 'default_persona' in PreferencesResponse.model_fields and 'default_persona' in PreferencesUpdate.model_fields; import routers.threads; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The python one-liner above exits 0 (all fields declared; routers.threads still imports after the rename).
    - PersonaResponse has EXACTLY {id,label,is_default} — no voice_block.
    - No other repo file imports ThreadModelUpdate (`grep -rn "ThreadModelUpdate" backend/` returns only comments or nothing; if a stray importer exists, it is updated or an alias added).
    - ThreadResponse.persona and both preferences models' default_persona all default to None.
  </acceptance_criteria>
  <done>schemas.py declares PersonaResponse + persona/default_persona; ThreadModelUpdate renamed to ThreadUpdate; threads.py import fixed so the module loads.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 035 — additive nullable persona pin columns</name>
  <files>supabase/migrations/20240301000035_add_persona_columns.sql</files>
  <read_first>
    - supabase/migrations/20240301000032_create_user_preferences_and_thread_model.sql lines 57-62 (the `ALTER TABLE threads ADD COLUMN model TEXT;` additive pattern + the "no new RLS needed" rationale)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"supabase/migrations/…035…" (two ADD COLUMN lines, no CHECK/FK per D-10, no policies)
    - .planning/phases/17-agent-personas/17-RESEARCH.md §"Pattern 3" (nullable, no backfill, own-row RLS covers the row)
  </read_first>
  <action>
    Create supabase/migrations/20240301000035_add_persona_columns.sql with a header comment (Phase 17 — persona pin columns, additive, mirrors migration 032; nullable, no backfill: existing threads/rows resolve to the system default via the resolver tier chain, D-08; NOT a CHECK/FK because a removed persona id must resolve to default, never raise, D-10; the own-row RLS on threads and user_preferences already covers the whole row — add NO policies) followed by exactly two statements: `ALTER TABLE threads ADD COLUMN persona TEXT;` and `ALTER TABLE user_preferences ADD COLUMN default_persona TEXT;`. Do NOT add any RLS policy, CHECK constraint, FK, or DEFAULT. Filename MUST be 20240301000035 (next after head 034).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20240301000035_add_persona_columns.sql && grep -qi "ALTER TABLE threads ADD COLUMN persona TEXT" supabase/migrations/20240301000035_add_persona_columns.sql && grep -qi "ALTER TABLE user_preferences ADD COLUMN default_persona TEXT" supabase/migrations/20240301000035_add_persona_columns.sql && ! grep -qiE "CREATE POLICY|CHECK|REFERENCES|DEFAULT" supabase/migrations/20240301000035_add_persona_columns.sql && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20240301000035_add_persona_columns.sql exists with exactly the two ADD COLUMN statements (persona on threads, default_persona on user_preferences).
    - The grep gate prints MIGRATION_OK: both columns present AND no CREATE POLICY / CHECK / REFERENCES / DEFAULT clause (additive-only, D-08/D-10).
    - Filename ordinal is 035 (strictly greater than the 034 head).
  </acceptance_criteria>
  <done>Migration 035 authored: two additive nullable TEXT columns, no RLS/CHECK/FK/DEFAULT; ready for the [BLOCKING] db push in 17-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DB (persona pin write) | The new nullable columns will hold a user-supplied persona id. No CHECK/FK is placed (D-10); validation is at the resolver (17-06). Own-row RLS from migration 032 scopes reads/writes per user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Tampering | threads.persona / user_preferences.default_persona values | accept | The column stores a raw string by design (a removed persona id must persist and resolve to default, D-10) — no CHECK/FK. Validation happens at read time in the resolver (17-06), not the DB. |
| T-17-13 | Information Disclosure | new columns cross-user read | mitigate | No new RLS added because the existing own-row policies on threads and user_preferences (migration 032) already cover every column of the row — a new column inherits them. |
| T-17-14 | Information Disclosure | PersonaResponse leaking prompt text | mitigate | PersonaResponse omits voice_block; only id/label/is_default are declared, so the serializer cannot ship prompt text (A5). |
</threat_model>

<verification>
- The Task 1 python one-liner exits 0 (schemas declared, threads.py imports).
- The Task 2 grep gate prints MIGRATION_OK.
- `cd backend && python -m pytest tests/test_config.py -x` still passes (schemas import cleanly; no runtime behavior changed yet).
- Migration is NOT applied here (apply is 17-08).
</verification>

<success_criteria>
- PersonaResponse + persona/default_persona fields are declared before any read/write wiring (Pitfall 1 pre-empted).
- Migration 035 is authored as a pure additive nullable clone of migration 032, no new RLS/CHECK/FK (D-08/D-10).
- ThreadModelUpdate→ThreadUpdate rename is complete and the backend still imports.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-05-SUMMARY.md`.
</output>
