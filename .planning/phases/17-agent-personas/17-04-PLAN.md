---
phase: 17-agent-personas
plan: 04
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - backend/config.py
  - backend/services/persona_service.py
  - backend/services/llm_service.py
autonomous: true
requirements: [PERS-02, PERS-03]
must_haves:
  truths:
    - "The operational base prompt carries citation/tool-error/tables/analyze rules but no board-game opener and no KB-first bias (D-01/D-02/D-03)"
    - "Exactly 2 personas exist in the registry; Board-Game Expert is the default and carries the KB-first bias; General Assistant is board-game-agnostic (D-05/D-06/PERS-03)"
    - "stream_chat_completion composes persona voice FIRST, then base, then tool_guide for every persona (D-01/D-04)"
    - "The persona registry validates an unknown id back to the default and never exposes voice_block to callers of list_personas (D-10/A5)"
  artifacts:
    - path: "backend/services/persona_service.py"
      provides: "PERSONAS registry + DEFAULT_PERSONA_ID + list_personas/resolve_persona_id/get_persona_voice (D-05/D-07/D-10)"
      contains: "DEFAULT_PERSONA_ID"
      min_lines: 30
    - path: "backend/config.py"
      provides: "system_prompt repurposed to the persona-agnostic operational base (D-02)"
      contains: "Sources:"
    - path: "backend/services/llm_service.py"
      provides: "persona_voice param + voice-first composition (D-01/D-04)"
      contains: "persona_voice"
  key_links:
    - from: "backend/services/llm_service.py"
      to: "backend/config.py system_prompt + persona_voice"
      via: "system_content = persona_voice + base + tool_guide"
      pattern: "persona_voice"
    - from: "backend/services/persona_service.py"
      to: "resolve_persona_id/get_persona_voice"
      via: "registry lookup with default fallback"
      pattern: "resolve_persona_id"
---

<objective>
Implement the ONE genuinely-novel piece of this phase: split the monolithic `settings.system_prompt` into a persona-agnostic operational BASE (config.py) + a per-persona VOICE registry (new persona_service.py), and compose them in stream_chat_completion (voice first, then base, then tool_guide). Turns the 17-01 RED scaffolds (test_config operational_base + test_persona_prompt) GREEN.

Purpose: PERS-02 (General Assistant is vanilla + full tools), PERS-03 (Expert default), D-01/D-02/D-03/D-04/D-05/D-06/D-07/D-10 prompt-composition decisions.
Output: persona_service.py (registry + helpers), config.py base-only prompt, llm_service.py composition seam.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md
@backend/config.py
@backend/services/llm_service.py

<interfaces>
<!-- Exact voice/base strings are pinned in 17-RESEARCH.md Pattern 1 (lines ~226-264) and 17-PATTERNS.md Pattern A. Use those verbatim. -->
Existing stream_chat_completion signature (llm_service.py:62-71): messages, tools, tool_guide, source_hint, scope_hint, api_key, model, trace. Add `persona_voice: str | None = None` in the same slot style as api_key/model. Compose at the current single line `system_content = settings.system_prompt` (~L90).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create persona_service.py registry + helpers</name>
  <files>backend/services/persona_service.py</files>
  <read_first>
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"Pattern A2" (the PERSONAS shape + 3 helper bodies) and §"Code Examples" in 17-RESEARCH.md (list_personas/resolve_persona_id/get_persona_voice)
    - backend/config.py lines 95-109 (the KB-first bias + opener text that MOVES into the Board-Game Expert voice_block per D-03)
    - backend/services/model_catalog_service.py (naming/style analog for a services/*.py module — snake_case, module docstring)
  </read_first>
  <behavior>
    - resolve_persona_id("board_game_expert") == "board_game_expert"; resolve_persona_id("general_assistant") == "general_assistant"; resolve_persona_id("__nope__") == DEFAULT_PERSONA_ID; resolve_persona_id(None) == DEFAULT_PERSONA_ID.
    - get_persona_voice("general_assistant") returns the General voice; get_persona_voice("__nope__") returns the Expert (default) voice; never raises.
    - list_personas() returns 2 dicts each with ONLY id/label/is_default (no voice_block); exactly one is_default True (board_game_expert).
  </behavior>
  <action>
    Create backend/services/persona_service.py with a module docstring (definition-in-code mirrors system_prompt/TOOL_SELECTION_GUIDE per D-07 — no DB, no cache). Define `DEFAULT_PERSONA_ID = "board_game_expert"` and a module-level `PERSONAS` list of exactly 2 dicts (D-05) each with keys id, label, is_default, voice_block:
    - board_game_expert (label "Board-Game Expert", is_default True) — voice_block carries the "You are a board-game expert…" opener + the D-03 KB-first bias ("Prefer the knowledge base for game rules and mechanics; use web_search only for current or external facts the knowledge base cannot answer (prices, availability, upcoming expansions, BGG rankings, designer/publisher news).") — use the exact wording pinned in 17-RESEARCH.md Pattern 1.
    - general_assistant (label "General Assistant", is_default False) — a board-game-agnostic "You are a helpful, general-purpose assistant…" voice per D-06 (non-prohibitive on tools, does not assume board games) — use the exact wording pinned in 17-RESEARCH.md Pattern 1.
    Add `list_personas()` (returns id/label/is_default only — never voice_block, A5), `resolve_persona_id(pinned: str | None) -> str` (returns pinned iff it is a registry id, else DEFAULT_PERSONA_ID — D-10, never raises), and `get_persona_voice(persona_id: str) -> str` (voice_block for the id, default voice on miss). Type hints per CLAUDE.md (str | None, list[dict]).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_persona_resolution.py -k "registry or unknown"</automated>
  </verify>
  <acceptance_criteria>
    - backend/services/persona_service.py exists; `python -c "from services.persona_service import PERSONAS, list_personas, resolve_persona_id, get_persona_voice, DEFAULT_PERSONA_ID; assert len(PERSONAS)==2; assert sum(p['is_default'] for p in PERSONAS)==1; assert resolve_persona_id('__nope__')==DEFAULT_PERSONA_ID; assert 'voice_block' not in list_personas()[0]"` exits 0.
    - The board_game_expert voice_block contains "Prefer the knowledge base" (D-03); the general_assistant voice_block does NOT.
    - test_persona_resolution.py registry + unknown-id tests turn GREEN.
  </acceptance_criteria>
  <done>persona_service.py provides the 2-entry registry + 3 helpers; unknown-id falls back to Expert; list_personas withholds voice_block.</done>
</task>

<task type="auto">
  <name>Task 2: Strip config.py system_prompt to the operational base</name>
  <files>backend/config.py</files>
  <read_first>
    - backend/config.py lines 95-109 (current system_prompt — drop the opener + KB-first bias, keep citation/tool-error/tables/analyze)
    - backend/tests/test_web_search.py lines 139-152 (test_system_prompt_citation_guidance — MUST stay GREEN: base keeps "Sources:" + "inline")
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"Pattern A1" (which lines to delete)
  </read_first>
  <action>
    Edit backend/config.py `system_prompt` (~L95-109): DELETE the opener line ("You are a helpful assistant with access to tools. Answer questions clearly and concisely.") and the KB-first bias sentences ("Prefer the knowledge base for game rules and mechanics; use web_search only for current or external facts … BGG rankings, designer/publisher news."). KEEP verbatim the operational-base sentences (D-02): the inline-markdown-link + "Sources:" citation guidance, the tool-error handling, the markdown-tables-for-DB-results rule, and the analyze_document-by-name guidance. Do NOT touch subagent_system_prompt (~L142) or explorer_system_prompt (~L151) — out of scope. Leave a short comment noting system_prompt is now the persona-agnostic operational base (D-02) and that any SYSTEM_PROMPT env value shadows it (Pitfall 6 — must be removed from .env/.env.prod at deploy).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_config.py -k "operational_base" tests/test_web_search.py::test_system_prompt_citation_guidance</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from config import Settings; s=Settings().system_prompt; assert 'Sources:' in s and 'inline' in s.lower(); assert 'Prefer the knowledge base' not in s; assert not s.lstrip().startswith('You are a helpful assistant')"` exits 0 (run with SYSTEM_PROMPT unset).
    - test_config.py operational_base tests turn GREEN AND test_web_search::test_system_prompt_citation_guidance stays GREEN.
    - subagent_system_prompt and explorer_system_prompt are unchanged (grep still finds them intact).
  </acceptance_criteria>
  <done>system_prompt is the base-only operational prompt; citation guidance retained, opener + KB-first bias removed; sub-agent prompts untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add persona_voice param + voice-first composition to stream_chat_completion</name>
  <files>backend/services/llm_service.py</files>
  <read_first>
    - backend/services/llm_service.py lines 62-132 (signature + system_content assembly at ~90-92 + the source_hint/scope_hint blocks that stay verbatim)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"Pattern A3" (compose voice FIRST then base then tool_guide — Pitfall 2)
    - .planning/phases/17-agent-personas/17-CONTEXT.md D-04 (tool_guide appends for ALL personas — unchanged mechanism)
  </read_first>
  <behavior>
    - With persona_voice=None the composed system_content equals the base-only behavior (back-compat for sub-agent callers).
    - With persona_voice set, the composed system_content places the voice string BEFORE the base "Sources:" text, and tool_guide (when supplied) is still appended after the base for every persona.
  </behavior>
  <action>
    Edit backend/services/llm_service.py: add `persona_voice: str | None = None` to the stream_chat_completion signature (same slot style as api_key/model) and document it in the docstring. Replace the single `system_content = settings.system_prompt` seed (~L90) so that: system_content starts as settings.system_prompt (base); if persona_voice is truthy, prepend it as `system_content = persona_voice + "\n\n" + system_content` (voice leads — Pitfall 2); the existing `if tool_guide:` append stays UNCHANGED (D-04 — appended for all personas). Leave the source_hint/scope_hint blocks (L94-123) and the "system_content" yield verbatim.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_persona_prompt.py -x</automated>
  </verify>
  <acceptance_criteria>
    - stream_chat_completion signature contains `persona_voice: str | None = None`.
    - test_persona_prompt.py turns fully GREEN (voice-leads, base-for-both, Expert-KB-first, General-no-board-game-framing, tools/tool_guide identical across personas).
    - Calling stream_chat_completion with persona_voice=None still yields a system_content equal to base + tool_guide (no regression for existing chat callers / sub-agents that do not pass persona_voice).
    - The source_hint/scope_hint composition blocks are byte-for-byte unchanged (grep-confirm the Source Routing text still present).
  </acceptance_criteria>
  <done>stream_chat_completion accepts persona_voice and composes voice→base→tool_guide; test_persona_prompt.py GREEN; None path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver → LLM system message | The persona voice_block string is injected into the LLM system prompt. Only server-defined registry strings may cross here — never user free-text (predefined-only, PERS-F1 deferred). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Tampering / Injection | get_persona_voice input | mitigate | Only PERSONAS voice_block strings ever reach the system message; resolve_persona_id validates any pinned id back to a registry entry (D-10), so no user-controlled string can be injected as a persona voice. |
| T-17-10 | Information Disclosure | list_personas output | mitigate | list_personas returns id/label/is_default only — voice_block (prompt text) is never returned to callers/clients (A5). |
| T-17-11 | Elevation of Privilege | tool_guide composition | accept | tool_guide is appended for every persona unchanged (D-04); no persona can suppress tool access via the system message — tool access is controlled elsewhere (tools list), never here. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_persona_prompt.py tests/test_config.py -k "persona or operational_base" -x` all GREEN.
- `cd backend && python -m pytest tests/test_web_search.py::test_system_prompt_citation_guidance` GREEN (regression).
- `cd backend && python -m pytest` — the existing llm_service/chat suites still pass (persona_voice defaults None → no behavior change for existing callers).
</verification>

<success_criteria>
- The operational base + 2-persona voice registry + voice-first composition are in place and the 17-01 RED scaffolds are GREEN.
- General Assistant reads board-game-agnostic; Expert keeps the KB-first bias; both carry the shared operational base and full tool_guide (PERS-02/PERS-03/D-04).
- No sub-agent prompt or existing composition block regressed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-04-SUMMARY.md`.
</output>
