---
phase: 17-agent-personas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_personas_api.py
  - backend/tests/test_thread_persona_patch.py
  - backend/tests/test_preferences_api.py
autonomous: true
requirements: [PERS-01, PERS-04, PERS-05]
must_haves:
  truths:
    - "A failing test pins that GET /api/personas is auth-gated and returns [{id,label,is_default}] with exactly one default (PERS-01/D-07)"
    - "A failing test pins that PATCH /api/threads/{id} {persona} writes scoped by id+user_id and a persona-only body does not clobber model (PERS-01/PERS-05)"
    - "A failing test pins that default_persona roundtrips PUT→GET and a theme-only PUT does not clobber it (PERS-04)"
  artifacts:
    - path: "backend/tests/test_personas_api.py"
      provides: "RED scaffold for GET /api/personas (mirror test_models_api.py)"
      contains: "def test_"
    - path: "backend/tests/test_thread_persona_patch.py"
      provides: "RED scaffold for PATCH persona (mirror test_thread_model_patch.py)"
      contains: "def test_"
    - path: "backend/tests/test_preferences_api.py"
      provides: "Extended default_persona roundtrip + no-clobber tests"
      contains: "default_persona"
  key_links:
    - from: "backend/tests/test_personas_api.py"
      to: "backend/routers/personas.py"
      via: "TestClient GET /api/personas with dependency_overrides[get_user_id]"
      pattern: "/api/personas"
    - from: "backend/tests/test_thread_persona_patch.py"
      to: "backend/routers/threads.py"
      via: "TestClient PATCH /api/threads/{id} {persona}"
      pattern: "PATCH|patch"
---

<objective>
Author the Wave 0 RED test scaffolds for the persona API SURFACE: the new GET /api/personas catalog endpoint, the extended PATCH /api/threads/{id} persona write, and the extended PUT/GET /api/preferences default_persona roundtrip. These FAIL RED until 17-06 (personas router) and 17-07 (threads/preferences routes) ship.

Purpose: Nyquist Dim 8 — pins PERS-01 (chat picker → PATCH persona), PERS-04 (default persona settings), PERS-05 (persistence) at the HTTP boundary, plus the IDOR/no-clobber security contracts.
Output: 2 new backend test files + extended test_preferences_api.py, all RED.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17-agent-personas/17-CONTEXT.md
@.planning/phases/17-agent-personas/17-RESEARCH.md
@.planning/phases/17-agent-personas/17-PATTERNS.md

<interfaces>
<!-- HTTP contracts these RED tests assert (shipped by 17-06 / 17-07). -->

GET /api/personas (17-06) — Depends(get_user_id); response_model list[PersonaResponse]; body [{id, label, is_default}] (NO voice_block). Exactly one is_default == true.

PATCH /api/threads/{id} (17-07) — body ThreadUpdate {model?: str|None, persona?: str|None}; writes body.model_dump(exclude_unset=True) scoped by .eq("id",tid).eq("user_id",uid); 404 on non-owned; a persona-only body must NOT include "model" in the update patch.

PUT /api/preferences (17-07) — body PreferencesUpdate now includes default_persona: str|None; GET /api/preferences response includes default_persona (defaults null for a new user). exclude_unset partial-upsert: a theme-only PUT must not carry default_persona.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffold — GET /api/personas (test_personas_api.py)</name>
  <files>backend/tests/test_personas_api.py</files>
  <read_first>
    - backend/tests/test_models_api.py lines 80-130 (TestClient + app.dependency_overrides[get_user_id] = lambda: "user-uuid" + finally clear — the auth-gated GET harness to mirror)
    - backend/routers/models.py lines 24-53 (the auth-gated catalog GET pattern the personas router clones)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"backend/routers/personas.py (NEW router)"
  </read_first>
  <action>
    Create backend/tests/test_personas_api.py mirroring test_models_api.py. In-function `from main import app` (RED until routers.personas is registered) + `from auth import get_user_id`. Use app.dependency_overrides[get_user_id] = lambda: "user-uuid" in a try/finally that clears overrides. No DB patch is needed (the catalog is a code constant), but if the router imports services.persona_service at module scope that will resolve once 17-04 lands. Write:
    - test_get_personas_returns_catalog: GET /api/personas → 200; body is a list of length 2; each item has exactly the keys {"id","label","is_default"} and NO "voice_block" key (A5 — prompt text never shipped).
    - test_exactly_one_default_and_expert_is_it: exactly one item has is_default True and its id == "board_game_expert" (PERS-03/D-05).
    - test_personas_requires_auth: with NO dependency override, GET /api/personas returns 401/403 (auth-gated, D-07 — mirror how test_models_api asserts the gate; if the app default raises 401, assert that).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_personas_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists; pytest run FAILS RED (from main import app cannot resolve /api/personas route → 404 on the GET, OR ImportError on routers.personas — either is the correct RED signal).
    - 3 test functions present (`grep -c "def test_"` == 3).
    - The catalog-shape test explicitly asserts "voice_block" is absent from each returned item.
    - No production file modified.
  </acceptance_criteria>
  <done>test_personas_api.py collects and fails RED; pins the auth-gated GET /api/personas [{id,label,is_default}] contract with voice_block withheld.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffold — PATCH persona pin (test_thread_persona_patch.py)</name>
  <files>backend/tests/test_thread_persona_patch.py</files>
  <read_first>
    - backend/tests/test_thread_model_patch.py (FULL — the _mock_db(owned) helper, ownership-re-check assertion via eq_calls, dependency_overrides, patch("routers.threads.get_supabase"))
    - backend/routers/threads.py lines 58-89 (current PATCH update_thread_model that hardcodes {"model": body.model} — to become exclude_unset in 17-07)
    - .planning/phases/17-agent-personas/17-PATTERNS.md §"backend/routers/threads.py — PATCH accepts persona" (exclude_unset partial-write)
  </read_first>
  <action>
    Create backend/tests/test_thread_persona_patch.py cloning test_thread_model_patch.py's _mock_db(owned) helper and patch targets. Write:
    - test_patch_sets_persona: PATCH /api/threads/{tid} {"persona":"general_assistant"} → 200; db.table("threads").update called once with a patch dict where patch["persona"]=="general_assistant"; the update chain is scoped by ("id",tid) AND ("user_id",user) (IDOR re-check, reuse the eq_calls set assertion from the analog).
    - test_patch_persona_only_does_not_clobber_model: PATCH {"persona":"general_assistant"} (no model key) → the update patch dict does NOT contain the key "model" (exclude_unset semantics — Pattern 4 / no-clobber). This is the test that FORCES the switch away from the hardcoded {"model": body.model}.
    - test_patch_model_only_still_works: PATCH {"model":"x/y"} → update patch == {"model":"x/y"} and does NOT contain "persona" (regression: the model-pin path stays intact under exclude_unset).
    - test_patch_persona_404_non_owned: _mock_db(owned=False) → PATCH {"persona":"general_assistant"} → 404 and db.table("threads").update NOT called (IDOR).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_thread_persona_patch.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists; pytest run FAILS RED: with today's hardcoded {"model": body.model} PATCH, the persona-set and no-clobber tests fail (persona not written / model key present).
    - 4 test functions present (`grep -c "def test_"` == 4).
    - test_patch_persona_only_does_not_clobber_model asserts "model" not in the update patch dict.
    - No production file modified.
  </acceptance_criteria>
  <done>test_thread_persona_patch.py collects and fails RED; pins persona-set + no-clobber-model + IDOR-404 for PATCH (PERS-01/PERS-05).</done>
</task>

<task type="auto">
  <name>Task 3: Extend test_preferences_api.py — default_persona roundtrip + no-clobber</name>
  <files>backend/tests/test_preferences_api.py</files>
  <read_first>
    - backend/tests/test_preferences_api.py lines 125-284 (the favorite_models roundtrip + no-clobber block — the EXACT pattern to clone for default_persona)
    - backend/routers/preferences.py lines 37-96 (GET/PUT — the .select strings + return dicts + exclude_unset upsert to extend in 17-07)
  </read_first>
  <action>
    Append a new "default_persona" section to backend/tests/test_preferences_api.py, cloning the favorite_models tests (do NOT modify existing tests). Reuse the file's existing _mock_db helper style. Write:
    - test_preferences_get_returns_default_persona_none_for_new_user: no row → GET body includes "default_persona": None (PERS-04 default).
    - test_preferences_put_default_persona_roundtrip: PUT {"default_persona":"general_assistant"} upserts a payload carrying default_persona + user_id + updated_at; the read-back GET/echo returns default_persona=="general_assistant".
    - test_preferences_put_theme_only_preserves_default_persona: PUT {"theme":"light"} → the upsert payload does NOT contain "default_persona" (exclude_unset — theme-only must not clobber the stored persona default, mirror test_preferences_put_theme_only_preserves_favorites).
    - test_preferences_put_default_persona_only_no_clobber: PUT {"default_persona":"..."} → payload does NOT contain "default_model" or "theme" (persona-only must not clobber siblings).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_preferences_api.py -k persona</automated>
  </verify>
  <acceptance_criteria>
    - New tests appended; existing test_preferences_api tests unmodified and the file still collects.
    - `pytest tests/test_preferences_api.py -k persona` selects exactly the new tests and FAILS RED (GET response has no default_persona key; PUT does not persist it yet).
    - Test names contain "persona" so the -k filter is precise.
    - No production file modified.
  </acceptance_criteria>
  <done>test_preferences_api.py carries RED default_persona roundtrip + no-clobber tests (PERS-04); existing tests untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new — test-only) | These tests encode the boundary checks (JWT-bound user_id, ownership re-check) that 17-06/17-07 must satisfy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Elevation of Privilege (IDOR) | PATCH /api/threads/{id} persona | mitigate (pinned by test) | test_patch_persona_404_non_owned encodes the ownership re-check .eq("id").eq("user_id") → 404 for non-owned threads (17-07 must satisfy). |
| T-17-05 | Tampering (field clobber) | PATCH threads + PUT preferences | mitigate (pinned by test) | no-clobber tests pin exclude_unset partial writes so a persona-only write cannot null a sibling field. |
| T-17-06 | Information Disclosure | GET /api/personas | mitigate (pinned by test) | test_get_personas_returns_catalog asserts voice_block is never in the response (prompt text stays server-side, A5). |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_personas_api.py tests/test_thread_persona_patch.py -x` runs and reports RED.
- `cd backend && python -m pytest tests/test_preferences_api.py -k persona` runs and reports RED.
- Existing preference tests (`-k "favorite or theme or default_model"`) still PASS (regression: this plan only appends).
- No production file modified.
</verification>

<success_criteria>
- 2 new backend test files + appended default_persona tests exist, all RED for the correct reason.
- The RED baseline covers PERS-01, PERS-04, PERS-05 at the HTTP boundary + IDOR + no-clobber.
- Zero production code touched.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-02-SUMMARY.md`.
</output>
