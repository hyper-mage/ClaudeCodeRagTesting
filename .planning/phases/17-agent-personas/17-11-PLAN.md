---
phase: 17-agent-personas
plan: 11
type: execute
wave: 6
depends_on: [17-10]
files_modified: []
autonomous: false
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, PERS-05, PERS-06]
must_haves:
  truths:
    - "Selecting a persona in the chat picker makes the agent respond in that persona (PERS-01)"
    - "General Assistant answers as a vanilla assistant yet still calls tools (KB/web/SQL/analyze) — full tool access (PERS-02)"
    - "Board-Game Expert is the default persona for a new/unpinned thread (PERS-03)"
    - "Setting a default persona in settings makes new threads start with it (PERS-04)"
    - "A thread's persona survives a page reload / reopen (PERS-05)"
    - "A mid-thread persona switch applies from the next turn (per-request resolution, D-11/PERS-06)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the persona feature end-to-end in the running app (dev), against Phase 17's five ROADMAP success criteria. Automated tests already pin the unit/integration contracts (waves 1-5 GREEN); this checkpoint confirms the visual/functional behavior a user actually experiences — the one dimension automation cannot fully assert (the agent audibly "sounds like" the selected persona, and tool access is retained).

Purpose: functional/visual sign-off for PERS-01..06 before /gsd-verify-work. UI hint = yes.
Output: user confirmation (or a defect list feeding gap-closure).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-agent-personas/17-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end persona verification (5 success criteria)</name>
  <files>(none — end-to-end human verification only)</files>
  <action>Walk the developer through the five ROADMAP success-criteria checks in the running dev app and record pass/fail for each. Block phase verification until all five pass or precise defects are logged for /gsd-plan-phase --gaps.</action>
  <read_first>
    - .planning/ROADMAP.md §"Phase 17: Agent Personas" Success Criteria 1-5 (the exact behaviors to confirm)
    - .planning/phases/17-agent-personas/17-CONTEXT.md D-06 (General Assistant is board-game-agnostic but keeps full tool access) + D-11 (switch applies next-turn)
    - CLAUDE.md Test Credentials (ragtest1@gmail.com / testpass123)
  </read_first>
  <what-built>
    The full persona feature: GET /api/personas catalog, the base+voice system-prompt composition, the per-turn persona resolver, the persona pin columns (dev), the chat-header PersonaSelector (PATCH per thread), and the settings DefaultPersonaSelector (PUT default). All wave-1..5 automated suites are GREEN and migration 035 is applied to dev.
  </what-built>
  <how-to-verify>
    Start the app (backend: `cd backend && uvicorn main:app`; frontend: `cd frontend && npm run dev`) and sign in with the test credentials. Then:
    1. PERS-01 + PERS-03 (default + select): Open a NEW chat. Confirm the header persona picker defaults to / reflects Board-Game Expert behavior — ask "How do you win Azul?" and confirm a board-game-expert style answer (KB-first). Then switch the picker to "General Assistant".
    2. PERS-02 (vanilla + full tools): In the General Assistant thread, ask a NON-board-game question (e.g. "What's a good way to structure a weekly meal plan?") — confirm the answer reads as a general assistant with NO board-game framing. Then ask something that forces a tool (e.g. "search the web for today's date" or a KB question) and confirm a tool card still appears — tools remain available under General Assistant.
    3. PERS-04 (default persona): Go to Settings → set Default persona to "General Assistant". Create a NEW chat and confirm it starts as General Assistant (the new thread inherits the user default).
    4. PERS-05 (persistence): In a thread pinned to a specific persona, reload the page (or reopen the thread) and confirm the picker still shows the pinned persona and the agent continues in it.
    5. PERS-06 / D-11 (per-request, next-turn): Mid-thread, switch the persona and send another message — confirm the NEW turn responds in the newly-selected persona (the switch applies from the next turn, not retroactively). Optionally, in a second browser/profile signed in as a different user, confirm that user's persona choice does not affect the first user's thread (no cross-user bleed).
    Note any criterion that fails with the specific thread/persona/behavior observed.
  </how-to-verify>
  <acceptance_criteria>
    - All five ROADMAP success criteria observably pass in the running dev app.
    - General Assistant answers non-board-game questions without board-game framing AND still shows tool cards when a tool is used (PERS-02 — full tool access retained).
    - Board-Game Expert is the default for an unpinned/new thread when no user default is set (PERS-03).
    - A pinned persona survives reload (PERS-05); a mid-thread switch takes effect on the next turn (D-11).
    - Any failure is recorded with enough detail to feed `/gsd-plan-phase --gaps`.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all five criteria pass, or list the failing criterion/criteria with the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user browser → running app | Manual functional verification only — no new code path introduced by this plan. The boundaries exercised (persona PATCH/PUT, per-turn resolution) are mitigated by the wave-3 route + resolver plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-32 | Information Disclosure | cross-user persona bleed | mitigate (verified) | Step 5's optional two-user check confirms the per-request non-cached resolver (17-06) keeps one user's persona out of another's turn (PERS-06). |
| T-17-33 | Elevation of Privilege | General Assistant tool access | accept (verified) | Step 2 confirms tools still fire under General Assistant — persona never gates tools (D-04/PERS-02). |
</threat_model>

<verification>
- User confirms all 5 ROADMAP success criteria pass in the running dev app.
- General Assistant retains tool access; Expert is the default; persona persists across reload; switch is next-turn.
- Any defect is captured for gap-closure.
</verification>

<success_criteria>
- Phase 17's five success criteria are functionally verified end-to-end and signed off, or a precise defect list is produced for `/gsd-plan-phase --gaps`.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-personas/17-11-SUMMARY.md`.
</output>
