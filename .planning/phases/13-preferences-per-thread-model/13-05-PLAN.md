---
phase: 13-preferences-per-thread-model
plan: 05
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/lib/themeBootstrap.ts
  - frontend/src/components/ModelSelector.tsx
  - frontend/src/components/ThemeToggle.tsx
  - frontend/src/components/ModelSelector.test.tsx
  - frontend/src/components/ThemeToggle.test.tsx
  - frontend/src/test/themeBootstrap.test.ts
autonomous: true
requirements: [MODEL-05, MODEL-06, PREF-02]
user_setup: []

must_haves:
  truths:
    - "An inline <head> script sets the <html> dark class from localStorage.theme BEFORE React mounts (flash-free first paint)"
    - "Tailwind v4 @custom-variant dark (&:where(.dark,.dark *)) governs the class strategy; core-surface palette tokens resolve per theme"
    - "ModelSelector is a hand-rolled accessible listbox dropdown over GET /api/models rows (no shadcn)"
    - "ThemeToggle flips the <html> class, writes localStorage.theme, and fires PUT /api/preferences {theme} fire-and-forget"
  artifacts:
    - path: "frontend/src/components/ModelSelector.tsx"
      provides: "reusable accessible model dropdown"
      min_lines: 60
    - path: "frontend/src/components/ThemeToggle.tsx"
      provides: "Sun/Moon neutral theme toggle"
      min_lines: 20
    - path: "frontend/src/lib/themeBootstrap.ts"
      provides: "testable applyStoredTheme() mirrored by the inline index.html script"
      exports: ["applyStoredTheme"]
    - path: "frontend/src/index.css"
      provides: "@custom-variant dark + core-surface palette tokens"
      contains: "@custom-variant dark"
  key_links:
    - from: "frontend/index.html inline script"
      to: "document.documentElement dark class"
      via: "localStorage.theme read before module script"
      pattern: "classList.toggle"
    - from: "frontend/src/components/ThemeToggle.tsx"
      to: "/api/preferences"
      via: "apiFetch PUT {theme}"
      pattern: "/api/preferences"
    - from: "frontend/src/components/ModelSelector.tsx"
      to: "/api/models"
      via: "apiFetch GET on open"
      pattern: "/api/models"
---

<objective>
Build the reusable frontend primitives + theming mechanism (no consumers wired yet — Plan 06 wires them): the FOUC-free theme bootstrap (inline index.html script + a testable applyStoredTheme), the Tailwind v4 class-strategy dark variant plus core-surface palette tokens (D-01 light mode), the hand-rolled accessible ModelSelector dropdown over GET /api/models, and the ThemeToggle. Includes the Wave 0 frontend test files for these primitives.

Purpose: PREF-02 theming foundation (flash-free, persisted) + the shared selector component both MODEL-05 and MODEL-06 consume in Plan 06. Interface-first: Plan 06 imports these components and the theme bootstrap without rebuilding them.
Output: 2 components, 1 lib (themeBootstrap), index.html + index.css edits, 3 test files.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md
@.planning/phases/13-preferences-per-thread-model/13-RESEARCH.md

<interfaces>
From frontend/src/lib/api.ts (the fetch helpers to use — already attach the bearer, handle JSON/204):
  apiFetch(path, options?)  → throws on non-2xx, returns parsed JSON (null on 204)

From backend GET /api/models → list of ModelResponse (Phase 12, render-ready — DO NOT recompute free/paid):
  { id: string, name: string|null, context_length: number|null, is_free: boolean,
    price_per_mtok_prompt: number|null, price_per_mtok_completion: number|null,
    popularity_rank: number|null, pricing: object }

From backend PUT /api/preferences (Plan 03) → body {theme?: 'light'|'dark', default_model?: string}, returns {default_model, theme}

From frontend/src/test/utils.tsx (REUSE for the component tests):
  renderWithProviders(ui, {isAnon?})  · makeApiMock()  · vi.mock('../lib/api', () => makeApiMock())

Tailwind v4 dark mode (RESEARCH Pattern 4 — VERIFIED against tailwindcss.com/docs/dark-mode):
  index.css:  @custom-variant dark (&:where(.dark, .dark *));   /* use :where() NOT :is() — zero specificity */
  index.html inline <head> script (BEFORE the module script):
    document.documentElement.classList.toggle("dark",
      localStorage.theme === "dark" || (!("theme" in localStorage) && matchMedia("(prefers-color-scheme: dark)").matches))

UI-SPEC § Components — ModelSelector a11y contract (LOCKED): trigger aria-haspopup="listbox" aria-expanded; role="listbox"/"option"; Enter/Space opens, Esc closes, arrow-nav, focus trap while open, focus returns to trigger on close, outside-click closes, ≥44px rows (min-h-11); selected row blue-600 check/left-border; trigger is NEUTRAL surface (gray-800 dark/gray-100 light), NOT accent.
UI-SPEC § Copywriting — LOCKED strings: 'Loading models…', 'Couldn't load models. Tap to retry.', Free tag 'Free', price '${price}/M tokens', context '{context_length/1000}K context'. ThemeToggle labels 'Switch to light theme' / 'Switch to dark theme'.
UI-SPEC § Color — core-surface tokens (D-01): dark bg gray-950/gray-900/gray-800; light bg white/gray-50/gray-100; muted light text gray-600 (NOT gray-400 — AA). Accent blue-600 theme-invariant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Theme bootstrap (testable applyStoredTheme + inline index.html script) + Tailwind dark variant + core-surface tokens</name>
  <read_first>
    - frontend/index.html (current head has no inline script — add one before the module script)
    - frontend/src/index.css (current: @import "tailwindcss"; @plugin "@tailwindcss/typography"; — add the custom-variant + tokens)
    - frontend/src/test/themeBootstrap.test.ts target behavior (this task creates it)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Pattern 4; Pitfalls 2, 5, 6; Open Question 1 — Option A CSS custom-property tokens RECOMMENDED)
    - .planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md (Color — resolved values for both themes)
  </read_first>
  <behavior>
    - applyStoredTheme(storage, root, matchMedia) sets root.classList "dark" when storage.theme === "dark" OR (theme absent AND matchMedia dark). Removes "dark" when storage.theme === "light". Returns the resolved 'light'|'dark'. (themeBootstrap.test.ts: dark-when-stored-dark, light-when-stored-light, system-fallback-when-absent)
    - An unknown stored value (e.g. "purple") normalizes to NOT-dark only if it isn't "dark" — i.e. treat any non-"dark" stored value as light (defensive; matches client normalize note in Security Domain).
    - The inline index.html script mirrors applyStoredTheme's logic exactly so first paint matches (the script is a tiny duplicate of the same boolean; the lib is the unit-tested source of truth).
  </behavior>
  <action>
    (1) Create frontend/src/lib/themeBootstrap.ts exporting applyStoredTheme(storage: {theme?: string} = localStorage, root = document.documentElement, mql = window.matchMedia) that computes isDark = storage.theme === "dark" || (!("theme" in storage) && mql("(prefers-color-scheme: dark)").matches), calls root.classList.toggle("dark", isDark), and returns isDark ? "dark" : "light". Keep it dependency-free and jsdom-safe (guard matchMedia access).
    (2) Edit frontend/index.html: add an inline <script> in <head> BEFORE <script type="module" src="/src/main.tsx">, containing the same boolean as Pattern 4 (document.documentElement.classList.toggle("dark", localStorage.theme === "dark" || (!("theme" in localStorage) && window.matchMedia("(prefers-color-scheme: dark)").matches))). This runs synchronously pre-bundle → no FOUC (Pitfall 5).
    (3) Edit frontend/src/index.css: after the @import/@plugin lines add `@custom-variant dark (&:where(.dark, .dark *));` (use :where() — Pitfall: :is() raises specificity). Then add core-surface palette tokens (Open Question 1 Option A): under :root define light values and under .dark define dark values for --surface-bg, --surface-secondary, --surface-tertiary, --text-primary, --text-muted, --border using the UI-SPEC resolved colors (light: white/gray-50/gray-100/gray-900/gray-600/gray-300; dark: gray-950/gray-900/gray-800/white/gray-400/gray-800). Keep blue-600/red-600 as literal utilities (theme-invariant). These tokens are consumed by Plan 06 on the four core surfaces.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/test/themeBootstrap.test.ts && grep -E "@custom-variant dark \(&:where" src/index.css && grep -E "classList.toggle\(\s*[\"']dark" index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/test/themeBootstrap.test.ts passes (dark-stored, light-stored, system-fallback cases GREEN).
    - index.css contains `@custom-variant dark (&:where(.dark, .dark *));` (uses :where, not :is) and the six core-surface tokens under :root + .dark.
    - index.html has an inline <head> script (before the module script) toggling the dark class from localStorage.theme.
    - applyStoredTheme is exported and jsdom-safe (no crash when matchMedia is stubbed).
  </acceptance_criteria>
  <done>Flash-free theme bootstrap (lib + inline script), Tailwind class-strategy dark variant, and core-surface palette tokens exist; the bootstrap unit test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelSelector — hand-rolled accessible listbox dropdown over GET /api/models</name>
  <read_first>
    - frontend/src/components/ThreadSidebar.tsx + frontend/src/components/MobileDrawer.tsx (existing button radii, focus-ring, focus-trap conventions to match)
    - frontend/src/lib/api.ts (apiFetch)
    - frontend/src/test/utils.tsx (renderWithProviders, makeApiMock)
    - .planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md (Components ModelSelector a11y contract + Copywriting + Color — ALL LOCKED)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Don't Hand-Roll — model catalog is GET /api/models; FE never recomputes is_free; Code Examples — listbox a11y skeleton)
  </read_first>
  <behavior>
    - Renders a trigger button (neutral surface, lucide ChevronDown) showing the selected model's name (or id if name null), or a caller-supplied placeholder/sub-state label. aria-haspopup="listbox", aria-expanded reflects open.
    - On open: fetches GET /api/models via apiFetch once (or uses a caller-passed list); shows 'Loading models…' while pending and 'Couldn't load models. Tap to retry.' on failure (retry re-fetches). role="listbox" with role="option" rows.
    - Each row ≥44px (min-h-11): model name (Body) + a Caption hint — 'Free' tag when is_free, else price '${price_per_mtok_prompt}/M tokens' (In/Out form only if both present) and a '{context_length/1000}K context' line (omitted when context_length null). FE renders the Phase-12-precomputed fields; never recomputes free/paid.
    - Selected row marked with a blue-600 check/left-border. Keyboard: Enter/Space opens; arrow keys move; Enter selects; Esc closes and returns focus to the trigger; outside-click closes; selecting calls onSelect(modelId) and closes.
    - onSelect is a prop (no direct PATCH/PUT here — Plan 06 supplies the handler); the component is presentation + a11y only.
  </behavior>
  <action>
    Create frontend/src/components/ModelSelector.tsx (default export). Props: value: string | null, onSelect: (modelId: string | null) => void, placeholder?: string, extraOption?: {label: string, value: null} (for the per-thread 'Use my default model' clear row — Plan 06 passes it), models?: ModelResponse[] (optional pre-fetched list; otherwise the component fetches GET /api/models on first open). Implement the full LOCKED a11y contract (aria-haspopup/expanded, role listbox/option, arrow-nav, Esc + focus-return, outside-click via a ref + document mousedown listener, focus trap while open, ≥44px rows). Use ONLY hand-rolled Tailwind (no shadcn — STATE.md locks shadcn to Phase 15). Use the core-surface tokens / theme-aware classes so it reads correctly in both themes; trigger uses the NEUTRAL secondary surface, NOT blue-600. Use the LOCKED copy strings verbatim. Define a local ModelResponse type mirroring the backend schema (id/name/context_length/is_free/price_per_mtok_prompt/price_per_mtok_completion/pricing).
    Create frontend/src/components/ModelSelector.test.tsx covering the a11y contract: trigger has aria-haspopup="listbox"; opening renders role="listbox" with options; rows are min-h-11 (assert the class); the selected value shows the blue-600 indicator; Esc closes and focus returns to the trigger; selecting a row calls onSelect with the model id; the loading + error copy strings render. Mock api via makeApiMock + vi.mock('../lib/api', ...).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ModelSelector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ModelSelector.test.tsx passes (a11y roles, keyboard, ≥44px rows, selected indicator, loading/error copy, onSelect fires).
    - Trigger carries aria-haspopup="listbox" and aria-expanded; open list uses role="listbox"/"option".
    - Free models show the 'Free' tag; paid show the '${price}/M tokens' hint; context line omitted when context_length null.
    - No shadcn import; no recompute of is_free (uses the row's is_free field).
    - Esc closes and returns focus to the trigger; outside-click closes.
  </acceptance_criteria>
  <done>A reusable, accessible, theme-aware ModelSelector renders the Phase-12 catalog with the locked copy and fires onSelect; its a11y test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ThemeToggle — Sun/Moon neutral toggle (class + localStorage + fire-and-forget PUT)</name>
  <read_first>
    - frontend/src/lib/themeBootstrap.ts (applyStoredTheme — reuse for the class flip; from Task 1)
    - frontend/src/lib/api.ts (apiFetch for the fire-and-forget PUT)
    - frontend/src/components/MobileDrawer.tsx / IconSidebar nav buttons (neutral icon-button styling to match)
    - .planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md (Components ThemeToggle + Copywriting labels + 'theme toggle is NOT accent')
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Code Examples — setTheme: localStorage = paint truth, server fire-and-forget; Pitfall 6 reconciliation)
  </read_first>
  <behavior>
    - Renders a neutral icon button (lucide Sun when current theme is dark → label 'Switch to light theme'; Moon when light → 'Switch to dark theme'). aria-pressed reflects the current theme. NOT blue-600.
    - On click: flips theme, sets document.documentElement dark class + writes localStorage.theme synchronously (localStorage is the paint source of truth — D-02), THEN fires apiFetch PUT /api/preferences {theme} fire-and-forget (.catch ignored — non-blocking; the class is already applied). (ThemeToggle.test.tsx: click flips html class + writes localStorage + calls apiFetch with PUT /api/preferences {theme})
    - Reads its initial theme from the current <html> class (set by the bootstrap) so it is consistent with first paint.
  </behavior>
  <action>
    Create frontend/src/components/ThemeToggle.tsx (default export). Internal state initialized from the current document.documentElement.classList.contains('dark'). On toggle: compute next theme, call applyStoredTheme-equivalent class flip (or directly classList.toggle('dark', next==='dark')), set localStorage.theme = next, then void apiFetch('/api/preferences', { method:'PUT', body: JSON.stringify({ theme: next }) }).catch(()=>{}). Render lucide Sun/Moon (20px) with the LOCKED aria-label per direction and aria-pressed. Neutral secondary-surface styling (theme-aware), focus:ring-blue-500 to match conventions. Do NOT make it accent-colored.
    Create frontend/src/components/ThemeToggle.test.tsx: render via renderWithProviders; mock ../lib/api with makeApiMock; assert initial label from the html class; click → assert document.documentElement.classList toggled, localStorage.theme written to the new value, and the mocked apiFetch called with '/api/preferences' + method PUT + a body containing the new theme. Assert the toggle is not blue-600 (aria-pressed present; neutral). Reset localStorage + html class between tests.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ThemeToggle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ThemeToggle.test.tsx passes (class flip + localStorage write + PUT fired with the new theme).
    - The PUT is fire-and-forget (a rejected apiFetch does not throw out of the click handler / does not revert the applied class).
    - aria-label matches the LOCKED direction strings; aria-pressed reflects the current theme.
    - lucide Sun/Moon used; styling is neutral (not blue-600).
  </acceptance_criteria>
  <done>ThemeToggle flips the root class, persists to localStorage, and fires a non-blocking PUT; its test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage.theme → first paint | A stored theme value drives the pre-mount class; an invalid value must not poison paint |
| GET /api/models response → DOM | Catalog strings (model names) render into the selector; must be escaped text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-THEME | Tampering | localStorage.theme | mitigate | applyStoredTheme treats any non-"dark" value as light (normalize); server CHECK + Pydantic Literal bound the persisted value (Plans 01/03) |
| T-13-XSS-SEL | Tampering | ModelSelector rows | mitigate | Model name/id rendered as React text (escaped by default), no dangerouslySetInnerHTML; data comes from the controlled model_cache via GET /api/models |
| T-13-CSRF | n/a | PUT /api/preferences from ThemeToggle | accept | apiFetch attaches the Supabase bearer; same-origin/proxy; no cookie-auth CSRF surface (existing app convention) |
</threat_model>

<verification>
- All three frontend test files green: `cd frontend && npx vitest run src/test/themeBootstrap.test.ts src/components/ModelSelector.test.tsx src/components/ThemeToggle.test.tsx`.
- `cd frontend && npm run build` (tsc + vite) passes — new files type-check under strict mode (noUnusedLocals/Parameters).
- index.css custom-variant uses :where(); index.html inline script present before the module script.
</verification>

<success_criteria>
- Flash-free theme bootstrap (testable lib + inline script), Tailwind class-strategy dark variant, and core-surface palette tokens exist.
- A reusable accessible ModelSelector renders the Phase-12 catalog with locked copy; a neutral ThemeToggle persists theme to localStorage + server.
- Three Wave 0 frontend tests green; build + tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/13-preferences-per-thread-model/13-05-SUMMARY.md`.
</output>
