---
phase: 13-preferences-per-thread-model
plan: 06
type: execute
wave: 5
depends_on: [13-03, 13-05]
files_modified:
  - frontend/src/components/DeprecationNotice.tsx
  - frontend/src/components/DefaultModelSelector.tsx
  - frontend/src/components/ChatContainer.tsx
  - frontend/src/components/ThreadSidebar.tsx
  - frontend/src/components/MobileDrawer.tsx
  - frontend/src/pages/ChatPage.tsx
  - frontend/src/hooks/useChat.ts
  - frontend/src/components/DeprecationNotice.test.tsx
  - frontend/src/components/DefaultModelSelector.test.tsx
  - frontend/src/components/ChatContainer.test.tsx
autonomous: false
requirements: [MODEL-05, MODEL-06, PREF-02]
user_setup: []

must_haves:
  truths:
    - "A per-thread ModelSelector lives in a NEW ChatContainer header row, PATCHing /api/threads/{id}; it shows the 'Default model' sub-state when threads.model is null (D-05)"
    - "A DefaultModelSelector + ThemeToggle live in the sidebar footer + mobile drawer, PUTting /api/preferences"
    - "Notice (role 'notice') rows render as a DeprecationNotice system line, not a bubble and not red"
    - "Theme reconciles server vs localStorage after GET /api/preferences (server wins if it differs)"
    - "Light mode reads coherently on the four core surfaces (chat, sidebar, login, composer)"
  artifacts:
    - path: "frontend/src/components/DeprecationNotice.tsx"
      provides: "persisted inline system notice line"
      min_lines: 12
    - path: "frontend/src/components/DefaultModelSelector.tsx"
      provides: "default-model control (heading + helper) PUTting preferences"
      min_lines: 25
    - path: "frontend/src/components/ChatContainer.tsx"
      provides: "new shrink-0 header row hosting the per-thread selector + notice render"
      contains: "ModelSelector"
  key_links:
    - from: "frontend/src/components/ChatContainer.tsx per-thread selector"
      to: "/api/threads/{id}"
      via: "onSelect → PATCH (or null clear)"
      pattern: "PATCH"
    - from: "frontend/src/components/DefaultModelSelector.tsx"
      to: "/api/preferences"
      via: "apiFetch PUT {default_model}"
      pattern: "/api/preferences"
    - from: "frontend/src/hooks/useChat.ts"
      to: "DeprecationNotice"
      via: "role 'notice' mapped + rendered as a system line"
      pattern: "notice"
---

<objective>
Wire the Plan 05 primitives into the real surfaces: a NEW per-thread ModelSelector header row in ChatContainer (PATCH /api/threads/{id}, with the 'Default model' sub-state when null — D-05), a DefaultModelSelector + ThemeToggle in the sidebar footer + mobile drawer (PUT /api/preferences — D-04), DeprecationNotice rendering for role 'notice' message rows (D-06), the post-login theme reconciliation (GET /api/preferences vs localStorage), and the D-01 light-mode application on the four core surfaces. Ends with a human-verify checkpoint for the visual/FOUC behaviors that jsdom cannot assert.

Purpose: completes MODEL-05, MODEL-06, PREF-02 on the UI side and surfaces SC#4's notice.
Output: 2 new components (DeprecationNotice, DefaultModelSelector), edits to ChatContainer/ThreadSidebar/MobileDrawer/ChatPage/useChat, 3 test files, and a human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md
@.planning/phases/13-preferences-per-thread-model/13-CONTEXT.md
@.planning/phases/13-preferences-per-thread-model/13-RESEARCH.md

<interfaces>
From Plan 05 (consume directly — do not rebuild):
  ModelSelector (default export) — props {value, onSelect, placeholder?, extraOption?, models?}
  ThemeToggle (default export)
  applyStoredTheme from src/lib/themeBootstrap.ts

From frontend/src/components/ChatContainer.tsx (CURRENT — the NEW header row goes above the scroll area; layout is flex-1 flex flex-col h-full):
  Props { messages: Message[]; onSend; isStreaming; onRetry }   // EXTEND with the active thread + its model
  Message { id; role: 'user'|'assistant'|'error'; content; toolsUsed? }   // ADD 'notice' to role union; render via DeprecationNotice

From frontend/src/hooks/useChat.ts (Message type + loadMessages map at lines 49-63; the map currently spreads m + toolsUsed):
  loadMessages maps server rows → Message; ADD 'notice' to the role union and carry it through (already spreads role/content)
  Message role union 'user'|'assistant'|'error' → ADD 'notice'

From frontend/src/pages/ChatPage.tsx (owns threads[], activeThreadId; handleSend; loadThreads):
  activeThread = threads.find(t => t.id === activeThreadId)   // EXTEND Thread type with model: string | null
  ThreadSidebar + MobileDrawer mount points for the default-model control + theme toggle

From frontend/src/components/ThreadSidebar.tsx — ThreadListContent renders the +New Chat + list; add a footer slot BELOW the list for the default-model control + toggle (desktop). MobileDrawer renders IconNavRow + ThreadListContent — add the same controls below IconNavRow.

From frontend/src/lib/api.ts: apiFetch (PATCH /api/threads/{id} {model}; PUT /api/preferences {default_model}; GET /api/preferences).

UI-SPEC LOCKED copy/placement:
  Per-thread header row: NEW row in ChatContainer, h-12, gray-900/gray-50 bg, border-b border-gray-800/gray-200, px-3, matches MobileTopBar; only renders when a thread is active.
  Per-thread selector: dropdown heading 'Model for this chat'; clear option 'Use my default model' (PATCH {model:null}); trigger sub-state 'Default model' when threads.model null.
  Default-model selector: heading 'Default model'; helper 'New chats use this model unless you pick a different one for a chat.' (PUT {default_model}).
  DeprecationNotice: full-width centered row, muted Info icon + Caption text, NOT a bubble, NOT a toast, NOT red. Dark gray-800/gray-300/gray-700; light gray-100/gray-700/gray-200. Copy is the server-composed content string (render verbatim, escaped).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeprecationNotice component + 'notice' role render in the message stream</name>
  <read_first>
    - frontend/src/hooks/useChat.ts (Message type + loadMessages map; the role union to extend)
    - frontend/src/components/ChatContainer.tsx (the messages.map render — add a 'notice' branch alongside the 'error' branch)
    - .planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md (DeprecationNotice component + Deprecation-notice colors + Caption typography)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Pattern 5 — notice is a persisted role row; Pitfall 3 — distinct role)
  </read_first>
  <behavior>
    - A message with role 'notice' renders as a DeprecationNotice: a full-width centered system line with a muted lucide Info icon + Caption text == the row's content, theme-aware quiet colors, NOT a MessageBubble, NOT red, NOT a toast. (DeprecationNotice.test.tsx)
    - The notice content (server-composed) renders as escaped React text (no dangerouslySetInnerHTML) — XSS mitigation.
    - useChat.loadMessages carries role 'notice' rows through unchanged (they already arrive from GET /api/threads/{id}); the Message role union includes 'notice'.
  </behavior>
  <action>
    Create frontend/src/components/DeprecationNotice.tsx (default export) — props { content: string }. Render a centered full-width row: muted lucide Info icon (14px, gray-400/gray-500) + Caption (text-xs) text = content, with the LOCKED quiet colors (dark gray-800 bg/gray-300 text/gray-700 border; light gray-100/gray-700/gray-200) via theme-aware classes; reuse the visual language of a system divider. Plain escaped text only.
    Edit frontend/src/hooks/useChat.ts: add 'notice' to the Message role union (and the duplicate union in ChatContainer). loadMessages already spreads role/content — confirm 'notice' rows pass through (no special handling needed beyond the type).
    Edit frontend/src/components/ChatContainer.tsx: in messages.map, add a branch — when msg.role === 'notice' render <DeprecationNotice content={msg.content} /> (alongside the existing 'error' → ErrorMessageBubble branch); otherwise MessageBubble. Extend the local Message role union to include 'notice'.
    Create frontend/src/components/DeprecationNotice.test.tsx: render with a content string; assert the text renders, an Info icon is present, it is NOT a message bubble (no MessageBubble role/markup), and no red/destructive classes. Also a ChatContainer-level assertion that a role 'notice' message renders the notice, not a bubble (can live in ChatContainer.test.tsx Task 3).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/DeprecationNotice.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DeprecationNotice.test.tsx passes (renders content, Info icon, not a bubble, not red).
    - useChat Message union + ChatContainer Message union both include 'notice'.
    - ChatContainer renders role 'notice' via DeprecationNotice, not MessageBubble.
    - Content rendered as escaped text (no dangerouslySetInnerHTML anywhere in the component).
  </acceptance_criteria>
  <done>role 'notice' rows render as a quiet, theme-aware system line; the notice test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-thread selector header row in ChatContainer + DefaultModelSelector + ThemeToggle placement + theme reconcile</name>
  <read_first>
    - frontend/src/components/ChatContainer.tsx (the flex-1 flex flex-col h-full layout — the new row is a shrink-0 sibling above the scroll area)
    - frontend/src/pages/ChatPage.tsx (Thread type, activeThread, handleSend, loadThreads — extend Thread with model; pass model into ChatContainer; reconcile theme after GET /api/preferences)
    - frontend/src/components/ThreadSidebar.tsx + MobileDrawer.tsx (footer/drawer mount points for the default-model control + toggle)
    - frontend/src/components/ModelSelector.tsx + ThemeToggle.tsx (Plan 05 — consume)
    - frontend/src/lib/themeBootstrap.ts (applyStoredTheme for reconcile)
    - .planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md (Components placements + Color core-surface tokens + Consistency requirements; Per-thread header row spec)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Pitfall 6 — server/localStorage reconcile is a one-frame snap, acceptable per D-02)
  </read_first>
  <behavior>
    - When a thread is active, ChatContainer renders a NEW shrink-0 h-12 header row (above the scroll area) hosting a ModelSelector with heading 'Model for this chat' and an extraOption {label:'Use my default model', value:null}. The trigger shows the thread's model name, or the 'Default model' sub-state when threads.model is null (D-05). On select of a model → PATCH /api/threads/{id} {model}; on the clear option → PATCH {model:null}. Hidden on the cold-start empty state (no active thread). (ChatContainer.test.tsx)
    - The sidebar footer (desktop ThreadSidebar) and the MobileDrawer (below IconNavRow) each host a DefaultModelSelector (heading 'Default model' + helper line, PUT /api/preferences {default_model}) and a ThemeToggle. (DefaultModelSelector.test.tsx)
    - After login/app load, ChatPage fetches GET /api/preferences; if the server theme differs from the current (localStorage-derived) theme, it applies the server theme via applyStoredTheme + updates localStorage (server wins — Pitfall 6); the default-model selector hydrates its current value from the same GET.
    - Light mode reads coherently on the four core surfaces: chat area (ChatContainer/ChatPage), sidebar (ThreadSidebar/MobileDrawer), composer (ChatInput is owned but its container surface), login (LoginPage) — apply the core-surface tokens so no orphan gray-950 panels bleed through (D-01). (Visual correctness is the human-verify in Task 4; this task wires the classes.)
  </behavior>
  <action>
    Create frontend/src/components/DefaultModelSelector.tsx (default export) — wraps ModelSelector with the LOCKED 'Default model' heading + helper sub-line; props { value: string | null, onChange: (id: string) => void }; on select calls onChange and the parent PUTs /api/preferences {default_model} (or PUT here directly via apiFetch — keep the PUT in this component for cohesion, mirroring ThemeToggle's self-contained PUT). Quiet styling per D-04 (temporary spot; P14 absorbs it).
    Edit frontend/src/components/ChatContainer.tsx: extend Props with activeThreadId: string | null and threadModel: string | null and an onThreadModelChange(modelId: string | null) callback. When activeThreadId is non-null, render the new shrink-0 h-12 header row (gray-900/gray-50 bg via tokens, border-b, px-3, matching MobileTopBar) hosting <ModelSelector value={threadModel} onSelect={onThreadModelChange} placeholder="Default model" extraOption={{label:'Use my default model', value:null}} /> with the 'Model for this chat' heading. Keep the existing flex-1 scroll area + ChatInput; the row is a shrink-0 sibling so the h-full layout is preserved (Consistency requirement). Apply core-surface tokens to the chat area background so light mode is coherent.
    Edit frontend/src/pages/ChatPage.tsx: extend the Thread type with model: string | null; pass activeThreadId, activeThread?.model ?? null, and an onThreadModelChange handler (PATCH /api/threads/{id} {model}, then update local threads state + the active thread's model) into ChatContainer. Add a one-time effect: GET /api/preferences on mount → reconcile theme (if server theme !== current, applyStoredTheme with the server value + write localStorage) and stash the default_model for the DefaultModelSelector. Mount DefaultModelSelector + ThemeToggle in the ThreadSidebar footer and the MobileDrawer (below IconNavRow) — thread the necessary props/handlers.
    Edit frontend/src/components/ThreadSidebar.tsx (ThreadListContent) + MobileDrawer usage: add a footer slot rendering the default-model control + ThemeToggle (quiet, secondary surface). Apply core-surface tokens to the sidebar background. Keep existing thread-list behavior unchanged.
    Create frontend/src/components/DefaultModelSelector.test.tsx: assert the heading + helper copy render; selecting a model fires PUT /api/preferences with {default_model: id} (mock apiFetch via makeApiMock).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/DefaultModelSelector.test.tsx && cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - DefaultModelSelector.test.tsx passes (heading/helper copy; select → PUT /api/preferences {default_model}).
    - ChatContainer renders the new h-12 header row only when activeThreadId is non-null; the per-thread selector shows 'Default model' when threadModel is null; selecting a model calls onThreadModelChange(id) and the clear option calls it with null (ChatPage PATCHes accordingly).
    - ChatPage reconciles theme after GET /api/preferences (server wins) and hydrates the default-model value.
    - DefaultModelSelector + ThemeToggle mounted in both the desktop sidebar footer and the mobile drawer.
    - `npm run build` (tsc strict + vite) passes — the layout edit preserves the flex h-full structure and all new props type-check.
  </acceptance_criteria>
  <done>The per-thread header selector, default-model control, theme toggle, and theme reconcile are wired into the real surfaces; build is clean and the default-model test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ChatContainer.test.tsx + full frontend suite wave-merge gate</name>
  <read_first>
    - frontend/src/components/ChatContainer.test.tsx (existing cases to extend, not replace)
    - .planning/phases/13-preferences-per-thread-model/13-VALIDATION.md (per-thread selector default sub-state case; per-wave merge runs npm test)
    - frontend/src/test/utils.tsx (renderWithProviders, makeApiMock)
  </read_first>
  <action>
    Extend frontend/src/components/ChatContainer.test.tsx with the MODEL-06 UI cases: (a) with activeThreadId set + threadModel null, the per-thread selector trigger shows the 'Default model' sub-state; (b) the new header row is absent on the cold-start empty state (activeThreadId null); (c) selecting a model in the header invokes onThreadModelChange with the model id and the clear option invokes it with null; (d) a role 'notice' message renders as the DeprecationNotice system line, not a bubble. Mock api via makeApiMock. Then run the FULL frontend suite as the wave-merge gate (13-VALIDATION Sampling Rate) to confirm no regression across the 999.1 chat tests and the Plan 05 component tests.
  </action>
  <verify>
    <automated>cd frontend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The extended ChatContainer.test.tsx cases pass (default sub-state, header hidden on empty state, onThreadModelChange fires with id/null, notice renders as system line).
    - `npm test` (full vitest run) is GREEN — no regression in the existing 999.1 ChatPage/ChatContainer tests or the Plan 05 ModelSelector/ThemeToggle/themeBootstrap tests.
  </acceptance_criteria>
  <done>ChatContainer's MODEL-06 + notice UI behaviors are covered; the full frontend suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify — light-mode visual coherence + FOUC-free reload + live per-thread model</name>
  <what-built>
    Claude has wired (and unit-tested where jsdom permits): the per-thread model selector header row, the default-model selector + theme toggle in the sidebar/drawer, the DeprecationNotice render, server/localStorage theme reconcile, and the D-01 light palette on the four core surfaces. The remaining checks require a real browser (rendered color/contrast and paint timing are not observable in jsdom — see 13-VALIDATION Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. Run frontend (npm run dev) + backend; log in (test creds in CLAUDE.md). 
    2. THEME: Toggle to light via the sidebar ThemeToggle. Inspect the four core surfaces — chat area, sidebar, composer (ChatInput), and login page — for coherent light styling, AA-readable muted text (price/context hints, 'Using default' sub-line), and NO orphan dark (gray-950) panels bleeding through (D-01). Toggle back to dark; confirm it still reads as before.
    3. FOUC: With light selected, hard-refresh (Cmd/Ctrl-R), ideally throttled — confirm NO dark flash before first paint (the inline index.html script applies the class pre-mount; Pitfall 5).
    4. CROSS-DEVICE (optional): change theme, confirm it persists after reload (localStorage) and that GET /api/preferences reconcile would win on a fresh device (Pitfall 6 — a one-frame snap is acceptable per D-02).
    5. PER-THREAD MODEL: open an active thread, pick a model in the header selector → confirm the trigger updates and (network tab) a PATCH /api/threads/{id} {model} fires; pick 'Use my default model' → PATCH {model:null} and the trigger shows 'Default model'. NOTE: the live LLM round-trip on the chosen model is deferred to the separate :free-model provider error (STATE.md D-999.1-LLM-A) — verify the PATCH + resolution wiring, not necessarily a streamed answer.
    6. DEFAULT MODEL: set a default via the sidebar control → PUT /api/preferences {default_model}; reload → the selector shows the persisted default.
  </how-to-verify>
  <action>
    Claude completes Tasks 1-3 (all automated wiring + the green unit suites) BEFORE this checkpoint — the checkpoint verifies only the jsdom-unobservable behaviors. Start the frontend (npm run dev) and backend, then pause for the human to run the six how-to-verify steps in a real browser (light-mode coherence on the four core surfaces, FOUC-free hard reload, server/localStorage theme reconcile, live PATCH on per-thread selection, persisted PUT for the default model). Do not mark the phase complete until the human confirms. If issues are reported (a dark panel bleeds through, a flash on reload), fix them in the relevant component before re-requesting approval.
  </action>
  <verify>
    <automated>MISSING — visual color/contrast + paint-timing (FOUC) are jsdom-unobservable per 13-VALIDATION Manual-Only Verifications; the automated coverage is the unit suites in Tasks 1-3 + Plan 05. This checkpoint is the visual/live gate.</automated>
  </verify>
  <acceptance_criteria>
    - Light mode reads coherently on chat/sidebar/composer/login with AA-readable muted text and no dark-panel bleed (D-01).
    - No theme flash on hard reload (FOUC-free).
    - Per-thread selection fires PATCH (set + null-clear); default-model fires PUT and persists across reload.
  </acceptance_criteria>
  <done>Light-mode coherence, FOUC-free reload, and the live PATCH/PUT wiring are human-confirmed; the phase UI side is complete.</done>
  <resume-signal>Type "approved" once light-mode coherence + FOUC-free reload + the PATCH/PUT wiring are confirmed, or describe issues (e.g. a dark panel bleeds through, a flash on reload) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server notice content → DOM | The role 'notice' content (composed in Plan 04) renders into the message stream |
| PATCH/PUT bodies from UI controls | Selector choices cross to the backend; user_id is JWT-bound server-side (Plans 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-XSS-NOTICE | Tampering | DeprecationNotice render | mitigate | content rendered as escaped React text, no dangerouslySetInnerHTML (Task 1); model ids originate from the controlled model_cache/owner default |
| T-13-XSS-SEL2 | Tampering | per-thread / default selectors | mitigate | model names rendered as escaped text (ModelSelector, Plan 05); FE never executes/interpolates them |
| T-13-THEME-UI | Tampering | theme reconcile | mitigate | server theme (Literal-validated, Plan 03) wins the reconcile; client normalizes any non-'dark' value to light (Plan 05 applyStoredTheme) |
| T-13-IDOR-UI | Tampering / EoP | PATCH /api/threads/{id} from header | mitigate | backend ownership re-check (Plan 03 T-13-IDOR); the UI only ever targets the active (owned) thread id |
</threat_model>

<verification>
- DeprecationNotice.test.tsx + DefaultModelSelector.test.tsx + extended ChatContainer.test.tsx green (Tasks 1-3).
- `cd frontend && npm test` full suite green (Task 3 wave-merge); `cd frontend && npm run build` clean (Task 2).
- Human-verify checkpoint (Task 4) approved for the jsdom-unobservable visual/FOUC/live-PATCH behaviors.
</verification>

<success_criteria>
- Per-thread selector (header row, 'Default model' sub-state, PATCH set/clear), default-model selector + theme toggle (sidebar/drawer, PUT), DeprecationNotice render, and theme reconcile are wired into the real surfaces.
- Light mode reads coherently on the four core surfaces with no FOUC (human-verified).
- Frontend suite + build green; all three requirement IDs satisfied end-to-end on the UI side.
</success_criteria>

<output>
After completion, create `.planning/phases/13-preferences-per-thread-model/13-06-SUMMARY.md`.
</output>
