---
phase: 13-preferences-per-thread-model
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - backend/routers/chat.py
autonomous: true
requirements: [MODEL-06]
user_setup: []

must_haves:
  truths:
    - "When a thread is pinned to a model absent from model_cache at send time, a persisted messages row with role 'notice' is inserted before the assistant row"
    - "The resolved model for that turn is overridden to the user default (or owner default) so the turn succeeds on the fallback model — no crash"
    - "The 'notice' row is filtered out of the history sent to the LLM (history map keeps only role in user/assistant)"
    - "An empty/partial model_cache does NOT trigger a spurious deprecation notice"
  artifacts:
    - path: "backend/routers/chat.py"
      provides: "deprecation detection + notice insert + model override + history filter"
      contains: "role"
  key_links:
    - from: "backend/routers/chat.py event_generator"
      to: "model_cache"
      via: "select model_id eq(thread.model) maybe_single"
      pattern: "model_cache"
    - from: "backend/routers/chat.py history map"
      to: "stream_chat_completion messages"
      via: "filter role in (user, assistant)"
      pattern: "role.*in.*assistant"
---

<objective>
Implement SC#4 / D-06: at send time, if the thread is pinned to a model that is no longer in the Phase 12 model_cache, insert a persisted inline notice row (role 'notice'), override the turn's model to the user default, and ensure the turn runs on the fallback model instead of crashing. Also filter 'notice' rows out of the LLM history map so the model never receives the notice as a turn.

Purpose: a pinned-then-deprecated model must degrade gracefully with a user-visible, reload-surviving notice — not a hard failure. Turns the Plan 01 Wave 0 deprecation-fallback tests GREEN.
Output: a localized edit in chat.py's event_generator (resolution helper _resolve_key_and_model stays a pure 3-tier function — the override lives in the caller, per RESEARCH Open Question 2 recommendation).
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-preferences-per-thread-model/13-RESEARCH.md
@.planning/phases/13-preferences-per-thread-model/13-CONTEXT.md
@.planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md

<interfaces>
From backend/routers/chat.py (the send-path region to edit — VERIFIED line refs):
  - history map at line 743: `messages = [{"role": m["role"], "content": m["content"]} for m in history.data]`
    → must become: `... for m in history.data if m["role"] in ("user", "assistant")` (Pitfall 3 — notice never reaches the LLM)
  - resolution at lines 787-789: `api_key, model, mode, is_user_key = _resolve_key_and_model(db, user_id, thread.data, body)` (UNCHANGED — keep it pure 3-tier)
  - the no_key refusal at 790-797 (return early) — the deprecation check goes AFTER this, before the assistant-row insert at 815-823
  - assistant-row insert at 815-823: `db.table("messages").insert({... "role": "assistant" ...})`
  - _safe_user_default_model(db, user_id) at lines 131-149 — reuse to get the fallback default

From RESEARCH Pattern 5 (the deprecation check shape; Assumption A2 — tolerate an empty cache):
  thread_model = thread.data.get("model")
  if thread_model:
      cached = db.table("model_cache").select("model_id").eq("model_id", thread_model).maybe_single().execute()
      if not (cached and cached.data):          # absent → deprecated
          default_model = _safe_user_default_model(db, user_id) or settings.llm_model
          db.table("messages").insert({"thread_id":..., "user_id":..., "role":"notice",
                                       "content": f'Model "{thread_model}" is no longer available — using {default_model} instead.'}).execute()
          model = default_model                 # OVERRIDE the resolved model for this turn

UI-SPEC § Copywriting Contract — LOCKED notice copy (exact, with runtime interpolation):
  Model "<model name>" is no longer available — using <default> instead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deprecation detection + notice insert + model override (after resolution, before assistant row)</name>
  <read_first>
    - backend/routers/chat.py (event_generator 745-823: history map 743, resolution 787-797, assistant insert 815-823; helpers 122-149)
    - backend/tests/test_deprecated_model_fallback.py (the RED Wave 0 cases this task turns green)
    - .planning/phases/13-preferences-per-thread-model/13-RESEARCH.md (Pattern 5; Pitfall 3; Open Question 2 — override in caller, not in resolver; Assumption A2 — empty-cache tolerance)
    - .planning/phases/13-preferences-per-thread-model/13-UI-SPEC.md (locked notice copy + colors — backend composes the exact sentence)
  </read_first>
  <behavior>
    - thread.model = a string NOT present in model_cache, cache non-empty → exactly one messages row inserted with role "notice" and content == `Model "<thread.model>" is no longer available — using <default_model> instead.`; the model passed to stream_chat_completion == default_model (not the deprecated string); no exception raised. (test_inserts_notice_and_falls_back)
    - thread.model is None (following default) → no model_cache lookup, no notice, model unchanged.
    - thread.model present AND in model_cache → no notice, model unchanged.
    - model_cache empty/partial (the lookup returns nothing for ALL ids, including valid ones) → skip the notice (Assumption A2: do not emit a spurious notice when the cache is mid-refresh/empty). Guard: only treat "absent" as deprecated when the cache has rows; if a quick `model_cache` existence/count read is empty, skip.
    - The notice is composed server-side from controlled strings and inserted as plain text (React escapes on render) — no HTML (T-13-XSS).
  </behavior>
  <action>
    Edit backend/routers/chat.py event_generator. AFTER the no_key early-return (line ~797) and BEFORE the assistant-row insert (line ~815), add the deprecation check:
    Read thread_model = thread.data.get("model"). If thread_model is truthy: first confirm the cache is non-empty (a lightweight read — e.g. select model_id limit 1 — to satisfy Assumption A2; if empty, skip the whole block). Then look up the pinned model: db.table("model_cache").select("model_id").eq("model_id", thread_model).maybe_single().execute(). If absent (not cached or not cached.data): compute default_model = _safe_user_default_model(db, user_id) or settings.llm_model; insert a messages row {thread_id, user_id, role:"notice", content: the LOCKED UI-SPEC sentence interpolating thread_model and default_model}; then OVERRIDE model = default_model for this turn (so stream_chat_completion runs on the fallback). Keep _resolve_key_and_model untouched (pure 3-tier — Open Question 2). The override is a local reassignment of the `model` variable already returned at line 787. Wrap the cache reads defensively so a model_cache read error never crashes the turn (log + skip the notice, fall back to whatever resolution produced).
    Then update the history map (line 743): change the comprehension to keep only rows where m["role"] in ("user", "assistant") — the 'notice' row must NOT flow into the LLM context (Pitfall 3). Note: line 743 runs before event_generator, so the filter must be applied where `messages`/`current_messages` is built from history.data; ensure the notice inserted inside event_generator is also not re-read into the model context for this turn (the inserted notice is for persistence/FE render only).
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest tests/test_deprecated_model_fallback.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_deprecated_model_fallback.py passes (test_inserts_notice_and_falls_back + test_notice_excluded_from_history GREEN).
    - The history/messages list passed to stream_chat_completion contains NO row with role "notice" (filter asserted by test_notice_excluded_from_history).
    - On a deprecated pin: exactly one role="notice" row is inserted with the LOCKED copy, and the model handed to stream_chat_completion equals the default (assertion in test_inserts_notice_and_falls_back).
    - When thread.model is None OR present-in-cache OR the cache is empty → no notice row inserted, model unchanged.
    - _resolve_key_and_model signature/body is unchanged (override lives in event_generator).
  </acceptance_criteria>
  <done>A thread pinned to a deprecated model inserts a persisted 'notice' line, falls back to the default model, and the notice never reaches the LLM — no crash. Empty-cache spurious notices are guarded out.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-merge backend suite + scrub/no-regression check</name>
  <read_first>
    - backend/routers/chat.py (the edited send path)
    - .planning/phases/13-preferences-per-thread-model/13-VALIDATION.md (Sampling Rate — per-wave merge runs the full backend suite)
  </read_first>
  <action>
    Run the full backend suite as the Wave-merge gate to confirm the chat.py edit did not regress the Phase 11 resolution/scrub/SSE-error behavior or the tool-loop. Confirm specifically that the existing chat send-path tests and test_key_model_resolution.py remain green (the deprecation override must not perturb the no_key fail-closed path or the usage drain). No code change in this task unless a regression is found; if one is found, fix it minimally within chat.py.
  </action>
  <verify>
    <automated>cd backend && ./venv/Scripts/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full backend suite green (deprecation-fallback cases plus all existing chat + resolution suites). The documented pre-existing record_manager debt (STATE.md Pending Todos) may remain — note it, do not fix here.
    - No change to the no_key fail-closed refusal or the SSE secret-scrub behavior (Phase 11 invariants).
  </acceptance_criteria>
  <done>Full backend suite green; the deprecation fallback is integrated without regressing the Phase 11 chat-loop seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored thread.model → LLM context | A pinned (possibly deprecated) model string and the composed notice must not poison the model context |
| composed notice → message stream | Server-composed text persisted as a message row, later rendered in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-NOTICE-HISTORY | Tampering / prompt-confusion | history map → stream_chat_completion | mitigate | History comprehension filters role in (user, assistant); the inserted 'notice' row is excluded from the LLM context (Pitfall 3) |
| T-13-XSS | Tampering | notice content | mitigate | Notice composed server-side from controlled model ids (model_cache / owner default); inserted as plain text; FE renders as escaped text, no dangerouslySetInnerHTML (Plan 06) |
| T-13-A2 | Denial of Service (spurious UX) | empty model_cache | mitigate | Guard skips the deprecation notice when the cache is empty/partial (Assumption A2) so a mid-refresh cache cannot flag valid models as deprecated |
| T-13-CRASH | Availability | model_cache read failure | mitigate | Cache reads wrapped defensively → a read error logs + skips the notice, never crashes the turn (SC#4 "not a crash") |
</threat_model>

<verification>
- tests/test_deprecated_model_fallback.py green (Task 1).
- Full backend suite green via `cd backend && ./venv/Scripts/python -m pytest -q` (Task 2 wave-merge gate).
- _resolve_key_and_model unchanged (grep the function body is byte-identical to pre-edit aside from the caller-side override).
</verification>

<success_criteria>
- A deprecated pin inserts a persisted role='notice' line, overrides the model to the default, and the turn succeeds — no crash.
- The notice never reaches the LLM (history filter).
- Empty-cache spurious notices guarded; cache-read failures degrade gracefully.
- Phase 11 chat-loop invariants intact (no_key fail-closed, scrub, usage drain).
</success_criteria>

<output>
After completion, create `.planning/phases/13-preferences-per-thread-model/13-04-SUMMARY.md`.
</output>
