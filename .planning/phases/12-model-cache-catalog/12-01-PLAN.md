---
phase: 12-model-cache-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/services/model_catalog_service.py
  - backend/tests/fixtures/openrouter_models_sample.json
  - backend/tests/test_model_catalog.py
  - backend/tests/test_config.py
autonomous: true
requirements: [MODEL-02, MODEL-03, MODEL-04, MODEL-07]
must_haves:
  truths:
    - "A `:free`-suffixed model AND a both-zero-priced model are both tagged free; an `openrouter/auto` `-1`-sentinel model is tagged NOT free and shows no price (D-11)"
    - "Per-Mtok hints compute correctly (gpt-4o-mini → 0.15 prompt / 0.60 completion); malformed/missing/negative pricing returns None and never crashes (D-11)"
    - "A model id in POPULAR_MODELS gets its list index as popularity_rank + source 'curated'; an id not in the list gets rank None (D-06, D-07, D-08, D-09)"
    - "model_cache_ttl_seconds defaults to 86400 and is env-overridable; refresh-if-stale treats TTL=0 and empty cache as stale (D-03)"
    - "refresh_if_stale fetches the full catalog with no subset and returns the existing cached rows unchanged when the upstream fetch fails — serve stale (D-01, D-04)"
  artifacts:
    - path: "backend/services/model_catalog_service.py"
      provides: "Pure tagging/pricing/popularity functions + refresh-if-stale orchestration + httpx catalog fetch"
      min_lines: 80
    - path: "backend/config.py"
      provides: "POPULAR_MODELS ordered list + model_cache_ttl_seconds setting"
      contains: "model_cache_ttl_seconds"
    - path: "backend/tests/fixtures/openrouter_models_sample.json"
      provides: "Offline real-shape OpenRouter fixture (gpt-4o-mini, a :free model, openrouter/auto -1, a missing-pricing edge)"
    - path: "backend/tests/test_model_catalog.py"
      provides: "Unit tests for tagging/Mtok/popularity/refresh covering MODEL-02/03/04/07"
  key_links:
    - from: "backend/services/model_catalog_service.py"
      to: "config.POPULAR_MODELS"
      via: "popularity_for() reads the ordered list at serve time"
      pattern: "POPULAR_MODELS"
    - from: "backend/services/model_catalog_service.py"
      to: "https://openrouter.ai/api/v1/models"
      via: "fetch_catalog() httpx GET, no Authorization header"
      pattern: "openrouter.ai/api/v1/models"
---

<objective>
Build the pure, side-effect-free core of the model catalog: defensive free/paid tagging, guarded per-Mtok price math, context-length surfacing, curated popularity tagging, and the refresh-if-stale orchestration helper — plus the `POPULAR_MODELS` list and `model_cache_ttl_seconds` config. This plan owns MODEL-02/03/04/07's testable logic and ships the Wave 0 unit-test scaffolds + offline fixture that the rest of the phase verifies against.

Purpose: The defensive pricing parse and refresh-if-stale logic are the only genuinely new reasoning in this phase (RESEARCH § Don't Hand-Roll). Isolating them as pure functions keeps them unit-testable offline (monkeypatched fetch, injected TTL=0) without a live API or DB.
Output: `backend/services/model_catalog_service.py`, `POPULAR_MODELS` + `model_cache_ttl_seconds` in `config.py`, the offline fixture, and `test_model_catalog.py` + extended `test_config.py`.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-model-cache-catalog/12-CONTEXT.md
@.planning/phases/12-model-cache-catalog/12-RESEARCH.md
@.planning/phases/12-model-cache-catalog/12-VALIDATION.md

<interfaces>
<!-- The verified OpenRouter per-model shape (RESEARCH § Verified Schema). Executor uses these directly — no live API exploration needed. -->
Per-model object keys this phase reads: `id` (str, `:free` suffix = free convention), `name` (str), `context_length` (int | null defensively), `pricing` (object, ALL VALUES STRINGS in per-token USD, e.g. prompt "0.00000015").
`pricing` sub-keys present: prompt, completion, image, audio, internal_reasoning, input_cache_read, input_cache_write, web_search. NOTE: `pricing.request` is ABSENT in the live API — do NOT depend on it.
Free rule (VERIFIED against 340 live models, zero contradictions): `is_free = id.endswith(":free") OR (pricing.prompt == "0" AND pricing.completion == "0")`.
`-1` sentinel: `openrouter/auto`/`fusion`/`pareto-code`/`bodybuilder` return prompt "-1" — NOT free, and per-Mtok must return None for them.
Per-Mtok: `float(per_token_str) * 1_000_000`, rounded — ONLY when parseable AND >= 0; else None.

From backend/services/budget_service.py (the httpx pattern to mirror — fetch_model_context_length, lines 83-106):
  httpx.get("https://openrouter.ai/api/v1/models", timeout=10) → resp.raise_for_status() → resp.json().get("data", []); swallow-on-failure with logger.warning.

From backend/config.py (Settings, pydantic-settings, @lru_cache get_settings()):
  Existing bool/int/str fields with defaults; env override via UPPER_SNAKE_CASE name. demo_fallback_model line 41, model_context_length line 126.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — offline fixture + RED unit-test scaffolds for catalog logic and config</name>
  <files>backend/tests/fixtures/openrouter_models_sample.json, backend/tests/test_model_catalog.py, backend/tests/test_config.py</files>
  <read_first>
    - backend/tests/test_config.py (existing test pattern: `from config import Settings; s = Settings()` + monkeypatch.setenv for env-override tests)
    - backend/tests/test_keys_status.py (test file structure norm; module-level imports inside test fns)
    - .planning/phases/12-model-cache-catalog/12-VALIDATION.md (exact test node IDs to create — bind to these names)
    - .planning/phases/12-model-cache-catalog/12-RESEARCH.md § Free/paid decision matrix + Per-Mtok math (the exact verified values to assert)
    - backend/tests/fixtures/explorer_fixtures.py (fixtures-dir convention; fixtures/ has __init__.py)
  </read_first>
  <behavior>
    - test_free_by_suffix: a model id ending `:free` → tag_is_free True
    - test_free_by_zero_price: pricing.prompt=="0" AND completion=="0" (no :free suffix) → True
    - test_sentinel_not_free: openrouter/auto with prompt "-1" → tag_is_free False AND price_per_mtok returns None
    - test_pricing_parse_guards: missing pricing key / None / non-numeric string → price_per_mtok None, no exception raised
    - test_price_per_mtok: "0.00000015" → 0.15; "0.0000006" → 0.60
    - test_context_length_nullsafe: model with context_length absent → surfaced as None, no KeyError
    - test_popularity_tagging: id at POPULAR_MODELS index 1 → (1, "curated"); id absent → (None, "curated")
    - test_refresh_when_stale: empty cache (and TTL=0) → fetch invoked, rows upserted, fresh model present
    - test_serve_cached_within_ttl: fresh fetched_at within TTL → fetch NOT invoked, cached rows returned
    - test_serve_stale_on_fetch_failure: stale rows present + fetch raises → existing rows returned, no exception
    - test_model_cache_ttl_default (in test_config.py): Settings().model_cache_ttl_seconds == 86400; MODEL_CACHE_TTL_SECONDS env override applies
  </behavior>
  <action>
    Create `backend/tests/fixtures/openrouter_models_sample.json` containing a small `data` list with exactly these real-shape rows (values copied from the RESEARCH verified schema): `openai/gpt-4o-mini` (pricing.prompt "0.00000015", completion "0.0000006", context_length 128000), one `:free` model (e.g. `meta-llama/llama-3.3-70b-instruct:free`, pricing both "0"), `openrouter/auto` (pricing.prompt "-1", completion "-1"), and one malformed-edge row (pricing key absent OR pricing.prompt null, context_length absent).
    Create `backend/tests/test_model_catalog.py` with the exact pytest node IDs listed under <behavior> (these MUST match 12-VALIDATION.md). Each test imports from `services.model_catalog_service` inside the function body (codebase norm). For refresh tests, monkeypatch `services.model_catalog_service.fetch_catalog` to return the fixture `data` (or raise for the failure case) and pass a MagicMock supabase `db` whose `.table("model_cache").select("*").execute().data` returns the cached-rows scenario; inject TTL via the `model_cache_ttl_seconds` setting (set 0 to force stale). Load the fixture via `Path(__file__).parent / "fixtures" / "openrouter_models_sample.json"`.
    Extend `backend/tests/test_config.py` with `test_model_cache_ttl_default` and `test_model_cache_ttl_env_override` mirroring the existing `test_chat_max_iterations_*` pattern (`Settings()` default + `monkeypatch.setenv("MODEL_CACHE_TTL_SECONDS", ...)`).
    These tests are written BEFORE the implementation — they MUST be RED on first run (import errors / AttributeError on the not-yet-created service symbols). Do NOT stub the implementation to make them pass here.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_model_catalog.py tests/test_config.py::test_model_cache_ttl_default -x 2>&1 | grep -Eq "ImportError|ModuleNotFoundError|AttributeError|error" && echo "RED as expected" || echo "UNEXPECTED: tests did not fail RED"</automated>
  </verify>
  <done>test_model_catalog.py exists with all 10 catalog node IDs from 12-VALIDATION.md; fixtures/openrouter_models_sample.json exists with the 4 required edge rows; test_config.py has model_cache_ttl default + env-override tests; the suite fails RED (service symbols not yet implemented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config — POPULAR_MODELS + model_cache_ttl_seconds; model_catalog_service pure functions + fetch + refresh-if-stale</name>
  <files>backend/config.py, backend/services/model_catalog_service.py</files>
  <read_first>
    - backend/config.py (Settings class — add fields alongside demo_fallback_model; @lru_cache get_settings())
    - backend/services/budget_service.py lines 83-106 (fetch_model_context_length: the exact httpx GET pattern + swallow-on-failure to generalize)
    - backend/services/openrouter_service.py (outbound httpx hygiene: timeout, raise_for_status, never log response body)
    - .planning/phases/12-model-cache-catalog/12-RESEARCH.md § Code Examples + Pattern 2 + Pattern 3 (fetch_catalog, price_per_mtok, tag_is_free, popularity_for, refresh_if_stale skeletons)
    - backend/tests/test_model_catalog.py (the RED tests from Task 1 — implement against their exact signatures)
  </read_first>
  <action>
    In `backend/config.py`: add `model_cache_ttl_seconds: int = 86400` (24h TTL, D-03; env override MODEL_CACHE_TTL_SECONDS) and `POPULAR_MODELS: list[str]` — an ordered list of OpenRouter model-id slugs curated from artificialanalysis.ai rankings (D-06/D-07, one-time human guide, NOT a runtime call); index 0 = most popular. Include a comment noting the list is code-reviewed/ships-with-deploy and that ids are finalized against live ids (a stale id self-heals to popularity_rank None per D-09). Seed with current well-known live ids (e.g. anthropic/claude-3.7-sonnet, openai/gpt-4o-mini, google/gemini-2.0-flash-001) — executor confirms against live slugs at build time.
    Create `backend/services/model_catalog_service.py` with pure functions: `fetch_catalog() -> list[dict]` (httpx.get to `https://openrouter.ai/api/v1/models`, timeout=10, NO Authorization header per RESEARCH § Auth — catalog is public; raise_for_status so caller decides D-04; never log the body); `price_per_mtok(per_token) -> float | None` (guarded: non-str → None, ValueError → None, v < 0 → None for the `-1` sentinel, else round(v * 1_000_000, 4)); `tag_is_free(model) -> bool` (id.endswith(":free") OR pricing.prompt=="0" AND pricing.completion=="0", all via .get()); `popularity_for(model_id, popular) -> tuple[int|None, str]` (popular.index → ("curated"); ValueError → (None, "curated"), D-08); `build_model_response(row) -> dict` composing is_free + price_per_mtok_prompt/completion + context_length (model.get, int|None) + popularity_rank/source + raw pricing retained (D-10).
    Add `refresh_if_stale(db) -> list[dict]` (D-03/D-04/D-05): select all rows, compute newest fetched_at, treat empty-or-(age > model_cache_ttl_seconds) as stale; if stale, call fetch_catalog() and upsert keyed on model_id (NEVER delete-all — Pitfall 5) then re-select; on fetch exception log a warning (scrub-free, no body) and return existing rows (serve stale, D-04). Advisory lock is OPTIONAL polish (RESEARCH A2 — idempotent upsert makes no-lock correct); skip it for minimum surface unless trivial. Read TTL via get_settings() at call time so tests can monkeypatch it.
    Defensive parsing is MANDATORY (D-11): never a blind float(); a malformed/missing pricing field must yield None and must not crash. Mirror budget_service's `from __future__ import annotations` + module logger.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_model_catalog.py tests/test_config.py::test_model_cache_ttl_default tests/test_config.py::test_model_cache_ttl_env_override -x</automated>
  </verify>
  <done>All test_model_catalog.py node IDs green; model_cache_ttl default 86400 + env override green; fetch_catalog sends no auth header; price_per_mtok returns None for "-1"/missing/non-numeric; tag_is_free matches the verified matrix; popularity_for returns rank by index else None; no blind float() anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenRouter upstream → backend service | Untrusted external catalog payload (string pricing, possible `-1` sentinel, possibly-missing fields) crosses into the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-V5-01 | Denial of Service | `price_per_mtok` / `tag_is_free` / `build_model_response` in model_catalog_service.py | mitigate | Defensive parse (D-11): guarded `float` inside try/except, `.get()` for every field, `int|None` context_length, `-1` → None. A malformed/hostile upstream model row must never raise. Covered by test_pricing_parse_guards + test_sentinel_not_free + test_context_length_nullsafe. |
| T-12-V5-02 | Tampering | `fetch_catalog` outbound URL | accept | URL is a hardcoded constant (`https://openrouter.ai/api/v1/models`); no user input flows into it → no SSRF surface. |
| T-12-INFO-01 | Information Disclosure | `fetch_catalog` httpx call | mitigate | Catalog endpoint is public/no-secret; the fetch sends NO Authorization header (RESEARCH § Auth) and never logs the response body — mirrors `openrouter_service.exchange_code` no-log-body hygiene even though there is no secret to leak here. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_model_catalog.py tests/test_config.py -x` green.
- Grep confirms no blind float of pricing: `grep -n "float(" backend/services/model_catalog_service.py` shows the conversion is inside a guarded helper only.
- `fetch_catalog` carries no `Authorization` header (grep shows no `Bearer`/`Authorization` in the fetch).
</verification>

<success_criteria>
- MODEL-02: free/paid tagging matches the verified matrix incl. `-1` sentinel = not free.
- MODEL-07: per-Mtok math + null-safe context_length surfaced via build_model_response.
- MODEL-03: popularity_rank from POPULAR_MODELS index; absent → None (degrades per D-09).
- MODEL-04: refresh_if_stale treats empty + TTL-lapsed as stale, serves stale on fetch failure, never delete-all.
- Defensive parsing (D-11) holds: no malformed input crashes the pure functions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-model-cache-catalog/12-01-SUMMARY.md`
</output>
