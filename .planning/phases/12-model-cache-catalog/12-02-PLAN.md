---
phase: 12-model-cache-catalog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240301000030_create_model_cache.sql
autonomous: true
requirements: [MODEL-01]
must_haves:
  truths:
    - "The model_cache table exists in the dev Supabase project after `supabase db push`"
    - "Any authenticated client can SELECT model_cache (permissive USING (true)); no authenticated INSERT/UPDATE/DELETE policy exists → clients cannot write"
    - "The service-role backend can upsert rows keyed on model_id (bypasses RLS)"
  artifacts:
    - path: "supabase/migrations/20240301000030_create_model_cache.sql"
      provides: "model_cache table (model_id PK, name, context_length, pricing JSONB, is_free, raw JSONB, fetched_at) + permissive SELECT RLS, no write policy"
      contains: "CREATE TABLE model_cache"
  key_links:
    - from: "supabase/migrations/20240301000030_create_model_cache.sql"
      to: "dev Supabase Postgres"
      via: "supabase db push (BLOCKING — table must physically exist before route tests in plan 12-03)"
      pattern: "model_cache"
---

<objective>
Create the `model_cache` Supabase table (migration 030) — the global, shared, non-user-scoped catalog store with row-per-model shape and a shared `fetched_at` staleness marker — with the inverted RLS posture (permissive SELECT, no client write policy → service-role-only writes). Then apply it LIVE to the dev Supabase project so the table physically exists before plan 12-03's route tests run.

Purpose: This is the persistence layer that survives Fly suspend / cold starts (data in Postgres, not process memory — D-05). Build/type checks pass WITHOUT the push (false-positive verification), so the `[BLOCKING]` push is mandatory: the live table must exist before the router and seed read/write it.
Output: `supabase/migrations/20240301000030_create_model_cache.sql` applied to dev Supabase.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-model-cache-catalog/12-CONTEXT.md
@.planning/phases/12-model-cache-catalog/12-RESEARCH.md

<interfaces>
<!-- RLS shape to MIRROR-AND-INVERT, from migration 20240301000025_create_user_api_keys.sql -->
user_api_keys is OWN-ROW (USING auth.uid() = user_id) + REVOKE SELECT from authenticated (it holds secrets).
model_cache is the INVERSE: GLOBAL shared, non-secret, identical for every user → permissive SELECT (USING (true)), and crucially NO INSERT/UPDATE/DELETE policy for authenticated (RLS denies client writes by default). The service-role client (database.get_supabase()) bypasses RLS and owns all writes.

Verified per-model fields to store (RESEARCH § Verified Schema):
  model_id (OpenRouter `id`, PK, e.g. openai/gpt-4o-mini), name (str), context_length (int, nullable-defensive — docs allow null), pricing (raw OpenRouter strings, JSONB — D-10 keeps raw), is_free (bool, precomputed at write per Open Q1 recommendation — enables SQL-side ?free_only), raw (optional JSONB full model), fetched_at (TIMESTAMPTZ, restamped every refresh = staleness marker).

Latest applied migration = 20240301000029_add_usage_to_messages.sql → next free = 030.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 030 — model_cache table + inverted (global-read, service-role-write) RLS</name>
  <files>supabase/migrations/20240301000030_create_model_cache.sql</files>
  <read_first>
    - supabase/migrations/20240301000025_create_user_api_keys.sql (the RLS migration shape to mirror-and-invert; header-comment convention; ENABLE ROW LEVEL SECURITY)
    - supabase/migrations/20240301000029_add_usage_to_messages.sql (most recent migration — confirms numbering + additive convention)
    - .planning/phases/12-model-cache-catalog/12-RESEARCH.md § Pattern 1 (the exact CREATE TABLE + RLS DDL) + § Security Domain (RLS posture rationale)
  </read_first>
  <action>
    Create `supabase/migrations/20240301000030_create_model_cache.sql`. Define `CREATE TABLE model_cache` with columns: `model_id TEXT PRIMARY KEY` (OpenRouter id), `name TEXT NOT NULL`, `context_length INTEGER` (nullable-defensive — Pitfall 4), `pricing JSONB NOT NULL` (raw OpenRouter pricing strings, D-10), `is_free BOOLEAN NOT NULL DEFAULT false` (precomputed at write per Open Q1 → enables SQL-side `?free_only`), `raw JSONB` (optional full raw model object for future fields), `fetched_at TIMESTAMPTZ NOT NULL DEFAULT now()` (staleness marker, restamped every refresh).
    `ALTER TABLE model_cache ENABLE ROW LEVEL SECURITY;`. Add ONE permissive SELECT policy: `CREATE POLICY "Anyone can read model catalog" ON model_cache FOR SELECT USING (true);`. Add NO INSERT/UPDATE/DELETE policy — RLS then denies all client writes by default; the service-role backend bypasses RLS and owns writes (state this in a header comment). Do NOT add an index beyond the PK unless trivial (a partial index on is_free is acceptable but optional). Lead with a header comment block mirroring migration 025's style explaining: global shared catalog, non-secret, the inverse of the own-row user_api_keys pattern, service-role-only writes.
  </action>
  <verify>
    <automated>test -f "supabase/migrations/20240301000030_create_model_cache.sql" && grep -q "CREATE TABLE model_cache" supabase/migrations/20240301000030_create_model_cache.sql && grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/20240301000030_create_model_cache.sql && grep -q "USING (true)" supabase/migrations/20240301000030_create_model_cache.sql && ! grep -Eqi "FOR (INSERT|UPDATE|DELETE)" supabase/migrations/20240301000030_create_model_cache.sql && echo "migration shape OK"</automated>
  </verify>
  <done>Migration file exists with model_cache table (model_id PK, pricing JSONB, is_free, raw JSONB, fetched_at), RLS enabled, one permissive SELECT policy USING (true), and zero client write policies.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] apply migration 030 to dev Supabase (supabase db push)</name>
  <files>supabase/migrations/20240301000030_create_model_cache.sql (applied — no new file)</files>
  <read_first>
    - supabase/migrations/20240301000030_create_model_cache.sql (the migration to apply)
    - Auto-memory [Supabase migration history repair] + [Dual Supabase envs] (push replays old migrations / "already exists"; .env = dev target)
  </read_first>
  <action>
    Apply migration 030 to the DEV Supabase project (the `.env` target — dual-env per project memory; dev is the default execution target). Run `supabase db push` from the repo root. If the push is non-TTY / prompts, set `SUPABASE_ACCESS_TOKEN` in the environment first. If `db push` replays old migrations or errors "already exists" (known per [Supabase migration history repair]), run `supabase migration repair --status applied <prior range>` for the already-applied 001-029 range, then re-run `supabase db push`.
    DEV ONLY this plan. PROD is deferred to deploy (D-03 precedent from Phases 9-11): for prod, the same `migration repair` caveat applies and the push must target `.env.prod` — capture that as an ops note in the SUMMARY, do NOT run a prod push as a dev task.
    This is BLOCKING: the live `model_cache` table must physically exist before plan 12-03's route/seed tests, because build/type checks pass without it (false-positive verification).
  </action>
  <verify>
    <automated>cd backend && python -c "from database import get_supabase; r = get_supabase().table('model_cache').select('model_id', count='exact', head=True).limit(1).execute(); print('model_cache reachable, count header OK')"</automated>
  </verify>
  <done>`model_cache` exists in dev Supabase and is reachable via the service-role client (a head/count probe succeeds, no relation-does-not-exist error). Prod push deferred to deploy with the migration-repair + .env.prod caveat recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client (incl. Text-to-SQL tool) → model_cache | Any logged-in client could attempt to read or write the catalog table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-V4-01 | Tampering | model_cache write path | mitigate | No INSERT/UPDATE/DELETE policy for `authenticated` → RLS denies all client writes by default; only the service-role backend (bypasses RLS) writes. A client cannot poison the model list. Verified by the absence of write policies in the migration (Task 1 grep gate). |
| T-12-V4-02 | Information Disclosure | model_cache read path | accept | Permissive SELECT `USING (true)` exposes the catalog to any authenticated client. Catalog is a non-secret, globally-identical public list (no per-user rows, no secrets) → exposure is intended. Contrast with user_api_keys which REVOKEs SELECT because it holds secrets. |
</threat_model>

<verification>
- Migration file present with the exact table shape + RLS posture (Task 1 grep gate).
- `model_cache` physically exists in dev Supabase (Task 2 service-role probe).
- No client write policy exists (grep gate in Task 1).
</verification>

<success_criteria>
- MODEL-01 (persistence half): the `model_cache` table backs `GET /api/models` and survives Fly suspend (data in Postgres).
- RLS posture correct: global read, service-role-only write — the inverse of the user_api_keys own-row secret pattern.
- Dev table live before route tests (the BLOCKING push closes the false-positive-verification gap).
</success_criteria>

<output>
After completion, create `.planning/phases/12-model-cache-catalog/12-02-SUMMARY.md`
</output>
