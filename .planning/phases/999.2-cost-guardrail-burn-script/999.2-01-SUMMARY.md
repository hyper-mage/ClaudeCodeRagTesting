---
phase: 999.2-cost-guardrail-burn-script
plan: 01
subsystem: testing
tags: [openrouter, owner-key, cost-guardrail, sec-03, ops-script, pytest, argparse, scrub-secrets]

# Dependency graph
requires:
  - phase: 11-per-request-key-model-resolution
    provides: "_resolve_key_and_model fail-closed seam (the D-05a app kill switch this plan pins by ID)"
  - phase: 11-per-request-key-model-resolution
    provides: "services.log_scrub.scrub_secrets (SEC-01 chokepoint) + llm_service.get_llm_client(trace=False)"
provides:
  - "backend/scripts/burn_owner_guardrail.py — safe-by-default OpenRouter owner-key burn script (the SEC-03 live-trip tool)"
  - "Pure, importable safety logic: should_abort / classify_status / next_batch_size / format_block_record"
  - "Three independent spend bounds: --confirm + ENV_FILE=.env.prod gate, $0.25 HARD abort, MAX_CALLS=300"
  - "Greppable D-05a kill-switch proof: test_sec03_killswitch_no_owner_spend_when_flag_off"
affects: [999.2-02 (the live human-gated trip), 15-options-ui-demo-gating (SEC-03 hard gate on demo_fallback_enabled)]

# Tech tracking
tech-stack:
  added: []  # no new dependencies — openai/httpx/pytest/argparse all already present
  patterns:
    - "Refuse-by-default ops script: stdlib argparse --confirm flag + os.environ ENV_FILE assert BEFORE any network (no prior repo analog)"
    - "Pure helpers extracted from a spend script so the safety math is unit-testable with ZERO spend / ZERO network"
    - "Mirror keys.py poll discipline in a script: scrubbed output, NO error-traceback logging, del key in finally"

key-files:
  created:
    - backend/scripts/burn_owner_guardrail.py
    - backend/tests/test_burn_guardrail.py
  modified:
    - backend/tests/test_key_model_resolution.py

key-decisions:
  - "SEC-03 NOT marked complete: this plan ships only the no-spend half (app kill switch + the burn tool). The provider-side $0.10 trip + empirical email finding is Plan 02 (human-gated). Marking SEC-03 done now would falsely clear the Phase 15 demo-flag gate."
  - "classify_status accepts BOTH 402 and 403 as blocked (OpenRouter docs contradict each other — RESEARCH Open Q1); 429 is rate_limited (back off), never a trip — avoids a false SEC-03 PASS."
  - "next_batch_size bounds worst-case added spend below the $0.25 HARD abort and shrinks toward 1 near the $0.10 trip, so natural overshoot is ~one call (Pitfall 3)."
  - "Module docstring made a raw string (r''') to kill an invalid-escape-sequence DeprecationWarning from the PowerShell venv\\Scripts path example."

patterns-established:
  - "Safe-by-default spend gate: assert_safe_to_spend raises SystemExit (non-zero, zero network) unless --confirm AND ENV_FILE=='.env.prod'."
  - "Greppable-by-requirement-ID test: re-assert an already-proven contract under a requirement-named test so evidence is discoverable by grep SEC-03."

requirements-completed: []  # SEC-03 only HALF-satisfied here; full closure is gated on Plan 02's live trip. Intentionally left unmarked.

# Metrics
duration: 12min
completed: 2026-06-30
---

# Phase 999.2 Plan 01: Cost-Guardrail Burn Script (no-spend half) Summary

**Safe-by-default OpenRouter owner-key burn script (TRIP_TARGET=0.10 / HARD_ABORT=0.25 / MAX_CALLS=300) with pure unit-tested safety logic plus a greppable SEC-03 app-kill-switch test — ships GREEN with ZERO money spent and ZERO paid OpenRouter calls.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-06-30T20:48:11Z
- **Completed:** 2026-06-30T21:00:19Z
- **Tasks:** 3
- **Files modified:** 3 (2 created, 1 modified)

## Accomplishments
- Shipped `backend/scripts/burn_owner_guardrail.py` — a standalone, refuse-by-default ops script that will exercise the prod owner account's $0.10 guardrail (D-05b) under three independent safety bounds: a `--confirm` + `ENV_FILE=.env.prod` gate, a $0.25 HARD abort (2.5×), and a MAX_CALLS=300 ceiling.
- Proved the script's safety logic with pure unit tests (`test_burn_guardrail.py`, 7 tests GREEN): abort thresholds, 402/403/429 classification against real `openai` typed errors, adaptive batch sizing that never overshoots the hard abort, and `sk-or-…` scrubbing in the block record.
- Pinned the D-05a app-level kill switch by requirement ID: `test_sec03_killswitch_no_owner_spend_when_flag_off` (GREEN) re-asserts that `demo_fallback_enabled=False` + no user key ⇒ `_resolve_key_and_model` returns `api_key=None` — no owner-key completion is ever minted.
- Wired the live serial adaptive burn loop + pre-flight `/api/v1/key` baseline poll + block classification behind the gate, with full secret hygiene (`trace=False` client, `scrub_secrets` on every line, NO error-traceback logging). The script is runnable but was NOT run — the live trip is Plan 02.

## Task Commits

Each task was committed atomically:

1. **Task 1: failing pure-logic tests (RED) + greppable SEC-03 kill-switch test (GREEN)** - `8ac0f49` (test)
2. **Task 2: pure core + safe-by-default spend gate (turn RED helpers GREEN)** - `78638dd` (feat)
3. **Task 3: serial adaptive burn loop + spend poll + block classification** - `1a95f7e` (feat)

**Plan metadata:** see final `docs(999.2-01)` commit.

## Files Created/Modified
- `backend/scripts/burn_owner_guardrail.py` - Safe-by-default burn script: spend bounds + pure helpers (`should_abort`, `classify_status`, `next_batch_size`, `format_block_record`), the `--confirm`+prod-env gate, the `poll_key_usage` spend reader, and the serial adaptive burn loop. Refuses to run (exit non-zero, zero network) by default.
- `backend/tests/test_burn_guardrail.py` - Pure-logic unit tests (zero spend, zero network) covering `-k abort`, `-k classify`, `-k spend`, `-k scrub` plus a constants-sanity check.
- `backend/tests/test_key_model_resolution.py` - Added `test_sec03_killswitch_no_owner_spend_when_flag_off` reusing the existing `_fake_settings` / `_db_with_key_row` fixtures; the two prior D-05a analog tests were left untouched.

## Decisions Made
- **SEC-03 left unmarked in REQUIREMENTS.md.** This plan satisfies only the no-spend half of SEC-03 (the app kill switch + the burn tool). SEC-03's done-bar (D-04: guardrail blocks + kill switch stops spend + email outcome recorded) requires the live provider-side trip, which is Plan 02 (human-gated). Marking it complete now would falsely clear the Phase 15 `demo_fallback_enabled` hard gate.
- **402 and 403 both count as `blocked`** (OpenRouter's own docs contradict each other — RESEARCH Open Q1); **429 is `rate_limited`** (back off, never a trip) to avoid a false PASS.
- **`resolved_llm_api_key` accessed bare** (it is a `@property`; calling it with `()` raises `TypeError`) and **never printed** — only crosses the boundary as a `Bearer` header inside `poll_key_usage`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Invalid escape sequence in the module docstring**
- **Found during:** Task 2 (script creation)
- **Issue:** The PowerShell usage example `venv\Scripts\python.exe scripts\burn_owner_guardrail.py` in the plain triple-quoted docstring produced `DeprecationWarning: invalid escape sequence '\S'` (and `\p`, `\b`) — a latent SyntaxError in future Python.
- **Fix:** Prefixed the module docstring with `r"""` so the backslash path examples are literal.
- **Files modified:** backend/scripts/burn_owner_guardrail.py
- **Verification:** `python -W error::DeprecationWarning -c "import scripts.burn_owner_guardrail"` imports clean; pytest emits no escape-sequence warning.
- **Committed in:** `78638dd` (Task 2 commit)

**2. [Rule 1 - Bug] Threat-model grep assertion `grep -c "exc_info" == 0` tripped by comments**
- **Found during:** Task 3 (burn loop wiring)
- **Issue:** I never use `exc_info=True`, but the literal token `exc_info` appeared in 4 explanatory comments, so the T-999.2-04 acceptance check `grep -c "exc_info" == 0` returned 4.
- **Fix:** Reworded the comments to "error-traceback logging" (preserving the T-999.2-04 reference and the no-traceback discipline) so the literal token is absent.
- **Files modified:** backend/scripts/burn_owner_guardrail.py
- **Verification:** `grep -c "exc_info" scripts/burn_owner_guardrail.py` == 0; behavior unchanged.
- **Committed in:** `1a95f7e` (Task 3 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 — bug). 
**Impact on plan:** Both are correctness/quality fixes within the plan's own files (one latent future-SyntaxError, one threat-model acceptance gate). No scope creep, no new functionality.

## Issues Encountered
- **Self-inflicted `git stash` reverted uncommitted Task 3 edits.** While checking whether the `test_config` failure was pre-existing, I ran `git stash` with Task 3 still uncommitted in the working tree; it stashed the entire Task 3 implementation, reverting the file to the Task 2 version. **Recovered cleanly with `git stash pop`** (the stash was intact); re-verified all Task 3 grep criteria + tests GREEN before committing. Lesson: never `git stash` with uncommitted task work in flight.

## Full-Suite Status (pre-existing, out of scope)
Full backend suite: **222 passed**, 1 failed, 2 errors. The 3 non-green items are pre-existing and unrelated to this plan's files (confirmed: my commits touch only the two test files + the new script). Logged to `.planning/phases/999.2-cost-guardrail-burn-script/deferred-items.md`:
- `test_config::test_key_encryption_secret_default` — fails because the dev `.env` has `KEY_ENCRYPTION_SECRET` set (Phase 9 BYOK work); reproduces in isolation (D-999.2-A).
- 2× `test_record_manager` integration errors — pre-existing missing-`user_id`-fixture debt, already in STATE.md Pending Todos (D-999.2-B).

## Security Notes
No new threat surface beyond the plan's `<threat_model>`. All five threats (T-999.2-01..05) are mitigated as planned: scrubbed output + `trace=False` (T-01), three spend bounds (T-02), `--confirm`+prod-env gate (T-03), no error-traceback logging where the key is live (T-04), 429≠trip classification (T-05). The only `sk-or-` literal in the script is a documentation reference (`sk-or-…` with an ellipsis) in the `format_block_record` docstring — never a real key.

## Next Phase Readiness
- **Plan 999.2-02 (live trip)** is unblocked: the runnable burn script + the verified-first pre-flight baseline poll are in place. The operator runs `ENV_FILE=.env.prod venv/Scripts/python.exe scripts/burn_owner_guardrail.py --confirm` (real money) and records the observed HTTP status + email outcome as the SEC-03 evidence.
- **SEC-03 remains OPEN** (half done). Phase 15's `demo_fallback_enabled` flip stays hard-gated until Plan 02 produces the SEC-03 PASS/BLOCKER finding.

---
*Phase: 999.2-cost-guardrail-burn-script*
*Completed: 2026-06-30*
