---
phase: 999.2
slug: cost-guardrail-burn-script
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-30
---

# Phase 999.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (backend) |
| **Config file** | backend (existing pytest setup; `backend/tests/`) |
| **Quick run command** | `backend/venv/Scripts/python -m pytest backend/tests/test_burn_guardrail.py -q` |
| **Full suite command** | `backend/venv/Scripts/python -m pytest backend/tests -q` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick run command
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _planner fills_ | | | SEC-03 | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] _planner fills_ — `backend/tests/test_burn_guardrail.py` stubs for the pure spend-math / abort / 402-403-classification / scrub logic
- [ ] Existing `backend/tests/test_key_model_resolution.py` already covers the D-05a app-level kill switch (`test_no_key_flag_off_refuses`, `test_fail_closed_no_or_fallback`) — reuse as SEC-03 evidence, do not duplicate.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live guardrail trip on prod owner account (402/403 block after ~$0.10) + email outcome | SEC-03 | Irreducibly requires real spend on the live OpenRouter account; the block + email are provider-side and cannot be exercised in CI | Operator runs the burn script against `ENV_FILE=.env.prod`, confirms calls blocked by ~$0.10 (hard abort $0.25), records observed status code + scrubbed body, and notes whether any email arrived (expected: none). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
