---
phase: 999.2-cost-guardrail-burn-script
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md
autonomous: false
requirements: [SEC-03]

must_haves:
  truths:
    - "D-04#1 / D-05b: a human operator runs the burn against the PROD owner account; the provider guardrail BLOCKS further owner-key calls just past ~$0.10 and the observed HTTP status (402 OR 403) + scrubbed body are recorded — OR the $0.25 HARD_ABORT is hit with no block, recorded as SEC-03 BLOCKER (D-06)"
    - "D-04#3: the email-on-trip outcome is empirically recorded by the operator; 'no email' is an ACCEPTED valid PASS finding (do NOT gate closure on a positive email)"
    - "D-05 (both switches): the finding cites the live provider block (D-05b) AND points to the GREEN app-level kill-switch tests from Plan 01 (D-05a) — owner-key exposure is bounded on both sides"
    - "The execution halts at the live-trip checkpoint under --chain/--auto: no paid call fires without an explicit human operator running it with --confirm"
    - "D-02 (rationale): the SEC-03 finding frames the verified $0.10 ceiling + kill switch as bounding owner-key exposure during owner-toggled demo windows (BYOK is the default for all other users) — this is why a PASS unblocks the Phase 15 demo-fallback flag"
  artifacts:
    - path: ".planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md"
      provides: "The durable SEC-03 finding Phase 15 reads before flipping demo_fallback_enabled in prod: observed HTTP status, scrubbed body excerpt, final cumulative spend, block-vs-abort verdict, email observation, D-05a evidence pointer, and a PASS/BLOCKER call"
      contains: "SEC-03"
  key_links:
    - from: "human operator"
      to: "backend/scripts/burn_owner_guardrail.py"
      via: "ENV_FILE=.env.prod venv/Scripts/python.exe scripts/burn_owner_guardrail.py --confirm"
      pattern: "ENV_FILE=.env.prod"
    - from: ".planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md"
      to: "backend/tests/test_key_model_resolution.py"
      via: "cites the GREEN D-05a kill-switch tests as the app-level half of SEC-03"
      pattern: "test_sec03_killswitch_no_owner_spend_when_flag_off"
---

<objective>
Discharge the irreducible, real-money half of SEC-03: a human operator runs the Plan 01 burn
script against the PROD owner OpenRouter account, observes the provider-side $0.10 guardrail trip
(D-05b), and the empirical outcome is recorded as the durable SEC-03 finding Phase 15 reads.

Purpose: SEC-03 cannot be closed without proving the provider guardrail actually blocks owner-key
spend on the live prod account (D-04#1). This action spends REAL money and trips an account-blocking
guardrail — it is intentionally human-gated (autonomous: false) so no paid call fires under --chain/--auto
without an operator. The block + email outcome (D-04#3) become the artifact that, together with Plan 01's
GREEN app-level kill-switch tests (D-05a), satisfies D-05 (both switches) and unblocks the Phase 15
demo-fallback flag.

Rationale (D-02): the demo fallback is owner-toggled ON only for controlled demos (BYOK is
everyone else's default), so this verified $0.10 ceiling + kill switch exist precisely to bound
owner-key exposure during those owner-initiated demo windows — that is what makes the finding the
gate for Phase 15's demo-fallback flag.

Output: .planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md (PASS or BLOCKER).
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.2-cost-guardrail-burn-script/999.2-CONTEXT.md
@.planning/phases/999.2-cost-guardrail-burn-script/999.2-RESEARCH.md
@.planning/phases/999.2-cost-guardrail-burn-script/999.2-01-SUMMARY.md

# This plan depends on Plan 01 having shipped backend/scripts/burn_owner_guardrail.py.
# Prior intel (auto-memory reference_openrouter_guardrails): Guardrails min threshold $0.10,
# $5 min top-up, NO email-alert toggle (Guardrails replaced email alerts) -> "no email" is EXPECTED.
# Dual envs (MEMORY): .env = dev, .env.prod = prod. D-01 requires .env.prod (the demo-funding account).
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator runs the LIVE burn against the prod owner account and observes the trip</name>
  <read_first>
    - backend/scripts/burn_owner_guardrail.py (the script being run — confirm the --confirm + PROD-env gate is present)
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-RESEARCH.md § Guardrail Trip Semantics / Open Questions 1 & 2 (402 vs 403 ambiguity; email unknown) + § Common Pitfalls 6 (account-empty 402 vs guardrail)
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-CONTEXT.md D-04 (the done-bar) + D-06 (the $0.25 abort)
  </read_first>
  <what-built>
    Plan 01 shipped backend/scripts/burn_owner_guardrail.py — a safe-by-default burn script (refuses
    without --confirm AND ENV_FILE=.env.prod), with a $0.25 hard abort, a MAX_CALLS=300 ceiling, serial
    adaptive batches, scrubbed output (never the sk-or- key), and 402/403=blocked vs 429=rate-limit
    classification. Its pure safety logic is unit-test GREEN; the live trip itself was deliberately NOT run.
  </what-built>
  <action>
    STOP — this task spends REAL money on the prod owner OpenRouter account and trips an account-blocking
    guardrail. It MUST be run by a human operator; do NOT run it autonomously. Instruct the operator to:
      1. Pre-check (Pitfall 6): confirm the prod owner account holds at least ~$0.30 of spendable credit so the
         run can reach the $0.25 abort if the guardrail fails to trip. The script's pre-flight poll prints the
         scrubbed current usage / limit_remaining — use it to confirm.
      2. Run, from the backend/ directory, against the PROD env (D-01):
         `ENV_FILE=.env.prod venv/Scripts/python.exe scripts/burn_owner_guardrail.py --confirm`
         (PowerShell: `$env:ENV_FILE=".env.prod"; venv\Scripts\python.exe scripts\burn_owner_guardrail.py --confirm`)
      3. OBSERVE and write down:
         - the observed HTTP status on the blocking call: 402 OR 403 = guardrail BLOCKED (SEC-03 PASS for the block
           condition); 429 = rate-limit, NOT a trip (the script backs off, keep watching); a 402 whose scrubbed body
           says "insufficient credits / add credits" is an account-empty result, NOT the guardrail (Pitfall 6).
         - the final cumulative spend printed by the script (expect ~$0.10–$0.12; if it reaches the $0.25 HARD_ABORT
           with no block, that is SEC-03 BLOCKER — do NOT enable the demo flag).
         - the scrubbed error-body excerpt the script prints (it must contain NO sk-or- fragment).
      4. EMAIL OUTCOME (D-04#3): after the run, manually check the OpenRouter account inbox / notifications and note
         whether ANY email arrived. "No email" is the EXPECTED and ACCEPTED finding — record it as-is; do NOT treat
         the absence of email as a failure.
    Report the observed status code, the final spend, the scrubbed body excerpt, and the email yes/no back so the
    finding artifact can be written in Task 2.
  </action>
  <how-to-verify>
    Operator confirms: the script blocked at ~$0.10 with an observed 402/403 (PASS) OR hit the $0.25 abort (BLOCKER);
    the final spend value; whether any email arrived. No sk-or- fragment appeared anywhere in the output.
  </how-to-verify>
  <acceptance_criteria>
    - The burn was run against ENV_FILE=.env.prod with --confirm by a human operator (not autonomously)
    - The operator has recorded: observed HTTP status code (402 / 403 / 429-then-block / none), final cumulative spend, the scrubbed body excerpt, and the email outcome (yes/no)
    - No `sk-or-` fragment appeared in the script output (confirmed by the operator)
    - A verdict is determined: PASS (blocked by ~$0.10, distinct from account-empty) or BLOCKER (reached $0.25 with no guardrail block)
  </acceptance_criteria>
  <resume-signal>Reply with the observed status code, final spend, scrubbed body excerpt, and email outcome (or "BLOCKER — hit $0.25 with no block").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the empirical SEC-03 finding (PASS or BLOCKER) for Phase 15</name>
  <read_first>
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-CONTEXT.md D-01..D-06 (the decisions the finding must cite)
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-01-SUMMARY.md (Plan 01 outcome — the GREEN D-05a tests to point at)
    - .planning/REQUIREMENTS.md (SEC-03 wording) + .planning/ROADMAP.md § Phase 15 success criterion 4 (the gate this finding releases)
  </read_first>
  <files>.planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md</files>
  <action>
    Using the operator-reported values from Task 1, write the SEC-03 finding artifact at
    .planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md. It MUST record, citing the
    decision IDs: the run date + that the target was the PROD owner account via ENV_FILE=.env.prod (D-01);
    the observed HTTP status on the blocking call and the verdict — BLOCKED at ~$0.10 via 402/403 = SEC-03
    PASS for D-05b, OR reached the $0.25 HARD_ABORT with no block = SEC-03 BLOCKER (D-06); the final cumulative
    spend; the scrubbed error-body excerpt (paste ONLY the scrubbed text the script printed — never a raw key,
    Bearer header, or unscrubbed body); the email-on-trip observation with "no email = ACCEPTED" framing (D-04#3);
    and the D-05 both-switches summary — the live provider block (D-05b) PLUS a pointer to the GREEN app-level
    kill-switch tests test_sec03_killswitch_no_owner_spend_when_flag_off / test_no_key_flag_off_refuses /
    test_fail_closed_no_or_fallback in backend/tests/test_key_model_resolution.py (D-05a). End with the explicit
    SEC-03 verdict (PASS or BLOCKER) and the one-line Phase 15 implication (safe to flip demo_fallback_enabled in
    prod, or do NOT). Do NOT paste any sk-or- fragment into the file.
  </action>
  <acceptance_criteria>
    - `.planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md` exists
    - `grep -c "sk-or-" ".planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md"` == 0 (no key fragment leaked into the artifact — T-999.2-06)
    - The file records all of: observed HTTP status, final cumulative spend, scrubbed body excerpt, email outcome, the D-05a test pointer, and an explicit PASS/BLOCKER SEC-03 verdict
    - The verdict references D-04 (done-bar), D-05 (both switches), and D-06 (the $0.25 abort)
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md" && ! grep -q "sk-or-" ".planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md" && grep -q "SEC-03" ".planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md" && echo OK</automated>
  </verify>
  <done>The SEC-03 finding artifact exists with the empirical block + email result, the D-05a evidence pointer, and an explicit PASS/BLOCKER verdict — and contains no sk-or- fragment. Phase 15 has the artifact it needs to gate demo_fallback_enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> prod owner OpenRouter account | Real money spent; an irreversible account-blocking guardrail trip |
| script output -> SEC-03 finding artifact | The recorded body/status could embed the owner key if not scrubbed |
| guardrail block signal -> PASS/BLOCKER verdict | A misread 429 (rate-limit) could yield a false SEC-03 PASS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.2-06 | Information Disclosure | 999.2-SEC-03-FINDING.md | mitigate | Record ONLY the scrubbed body excerpt the script prints; never paste a raw key/Bearer/unscrubbed body. Asserted by `grep -c "sk-or-" == 0` on the artifact. |
| T-999.2-07 | Repudiation (false PASS) | the operator's block reading | mitigate | The script classifies 429 as rate-limited (NOT a trip); only 402/403 count as a block. A 402 with an "insufficient credits" body is account-empty, NOT the guardrail (Pitfall 6) — the operator distinguishes them from the scrubbed body before recording PASS. |
| T-999.2-08 | Denial of Service (self-inflicted $$) | the live run | mitigate | Plan 01's $0.25 HARD_ABORT + MAX_CALLS=300 + serial batches bound the spend; the operator pre-confirms ~$0.30 credit (Pitfall 6) and watches the printed running total. |
| T-999.2-09 | (intentional, accepted) | temporary owner-account block | accept | Tripping the guardrail temporarily blocks the owner account, but demo_fallback_enabled=False means no user is on the owner key (D-01) — zero user impact; the block is the desired SEC-03 evidence. |
</threat_model>

<verification>
- The live trip was run by a human against ENV_FILE=.env.prod (D-01), not autonomously.
- D-04 done-bar satisfied: (1) guardrail BLOCKS at ~$0.10 (402/403 recorded) OR documented BLOCKER at $0.25; (2) kill switch — D-05a GREEN tests (Plan 01) + D-05b live block recorded; (3) email outcome empirically recorded ("no email" accepted).
- 999.2-SEC-03-FINDING.md exists, contains no sk-or- fragment, and states an explicit PASS/BLOCKER verdict.
</verification>

<success_criteria>
- A human operator ran the burn against the prod owner account and observed the result.
- The SEC-03 finding artifact records the observed HTTP status, final spend, scrubbed body, email outcome, the D-05a test pointer, and a PASS/BLOCKER verdict — with no secret leak.
- Phase 15 has a durable SEC-03 finding to read before flipping demo_fallback_enabled in prod.
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-cost-guardrail-burn-script/999.2-02-SUMMARY.md`.
</output>
