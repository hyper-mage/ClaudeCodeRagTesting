---
phase: 999.2-cost-guardrail-burn-script
plan: 02
subsystem: security
tags: [sec-03, openrouter, cost-guardrail, owner-key, live-trip, human-gated]

requires:
  - phase: 999.2-cost-guardrail-burn-script
    provides: "backend/scripts/burn_owner_guardrail.py (Plan 01 — the burn tool) + GREEN D-05a kill-switch tests"
provides:
  - ".planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md — the durable SEC-03 PASS finding Phase 15 reads before flipping demo_fallback_enabled"
affects: [15-options-ui-demo-gating (SEC-03 hard gate — now satisfied)]

key-files:
  created:
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-SEC-03-FINDING.md

key-decisions:
  - "Live trip observed status 403 (not 402) — resolves RESEARCH Open Q1; classify_status accepts both, correctly classified 403 as blocked."
  - "Blocked at $0.1026 (~$0.10), well under the $0.25 hard abort — genuine budget guardrail, not account-empty (Pitfall 6 cleared)."
  - "No email on trip — EXPECTED + ACCEPTED per D-04#3; closure not gated on email."
  - "SEC-03 verdict PASS: both kill switches proven (D-05b live 403 block + D-05a GREEN tests)."

requirements-completed: []  # SEC-03 dependency (999.2) satisfied; SEC-03 itself closes in Phase 15 when the demo flag decision is made using this finding.

duration: manual (human-gated live run)
completed: 2026-06-30
---

# Phase 999.2 Plan 02: Live Guardrail Trip + SEC-03 Finding

**A human operator ran the Plan-01 burn against the PROD owner OpenRouter account; the provider budget guardrail BLOCKED with HTTP 403 at $0.1026, no email — SEC-03 PASS, recorded as the durable finding Phase 15 reads.**

## Accomplishments
- Ran `ENV_FILE=.env.prod ... burn_owner_guardrail.py --confirm` against the prod owner account (D-01), authorized by the operator.
- Provider-side ceiling (D-05b) confirmed: **403 "API key budget limit exceeded (daily limit)"** after **$0.1026** over 71 serial calls; never reached the $0.25 abort.
- Email outcome (D-04#3): **none** — expected + accepted.
- Wrote `999.2-SEC-03-FINDING.md` with the verdict (PASS), observed status, spend, scrubbed body, email outcome, and both-switch evidence (D-05a tests + D-05b block).
- No OpenRouter key fragment (`sk-or…`) in output or artifact (SEC-01 / T-999.2-06 clean).

## Task Commits
1. **Task 1 (checkpoint:human-action):** operator ran the live trip; observed 403 block at $0.1026, no email.
2. **Task 2:** SEC-03 finding artifact written.

## Decisions Made
- Trip status is **403** for this account's guardrail (RESEARCH Open Q1 resolved).
- SEC-03 dependency (999.2) satisfied; SEC-03 itself remains for Phase 15 to close when it acts on the demo flag using this finding.

## Deviations from Plan
- **Owner-key config gap surfaced + fixed mid-run:** the first attempt 401'd because `.env.prod` had no `LLM_API_KEY` (fell back to an OpenAI `sk-proj…` key, invalid for OpenRouter). Operator authorized copying the dev `.env` OpenRouter owner key (`sk-or…` prefix) into `.env.prod` (same account); re-run succeeded. Flagged as a Phase 15 deployment note (Fly prod must set the same `LLM_API_KEY`).

## Security Notes
- Owner OpenRouter key (`sk-or…`) never printed/logged; the only body recorded is the scrubbed 403 guardrail message. `.env.prod` is gitignored — the copied key is local-only.

## Next Phase Readiness
- **SEC-03 gate for Phase 15 is now GREEN** (PASS finding exists). Phase 15 may enable `demo_fallback_enabled` in prod from a cost-exposure standpoint, after ensuring the Fly prod `LLM_API_KEY` owner-key secret is set (deployment note in the finding).

---
*Phase: 999.2-cost-guardrail-burn-script*
*Completed: 2026-06-30*
