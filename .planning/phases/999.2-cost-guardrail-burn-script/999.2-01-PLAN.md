---
phase: 999.2-cost-guardrail-burn-script
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/burn_owner_guardrail.py
  - backend/tests/test_burn_guardrail.py
  - backend/tests/test_key_model_resolution.py
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "D-05a (app-level kill switch): with demo_fallback_enabled=False and no user key, chat._resolve_key_and_model returns api_key=None — no owner-key completion is minted — re-asserted GREEN and greppable by SEC-03"
    - "D-01 / safe-by-default: burn_owner_guardrail.py refuses to run (exits non-zero, makes ZERO network calls) unless BOTH --confirm is passed AND os.environ['ENV_FILE'] == '.env.prod'"
    - "D-06: the script's pure core HARD-ABORTS at $0.25 cumulative spend and at MAX_CALLS=300, sizes serial batches so worst-case added spend never crosses $0.25, and classify_status maps 402/403 -> blocked, 429 -> rate_limited, else -> unexpected"
    - "SEC-01: every line the script emits and every provider error body passes through scrub_secrets — a synthetic sk-or- token is redacted to [redacted-key], never echoed"
    - "D-03: the guardrail is treated as already-configured on the prod owner account; the script does a defensive pre-flight /api/v1/key usage poll to read the starting cumulative spend before any paid call (verify-first), so the live trip in Plan 02 measures the delta from a known baseline"
  artifacts:
    - path: "backend/scripts/burn_owner_guardrail.py"
      provides: "Safe-by-default burn script: TRIP_TARGET=0.10 / HARD_ABORT=0.25 / MAX_CALLS=300 constants, should_abort, classify_status, next_batch_size, format_block_record pure helpers, --confirm + PROD-env safety gate, serial adaptive burn loop, scrubbed POSIX-exit main"
      contains: "HARD_ABORT = 0.25"
      min_lines: 120
    - path: "backend/tests/test_burn_guardrail.py"
      provides: "Pure-logic unit tests (zero spend, zero network): -k abort, -k classify, -k spend, -k scrub"
      min_lines: 60
    - path: "backend/tests/test_key_model_resolution.py"
      provides: "Adds test_sec03_killswitch_no_owner_spend_when_flag_off re-asserting the D-05a contract, greppable by SEC-03"
      contains: "test_sec03_killswitch_no_owner_spend_when_flag_off"
  key_links:
    - from: "backend/tests/test_burn_guardrail.py"
      to: "backend/scripts/burn_owner_guardrail.py"
      via: "from scripts.burn_owner_guardrail import should_abort, classify_status, next_batch_size, format_block_record, TRIP_TARGET, HARD_ABORT, MAX_CALLS"
      pattern: "from scripts.burn_owner_guardrail import"
    - from: "backend/scripts/burn_owner_guardrail.py"
      to: "config.get_settings"
      via: "settings.resolved_llm_api_key (BARE @property, no parens) + settings.llm_base_url"
      pattern: "settings\\.resolved_llm_api_key"
    - from: "backend/scripts/burn_owner_guardrail.py"
      to: "services.log_scrub.scrub_secrets"
      via: "every print/log line + every error body routed through scrub_secrets; client built trace=False"
      pattern: "scrub_secrets"
---

<objective>
Build the no-spend half of SEC-03: a safe-by-default OpenRouter burn script plus its
pure-logic unit tests, and re-assert the already-proven app-level kill switch (D-05a) as
a greppable SEC-03 test. NOTHING in this plan spends money or makes a paid OpenRouter
call — the live trip is Plan 02 (a human-gated checkpoint).

Purpose: SEC-03 requires owner-key cost exposure to be bounded by BOTH an app-level kill
switch (D-05a, demo_fallback_enabled=False) and a provider-side $0.10 guardrail (D-05b).
This plan ships the tool that will exercise D-05b under three independent safety bounds
(--confirm gate, $0.25 hard abort, MAX_CALLS ceiling), proves its safety logic with pure
unit tests, and pins the D-05a contract by requirement ID. Half of SEC-03 ships GREEN here.

Output: backend/scripts/burn_owner_guardrail.py, backend/tests/test_burn_guardrail.py, and
one added test in backend/tests/test_key_model_resolution.py.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.2-cost-guardrail-burn-script/999.2-CONTEXT.md
@.planning/phases/999.2-cost-guardrail-burn-script/999.2-RESEARCH.md
@.planning/phases/999.2-cost-guardrail-burn-script/999.2-PATTERNS.md

# Project rules (CLAUDE.md): raw OpenAI SDK only (NO LangChain), backend runs under venv,
# secrets NEVER printed/logged, Python type hints + `str | None` syntax, double quotes / 4-space.

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration required. -->

From backend/config.py (load_dotenv fires AT IMPORT, keyed on os.environ["ENV_FILE"] — DO NOT set ENV_FILE after `import config`):
```python
# config.py:7-10
_env_filename = os.environ.get("ENV_FILE", ".env")
load_dotenv(os.path.join(os.path.dirname(__file__), "..", _env_filename))

# config.py:92  llm_base_url: str = "https://openrouter.ai/api/v1"
# config.py:37  demo_fallback_enabled: bool = False   (the D-05a app kill switch)

@property                                   # config.py:182-184 — PROPERTY. Access BARE, NO ().
def resolved_llm_api_key(self) -> str:      # settings.resolved_llm_api_key() raises TypeError.
    return self.llm_api_key or self.openai_api_key

@lru_cache                                  # config.py:195-197
def get_settings() -> Settings: ...
```

From backend/services/log_scrub.py (SEC-01 chokepoint — do NOT hand-roll a new regex):
```python
def scrub_secrets(s: str) -> str:           # re.sub(r"sk-or-[A-Za-z0-9_-]+", "[redacted-key]", s)
    ...                                      # non-str passes through unchanged
```

From backend/services/llm_service.py (build the client WITHOUT LangSmith pollution — trace=False):
```python
def get_llm_client(api_key: str | None = None, trace: bool = True) -> OpenAI: ...
# usage.cost is read off the response via getattr (llm_service.py:55):
#   call_cost = getattr(resp.usage, "cost", 0.0) or 0.0   # USD, always present now
```

From backend/routers/keys.py:135-165 (GET /api/v1/key poll shape + no-exc_info discipline to mirror):
```python
resp = httpx.get("https://openrouter.ai/api/v1/key",
                 headers={"Authorization": f"Bearer {owner_key}"}, timeout=15)
resp.raise_for_status()
data = resp.json().get("data") or {}
server_usage = data.get("usage")            # cumulative USD on the key (cross-check)
limit_remaining = data.get("limit_remaining")  # informational only; may be None
# on failure: logger.warning(scrub_secrets(str(e)))  — NO exc_info; `del key` in finally.
```

From backend/tests/test_error_surfacing.py:122-127 (typed-error builder — copy verbatim into the test file):
```python
def _status_error(status_code: int, message: str) -> openai.APIStatusError:
    req = httpx.Request("POST", "https://openrouter.ai/api/v1/chat/completions")
    resp = httpx.Response(status_code, request=req)
    cls = openai.RateLimitError if status_code == 429 else openai.APIStatusError
    return cls(message, response=resp, body=None)
```

From backend/routers/chat.py:152-207 (the D-05a contract the greppable test pins):
```python
def _resolve_key_and_model(db, user_id, thread_row, body) -> tuple[str | None, str, str, bool]:
    # keyless + demo_fallback_enabled=False -> returns (None, model, "no_key", False)
    # NO owner-key completion is minted.
```

From backend/tests/test_key_model_resolution.py:30-87 (REUSE these fixtures for the new test):
```python
def _fake_settings(**overrides): ...        # defaults demo_fallback_enabled=False, resolved_llm_api_key="sk-or-v1-OWNERKEY"
def _db_with_key_row(encrypted_key, ...): ...  # encrypted_key=None -> no key row
# existing analogs to mirror: test_no_key_flag_off_refuses (:90-107), test_fail_closed_no_or_fallback (:265-289)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing pure-logic tests (RED) + the greppable SEC-03 kill-switch test (GREEN)</name>
  <read_first>
    - backend/tests/test_burn_guardrail.py (the file you are CREATING — confirm absent first)
    - backend/tests/test_error_surfacing.py:122-127 (the _status_error typed-error builder to copy verbatim)
    - backend/tests/test_keys_balance.py:35-40 (the _key_response {"data": {...}} httpx.Response builder, if a poll-parse test is added)
    - backend/tests/test_key_model_resolution.py:1-22, 30-87, 90-107, 265-289 (module-docstring style + _fake_settings/_db_with_key_row fixtures + the two existing D-05a analogs)
    - backend/pytest.ini (testpaths=tests, python_files=test_*.py, --strict-markers, asyncio_mode=auto)
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-RESEARCH.md § Validation Architecture (the Req->Test Map: function names + -k selectors)
  </read_first>
  <files>backend/tests/test_burn_guardrail.py, backend/tests/test_key_model_resolution.py</files>
  <action>
    Create backend/tests/test_burn_guardrail.py covering the script's PURE helpers (zero spend,
    zero network). It MUST `from scripts.burn_owner_guardrail import should_abort, classify_status,
    next_batch_size, format_block_record, TRIP_TARGET, HARD_ABORT, MAX_CALLS` — this import RED-fails
    until Task 2 creates the module; that failure IS the RED state. Function names MUST contain the
    -k selectors from the RESEARCH Test Map so the verify commands resolve: abort, classify, spend, scrub.
      - `-k abort`: should_abort returns "BLOCKER_NO_TRIP_AT_ABORT" when running_total >= HARD_ABORT (0.25),
        "BLOCKER_CALL_CEILING" when calls >= MAX_CALLS (300), None below both.
      - `-k classify`: classify_status(402) == "blocked", (403) == "blocked", (429) == "rate_limited",
        (400)/(401)/(500) == "unexpected". Build the real typed errors with the copied `_status_error` helper
        and assert classify_status(e.status_code) where useful.
      - `-k spend`: next_batch_size never plans a batch whose worst-case added cost
        (size * max_cost_per_call) would push running_total past HARD_ABORT, returns >= 1, and shrinks toward 1
        as running_total approaches TRIP_TARGET (0.10). Assert the bound for several (running_total, max_cost_per_call) pairs.
      - `-k scrub`: feed a synthetic `sk-or-v1-LEAKEDSECRETKEY1234567890` inside the body arg of
        format_block_record(403, body, 0.11); assert "sk-or-" not in the result AND "[redacted-key]" in it.
    Then add ONE test to backend/tests/test_key_model_resolution.py reusing its existing _fake_settings /
    _db_with_key_row fixtures (do NOT duplicate them): `test_sec03_killswitch_no_owner_spend_when_flag_off` —
    docstring naming SEC-03 + D-05a; with _db_with_key_row(None) and _fake_settings(demo_fallback_enabled=False),
    patch.object(chat, "get_settings", ...), call chat._resolve_key_and_model, assert api_key is None,
    mode == "no_key", is_user_key is False, and api_key != settings.resolved_llm_api_key. This pins the
    app-level kill switch by requirement ID; it is GREEN immediately (the contract already ships). Keep all
    tests synchronous plain-assert (no async). Do NOT touch the two existing analog tests.
  </action>
  <acceptance_criteria>
    - `grep -c "from scripts.burn_owner_guardrail import" backend/tests/test_burn_guardrail.py` returns >= 1
    - test_burn_guardrail.py defines test functions whose names match `-k abort`, `-k classify`, `-k spend`, `-k scrub`
    - `grep -c "def test_sec03_killswitch_no_owner_spend_when_flag_off" backend/tests/test_key_model_resolution.py` == 1
    - The new kill-switch test reuses existing _fake_settings / _db_with_key_row (no re-definition of those helpers)
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_key_model_resolution.py::test_sec03_killswitch_no_owner_spend_when_flag_off -x` PASSES (GREEN — D-05a proven)
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_burn_guardrail.py` FAILS at collection/import (RED — scripts.burn_owner_guardrail not yet created)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_key_model_resolution.py::test_sec03_killswitch_no_owner_spend_when_flag_off -x</automated>
  </verify>
  <done>The kill-switch test passes (D-05a greppable by SEC-03); the burn-helper tests exist and fail only on the missing import (RED, ready for Task 2). No production code written yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the script's pure core + safety gate (turn RED helpers GREEN)</name>
  <behavior>
    - should_abort(running_total, calls): "BLOCKER_NO_TRIP_AT_ABORT" at running_total >= 0.25; "BLOCKER_CALL_CEILING" at calls >= 300; None otherwise.
    - classify_status(code): 402 -> "blocked", 403 -> "blocked", 429 -> "rate_limited", else -> "unexpected".
    - next_batch_size(running_total, max_cost_per_call, base_batch): >= 1; never returns a size where running_total + size*max_cost_per_call > 0.25; shrinks toward 1 as running_total nears 0.10.
    - format_block_record(code, body, running_total): returns a single scrubbed summary string — scrub_secrets applied so no sk-or- survives.
    - safety gate: refuses (raises SystemExit non-zero, ZERO network) unless --confirm AND os.environ["ENV_FILE"] == ".env.prod".
  </behavior>
  <files>backend/scripts/burn_owner_guardrail.py</files>
  <read_first>
    - backend/scripts/burn_owner_guardrail.py (the file you are CREATING)
    - backend/scripts/verify_langsmith_routing.py:23-34 (bootstrap: sys.path.insert("..") then `from config import get_settings`; `def main() -> int` / `sys.exit(main())`)
    - backend/config.py:7-10, 92, 182-184 (import-time dotenv load keyed on ENV_FILE; llm_base_url; resolved_llm_api_key is a @property — BARE access)
    - backend/services/log_scrub.py:15-26 (scrub_secrets — import it, do not re-implement)
    - backend/scripts/seed_model_cache.py:44-81 (try/except -> logged, non-zero POSIX exit; main()/sys.exit shape)
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-PATTERNS.md §A,§G,§H + § No Analog Found (the --confirm + PROD-assert gate has no repo precedent — use stdlib argparse)
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-RESEARCH.md § Code Examples (pure spend/abort core) + § Anti-Patterns (no parens on resolved_llm_api_key; ENV_FILE before import)
  </read_first>
  <action>
    Create backend/scripts/burn_owner_guardrail.py with the standalone bootstrap copied from
    verify_langsmith_routing.py:23-34 (sys.path.insert(0, join(dirname(__file__), "..")) then
    `from config import get_settings`). Add a module docstring stating: targets the PROD owner account
    (D-01), spends REAL money, run only as `ENV_FILE=.env.prod venv/Scripts/python.exe scripts/burn_owner_guardrail.py --confirm`.
    Define module-level constants TRIP_TARGET = 0.10, HARD_ABORT = 0.25 (D-06), MAX_CALLS = 300, and
    PAID_MODEL = "openai/gpt-4o-mini" (D-06). Import scrub_secrets from services.log_scrub.
    Implement the PURE, importable helpers exactly to the Task 1 test contract:
      - should_abort(running_total: float, calls: int) -> str | None
      - classify_status(code: int) -> str  (402/403 -> "blocked", 429 -> "rate_limited", else "unexpected"; do NOT pin a single code — OpenRouter docs disagree 402 vs 403, RESEARCH Open Q1)
      - next_batch_size(running_total: float, max_cost_per_call: float, base_batch: int = 5) -> int
        (bound so running_total + size*max_cost_per_call <= HARD_ABORT; min 1; shrink toward 1 near TRIP_TARGET)
      - format_block_record(code: int, body: str, running_total: float) -> str  (one scrubbed line; wrap body and the whole output in scrub_secrets)
    Add the safety gate function (no repo analog) using stdlib argparse: a `--confirm` store_true flag; the gate
    raises SystemExit with a scrubbed non-zero message UNLESS BOTH `args.confirm` is True AND
    `os.environ.get("ENV_FILE") == ".env.prod"`. The gate MUST run before any client build or network call.
    Add `def main() -> int` that parses args, runs the gate, and (for now) returns 0 after the gate passes —
    the live burn loop + poll land in Task 3. Close with `if __name__ == "__main__": sys.exit(main())`.
    NEVER print settings.resolved_llm_api_key; access it BARE (no parens). No fenced implementations beyond the
    cited analogs.
  </action>
  <acceptance_criteria>
    - `grep -E "TRIP_TARGET = 0\.10|HARD_ABORT = 0\.25|MAX_CALLS = 300" backend/scripts/burn_owner_guardrail.py` matches all three constants
    - `grep -c "settings.resolved_llm_api_key(" backend/scripts/burn_owner_guardrail.py` == 0 (BARE property — never called with parens)
    - `grep -c "from services.log_scrub import scrub_secrets" backend/scripts/burn_owner_guardrail.py` == 1
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_burn_guardrail.py -x` is GREEN (all pure-logic tests pass)
    - `cd backend && venv/Scripts/python.exe scripts/burn_owner_guardrail.py` (no --confirm, no ENV_FILE) exits NON-ZERO with a refusal and makes no network call
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_burn_guardrail.py -x</automated>
  </verify>
  <done>All four pure-logic tests are GREEN; the safety gate refuses without --confirm AND ENV_FILE==.env.prod; resolved_llm_api_key is accessed bare; scrub_secrets is imported, not re-rolled.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the serial adaptive burn loop + spend poll + block classification (no spend in CI)</name>
  <read_first>
    - backend/scripts/burn_owner_guardrail.py (the file from Task 2 — you are extending it)
    - backend/services/llm_service.py:11-28, 43-59 (get_llm_client(api_key, trace=False); usage.cost via getattr)
    - backend/routers/keys.py:135-165 (httpx.get /api/v1/key, {"data": {...}}, 15s timeout, scrubbed warning, NO exc_info, `del key` in finally)
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-PATTERNS.md §C,§D,§E,§F (client build / usage.cost / poll / block classification)
    - .planning/phases/999.2-cost-guardrail-burn-script/999.2-RESEARCH.md § Common Pitfalls 2,3,6 (per-call cost too small -> big prompt + high max_tokens + adapt from usage.cost; overshoot; account-empty 402 vs guardrail)
  </read_first>
  <files>backend/scripts/burn_owner_guardrail.py</files>
  <action>
    Extend main() (after the gate passes) with the live burn flow, all behind the Task 2 gate so nothing runs
    without --confirm + prod env. Build the client via get_llm_client(api_key=settings.resolved_llm_api_key,
    trace=False) — trace=False keeps burn traffic OUT of the prod LangSmith project (Pitfall 5). Define a sizeable
    BIG_PROMPT and a high max_tokens so each gpt-4o-mini call costs enough to reach $0.10 in a bounded number of
    calls (Pitfall 2); adapt batch sizing from the observed per-call usage.cost. PRE-FLIGHT (D-03 defensive +
    Pitfall 6): poll GET /api/v1/key (mirror keys.py:135-151), print the SCRUBBED current usage + limit_remaining
    (never the key), and abort with a clear message if the account looks under-funded to reach $0.25. BURN LOOP:
    serial issuance in small batches sized by next_batch_size(running_total, observed_max_cost_per_call); after each
    call add getattr(resp.usage, "cost", 0.0) or 0.0 to running_total; between batches re-poll /api/v1/key as a
    reconciliation cross-check and call should_abort(running_total, calls) — on a non-None result, emit the scrubbed
    BLOCKER record and return non-zero (SEC-03 BLOCKER: guardrail did NOT trip by $0.25). Wrap each create() in
    `except openai.APIStatusError as e`: read e.status_code, build a scrubbed body via
    scrub_secrets(getattr(getattr(e, "response", None), "text", "") or str(e)), branch on classify_status(code):
    "blocked" -> print format_block_record(code, body, running_total) and return 0 (PASS-block, the SEC-03 trip
    signal); "rate_limited" -> back off (sleep) and continue (NOT a trip); "unexpected" -> emit scrubbed + return
    non-zero. Mirror keys.py discipline: NO exc_info on any path where the key/Bearer local is live; route every
    print/log line through scrub_secrets. Keep the script synchronous. Distinguish a guardrail 402/403 from an
    account-empty 402 by recording the scrubbed body text (Pitfall 6) so the operator can tell them apart.
  </action>
  <acceptance_criteria>
    - `grep -c "trace=False" backend/scripts/burn_owner_guardrail.py` >= 1
    - `grep -c "exc_info" backend/scripts/burn_owner_guardrail.py` == 0 (no traceback path can capture the Bearer key — T-999.2-04)
    - `grep -E "openrouter.ai/api/v1/key" backend/scripts/burn_owner_guardrail.py` matches (pre-flight + between-batch poll)
    - `grep -c "classify_status" backend/scripts/burn_owner_guardrail.py` >= 1 (loop uses the pure classifier)
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_burn_guardrail.py -x` still GREEN
    - `cd backend && venv/Scripts/python.exe scripts/burn_owner_guardrail.py --confirm` (ENV_FILE unset -> not prod) exits NON-ZERO via the PROD-assert, making no paid call
  </acceptance_criteria>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_burn_guardrail.py -x && cd backend && venv/Scripts/python.exe -m pytest tests/test_key_model_resolution.py -k "killswitch or fail_closed or no_key_flag_off" -x</automated>
  </verify>
  <done>The burn loop, pre-flight poll, and block classification are wired behind the safety gate; the unit suite + the D-05a kill-switch tests are GREEN; --confirm without prod env still refuses to spend. The script is runnable but is NOT run here — the live trip is Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops script -> OpenRouter API | Owner `sk-or-…` key crosses as `Authorization: Bearer …`; paid completions are minted here |
| ops script -> stdout / logs | Every emitted line could leak the owner key or an unscrubbed provider body |
| CLI flags / env -> spend path | --confirm + ENV_FILE gate the irreversible real-money action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.2-01 | Information Disclosure | every print/log line + provider error body in burn_owner_guardrail.py | mitigate | Route all output + error bodies through `scrub_secrets`; never print `settings.resolved_llm_api_key`; build client with `trace=False` (no LangSmith). Asserted by the `-k scrub` test and `grep` on `format_block_record`. |
| T-999.2-02 | Denial of Service (self-inflicted $$) | the serial burn loop | mitigate | Three independent bounds: `--confirm` gate, `HARD_ABORT = 0.25` (2.5× threshold), `MAX_CALLS = 300`; serial small batches sized by `next_batch_size` with a between-batch spend re-check. Asserted by `-k abort` / `-k spend`. |
| T-999.2-03 | Tampering / Repudiation | run target (account/env) | mitigate | Safety gate refuses unless `--confirm` AND `os.environ["ENV_FILE"] == ".env.prod"`; ENV_FILE set in the shell BEFORE `import config` (load_dotenv fires at import). Asserted by the no-flag and --confirm-without-prod-env refusal checks. |
| T-999.2-04 | Information Disclosure | exception handling around create() and the /key poll | mitigate | Catch + scrub provider errors; NO `exc_info` on any path where the Bearer/key local is live (mirror keys.py:154-165). Asserted by `grep -c "exc_info" == 0`. |
| T-999.2-05 | Tampering (false PASS) | block classification | mitigate | `classify_status` treats 429 as `rate_limited` (back off), NEVER as a trip; only 402/403 count as `blocked`. Do not pin a single code (docs disagree). Asserted by `-k classify`. |
</threat_model>

<verification>
- Full backend suite green: `cd backend && venv/Scripts/python.exe -m pytest`
- Burn-script pure-logic suite green: `cd backend && venv/Scripts/python.exe -m pytest tests/test_burn_guardrail.py -x`
- D-05a kill switch greppable + GREEN: `cd backend && venv/Scripts/python.exe -m pytest tests/test_key_model_resolution.py -k killswitch -x`
- Safety gate proven without spend: the no-flag run and the --confirm-without-prod-env run both exit non-zero with NO network call.
- No secret leak: `grep -rn "sk-or-" backend/scripts/burn_owner_guardrail.py` returns only the scrub-pattern reference (never a literal key); `resolved_llm_api_key` is never called with parens.
</verification>

<success_criteria>
- backend/scripts/burn_owner_guardrail.py exists with TRIP_TARGET=0.10 / HARD_ABORT=0.25 / MAX_CALLS=300, the four pure helpers, the --confirm + PROD-env gate, and the serial adaptive burn loop — and refuses to spend by default.
- backend/tests/test_burn_guardrail.py covers abort / classify / spend / scrub and is GREEN.
- backend/tests/test_key_model_resolution.py contains test_sec03_killswitch_no_owner_spend_when_flag_off (GREEN) — D-05a (app kill switch) is proven and greppable by SEC-03.
- Zero money spent; no paid OpenRouter call made anywhere in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-cost-guardrail-burn-script/999.2-01-SUMMARY.md`.
</output>
