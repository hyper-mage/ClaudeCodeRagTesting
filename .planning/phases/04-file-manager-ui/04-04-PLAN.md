---
phase: 04-file-manager-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - frontend/package.json
  - frontend/src/hooks/useSelection.ts
  - frontend/src/components/BulkActionBar.tsx
  - frontend/src/components/FolderTreeItem.tsx
  - frontend/src/components/FileListRow.tsx
  - frontend/src/components/FileListView.tsx
  - frontend/src/pages/DocumentsPage.tsx
autonomous: false
requirements:
  - FMGR-05
  - FMGR-07
  - FMGR-09
user_setup: []
must_haves:
  truths:
    - "User can drag a file from the file list onto a folder in the tree to move it"
    - "User can drag a folder in the tree to reorder/move it within private folders"
    - "User can drop an external file from the OS onto a folder in the tree to upload it there"
    - "Valid drop targets show blue highlight border; Board Games folders show not-allowed cursor"
    - "User can select multiple files via checkboxes and shift-click range selection"
    - "Bulk action bar appears when items selected showing count and Delete/Move buttons"
  artifacts:
    - path: "frontend/src/hooks/useSelection.ts"
      provides: "Checkbox state, shift-click range select, selectAll, clearSelection"
      exports: ["useSelection"]
    - path: "frontend/src/components/BulkActionBar.tsx"
      provides: "Fixed toolbar for bulk delete and move operations"
      exports: ["default"]
  key_links:
    - from: "frontend/src/pages/DocumentsPage.tsx"
      to: "@dnd-kit/react"
      via: "DragDropProvider wrapper"
      pattern: "DragDropProvider"
    - from: "frontend/src/components/FolderTreeItem.tsx"
      to: "@dnd-kit/react"
      via: "useDroppable hook for folder drop targets"
      pattern: "useDroppable"
    - from: "frontend/src/components/FileListRow.tsx"
      to: "@dnd-kit/react"
      via: "useDraggable hook for file drag sources"
      pattern: "useDraggable"
---

<objective>
Add drag-and-drop (internal moves + external file uploads), bulk selection with shift-click, and bulk action bar. Uses @dnd-kit/react for internal DnD and native events for external file drops.

Purpose: Completes the file manager interaction model: users can reorganize their documents by dragging, select multiple items for batch operations, and upload directly to folders by dropping files.
Output: Full drag-drop reorganization, bulk operations, and drop-to-upload functionality.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-file-manager-ui/04-CONTEXT.md
@.planning/phases/04-file-manager-ui/04-RESEARCH.md
@.planning/phases/04-file-manager-ui/04-UI-SPEC.md
@.planning/phases/04-file-manager-ui/04-01-SUMMARY.md
@.planning/phases/04-file-manager-ui/04-02-SUMMARY.md
@.planning/phases/04-file-manager-ui/04-03-SUMMARY.md

<interfaces>
<!-- @dnd-kit/react API (from research) -->
@dnd-kit/react 0.3.2 provides:
- DragDropProvider: wraps the entire DnD context
- useDraggable({ id }): returns { ref, isDragging } for drag sources
- useDroppable({ id }): returns { ref, isDropTarget } for drop targets
- onDragEnd callback on DragDropProvider: receives { source, target } with IDs

Note: @dnd-kit/react 0.3.2 is pre-1.0. The API above is based on research.
Verify actual API by checking node_modules after install. Key patterns:
- DragDropProvider wraps the tree, onDragEnd handles the drop logic
- useDraggable on FileListRow and FolderTreeItem (drag source)
- useDroppable on FolderTreeItem (drop target)

<!-- From Plan 02/03 frontend -->
From frontend/src/hooks/useFolderTree.ts:
```typescript
export function useFolderTree() {
  // Returns { folders, selectedFolderId, expandedIds, folderContents,
  //   loadingContents, selectFolder, toggleExpand, refreshTree, breadcrumbs,
  //   createFolder, renameFolder, deleteFolder, moveFolder, moveDocument, renameDocument }
}
```

From frontend/src/hooks/useDocuments.ts:
```typescript
export function useDocuments() {
  // Returns { documents, loading, uploadDocument, deleteDocument, loadDocuments }
  // uploadDocument(file: File, folderId?: string)
}
```

Backend bulk endpoints (from Plan 01):
- POST /api/documents/bulk-delete { ids: string[] }
- POST /api/documents/bulk-move { ids: string[], folder_id: string | null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit/react, create useSelection hook and BulkActionBar, add DnD to tree and file list</name>
  <files>frontend/package.json, frontend/src/hooks/useSelection.ts, frontend/src/components/BulkActionBar.tsx, frontend/src/components/FolderTreeItem.tsx, frontend/src/components/FileListRow.tsx, frontend/src/components/FileListView.tsx, frontend/src/pages/DocumentsPage.tsx</files>
  <read_first>
    - frontend/package.json (current dependencies, check no @dnd-kit already)
    - frontend/src/pages/DocumentsPage.tsx (current state from Plan 03, wire DnD + bulk selection)
    - frontend/src/components/FolderTreeItem.tsx (add drop target + drag source)
    - frontend/src/components/FileListRow.tsx (add drag source + checkbox)
    - frontend/src/components/FileListView.tsx (add bulk bar, external file drop zone)
    - frontend/src/hooks/useFolderTree.ts (moveFolder, moveDocument APIs)
    - frontend/src/hooks/useDocuments.ts (uploadDocument with folderId)
    - .planning/phases/04-file-manager-ui/04-RESEARCH.md (DnD patterns, useSelection pattern, external file detection)
    - .planning/phases/04-file-manager-ui/04-UI-SPEC.md (drag feedback styles, bulk action bar interaction)
  </read_first>
  <action>
**Install @dnd-kit/react:**
```bash
cd frontend && npm install @dnd-kit/react
```

**IMPORTANT:** After install, check the actual API by reading `node_modules/@dnd-kit/react/dist/index.d.ts` or the package README. The @dnd-kit/react 0.3.2 API may differ from older @dnd-kit/core. Adapt imports and component names to match the actual installed package. Key things to verify:
- The provider/context component name (DragDropProvider, DndContext, or similar)
- The draggable hook name and signature (useDraggable, useDrag, or similar)
- The droppable hook name and signature (useDroppable, useDrop, or similar)
- The event callback shape (onDragEnd, onDrop, etc.)
If the API differs from what's described below, adapt accordingly while maintaining the same user-facing behavior.

**Create `frontend/src/hooks/useSelection.ts`:**

Follow the exact pattern from 04-RESEARCH.md code example:
```typescript
export function useSelection(items: { id: string }[]) {
  // State: selected: Set<string>, lastClicked: string | null
  // toggle(id: string, shiftKey: boolean): single toggle or shift-range select
  // selectAll(): select all items
  // clearSelection(): clear all + reset lastClicked
  // Return { selected, toggle, selectAll, clearSelection }
}
```

Implementation notes:
- Range select: when shiftKey is true and lastClicked exists, find indices of lastClicked and id in items array, select all items in that range (inclusive)
- Without shift: toggle single item (add if not present, remove if present)
- Always update lastClicked to current id

**Create `frontend/src/components/BulkActionBar.tsx`:**

Props: `{ selectedCount: number, onDelete: () => void, onMove: () => void, onClear: () => void }`

Per UI-SPEC Bulk Action Bar contract:
- Container: `px-4 py-2 bg-gray-800 border-b border-gray-700 flex items-center justify-between`
- Left: `{N} selected` in `text-sm text-gray-300`
- Right: two buttons:
  - "Delete {N} Items": `bg-red-600 hover:bg-red-700 text-white text-xs px-3 py-1.5 rounded flex items-center gap-1` with Trash2 icon
  - "Move to...": `bg-gray-700 hover:bg-gray-600 text-white text-xs px-3 py-1.5 rounded flex items-center gap-1` with FolderInput icon
- Escape key: call onClear to deselect all (add useEffect keydown listener)

**Update `frontend/src/components/FolderTreeItem.tsx`:**

Add @dnd-kit drop target behavior:
- Use the droppable hook from @dnd-kit/react on the tree item row (for private folders only)
- Use the draggable hook for private folders (not public folders per D-03)
- Drop target styles per D-09 and UI-SPEC: when isDropTarget and folder is private, add `border-2 border-blue-500 bg-blue-500/10`. When isDropTarget and folder is public, add `cursor-not-allowed opacity-50`.
- Drag ghost: `opacity-50` on the dragged element per UI-SPEC

Add external file drop support:
- Add native `onDrop` and `onDragOver` handlers on the tree item div
- In onDragOver: check `e.dataTransfer.types.includes('Files')` -- if yes, set drag style. e.preventDefault() to allow drop.
- In onDrop: check `e.dataTransfer.files`. If files present and folder is private, call the upload handler with the dropped file and this folder's ID. If folder is public, do nothing (not-allowed).
- Per RESEARCH.md Pitfall 3: distinguish external file drops from internal @dnd-kit drags by checking dataTransfer.types for 'Files'.

**Update `frontend/src/components/FileListRow.tsx`:**

- Add checkbox column: `<input type="checkbox" checked={isSelected} onChange={() => onToggleSelect(doc.id, false)} />` in the reserved 16px space. Style: `accent-blue-600`
- Add @dnd-kit draggable hook: make the row draggable. When isDragging, add `opacity-50` class.
- Add `isSelected` and `onToggleSelect(id: string, shiftKey: boolean)` props
- onClick on the row: call `onToggleSelect(doc.id, e.shiftKey)` for shift-click range select

**Update `frontend/src/components/FileListView.tsx`:**

- Add BulkActionBar at top when selection count > 0
- Pass selection state to FileListRow components
- Add external file drop zone: on the file list container, add native onDrop/onDragOver for file uploads to current folder
- Wire bulk delete: when BulkActionBar delete clicked, show ConfirmDialog with bulk copy: "Delete {N} items?" / "Are you sure you want to delete {N} selected items? This cannot be undone." / "Delete {N} Items" / "Keep Items"
- Wire bulk move: when BulkActionBar move clicked, show FolderPicker

**Update `frontend/src/pages/DocumentsPage.tsx`:**

- Import and wrap the content area with @dnd-kit's provider component
- Handle onDragEnd:
  - Extract source and target IDs from the event
  - Parse IDs to determine if source is a file or folder (prefix IDs: `file-{id}` and `folder-{id}`)
  - If target is a private folder: call moveDocument or moveFolder accordingly
  - If target is a public folder: do nothing (ignore)
- Import and use useSelection hook
- Wire bulk operations: add bulk delete (loop deleteDocument for each selected) and bulk move (loop moveDocument for each selected) handlers. After bulk operations, clear selection.

**Backend bulk endpoints:** If not already created in Plan 01, add to documents.py:
- POST /api/documents/bulk-delete: accept `{"ids": ["id1", "id2"]}`, verify ownership of each, delete all
- POST /api/documents/bulk-move: accept `{"ids": ["id1", "id2"], "folder_id": "target-id"}`, verify ownership and target folder, update all

These are convenience endpoints to avoid N individual API calls for bulk operations. Implementation: loop through IDs, verify each document belongs to user, then batch delete/update.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains `@dnd-kit/react`
    - frontend/src/hooks/useSelection.ts contains `export function useSelection(`
    - frontend/src/hooks/useSelection.ts contains `shiftKey`
    - frontend/src/components/BulkActionBar.tsx contains `selected` and `Delete`
    - frontend/src/components/BulkActionBar.tsx contains `Move to`
    - frontend/src/components/FolderTreeItem.tsx contains `border-blue-500` or `border-2 border-blue-500`
    - frontend/src/components/FolderTreeItem.tsx contains `cursor-not-allowed`
    - frontend/src/components/FolderTreeItem.tsx contains `dataTransfer` (external file detection)
    - frontend/src/components/FileListRow.tsx contains `checkbox` or `type="checkbox"`
    - frontend/src/components/FileListRow.tsx contains `opacity-50` (drag ghost)
    - frontend/src/pages/DocumentsPage.tsx contains `DragDropProvider` or `DndContext` or equivalent @dnd-kit provider
    - frontend/src/pages/DocumentsPage.tsx contains `useSelection`
    - frontend/src/pages/DocumentsPage.tsx contains `onDragEnd` or equivalent drop handler
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Drag-drop moves files/folders between private folders. External files dropped on tree folders trigger upload. Checkboxes enable selection, shift-click selects ranges, bulk bar offers delete and move.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete file manager UI</name>
  <files>frontend/src/pages/DocumentsPage.tsx</files>
  <action>
Human verification of the complete file manager UI across all 4 plans. No code changes -- this is a manual walkthrough of all functionality built in Plans 01-04.
  </action>
  <what-built>Complete file manager UI: tree sidebar with Board Games (read-only) and My Documents (editable) roots, folder CRUD (create/rename/delete/move), file list with breadcrumbs, drag-and-drop for file/folder moves and uploads, right-click context menus, inline rename, confirmation dialogs, bulk selection with shift-click, and bulk operations.</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && source venv/Scripts/activate && python -m uvicorn main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Open http://localhost:5173/documents (login with ragtest1@gmail.com / testpass123)
    4. **Tree sidebar:** Verify BOARD GAMES section shows seeded game folders with shield icons and muted text. Verify MY DOCUMENTS section exists.
    5. **Folder navigation:** Click a Board Games folder -> right panel shows its contents. Click breadcrumb segments to navigate back.
    6. **No folder selected:** Verify FileUpload drop zone appears in right panel.
    7. **Read-only enforcement:** Right-click a Board Games folder -> no context menu. Try dragging onto Board Games -> not-allowed cursor.
    8. **Create folder:** Click New Folder in toolbar (or right-click -> New subfolder). Enter name. Verify it appears in tree.
    9. **Rename folder:** Double-click a private folder name -> inline input appears. Type new name, Enter. Verify name updated.
    10. **Delete folder:** Right-click a private folder -> Delete. Confirm dialog shows. Confirm. Folder disappears.
    11. **Upload to folder:** Select a private folder. Drop a file from OS onto the tree folder. Verify file appears in that folder's contents.
    12. **Drag-drop move:** Drag a file from file list onto a different private folder in tree. Verify file moves.
    13. **Bulk select:** Click checkboxes on multiple files. Shift-click for range. Bulk bar appears with count and actions.
    14. **Bulk delete:** Select 2+ files, click Delete in bulk bar. Confirm dialog shows "Delete {N} items?". Confirm. Files removed.
    15. **Context menu - Move:** Right-click a file -> Move to... -> folder picker appears -> select destination -> file moves.
  </how-to-verify>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && cd ../backend && python -m pytest tests/test_folders_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with no errors
    - All backend folder API tests pass
    - Human confirms all 15 verification steps pass
  </acceptance_criteria>
  <done>All file manager UI features verified: tree navigation, folder CRUD, context menus, inline rename, drag-drop, bulk operations, and read-only Board Games enforcement.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` -- no TypeScript errors
- `cd frontend && npm run lint` -- no lint errors
- `cd backend && python -m pytest tests/test_folders_api.py -v` -- all backend tests pass
- Full manual UI walkthrough per checkpoint above
</verification>

<success_criteria>
- Drag-and-drop moves files between folders (internal @dnd-kit)
- External file drops onto folders trigger upload to that folder
- Blue highlight on valid drop targets, not-allowed on Board Games
- Checkbox selection + shift-click range select work
- Bulk action bar appears with correct count and actions
- Bulk delete and move work with confirmation
- All interactions consistent with UI-SPEC visual and interaction contracts
</success_criteria>

<output>
After completion, create `.planning/phases/04-file-manager-ui/04-04-SUMMARY.md`
</output>
