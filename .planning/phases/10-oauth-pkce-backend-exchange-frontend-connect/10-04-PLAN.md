---
phase: 10-oauth-pkce-backend-exchange-frontend-connect
plan: 04
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/IconSidebar.tsx
  - frontend/src/components/MobileTopBar.tsx
autonomous: false
requirements: [KEY-01, KEY-03]
must_haves:
  truths:
    - "/settings and /settings/openrouter/callback are registered routes (callback served by the SPA fallback in prod)"
    - "A Settings/gear entry in the IconSidebar (desktop rail + mobile IconNavRow) navigates to /settings"
    - "A connection-status dot is visible on the chat route, distinct from the DemoPill"
    - "The full connect round-trip succeeds on the prod Cloudflare Pages origin (not just localhost)"
    - "D-02: a Settings gear in IconSidebar opens /settings PLUS an always-visible connection dot on the chat route (folds Pitfall 13's always-visible signal forward)"
    - "D-04: the chat-route indicator is a dot ONLY (connected vs not) — the masked label lives on /settings, not the header"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "/settings + /settings/openrouter/callback routes"
      contains: "settings/openrouter/callback"
    - path: "frontend/src/components/IconSidebar.tsx"
      provides: "Settings gear nav entry (rail + IconNavRow)"
      contains: "Settings"
    - path: "frontend/src/components/IconSidebar.tsx"
      provides: "desktop connection-status dot beside DemoPill (always-visible on the chat route via AuthenticatedLayout)"
      contains: "useKeyStatus"
    - path: "frontend/src/components/MobileTopBar.tsx"
      provides: "connection-status dot left of DemoPill"
      contains: "useKeyStatus"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "SettingsPage + OAuthCallbackPage"
      via: "<Route> elements"
      pattern: "OAuthCallbackPage"
    - from: "frontend/src/components/IconSidebar.tsx"
      to: "/settings"
      via: "navigate('/settings')"
      pattern: "/settings"
    - from: "frontend/src/components/IconSidebar.tsx"
      to: "/api/keys/status"
      via: "useKeyStatus().status.connected"
      pattern: "useKeyStatus"
    - from: "frontend/src/components/MobileTopBar.tsx"
      to: "/api/keys/status"
      via: "useKeyStatus().status.connected"
      pattern: "useKeyStatus"
---

<objective>
Wire the Plan 03 pages into the app: register `/settings` and `/settings/openrouter/callback` in `App.tsx`, add the Settings/gear nav entry to `IconSidebar` (desktop rail + mobile `IconNavRow`), and render the always-visible connection-status dot on the chat route (mobile `MobileTopBar`, left of `DemoPill`; desktop in `IconSidebar` rail beside `DemoPill`). Then verify the full connect round-trip on the prod Cloudflare Pages origin (ROADMAP success criterion #4).

Purpose: KEY-01 (route + prod round-trip), KEY-03 (gear entry + header dot). Pure wiring against the Plan 03 pages/hook + the existing nav components.
Output: Two routes, the gear nav entry, the header dot, and a verified prod round-trip.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-UI-SPEC.md
@.planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-PATTERNS.md
@frontend/src/App.tsx
@frontend/src/components/IconSidebar.tsx
@frontend/src/components/MobileTopBar.tsx
@frontend/src/components/DemoPill.tsx
@frontend/public/_redirects

<interfaces>
From Plan 03 (consume, do NOT modify):
  default export SettingsPage  (frontend/src/pages/SettingsPage.tsx)
  default export OAuthCallbackPage (frontend/src/pages/OAuthCallbackPage.tsx)
  useKeyStatus() → { status: { connected: boolean; masked_label?; connected_at? } | null, loading, refresh } (frontend/src/hooks/useKeyStatus.ts)
Existing App.tsx in scope: ProtectedRoute, AuthenticatedLayout (IconSidebar + children).
IconSidebar.tsx: desktop rail buttons (Chat MessageSquare / Documents FileText) use `p-2 rounded mb-2`, glyph size={20}, active `bg-gray-800 text-white` else `text-gray-500 hover:text-white`; IconNavRow mirrors WITHOUT mb-2. Both have a `<div className="flex-1" />` spacer before DemoPill + Sign Out.
MobileTopBar.tsx: right slot is `<div className="h-11 w-11 flex items-center justify-center"><DemoPill /></div>`.
DemoPill convention: role="status" + aria-label non-interactive badge.
Cloudflare Pages SPA fallback: frontend/public/_redirects = `/* /index.html 200` (already serves the callback path — no change needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register /settings + callback routes; add IconSidebar gear (rail + IconNavRow)</name>
  <files>frontend/src/App.tsx, frontend/src/components/IconSidebar.tsx</files>
  <read_first>
    - frontend/src/App.tsx (the existing /documents Route block — mirror it for /settings; the page-import lines at the top)
    - frontend/src/components/IconSidebar.tsx (the Documents button in BOTH the desktop rail and IconNavRow — add the gear identically after Documents, before the flex-1 spacer; the lucide import on line 1)
    - .planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-UI-SPEC.md (§Surface 2 — gear treatment, placement, active state bg-gray-800 NOT blue, title="Settings")
    - .planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-PATTERNS.md (§"App.tsx" + §"IconSidebar.tsx" — verbatim route + button excerpts)
  </read_first>
  <action>
    In `frontend/src/App.tsx`: import `SettingsPage` and `OAuthCallbackPage` (mirror the existing page imports). Add a `/settings` route wrapped `<ProtectedRoute><AuthenticatedLayout><SettingsPage /></AuthenticatedLayout></ProtectedRoute>` (mirror the /documents block) and a `/settings/openrouter/callback` route wrapped `<ProtectedRoute><OAuthCallbackPage /></ProtectedRoute>` (bare — no AuthenticatedLayout/sidebar per UI-SPEC Surface 4; Protected so the session survives the round-trip for the bearer'd exchange POST, RESEARCH Q2/Pitfall 7).
    In `frontend/src/components/IconSidebar.tsx`: add `Settings` to the lucide-react import on line 1. In BOTH the desktop rail AND `IconNavRow`, add a gear button immediately after the Documents button and before the `<div className="flex-1" />` spacer. Desktop rail: `className={`p-2 rounded mb-2 ${isSettings ? 'bg-gray-800 text-white' : 'text-gray-500 hover:text-white'}`}`, `title="Settings"`, glyph `<Settings size={20} />`, onClick `navigate('/settings')`. IconNavRow: same but drop `mb-2` (match its siblings) and use `handleNavigate('/settings')`. Compute `isSettings = location.pathname === '/settings'` in both. Active state is `bg-gray-800` (NOT blue — UI-SPEC Color).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/App.tsx` imports `SettingsPage` and `OAuthCallbackPage` and registers both routes; the callback route path is exactly `/settings/openrouter/callback` and is `ProtectedRoute`-wrapped WITHOUT `AuthenticatedLayout`.
    - `/settings` is `ProtectedRoute` + `AuthenticatedLayout` wrapped (mirrors /documents).
    - `IconSidebar.tsx` imports `Settings` from lucide-react and renders a gear button in BOTH the desktop rail and `IconNavRow`, after Documents, before `flex-1`, with `title="Settings"`, glyph `size={20}`, active `bg-gray-800`, navigating to `/settings`.
    - No `bg-blue-` on the gear button (active is gray, UI-SPEC).
    - `frontend/public/_redirects` is unchanged (still `/* /index.html 200`).
    - `npm run build` and `npm run lint` both exit 0.
  </acceptance_criteria>
  <done>Routes registered; gear nav entry in rail + drawer navigates to /settings; callback bare+protected; build + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Connection-status dot on the chat route (MobileTopBar left of DemoPill + IconSidebar desktop dot)</name>
  <files>frontend/src/components/MobileTopBar.tsx, frontend/src/components/IconSidebar.tsx</files>
  <read_first>
    - frontend/src/components/MobileTopBar.tsx (the DemoPill right-slot — place the dot in the right region, to the LEFT of DemoPill)
    - frontend/src/components/IconSidebar.tsx (the desktop rail bottom region — the `<div className="flex-1" />` spacer + the DemoPill slot; place the desktop dot beside DemoPill, mirroring mobile)
    - frontend/src/components/DemoPill.tsx (the role="status" + aria-label non-interactive badge convention to mirror)
    - frontend/src/hooks/useKeyStatus.ts (consume .status.connected)
    - .planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-UI-SPEC.md (§Surface 3 — 8px dot h-2 w-2 rounded-full, green/gray, aria-label, display-only recommended, distinct from DemoPill, do NOT poll)
    - .planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-PATTERNS.md (§"MobileTopBar.tsx" — the dot excerpt)
  </read_first>
  <action>
    In `frontend/src/components/MobileTopBar.tsx`: consume `useKeyStatus()`; render an 8px display-only dot (`h-2 w-2 rounded-full`, `bg-green-500` when `status?.connected`, else `bg-gray-500`) with `role="status"` and `aria-label={connected ? 'OpenRouter connected' : 'OpenRouter not connected'}`, placed in the existing right region to the LEFT of the `<DemoPill />` slot so demo-identity and key-state read as two distinct signals (UI-SPEC Surface 3). The dot is non-interactive (display-only is the recommended default — the gear entry already routes to /settings). Do NOT poll; `useKeyStatus` fetches once on mount and exposes `refresh()` for after connect/disconnect.
    Desktop anchor (PINNED — not executor discretion): render the same display-only dot in `frontend/src/components/IconSidebar.tsx` desktop rail, in the bottom region beside the existing `<DemoPill />` (after the `<div className="flex-1" />` spacer, before/adjacent to DemoPill), mirroring the mobile placement (dot adjacent to DemoPill). IconSidebar renders on the chat route via `AuthenticatedLayout`, so the dot is always visible there and reads as a distinct signal from DemoPill. Same `h-2 w-2 rounded-full`, `bg-green-500`/`bg-gray-500`, `role="status"` + connected/not `aria-label`, display-only, no polling (consume the same `useKeyStatus()`; it fetches once on mount). Do NOT add a header bar to `ChatContainer` and do NOT introduce a new file — `IconSidebar.tsx` is already in this plan's `files_modified`. Note IconSidebar is also edited in Task 1 (gear); keep the two edits non-overlapping (gear after Documents/top; dot in the bottom DemoPill region).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/MobileTopBar.tsx` consumes `useKeyStatus` and renders a `h-2 w-2 rounded-full` dot, `bg-green-500` when connected else `bg-gray-500`, with `role="status"` + a connected/not-connected `aria-label`, positioned LEFT of `<DemoPill />`.
    - The dot is visible on the chat route and is a distinct element from `DemoPill` (not merged into it); it does not use amber/blue.
    - A desktop connection dot is rendered in `frontend/src/components/IconSidebar.tsx` (desktop rail, bottom region beside `DemoPill`), display-only, same `h-2 w-2` + green/gray + `role="status"` aria-label; no new file is introduced for the dot, and it does not overlap the Task 1 gear edit.
    - No polling loop / setInterval added.
    - `npm run build` and `npm run lint` both exit 0.
  </acceptance_criteria>
  <done>Connection dot live on the chat route (mobile + desktop), green/gray, accessible, distinct from DemoPill, no polling; build + lint green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Prod round-trip verification on the Cloudflare Pages origin</name>
  <read_first>
    - frontend/public/_redirects (confirm the SPA catch-all serves the callback path in prod)
    - .planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-RESEARCH.md (§Pitfall 5 — SPA fallback / callback 404 in prod; §user_constraints — prod round-trip is ROADMAP success criterion #4)
    - CLAUDE.md (dual-env: prod is .env.prod / the Cloudflare Pages origin)
  </read_first>
  <what-built>
    The full BYOK connect/disconnect flow: Connect OpenRouter from /settings (PKCE init), the OpenRouter auth screen, the callback exchange (backend encrypts + upserts), the connected status (masked tail + connected-since), the chat-header dot, and disconnect. All endpoints, pages, routes, nav, and the Sentry scrub are deployed.
  </what-built>
  <how-to-verify>
    On the PROD Cloudflare Pages origin (NOT localhost), signed in as a real user:
    1. Open the deployed site → click the gear → /settings loads → shows "Connect OpenRouter" (not-connected).
    2. Click "Connect OpenRouter" → you reach the OpenRouter `/auth` screen → authorize.
    3. You return to `<prod-origin>/settings/openrouter/callback` (confirm it does NOT 404 — SPA fallback) → see the "Connecting your OpenRouter account…" spinner → auto-redirect to /settings (Connected) + the "OpenRouter connected." toast.
    4. /settings now shows the masked tail (`sk-or-v1-…wXyZ`) + "Connected since {today}"; the chat-header dot is green.
    5. Hard-refresh the callback URL mid-flow (re-trigger by reconnecting and refreshing on the callback) → it SUCCEEDS, not an error (D-07, sessionStorage).
    6. Forge a callback: open `<prod-origin>/settings/openrouter/callback?code=x&state=WRONG` directly → it shows the generic "Couldn't connect…" inline error + Retry/Back, and is rejected (CSRF state mismatch, ROADMAP criterion #2).
    7. Disconnect → confirm dialog → confirm → state flips to not-connected + Connect CTA; dot goes gray. Reconnect → "Connected since" re-stamps to the new time.
    8. Confirm no `sk-or-v1-…` appears in the browser console, network tab response bodies (the exchange/status responses), or any Sentry event for this flow.
    Record pass/fail per step in the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>cd backend && venv/Scripts/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - The callback path resolves on the prod origin (no 404 — SPA fallback serves it).
    - The full connect → connected → disconnect → reconnect round-trip succeeds on prod.
    - A forged/mismatched `state` callback is rejected with the generic inline error.
    - A hard-refresh on the callback is the success path.
    - No `sk-or-v1-…` value appears in console / network response bodies / Sentry for the flow.
    - Full backend suite (`pytest -q`) is green (no regression, incl. Phase 9 lockdown).
  </acceptance_criteria>
  <resume-signal>Type "verified" once all 8 prod steps pass, or describe which step failed.</resume-signal>
  <done>Full connect round-trip verified on the prod Cloudflare Pages origin; forged-state rejected; hard-refresh succeeds; no key leak observed; backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare Pages CDN → SPA | The callback deep-link must be served by the SPA fallback in prod (not 404) |
| prod browser → backend exchange | The exchange POST must carry a valid Supabase bearer across the OAuth full-page round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Denial of Service (false failure) | Callback path 404 in prod (SPA fallback gap) | mitigate | `frontend/public/_redirects` = `/* /index.html 200` already serves any deep path; Task 3 prod step 3 explicitly verifies the callback does not 404 on the prod origin. (Pitfall 5) |
| T-10-13 | Spoofing (CSRF) — prod | Forged callback accepted in prod | mitigate | The SPA `state` check ships in the deployed bundle (Plan 03); Task 3 step 6 forges a callback on the prod origin and confirms rejection (ROADMAP criterion #2). |
| T-10-14 | Information Disclosure — prod | `sk-or-v1-…` visible in console / network / Sentry on the prod flow | mitigate | Exchange returns no key, `/status` returns masked-only, the Sentry scrub is in the bundle; Task 3 step 8 inspects console/network/Sentry on prod for any `sk-or-v1-…`. |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` exit 0.
- The two routes resolve; the gear nav entry navigates to /settings; the chat-route dot is visible (green/gray, distinct from DemoPill).
- Prod round-trip verified per Task 3 (connect/disconnect/reconnect, forged-state rejected, hard-refresh succeeds, no key leak).
- `cd backend && venv/Scripts/python -m pytest -q` green.
</verification>

<success_criteria>
- KEY-01: /settings + callback routes registered; the full connect round-trip succeeds on the prod Cloudflare Pages origin (criterion #4); forged `state` rejected (criterion #2); hard-refresh succeeds.
- KEY-03: the gear nav entry opens /settings; the connection dot is always visible on the chat route.
</success_criteria>

<output>
After completion, create `.planning/phases/10-oauth-pkce-backend-exchange-frontend-connect/10-04-SUMMARY.md` (record per-step prod verification results).
</output>
