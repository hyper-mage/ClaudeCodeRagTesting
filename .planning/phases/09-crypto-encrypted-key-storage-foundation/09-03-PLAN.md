---
phase: 09-crypto-encrypted-key-storage-foundation
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified: []
autonomous: false
requirements: [KEY-02, SEC-02]
user_setup:
  - service: supabase
    why: "Apply migrations 025/026 to the DEV Supabase project (D-03 — dev only this phase; prod at deploy)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (for non-TTY supabase db push / link)"
    dashboard_config:
      - task: "Confirm the CLI is linked to the DEV project ref (derive ref from the .env SUPABASE_URL/VITE_SUPABASE_URL subdomain), OR apply both migration files via the dashboard SQL editor"
        location: "Supabase Dashboard -> dev project -> SQL Editor (fallback) / supabase link --project-ref <dev-ref>"
must_haves:
  truths:
    - "Migrations 025 and 026 are applied to the DEV Supabase project (not prod) — the user_api_keys table exists and execute_readonly_query carries the FROM-table allowlist"
    - "A raw inspection of user_api_keys shows ciphertext-only columns (encrypted_key + key_version) with per-user RLS present"
    - "A live 'select * from user_api_keys' through the SQL-tool path against dev returns a permission error or empty (REVOKE + allowlist enforced)"
    - "A live legitimate query (e.g. select from threads/documents) still succeeds against dev (no regression)"
  artifacts: []
  key_links:
    - from: "supabase/migrations/20240301000025_create_user_api_keys.sql + 026"
      to: "dev Supabase project (schema_migrations)"
      via: "supabase db push (or dashboard SQL editor fallback)"
      pattern: "user_api_keys"
---

<objective>
Apply the Phase 9 schema changes to the DEV Supabase project and verify the SEC-02 lockdown is live: migrations 025 (user_api_keys table + RLS + REVOKE) and 026 (execute_readonly_query allowlist) are pushed to dev only (D-03), then the live REVOKE + allowlist are probed against the real DB to prove success criteria #2 and #3.

Purpose: Build/type checks and unit tests pass WITHOUT the live push — that is a false-positive verification state. The user_api_keys table and the SEC-02 REVOKE only EXIST after the push. This [BLOCKING] plan converts the written SQL (Plan 02) into live, verified protection on dev. Prod is intentionally deferred to the phase deploy step (D-03) and is NOT touched here.

Output: applied dev migrations + recorded live-verification evidence in the SUMMARY. No repo files modified.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-CONTEXT.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-VALIDATION.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-02-PLAN.md

<interfaces>
Migration-apply approach (proven in this repo — Phase 3 SUMMARY 03-01):
- The repo uses `supabase db push` with the CLI linked to a project ref; migrations are recorded in `supabase_migrations.schema_migrations`. Supabase CLI 2.95.4 was used in v1.1.
- `supabase/config.toml` exists (project_id "claude-code-agentic-rag-masterclass"). Linking targets a remote project by ref via `supabase link --project-ref <ref>`.
- CRITICAL per D-03: target the DEV project this phase. The dev project ref is the subdomain of SUPABASE_URL / VITE_SUPABASE_URL in `.env` (e.g. https://<ref>.supabase.co). Do NOT link/push the prod ref (that lives in `.env.prod` and is applied at deploy).
- Non-TTY workaround: set SUPABASE_ACCESS_TOKEN env var for `supabase db push` / `link`. If interactive prompts cannot be suppressed, this plan is autonomous:false — pause and flag for manual run.
- Documented fallback (CLAUDE.md): if the CLI is unavailable/unlinked, apply both migration files via the Supabase dashboard SQL editor against the dev project.

Live probe seam:
- backend/services/sql_service.py execute_sql(user_id, query) drives the real db.rpc("execute_readonly_query", ...) path; running it (or the equivalent SQL in the dashboard under the authenticated role / a user JWT) against dev exercises the live REVOKE + allowlist.
- 09-VALIDATION.md "Manual-Only Verifications" defines the two live checks (schema inspection + REVOKE probe).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migrations 025 + 026 to the DEV Supabase project</name>
  <files>supabase/migrations/20240301000025_create_user_api_keys.sql, supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql</files>
  <read_first>
    - supabase/migrations/20240301000025_create_user_api_keys.sql (the table + RLS + REVOKE migration to apply — from Plan 02)
    - supabase/migrations/20240301000026_harden_sql_tool_allowlist.sql (the RPC allowlist migration to apply — from Plan 02)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-CONTEXT.md (D-03 — DEV ONLY this phase, lines 27-28)
    - .planning/milestones/v1.1-phases/03-prod-supabase-project/03-01-SUMMARY.md (the proven supabase db push workflow + deviation-fix discipline)
  </read_first>
  <action>
    AUTOMATE FIRST, then pause for human confirmation. Confirm the CLI is linked to the DEV project ref (derive from the .env SUPABASE_URL/VITE_SUPABASE_URL subdomain; run supabase link --project-ref <dev-ref> with SUPABASE_ACCESS_TOKEN set if not already linked — NEVER the .env.prod ref). Verify the live dev execute_readonly_query matches migration 015 before patching (RESEARCH assumption A3). Then run supabase db push (non-TTY: SUPABASE_ACCESS_TOKEN set) to apply 20240301000025 and 20240301000026 to dev. If the CLI is unavailable or prompts cannot be suppressed, fall back to pasting both migration files into the dev project's Dashboard SQL Editor. This is a checkpoint:human-action because the live apply touches a real Supabase project and may need an interactive auth/link step — pause for the human to confirm the apply landed on dev (not prod).
  </action>
  <what-built>
    Plan 02 wrote migrations 025 (user_api_keys table + per-user RLS + REVOKE SELECT FROM authenticated) and 026 (execute_readonly_query CREATE OR REPLACE with the FROM-table allowlist). The executor runs the push, then this checkpoint confirms it landed on dev.
  </what-built>
  <how-to-verify>
    1. Confirm the target is the DEV project (ref matches `.env`, NOT `.env.prod`). State the ref used.
    2. `supabase migration list --linked` shows rows for 20240301000025 and 20240301000026 synced (or the dashboard shows both ran without error).
    3. Schema inspection of dev `user_api_keys`: only columns user_id, provider, encrypted_key, key_version, key_label, created_at, updated_at — NO plaintext column; per-user RLS policies present; the table is REVOKE'd from authenticated.
    4. Confirm no prod-only migration failures recurred.
  </how-to-verify>
  <verify>
    <automated>MANUAL — live Supabase apply. Human confirms via `supabase migration list --linked` (rows 025+026) or dashboard; schema inspection shows ciphertext-only user_api_keys + RLS. No unit test can prove a live REVOKE.</automated>
  </verify>
  <acceptance_criteria>
    - Migrations 025 and 026 are recorded as applied on the DEV project (migration list or dashboard confirmation).
    - dev user_api_keys exists with ciphertext-only columns + key_version + per-user RLS.
    - The prod project was NOT touched (no link/push against the .env.prod ref).
  </acceptance_criteria>
  <done>Migrations 025 + 026 applied to the dev Supabase project; user_api_keys exists ciphertext-only with RLS + REVOKE; prod untouched.</done>
  <resume-signal>Type "applied to dev" and paste the migration-list / dashboard confirmation, or describe any apply error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Live-probe the SEC-02 lockdown on dev (REVOKE + allowlist) and a legitimate query</name>
  <files>backend/services/sql_service.py</files>
  <read_first>
    - backend/services/sql_service.py (execute_sql path that drives the live RPC; the legitimate QUERYABLE_SCHEMA tables)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-VALIDATION.md (Manual-Only Verifications table — the two live checks; Observable signals #2 and #3)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md (success criterion #3 observable signals; Pitfall 1 — the REVOKE is the real privilege gate)
  </read_first>
  <action>
    AUTOMATE FIRST, then pause for human verification. Drive the exfiltration probe against dev — either execute_sql(user_id=<a dev test user>, query="select * from user_api_keys") with the backend pointed at dev (.env), or the equivalent SQL under the authenticated role / a user JWT in the dashboard SQL editor. Run a non-allowlisted-table probe ("select * from folders") to confirm the migration-026 allowlist exception fires. Run a legitimate probe (execute_sql(..., "select count(*) from threads") or a documents query) and confirm success. This is a checkpoint:human-verify because the live REVOKE/allowlist effect is observable only against the real dev DB — pause for the human to confirm the probe outputs.
  </action>
  <what-built>
    With 025/026 live on dev, the two independent SEC-02 gates (REVOKE + allowlist) are enforced by the real Postgres. This checkpoint proves the exfiltration probe returns nothing AND a legitimate query still works — the live half of success criteria #2/#3 that unit tests cannot reach.
  </what-built>
  <how-to-verify>
    1. Exfiltration probe: "select * from user_api_keys" via the SQL-tool path against dev returns success=False / a permission error / empty rows — the model gets nothing. Record the exact error/empty result.
    2. Allowlist probe (independent gate): "select * from folders" (a non-allowlisted table) is rejected with the "non-allowlisted table" exception from migration 026.
    3. Regression probe: "select count(*) from threads" (or documents) returns success=True with rows — the four allowlisted tables still work.
    4. Confirm the user's OWN ciphertext row is also unreachable via the tool (REVOKE denies even own-row reads).
  </how-to-verify>
  <verify>
    <automated>MANUAL — live dev probe. Human confirms `select * from user_api_keys` returns permission-error/empty AND `select count(*) from threads` succeeds against the live dev DB. The allowlist set itself is unit-covered in Plan 02 (tests/test_sql_keys_lockdown.py); the live REVOKE effect requires the real DB.</automated>
  </verify>
  <acceptance_criteria>
    - Live "select * from user_api_keys" through the SQL-tool path returns a permission error or empty — never a ciphertext blob (success criterion #3).
    - Live legitimate threads/documents query still succeeds (no regression from the allowlist).
    - A non-allowlisted-table query raises the migration-026 allowlist exception (Gate 2 independently observable).
    - Evidence (exact outputs) recorded in the plan SUMMARY for the verify-work gate.
  </acceptance_criteria>
  <done>Live dev probe proves the keys table is unreachable via the SQL tool (REVOKE + allowlist) while legitimate queries still succeed — the live half of SEC-02 success criteria #2/#3.</done>
  <resume-signal>Type "lockdown verified" and paste the probe outputs (keys-table blocked + legitimate query success), or describe any deviation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo migration files -> live dev Supabase project | The written SQL must actually be applied; an unapplied REVOKE is a false-positive (build passes, protection absent). |
| dev project vs prod project | D-03 mandates dev-only this phase; touching prod here would apply the table before the deploy-step REVOKE coordination. |
| LLM SQL -> live execute_readonly_query (dev) | The real exfiltration surface; this plan proves the live gates deny it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Information Disclosure | un-applied REVOKE (false-positive verification) | mitigate | This [BLOCKING] plan applies 025/026 to dev and live-probes the REVOKE + allowlist (Tasks 1+2); verification cannot pass without the live apply. |
| T-09-12 | Tampering | applying the migration to the WRONG project (prod) | mitigate | Task 1 derives the dev ref from `.env` and explicitly asserts the prod (`.env.prod`) ref is NOT targeted; D-03 dev-only enforced. |
| T-09-13 | Information Disclosure | live own-row ciphertext returnable via the SQL tool | mitigate | Task 2 step 4 confirms even the user's own row is denied (REVOKE precedes RLS) — the model gets nothing. |
| T-09-14 | Tampering | live dev RPC drifted from migration 015 (silent overwrite by CREATE OR REPLACE) | mitigate | Task 1 verifies the live dev RPC matches migration 015 before patching (RESEARCH assumption A3); deviations recorded. |
</threat_model>

<verification>
- Migrations 025 + 026 applied to dev (migration list / dashboard confirmation).
- Live keys-table exfiltration probe returns permission-error/empty; legitimate query succeeds; non-allowlisted-table query raises the allowlist exception.
- Prod untouched (D-03).
- Evidence recorded in 09-03-SUMMARY.md for the phase verify-work gate. Prod apply + re-check is explicitly deferred to the phase deploy step (D-03/D-05).
</verification>

<success_criteria>
- ROADMAP criterion #2 (user_api_keys migration applies cleanly to dev, ciphertext only) — verified live (Task 1).
- ROADMAP criterion #3 (prompt-injected select * from user_api_keys returns nothing via REVOKE + RPC allowlist) — verified live against dev (Task 2).
- Dev/prod isolation preserved (D-03): prod applied later at deploy, never here.
</success_criteria>

<output>
After completion, create `.planning/phases/09-crypto-encrypted-key-storage-foundation/09-03-SUMMARY.md`
</output>
