---
phase: 09-crypto-encrypted-key-storage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/services/crypto_service.py
  - backend/tests/test_crypto_service.py
  - backend/tests/test_config.py
  - .planning/phases/09-crypto-encrypted-key-storage-foundation/KEY-ROTATION-RUNBOOK.md
autonomous: true
requirements: [KEY-02]
must_haves:
  truths:
    - "A plaintext OpenRouter key encrypts and decrypts round-trip via crypto_service (Fernet) — ciphertext != plaintext, decrypt(encrypt(x)) == x"
    - "A second (new) master key decrypts a token encrypted under the old key and re-encrypts it under the new key (MultiFernet rotation)"
    - "KEY_ENCRYPTION_SECRET loads from env into Settings with an empty default, distinct per environment"
    - "The master key and plaintext are never logged, traced, or returned anywhere in crypto_service"
  artifacts:
    - path: "backend/services/crypto_service.py"
      provides: "encrypt_key / decrypt_key / rotate_token on MultiFernet"
      contains: "def encrypt_key("
    - path: "backend/config.py"
      provides: "key_encryption_secret setting"
      contains: "key_encryption_secret"
    - path: "backend/tests/test_crypto_service.py"
      provides: "round-trip + rotation regression tests"
      contains: "def test_encrypt_decrypt_roundtrip"
    - path: ".planning/phases/09-crypto-encrypted-key-storage-foundation/KEY-ROTATION-RUNBOOK.md"
      provides: "documented rotation procedure (satisfies ROADMAP criterion #4 doc half)"
      contains: "MultiFernet"
  key_links:
    - from: "backend/services/crypto_service.py"
      to: "backend/config.py"
      via: "get_settings().key_encryption_secret read inside _multifernet() at call time"
      pattern: "get_settings\\(\\)\\.key_encryption_secret"
---

<objective>
Build the app-layer encryption foundation for BYOK: a `crypto_service` wrapping `MultiFernet` (encrypt with the primary key, decrypt with any configured key, lazy re-encrypt via `.rotate()`), a `KEY_ENCRYPTION_SECRET` setting in `config.py`, the pytest regression gate for round-trip + rotation + config-load, and a rotation runbook.

Purpose: This is the crypto half of Phase 9 (KEY-02). Phase 10 will call `encrypt_key` after the OAuth exchange; Phase 11 will call `decrypt_key` per request. The API must be minimal and stable. Zero new dependencies — `cryptography 46.0.5` is already pinned (used by the ES256 JWT path in `backend/auth.py`).

Output: `backend/services/crypto_service.py`, an extended `backend/config.py`, two test files (one new, one extended), and `KEY-ROTATION-RUNBOOK.md`.
</objective>

<execution_context>
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/56kbps/Desktop/Projects/VibeCoded Projects/AI Automators Masterclass/claude-code-agentic-rag-masterclass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-CONTEXT.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-PATTERNS.md
@.planning/phases/09-crypto-encrypted-key-storage-foundation/09-VALIDATION.md

<interfaces>
backend/config.py — existing Settings + @lru_cache get_settings() (the file you extend):
- Settings(BaseSettings) with empty-default secret fields: supabase_url, supabase_service_role_key, supabase_jwt_secret, openai_api_key, langsmith_api_key (lines 13-17).
- ENV_FILE override at lines 6-9: `_env_filename = os.environ.get("ENV_FILE", ".env")` then load_dotenv — makes dev (.env) vs prod (.env.prod) work for D-03/D-04.
- get_settings() is @lru_cache'd (lines 143-145) → tests mutating env MUST call get_settings.cache_clear().
- pydantic-settings auto-maps UPPER_SNAKE env vars to snake_case attrs (KEY_ENCRYPTION_SECRET -> key_encryption_secret).

backend/services/web_search_service.py — the service-module shape to mirror:
- `from config import get_settings`; call `get_settings()` INSIDE the function (deferred to call-time), not at import time. Private helpers prefixed `_`.

cryptography 46.0.5 (already installed) — VERIFIED behavior in 09-RESEARCH.md Pattern 1:
- `from cryptography.fernet import Fernet, MultiFernet`
- `Fernet.generate_key().decode()` -> 44-char url-safe base64 string (32 raw bytes).
- `MultiFernet([f_first, f_second]).encrypt(...)` encrypts under the FIRST key; `.decrypt(...)` tries each key in order; `.rotate(token)` re-encrypts under the FIRST key.

Consumer-facing API names that Phase 10/11 and the RESEARCH test examples depend on (keep stable):
- `encrypt_key(plaintext: str) -> str`
- `decrypt_key(ciphertext: str) -> str`
- `rotate_token(ciphertext: str) -> str`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — add KEY_ENCRYPTION_SECRET to config + write failing crypto/config tests</name>
  <files>backend/config.py, backend/tests/test_config.py, backend/tests/test_crypto_service.py</files>
  <read_first>
    - backend/config.py (the file being modified — existing secret-field pattern at lines 13-17; ENV_FILE at 6-9; @lru_cache get_settings at 143-145)
    - backend/tests/test_config.py (the file being extended — mirror the test_chat_max_iterations_default / _env_override pair at lines 5-17; this file uses Settings() DIRECTLY so it does NOT need cache_clear)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md (Code Examples lines 318-362 — verified test bodies; the cache_clear note at line 349)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-PATTERNS.md (config.py MODIFY section lines 69-91; test_config.py MODIFY section lines 187-200; test_crypto_service.py NEW section lines 164-183)
  </read_first>
  <behavior>
    - test_config.py::test_key_encryption_secret_default — Settings().key_encryption_secret == "" (empty default like other secrets).
    - test_config.py::test_key_encryption_secret_env_override — with KEY_ENCRYPTION_SECRET=abc set via monkeypatch.setenv, Settings().key_encryption_secret == "abc".
    - test_crypto_service.py::test_encrypt_decrypt_roundtrip — generate a key with Fernet.generate_key().decode(), set KEY_ENCRYPTION_SECRET via monkeypatch.setenv, call get_settings.cache_clear(), then assert encrypt_key("sk-or-v1-example") != "sk-or-v1-example" AND decrypt_key(that) == "sk-or-v1-example".
    - test_crypto_service.py::test_rotation_decrypts_old_and_reencrypts — encrypt under old key; set KEY_ENCRYPTION_SECRET to f"{new},{old}" + cache_clear; rotate_token; set to new alone + cache_clear; assert decrypt_key(rotated) == original plaintext.
  </behavior>
  <action>
    Add the setting to backend/config.py: a key_encryption_secret: str = "" field on the Settings class, placed alongside the other empty-default secret fields near the top (after the langsmith fields, before the cors block). Precede it with a two-line comment matching the file's style noting it is the BYOK master key per Phase 9 D-04 — comma-separated url-safe base64 Fernet keys, NEW KEY FIRST for MultiFernet rotation, distinct value per env (.env vs .env.prod). No property accessor is needed (raw string; crypto_service parses it). The env var name is KEY_ENCRYPTION_SECRET (pydantic auto-maps).

    Extend backend/tests/test_config.py with the two tests above, mirroring the existing test_chat_max_iterations_* shape EXACTLY (one-line docstring, from config import Settings, Settings() constructed directly — no cache_clear here because the file builds Settings() directly).

    Create backend/tests/test_crypto_service.py (NEW) with the two crypto tests above. These reference services.crypto_service which does NOT yet exist — so these two tests MUST fail at this point (RED). Use from cryptography.fernet import Fernet to self-generate keys; monkeypatch.setenv("KEY_ENCRYPTION_SECRET", ...) then from config import get_settings; get_settings.cache_clear() BEFORE importing/calling crypto_service (crypto_service reads via get_settings(), so the lru_cache must be cleared per the RESEARCH note at line 349). No shared fixture — tests self-generate keys.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_config.py::test_key_encryption_secret_default tests/test_config.py::test_key_encryption_secret_env_override -x</automated>
  </verify>
  <acceptance_criteria>
    - backend/config.py contains the literal key_encryption_secret: str = "".
    - cd backend && venv/Scripts/python.exe -m pytest tests/test_config.py::test_key_encryption_secret_default tests/test_config.py::test_key_encryption_secret_env_override -x exits 0 (both config tests pass — the setting exists and loads).
    - cd backend && venv/Scripts/python.exe -m pytest tests/test_crypto_service.py -x exits NON-zero with a collection/import error or assertion failure (crypto_service.py not yet written — confirms RED state for Task 2).
    - backend/tests/test_crypto_service.py contains def test_encrypt_decrypt_roundtrip and def test_rotation_decrypts_old_and_reencrypts.
    - backend/tests/test_crypto_service.py calls get_settings.cache_clear() after every monkeypatch.setenv("KEY_ENCRYPTION_SECRET", ...).
  </acceptance_criteria>
  <done>Config setting added and proven loadable by passing config tests; crypto test file exists and is failing for the right reason (crypto_service absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement crypto_service on MultiFernet (encrypt / decrypt / rotate) — GREEN</name>
  <files>backend/services/crypto_service.py</files>
  <read_first>
    - backend/services/web_search_service.py (service-module shape: from config import get_settings, settings = get_settings() called INSIDE the function, _underscore private helper naming)
    - backend/services/crypto_service.py (the file being created — currently absent)
    - backend/tests/test_crypto_service.py (the tests that must pass — written in Task 1)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md (Pattern 1 lines 163-192 — the VERIFIED MultiFernet implementation + empirical behavior notes; Pitfall 2 lines 292-296 — Fernet key encoding mismatch)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-PATTERNS.md (crypto_service.py NEW section lines 21-65)
  </read_first>
  <behavior>
    - encrypt_key(plaintext) returns a Fernet token string (not the plaintext), encrypted under the FIRST key in KEY_ENCRYPTION_SECRET.
    - decrypt_key(ciphertext) returns the original plaintext, trying each configured key in order.
    - rotate_token(ciphertext) returns a token re-encrypted under the current primary (first) key.
    - All three read the key list at CALL TIME via get_settings() so test cache_clear() takes effect.
  </behavior>
  <action>
    Create backend/services/crypto_service.py. Import from cryptography.fernet import Fernet, MultiFernet and from config import get_settings. Add a module logger via logging.getLogger(__name__) for consistency, but DO NOT log the secret, plaintext, or ciphertext anywhere (D-04 — never logged/traced/returned).

    Define a private helper _multifernet() -> MultiFernet that reads get_settings().key_encryption_secret, splits on the comma, strips whitespace, drops empty entries, and returns MultiFernet([Fernet(k.encode()) for k in keys]). The list order is NEW-KEY-FIRST (encrypt uses the first key; decrypt tries all). Call get_settings() INSIDE the helper (deferred to call-time per the web_search_service pattern) so tests can cache_clear().

    Define three public functions with type hints and one-line docstrings: encrypt_key(plaintext: str) -> str returns _multifernet().encrypt(plaintext.encode()).decode(); decrypt_key(ciphertext: str) -> str returns _multifernet().decrypt(ciphertext.encode()).decode(); rotate_token(ciphertext: str) -> str returns _multifernet().rotate(ciphertext.encode()).decode(). Use double quotes, 4-space indent, str (not Optional) per CLAUDE.md conventions. Keep the API exactly these three names — Phase 10/11 consumers and the tests depend on them.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_crypto_service.py tests/test_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - backend/services/crypto_service.py contains def encrypt_key(, def decrypt_key(, and def rotate_token(.
    - backend/services/crypto_service.py reads the setting via get_settings().key_encryption_secret inside a helper (NOT at module import time).
    - cd backend && venv/Scripts/python.exe -m pytest tests/test_crypto_service.py::test_encrypt_decrypt_roundtrip -x exits 0.
    - cd backend && venv/Scripts/python.exe -m pytest tests/test_crypto_service.py::test_rotation_decrypts_old_and_reencrypts -x exits 0.
    - cd backend && venv/Scripts/python.exe -m pytest tests/test_crypto_service.py tests/test_config.py -x exits 0 (full Phase 9 crypto+config unit set green).
    - No logger.* call in crypto_service.py interpolates the plaintext, ciphertext, or key_encryption_secret.
  </acceptance_criteria>
  <done>crypto_service implements encrypt/decrypt/rotate on MultiFernet; round-trip and rotation tests pass; secret never logged.</done>
</task>

<task type="auto">
  <name>Task 3: Write the key-rotation runbook (satisfies ROADMAP criterion #4 documentation)</name>
  <files>.planning/phases/09-crypto-encrypted-key-storage-foundation/KEY-ROTATION-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/KEY-ROTATION-RUNBOOK.md (the file being created — currently absent)
    - backend/services/crypto_service.py (the rotation surface being documented — written in Task 2)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-CONTEXT.md (D-02 rotation decision lines 24-25; D-04 per-env secret line 29; Specifics line 92 — MultiFernet key order)
    - .planning/phases/09-crypto-encrypted-key-storage-foundation/09-RESEARCH.md (Pitfall 3 lines 298-302 — cross-env key reuse; State of the Art rotation row)
  </read_first>
  <action>
    Create .planning/phases/09-crypto-encrypted-key-storage-foundation/KEY-ROTATION-RUNBOOK.md. Document the lazy MultiFernet rotation procedure as concrete numbered steps: (1) generate a NEW per-env key with cd backend && venv/Scripts/python.exe -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"; (2) set KEY_ENCRYPTION_SECRET to the comma-separated list NEW-KEY-FIRST, i.e. new,old, in the target env's secret store (.env for dev; .env.prod + Fly secrets for prod) — never reuse the dev key in prod (D-04 / Pitfall 3); (3) deploy/restart so the backend reads the new list; (4) lazy re-encryption — when a row is read and decrypted under the old key, call rotate_token(...) and write the re-encrypted token back with a bumped key_version (the write path lands in Phase 10/11 consumers; document the intended UPDATE shape against user_api_keys); (5) once all rows are re-encrypted (or after the rotation window), drop the old key from KEY_ENCRYPTION_SECRET leaving only new. State explicitly: encrypt always uses the FIRST key; decrypt tries each key; key_version is an integer counter on user_api_keys tracking which master key encrypted the row. Reference backend/services/crypto_service.py functions encrypt_key / decrypt_key / rotate_token by name. Keep it operational (a runbook, not prose).
  </action>
  <verify>
    <automated>grep -lq "rotate_token" ".planning/phases/09-crypto-encrypted-key-storage-foundation/KEY-ROTATION-RUNBOOK.md" && grep -lq "KEY_ENCRYPTION_SECRET" ".planning/phases/09-crypto-encrypted-key-storage-foundation/KEY-ROTATION-RUNBOOK.md" && grep -lq "MultiFernet" ".planning/phases/09-crypto-encrypted-key-storage-foundation/KEY-ROTATION-RUNBOOK.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - KEY-ROTATION-RUNBOOK.md exists in the phase directory.
    - File contains rotate_token, KEY_ENCRYPTION_SECRET, MultiFernet, and key_version.
    - File documents the NEW-KEY-FIRST ordering for KEY_ENCRYPTION_SECRET and the distinct-per-env rule (D-04).
    - File references encrypt_key, decrypt_key, and rotate_token by name.
  </acceptance_criteria>
  <done>Operational rotation runbook exists, documenting the lazy MultiFernet rotation path and per-env key discipline — completing the documentation half of ROADMAP criterion #4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/secret store -> backend process | KEY_ENCRYPTION_SECRET crosses here; backend-only secret, distinct per environment (D-04). |
| backend memory -> logs/traces/responses | Plaintext key and master secret exist only transiently in backend memory; must NOT cross into logs, LangSmith traces, or any response. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | crypto_service.py logging/return surface | mitigate | No logger.* interpolates plaintext/ciphertext/secret (Task 2 acceptance check); functions return only their declared values. |
| T-09-02 | Information Disclosure | KEY_ENCRYPTION_SECRET custody (D-04) | mitigate | Empty default in Settings; loaded only from env via pydantic-settings; never echoed. Distinct value per env documented in runbook (Task 3). |
| T-09-03 | Tampering / Information Disclosure | master-key compromise with no rotation path (D-02) | mitigate | MultiFernet .rotate() + key_version; runbook documents the dual-key decrypt window and lazy re-encrypt (Task 2 + Task 3). |
| T-09-04 | Information Disclosure | cross-env key reuse (dev key in prod) | mitigate | Runbook mandates a separately-generated Fernet.generate_key() per env; dev value never copied to prod (D-04 / Pitfall 3). |
| T-09-05 | Spoofing / Tampering | hand-rolled crypto (IV reuse, padding oracle) | accept | Not applicable — uses audited cryptography.fernet (AES-128-CBC + HMAC-SHA256); no custom crypto written. |
</threat_model>

<verification>
- cd backend && venv/Scripts/python.exe -m pytest tests/test_crypto_service.py tests/test_config.py -x exits 0 (round-trip + rotation + config default/override all green).
- cd backend && venv/Scripts/python.exe -m pytest tests/ -q — full backend suite still green (no regression from the config change).
- KEY-ROTATION-RUNBOOK.md present with the four required tokens.
- Manual grep confirms no plaintext/secret logging in crypto_service.py.
</verification>

<success_criteria>
- ROADMAP criterion #1 (round-trip via crypto_service using a dedicated KEY_ENCRYPTION_SECRET) proven by test_encrypt_decrypt_roundtrip.
- ROADMAP criterion #4 (second master key decrypts + lazily re-encrypts; rotation runbook documented) proven by test_rotation_decrypts_old_and_reencrypts + KEY-ROTATION-RUNBOOK.md.
- KEY_ENCRYPTION_SECRET wired into Settings with empty default + env override (config tests).
- crypto_service API (encrypt_key / decrypt_key / rotate_token) is stable for Phase 10/11 consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/09-crypto-encrypted-key-storage-foundation/09-01-SUMMARY.md`
</output>
