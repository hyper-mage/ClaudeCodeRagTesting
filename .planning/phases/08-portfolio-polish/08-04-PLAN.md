---
phase: "08"
plan: "04"
type: execute
wave: 2
depends_on: ["08-02", "08-03"]
files_modified:
  - frontend/src/contexts/AuthContext.tsx
  - frontend/src/components/DemoPill.tsx
  - frontend/src/components/ErrorMessageBubble.tsx
  - frontend/src/components/IconSidebar.tsx
  - frontend/src/components/MobileTopBar.tsx
  - frontend/src/components/ChatContainer.tsx
  - frontend/src/hooks/useChat.ts
  - frontend/src/pages/LoginPage.tsx
autonomous: false
requirements:
  - PORT-01
  - PORT-02

must_haves:
  truths:
    - "LoginPage shows a 'Try the demo' CTA prominently above the email/password form (D-04, UI-SPEC Surface 1)"
    - "Clicking 'Try the demo' invokes supabase.auth.signInAnonymously() then POSTs /api/demo/bootstrap then navigates to / (D-01, D-02)"
    - "Anon user sees a 'Demo' amber pill in IconSidebar (desktop), MobileTopBar (mobile), and drawer IconNavRow (UI-SPEC Surface 3)"
    - "Permanent user sees NO Demo pill anywhere"
    - "AuthContext exposes `isAnon: boolean` derived from `user.is_anonymous`"
    - "When SSE chat stream fails, an in-thread red ErrorMessageBubble replaces the empty assistant placeholder + a simultaneous 4s error toast appears (D-06, UI-SPEC Surface 2)"
    - "ErrorMessageBubble's Retry button re-sends the last user message via POST /api/threads/{id}/messages?retry=true and is disabled while isStreaming (D-07)"
    - "Sentry.captureException(err) fires inside the catch block (RESEARCH Pitfall 4)"
    - "Anon-user empty-state copy in ChatContainer references the sample D&D 5e quick-reference (UI-SPEC Copywriting Contract)"
    - "Tool-level failures (rerank/web_search/subagent) produce NO user-visible UI element (D-08 silent continue)"
    - "All locked copy strings from UI-SPEC § Copywriting Contract appear verbatim in the codebase"
  artifacts:
    - path: "frontend/src/contexts/AuthContext.tsx"
      provides: "AuthContextType extended with `isAnon: boolean`"
      contains: "isAnon"
    - path: "frontend/src/components/DemoPill.tsx"
      provides: "Amber pill rendered when isAnon === true (returns null otherwise)"
      exports: ["default"]
    - path: "frontend/src/components/ErrorMessageBubble.tsx"
      provides: "Red-wash error bubble with AlertCircle icon + Retry button"
      exports: ["default"]
    - path: "frontend/src/hooks/useChat.ts"
      provides: "Catch block dispatches error bubble + toast + Sentry; retryLastUserMessage callback"
      contains: "retryLastUserMessage"
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Try-demo CTA above email/password form with locked copy"
      contains: "Try the demo"
    - path: "frontend/src/components/ChatContainer.tsx"
      provides: "Renders ErrorMessageBubble for role=='error' messages; anon-specific empty state"
      contains: "ErrorMessageBubble"
  key_links:
    - from: "LoginPage handleTryDemo"
      to: "supabase.auth.signInAnonymously + apiFetch('/api/demo/bootstrap')"
      via: "async/await chain"
      pattern: "signInAnonymously|api/demo/bootstrap"
    - from: "useChat retryLastUserMessage"
      to: "POST /api/threads/{id}/messages?retry=true (Plan 08-03)"
      via: "apiStream with retry query param appended"
      pattern: "retry=true"
    - from: "useChat catch block"
      to: "Sentry + ToastContext"
      via: "Sentry.captureException + showToast"
      pattern: "captureException.*showToast|showToast.*captureException"
    - from: "ChatContainer messages.map"
      to: "ErrorMessageBubble component"
      via: "msg.role === 'error' branch"
      pattern: "role === 'error'"
---

<objective>
Land all frontend changes for PORT-01 + PORT-02 in one cohesive plan. Eight files, organized into two natural sub-units:

- **PORT-01 frontend** (Tasks 1-2): AuthContext.isAnon exposure → DemoPill component → IconSidebar/MobileTopBar wiring → LoginPage Try-demo CTA.
- **PORT-02 frontend** (Task 3): ErrorMessageBubble component → useChat catch-block rewrite (with Sentry + Toast + retry callback) → ChatContainer ErrorMessageBubble rendering + anon empty-state copy.

A human-verify checkpoint at the end (Task 4) is mandatory — the project has no frontend test framework (verified via CLAUDE.md and package.json grep), so visual + interactive correctness can only be confirmed via deployed UAT against CF Pages, mirroring the Phase 06.1 P02 14-point UAT pattern.

Purpose: Deliver the two UI-visible surfaces locked by Phase 8 success criterion #5.
Output: 8 modified/new frontend files + a 12-point manual UAT pass against the deployed CF Pages URL.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-portfolio-polish/08-CONTEXT.md
@.planning/phases/08-portfolio-polish/08-UI-SPEC.md
@.planning/phases/08-portfolio-polish/08-RESEARCH.md
@.planning/phases/08-portfolio-polish/08-PATTERNS.md
@frontend/src/contexts/AuthContext.tsx
@frontend/src/contexts/ToastContext.tsx
@frontend/src/lib/sentry.ts
@frontend/src/lib/api.ts
@frontend/src/lib/supabase.ts
@frontend/src/pages/LoginPage.tsx
@frontend/src/hooks/useChat.ts
@frontend/src/components/MessageBubble.tsx
@frontend/src/components/ChatContainer.tsx
@frontend/src/components/IconSidebar.tsx
@frontend/src/components/MobileTopBar.tsx

<interfaces>
<!-- AuthContext (verified existing) -->
```typescript
interface AuthContextType {
  user: User | null
  session: Session | null
  loading: boolean
  signOut: () => Promise<void>
  // NEW (this plan): isAnon: boolean
}
export function useAuth(): AuthContextType
```

<!-- ToastContext (verified existing — UI-SPEC § Toast primitive) -->
```typescript
export function useToast(): {
  showToast: (message: string, variant: 'error' | 'warning' | 'success' | 'info') => void
}
// AUTO_DISMISS_MS = 4000
// VARIANT_STYLES.error uses bg-red-700 + text-white + border border-red-500
```

<!-- @supabase/supabase-js (verified ^2.99.3) -->
```typescript
supabase.auth.signInAnonymously(): Promise<{ data: { session: Session | null; user: User | null }; error: AuthError | null }>
// User.is_anonymous: boolean (shipped v2.43.x)
```

<!-- frontend/src/lib/api.ts (verified) -->
```typescript
export async function apiFetch(path: string, init?: RequestInit): Promise<Response>
export async function apiStream(path: string, init: RequestInit, signal?: AbortSignal): Promise<ReadableStreamDefaultReader<Uint8Array>>
// Both auto-inject Authorization: Bearer <session.access_token>
```

<!-- frontend/src/lib/sentry.ts (verified — Phase 7) -->
```typescript
import * as Sentry from '@sentry/react'
// Sentry.captureException(err) — explicit capture for caught errors (Sentry global handlers only cover UNCAUGHT)
```

<!-- useChat.ts current Message type (verified L23-28) -->
```typescript
export interface Message {
  id: string
  role: 'user' | 'assistant'   // NEW (this plan): | 'error'
  content: string
  toolsUsed?: ToolEvent[]
}
```

<!-- Backend retry contract (Plan 08-03 — must already be live) -->
POST /api/threads/{thread_id}/messages?retry=true
  - Deletes most-recent assistant row for thread+user before insert
  - Skips user-message insert (preserves existing user row from prior failed send)
  - Returns SSE stream as normal
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AuthContext.isAnon + DemoPill component + sidebar/topbar wiring</name>
  <files>frontend/src/contexts/AuthContext.tsx, frontend/src/components/DemoPill.tsx, frontend/src/components/IconSidebar.tsx, frontend/src/components/MobileTopBar.tsx</files>
  <read_first>
    - frontend/src/contexts/AuthContext.tsx (entire file — verify Provider value shape on lines 38-42)
    - frontend/src/components/IconSidebar.tsx (entire file — verify desktop block L29-36 + IconNavRow block L85-93)
    - frontend/src/components/MobileTopBar.tsx (entire file — verify right-spacer L32)
    - .planning/phases/08-portfolio-polish/08-UI-SPEC.md (§ Color, § Surface 3 — Anon-Mode Demo Badge, § Copywriting Contract — "Demo" label + tooltip)
    - .planning/phases/08-portfolio-polish/08-PATTERNS.md (§frontend/src/contexts/AuthContext.tsx, §frontend/src/components/DemoPill.tsx, §IconSidebar, §MobileTopBar sections, lines 96-348)
  </read_first>
  <action>
    Extend `AuthContext.tsx` per PATTERNS.md lines 330-348:
    - Add `isAnon: boolean` to `AuthContextType` interface.
    - Compute `const isAnon = user?.is_anonymous ?? false` inside the provider body.
    - Add `isAnon` to the Provider's value object.
    - No other changes — keep existing `user`, `session`, `loading`, `signOut` shape.

    Create `frontend/src/components/DemoPill.tsx` per PATTERNS.md lines 99-117 (verbatim shape). Locked Tailwind classes per UI-SPEC § Color: `inline-flex items-center justify-center px-2 py-0.5 rounded-full text-xs font-semibold bg-amber-500/15 text-amber-300 border border-amber-500/30`. Label: `Demo`. Tooltip via native `title` attribute: `You're using a temporary demo account. Data is cleared after 7 days.` `aria-label="Demo account"` for screen readers. Returns `null` when `!isAnon`.

    Modify `IconSidebar.tsx` per PATTERNS.md lines 290-306:
    - Desktop block (L29-36): insert `<DemoPill />` between the `<div className="flex-1" />` spacer and the LogOut button.
    - IconNavRow drawer block (L85-93): insert `<DemoPill />` at the same relative position.
    - Import `DemoPill` from `./DemoPill`.

    Modify `MobileTopBar.tsx` per PATTERNS.md lines 312-322:
    - Replace the dead `<div className="h-11 w-11" />` spacer at L32 with `<div className="h-11 w-11 flex items-center justify-center"><DemoPill /></div>` — wrapper preserves layout balance regardless of pill visibility.
    - Import `DemoPill` from `./DemoPill`.

    Do NOT introduce new color tokens beyond the amber set already locked by UI-SPEC. Do NOT change pill copy. Do NOT add an `onClick` handler — the pill is non-interactive (display-only per UI-SPEC).
  </action>
  <verify>
    <automated>`cd frontend && npm run build` — TypeScript + Vite production build succeeds. Plus: `grep -F "isAnon" frontend/src/contexts/AuthContext.tsx | wc -l` ≥ 2 (type + value).</automated>
  </verify>
  <done>AuthContext exposes isAnon; DemoPill renders amber pill when isAnon=true and null otherwise; IconSidebar (desktop + drawer) and MobileTopBar all render DemoPill; npm build green.</done>
  <acceptance_criteria>
    - `grep -F "isAnon: boolean" frontend/src/contexts/AuthContext.tsx` matches.
    - `grep -F "user?.is_anonymous" frontend/src/contexts/AuthContext.tsx` matches.
    - File `frontend/src/components/DemoPill.tsx` exists; exports default function; contains string literals `"Demo"` and `"temporary demo account"`.
    - `grep -F "from './DemoPill'" frontend/src/components/IconSidebar.tsx` matches.
    - `grep -F "from './DemoPill'" frontend/src/components/MobileTopBar.tsx` matches.
    - `grep -F "bg-amber-500/15" frontend/src/components/DemoPill.tsx` matches.
    - `cd frontend && npm run build` exits 0.
    - No new npm dependencies introduced: `git diff frontend/package.json frontend/package-lock.json` is empty.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LoginPage Try-demo CTA (D-01, D-04, UI-SPEC Surface 1)</name>
  <files>frontend/src/pages/LoginPage.tsx</files>
  <read_first>
    - frontend/src/pages/LoginPage.tsx (entire file — verify handleSubmit shape L20-39 + existing button styling L66-72)
    - frontend/src/lib/supabase.ts (the exported `supabase` client)
    - frontend/src/lib/api.ts (the `apiFetch` helper)
    - .planning/phases/08-portfolio-polish/08-UI-SPEC.md (§ Surface 1 — Try Demo CTA, § Copywriting Contract — all 4 Try-demo strings, § Spacing — py-3 mandate, § Color — bg-blue-600 reserved for primary CTAs)
    - .planning/phases/08-portfolio-polish/08-PATTERNS.md (§frontend/src/pages/LoginPage.tsx section, lines 124-181)
  </read_first>
  <action>
    Modify `LoginPage.tsx` per PATTERNS.md lines 124-181:

    Imports — ADD `import { apiFetch } from '../lib/api'`. Existing imports unchanged.

    Add `handleTryDemo` async function mirroring `handleSubmit` shape (PATTERNS.md lines 141-157):
    - `setError('')` + `setLoading(true)`.
    - `await supabase.auth.signInAnonymously()`; throw on error or missing session.
    - `await apiFetch('/api/demo/bootstrap', { method: 'POST' })`.
    - `navigate('/', { replace: true })` on success.
    - On any failure: `setError("Couldn't start the demo. Please try again.")` (verbatim from UI-SPEC Copywriting Contract — no provider name, no HTTP code).
    - `setLoading(false)` in finally.

    Insert the Try-demo CTA block BEFORE the existing `<form>` element (around L47 — verify exact line via Read). Locked structure from PATTERNS.md lines 161-179:
    - Primary CTA button: `type="button"`, `onClick={handleTryDemo}`, `disabled={loading}`, classes `w-full py-3 bg-blue-600 hover:bg-blue-700 rounded font-semibold disabled:opacity-50`. Label: `{loading ? 'Setting up your demo…' : 'Try the demo'}` (exact strings from UI-SPEC Copywriting — note the single ellipsis character `…`, not three dots).
    - Sub-label `<p className="text-xs text-gray-500 mt-2 text-center">No signup. Your demo session expires after 7 days.</p>` (verbatim from UI-SPEC).
    - Divider block per PATTERNS.md lines 174-178: horizontal rule with `or sign in with email` text centered on top.

    Do NOT change the existing email/password form, signup link, or any other LoginPage element. Do NOT add new state hooks (reuse existing `loading` and `error`).
  </action>
  <verify>
    <automated>`cd frontend && npm run build` — succeeds. Plus: `grep -F "Try the demo" frontend/src/pages/LoginPage.tsx` matches; `grep -F "Setting up your demo…" frontend/src/pages/LoginPage.tsx` matches (verify the `…` single char, not `...`); `grep -F "Couldn't start the demo. Please try again." frontend/src/pages/LoginPage.tsx` matches; `grep -F "signInAnonymously()" frontend/src/pages/LoginPage.tsx` matches; `grep -F "/api/demo/bootstrap" frontend/src/pages/LoginPage.tsx` matches.</automated>
  </verify>
  <done>LoginPage shows Try-demo CTA + sub-label + divider above email/password form; clicking triggers signInAnonymously → bootstrap → navigate('/'); locked copy strings present verbatim; npm build green.</done>
  <acceptance_criteria>
    - `grep -F "import { apiFetch }" frontend/src/pages/LoginPage.tsx` matches.
    - `grep -F "supabase.auth.signInAnonymously()" frontend/src/pages/LoginPage.tsx` matches.
    - `grep -F "apiFetch('/api/demo/bootstrap'" frontend/src/pages/LoginPage.tsx` matches.
    - `grep -F "Try the demo" frontend/src/pages/LoginPage.tsx` matches at least 2 times (button label + loading-state fallback OR comment + label).
    - `grep -F "Setting up your demo…" frontend/src/pages/LoginPage.tsx` matches (single `…` char).
    - `grep -F "No signup. Your demo session expires after 7 days." frontend/src/pages/LoginPage.tsx` matches.
    - `grep -F "or sign in with email" frontend/src/pages/LoginPage.tsx` matches.
    - `grep -F "Couldn't start the demo. Please try again." frontend/src/pages/LoginPage.tsx` matches.
    - `grep -F "py-3" frontend/src/pages/LoginPage.tsx | grep -F "bg-blue-600"` matches at least once (the new CTA button — UI-SPEC § Spacing mandates py-3 ≥44px touch target).
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ErrorMessageBubble + useChat catch rewrite + ChatContainer wiring + anon empty-state</name>
  <files>frontend/src/components/ErrorMessageBubble.tsx, frontend/src/hooks/useChat.ts, frontend/src/components/ChatContainer.tsx</files>
  <read_first>
    - frontend/src/hooks/useChat.ts (entire file — verify catch block at L188-199 + Message type at L23-28 + return-object shape at L207)
    - frontend/src/components/MessageBubble.tsx (analog: container shape L17-24, props pattern L6-10)
    - frontend/src/components/ChatContainer.tsx (entire file — verify Props interface L20-24, empty-state L32, messages.map L35-37)
    - frontend/src/contexts/ToastContext.tsx (verify useToast signature + 'error' variant + 4s AUTO_DISMISS_MS)
    - frontend/src/lib/sentry.ts (the `Sentry` named export — verify the import path)
    - frontend/src/lib/api.ts (apiStream signature — verify how query params are appended; the retry call must hit `/api/threads/${threadId}/messages?retry=true`)
    - .planning/phases/08-portfolio-polish/08-UI-SPEC.md (§ Surface 2 — Graceful Chat Error, § Copywriting Contract — error bubble + toast + Retry copy, § Color — bg-red-950/40 + border-red-700, § Accessibility — role="alert")
    - .planning/phases/08-portfolio-polish/08-PATTERNS.md (§ErrorMessageBubble, §useChat.ts MOD, §ChatContainer.tsx MOD sections, lines 46-93 + 185-287)
    - .planning/phases/08-portfolio-polish/08-RESEARCH.md (§Common Pitfalls — Pitfall 4 Sentry captureException, lines 759-772)
  </read_first>
  <action>
    Create `frontend/src/components/ErrorMessageBubble.tsx` per PATTERNS.md lines 47-93 + UI-SPEC § Surface 2:
    - Imports: `import { AlertCircle, RotateCw } from 'lucide-react'` only.
    - Props interface: `{ onRetry: () => void; isStreaming: boolean }`.
    - Outer wrapper: `<div className="flex justify-start mb-4">` (mirror MessageBubble L17).
    - Inner container: `role="alert"` + `className="max-w-[85%] md:max-w-[70%] px-4 py-3 rounded-lg bg-red-950/40 border border-red-700 text-gray-100"` (locked classes per UI-SPEC; max-w expansion on mobile per UI-SPEC § Mobile behavior).
    - Two-row content: top row = `<AlertCircle size={16} className="text-red-400 mt-0.5 shrink-0" />` + 8px gap + locked body `"The assistant ran into a problem. Try again, or rephrase your question."` in `<p className="text-sm leading-[1.5]">` (UI-SPEC § Typography line-height 1.5).
    - Bottom row Retry button: `<button type="button" onClick={onRetry} disabled={isStreaming} className="inline-flex items-center gap-1 px-3 py-2.5 md:py-1.5 rounded text-sm font-semibold bg-blue-600 hover:bg-blue-700 text-white disabled:opacity-50 disabled:cursor-not-allowed">` with `<RotateCw size={14} />` icon + label `Retry`. py-2.5 md:py-1.5 = mobile-44px / desktop-32px touch target per UI-SPEC.

    Modify `useChat.ts` per PATTERNS.md lines 185-247:
    - Imports — ADD `import * as Sentry from '@sentry/react'` and `import { useToast } from '../contexts/ToastContext'`.
    - Message type — extend role union to `'user' | 'assistant' | 'error'`.
    - Inside `useChat`: call `const { showToast } = useToast()`.
    - Replace the catch block at L188-199 with the version from PATTERNS.md lines 211-228: keep the `AbortError` early-return; add `Sentry.captureException(err)` BEFORE the message dispatch (RESEARCH Pitfall 4 — Sentry global handlers don't capture caught exceptions); REPLACE (not delete) the empty assistant placeholder with `{ ...m, role: 'error' as const, content: "The assistant ran into a problem. Try again, or rephrase your question." }`; call `showToast("The assistant didn't respond. Tap the message to retry.", 'error')` (verbatim toast copy per UI-SPEC Copywriting Contract).
    - Add `retryLastUserMessage` callback per PATTERNS.md lines 234-241: guard against `isStreaming`; find the most recent user message via `[...messages].reverse().find(m => m.role === 'user')`; bail if none; remove all error messages from state via `setMessages(prev => prev.filter(m => m.role !== 'error'))`; call `sendMessage(lastUser.content, { retry: true })` OR set a flag the existing `sendMessage` reads to append `?retry=true` to the POST URL.
    - Modify `sendMessage` to accept an optional `{ retry?: boolean }` param; when retry is true, append `?retry=true` to the request URL passed to `apiStream` (verify exact URL build site in current useChat — likely a `/api/threads/${threadId}/messages` template literal).
    - Expand the return object to include `retryLastUserMessage` (mirror PATTERNS.md line 246).

    Modify `ChatContainer.tsx` per PATTERNS.md lines 253-287:
    - Import `ErrorMessageBubble` from `./ErrorMessageBubble`.
    - Import `useAuth` from `../contexts/AuthContext`.
    - Add `onRetry: () => void` to Props interface.
    - Inside the component, read `const { isAnon } = useAuth()`.
    - Replace the messages.map with the branching version (PATTERNS.md lines 271-275): if `msg.role === 'error'`, render `<ErrorMessageBubble key={msg.id} onRetry={onRetry} isStreaming={isStreaming} />`; otherwise render the existing `<MessageBubble />`.
    - Replace the empty-state copy at L32 with the conditional from PATTERNS.md lines 260-265: anon users see `"Ask me about the board games in the library, or about the sample D&D 5e quick-reference that's already attached."` (verbatim per UI-SPEC); non-anon keep the existing `"Send a message to start the conversation."`.
    - The parent page (`ChatPage.tsx` or wherever ChatContainer is consumed) must now pass `onRetry={retryLastUserMessage}` from useChat — read ChatPage.tsx first to identify the exact wiring site; if ChatPage already destructures useChat's return, just thread the new field through. This is a small additive prop wiring, not a structural change.

    Do NOT add new color tokens beyond the red + amber set already locked by UI-SPEC. Do NOT install new npm packages (no sonner, no react-hot-toast — UI-SPEC § Toast primitive explicitly forbids). Do NOT modify ToastContext or sentry.ts. Do NOT modify MessageBubble.tsx (the sibling-component path is recommended per UI-SPEC § Open Questions Q1).

    Tool-failure UI: explicitly do NOTHING. D-08 locks silent-continue; the existing per-tool try/except in `chat.py:386-461` already returns `{error: ...}` to the LLM. No frontend code reads or surfaces these in any visible way.
  </action>
  <verify>
    <automated>`cd frontend && npm run build` — TypeScript + Vite production build succeeds. Plus copy + behavior greps: `grep -F "Sentry.captureException" frontend/src/hooks/useChat.ts` matches; `grep -F "The assistant ran into a problem" frontend/src/hooks/useChat.ts` matches; `grep -F "The assistant didn't respond. Tap the message to retry." frontend/src/hooks/useChat.ts` matches; `grep -F "retry=true" frontend/src/hooks/useChat.ts` matches; `grep -F "role: 'error'" frontend/src/hooks/useChat.ts` matches; `grep -F "ErrorMessageBubble" frontend/src/components/ChatContainer.tsx` matches; `grep -F "isAnon" frontend/src/components/ChatContainer.tsx` matches; `grep -F "sample D&D 5e quick-reference" frontend/src/components/ChatContainer.tsx` matches; `grep -F "role=\"alert\"" frontend/src/components/ErrorMessageBubble.tsx` matches; `grep -F "bg-red-950/40" frontend/src/components/ErrorMessageBubble.tsx` matches.</automated>
  </verify>
  <done>ErrorMessageBubble exists with locked red-wash + alert role + Retry button; useChat catch dispatches Sentry + bubble + toast + retry callback; ChatContainer renders ErrorMessageBubble for error-role messages + anon-specific empty state; build green; no new npm deps; all locked copy strings present verbatim.</done>
  <acceptance_criteria>
    - File `frontend/src/components/ErrorMessageBubble.tsx` exists, exports default function, props interface includes `onRetry` and `isStreaming`.
    - `grep -F "role=\"alert\"" frontend/src/components/ErrorMessageBubble.tsx` matches.
    - `grep -F "bg-red-950/40" frontend/src/components/ErrorMessageBubble.tsx` matches.
    - `grep -F "AlertCircle" frontend/src/components/ErrorMessageBubble.tsx` matches.
    - `grep -F "RotateCw" frontend/src/components/ErrorMessageBubble.tsx` matches.
    - `grep -F "import * as Sentry from '@sentry/react'" frontend/src/hooks/useChat.ts` matches.
    - `grep -F "useToast" frontend/src/hooks/useChat.ts` matches.
    - `grep -F "Sentry.captureException(err)" frontend/src/hooks/useChat.ts` matches.
    - `grep -F "'user' | 'assistant' | 'error'" frontend/src/hooks/useChat.ts` matches (or the union with different formatting — verify the `'error'` literal is in the role union).
    - `grep -F "retryLastUserMessage" frontend/src/hooks/useChat.ts` matches at least 2 times (definition + return).
    - `grep -F "retry=true" frontend/src/hooks/useChat.ts` matches.
    - `grep -F "The assistant ran into a problem. Try again, or rephrase your question." frontend/src/hooks/useChat.ts` matches.
    - `grep -F "The assistant didn't respond. Tap the message to retry." frontend/src/hooks/useChat.ts` matches.
    - `grep -F "ErrorMessageBubble" frontend/src/components/ChatContainer.tsx` matches.
    - `grep -F "isAnon" frontend/src/components/ChatContainer.tsx` matches.
    - `grep -F "sample D&D 5e quick-reference" frontend/src/components/ChatContainer.tsx` matches.
    - `grep -F "onRetry" frontend/src/components/ChatContainer.tsx` matches at least 2 times (props + render).
    - `cd frontend && npm run build` exits 0.
    - `git diff frontend/package.json frontend/package-lock.json` is empty (no new npm deps).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Deployed UAT — 12-point PORT-01 + PORT-02 verification on CF Pages</name>
  <what-built>
    Tasks 1-3 landed all frontend code for PORT-01 + PORT-02. Specifically:
    - AuthContext exposes isAnon; DemoPill renders amber pill when isAnon=true.
    - IconSidebar (desktop + drawer) + MobileTopBar render DemoPill.
    - LoginPage has a Try-demo CTA that calls signInAnonymously() + POST /api/demo/bootstrap + navigate('/').
    - useChat catch block dispatches an in-thread red ErrorMessageBubble + 4s error toast + Sentry.captureException + retryLastUserMessage callback.
    - ChatContainer renders ErrorMessageBubble for `role==='error'` messages and shows anon-specific empty state.

    To verify, the developer must deploy the new build to CF Pages (push to main → CF auto-deploy) AND set Fly env var OPENROUTER_API_KEY to an invalid value temporarily (so chat sends provoke a real upstream failure for the error-bubble drill).
  </what-built>
  <how-to-verify>
    Run a 12-point checklist against the deployed CF Pages URL (https://boardgame-rag-prod.pages.dev). Mirror the Phase 06.1 P02 14-point UAT methodology — record PASS/FAIL per item:

    **PORT-01 (Try demo flow):**
    1. Open https://boardgame-rag-prod.pages.dev in a fresh incognito window. The login page shows the "Try the demo" CTA prominently ABOVE the email/password form. Sub-label reads "No signup. Your demo session expires after 7 days." Divider reads "or sign in with email".
    2. Click "Try the demo". Button label changes to "Setting up your demo…" (note the single ellipsis char). After ~1-3s, navigates to `/`.
    3. The chat page loads with one seeded thread titled "Welcome to the demo" in the sidebar (visible on desktop, behind hamburger on mobile). The thread already contains a user message ("What 2-player strategy games...") + an assistant welcome reply.
    4. Navigate to /documents. The D&D 5e quick-reference document is visible in the private documents list with status=indexed (or "processing" briefly, then "indexed").
    5. The "Demo" amber pill is visible in the IconSidebar (desktop, above Sign Out) AND in the MobileTopBar right slot when viewport ≤768px. Hover the desktop pill — tooltip shows "You're using a temporary demo account. Data is cleared after 7 days."
    6. Sign out, sign in with the permanent test user (ragtest1@gmail.com / testpass123). Confirm the "Demo" pill is ABSENT from sidebar + topbar.

    **PORT-02 (graceful error flow — requires forcing a failure):**
    7. Set Fly env to break LLM: `flyctl secrets set OPENROUTER_API_KEY=invalid_for_uat -a boardgame-rag-prod` (record current value first for restore in step 12). Wait ~30s for redeploy.
    8. As the permanent test user (or anon — either works), send any chat message. After a few seconds the assistant bubble should be REPLACED with a red error bubble containing icon + body "The assistant ran into a problem. Try again, or rephrase your question." + a Retry button. Simultaneously a red toast appears bottom-right saying "The assistant didn't respond. Tap the message to retry." Toast disappears after ~4s; the bubble persists.
    9. Open the Sentry dashboard (Phase 7 project). Confirm at least one new event arrived from this chat error (proves `Sentry.captureException` fired — RESEARCH Pitfall 4 mitigated).
    10. While the error bubble is visible, restore the valid OpenRouter key: `flyctl secrets set OPENROUTER_API_KEY=<original_value> -a boardgame-rag-prod`. Wait ~30s for redeploy. Click the Retry button in the error bubble. A fresh SSE stream should start; the error bubble disappears; a normal assistant response streams in. Verify in Supabase SQL editor that the thread has EXACTLY ONE user-message row + ONE final assistant-message row for that turn (no orphan empty assistant row — proves Plan 08-03's retry cleanup worked).
    11. Trigger a tool-level failure to confirm SILENT continue (D-08): e.g. send a chat message that exercises web_search while having an invalid TAVILY_API_KEY temporarily set. Confirm the assistant still completes the turn normally with NO error bubble + NO toast (tool failure is silent per D-08). Restore Tavily key.
    12. Mobile parity check at 375×667 + 414×896 (Chrome DevTools): Try-demo CTA renders correctly + tap-friendly (button ≥44px); error bubble fits 85% viewport; Retry button is ≥44px tall on mobile; Demo pill visible in MobileTopBar right slot.

    Record results in `08-04-VERIFICATION.md` alongside this plan. Any FAIL blocks Wave 3 from starting.

    **Cleanup AFTER all 12 items pass:** ensure all Fly secrets are restored to working values; sign-out any anon session you created during UAT (or let the next bootstrap call purge it via the >7d cleanup, though for fresh anon users you'd need to wait 7 days — easier to clean up manually via Supabase Dashboard → Auth → Users → delete anon entries created in the last hour).
  </how-to-verify>
  <verify>
    <automated>MISSING — manual UAT only; no frontend test framework wired (verified via package.json grep). 12-point checklist outcome recorded in 08-04-VERIFICATION.md gates Wave 3.</automated>
  </verify>
  <done>All 12 checklist items PASS on deployed CF Pages URL. Results recorded in 08-04-VERIFICATION.md. Any Fly secret changes reverted to production-good values. Anon-user clutter from UAT cleaned up.</done>
  <resume-signal>Reply with "12/12 PASS" + path to 08-04-VERIFICATION.md, OR list failing items with reproduction steps and Sentry/LangSmith trace links if applicable.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → backend (anon JWT) | Try-demo CTA mints anon JWTs freely; backend rate-limit + idempotency guard (Plan 08-02) is the trust boundary. |
| Browser → Sentry (caught exception) | Sentry.captureException MUST go through the existing PII-scrub `beforeSend` in lib/sentry.ts; do not bypass. |
| Browser → backend (retry POST) | `?retry=true` is untrusted; backend (Plan 08-03) scopes the delete chain to thread_id + user_id + role='assistant'. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-PII | Information Disclosure | Sentry.captureException(err) in useChat catch block | mitigate | Reuses the existing `lib/sentry.ts` init from Phase 7 — `beforeSend` strips JWTs, email, and user UUIDs from `event.user`. Plan does NOT call `Sentry.setUser(...)` (would leak anon UUID per RESEARCH §Anti-Patterns). |
| T-08-04-XSS | Tampering / Injection | ErrorMessageBubble body copy (locked string) | accept | Locked copy is a static string literal, not user-injected content. No `dangerouslySetInnerHTML`. React auto-escapes. |
| T-08-04-RETRY-DOS | Denial of Service | Retry button | mitigate | Button disabled via `disabled={isStreaming}` while a fresh attempt is mid-stream (UI-SPEC § Retry behavior); existing per-user `/api/chat` rate-limit (Phase 6 SEC-04) still applies; no auto-retry (D-07). |
| T-08-04-ANON-LEAK | Information Disclosure | Demo pill exposes anon-mode status visually | accept | Anon status is non-sensitive; the visible pill prevents reviewer confusion. Tooltip discloses the 7-day cleanup explicitly. |
| T-08-04-COPY | Information Disclosure | Error messages leak provider names or HTTP codes | mitigate | All copy strings audited against UI-SPEC § Copywriting Contract — no provider names, no HTTP codes, no Supabase/OpenRouter mentions. Verified by greps in Tasks 2 + 3. |
</threat_model>

<verification>
- Task 1 + 2 + 3: `cd frontend && npm run build` exits 0 with zero TS errors and zero new npm deps.
- Task 4: 12/12 UAT checklist PASS on deployed CF Pages URL; Sentry receives at least one captured exception during the error-drill; Supabase confirms no duplicate assistant row after retry.
- All locked copy strings from UI-SPEC § Copywriting Contract present in source (verified by greps in each task's acceptance_criteria).
- No new npm dependencies introduced (verified by `git diff frontend/package.json frontend/package-lock.json` being empty).
- Phase 06.1 mobile drawer pattern unchanged (no regression on ≤768px layouts).
</verification>

<success_criteria>
- Try-demo CTA flow works end-to-end against prod: click → anon signin → bootstrap → land in chat with seeded thread + sample doc visible.
- Demo pill appears for anon users in all three render sites (IconSidebar desktop, IconNavRow drawer, MobileTopBar mobile).
- Chat error path renders inline red bubble + 4s toast + fires Sentry event; Retry button works against Plan 08-03's backend contract with no duplicate rows.
- Tool failures remain silent (D-08).
- All locked copy strings verbatim in source.
- Build green, no new deps, Phase 06.1 mobile drawer behavior preserved.
- Phase 8 success criteria #1 (one-click demo) + #2 (graceful error) + #5 (UI-visible Try-demo + error surface) all verified live.
</success_criteria>

<output>
After completion, create `.planning/phases/08-portfolio-polish/08-04-SUMMARY.md` capturing:
- Task 1-3 build output (zero TS errors, npm install deps unchanged).
- Task 4 UAT pass/fail counts + link to 08-04-VERIFICATION.md.
- Sentry event link from the error-drill.
- Confirmation that no Fly secret was left in a broken state post-UAT.
</output>
