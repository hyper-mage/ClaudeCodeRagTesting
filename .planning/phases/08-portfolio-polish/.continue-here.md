---
context: phase
phase: 08-portfolio-polish
plan: 00 (Task 1 pending) + 04 (Task 4 pending) + 05/06/07 not started
task: USER-1 (next action)
total_tasks: 5 user actions remain
status: paused_awaiting_user_steps
resume_mode: interactive_step_by_step
last_updated: 2026-05-18T21:20:42.182Z
---

# RESUME PHASE 8 — Interactive Mode

> **Critical:** User has explicitly requested **one user-action at a time**. Do NOT batch.
> Present USER-1. Wait for their reply. Then USER-2. Etc.
> Each user-action below has full instructions ready to paste verbatim.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Batched user requests | Prior session asked for 3 dashboard outputs at once; user reported only 1 fully (Supabase toggle) and gave wrong artifact for the others (UR status page URL instead of monitor API key). | advisory | Present ONE action at a time with concrete click-path. Wait for confirmation. Do not move to next until current artifact is the correct shape (validated against the expected pattern). |
| Worktree branched from stale tip | A spawned executor branched from Phase 7 tip and missed Plan 08-00 commits; had to port fixtures + caused merge conflict on conftest.py. | advisory | Before spawning any subagent, confirm worktree base equals current master tip. If not, dispatcher merges master into the worktree branch upfront. |
| Path-drift in worktree edits | Plan 08-04 executor wrote files to main repo instead of worktree on first batch; recovered manually. | advisory | When spawning executors in worktrees, instruct them to verify `pwd` matches the worktree path before every Edit/Write. |

<current_state>
Phase 8 (Portfolio Polish, milestone v1.1) is 5-of-8 plans code-complete:
- Wave 0 Plan 08-00: Tasks 2+3 done; Task 1 (JWT decode) PENDING USER
- Wave 0 Plan 08-07: not started; needs USER for UR key + GitHub public flip
- Wave 1 Plans 08-01, 08-02, 08-03: all merged to master, tests green
- Wave 2 Plan 08-04: code merged (Tasks 1-3); Task 4 (12-point UAT) PENDING USER deploy + browser test
- Wave 3 Plan 08-06: not started; ALL tasks need USER (Excalidraw + screenshots + GIF)
- Wave 4 Plan 08-05: not started; blocked on 08-06 assets + 08-07 badges

Working tree is clean. 11 new commits landed this session on master (no branches/PRs).

Verification:
- backend pytest: 139 passed (2 pre-existing test_record_manager fixture errors documented in deferred-items.md, NOT regression)
- frontend `npm run build`: green, 0 TS errors
- frontend `npm run lint`: 4 pre-existing errors documented in deferred-items.md, NOT regression
</current_state>

<completed_work>

Commits landed this session (chronological):
- `22463f0` feat(08-00): D&D 5e quickref + CREDITS attribution (Task 2)
- `c89c4a2` test(08-00): conftest fixtures + 4 test stubs (Task 3)
- `512f181` chore(08): redact owner email across .planning/ + HANDOVER for public repo
- `43be596` docs(08-00): provisional SUMMARY — Tasks 2+3 done, Task 1 PENDING
- `cb3341b` test(08-01): 4 anon+permanent JWT acceptance tests
- `60c234b` docs(08-01): complete backend auth JWT widening plan
- `633a407` merge(08-01): land backend auth JWT test coverage (Wave 1)
- `7715bb4` feat(08-02): demo_service seed + purge + cascade-delete (Task 1)
- `0f81e49` feat(08-02): /api/demo/bootstrap router + main.py registration (Task 2)
- `34b854b` docs(08-02): complete demo-bootstrap plan (SUMMARY)
- `8662224` merge(08-02): /api/demo/bootstrap router + service + cleanup (Wave 1)
- `1e63e95` test(08-03): RED phase — failing retry-dedup tests
- `b338632` feat(08-03): retry-aware POST /threads/{id}/messages dedupes orphan assistant row
- `b3d465d` docs(08-03): complete backend chat retry dedup plan
- `e8c51b4` merge(08-03): retry-aware POST /messages dedupes orphan assistant row (Wave 1)
- `31fcf2b` chore(08): STATE.md reflects Wave 1 merged; Wave 2 blocked on Wave 0 user steps
- `a3fbda1` feat(08-04): AuthContext.isAnon + DemoPill + sidebar/topbar wiring (Task 1)
- `eb768c7` feat(08-04): LoginPage Try-demo CTA (Task 2)
- `94a078c` feat(08-04): ErrorMessageBubble + useChat error handling + retry (Task 3)
- `f2c486b` docs(08-04): Plan 08-04 code-complete SUMMARY (Tasks 1-3); Task 4 UAT deferred
- `ddd3e4f` merge(08-04): frontend Try-demo CTA + DemoPill + error/retry UI (Wave 2)
</completed_work>

<remaining_work>

5 USER actions to clear remaining work. Present ONE AT A TIME, in this order:
1. USER-1 — Decode anon JWT in prod (unblocks Plan 08-00 Task 1 confirmation)
2. USER-2 — UptimeRobot monitor API key (unblocks Plan 08-07 Task 1)
3. USER-3 — Flip GitHub repo to public + report owner/repo (unblocks Plan 08-07 Task 2)
4. USER-4 — Deploy backend + frontend + run 12-point UAT (unblocks Plan 08-04 Task 4)
5. USER-5 — Excalidraw diagram + 4 screenshots + hero GIF (unblocks Plan 08-06; then I write Plan 08-05 README)

After USER-5, I write Plan 08-05 README (autonomous — references the assets you produced) and Plan 08-07 SUMMARY (badge URLs from USER-2 + USER-3). Phase 8 closes with `/gsd:execute-phase 8 --gaps-only` for any verification gaps.
</remaining_work>

<decisions_made>

- **Plan 08-01 took no-op auth.py path (provisional).** Default Supabase anon JWT carries `aud="authenticated"`; if USER-1 reveals different value, fix is a one-line patch to `backend/auth.py:42` (widen to `["authenticated", "<reported>"]`) + update of `_ANON_AUD_CLAIM` constant in `backend/tests/conftest.py`. The 4 tests will keep passing (jwt.decode is mocked).
- **Plan 08-02 permanent-user guard returns `{seeded:false}` silently (not 403).** Minimum friction for prod debugging. Rationale in 08-02-SUMMARY.md.
- **Plan 08-03 uses SELECT-then-DELETE-by-id rather than chained `.delete().order().limit()`.** supabase-py 2.13.0 postgrest filter builder does not support order/limit on delete chain (verified empirically). Two queries, both indexed by primary key. Rationale in 08-03-SUMMARY.md.
- **Email `<your-email>` (was `mlynn808138@gmail.com`) redacted across .planning/ + HANDOVER.md.** Repo going public for shields.io last-commit badge; reduces scrape surface at HEAD. Git history still contains email — acceptable trade-off for portfolio repo. Committed as `512f181`.
- **Wave 0 + Wave 1 split execution.** User explicitly opted to advance the code parts (Wave 1 backend + Wave 2 frontend) while doing manual dashboard steps in parallel. This is why 08-01 + 08-04 landed with provisional/deferred markers instead of full SUMMARY completion.
</decisions_made>

<blockers>
All blockers are user-action items below. No technical blockers.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current phase position + completed plan counts
2. `.planning/HANDOFF.json` — structured equivalent of this file
3. `.planning/phases/08-portfolio-polish/08-00-SUMMARY.md` — provisional summary; Task 1 still PENDING
4. `.planning/phases/08-portfolio-polish/08-01-SUMMARY.md` — provisional no-op path; revisit-if-aud-differs note
5. `.planning/phases/08-portfolio-polish/08-04-SUMMARY.md` — code-complete; Task 4 UAT deferred
6. `.planning/phases/08-portfolio-polish/08-07-PLAN.md` — full details of badge tasks user is about to do
7. `.planning/phases/08-portfolio-polish/08-06-PLAN.md` — full details of asset capture tasks user is about to do
8. `.planning/phases/08-portfolio-polish/08-04-PLAN.md` lines 334-374 — 12-point UAT checklist to walk user through

## Infrastructure State

- **Supabase anon sign-ins:** ✅ ENABLED in prod (user confirmed)
- **UptimeRobot:** Public Stats slug `https://stats.uptimerobot.com/mpLu1UQaFy` exists; monitor-specific API key NOT yet obtained
- **GitHub repo:** PRIVATE; user will flip to public after USER-2 confirms badge approach
- **Fly backend:** STALE — does NOT contain Wave 1 changes (08-01/02/03). Needs `cd backend && flyctl deploy` before USER-4.
- **CF Pages frontend:** STALE — does NOT contain Wave 2 changes. Needs push to main branch for auto-deploy before USER-4.
- **Worktrees:** 4 locked Phase 8 worktrees in `.claude/worktrees/`; harness cleans automatically. Do NOT attempt manual removal.

---

# USER ACTIONS — Present one at a time

## USER-1: Decode anon JWT in prod Supabase

**Why:** Plan 08-00 Task 1 — confirms `aud` claim value. Currently provisional `aud="authenticated"` (Supabase default). If different, Plan 08-01 needs `backend/auth.py:42` widening + redeploy.

**Instructions (paste verbatim to user):**

> Open https://boardgame-rag-prod.pages.dev in a fresh incognito window.
> F12 → Console tab. Paste:
> ```js
> const { data } = await supabase.auth.signInAnonymously()
> console.log(JSON.parse(atob(data.session.access_token.split('.')[1])))
> ```
> If `supabase` is undefined in the console (Vite tree-shakes it), instead:
> 1. Click the "Try the demo" CTA on the login page (once deploy lands; if not yet deployed, use the manual mint).
> 2. Open Application tab → Local Storage → `https://boardgame-rag-prod.pages.dev`.
> 3. Find the `sb-<project-ref>-auth-token` key. Copy its `access_token` field.
> 4. Paste the access_token at https://jwt.io and report the `aud` + `role` claim values from the decoded payload.

**Expected output:** JSON payload, specifically `aud` and `role` claim values.

**On reply:**
- If `aud == "authenticated"`: upgrade `08-00-SUMMARY.md` to `status: complete, provisional: false`. No code change needed. Commit `docs(08-00): confirm aud="authenticated" — provisional path locked`.
- If `aud != "authenticated"`: patch `_ANON_AUD_CLAIM` in `backend/tests/conftest.py` + widen `audience=["authenticated", "<reported>"]` in `backend/auth.py:42` + add citation comment + re-run `pytest tests/test_auth_anon.py -x`. Commit `fix(08-01): widen JWT audience per empirical aud='<reported>'`.

## USER-2: UptimeRobot Monitor-Specific API key

**Why:** Plan 08-07 Task 1 — build shields.io uptime-ratio badge URL.

**Instructions (paste verbatim to user):**

> UptimeRobot dashboard → top-right avatar → **My Settings** → **API Settings** tab.
> Scroll to **"Monitor-Specific API Keys"** section.
> Find the row for the `/api/health` monitor (the Fly one, created in Phase 7 Plan 05).
> Click "Create" if no key exists yet, or copy existing key.
> Key format: `m<digits>-<alphanumeric>` (e.g. `m803088267-abc123def456ghi789`).
>
> Note: this is DIFFERENT from the status page slug (`mpLu1UQaFy`) you provided last session — that's the public status page, not the monitor API key. shields.io endpoint needs the monitor key specifically.

**Expected output:** key string starting with `m` followed by digits-dash-alphanumeric.

**On reply:**
1. Build URL: `https://img.shields.io/uptimerobot/ratio/7/<MONITOR_KEY>?label=uptime&style=flat`
2. Fetch URL in a curl (or paste in browser) — must return valid SVG showing percentage.
3. Record URL in `08-07-SUMMARY.md` (created on demand).

## USER-3: Flip GitHub repo to public + report owner/repo

**Why:** Plan 08-07 Task 2 — shields.io `last-commit` badge requires public repo. Email already redacted (`512f181`).

**Instructions (paste verbatim to user):**

> 1. github.com → repo → Settings → General → scroll to bottom (Danger Zone) → **Change visibility** → **Make public** → confirm.
> 2. After flip, paste back the `owner/repo` path (e.g. `hyper-mage/claude-code-agentic-rag-masterclass`).

**Expected output:** owner/repo string + confirmation.

**On reply:**
1. Build URL: `https://img.shields.io/github/last-commit/<owner>/<repo>?style=flat`
2. Test URL in browser — must return valid SVG.
3. Update `08-07-SUMMARY.md` with both badge URLs. Mark Plan 08-07 complete. Commit `docs(08-07): UR + GitHub last-commit shields.io badge URLs`.

## USER-4: Deploy + 12-point UAT

**Why:** Plan 08-04 Task 4 — gates Wave 3 (asset capture) and validates PORT-01 + PORT-02 end-to-end against live prod.

**Instructions (paste verbatim to user):**

> ### Step 1: Deploy backend
> ```powershell
> cd backend
> flyctl deploy -a boardgame-rag-prod
> ```
> Wait for deploy success. Verify with `flyctl status -a boardgame-rag-prod`.
>
> ### Step 2: Deploy frontend
> ```powershell
> git push origin master
> ```
> Cloudflare Pages auto-builds from main. Watch CF dashboard until "Production deployment" shows green.
>
> ### Step 3: Run 12-point UAT
> Open https://boardgame-rag-prod.pages.dev in a fresh incognito window. Walk the 12-point checklist from `.planning/phases/08-portfolio-polish/08-04-PLAN.md` lines 347-364. For PORT-02 items 7-11 you will temporarily break `OPENROUTER_API_KEY` via `flyctl secrets set OPENROUTER_API_KEY=invalid_for_uat -a boardgame-rag-prod` and restore after item 10.
>
> Record PASS/FAIL per item.

**Expected output:** "12/12 PASS" OR list of failing items with reproduction steps.

**On reply:**
- If 12/12 PASS: write `.planning/phases/08-portfolio-polish/08-04-VERIFICATION.md` capturing results + Sentry event link from item 9. Update 08-04-SUMMARY.md `status: complete`. Commit `docs(08-04): 12/12 UAT PASS on deployed CF Pages`.
- If any FAIL: triage with user; create gap-closure plan if needed; do NOT proceed to USER-5 until UAT passes (Wave 3 hard-gates on this per PLAN.md).

## USER-5: Excalidraw + 4 screenshots + hero GIF

**Why:** Plan 08-06 — produces the assets Plan 08-05 README embeds.

**Sub-tasks (present in this order):**

### USER-5a: Excalidraw architecture diagram
> Open https://excalidraw.com (free, no signup). Author the diagram per `.planning/phases/08-portfolio-polish/08-06-PLAN.md` Task 1 §what-built — nodes + edges list provided there.
> Nodes: Browser (React SPA), Cloudflare Pages, Fly.io (FastAPI + Docling), Supabase (4 sub-nodes: Postgres+pgvector, Auth, Storage, Realtime), OpenRouter, Docling, Sentry, LangSmith, UptimeRobot.
> Edges (with labels): Browser→Cloudflare Pages `HTTPS (static)`, Browser→Fly.io `HTTPS + SSE (/api/*)`, Browser→Supabase Auth `JWT auth`, Browser→Supabase Realtime `WS (doc status)`, Fly→Supabase Postgres `service-role queries`, Fly→Supabase Storage `signed URLs`, Fly→OpenRouter `LLM completions`, Fly→LangSmith `trace capture`, UptimeRobot→Fly `health probe (5min)`, Browser→Sentry `client errors`.
>
> Export:
> - Top menu → Export image → SVG or PNG → uncheck "Embed scene" → save as `docs/architecture.png`.
> - File → Save to file → save as `docs/architecture.excalidraw` (the JSON source — checked into git for future edits).

### USER-5b: 4 screenshots
> Capture against the deployed app (https://boardgame-rag-prod.pages.dev) in incognito + with a fresh anon-demo session (no real user PII):
> 1. `docs/screenshots/01-login-try-demo.png` — login page with Try-demo CTA prominent
> 2. `docs/screenshots/02-chat-tool-calls.png` — chat with at least one tool-call card EXPANDED
> 3. `docs/screenshots/03-documents-upload.png` — documents page with an upload in progress
> 4. `docs/screenshots/04-mobile-drawer.png` — mobile drawer open over chat (375×667 viewport — Chrome DevTools device mode)
>
> Crop tight; 16:9 or 4:3 aspect; PNG; ≤500 KB each. Tools: built-in Windows Snipping Tool (`Win+Shift+S`) or ShareX or Greenshot.

### USER-5c: Hero GIF
> `docs/hero.gif`, ≤5 MB, ~15-20s. Shows: login click Try-demo → chat page loads → send a question → tool-call cards stream in → assistant streams answer.
>
> Recommended tools (Windows):
> - ScreenToGif (free, https://www.screentogif.com/) — record screen region as GIF directly
> - OR record via OBS as MP4, convert via `ffmpeg -i input.mp4 -vf "fps=15,scale=800:-1:flags=lanczos" -loop 0 docs/hero.gif`
>
> Optimize: target ~3-5 MB. If over 5 MB, drop fps to 10 or scale down.

**On reply (after each sub-task lands):**
- Verify file exists + size limit. Commit per sub-task:
  - `feat(08-06): architecture diagram (Excalidraw source + PNG export)`
  - `feat(08-06): 4 portfolio screenshots`
  - `feat(08-06): hero GIF`
- After all 3 sub-tasks: write `08-06-SUMMARY.md`. Commit `docs(08-06): complete asset capture plan`.

## After USER-5: I land Plan 08-05 README autonomously

Plan 08-05 is autonomous from my side once the assets exist:
- Move existing course README to `docs/MASTERCLASS.md`
- Write portfolio README at repo root: title + live demo link + badges row (from 08-07) + hero GIF + tech tables + architecture diagram embed + screenshots + deploy command sequence + link back to MASTERCLASS.md
- Commit + 08-05-SUMMARY.md

Then verify Phase 8 closure: `/gsd:execute-phase 8 --gaps-only` if any verification gaps remain, otherwise `/gsd:verify-work 8` to mark complete.

<context>

Approach this resume session like pair-programming, not autonomy. The user is doing real-world dashboard work and wants to be guided through each step with full instructions to paste verbatim. Do NOT use AskUserQuestion to batch — use plain text presentation with the click-paths quoted, ask for the artifact, validate the artifact has the right shape (e.g. UR key starts with `m` and has the `m<digits>-` format; owner/repo has a slash) before proceeding.

Caveman mode is active for this session. Keep responses terse. Code/commits stay normal.
</context>

<next_action>
Read this entire file. Confirm to the user: "Resuming Phase 8. 5 plans done, 5 user-actions remain. Starting with USER-1: decode anon JWT in prod Supabase. Instructions next."

Then present USER-1 verbatim from the section above.

After user reports `aud` value, execute the on-reply logic for USER-1 (either lock the provisional path OR patch auth.py + conftest).

Move to USER-2 only after USER-1 is fully resolved.
</next_action>
